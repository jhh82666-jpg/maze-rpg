# 战斗 AP / 卡牌栏 UI·UX 规格书

> 版本 v1.0 · 文策渊 · 供程基岩实现用
> 逻辑分辨率 1920×1080（GAME_WIDTH / GAME_HEIGHT 变量，所有坐标按比例换算，禁止写死像素常量于逻辑中）。
> 风格目标：明日方舟式——底部干员卡牌 + 顶部信息带，扁平深色面板 + 高亮色描边。

---

## 1. 战斗画面布局规格

### 1.1 区域划分（自上而下）

```
┌──────────────────────────────────────────────────────────────┐
│ A 顶部信息带 (y 0..56)                                        │
│   左: 第 N 回合 / combo 数      右: 攻击·生命·连击/暴击/强化   │
├──────────────────────────────────────────────────────────────┤
│ B 战场区 (y 56..700)                                          │
│   敌方单位一排 (y≈196) · 左侧竖向队伍 HP/MP 池条 (x 24/50)    │
│   我方不再画方块小人，由底部卡牌代表（见决策 D1）              │
├──────────────────────────────────────────────────────────────┤
│ C 底部指令台 (y 700..1064, 整块深色面板, 上边线高亮)           │
│  ┌─────────┬──────────────────────────────┬───────────────┐  │
│  │ C1 日志 │ C2 角色卡牌 ×6 (核心交互区)   │ C3 AP + 指令  │  │
│  │ +概率   │                              │ +回合         │  │
│  └─────────┴──────────────────────────────┴───────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

| 区域 | 位置（1920 基准） | 内容 |
|---|---|---|
| A 顶部信息带 | 通栏 y 0..56 | 左：回合数/combo；右：队伍攻击、最大生命、连击/暴击/强化概率（即现有 `probText` 上移）、自动战斗标记 |
| B 战场区 | y 56..700 | 敌人单位（保持现有圆形占位+血条+名字）；左侧共享 HP/MP 竖条保留 |
| C1 日志区 | x 24..420, y 716..900 | 最近 4 行日志（字号 13，现 10 行太多）；下方一行连击/暴击/强化小字摘要 |
| C2 卡牌区 | x 436..1484, y 700..1064 | 6 个卡牌栏位，单卡 168×340，间距 12（6×168+5×12=1068，居中） |
| C3 指令区 | x 1500..1896, y 700..1064 | 见 §1.4 |

### 1.2 角色卡牌信息层级（单卡 168×340，自上而下）

1. **半身像区**（168×220，占 65%）：bust 立绘居中裁剪；未行动/已行动/倒下三态（倒下：灰度+暗角+「倒下」斜标签）。
2. **当前行动者指示**：卡牌整体描边 3px 高亮色 + 顶部「行动中」旗标 + 卡牌向上浮起 8px（tween 200ms）。**非行动角色的卡牌整体降至 70% 透明度且技能图标不可点**。
3. **名字行**（高 26）：名字 + Lv，居中，13px bold。
4. **技能图标行**（高 68，底部）：每技能一个 44×44 图标格，横排最多 4 个（超出时取前 4，技能数上限由设计控制在 4 个主动技以内）。
   - 图标角标：**右下角** MP 消耗数字（蓝）；**左上角** AP 消耗（默认 1 枚小菱形）。
   - **冷却中**：图标置灰 + 中央叠加白色数字「剩余回合数」。
   - **MP 不足**：图标蓝色调暗 + 角标 MP 数字变红。
   - **AP 不足**：图标整体灰显 + 左上角菱形变红叉。
   - 可用：图标亮色描边，hover 放大 1.06× + 弹出技能名/效果 tooltip（150 字内）。

### 1.3 AP 显示方式（C3 顶部）

- **菱形 pips**：10 个槽位，每枚 20×20 旋转 45° 菱形，两排排列（5+5）以容纳科技扩容后的 10 点。
  - 已填充：高亮青（Palette.accent）；空槽：暗色描边；**未解锁的第 9/10 槽**：锁形图标 + 虚线描边（未研究 tech_ap_cap 时）。
- pips 右侧大号数字 `3 / 8`。
- 回复表现：回合开始时获得 AP 的 pips 逐个亮起（间隔 80ms）+ 浮动文字「+2 AP」（科技生效时追加「+1 奇袭节拍」金色字）。

### 1.4 通用指令（C3 中下，AP 显示之下）

竖排 4 个按钮（宽 180，高 44，间距 10），**均不耗 AP、不占用行动**（除逃跑见下）：

| 按钮 | 行为 | 备注 |
|---|---|---|
| 防御 | 当前行动者进入防御，立即结束其行动 | 消耗该角色的行动机会，但不耗 AP/MP |
| 道具 | 弹出道具面板（沿用现有 item menu 交互） | 使用后**不结束行动**，可继续放技能（沿用现有规则） |
| 逃跑 | 立即尝试逃跑 | 有二次确认；BOSS 战禁用（若有该标记） |
| 自动战斗 | 开关切换，开启时按钮常亮 | 自动 AI 见 §3.4 |

按钮下方：回合数小字「第 N 回合 / 上限 X」。

### 1.5 日志与概率安置

- 完整概率明细固定在 A 区右侧（一行）；C1 只放滚动日志。
- 日志不再与指令区抢空间（现状是指令按钮挤在右下角、日志横向被压缩到 wordWrap GAME_WIDTH-420）。

---

## 2. 交互流程

### 2.1 释放技能完整链路

```
[我方角色进入行动]
  → 其卡牌高亮浮起，其余卡牌降透明
  → 玩家点击该卡上的技能图标
    ├─ target = self / ally_all / enemy_all → 直接结算（播放动画）
    └─ target = enemy(单体) → 进入选敌模式:
         · 底部浮出提示条「选择目标 · 右键/Esc 取消」
         · 存活敌人身上出现可点击热区 + 呼吸描边
         · 点击敌人 → 结算;  右键/Esc/点击空白 → 返回指令态（不耗 AP）
[结算] → 扣 AP 1 点（pips 熄灭动画）→ 扣 MP → 进冷却 → 行动结束，下一人
```

- 一次行动**只能放 1 个技能**（放完即换人），AP 是全局池，限制「同一回合多人连放」而非「一人连放」。
- 选敌模式保留「自动选敌」（选血量最少者）按钮在提示条右侧。

### 2.2 禁用态与提示文案（统一规范）

点击不可用技能图标时：图标抖动 120ms + 底部提示条/toast 显示原因（取第一个命中的原因）：

| 原因（判定顺序） | 文案 |
|---|---|
| 1. 非当前行动者 | 「现在轮到 {行动者名} 行动」 |
| 2. 冷却中 | 「{技能名} 冷却中，还需 {n} 回合」 |
| 3. MP 不足 | 「MP 不足：需要 {cost}，当前 {mp}」 |
| 4. AP 不足 | 「行动力不足：需要 {apCost} 点 AP，当前 {ap} 点」 |

图标上的视觉态（灰显/红字/冷却数字）必须**常驻可见**，不依赖点击——玩家扫一眼就知道哪个能点（对应用户需求 5）。

### 2.3 回合开始 AP 回复表现

1. C3 区 pips 逐个点亮 + 「+2 AP」浮字（§1.3）。
2. 日志追加一行「第 N 回合：行动力回复 2 点（当前 X/8）」。
3. 若回复被上限截断（溢出），浮字改为「+2 AP（已达上限）」，日志同述。

---

## 3. AP 数值规则精确定义

### 3.1 规则表

| 项 | 值 | 说明 |
|---|---|---|
| 初始 AP | **3** | 每场战斗开始时（第 1 回合）即为 3 |
| 每回合回复 | **+2** | 第 2 回合起，在**回合开始时**回复（实现落点：`CombatSystem.endRound()` 内 `state.round += 1` 之后，与现有「—— 第 N 回合 ——」日志同位置） |
| 基础上限 | **8** | 溢出直接截断，不结转、不存储 |
| 单次消耗 | 技能 `apCost` 字段，**默认 1** | 防御/道具/逃跑/自动 = 0 |
| 归属 | **队伍共享池**（不是每角色各一份） | 存于 `BattleState.ap / apCap`，与 HP/MP 池的设计语言一致 |

- **回复时机定为「回合开始」**而非回合结束：避免首轮就多 2 点破坏开局节奏，也与冷却/增益递减（endRound 内）同一结算点，实现简单。
- 敌人不消耗 AP（AP 是玩家资源，与兰斯 10 一致）。
- 撤退/超时/战斗结束不清算 AP。

### 3.2 数据结构改动（给程基岩的接口约定）

```ts
// BattleState 增加:
ap: number;        // 当前行动力
apCap: number;     // 上限（读科技后 = 8 或 10）
apRegen: number;   // 每回合回复基础值 = 2
apOddBonus: number;// 科技生效时 = 1，否则 0

// Skill 增加（配表 skills.json 同步加字段，缺省视为 1）:
apCost?: number;   // 默认 1
```

回复公式（回合开始时，round ≥ 2）：
```
gain = apRegen + (apOddBonus > 0 && round % 2 === 1 ? apOddBonus : 0)
ap = min(apCap, ap + gain)
```

### 3.3 新科技定义（追加到 tech.json）

```json
"tech_ap_cap": {
  "id": "tech_ap_cap",
  "name": "战术扩容",
  "description": "行动力上限由 8 提升至 10。",
  "cost": 10,
  "prerequisites": [],
  "order": 35,
  "effects": [{ "type": "ap_cap_bonus", "value": 2 }]
},
"tech_ap_odd": {
  "id": "tech_ap_odd",
  "name": "奇袭节拍",
  "description": "奇数回合开始时额外获得 1 点行动力。",
  "cost": 14,
  "prerequisites": ["tech_ap_cap"],
  "order": 45,
  "effects": [{ "type": "ap_regen_odd", "value": 1 }]
}
```

定价依据：现有功能型科技「队伍扩编 I」5 点、属性科技 6-7 点、「远征装备」16 点。AP 上限直接抬升每场战斗的爆发天花板，定 10 点（≈扩编 II 的 12 点档位）；奇数回合+1 全局期望约 +0.5 AP/回合、与上限扩容有协同，定为进阶节点 14 点并挂 tech_ap_cap 前置，形成一条「AP 支线」。**前置链是否必要列为决策 D3。**

科技效果落地：参照 `techStatBonuses()` 的模式新增 `techApBonuses()`，在 `CombatSystem` 构造时读取写入 `apCap / apOddBonus`。

### 3.4 自动战斗 AI 调整

取消普攻后，`chooseAllyAutoAction` 的兜底 `kind: 'attack'` 改为：
1. 优先放可用技能（现有逻辑不变，但需先过滤 AP：把 `availableSkills` 增加 `s.apCost ?? 1 <= state.ap` 条件）；
2. 无可用技能（AP/MP/冷却全锁）→ **防御**（不再普攻）。
`availableSkills` 过滤条件对敌人侧不生效（敌人不看 AP）。

### 3.5 平衡红线（策划自审）

- **主导策略检查**：全部技能 1 AP 时，「每回合必放最高倍率技」可能成为唯一解——靠 MP 池与冷却双重约束对冲；apCost 字段留给后续把高倍率技调到 2-3 AP，配表阶段必须预留该调参空间。
- 开局 3 AP + 每回合 2：6 人满编时一回合理论最多行动 6 次，AP 天然限流到 2-3 次技能，节奏与现 MP 约束接近，不会造成战力跳变。
- 认知过载：卡牌图标同时承担「可用性四态（AP/MP/CD/非行动）」，必须用常驻视觉态表达（§2.2），禁止只做 toast。

---

## 4. 营地（CampScene）UX 问题清单（按优先级）

1. **主操作无层级**：中栏 10 个 420×54 按钮等大等距，「出发探索」排第 9 位被淹没。→ 把「出发探索」拆出按钮列，做成右下角独立主按钮（放大至 420×72 + 高亮描边 + ▶ 图标），其余 8 个改 2 列网格小按钮。
2. **资源可见性差**：金币/科技点只是标题下一行 14px 灰字。→ 顶部改为常驻资源栏（图标 + 数字的胶囊形 chip，右对齐），数值变化时跳动 + 变色反馈（参照明日方舟顶栏）。
3. **备战栏不可交互**：右栏超 8 人折叠成「……另有 N 人」死文字。→ 改用 ScrollList 全量展示，每行右侧加「编入」快捷按钮（队伍未满时），减少「编成面板来回切」的路径。
4. **招募未解锁态误导**：按钮常亮可点，点了才弹「尚未解锁」。→ 按钮置灰 + 🔒 前缀 + 标签直接写「招募队友（需科技：招募许可 I）」。
5. **左栏纯文本堆砌**：出战成员行是 11px 六维字符串，无头像。→ 成员行左端加 40×40 立绘头像（复用 HUD 的 portraitKey 方案），六维缩为「合计 N」一行。
6. **「设置」定位混乱**：营地设置弹层只有方向键透明度等 4 项，与主菜单设置割裂。→ 营地移除设置按钮，只保留主菜单设置；或改为「快捷设置」明确标注（推荐前者）。
7. **就绪提示埋在信息栏**：「警告：没有出战角色」在 y=520 的 13px 文字。→ 无出战角色时「出发探索」按钮置灰 + 按钮上方常驻红色警示行。
8. **键盘导航缺失**：全场景无 Esc/快捷键约定（HUD 有「菜单（Esc）」而营地没有）。→ 统一：Esc 返回大地图，数字键 1-9 映射功能按钮。

## 5. 图鉴（CodexPanel）UX 问题清单（按优先级）

1. **详情栏太窄**：detailW 上限 320px，大立绘只有 ~200px 高，与「图鉴看立绘」的核心诉求矛盾。→ 详情栏加宽至面板宽 32%（约 470px），立绘高度放到 320px。
2. **简介硬截断**：详情简介固定占位 70px，长文本溢出被遮罩吃掉且无省略号提示。→ 改为按行数实测高度，超出时显示「…」+ 可滚动已在，但需加渐变淡出提示可滚。
3. **未解锁泄露策略不一致**：名字显示「？？？」却照常显示职业名与稀有度星级。→ 决策 D2：要么全遮（职业也「？？？」），要么明确「预告制」显示职业+星级作为收集引导（**推荐预告制**，但把简介、六维、具体技能继续锁定）。
4. **无排序能力**：只能按模板 id 浏览。→ 筛选行右侧加排序下拉（稀有度 / 职业 / 等级），默认稀有度降序。
5. **六维可读性差**：`筋力 12(A)` 纯文本 2 列，无横向对比感。→ 改为迷你条形（属性值 0-99 映射条宽 + 潜力字母着色），已解锁/未解锁样式统一骨架。
6. **行信息过载**：列表行三行文本（名字称号 / 职业等级 / 状态+简介首句）在 104px 行高内过挤。→ 砍掉简介首句（详情栏已有），状态标签改右上角彩色徽章（已解锁=绿 / 未解锁=灰）。
7. **收集进度无可视化**：仅「已解锁 N / 15」文字。→ 标题旁加一条细进度条 + 百分比。
8. **未解锁行的引导文案误导**：「点击可在右侧查看职业定位」，但右侧只显示技能池没有定位图。→ 未解锁详情页补一块「职业定位说明」（从 classes.json 的 role 字段渲染）。

## 6. 未解锁剪影新表现方案（科技感，3 选 1）

- **方案 A「信号未接入」**：立绘区域为深色全息底板，人形剪影由扫描线（横向 2px 亮线匀速下移）与噪点构成，四角加科技感取景框 bracket，中央悬浮「SIGNAL LOST / ？？？」小字。剪影轮廓用实际立绘的 alpha 通道填深色，保留体型辨识度。
- **方案 B「蓝图线框」**：藏青底 + 立绘轮廓以 1px 青色线框勾勒（从 alpha 通道提边），叠加网格底纹与标注引线（如「CLASS: ???」），像未解锁的战术档案。
- **方案 C「故障闪烁」**：剪影做 glitch 效果（随机水平切片错位 + RGB 色散 2px），每隔 2-3 秒闪一下完整立绘的 10% 残影，营造「数据库损坏待修复」感。

**推荐方案 A**：实现成本最低（Graphics 扫描线 tween + 现有 alpha 剪影即可，不需要提边/切片shader），与明日方舟式「终端/档案」气质最贴，且动效克制不抢戏；B 可作为后续高稀有度（5★）未解锁的差异化皮肤。

---

## 7. 待用户拍板决策项

| # | 问题 | 选项 | 建议 |
|---|---|---|---|
| D1 | 战场上我方方块小人是否随卡牌栏上线而移除？ | 移除（卡牌即代表）/ 保留（战场仍有单位感） | 移除，战场只留敌人+共享池条，避免双份表达 |
| D2 | 未解锁图鉴是否显示职业与稀有度？ | 预告制（显示）/ 全遮蔽 | 预告制 |
| D3 | tech_ap_odd 是否必须前置 tech_ap_cap？ | 串联 / 两科技独立 | 串联（引导 AP 支线），独立亦可接受 |
| D4 | 逃跑在 BOSS 战是否禁用？（现有代码无 BOSS 标记） | 禁用需加配置 / 保持全可逃 | 暂保持全可逃，BOSS 标记后续单独立项 |
| D5 | 道具使用后是否仍不结束行动？ | 保持现状（不结束）/ 改为结束行动 | 保持现状（现有规则，改动会牵扯平衡） |
