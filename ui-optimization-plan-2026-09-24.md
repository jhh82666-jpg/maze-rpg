# 迷宫 RPG · UI 系统性优化方案（落地索引与裁决记录）

> **本文件的地位**：用户提供的《迷宫 RPG · UI 系统性优化方案（基于 handbook §50）》原始文件是 **7 页纯图片 PDF（无文本层）**。
> 本文件是该方案的**保真转录 + 落地索引 + 裁决记录**，供工程线据以实现、供后人回溯。
> 依据：`docs/handbook.md` 第 50 章（美术 · UI/UX）与 GDD 裁决，逻辑分辨率 **1920×1080** 基准，各界面以 **2/3 比例（1280×720）** 呈现。
> 方案原图（含各界面设计稿）保存在 `_ui-plan/page-1..7.png`。
> ⚠️ **本文件是过程性工作文档**，UI 优化收尾后应并入 `docs/handbook.md` §50 并归档。

---

## 1. 优化目标（方案原图第 1 页）

| # | 目标 | 内容 |
|---|---|---|
| ① | **视觉统一** | 全部界面对齐 §50.1 色板与切角语言：平色、**零渐变零发光**；清除圆角与旧蓝调残留，稀有度 / 语义色各就其位 |
| ② | **布局适配** | 1920×1080 基准锚点体系；战斗指令台锚底、迷宫顶部信息带通栏；面板切角分档（**14 / 10 / 8 / 6 / 4**）贯穿所有场景 |
| ③ | **交互反馈** | 按钮三态完整（常态 / hover / 按下 / 禁用）；**禁用原因常驻可见、不依赖点击**；升耗 `+N` 金色角标；**AP pips 逐枚亮起 80ms** |
| ④ | **操作流畅度** | 主 CTA **单屏唯一**；`Esc` 返回 + **数字键 1–9** 设施直达；`ScrollList` 跨重建视保持；**热区严格等于绘制尺寸** |
| ⑤ | **多分辨率与无障碍** | `auto` 向下取档 + `setGameSize` + 场景重启流程；文本缩放 **100 / 125 / 150**；高对比模式只增强可读性、不破坏切角语言 |

---

## 2. 优化范围与优先级（方案原图第 1 页）

| 优先级 | 模块 | 代码落点 | 现状差距（方案判定） |
|---|---|---|---|
| **P0** | 战斗界面 | `src/scenes/BattleScene.ts` | 角色卡 **168×400 未回调 D24 裁决的 340**；AP pips 仍是平行四边形，**D25 菱形 10 槽未落地**；D30 已出手瞬时反馈待核对 |
| **P0** | 营地 | `src/scenes/CampScene.ts`、`src/config/campLayout.ts` | §50.4.3 八条 UX 问题：主 CTA 无层级、资源可见性差、招募未解锁态误导、就绪提示埋没、键盘导航缺失 |
| **P1** | 迷宫 HUD | `src/ui/HUD.ts`、`src/scenes/DungeonScene.ts` | 顶部信息带三组信息密度需梳理；已探索迷雾 alpha 降到 **0.30** 更通透；地图元素统一 **2px 线框图标化** |
| **P1** | 设置页 | `src/scenes/UIScene.ts`、`src/config/graphics.ts` | 分辨率五档 + 文本缩放 / 高对比需要分区呈现；两套存储命名空间（随档 `state.settings` vs 全局 `maze_rpg_settings`）要在 UI 上可读 |
| **P2** | 主菜单 | `src/scenes/MainMenuScene.ts` | 视觉锚点弱；未使用「停机坪」背景语言；按钮层级平，主行动「继续游戏」不突出；存档槽入口层级模糊 |
| **P2** | 背包 / 商店 | `src/ui/InventoryPanel.ts`、`src/ui/ShopPanel.ts` | 稀有度边框与堆叠数层级弱；限购与高亮商品（升阶谕令）缺视觉特权；饰品槽位效果说明**须保留**（D117 豁免） |

---

## 3. 视觉铁律与设计 Token（方案原图第 1 页 §③，对应 §50.1–50.2 · §50.12）

### 3.1 三条铁律

| # | 铁律 | 细则 |
|---|---|---|
| **铁律 1** | **全平色，零渐变零发光** | 层次感只靠「**明度阶梯 + 1–2px 细线**」；**禁止** `fillGradientStyle`、阴影、模糊、外发光 |
| **铁律 2** | **直角为体，切角为饰** | **圆角全废**；切角深度**仅允许** 14（大面板）/ 10（中卡片）/ 8（小控件）/ 6（按钮，**仅切右下**）/ 4（微件）px |
| **铁律 3** | **强调色克制** | `#FFD400` 单屏面积 **≤5%**，只给「当前可交互 / 当前选中 / 关键数值」；红绿青状态色各就其位、不混用 |

### 3.2 色板（主色，与 `src/config/constants.ts` 的 `Palette` 一一对应）

| 名称 | 值 | `Palette` 槽位 |
|---|---|---|
| bg | `#0B0E14` | `bg` |
| panel | `#14181F` | `panel` |
| panelLight | `#1C212B` | `panelLight` |
| border | `#3A4150` | `border` |
| accent | `#FFD400` | `accent` |
| gold | `#F0A824` | `gold` |
| tech | `#2ED6C8` | `tech` |
| danger | `#E5483F` | `danger` |

### 3.3 语义色

| 语义 | 值 | `Palette` 槽位 |
|---|---|---|
| HP | `#3FD08C` | `hp` ✅ |
| MP | `#3FA9F5` | ⚠️ **`Palette` 中无对应槽位** —— 该色目前只出现在 `WorldMapScene.ts` 的 `dungeon: 0x3fa9f5`。若要让 MP 用此色，属**新增语义色**（设计变更，需用户确认） |
| 经验 | `#C9A54C` | `exp` ✅ |
| 文本 | `#EDEFF2` | `text` ✅ |
| 弱文本 | `#8A919D` | `textDim` ✅ |

### 3.4 稀有度色

| 星级 | 值 |
|---|---|
| 5★ | `#FFAE1A` |
| 4★ | `#9E7FD8` |
| 3★ | `#4A9FE8` |
| 2★ | `#4CC98A` |
| 1★ | `#9AA3AF` |

### 3.5 字阶

**大标题 28 Bold / 面板标题 20 Bold / 小节 14 Bold accent / 正文 15 / 辅助 13 / 微注 11–12 / 关键数值 16 Bold**
取向：**标题更大更重，正文更小更灰。**

### 3.6 动效

| 场景 | 时长 / 缓动 |
|---|---|
| 卡牌抬升 | **120ms** `Quad.easeOut` |
| AP pips 亮起间隔 | **80ms** |
| 不可用点击抖动 | **120ms** |
| toast | **2000ms** |
| 信标浮动 | **1800ms** `Sine.easeInOut` |
| 镜头对准 | **140ms** |

---

## 4. 逐界面「问题 → 对策」（方案原图第 2–7 页原文转录）

### 4.1 主菜单 `MainMenuScene`（P2）

| # | 问题 | 对策 |
|---|---|---|
| 1 | **视觉锚点弱**：纯色背景 + 居中按钮列，与营地 / 迷宫缺乏共同语言 | 引入**停机坪背景语言**：斜切几何色块 + 网格地平线 + 全息信标 + 顶部警示带（§50.4.2，**面积克制**） |
| 2 | **按钮层级平**：继续 / 新游戏 / 设置同等 weight，主行动淹没 | 「**继续游戏**」升为**唯一主行动**：accent 填充 + 黑字 bold；其余为**描边次级按钮**，纵向节奏 **14px** |
| 3 | **存档入口层级模糊**：槽位状态（难度 / 层数 / 队伍）不可见 | 右侧常驻「**最近存档**」卡：槽位 / 难度 / 层数 / 出战**一眼可读**，点击进入读取流程（**E-23 三步覆盖保护不变**） |
| 4 | 设置入口与营地重复（§50.4.3-6），职责分散 | 设置**仅保留在主菜单**；营地移除重复入口，**全局职责唯一** |

### 4.2 设置页（P1）

| # | 问题 | 对策 |
|---|---|---|
| 1 | 分辨率与无障碍混排，`auto`「向下取档」规则玩家不可见 | 「**画面**」区六档 **chips 平铺**；`auto` 标注「**推荐 · 向下取最近固定档**」；切换流程（`setGameSize` + 场景重启）写成**一行微注** |
| 2 | 两套存储命名空间（D315 / E-20）在 UI 无体现：玩家不知道哪些设置随档、哪些是全局 | **分区标题直接标注存储归属**：「无障碍 · 全局 `maze_rpg_settings`」vs「随档设置 · `state.settings`」；写入纪律「**读旧值→合并→写回**」不变 |
| 3 | 文本缩放 100/125/150 只缩字号、布局常量不联动（D100 连带），易被误解为整体 UI 缩放 | 缩放档**旁加微注**「**仅缩放字号，布局保持固定**」；默认档**恒等性铁律**（`verify` §22.1）不受影响 |
| 4 | 音量滑杆存在但**无音频资源**（D31 / D139），玩家调节无反馈会困惑 | 滑杆保留并标注「**接口已接 · 资源占位（静默模式）**」；BGM / 音效**分两行**，主音轨真实受控 |

### 4.3 营地 `CampScene`（P0）

| # | 问题 | 对策 |
|---|---|---|
| 1 | **主操作无层级**：「出发探索」与 8 个设施按钮同列等大，玩家进营地不知道下一步点哪 | 主 CTA 拆出为**右下角独立大按钮（420×72）**，accent 填充 + **播放图标**；设施改 **2 列网格次级按钮** |
| 2 | **资源可见性差**：金币 / 科技点 / 出战人数埋在文本段里，扫读拿不到 | **顶部常驻资源栏**：图标 + 数字**胶囊 chip 右对齐**；数值变化时**跳动 + 变色反馈** |
| 3 | **状态误导与提示埋没**：招募未解锁态看似可点；「无出战角色」提示埋在信息栏深处 | 未解锁按钮**置灰 + 锁标 + 直写条件**「需科技：招募许可 1」；无出战角色时**主 CTA 置灰 + 上方常驻红色警示行** |
| 4 | **键盘导航缺失 / 左栏文本堆砌**：全靠鼠标点选；成员行是纯文本墙，读不出谁在前排 | `Esc` 返回大地图、**数字键 1–9 直达设施**（按钮标注键位）；成员行加 **40×40 头像**，六维缩为「**合计 N · 站位**」一行 |

### 4.4 迷宫 HUD `HUD.ts` / `DungeonScene`（P1）

| # | 问题 | 对策 |
|---|---|---|
| 1 | **信息带密度不均**：地牢名 / 层数 / 种子 / HP / 出战 / 探索度混排，扫读路径不固定 | **三段固定栅格**：左「地牢名 · 层数 · 种子」、中「HP / MP 细条」、右「出战头像条 + 探索度 + 菜单」；**基线对齐** |
| 2 | **已探索迷雾太闷**：走过区域被厚遮罩压暗，扁平地图应有的通透性不足 | 已探索迷雾 **alpha 降到 0.30**；未探索保持 `fogUnknown` **全盖**（§50.4.1） |
| 3 | **地图元素风格不统一**：宝箱圆角、敌人红圆、玩家光晕块等旧语言残留 | 全部 **2px 线框图标化**：敌人菱形 / 宝箱线框 / 陷阱三角 / 事件方框点 / 终点双竖线门；**填充只给玩家切角块与精英敌** |
| 4 | **墙体亮条 = 渐变感来源**：墙顶 4px 亮条制造伪立体感，违反零渐变铁律 | **删亮条**，改内缩 3px 的 **1px border 内框线**；迷宫色名**唯一真源** `mazeRoad` / `mazeWall` / `mazeVoid`（D36） |

### 4.5 战斗界面 `BattleScene`（P0）

> 标题：**GDD 未落地裁决收口**

| # | 问题 | 对策 |
|---|---|---|
| 1 | **角色卡尺寸未回调（D24）**：代码现值 168×400 超出裁决的 340，指令台被撑高、战区受压缩 | 卡高回调 **340**（本图 2/3 缩放 ⇒ 227），`CONSOLE_H` 同步 **−60**；半身像 / 名字带 / 技能条坐标随动 |
| 2 | **AP pips 形态未落地（D25）**：代码现值仍是斜切平行四边形，与裁决的**菱形 10 槽**不符 | **菱形 20×20 两排 5+5 共 10 槽**；第 9/10 槽**解锁画锁形 + 虚线描边**；右侧大号「**3 / 8**」；回复时**逐枚亮起 80ms** |
| 3 | **禁用原因依赖点击**：玩家要点了才知道技能为什么不能放，试探成本高 | 禁用态**常驻可见（D37）**：`SPENT_FILL` 灰条 + 左色条 alpha 0.35 + AP 不足红字斜线 + 升耗金色 `+N` 角标；点击再补原因文案 |
| 4 | **「轮到谁」高亮无依附（D30）**：阶段制下 `currentActor` 我方阶段恒 `null`，常驻高亮三件套失去语义 | 只保留「**已出手**」瞬时反馈：**120ms `Quad.easeOut` 抬升即落**；队伍战术行挂 HUD（`allyTacticRow`），**卡内不再画 buff 行** |

### 4.6 背包 / 商店 `InventoryPanel` / `ShopPanel`（P2）

| # | 问题 | 对策 |
|---|---|---|
| 1 | **稀有度层级弱**：物品格全靠小字标注，高价值物品扫读不出来 | 物品格 **2px 稀有度边框 + 右下堆叠数 bold**；**5★ 橙金唯一高亮**，其余降饱和（§50.1.4） |
| 2 | **限购商品无特权（D449–D451）**：「升阶谕令」全队各升 1 级、每店限购 1，与普通消耗品视觉无差别 | 限购商品加 **gold 角标「限购 1」+ 5★ 边框**；普通商品保持 **1px 灰边**，对比即特权 |
| 3 | **点选坐标系曾三处抄错**：「内容坐标当视口坐标」导致滚动后整列点不中（族 C bug，**已修**） | 命中**一律走** `listRowIndexAt` / `columnIndexAtWorldX` **唯一出口**；选中格 accent **2px 描边**，落间隙 / 滚动条带 / 视口外**返回 null** |
| 4 | **效果说明 vs 资料入口边界**：图鉴已废止（D106 / D117），但**饰品效果说明**容易被误删 | 槽位卡**保留**「效果说明」一行（13px `textDim`）—— 它是 **D117 豁免**的槽位必要标注，与图鉴废止不冲突 |

---

## 5. 用户裁决（2026-09-24，本次执行依据）

| ID | 议题 | 用户裁决 |
|---|---|---|
| **U1** | 「战斗角色栏不变」的边界 | **只冻结角色卡栏**（C2 卡牌容器：6 张 168×400 角色卡、半身像、名字带、卡内技能栏及全部卡内坐标与动画）。战斗页其余按方案落地：AP pips 菱形化、指令台排布、顶部信息带、C1 日志区 |
| **U2** | 营地布局与职责变更 | **全部照方案执行**（含**移除营地重复设置入口**、主 CTA 移右下 420×72、设施改 2 列网格） |
| **U3** | 交付节奏 | **先做 P0（战斗 + 营地）→ 用户确认效果 → 再做 P1 / P2** |

---

## 6. 执行边界与冻结清单

### 6.1 战斗角色栏冻结清单（🔴 不可改）

`CARD_W = 168`、`CARD_H = 400`、`CARD_GAP = 12`、`CARD_SLOTS = 6`、`BUST_H = 212`、`SKILL_AREA_Y = 282`、`SKILL_BAR_H = 46`、`SKILL_BAR_X = 6`、`CHAMFER_CARD = 10`、`CONSOLE_H = 424`。
卡内一切：半身像裁切与位置、名字带（y=222 h=26）、技能栏四态、hover 高亮、点击不可用时的 120ms 横向抖动 —— **全部不动**。

**因此被放弃的方案条目**：
- ❌ 方案 §4.5 对策 1（卡高回调 340 + `CONSOLE_H` −60）
- ❌ 方案 §4.5 对策 4 的「已出手 120ms 抬升反馈」（同属角色栏动画）

### 6.2 与既有裁决的冲突登记

| 既有裁决 | 方案要求 | 本次执行 | 说明 |
|---|---|---|---|
| **GDD D24**：战斗角色卡 **168×340**（并标注「⚠️ 要改代码：现值 168×400」） | 回调 340 | **不执行，保持 400** | 用户 U1 裁决。D24 的 340 来自 `battle-ap-ux-spec.md`，但**用户已明确要求保持现状**。已在 `BattleScene.ts` 的 `CARD_H` 上方加防御性注释，避免后人再来"修正" |
| **GDD D25**：AP pips 菱形 10 槽 | 落地 | **执行** | 10 槽两排此前已实现，本次只补**菱形形态**与 80ms 节奏 |
| **GDD D37**：禁用态常驻可见 | 落地 | **已实现，无需改** | 代码中 `SPENT_FILL` + 左色条 α0.35 + AP 不足红斜线 + 升耗金色 `+N` + 点击补原因**均已存在** |
| **GDD D30**：已出手瞬时反馈 | 落地 | **不做**（属角色栏动画） | 阶段制改造后卡牌抬升已被整体删除（见 `rebuildCards()` 上方注释），是否恢复需用户另行裁决 |

### 6.3 文件归属（并行工程线的隔离边界）

| 工程线 | 可改文件 | 禁止触碰 |
|---|---|---|
| **UI-P0-1 营地** | `src/scenes/CampScene.ts`、`src/config/campLayout.ts` | `BattleScene.ts`、`scripts/verify.mjs` |
| **UI-P0-2 战斗** | `src/scenes/BattleScene.ts` | `CampScene.ts`、`campLayout.ts`、`scripts/verify.mjs` |

> ⚠️ `scripts/verify.mjs` **同一时刻只许一个写者**（并发追加会覆盖前一节标题）。本次 P0 两条线**均不得修改**该文件；回归断言在汇总阶段统一追加。

### 6.4 验证门（每条工程线都必须过）

1. `node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` → **exit 0**
2. `scripts/verify.mjs` → 保持 **250 项 / 失败 0**（改动前基线）
3. 真实浏览器截图取证 → `docs/evidence/ui-p0/<module>/`（`--headless=new`、`--window-size=1920,1080`、`--disable-gpu`、`--enable-unsafe-swiftshader`）
4. 几何自检脚本（`scripts/zz-ui-p0-*.mjs`）：热区 == 绘制尺寸；元素包围盒互不重叠

### 6.5 改动前备份

`.backup/ui-p0-2026-09-24/` —— 含 `CampScene.ts`、`BattleScene.ts`、`campLayout.ts`、`constants.ts`、`PartyPanel.ts`、`UIScene.ts`，**逐字节校验一致**。
