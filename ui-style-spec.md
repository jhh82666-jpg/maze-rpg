# UI 美术风格规格 —— 明日方舟式扁平科幻

> 面向程序化绘制（Phaser Graphics + Text，无贴图 UI）。所有数值基于逻辑分辨率 1920×1080。
> 工程落点：`src/config/constants.ts` 的 `Palette`、`src/ui/Widgets.ts` 的 `drawPanel / drawBar / Button / rarityColor`。
> 本规格不写代码，但所有数值可直接照抄。

---

## 0. 风格总则（三条铁律）

1. **全平色，零渐变零发光。** 禁止 `fillGradientStyle`、禁止阴影/模糊/外发光。层次感靠「明度阶梯 + 1–2px 细线」获得。
2. **直角为体，切角为饰。** 所有圆角（`fillRoundedRect` / `strokeRoundedRect`）全部废除，改用斜切角多边形（见 §2.1 的几何定义）。
3. **强调色克制。** 亮黄 `#FFD400` 是唯一主强调色，单屏出现面积 ≤5%，只给「当前可交互 / 当前选中 / 关键数值」。状态色（红/绿/青）各就其位，不混用。

---

## 1. Palette 色板（逐字段替换表）

对照 `src/config/constants.ts` 现有 16 个字段，命名与数量不变，只换值：

| 字段 | 旧值 | **新值** | 用途与说明 |
|---|---|---|---|
| `bg` | `0x0b0d12` | **`0x0B0E14`** | 全局底色，近黑微蓝 |
| `panel` | `0x151a26` | **`0x14181F`** | 面板底，去蓝化的枪铁灰 |
| `panelLight` | `0x1e2536` | **`0x1C212B`** | 面板亮层 / 按钮常态底 / 列表行底 |
| `border` | `0x33405c` | **`0x3A4150`** | 面板与控件描边（中性灰，非蓝） |
| `text` | `0xd7e3ff` | **`0xEDEFF2`** | 主文本，近白冷灰 |
| `textDim` | `0x8fa3c8` | **`0x8A919D`** | 弱文本 / 说明文字 |
| `accent` | `0x4f8cff` | **`0xFFD400`** | **主强调：明日方舟标志性亮黄**。选中态、主行动、关键角标 |
| `gold` | `0xf0c05a` | **`0xF0A824`** | 金币。比 accent 深一档，避免与强调黄混淆 |
| `tech` | `0x59e0c0` | **`0x2ED6C8`** | 科技点 / 辅助信息青。压低保和，只做次级强调 |
| `danger` | `0xe0574f` | **`0xE5483F`** | 危险 / 敌人 / 删除 |
| `hp` | `0x4fd07a` | **`0x3FD08C`** | HP。偏青绿，与工业风协调 |
| `mp` | `0x4f8cff` | **`0x3FA9F5`** | MP。与 accent 黄拉开色相距离 |
| `exp` | `0xf0c05a` | **`0xC9A54C`** | 经验。哑光琥珀，故意比 gold 暗，避免三条黄 |
| `floor` | `0x232b3d` | **`0x1A1F28`** | 迷宫地砖 A |
| `floorAlt` | `0x1d2434` | **`0x151A21`** | 迷宫地砖 B（棋盘格交替） |
| `wall` | `0x39445e` | **`0x2E3540`** | 墙体 |
| `fogUnknown` | `0x05070c` | **`0x04060A`** | 未探索迷雾（近乎纯黑） |
| `fogExplored` | `0x8a93a8` | **`0x7C8492`** | 已探索迷雾遮罩色 |

**Widgets.ts 内部常量同步替换：**

| 常量 | 旧值 | 新值 |
|---|---|---|
| `DISABLED_FILL` | `0x1a1f2b` | `0x14171D` |
| `PRESSED_FILL` | `0x39456b` | `0x11141A` |
| `DISABLED_BORDER` | `0x2a3242` | `0x272C35` |
| 禁用文本色（`0x5b6577`，两处） | — | `0x565D68` |
| `drawBar` 默认 `track` | `0x2a3242` | `0x0C0F15` |
| `Button` 默认 `hoverFill` | `0x2b3550` | `0x262C37` |
| 各处写死的 hover/选中 `0x2b3550`（如 CodexPanel） | — | `0x262C37` |

**稀有度用色（替换 `rarityColor()`，克制化、去荧光）：**

| 稀有度 | 旧值 | 新值 | 说明 |
|---|---|---|---|
| 5★ | `0xffb347` | `0xFFAE1A` | 橙金，唯一高亮稀有度 |
| 4★ | `0xc07bff` | `0x9E7FD8` | 哑光紫，降饱和 |
| 3★ | `0x4f8cff` | `0x4A9FE8` | 工业蓝 |
| 2★ | `0x4fd07a` | `0x4CC98A` | 青绿 |
| 1★ | `0x9aa7bd` | `0x9AA3AF` | 中性灰 |

**PortraitCard.ts 同步：** `SILHOUETTE_TINT` `0x1a1a22` → `0x0A0C11`；`RARITY_TINT` 各值整体压暗 30%（`5:0x2E2410 / 4:0x221A30 / 3:0x14202F / 2:0x12241A / 1:0x1B1F26`）；剪影占位文字色 `0x4a5266` → `0x454C58`。

---

## 2. 控件造型语言

### 2.1 切角几何定义（全项目唯一标准）

斜切角用 8 顶点（或 6 顶点）多边形代替圆角矩形，Phaser 侧用 `fillPoints / strokePoints`（`closeShape: true`）实现。

**标准切角（面板 / 卡片）—— 左上 + 右下对角切：**

```
切角深度 c（沿边长的像素），矩形 (x, y, w, h)，顶点顺时针：
(x+c, y) → (x+w, y) → (x+w, y+h-c) → (x+w-c, y+h) → (x, y+h) → (x, y+c) → 闭合
```

**切角深度分档（不允许出现其他值）：**

| 档位 | c | 用于 |
|---|---|---|
| 大面板 | 14px | 全屏弹窗、营地各面板底板（≥600px 宽） |
| 中卡片 | 10px | 战斗角色卡、图鉴详情立绘框 |
| 小控件 | 8px | 列表行、小面板、输入框 |
| 按钮 | 6px | 所有按钮（只切右下一角，见 §2.3） |
| 微件 | 4px | 进度条端头、标签 chip |

`drawPanel` 的 `radius` 参数废弃，改为 `chamfer?: number`，默认 8。面板 alpha 从 0.96 提到 **1.0**（扁平风格不透底）。

### 2.2 面板规格

| 项 | 值 |
|---|---|
| 填充 | `Palette.panel`（`0x14181F`），alpha 1.0 |
| 边框 | 2px `Palette.border`（`0x3A4150`），alpha 1.0 |
| 切角 | 按 §2.1 分档 |
| 角标装饰 | 主面板在**左上角**加 accent 黄色 L 形角标：横竖各 18px、粗 3px，距面板边 6px；次级面板不加 |
| 标题装饰 | 标题左侧 accent 竖条从现有 4×22 改为 **4×20、距标题 10px**，颜色 `accent` |
| 内边距 | 20px（现状 PAD，不变） |
| 层级分隔 | 面板内分区只用 1px `border` 色横线 + 12px 留白，不加第二层面板底色；必须分层时用 `panelLight`，面积差一级明度 |

### 2.3 按钮三态（`Button.redraw()` 重绘规格）

按钮形状：**矩形 + 右下角切 6px**（5 顶点多边形），不再用圆角、圆形按钮除外（✕ 关闭钮保留圆形）。

**常规按钮**（默认）：

| 状态 | 填充 | 边框 | 文本 | 附加 |
|---|---|---|---|---|
| 常态 | `panelLight` `0x1C212B` | 2px `border` `0x3A4150` | `0xEDEFF2` | — |
| hover | `0x262C37` | 2px **`accent` `0xFFD400`** | `0xFFFFFF` | 左侧内缘加 3×(h-16)px accent 竖条（距左边 4px） |
| 按下 | `0x11141A` | 2px `accent` | `0xFFD400` | 整体内容不变，仅变色（不做位移缩放） |
| 禁用 | `0x14171D` | 2px `0x272C35` | `0x565D68` | 无竖条，不响应 hover |

**主行动按钮**（每场战斗「攻击」、营地「出发探索」等一个屏幕唯一的主 CTA，通过 `fill` 参数传入即可识别）：

| 状态 | 填充 | 边框 | 文本 |
|---|---|---|---|
| 常态 | `accent` `0xFFD400` | 无 | `0x111417`，bold |
| hover | `0xFFE03A` | 无 | `0x111417` |
| 按下 | `0xD9B400` | 无 | `0x111417` |
| 禁用 | `0x272C35` | 无 | `0x565D68` |

图标按钮（iconMode）：✕ 用 22px 字形 `textDim` 色，hover 变 `danger` 色；不画底板圆，只保留圆形热区。＋/− 等同理，hover 变 `accent`。

### 2.4 进度条（`drawBar` 重绘规格）

| 项 | 值 |
|---|---|
| 标准高度 | 8px（迷你条 6px，如卡片上的 HP） |
| 槽 | `0x0C0F15` 平色，**无圆角**（直角矩形），无描边 |
| 前景 | 对应语义色平色，无渐变 |
| 端头 | 前景右端切 4px 斜角（平行四边形收尾）：顶点 `(x, y) (x+fw, y) (x+fw-4, y+h) (x, y+h)` |
| 满值标记 | 槽右端画 1×h px `border` 色刻度线，提示满值位置 |
| 文字 | 数值文本放条上方 2px，12px，主色 text，不压在条内 |

### 2.5 字体层级

字体族保持 `"Segoe UI", "Microsoft YaHei", system-ui, sans-serif` 不变（零资源依赖）；英文/数字混排处追加 `Bahnschrift, Consolas` 前置。

| 层级 | 字号 | 字重 | 颜色 | 用途 |
|---|---|---|---|---|
| 场景大标题 | 28px | bold | `text` | makeHeader 标题（现 24 → 28），字符间距 +1 |
| 面板标题 | 20px | bold | `text` | 弹窗标题（现 22 → 20，收紧） |
| 小节标题 | 14px | bold | `accent` | 面板内「简介 / 六维属性」等分节（现用 gold，改 accent） |
| 正文 | 15px | regular | `text` | 按钮、列表主行（现 16 → 15） |
| 辅助 | 13px | regular | `textDim` | 元信息、说明 |
| 微注 | 11–12px | regular | `textDim` | 页脚提示、冷却秒数 |
| 关键数值 | 16px | bold | `accent` / 语义色 | 伤害数、已解锁计数 |

原则：**标题更大更重、正文更小更灰**，拉开对比是工业感排版的核心。

---

## 3. 战斗卡牌栏位视觉规格

### 3.1 角色卡

| 项 | 值 |
|---|---|
| 尺寸 | **96 × 128px**（3:4，与 bust 纹理 384×512 同比例，cover 裁剪零变形） |
| 间距 | 卡间 10px，栏位整体距屏幕底边 16px |
| 形状 | 切角 c=10（左上+右下，§2.1） |
| 边框 | 2px，颜色 = 稀有度色（§1 表），alpha 1.0 |
| 半身像 | `drawPortrait(bust: true)` 的居中裁剪帧直接复用（96×128 与源同比例，裁剪逻辑天然退化为整图缩放） |
| 底部信息带 | 卡底 22px 高 `0x0B0E14` alpha 0.85 平色条，内排：名字 12px bold `text`（左）+ 等级 11px `textDim`（右） |
| HP/MP 条 | 信息带上方叠两条迷你条：HP 4px `hp` 色、MP 3px `mp` 色，间隔 1px，宽 = 卡宽 − 12 |
| 死亡态 | 半身像 `setTint(0x3A3F47)` + 卡 alpha 0.5，边框改 `0x272C35`，不画叉 |

### 3.2 技能图标

| 项 | 值 |
|---|---|
| 尺寸 | 36 × 36px，图标间距 6px，排在角色卡正上方一行 |
| 形状 | 直角方块（**不**切角，与卡形成节奏对比），底 `panel` 色 |
| 边框 | 可用：2px `tech` 青；选中：2px `accent` 黄 + 外扩 3px 的 1px accent 框线 |
| 图标本体 | 现文字缩写/符号，14px bold，可用态 `text` 色 |
| **冷却遮罩** | ① 黑色 alpha 0.75 全盖；② 剩余比例扇形：`slicePie(cx, cy, 17, -90°, -90°+360°×剩余比)` 填充 `0x0B0E14` alpha 0.85（顺时针消退）；③ 中央剩余秒数 13px bold `0xFFFFFF`；④ 扇形外缘 1px `textDim` 环 |
| **不可用灰态**（MP 不足 / 未解锁） | 图标 `setTint(0x3A3F47)` + alpha 0.6，边框 `0x272C35`，不加遮罩、不显示秒数 |
| MP 消耗 | 图标右下角 10px `mp` 色数字，超出图标 2px 外露（明日方舟式出血排版） |

### 3.3 AP pip（行动点）

- 单个 pip：**斜切平行四边形 14 × 5px**（左右各斜切 3px，顶点 `(3,0) (14,0) (11,5) (0,5)`），pip 间距 3px。
- 有余量：填 `accent` 黄；已消耗：填 `0x272C35`。
- 位置：角色卡顶边上方 4px，水平居中；一行排不下时上限 8 个，超出合并为「×N」11px 文本。
- 不用圆形、不用菱形——平行四边形与全局切角语言同构。

### 3.4 当前行动者高亮（明日方舟式：位移 + 描边 + 角标，零发光）

三件套，缺一不可：

1. **抬升**：卡片整体上移 8px（y − 8，tween 120ms Quad.easeOut，行动结束回落 150ms）。
2. **外层切角描边**：卡外扩 3px 画一圈 2px `accent` 色切角框（c=12，与卡同切角方向），静态不闪。
3. **行动角标**：卡左上角贴 16×16px `accent` 色方块（右下角切 4px），内放黑色 bold 11px「▶」；敌方行动者用 `danger` 红替代黄。

辅助：非行动者卡片 alpha 降至 0.75（整体压暗，不逐元素改色）。

---

## 4. 图鉴未解锁剪影方案（3 选 1）

现状：`drawPortrait` 的 `silhouette` 分支 = `setTint(0x1a1a22)` + alpha 0.55。以下方案均在此分支上叠加，**全部可用现有裁剪帧机制实现，无需 GeometryMask**（裁剪帧尺寸恰为框尺寸，overlay 的 Graphics 按框矩形绘制即可，不会溢出）。

### 方案 A：纯黑剪影 + 扫描线（推荐）

- 图：`setTint(0x0A0C11)` + alpha 0.92 → 全黑轮廓。
- 扫描底纹：Graphics 每 4px 一条 1px 横线，`accent` 色 alpha 0.06，铺满框体。
- 动扫描线：一条 2px 高 `accent` alpha 0.35 横带，tween 从框顶移动到框底，2400ms Linear，yoyo 往返，延迟按条目 index × 300ms 错开。
- 实现要点：列表缩略图（72×96）**关掉动扫描线只留底纹**（每行一个 tween 性能浪费）；详情栏大立绘（约 224×200）才开动效。
- 成本：每框 1 个 Graphics（约 25/50 条线）+ 至多 1 个 tween。

### 方案 B：黑剪影 + 静态噪点干扰

- 图同方案 A。
- 噪点：种子化 RNG（以 portraitId 为种子，保证每次打开一致）画 40–60 条 2–8px 长的 1px 横线段，`textDim` 色 alpha 0.15，随机散布——模拟信号干扰的横纹。
- 成本：一次性 Graphics，零 tween，性能最优；但静态画面「科技感」弱于 A。

### 方案 C：错位轮廓光（故障风）

- 不用 tint 提取边缘（纯 Graphics 做不了 edge detect），改用**双层剪影**：底层放一张裁剪帧拷贝 `setTint(0x2ED6C8)` alpha 0.30、向左上偏移 1.5px；主层 `setTint(0x0A0C11)` alpha 0.95 正常位 → 轮廓边缘漏出一圈青色错边。
- 成本：每框多 1 个 Image 对象，零 tween。
- 风格最「赛博」，但与全局克制的用色原则有冲突，且多一层 Image 在 15 行列表里翻倍纹理对象。

**推荐：方案 A。** 扫描线是明日方舟图鉴/情报界面的标志性语言，底纹静态部分满足扁平原则，动线仅一处（详情栏）符合「强调克制」；列表用静态底纹即方案 B 的退化形态，两者天然兼容。

---

## 5. 迷宫画面扁平化重绘（要点）

现状：`DungeonScene.redrawMaze()`，TILE=34，地砖棋盘格 + 墙体顶部 4px 亮条 + 圆角玩家块。

| 元素 | 现状 | 重绘规格 |
|---|---|---|
| 地砖 | 棋盘格平色（保留思路） | 新 `floor / floorAlt`，格间留 1px `bg` 色缝（`fillRect(px, py, TILE-1, TILE-1)` 不变，缝隙色即底色） |
| 墙 | 平色 + 顶部 4px 亮条 | **去掉亮条**（渐变感来源），改：整块 `wall` 平色 + 内缩 3px 画 1px `border` 色内框线 → 工业面板感 |
| 玩家标记 | 光晕圆 + 圆角块 | 删光晕圆；切角方块（内缩 6px，c=4，填 `accent`）+ 中心 4×4 `0x111417` 黑点；朝向用黑点上 2px 宽的短线指示 |
| 宝箱 | 圆角矩形+扣 | 直角箱体 `gold` 边框 2px 不填充（线框风），盖线 1 条 |
| 敌人 | 红圆 | 菱形（旋转 45° 方块）`danger` 2px 线框，精英填实 |
| 陷阱 | 三角 | 保持三角但改 2px `danger` 线框不填充 |
| 事件/存档/终点 | 各异 | 统一 2px 线框语言：事件 = `tech` 方框内一点；存档 = `hp` 十字（现十字保留，改 2px 线宽一致）；终点 = `accent` 双竖线门形 |
| 迷雾 | 两档遮罩 | 未探索 `fogUnknown` 全盖不变；已探索 alpha 从现值降到 **0.30**（扁平地图应更通透） |

原则：元素一律「线框图标化」，填充只留给玩家和精英敌。

---

## 6. 营地背景科幻化重绘（要点）

现状：`CampScene.drawBackground()` = 夜空渐变 + 星点 + 帐篷 + 篝火（光晕 tween）。改造为**前哨基地停机坪**：

1. **底色**：`bg` 平色全铺，删渐变、删星点。
2. **远景几何**：2–3 个大尺寸斜切多边形色块（`panel` 与 `panelLight` 交替，alpha 1.0），从屏幕左右边缘斜插入画面下 1/3，模拟建筑剪影；色块边缘用 1px `border` 色勾线。
3. **网格地平线**：画面中下部一条 1px `border` 色横线（y ≈ 62% 屏高），其下画间距 48px 的 1px 竖向透视线 5–7 条，alpha 0.12，模拟停机坪地面标线。
4. **全息信标（替换篝火）**：画面中心偏下，底座 = 梯形（上宽 40 下宽 64 高 18，`panelLight` + `border` 描边）；上方悬浮一个 18×18 菱形（旋转 45°）`tech` 青 2px 线框，tween 上下浮动 4px（1800ms Sine.easeInOut yoyo）；菱形外一圈 1px `tech` alpha 0.25 的圆环（静态）。**替代原 fireGlow 的透明度补间**，动效对象数量不变。
5. **顶部装饰**：屏顶 58px 标题栏下方加一条 4px 高 `accent` 色斜纹警示带（黄黑斜纹，间距 12px，黄纹宽 6px，`accent` + `bg` 交替，长 120px 两段置于左右上角）——只此一处用警示纹，面积克制。
6. 帐篷等写实元素全部删除。

---

## 7. 落地清单（给工程的改动映射）

| 文件 | 改动 |
|---|---|
| `config/constants.ts` | `Palette` 16 字段换值（§1 表） |
| `ui/Widgets.ts` | 内部常量换值；`drawPanel`/`Button`/`drawBar` 圆角 → 切角多边形（§2）；`rarityColor` 换值；`makeHeader` 标题 28px、加 accent 角标 |
| `ui/PortraitCard.ts` | `SILHOUETTE_TINT` / `RARITY_TINT` 换值；silhouette 分支加扫描线 overlay（§4 方案 A） |
| `ui/CodexPanel.ts` 等各面板 | 写死的 `0x2b3550` → `0x262C37`；分节标题 gold → accent；圆角参数删除 |
| `scenes/BattleScene.ts` | 卡牌栏位按 §3 新建/改造 |
| `scenes/DungeonScene.ts` | `redrawMaze()` 按 §5 |
| `scenes/CampScene.ts` | `drawBackground()` + `drawCampfire()` 按 §6 |

## 8. 可访问性备注

- 新色板对比度：`text`/`bg` = 14.8:1、`textDim`/`bg` = 5.1:1、`accent` 黄底黑字 = 12.4:1，均过 WCAG AA；`accent` 黄在白底上不足，故黄底一律配 `0x111417` 黑字（本规格已贯彻）。
- 稀有度 3★蓝 与 MP 蓝、2★绿 与 HP 绿色相接近，但稀有度只出现在边框/星标，语义色只出现在数据条，场景不重叠，色盲用户无歧义。
- 冷却遮罩同时给「扇形 + 秒数」双编码，不依赖纯色彩。
