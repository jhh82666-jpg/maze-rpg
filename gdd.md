# 迷宫 RPG · 游戏设计文档（GDD）

| 项 | 值 |
|---|---|
| 文档版本 | **v2.0**（整合版，替代不存在的 v1.0） |
| 生成日期 | 2026-09-17 |
| 项目根 | `H:\work\maze-rpg` |
| 技术栈 | Phaser 3.90 + TypeScript 5.9 + Vite 7，纯前端单机、无后端 |
| 美术方式 | 100% 程序化绘制（`Graphics`），无外部图片依赖（除角色立绘 PNG/WebP） |
| 文档状态 | **正文已按代码实测填写；标 ❓ 处为设计缺失，等待拍板** |

---

## 0. 文档说明

### 0.1 这份文档的地位

项目原先引用《迷宫 RPG 游戏设计文档 v1.0》，**该文档不在仓库中**（已确认缺失）。`README.md` 里保留着 v1.0 的描述，但与现状大面积冲突（见附录 A）。

因此本文档的**真源优先级**为：

```
代码实现 + 配表 JSON  >  本文档  >  README.md（已过期，勿采信）
```

> ⚠️ 本项目的设计**不是**先写文档再实现，而是"规则即代码"。本文档是对既有实现的**逆向整理**，所以正文描述的是**实际行为**——包括那些看起来像 bug 的行为。凡"实现与意图不符"的，集中列在 §10 与附录 C，等待作者裁决。

### 0.2 符号约定

| 符号 | 含义 |
|---|---|
| ✅ | 已实现且行为符合预期 |
| ⚠️ | 已实现，但存在已知问题（见 §10 对应编号） |
| ❌ | **设计缺失**——玩法上必须有，代码里完全没有 |
| 🪦 | 已废弃但残留（字段/模块/注释），见附录 B |
| ❓ | 需要作者拍板的未决问题，见附录 C |

### 0.3 数值改动入口（重要）

| 想改什么 | 改哪里 | 改完要做什么 |
|---|---|---|
| 几乎所有平衡数值 | `src/data/tables/balance.json` | `npm run gen:tables` |
| 角色 / 技能 / 敌人 / 道具 / 科技 / 地牢 / 地图 | 同名 `.json` | 同上 |
| **迷宫难度阶梯 N1–N6** | `src/config/mazeDifficulty.ts`（TS 模块，**不是配表**） | 无需 gen-tables |
| 配色 / 分辨率 / 场景键 | `src/config/constants.ts` | 无需 |
| **AP 三项基础值** | `src/systems/CombatSystem.ts:42-44`（❓**硬编码，不在配表**） | 需改代码 |
| **宝箱/事件/陷阱概率** | `src/systems/MazeGenerator.ts:531-571`（❓**硬编码**） | 需改代码 |

> 🪦 `src/config/balance.ts` 的 `BALANCE` 常量与 `balance.json` **当前数值完全一致**，即 json 实际是"零覆盖"。两者是"默认值 + 覆盖层"关系（`mergeBalance` 浅合并，`potentialMultipliers`/`rarityWeights` 深合并）。**约定：唯一改数值入口是 json**，不要改 TS 默认值。

### 0.4 术语表（先钉死，避免混用）

| 术语 | 定义 | 易混淆点 |
|---|---|---|
| **层 / floor** | 地牢内的第几层。真源是运行时 `DungeonScene.floor` | 🪦 `dungeons.json` 的 `floor` 是"**起始层**"，4 个地牢**全是 1**，实际几乎无用 |
| **难度档 N1–N6** | 迷宫尺寸/拓扑档位，**由层数决定**（第 N 层 = N_N） | 不是玩家可选的"难度设置"（❌ 无难度选择） |
| **大回合 / round** | 我方阶段 + 敌方阶段 合并为一个回合 | 不是"一个单位行动一次" |
| **AP** | 队伍**共享**的行动点池，用于释放技能 | 不是每个角色各自的资源 |
| **升耗** | 重复使用同一技能导致下回合 AP 消耗 +1 | 分「临时(temp)」与「永续(permanent)」两种，**互斥** |
| **主干 / 支路** | 迷宫中起点→出口的唯一路径 / 死胡同 | 长度单位是**玩家步数**（不是格子数） |
| **生命池 / pool** | 迷宫里队伍共享的一条 HP（耐力×20） | ⚠️ 与**战斗中的 HP 是两套、不连通**，见 §10-1 |

---

## 0.5 决策记录（Decision Log）

> 只追加、不删改。每条记录：轮次 / 问题 / 裁决 / 影响面。裁决后同步更新正文并把附录 C 对应条目标为「已决」。

### 第 1 轮（2026-09-17）

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| D1 | **生命池语义** | **真共享一份**：全队共用一条 HP；**战斗与迷宫是同一份**；伤害扣它，任何角色的治疗都回它。 | 解决 §10-1/2；需重写血条读数、治疗目标、战斗开局 HP 继承；`GameState.setVitals/vitalsOf` 要真正接线 |
| D2 | **通关与结局** | **只做单图通关，不做全局结局**。每地牢第 6 层设**固定 Boss**（不可回避），击败即该图通关。 | 解决 §10-3；需新增 Boss 标记字段 + 第 6 层固定 Boss 规则；`cleared` 改由 Boss 击败触发 |
| D3 | **群体技与多敌** | **永久单敌，清掉群体概念**。承认 `enemy_all ≡ enemy`，把 25 条群体技改为明确的单体技，**删除 `enemy_all` 目标类型与 `count` 字段**。 | 解决 §3.3.2 与 §3.5.2；技能表 25 条需改写；`count` 折算逻辑（含 0.35 系数）整段删除 |
| D4 | **核心体验支柱** | 定为 **3 条**：① AP 战术分配 ② 迷宫探索与风险管理 ③ 收集与养成。**不含**「硬核生存压力」。 | §1.2 定稿；D1 是支柱②的前提；§3.2.1「apCost 全为 1」使支柱①失效，需在后续轮次解决 |

**尚未裁决但被 D1–D4 牵动的**：团灭代价（C1-3）、难度选择（C1-8）、Boss 的具体定义与掉落（C2-20）、`element` 克制（C1-6）。

### 第 2 轮（2026-09-17）

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| D5 | **技能 AP 消耗分化** | **按强度分档**：弱技 1 AP / 中技 2 AP / 大招 3 AP（按 `power` 与 `hits` 分档）。 | 支柱① 的成立前提。80 条技能需重新配 `apCost`；需定分档阈值（待细化） |
| D6 | **Boss 定义** | **每图一个专属 Boss**：4 个地牢各指定一个，打 `isBoss` 标记，**普通遇敌池必须排除 Boss**。 | 修复"最终 Boss 当路边怪"。需新增 1 个 Boss（现有高 HP 敌人只有 3 个）；需定 Boss 掉落与首通奖励 |
| D7 | **「越深越难」实现** | **两者都做**：既按层数缩放敌人数值（HP/攻/防），也按层数切换敌人池。 | 需定缩放系数与每层池子映射；调平衡变量翻倍 |
| D8 | **团灭代价** | **减轻：不扣金币**。只清 `activeRun`（探索进度）并送回营地。 | 修正"送回营地/实回世界地图"的文案矛盾；与 D4（无硬核压力）一致 |

**尚未裁决**：AP 使用细则（C2-11/12/13）、buff 生效路径（C2-15）、经验双重倍率（C2-16/17）、`escalation:'permanent'` 是否启用（C2-10）、`element` 克制（C1-6）。

### 第 3 轮（2026-09-17）

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| D9 | **AP 规则漏洞** | **补 AP 校验**（`submitAction` 拒绝 AP 不足）+ **同一单位每回合限出手 1 次**；**但保留"AP=0 时无法防御/道具/逃跑"**（不修）。 | 规则漏洞①③修掉；②按原样保留 → **AP 归零即自动让出我方阶段，玩家无法手动防御**，这是有意的设计取舍。需同步改 UI 自动切阶段逻辑 |
| D10 | **buff 生效范围** | **只保留攻击类 buff，其余全部删除**（耐力 / 魔力 / 敏捷 / 幸运 / 灵性 类 buff 及其技能效果一并移除）。 | ⚠️ **见下方副作用警告**——会波及大量纯辅助技能，必须先做内容影响面普查 |
| D11 | **属性系统** | **删除 `element` 字段**。承认不做属性克制，技能表移除该字段及其 UI 展示。 | 88 条技能需删字段；`SkillElement` 类型与相关 UI 一并清理（§10-13 转为「待实现」） |
| D12 | **永续升耗** | **启用**，配给 **power 最高的几个技能**（形成"越用越贵"的取舍）。 | `escalation:'permanent'` 需正式配置；`permStack` 机制已在代码中就绪；需定"哪些技能算高倍率大招"的阈值 |

> ### ⚠️ D10 的副作用警告（必须在下一轮处理）
>
> 技能表里 **28 条 `buff` + 14 条 `debuff`**，其中**非攻击类占绝大多数**。删除它们会直接掏空一批技能：
>
> - **纯辅助技会变成空壳**：例如「白金礼赞」`power=0`，唯一效果是 `buff spirit+8` —— 删掉后就**什么都不剩**（一个消耗 AP 却无任何效果的技能）。同类还有「茶点侍奉」等。
> - **敌方 debuff 同属无效**：敌方只有 `attack`/`defense` 参与计算，所以 `debuff spirit/magic/agility` 对敌人**本来就不产生任何效果**。
> - **含混合效果的技能会"半残"**：如「不落壁垒」= `buff endurance+20` + `heal 60`，删掉 buff 后只剩治疗。
>
> 因此 D10 落地前必须回答：**这些被掏空的技能是改造（改成攻击/治疗/削弱敌方防御），还是直接删除？**（已列为 C2-15b）

### 第 4 轮（2026-09-17）

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| D13 | **被 D10 掏空的技能如何处置** | **改造成有效技能**：改成攻击 / 治疗 / **削弱敌方 `attack`·`defense`**（敌人只有这两个属性参与计算，所以只有这两种减益真实有效）。 | 解决 C2-15b。需对 88 条技能逐条重配；保留技能数量与角色辨识度 |
| D14 | **敌人随层缩放的形状** | **线性缩放**：第 N 层 HP `×(1+0.5×(N−1))`、攻 `×(1+0.25×(N−1))`、防 `×(1+0.3×(N−1))`，奖励小幅上浮。**系数写在 `mazeDifficulty.ts` 做成可调项**。 | 解决 C1-7。落地时需同时按层切换敌人池（D7 的另一半） |
| D15 | **`apCost` 分档阈值** | **按 `power` 阈值**：`≤60 → 1 AP`；`61–120 → 2 AP`；`>120 → 3 AP`（`hits≥2` 按等效 power 折算）。 | 支柱① 的成立前提。80 条技能可机械批量重配 |
| D16 | **目标受众与单次时长** | **中度核心**，单次 **40–90 分钟**（一次地牢 6 层下探需完整规划）。 | §1.3 定稿 |

> ### ⚠️ D16 与 D8 的张力（必须解决）
>
> D16 选了「中度核心」——我在选项里已注明它「**需要重新评估 D8 的轻惩罚是否合适**」。而 D8 刚决定**团灭不扣金币、只清探索进度**。
>
> 中度核心定位通常需要「失败的代价足以让人在意」，否则 40–90 分钟的规划投入缺少风险对价。**两条裁决目前不自洽**，需在第 5 轮二选一或折中（例如：不扣金币，但改为「本层进度回退」或「损失本层已拾取的道具」）。

**尚未裁决**：`escalation:'permanent'` 的分配阈值（C2-10 的细则）、经验双重倍率（C2-16/17）、D8 与 D16 的张力（C1-3 复议）、经济预算（C3-21/22）、UI 规格矛盾（C4-29~33b）、音频/引导（C1-8）。

### 0.5.1 文档复核记录

| 日期 | 复核范围 | 结论 |
|---|---|---|
| 2026-09-17 | **§8 美术/UI/UX + §0.3 常量归属**（独立第三方对照两份规格书与 `constants.ts` 逐条核验） | 发现 **9 处问题并已全部修正**：① `wall` 零使用却写成"面板底板"；② `gold` 的"宝箱"用途已过期；③ `hp` 被误并入渐变端点；④ 相机缩放公式三处偏差（分母应为最大边长、round 是保留两位小数、夹取在取整前）；⑤ **§8.6 两条"已知冲突"在规格书里零命中——属臆造**，已替换为 5 条真实冲突（新建 §8.7）；⑥ 漏第三档立绘 `full` 512×1024（与 §2.1「40×3」自相矛盾）；⑦ 漏 `fogUnknown/fogExplored`；⑧ 动效表漏规格书已成文项、且有一行"按规格"是编造；⑨ §8.3 漏布局常量归属（`TILE`/`DPAD_MARGIN`/`FADE` 在 `DungeonScene.ts`，不在 `constants.ts`）。<br>**核对无误**：§8.2 全部**色值**逐条正确、§8.4 双相机、§8.5 交互约定、§8.6 的升耗视觉、§8.3 的 `TOPBAR_H=64`/`TILE=34`/方向键几何。 |

> **教训**：逆向整理型文档必须做**独立复核**。"色值抄对了"不代表"用途写对了"——本次 3 处错误全在用途栏，而非数值栏；另有 2 条冲突是**跨文件串味**（把别处的条目安到了规格书头上）。

### 第 5 轮（2026-09-17）

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| **D17** | **团灭代价复议**（调和 D8 与 D16） | **本图进度全部消失** + **背包物品损失（约合总价值 50%）**。**不再**只清本层——整张地牢的进度全清；资产层面从"不扣金币"升级为"损失背包物品"。 | ⚠️ **有歧义待澄清**，见下方注。金币不再直接扣，但资产损失以物品形式回归 → 与 D16「中度核心」自洽 |
| D18 | **永续升耗配给范围** | **只给少数最强技能**（人工挑 **5–8 条**），保持稀缺感。 | 需人工逐条挑选；`power` 最高的几条（花魁杀 200、死神镰刀 205 等）是候选 |
| D19 | **经验倍率双重计算** | **只保留个体倍率**（`expMultiplierFor(card)`），**去掉队伍层**倍率。 | ⚠️ 修正后升级节奏会**明显变慢** → 需重算 D16 下"单次 40–90 分钟能升几级"。副作用：`team.expMultiplier`（Σ魔力×0.02）变为**死数据**，魔力成为**纯个体属性** |
| D20 | **科技点经济** | **组合方案**：① **下调科技树总价** ② **提高科技点产出** ③ **产出与难度挂钩**（深层给得更多）。 | 需重算：科技树 16 条新定价 + 敌人 `techPoints` 随层缩放（与 D14 的奖励上浮合并处理） |

> #### ⚠️ D17 待澄清的两点（下一轮必须问）
>
> 你的原话是「**本图进度全部消失，背包里的物品丢失 50% 价值的物品**」。这两处我按最合理的方式解读了，但都需要你确认：
>
> 1. **「本图进度全部消失」的范围**：是指**该地牢整张图的进度**（含 `maxFloor` / `cleared` / `activeRun` 全清，下次从第 1 层重来）？还是仅清 `activeRun`（位置与已消耗元素）但保留 `maxFloor` 与 `cleared` 记录？
> 2. **「丢失 50% 价值的物品」的算法**：是 ① 按**总价值**随机挑物品直到凑够背包总价的一半？还是 ② **每件物品各有 50% 概率**消失？两者的实际损失额差别很大（前者每次恰好一半，后者是二项分布、方差大）。
> 3. 附带：**材料类道具（魔铁矿/深渊结晶/魔核）算不算在内**？它们没有使用途径、只能卖钱，损失它们约等于扣金币。

### 第 6 轮（2026-09-17）—— D17 澄清 + 经济定标 + UI 规格首项

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| **D21** | **D17 澄清①：进度的消失范围** | **全清，但保留「已通关」记录**：`maxFloor` 归零、`activeRun` 清空，`cleared` **保留**作成就。 | D17 定稿前半句。下次进这张图从第 1 层重来，但"我通关过"的记录不会被抹掉 |
| **D22** | **D17 澄清②：物品损失算法** | **按总价值凑一半**：随机挑物品直到累计价值达背包总价 **50%**。**材料类计入**（未排除）。 | D17 定稿后半句。损失额稳定可预期；⚠️ 材料计入意味着"变相扣金币"的成分仍在（D8 已由 D17 取代，故不冲突） |
| **D23** | **科技树经济定标** | **首次全清点满约 80%**：总价降至 **约 80 点**；四图首通合计 **约 45–50 点**；其余靠战斗（`techPoints` **随层缩放**，与 D14 奖励上浮合并）。 | 解决 C3-21。需重定 16 条科技的价格；需给敌人 `techPoints` 加层数缩放 |
| **D24** | **战斗角色卡尺寸** | **168×340**（`battle-ap-ux-spec.md` 值）。 | ⚠️ **要改代码**：现值 `BattleScene.ts:46-47` 是 168×400，且 `CONSOLE_H` 当初是为 400 加高的 → 需**回调 `CONSOLE_H` 与卡内元素坐标**（半身像、名字带、buff 行、技能条） |

> ⚠️ **D23 勘误（第 101 轮 · 落地实测）**：D23 原文「总价降至 **约 80 点**」里的 **80 是"首次全清四图的达成率目标"，不是科技树的总价** —— 同一个数字被用在了两个语境（达成率 vs 总价）。**树的总价落地为 99 点（21 节点）**（见 §13.3 / §14：`src/data/tables/tech.json` 的 21 条 `cost` 合计 **99**），**实测首清达成率 76.3 / 99 = 77.1%**，落在 D23 自定的 **75–85%** 带内（D297 把总价细化为 99，原文"约 95"作废）。⇒ **80 保留为达成率目标，不改**；总价一律以 **99** 为准。

> #### 📌 D23 / D24 的连带效应
>
> - **D23**：科技树 16 条要重新定价（现 3–30 点不等，合计 166）；敌人 `techPoints` 加层缩放会和 D14 的"奖励小幅上浮"重叠，**必须一起算**，否则会双重放大。
> - **D24**：这是**第一项落地的 UI 规格裁决**，它同时解决了 §8.7-1 的三方冲突。但要注意 `ui-style-spec.md:164` 的 96×128 仍留在规格书里，需一并标注废弃，否则以后还会有人照它改。

**尚未裁决**：§8.7 余下四项 UI 冲突（AP pip / 技能栏形态 / 卡牌抬升时长 / 迷宫色名）、D19 的升级曲线重算、音频与引导（C1-8）、备战成员经验（C2-17）。

### 第 7 轮（2026-09-17）—— 清空 UI 规格冲突 + 备战经验

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| **D25** | **AP pip 形态** | **菱形 20×20，两排 5+5 共 10 槽**（BUX 规格）；未解锁的第 9/10 槽画锁形 + 虚线描边。 | ⚠️ **需改代码**：现值 10 槽用的是**平行四边形**（`BattleScene.ts:175`），要改成菱形。规格书两边需统一成这一条 |
| **D26** | **战斗技能栏形态** | **156×46 横向条**（代码现值）：左技能名 + 右动态 AP 消耗 + 左 3px 语义色条，**无图标格、无扇形、无 MP 角标**。 | ✅ 不改代码，改规格书去对齐代码。`ui-style-spec.md` 的 36×36+冷却扇形应标废弃（**冷却已废除**） |
| **D27** | **卡牌抬升时长** | **120ms** `Quad.easeOut`（回落 150ms）。 | ⚠️ **见下方注意**——这项可能作用于一个已不存在的机制 |
| **D28** | **备战成员是否分经验** | **不分（维持现状）**：备战栏永远 0 经验，换人即等级断层。 | 与 D19（升级变慢）叠加后，**换人成本进一步升高**；这是有意强化"出战才有成长"的取舍 |

> #### ⚠️ D27 的注意点：抬升机制可能已无依附对象
>
> 「卡牌抬升 / 高亮」在原设计里属于**「当前行动者」三件套**（`ui-style-spec.md` §3.4）。但本项目已改为**双方阶段制**——我方阶段**全员都可行动、没有"轮到谁"**，`currentActor()` 在我方阶段**恒返回 null**。
>
> 也就是说：**「当前行动者的卡牌抬升」这个动作，在阶段制下没有触发对象。** 因此 D27 定了 120ms，但要先回答前置问题（已列入 C2-15d）：
>
> - 阶段制下**还要不要**高亮某个单位？（比如"最后一次出手的单位"或"当前选中待操作的单位"）
> - 若不要，则 §3.4 的整段行动者三件套（抬升 8px / 切角描边 2px / 行动角标 16×16）应整段废弃，D27 自动失效。

**尚未裁决**：C2-15d（阶段制下要不要保留行动者高亮）、音频与引导（C1-8）、D19 升级曲线重算（已派核算）、迷宫色名（C4-33b）。

### 第 8 轮（2026-09-17）—— 核算结果引发的裁决

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| **D29** | **D16 单局时长修正** | **改为 30–60 分钟**（原 40–90）。 | 与实测对齐（一次 6 层下探 25–60 分钟，中位 35–40）。若要接近 90 分钟上限，走"一次出门连打 2 个地牢" |
| **D30** | **阶段制下的高亮（C2-15d）** | **只保留「已出手」瞬间反馈**：单位出手时短暂闪一下（120ms 抬升后立即回落），**不表示"轮到谁"**。 | 原「当前行动者」三件套（常驻抬升 8px / 切角描边 2px / 行动角标 16×16）**废弃**；D27 的 120ms 应用于这个瞬时反馈 |
| **D31** | **音频 vs 新手引导** | **先做音频，引导暂缓**。 | 设置里已有音量滑杆但**无音频资源**（`public/assets/audio` 空、`BootScene` 只做 pause/resume、全项目无 `sound.play`）→ 先接 BGM + 基础音效 |
| **D32** | **升级曲线调整（澄清后）** | ✅ **让升级需求涨得更陡，压后期** —— `expExponent` **1.6 → 1.8**（待实测复核）。目标是把腐朽王座一次下探的 **8.56 级**压下来。 | ⚠️ **已知副作用：前期缺口会进一步恶化** —— 无根回廊 ★4 会从 1.05 级降到约 **0.8 级**、★5 从 0.89 降到约 **0.7 级**（零成长率上升）。<br>⚠️ 升级需求对照（粗算）：L5 需求 263→**362**、L10 需求 796→**1262** |

> **已由第 9 轮 D33 定案**：实测 1.8 不达标（只到 6.64），最终取 **1.9 + 方案 A**，见下方。

### 第 9 轮（2026-09-17）—— 升级曲线定案

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| **D33** | **升级曲线定案** | ✅ **采纳 `expExponent = 1.9` + 方案 A**（无根回廊 `enemyDensity` 0.035→**0.060**，黏液怪/蝙蝠/巨鼠 exp 14/12/16→**20/17/23**） | 腐朽王座 8.59 → **5.88**（达成 5–6 目标）；无根回廊 ★4 1.00 → **2.00**、★5 0.19 → **1.25**；全档位零成长率 **0%**；静谧/腐朽数字不变。**纯改配表，零代码** |
| **D34** | **前期台阶** | ✅ **提高苔痕回廊密度**（而非降低无根密度），让第二张图重新明显高于第一张 | ⚠️ **具体数值待补扫**（已派核算）；约束：不能动到静谧 3.64 / 腐朽 5.88 |

> **D33 落地清单（纯配表，改完跑 `npm run gen:tables`）**：
> 1. `src/data/tables/balance.json`：`expExponent` `1.6` → `1.9`
> 2. `src/data/tables/dungeons.json`：无根回廊 `enemyDensity` `0.035` → `0.060`；苔痕回廊 `enemyDensity` 待 D34 补扫后定
> 3. `src/data/tables/enemies.json`：`enemy_slime` exp `14`→`20`、`enemy_bat` `12`→`17`、`enemy_rat` `16`→`23`
>
> ⚠️ `expExponent` 是**全域旋钮**，影响所有角色；三小怪 exp 上调会轻微影响苔痕回廊（其池含蝙蝠 15 / 巨鼠 20 权重），实测仅 +0.04 级。

### 第 10 轮（2026-09-17）—— 苔痕密度定案

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| **D35** | **苔痕回廊 `enemyDensity`** | ✅ **0.045 → 0.064**（★4 升级数 2.01 → **2.75**） | 四图 ★4 变为 **2.00 / 2.75 / 3.64 / 5.88**，相邻差值 **+0.75 / +0.89 / +2.24** —— **严格单调递增**，台阶回来了 |

**扫描数据（前提：expExponent 1.9 + 方案 A 全程生效，2000 次/组）**：

| 苔痕密度 | 0.045 | 0.055 | **0.064** | 0.070 | 0.075 | 0.095 | 0.120 |
|---|---|---|---|---|---|---|---|
| 苔痕 ★4 | 2.01 | 2.23 | **2.75** | 2.93 | 3.00 | 3.58 | 4.05 |
| 相邻差值（苔痕→静谧） | — | — | **+0.89** | +0.71 | **+0.64** | — | — |

- **为什么不是 0.075**：0.075 的四段差值是 +1.00 / **+0.64** / +2.24 —— 第二段反而变小，**台阶倒挂**。0.064 是唯一"既落在 2.6–3.0 中段、又让四段差值严格递增"的舒适点（可接受带 0.062–0.066）。
- **三条约束实测通过**：静谧 3.6434 / 腐朽 5.8779 在全部 18 个密度档下**四位小数完全一致、零漂移**；苔痕零成长率 **0.0%**；未顶到敌人上限（0.064 下触及 `floorTiles×0.12` 上限的地图占比 **0.0%**，而 0.085 起开始触顶，0.120 时达 78%）。

**❌ 备选方案已实测否决：上调苔痕池内中阶敌人 exp**

| 哥布林/骷髅兵/食尸鬼 exp 倍率 | 1.0 | 1.4 | 1.6 | 2.0 |
|---|---|---|---|---|
| 苔痕 ★4 | 2.01 | 2.45 | 2.78 | 3.07 |
| **静谧墓所 ★4** | 3.6434 | **3.9850** | **4.1182** | **4.4043** |

> **根因**：静谧墓所的敌人池**共享骷髅兵（22%）与食尸鬼（22%）**，调它们的 exp 会把静谧从 3.64 推到 3.83–4.40，违反"静谧/腐朽必须不变"的约束。**密度是唯一干净的杠杆。**

**⚠️ D35 的已知副作用（已接受）**：0.064 下苔痕回廊一趟的总战斗数会超过其他三张图（无根 38.2 / **苔痕 41.9** / 静谧 31.9 / 腐朽 34.7），即"**第二张图怪最多、跑图最长**"。这是"用密度拉苔痕"的必然结果，无法在不动 exp 的前提下规避。

> **D33 + D35 完整落地清单（纯配表，改完跑 `npm run gen:tables`）**：
> 1. `balance.json`：`expExponent` `1.6` → `1.9`
> 2. `dungeons.json`：无根 `enemyDensity` `0.035` → `0.060`；苔痕 `0.045` → `0.064`
> 3. `enemies.json`：黏液怪 exp `14`→`20`、蝙蝠 `12`→`17`、巨鼠 `16`→`23`

### 第 11 轮（2026-09-17）—— UI 规格收尾 + 引导与音频定案

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| **D36** | **迷宫色名冲突** | ✅ **改规格书对齐代码**：`mazeRoad` / `mazeWall` / `mazeVoid` 为唯一真源；把 `ui-style-spec.md:36-38/238-239` 描述迷宫的 `floor/floorAlt/wall` 改成 `mazeXxx`，并标注 **`wall` 废弃**（全项目零使用） | 需编辑 `docs/art/ui-style-spec.md`。`floor`/`floorAlt` **保留**——它们是 UI 面板底板（`BattleScene:308` 等 6 处） |
| **D37** | **升耗视觉提示** | ✅ **补写成正式规格**（写进 `battle-ap-ux-spec.md`）：数字转 `gold` 金色 + 右上角 **9px `+N`** 角标 + AP 不足时转 `danger` 红并加 **2px 斜线** + 技能条降为 `SPENT_FILL #272C35`（左侧色条 alpha 1→0.35） | 需编辑 `design/ux/battle-ap-ux-spec.md`。**零代码改动**——当前实现已如此，只补文档 |
| **D38** | **目标引导** | ✅ **加任务列表面板**（完整任务 / 目标列表，如"通关无根回廊 3/6 层"） | 需新增 UI 面板 + 任务数据结构；信息最全、改动最大 |
| **D39** | **音频范围** | ✅ **BGM + 核心音效**：地牢 / 战斗 / 营地 **3 首 BGM** + **UI 点击 / 技能释放 / 受击 / 升级 / 获得道具** 5 类音效 | 需新建 `public/assets/audio/` 资源 + `BootScene` 加载 + 各场景 `sound.play`；音量滑杆已存在 |

> **D36 / D37 是"补文档"，不改代码**；D38 / D39 是新增实现。

### 第 12 轮（2026-09-17）—— 内容扩充与工程收尾

| # | 议题 | 裁决 | 影响面 |
|---|---|---|---|
| **D40** | **Boss 来源** | ✅ **全部新增 4 个专属 Boss**（每个含专属技能与掉落）；现有 3 个高 HP 敌人（迷宫的守护者 / 骸骨暴君 / 深渊之王）**降为精英怪** | 需新增 4 条敌人 + 若干专属技能 + 掉落配置；内容量最大但辨识度最好 |
| **D41** | **科技树定价** | ✅ **等比缩放 ×0.48**（166 → 79 点），保留原有相对价值关系 | 仅改 `tech.json` 的 `cost`；下方给出逐条具体值 |
| **D42** | **死字段与孤儿模块** | ✅ **全部清理**：删 `skills` 的 `cooldown`/`cost`、`enemies` 的 `speed`、`balance` 的 `baseSpeed`/`levelUpTechPointBonus`，并删除无人 import 的 **`src/entities/` 整个目录** | 属附录 B 的整体执行；需同步清理 `types/` 里的对应字段与读取它们的死逻辑 |
| **D43** | **新手引导** | ✅ **上下文触发提示**：首次遇到某机制时弹一次说明（第一次开战斗讲 AP、第一次踩陷阱讲陷阱…） | 需新增"首次触发"标记与提示文案；比开场集中说明更有效，但改动稍大 |

**D41 逐条定价（`tech.json`，×0.48 后四舍五入）**：

| 科技 | 现价 | **新价** | | 科技 | 现价 | **新价** |
|---|---|---|---|---|---|---|
| 队伍扩编 I | 5 | **2** | | 疾行训练 | 7 | **3** |
| 招募许可 I | 3 | **1** | | 幸运徽记 | 7 | **3** |
| 深层探索 | 8 | **4** | | 秘法研习 | 7 | **3** |
| 战术扩容 | 10 | **5** | | 灵性启迪 | 7 | **3** |
| 队伍扩编 II | 12 | **6** | | 招募许可 II | 10 | **5** |
| 奇袭节拍 | 14 | **7** | | 深渊入口 | 18 | **9** |
| 兵刃淬炼 | 6 | **3** | | 远征装备 | 16 | **8** |
| 重甲锻造 | 6 | **3** | | 深渊核心 | 30 | **14** |

> **合计 166 → 79 点**（目标 ≈80 ✓）。⚠️ 缩放后属性科技普遍只要 3 点，功能解锁 1–7 点，地牢解锁 4–14 点 —— 若之后觉得"科技点太不值钱"，可再整体 ×1.5 回到 ~120 点。

### 第 13 轮（2026-09-17）—— 工程与内容收尾

| # | 议题 | 裁决 |
|---|---|---|
| D44 | **README.md** | ✅ **按 GDD 重写**设计章节（现为 v1.0：10 职业/15 角色/60 固有技能/普攻/MP/速度单动/图鉴，与现状全面冲突） |
| D45 | **根目录调试产物** | ✅ **全部清理**（`scan-*` / `tmp-*` / `answer52.txt` / `task28-*` / `gen-out.txt` / `zz-*.log` 等约 25 个文件） |
| D46 | **存档导入/导出** | ✅ **接 UI 入口**（主菜单或营地设置；`exportJson/importJson` 已实现） |
| D47 | **定时自动存档** | ⚠️ **后被 D68 修正**：不做定时存档，仅事件触发；但 **`playTime` 计时仍需接线**（现恒为 0，而 UI 处处显示"游戏时长"） |

### 第 14 轮 —— Boss 具体设计（落实 D40）

| # | 议题 | 裁决 |
|---|---|---|
| D48 | **Boss 强度定位** | ✅ **明显更长但不秒人**：等级 ≈ 该图推荐等级 **+4**，HP ≈ 同层普通敌人的 **8–10 倍** |
| D49 | **Boss 掉落** | ✅ **必得专属掉落**（该图"核心"材料 + 大额科技点），作为通关里程碑的实质回报 |
| D50 | **Boss 战逃跑** | ✅ **禁用逃跑**（现任何战斗 3 次失败后成功率即 90%+，会让 Boss 战形同虚设） |
| D51 | **Boss 技能** | ✅ **每个 Boss 2–3 个全新专属技能**（不复用现有 8 个敌方技能），辨识度优先 |

### 第 15 轮 —— 内容扩充

| # | 议题 | 裁决 |
|---|---|---|
| D52 | **敌人数量** | ✅ **大幅扩充到 30+**（现 14 个；配合 D7「按层切池」与 D48 的 4 个 Boss） |
| D53 | **大地图节点类型** | ✅ **补上商店与事件节点**（`shop` / `event` 两种从未使用；`boss` 类型因 D2 已无用，应删除） |
| D54 | **三个材料** | ✅ **加锻造系统**（魔铁矿/深渊结晶/魔核 按配方合成道具或强化；消除"描述写锻造材料但无锻造"的空头承诺） |
| D55 | **角色技能数** | ✅ **保持 2 个**（共 80 条；角色差异靠技能组合与属性，不加第 3 技） |

### 第 16 轮 —— 战斗细节

| # | 议题 | 裁决 |
|---|---|---|
| D56 | **`combo_bonus` 空转** | ✅ **修复让它生效** ⚠️ 需改伤害流程：命中总数在循环开始前已定完，要改成**可追加**，否则逻辑上不可达 |
| D57 | **敌方 AI** | ✅ **重做并搬进配表**：<br>• **只放技能，不再普攻**<br>• **普通敌人**：每轮使用 **1–2 次**技能<br>• **精英敌人**：每轮 **2 次**技能，且**每 3 轮**额外增加 1 次威力巨大的技能<br>• **Boss**：不遵循上述规律，**每个 Boss 有独一无二的专属技能释放顺序** |
| D58 | **「迟缓」陷阱** | ✅ **删除该类型**（陷阱改为 伤害 / 传送 各 50%） |
| D59 | **战斗道具** | ✅ **改为消耗 1 AP**（与技能统一进 AP 经济，堵住无限嗑药） |

### 第 17 轮 —— 迷宫探索

| # | 议题 | 裁决 |
|---|---|---|
| D60 | **宝箱金币** | ✅ **跟难度档 N1–N6 挂钩**（与迷宫尺寸/拓扑同源，改 `mazeDifficulty.ts`）；弃用 `config.floor`（4 张图全是 1，导致金币恒为 45） |
| D61 | **埋伏选敌** | ✅ **改为按权重随机**（现恒取 `pool[0]`，永远是最弱的怪） |
| D62 | **可复现性** | ✅ **传送改用项目 Rng**（现用 `Math.random()`，破坏"同种子同结果"） |
| D63 | **层元素重生** | ✅ **保持下层重生**（宝箱/陷阱/事件/存档点重新生成，保证每层能补给） |

### 第 18 轮 —— 角色与招募

| # | 议题 | 裁决 |
|---|---|---|
| D64 | **重复角色** | ✅ **获得重复角色 → 该角色升 1 级**（非分解、非突破） |
| D65 | **`rarityWeights` 死权重** | ✅ **补上 1★ / 2★ 角色模板**，让权重表完整生效（现 70% 权重从未生效） |
| D66 | **稀有度价值** | ✅ **不动数值**，靠**技能倍率**体现（接受 3★ 可能强于 5★ 的随机性） |
| D67 | **招募定价** | ✅ **固定 500 金币一次**，**取消 `1.35^n` 递增**（科技点 2 沿用） |

### 第 19 轮 —— 存档与系统

| # | 议题 | 裁决 |
|---|---|---|
| D68 | **存档频率（修正 D47）** | ✅ **仅事件触发，不做定时自动存档**；但 `playTime` 计时仍需接线 |
| D69 | **战斗中存档** | ✅ **不支持**，退出后退回战斗开始前（保持现状） |
| D70 | **存档槽** | ✅ **改为命名存档列表**（不再是固定 3 槽） |
| D71 | **主菜单续档** | ✅ **保持现状**（不自动续档，无进度提示） |

### 第 20 轮 —— 整体范围（第 20 轮检查点）

| # | 议题 | 裁决 |
|---|---|---|
| D72 | **内容量目标** | ✅ **先做模板/结构，后续可随意扩建** —— 不为当前数量纠结，优先保证可扩展 |
| D73 | **全局难度系统** | ✅ **新增 4 档难度 + 噩梦 4 个可选机制**（详见下表）—— ⚠️ 这是**全新系统**，此前 GDD 记载为"无难度选择" |
| D74 | **本地化 i18n** | ✅ **不做**（文案继续硬编码在代码与配表） |
| D75 | **推进方式** | ✅ **继续提问**（第 20 轮检查点后继续第 21–30 轮） |

#### D73 全局难度系统（新增）

| 难度 | 敌人属性 | 金币 / 经验 |
|---|---|---|
| **简单** | **×0.75** | **×1.25** |
| **普通** | ×1.0 | ×1.0（当前基准） |
| **困难** | **×1.25** | **×0.75** |
| **噩梦** | 在困难基础上叠加 | 在困难基础上叠加 |

**噩梦难度的 4 个可选额外机制**（玩家可自选触发，可组合）：

| # | 代价 | 补偿 |
|---|---|---|
| 1 | 埋伏事件时**敌人先动** | 埋伏事件的金币与经验 **×1.5** |
| 2 | 商店物品价格 **×2** | 商店**额外 +4 个商品栏位**，且稀有度上升 |
| 3 | 敌方属性**额外 ×1.5** | 己方每回合 AP 回复**变为 3**（基础为 2） |
| 4 | 招募价格 **×2** | 重复角色的等级提升 **1 → 3**（联动 D64） |

> ⚠️ **连带影响**：
> - 机制 2 依赖 D53 的**商店节点**，须先落地商店
> - 机制 3 与 D9（每单位每回合限 1 次出手）、D15（apCost 分档）叠加后，AP 经济的宽松度会明显变化，需重新验证
> - 机制 4 联动 D64（重复角色升级）
> - 「敌人属性 ×0.75 / ×1.25」与 D14 的**按层线性缩放**是**乘算**关系，需注意困难难度下 N6 的强度是否会失控

### 第 21 轮（2026-09-17）

| # | 议题 | 裁决 |
|---|---|---|
| D76 | **4 个新 Boss 的身份** | ✅ **各自呼应地图主题**（藤蔓 / 孢子 / 亡骸 / 深渊），彼此独立，不做统一叙事 |
| D77 | **锻造产出什么** | ✅ **材料打造装备 —— 引入装备系统**（不是合成消耗品） |
| D78 | **商店卖什么** | ✅ **消耗品为主 + 随机折扣**（每次进店随机 1–2 个折扣商品）；与 D73 噩梦机制 2 的"+4 栏位 / 稀有度上升"兼容 |
| D79 | **难度在哪选 / 能否改** | ✅ **新游戏时选择，之后锁定**（含噩梦 4 机制也一并锁定） |

### 第 22 轮 —— 装备系统骨架（落实 D77）

| # | 议题 | 裁决 |
|---|---|---|
| D80 | **装备槽位** | ✅ **只有 2 个槽：饰品槽 + 消耗品槽**。**不设立武器 / 防具等其它装备类型** |
| D81 | **装备属性** | ✅ 按 D80 的分类设定（见下） |
| D82 | **装备获取** | ✅ **锻造 + Boss 掉落 + 商店** 三条途径 |
| D83 | **装备强化** | ✅ **不可强化**（属性固定，想更强就去打更强的 Boss 或锻造更好的） |

**D80 详细设定**：

- **消耗品槽**：装消耗品，作为**主动物品**在战斗中使用。
  - **使用次数 = 背包中该物品的数量**
  - **每次使用仅消耗 1 AP**（与 D59「战斗道具消耗 1 AP」一致）
- **饰品槽**：装饰品，**被动生效**。饰品的**功能划分为 4 类**：

| 类 | 功能 |
|---|---|
| 1 | **属性加成**（六维） |
| 2 | **连击率 / 暴击率 / 强化率加成**（对应三条概率曲线） |
| 3 | **战术效果**（其它机制性效果） |
| 4 | **每回合生效 1 次** |

> ⚠️ 这是与常见 RPG 不同的设计：**没有武器 / 防具**，角色的攻防仍完全来自六维（攻击 = Σ筋力×5、生命 = Σ耐力×20）。饰品只做"加成与特效"，**不改变攻防的基本结构**。

### 第 23 轮

| # | 议题 | 裁决 |
|---|---|---|
| D84 | **饰品数量** | ✅ **先做 12 件**（四类各 3 件），建立模板，后续按 D72 随意扩建 |
| D85 | **饰品稀有度** | ✅ **沿用现有 1–5 星**体系（与角色、道具一致） |
| D86 | **消耗品槽规则** | ✅ **每角色一个槽，每场战斗使用次数不限**（受背包数量与 1 AP/次 约束） |
| D87 | **4 个 Boss 的数值** | ✅ **先定名字与技能释放顺序，数值后补**（待 D52 敌人扩充与 D73 难度系统确定后统一填） |

### 第 24 轮

| # | 议题 | 裁决 |
|---|---|---|
| D88 | **锻造配方** | ✅ **每件饰品独立配方**（指定材料与数量），不按稀有度套模板 |
| D89 | **30+ 敌人的分配** | ✅ **每图独立敌人池 + 池内按层分两组**（浅层组 / 深层组），配合 D7 的按层切池 |
| D90 | **商店定价** | ✅ **按标价原价卖**（买价 = `price` × 1.0，背包卖出 = `price` × 0.5），形成 2 倍买卖差价 |
| D91 | **任务面板内容** | ✅ **主线 + 支线任务**（主线 = 击败该图 Boss；支线 = 首次通关第 N 层、招募 N 名角色、锻造 N 件饰品等） |

### 第 25 轮

| # | 议题 | 裁决 |
|---|---|---|
| D92 | **任务奖励** | ✅ **发金币 + 科技点**（成为 D23 科技点产出的一部分，缓解"166 点只能靠刷"的问题） |
| D93 | **新敌人设计方向** | ✅ **每图一个主题族群**（如丛林兽群 / 真菌孢子 / 亡骸军团 / 深渊眷属），每族 **7–8 只**，视觉与技能呼应地图 |
| D94 | **商店刷新** | ✅ **每次进店刷新**（商品与折扣都刷新） |
| D95 | **战斗道具 UI** | ✅ **角色卡上显示携带的消耗品与剩余数量，点击即用**（消耗 1 AP），与技能横条并列 |

### 第 26 轮

| # | 议题 | 裁决 |
|---|---|---|
| D96 | **BGM 风格** | ✅ **氛围 / 压迫向**（低频脉冲 + 稀疏音型，强调迷宫的孤独与压迫），与"零渐变零发光"的冷峻视觉一致 |
| D97 | **音效清单** | ✅ **按 D39 的五类**：UI 点击 / 技能释放 / 受击 / 升级 / 获得道具 |
| D98 | **上下文引导触发点** | ✅ **覆盖四项反直觉机制**：首次进入战斗（AP 与升耗）、首次下探（迷雾与层）、首次踩陷阱、首次招募（潜力） |
| D99 | **数值复核** | ✅ **已完成**（原派 general-purpose-2 因 429 配额失败，改由主代理自建脚本跑完）。结论见 **§0.6**：D73×D14 乘算**未触发任何警戒线**，但原因是战斗本身 1 回合结束、难度倍率被淹没；真正的结构性问题是「我方单回合输出 ∝ N²」 |

### 第 27 轮（本轮达成第 100 条裁决 D100）

| # | 议题 | 裁决 |
|---|---|---|
| D100 | **可访问性** | ✅ **文本缩放 + 高对比模式**（全局字号 100%/125%/150%；高对比增强描边与文字对比）。⚠️ 与 §8.1「切角代替圆角、零渐变」不冲突，只增强可读性 |
| D101 | **文本呈现** | ✅ **逐字显示，可跳过**（战斗日志与对话；点击或按键立即全出） |
| D102 | **战斗结算界面** | ✅ **完整结算**：获得经验（含每人升级情况）、金币、科技点、掉落道具 |
| D103 | **背包上限** | ✅ **不设总格数上限**（继续按 `stackLimit` 堆叠；D54 锻造会增加材料囤积，但不设限） |

> ⚠️ **D100 的连带**：文本缩放会影响 §8.3 的固定字号（28/20/14/15/13/11-12/16）与固定布局尺寸（`TOPBAR_H=64`、卡片 168×340 等）。落地时需确认哪些尺寸改为"随缩放系数联动"，否则放大后布局会溢出。

### 第 28 轮

| # | 议题 | 裁决 |
|---|---|---|
| D104 | **防御指令改造** | ✅ **保留指令 + 消耗 1 AP + 效果改为「本回合受到的伤害 ×0.8」**（替代当前"不耗 AP、伤害减半"）。⚠️ 见下方两条连带 |
| D105 | **统计与成就** | ✅ **只做统计，不做成就**。统计项：游戏时长 / 通关次数 / 击败数 / 金币 / 最高层 |
| D106 | **图鉴 CodexPanel** | ✅ **不恢复**（维持现状，不投入修复或重做） |
| D107 | **营地新设施** | ✅ **训练场**（用**金币换经验**），成为 D23 之外金币的第二条主要出口 |

> ⚠️ **D104 连带一（机制不是"减半→八折"这么简单）**：当前代码里 `defendReduction`（`balance.json` = 0.5）**不是独立伤害乘区**，而是 `DamageCalculator.ts:231` 的 `target.defense + (targetDefending ? balance.defendReduction : 0)` 再 `clamp(0, 0.95)`——即**加到防御率上的加法项**。因此：
> 1. 减伤效果随角色自身防御率**递减**（防御率 ≥0.45 的单位防御后直接顶到 0.95 上限，收益被 clamp 吃掉）；
> 2. 战斗日志 `CombatSystem.ts:430` 输出的"伤害降低 50%"是**名不副实**的文案，实际减伤通常低于 50%。
> 改为「×0.8」时**必须新增一个独立的伤害乘区**（在 clamp 之后作用），否则改动会被 clamp 吞掉、玩家感知为零。
>
> ⚠️ **D104 连带二（与 D9 冲突，会产生非法动作）**：D9 明确"AP=0 时无法防御/道具/逃跑"。防御一旦耗 1 AP，`CombatSystem.chooseAllyAutoAction()`（`CombatSystem.ts:657`、`:675`）在**没有可用技能时兜底返回 `defend`**——此时若 AP=0，将提交一个 AP 不足的非法动作。落地时必须给兜底分支加"AP < 1 则改为跳过该单位/直接结束我方阶段"的处理，否则自动战斗会卡死或抛错。

### 第 29 轮

| # | 议题 | 裁决 |
|---|---|---|
| D108 | **输出 ∝ N² 的修法** | ⏸ **暂不修**，等 Boss（D40）/ 敌人扩充（D52）/ 装备饰品（D80）全部落地后**一次性整体重算**。⚠️ 见下方连带 |
| D109 | **噩梦机制叠加** | ✅ **可任意叠加，最多 4 个全开**（每个机制自带代价↔补偿，玩家自由组合；不额外设全开奖励） |
| D110 | **训练场规则** | ✅ **固定汇率：金币 → 经验值**（直接注入角色经验条，走正常升级曲线）。⚠️ **汇率数值未定**，见下方待决 |
| D111 | **Boss 技能释放顺序** | ✅ **固定循环 + 血量阈值插入大招**（平时按配表顺序循环，血量跌破阈值时强制插入大招） |

> ⚠️ **D108 的连带风险（必须记住）**：选择"延后重算"意味着在重算之前，任何**提升我方输出**的内容都会让失衡进一步加剧——尤其是 D80 的饰品（连击率 / 暴击率 / 强化率加成）与 D85 的锻造。
> 因此**重算必须排在 D80–D85 之后**，且重算时要把「饰品加成」与「D5 的 `apCost` 分档」一并纳入（D5 让高倍率技能变贵，是唯一已经在压输出的既定改动）。在此之前**不要再引入新的我方输出来源**。

> ❓ **D110 待决：金币 → 经验的汇率**。候选 **1 金币 = 1 / 2 / 3 经验**。参考量级：`expForLevel(5) ≈ 420`；一次下探的金币收入尚未实测标定（属 D23 / D92 的经济核算缺口）。下一轮定标。

### 第 30 轮（第 20 轮后的第一个检查点）

| # | 议题 | 裁决 |
|---|---|---|
| D112 | **训练场汇率定值** | ✅ **汇率随等级递增（越后期越贵）**，非固定值 —— 推翻上一轮"固定汇率"的表述。建议公式见下，**斜率待你确认** |
| D113 | **商店商品构成** | ✅ **消耗品 + 锻造材料 + 随机 1 件稀有饰品（每店限购 1 件）** |
| D114 | **敌人族群特性** | ✅ **加，每族 1 条被动特性**（具体 4 条效果待定）。不做"同族共鸣"那种需要同场判定的复杂联动 |
| D115 | **全局难度的默认与切换** | ✅ **开局选择，选定后不可更改**（存档记录难度；噩梦的 4 个可选机制同样在开局一并选定并锁定） |

> 📐 **D112 建议公式（待确认斜率）**
> ```
> 每点经验的金币成本 = 1 + floor((当前等级 - 1) / 3)
>   等级 1–3  → 1 金 / 经验
>   等级 4–6  → 2 金 / 经验
>   等级 7–9  → 3 金 / 经验   …… 以此类推
> 升 1 级总价 = expForLevel(当前等级) × 该系数
> ```
> 例：`expForLevel(5) ≈ 420` → 5 级升 6 级约 **840 金**；`expForLevel(11) ≈ 1 480` → 约 **5 920 金**。
> 若嫌后期太陡，可改为「每 2 级一档」（`1 + floor((L-1)/2)`）或加一个上限系数（如封顶 6）。

> ⚠️ **D115 的连带**：难度选定后不可更改 ⇒ ① 新游戏流程需要一个**难度选择界面**（含噩梦 4 机制的勾选项）；② 存档 schema 需新增 `difficulty` 与 `nightmareMods[]`；③ **D105 的统计面板**应展示当前难度，否则玩家事后无法证明；④ 与 D109「可任意叠加」合并后，理论上存在 **4 档 × 16 种机制组合** 的存档分支，回归测试要覆盖至少「全关」与「全开」两端。

### 第 31 轮

| # | 议题 | 裁决 |
|---|---|---|
| D116 | **训练场汇率斜率** | ✅ **系数固定，靠经验曲线自然变贵** —— 即 `升 1 级总价 = expForLevel(当前等级) × 固定系数`，不再按等级分档。⚠️ **系数取值与 D110 有冲突，见下** |
| D117 | **资料查看入口** | ✅ **不做资料入口**（不建图鉴、不并入面板）。信息只靠战斗内的数值条与技能描述临时查看。⚠️ 见下方风险 |
| D118 | **精英怪出现规则** | ✅ **按概率随机出现**（建议 **15%** 替换普通遇敌），属性 **×1.5**，**必掉锻造材料** |
| D119 | **族群特性强度** | ✅ **强特性，需要针对性应对**（明确改变打法，而非 ±10% 的 flavor）。具体 4 条效果由我拟定后逐条拍板 |

> 🔴 **D116 与 D110 / D112 的口径冲突（需要你二选一）**
> 三轮表述叠起来后，实际汇率是 **1 金币 = 0.5 经验**（即 2 金 / 经验）：
> - `expForLevel(5) ≈ 420` ⇒ 5 级升 6 级要 **840 金**
> - 而 D110 当初给的候选是 **1 金币 = 1 / 2 / 3 经验**（5→6 级只要 420 / 210 / 140 金）
>
> 即**现结论比 D110 的三个候选都贵 2–6 倍**。两种可能：
> - **(a) 系数 = 2**：你要的就是"贵"，训练场是奢侈品，金币主出口仍在商店 / 招募 / 锻造。选此则本条关闭。
> - **(b) 系数 = 0.5**：你本意仍是「1 金币 = 2 经验」，只是让它**随等级自然变贵**。5→6 级仍 210 金，11→12 级因经验曲线变成 740 金。
>
> **我倾向 (b)**：`expForLevel = round(20 × L^1.9)` 本身已是超线性（L 从 1→11，需求 20→1 480，涨 74 倍），再叠 2 金/经验会让后期价格失控。

> ⚠️ **D117 的落地理解（不再追问）**：不做资料入口 ⇒ 玩家无法查证 **D80 的 12 件饰品**加成什么、**D5 分档后的技能实际倍率**、以及敌人属性构成。
> 但"饰品槽位上的效果说明"不算资料入口，属槽位自身的必要标注 —— **落地时按此执行**，否则玩家只能靠伤害数字猜。

> ❓ **D119 待办**：产出 4 条族群特性的具体效果（含数值与判定时机），与 D93 的四个主题族群一一对应。**下一轮给方案。**

### 第 32 轮

| # | 议题 | 裁决 |
|---|---|---|
| D120 | **训练场汇率系数** | ✅ **系数 0.5（1 金币 = 2 经验）** —— 即 `升 1 级总价 = expForLevel(当前等级) × 0.5`。5→6 级 **210 金**，11→12 级 **740 金**。**D116 的口径冲突就此关闭**（采用上一轮倾向的 (b)） |
| D121 | **4 条族群特性** | ✅ **采纳，但第 4 条换成更直接的**：腐朽王座改为**每回合开始对我方全队造成当前生命 8% 的伤害**（原「AP 上限 −1」因现行“一回合清场”而无感）。完整 4 条见下 |
| D122 | **12 件饰品分布** | ✅ **4 类各 3 件**（属性加成 / 连击·暴击·强化率 / 战术效果 / 每回合触发 1 次），每类 3 件供玩家组 build |
| D123 | **D5 分档的补偿** | ✅ **不补偿，"防御就是兵底"**，让 AP 分配成为真正的核心决策。⚠️ **但这句在当前三条裁决下不成立，见下方冲突** |

> 📌 **D121 定案的 4 条族群特性（均兼容 D3「永久单敌」，不依赖同场多名同族）**
>
> | 图 | 族群主题 | 特性 | 效果 |
> |---|---|---|---|
> | 无根回廊 | 杂居兽群 | **群体本能** | HP 归零后**分裂成「残躯」**（25% HP、攻击 ×0.7）继续战斗 |
> | 苔痕回廊 | 真菌孢子 | **孢子爆发** | 被击败时我方全队**中毒 2 回合**；但若死于**连击第 2 段及以后则不触发**（奖励精准爆发） |
> | 静谧墓所 | 亡骸军团 | **亡骸重整** | 受到**治疗**时反而受到**等量伤害**（治疗反转） |
> | 腐朽王座 | 深渊眷属 | **深渊吞噬** | **每回合开始对我方全队造成当前生命 8% 的伤害**（惩罚拖回合，压速攻） |
>
> ⚠️ 第 4 条与「D108 延后重算」有张力：在战斗 1 回合结束的现状下，8% 的真实上限只有 8%（仅 1 回合）；**等重算后回合数拉长，它会变成最强的压制项**，重算时必须一起复核。

> 🔴 **D123 的裁决链冲突（必须下一轮解决）**
> 三条裁决叠起来后，**"防御"这个兵底在最需要它的时刻不存在**：
> - **D104**：防御**消耗 1 AP**
> - **D9**：**AP = 0 时不能防御**（原样保留，属有意取舍）
> - **D5**：技能按 power 分档涨价 ⇒ AP 更容易被榨干
> - **D123**：不补偿，"防御就是兵底"
>
> ⇒ AP 耗尽时，玩家**既放不出技能、也防御不了**，只能结束我方阶段。这不是"兵底"，是"没有动作可做"。
> 三选一（下一轮拍板）：
> - **(a)** 防御**不耗 AP**（推翻 D104 的耗 AP 部分，保留「受伤 ×0.8」）—— 最直接，兵底真正存在
> - **(b)** 防御耗 1 AP，但**允许"AP 不足时仍可防御"**（放宽 D9）—— 保留 AP 成本的策略性，同时保住兵底
> - **(c)** 承认没有兵底，AP 耗尽就结束阶段 —— 与 D123 的表述矛盾，需改 D123 的措辞
>
> **我倾向 (b)**：AP 仍是稀缺资源（防御要花 1 点），但不会出现"无动作可做"的死局；且与 D104 的"防御改造"意图一致。

### 第 33 轮

| # | 议题 | 裁决 |
|---|---|---|
| D124 | **防御兵底冲突** | ✅ **承认没有兵底**：AP 耗尽就结束我方阶段。⇒ **修正 D123 的措辞**（"防御就是兵底"作废，防御是**有 AP 时**的最优保底，不是无 AP 时的退路）；D9 的"AP=0 不能防御"原样保留 |
| D125 | **4 个新 Boss 的命名** | ✅ **由我拟定，与四个族群主题呼应**（亡骸军的 Boss 同样带治疗反转等）。数值与专属技能后续再落 |
| D126 | **12 件饰品的获取** | ✅ **只靠锻造**（D88 每件独立配方）。材料来自精英怪（D118 必掉）与 Boss。⚠️ **与 D113 冲突，见下** |
| D127 | **商店的位置与数量** | ✅ **营地 1 个 + 迷宫每层最少 1 个、较低概率出现第 2 个**（不是只在大地图） |

> 🔴 **D126 与 D113 直接矛盾（必须下一轮解决）**
> - **D113**：商店出售「消耗品 + 锻造材料 + **随机 1 件稀有饰品（每店限购 1 件）**」
> - **D126**：饰品**只靠锻造**
>
> 两者不能同时成立。叠加 **D127（迷宫每层 ≥1 个商店）+ D94（每次进店刷新）** 后，"商店买饰品"实际是一条比锻造更顺手的路径 —— 玩家会倾向于**每层刷店**而不是攒材料锻造，锻造系统会被架空。
> 二选一（下一轮拍板）：
> - **(a)** 商店**不卖成品饰品**，只卖消耗品与材料（改 D113）⇒ 锻造是唯一饰品来源，与 D126 一致
> - **(b)** 商店保留"随机 1 件稀有饰品（限购 1）"，**D126 改为"锻造 + 商店限购"** ⇒ 饰品有两条来源，但锻造仍是主力（配方产出的更强）
>
> **我倾向 (a)**：饰品是 D80–D85 的核心养成系统，若商店能直接买到，玩家会绕过整条"打材料 → 锻造"链路；而 D113 加那 1 件的初衷只是"惊喜位"，可以改成**随机 1 件消耗品/材料的高级版本**。

> ⚠️ **D127 的落地影响**：商店成为**迷宫内元素**（现状迷宫元素只有宝箱 / 陷阱 / 事件 / 存档点）⇒ `MazeGenerator` 需新增商店放置规则（每层 ≥1、低概率 2）。且商店**商品档次应跟层数挂钩**（与 D60「宝箱金币跟难度档 N1–N6 挂钩」同一口径），否则第 1 层与第 6 层卖同样的东西。
> 连带：`events.json` / 迷宫元素权重（现为 宝箱·陷阱·事件 各有权重）需要加上商店，并避免商店与存档点抢同一支路末端。

> ⚠️ **D124 的代码连带**：`CombatSystem.chooseAllyAutoAction()`（`CombatSystem.ts:657`、`:675`）在无可用技能时**无条件返回 `defend`**。在"AP=0 不能防御 + 防御耗 1 AP"下这会产生非法动作 ⇒ 必须改为"AP < 1 时返回 `null`/跳过该单位"，否则自动战斗会卡死。

### 第 34 轮

| # | 议题 | 裁决 |
|---|---|---|
| D128 | **D113 vs D126 冲突** | ✅ **保留商店限购，改 D126** ⇒ **饰品两条来源**：**锻造（主力，属性更强）** + **商店每店限购 1 件随机稀有饰品** |
| D129 | **商店商品档次** | ✅ **跟层数档位 N1–N6 挂钩**（与 D60 宝箱金币同一口径）—— 第 6 层的商店不会卖第 1 层的药水 |
| D130 | **4 个新 Boss 命名** | ✅ **采用**：碎巢母 / 菌王·腐甜之主 / 不眠的司祭 / 王座上的等待者 |
| D131 | **商店在迷宫中的位置** | ✅ **支路末端**（与宝箱 / 存档点同机制，不占主干），鼓励探支路；按 D127 每层至少 1 个，剩余的低概率出第 2 个 |

> 📌 **D130 定案：4 个专属 Boss**

| 图 | 族群主题 | Boss 名 | 命名依据 | 需呼应的族群特性 |
|---|---|---|---|---|
| 无根回廊 | 杂居兽群 | **碎巢母** | 无根 = 没有归属的巢 | 群体本能（分裂成残躯） |
| 苔痕回廊 | 真菌孢子 | **菌王·腐甜之主** | 呼应原文"空气里全是腐朽的甜味" | 孢子爆发（死亡中毒） |
| 静谧墓所 | 亡骸军团 | **不眠的司祭** | 呼应原文"这里的死者并不安眠" | 亡骸重整（治疗反转） |
| 腐朽王座 | 深渊眷属 | **王座上的等待者** | 呼应原文"王座上坐着的东西已经等了太久" | 深渊吞噬（每回合 8% 削血） |

> ⚠️ **D128 修正记录**：**D126 的原裁决（"饰品只靠锻造"）作废**，改为"锻造 + 商店限购 1 件"。
> 为保住锻造的主力地位，落地时必须保证 **锻造产出的饰品属性明显强于商店随机件**（例如商店件只出到 1 类效果、锻造件可出 2 类），否则 D80 的配方系统仍会被绕开。

> ⚠️ **D131 的空间冲突**：商店与**宝箱（D60）/ 存档点（`saveCount`）/ 事件（`eventCount`）**都抢支路末端。
> N1 只有 **0 条支路**（见 `mazeDifficulty.ts`）⇒ **D127 的"每层至少 1 个商店"在 N1 物理上放不下**。
> 必须解决，三种可能：① N1 的商店放到主干上（唯一的例外层）；② 给 N1 加 1 条支路（改难度阶梯，会连带 `branchCount` 与通关步数）；③ N1 不设商店（第 1 层作为纯教学层）。
> **下一轮拍板。**

### 第 35 轮

| # | 议题 | 裁决 |
|---|---|---|
| D132 | **N1 没有支路放商店** | ✅ **给 N1 加 1 条支路**（`branchCount` 0 → 1）⇒ ⚠️ 需改 `src/config/mazeDifficulty.ts` 并**重跑迷宫难度探针**（该探针会断言"支路数 = 设计值"）。**主干长度不变 ⇒ 通关步数 20 不变** |
| D133 | **锻造件 vs 商店件的强度差** | ⚠️ **无区别**（不给差异，靠"商店限购 1 件"克制）。见下方风险 |
| D134 | **旧存档处理** | ✅ **旧档不可续档**（升版本；旧档只读，提示开新档）。因为 D115 定了"难度选定后不可改"，**没有难度的旧档本身就不合法** |
| D135 | **统计面板入口** | ✅ **主菜单新增一个入口**，与"游戏说明 / 读档"并列（不依赖游戏内场景，随时可看） |

> 🔴 **D133 的风险（已知并接受，但必须留复核点）**：商店件与锻造件**完全无差异**，唯一的克制是"每店限购 1 件"。
> 乘积效应是：**D127（迷宫每层 ≥1 个商店）× D94（每次进店刷新）× D129（商品跟层档）** ⇒ 玩家**每层都能刷一次 1 件饰品**，一趟 6 层就是 6 件，与"12 件"的总量相比相当于**半套免费发放**。
> 而锻造需要：打精英（15% 随机，D118）→ 攒材料 → 找配方（D88 每件独立配方）。**成本明显高于每层顺路刷店**。
> ⇒ **建议在 D99 的数值重算里加一项「金币产出 vs 饰品价格」的实测**，若发现刷店成为主路径，再回头给锻造件加 2 类效果（即原 D133 的推荐项）。

> ⚠️ **D134 的连带**：① 需要"旧档只读"的提示 UI；② **D46 的存档导入导出必须按版本号拒绝旧档**，否则导入可绕过版本检查；③ 开发期自身的存档会失效，落地时记得备份一次。

> ⚠️ **D132 的连带**：N1 的**支路长度上限未定**（原阶梯里 N1 的支路长度是"—"）。需补一个值，且不能长到把 15×15 塞满（N1 尺寸最小、房间数最少）。**下一轮定。**

### 第 36 轮

| # | 议题 | 裁决 |
|---|---|---|
| D136 | **N1 支路长度上限** | ✅ **4–6 步（与 N2 同档）** ⇒ 难度阶梯变为：N1 `1 条 / 4–6`、N2 `2 条 / 4–6`、N3 `4 条 / 6–10` …… 支路**条数**仍严格递增 |
| D137 | **"最高层"统计口径** | ✅ **历史最高，团灭不重置**（D21 只归零 `maxFloor`，不动历史字段）⇒ 存档需新增 `maxFloorEver` |
| D138 | **最大内容缺口的推进方式** | ✅ **派队员起草，我审核**（12 件饰品 / 4 个 Boss 专属技能与数值 / 4 条族群特性数值）。不碰游戏代码 |
| D139 | **音频资源来源** | ✅ **先只接接口与事件**（D97 五类音效 + D96 三首 BGM），资源用占位，后续再补真实音频 |

> 📐 **D136 后的完整难度阶梯（唯一真源：`src/config/mazeDifficulty.ts`）**

| | 尺寸 | 通关步数 | 支路数 | 支路长度 | 商店 |
|---|---|---|---|---|---|
| N1 | 15×15 | 20 | **1**（D132 由 0 改） | **4–6**（D136） | 每层 ≥1，支路末端 |
| N2 | 19×19 | 32 | 2 | 4–6 | 同上 |
| N3 | 23×23 | 48 | 4 | 6–10 | 同上 |
| N4 | 27×27 | 58 | 6 | 8–14 | 同上 |
| N5 | 31×31 | 74 | 8 | 10–18 | 同上 |
| N6 | 35×35 | 90 | 10 | 12–24 | 同上 |

> ⚠️ 改 `mazeDifficulty.ts` 后**不需要**跑 `gen-tables`（它是 TS 模块不是配表），但**必须重跑迷宫难度探针**与 `verify.mjs`（其中有一项断言"难度阶梯合法且严格递进"）。

> ⚠️ **D139 的落地口径**：音频只接接口 ⇒ ① `AudioSystem` 需要一个"静默模式"（资源缺失时不报错、不阻塞）；② `smoke.mjs` 的"控制台错误"断言要保持通过（缺资源的 404 会被算作请求失败，需把音频路径加入忽略名单或先放占位空文件）；③ 音量滑杆（已存在）要真正接到主音轨。

### 第 37 轮

| # | 议题 | 裁决 |
|---|---|---|
| D140 | **消耗品槽规则** | ✅ **每角色 1 个消耗品槽、装 1 种**；**使用消耗品占用该角色本回合的出手机会**（与 D9「每单位每回合限出手 1 次」一致），并消耗 1 AP |
| D141 | **饰品槽归属** | ✅ **每个角色 1 个饰品槽**（6 人队 = 最多 6 件同时生效；饰品可自由装卸） |
| D142 | **迷宫导航辅助** | ✅ **不做小地图、不做方向箭头** —— 迷路本身是玩法（呼应支柱 B「迷宫探索与风险管理」）。见下方风险 |
| D143 | **难度经济倍率的适用范围** | ✅ **只管战斗奖励**（敌人掉落的金币与经验）；**宝箱、任务奖励、训练场不受影响** |

> ⚠️ **D142 的风险（已知并接受）**：不做导航辅助 ⇒ N6 是 35×35、相机 zoom 仅 1.04、队伍恒居中且迷宫外为黑色，**玩家在深层会长时间失去方位感**。
> 三条可以缓解、且不违背本条裁决的手段（建议在落地时保留）：
> 1. **地标**：存档点（每层 `saveCount`）与商店（D127 每层 ≥1）本身成为可记忆的参照物；
> 2. **三态迷雾的"已探索"档**要真的保留（现状已有），玩家能靠走过的形状回忆路线；
> 3. **出口不随迷雾隐藏**（现状 `exit` 是固定的可见标记）—— 这一条是"不加导航"但仍有方向锚点的关键。
> 若日后实测"迷路到弃游"，优先加"出口方向箭头"（成本最低），而不是小地图。

> ⚠️ **D140 的连带**：道具使用占出手 ⇒ **D104 的防御、技能、道具三者共享同一个"每回合 1 次"行动额度**。
> 这会让"用道具"的隐性成本从"1 AP"变成"1 AP + 少放一次技能"，与 D123「不补偿、AP 分配是核心决策」的方向一致。
> ⇒ 战斗 UI 需要明确提示"使用道具将消耗本回合行动"（与 D95「角色卡上显示携带的消耗品与剩余数量，点击即用」配合）。

> ❓ **D143 留了一个口径缺口**：`enemies.json` 的 `techPoints` 字段（1–8 点）也是"敌人战斗掉落"，但 D143 只写了"金币与经验"。
> **科技点是否也受难度倍率影响？下一轮拍板。**

### 第 38 轮

| # | 议题 | 裁决 |
|---|---|---|
| D144 | **科技点是否受难度倍率影响** | ✅ **也受影响**（简单 ×1.25 / 困难 ×0.75），口径统一为"难度 = 收益与风险的交换比"。⚠️ **与 D41 的定价核算冲突，见下** |
| D145 | **团灭惩罚是否分难度** | ✅ **不改，四档统一**按 D17 / D21 / D22 执行（本图进度消失 + 丢 50% 价值物品） |
| D146 | **技能/物品描述文案** | ✅ **改为配表手写**（新增 `desc` 字段），逐步替换 `DataLoader` 的程序拼接 |
| D147 | **自动战斗** | ✅ **保留且无限制**（含 Boss 战） |

> 🔴 **D144 与 D41 的核算冲突（需记录并给出处理）**
> D20 / D23 / D41 把科技经济算得很精确：**总价 166 → 79 点**、四图首通合计 **≈45–50 点**、目标节奏 **"首次全清点满约 80%"**。这套数字是**按"普通难度"标定的**。
> 一旦科技点也吃难度倍率：
> - **困难（×0.75）**：首通 ≈34–38 点 ⇒ 点满 79 点需要的刷图次数增加约 **1/3**，"80%"目标实际变成 **≈60%**；
> - **简单（×1.25）**：首通 ≈56–63 点 ⇒ 轻松超过 80%，经济被松掉。
>
> **处理方式（落地时按下述执行，不推翻 D41）**：
> 1. **79 点作为"普通难度基准"**，D41 的逐条价格**不做二次修改**；
> 2. 难度倍率只作用于**产出端**，不作用于**价格端**（否则变成双重调节）；
> 3. 把"首次全清点满约 80%"的表述**限定为普通难度**，简单/困难各自偏离属预期。

> ⚠️ **D146 的落地范围**：改 `skills.json` / `items.json` 新增 `desc` 后**必须跑 `gen:tables`**。
> 建议与 **D42（清理 `cooldown` / `cost` 等死字段）合并成同一次配表改动** —— 两者都要动同一批文件、同一次重新生成，分开做等于白跑两遍。
> 另注意 `DataLoader.ts:157` 那句 `消耗 ${s.apCost ?? 1} AP` 是程序拼接的既有实现，改手写后要一并移除，避免描述里出现两段重复信息。

> ⚠️ **D147 的两条连带**：
> 1. **自动战斗在 D124 下会踩非法动作**：`chooseAllyAutoAction()` 无可用技能时无条件返回 `defend`，而 D124 定为"AP=0 不能防御 + 防御耗 1 AP" ⇒ 必须改成"AP < 1 时返回 `null`/跳过"，否则**自动战斗会卡死**。这是本裁决的**前置修复项**。
> 2. **削弱 Boss 设计价值**：D111 的"固定循环 + 血量阈值大招"是给玩家学习用的，自动战斗会绕过这套机制。既已选择"无限制"，则**Boss 的强度必须能在自动战斗下也被验证**（否则玩家一键通关，Boss 白做）⇒ 建议在 D99 的数值重算里**增加一项"自动战斗下的 Boss 胜率"**作为护栏指标。

### 第 39 轮

| # | 议题 | 裁决 |
|---|---|---|
| D148 | **敌人扩编的确切数量** | ✅ **每图 8 只普通敌（共 28 只）**；**浅层组 4 只（第 1–3 层）+ 深层组 4 只（第 4–6 层）**。合计敌人条目 = 28 普通 + 4 Boss + N 精英 |
| D149 | **精英怪的配表实现** | ✅ **独立配表条目**（有自己的名字、技能、掉落，像 Boss 一样定制，而非普通怪 ×1.5 的变体）。⚠️ **数量对不上，见下** |
| D150 | **消耗品槽与背包的关系** | ✅ **实时从背包取**（槽位指定"用哪一类消耗品"，数量实时扣除；背包用完则槽位变灰） |
| D151 | **敌方每回合出手次数** | ✅ **普通敌 1 次 / 精英 2 次且每 3 轮 1 次大招 / Boss 按 D111 的固定循环 + 血量阈值插大招**。把 D57 的"1–2 次"明确为**按类别**而非随机 |

> 🔴 **D149 暴露的数量缺口**：D40 把 **迷宫的守护者（Lv10，420HP）/ 骸骨暴君（Lv12，560HP）/ 深渊之王（Lv15，900HP）** 降为精英怪 —— **只有 3 只，而有 4 张图**。
> 按现有等级分布看，它们天然对应**静谧墓所 / 腐朽王座**，而 **无根回廊与苔痕回廊没有任何精英怪**。
> 需要补。两种方案（**下一轮拍板**）：
> - **(a)** 每图 **1 只专属精英**，共 4 只 ⇒ 新增 1 只（无根）+ 1 只（苔痕）或 2 只，另 2 只沿用降级的旧 Boss；
> - **(b)** 每图 **2 只精英**（浅层 1 + 深层 1），共 8 只 ⇒ 新增 5 只，内容量更大、覆盖面更好。

> ⚠️ **D148 与 D89 的组合口径（写清楚，避免落地时歧义）**：D89 的"池内按层分两组"与 D7 的"按层切池"是**同一件事的两种说法**，落地时只做一次实现：
> 每图的 `enemyPool` 拆成 `shallow`（4 只，第 1–3 层）与 `deep`（4 只，第 4–6 层）两组，`MazeGenerator.rollEnemy()` 按当前层选组再按权重抽。
> ⚠️ 注意：第 6 层要**先排除 Boss**（D6：Boss 打 `isBoss` 且普通池排除），Boss 是**该层固定出现**而非池内抽取。

> ⚠️ **D151 的连带**：精英"2 次/轮"意味着**玩家的 TTK 容错更低**（同一回合吃两次攻击）。在 D99 实测的现状下（我方 1 回合清场）无感，但 **D108 重算后这会是主要压力来源**，重算时必须把"精英二动"计入。

### 第 40 轮（检查点）

| # | 议题 | 裁决 |
|---|---|---|
| D152 | **精英怪数量** | ✅ **每图 1 只专属精英，共 4 只**：无根、苔痕各新增 1 只；静谧墓所沿用降级的**迷宫的守护者**，腐朽王座沿用**骸骨暴君**。**深渊之王**同为精英，可作腐朽王座的深层精英或改作它用（后续定） |
| D153 | **敌人技能数量** | ✅ **普通敌 2 个 / 精英 3 个 / Boss 3–4 个**（含专属技能） |
| D154 | **下一步走法** | ✅ **不碰代码，继续问答到第 100 轮**，设计做完再一次性实现 |
| D155 | **内容草案的处理** | ✅ **等队员交回后我逐条审核，再让你拍板**（草案路径 `design/drafts/content-draft-v1.md`，**只读不落地**） |

> 📊 **第 40 轮检查点：本检查点累计新增 47 条裁决（D105–D155）**
> - **内容规格**：敌人 28 普通（每图 8，浅/深各 4）+ 4 Boss + 4 精英 = **36 个敌人条目**，普通敌 2 技能 / 精英 3 / Boss 3–4 ⇒ 敌方技能量约 **28×2 + 4×3 + 4×3.5 ≈ 82 个**
> - **装备**：12 件饰品（4 类 × 3）+ 每角色 1 饰品槽 + 每角色 1 消耗品槽（实时取背包、占出手）
> - **迷宫**：N1 加 1 条支路（4–6 步）、商店成为迷宫元素（每层 ≥1、支路末端、商品跟 N 档）
> - **难度**：开局选定不可改、机制可叠加 4 个、倍率只作用于战斗奖励（含科技点）
> - **经济**：训练场 1 金 = 2 经验、商店限购 1 件饰品、精英必掉材料
> - **冲突已解**：D113 vs D126（→ D128）、N1 无支路放商店（→ D132）、D116 汇率口径（→ D120）、防御兵底（→ D124）
> - **仍开放**：`§10` 的 #32（输出 ∝N²，D108 延后）、D133 的锻造 vs 商店强度差（已接受风险）、深渊之王的最终归属

---

## 0.7 内容草案 v1（队员产出，已审）

**路径**：`design/drafts/content-draft-v1.md`（12 件饰品 / 4 个 Boss 与 12 条专属技能 / 4 条族群特性；**只读，未落地**）。

**我已独立核对它的 3 条承重断言，全部命中**：
| 断言 | 核对结果 |
|---|---|
| `fromCombo: i >= baseHits`（只有连击额外段为 true） | ✅ `DamageCalculator.ts:155` |
| 敌方自愈 = `team.attack × 倍率` 且只写给 `caster` | ✅ `DamageCalculator.ts:264-266` |
| 我方每人各持一份 `teamHp`（HP 池并未共享） | ✅ `buildAllyUnit`（`CombatSystem.ts:103-126`），即 §10 已知问题 #4 |

**队员自己挖出的 3 个真问题**（我没提示）：
1. **三条概率曲线是饱和曲线** —— 队伍该维从 78 涨到 325（+317%），连击率只从 0.374 涨到 0.590（+58%）⇒ 这正是 D122 要把"概率加成"单列成第 2 类的原因。
2. **第 6 层 ×2.5 会把 Boss 防御顶到 clamp** —— 基础防御 > 0.38 就被 `clamp(0.95)` 吃掉 ⇒ 四个 Boss 基础防御封顶 **0.18**。
3. **旧 Boss 降级后必须砍数值** —— 否则第 6 层 `×3.5 ×1.5` 后，路边随机精英（深渊之王 4 725 HP）**比该图最终 Boss（4 648）还硬**。建议 HP −61%~−71%。

### 第 41 轮

| # | 议题 | 裁决 |
|---|---|---|
| D156 | **Boss 科技点预算冲突** | ✅ **并入 `firstClearTechPoints`**：四图从 3/3/5/10 上调到 **10/12/14/16（合计 52）**；`enemies.json` 的 `techPoints` 只表示**重复击杀**的小额值（草案的 4/5/6/8 正好填这里） |
| D157 | **新增 `heal_max_ratio` 效果类型** | ✅ **新增**（按自身最大生命比例治疗）。不新增则四个 Boss 的自愈技在配表层就是废技（现公式只能回 4.7% 最大生命） |
| D158 | **12 件的 3 档 × 4 类网格** | ✅ **接受**。额外好处：角色级槽下装满同类 3 槽必然是 **I+II+III 各一**，天然封住"堆同一件" |
| D159 | **深渊吞噬的触发点** | ✅ **新增强制钩子 `onAllyPhaseStart()`，改挂"我方阶段开始"**。此举同时救活 **D122 的类 4 三件饰品**（原本也永不触发）⇒ **Q5 一并解决** |

> 🔴 **D159 引发的新问题（必须下一轮解决）**
> 改挂"我方阶段开始"后，若**战斗首个我方阶段也算**，则腐朽王座的**每一场**战斗都会先抽 8% 血。
> 而 D1 已裁「**真共享生命池**」（HP 在迷宫与战斗间**持续**），腐朽王座 `enemyDensity 0.055` ⇒ 一趟 6 层约 **35 场战斗**：
> ```
> 35 场 × 8% = 280% 队伍最大生命
> ```
> ⇒ **必然团灭**。这条特性会从"惩罚拖回合"变成"**每场战斗固定抽 8% 税**"。
> 而若"首个我方阶段不触发"，则在现行"一回合清场"下**又回到 0 次**，D159 就白改了。
>
> 四个方向（**下一轮拍板**）：
> - **(a)** 只在**第 2 个及以后**的我方阶段触发 ⇒ 保住"惩罚拖回合"的原意；一回合清场下 = 0 次（等于接受"等 D108 重算"）
> - **(b)** 每次战斗只触发 **1 次**，且限"战斗进入第 3 个我方阶段后" ⇒ 既惩罚拖长，又不会变成每场税
> - **(c)** 数值从 8% 降到 **2–3%** ⇒ 一回合清场下约 35 场 ×2.5% ≈ 87%，仍需场外回血配合
> - **(d)** 保留 8%/回合，但**配套加"场外回血"**（存档点回复 / 营地回满 / 商店买药）⇒ 把成本转成资源压力
>
> **我倾向 (b)**：它同时满足 D119「强特性」、D4「不做硬核生存压力」、以及"一回合清场时仍有 1 次感知"。

> ⚠️ **D159 的连带范围**：新增 `onAllyPhaseStart()` 后，**类 4 三件饰品**（自愈镀层 4%/回合、相位反射片、终末采样器 +2AP/回合）也挂在这个钩子上 ⇒ **R1 与深渊吞噬都是"每场战斗至少 1 次"**。
> 这会让 `R1 自愈镀层` 从"腐朽王座的半个对策"变成"**每场固定回 4%**"，需要与上一条一起复核。

### 第 42 轮

| # | 议题 | 裁决 |
|---|---|---|
| D160 | **深渊吞噬导致必然团灭** | ✅ **HP 改为战斗后自动恢复** —— 用"HP 不入跨场账"的方式一次性消解该问题（8% 永远只是**单场内**的一次削血） |
| D161 | **场外回血手段** | ✅ **战斗结束后自动回满** —— 不设存档点回血、不做每层通关回复 |
| D162 | **Boss 是否享受族群特性** | ⚠️ **享受**。⇒ 必须逐只处理例外，见下（否则有两只 Boss 会出现"无意义"或"自废技能"） |
| D163 | **亡骸重整的定位** | ✅ **诱饵**：刻意给亡骸族 7–8 只里的 **3 只配自愈技**，做成"识别与优先级的谜题" |

> 🔴🔴 **D160 + D161 实际上推翻了 D1，必须确认口径**
> **D1 原文**：「**真共享生命池**（战斗与迷宫同一份）」。它的**目的**是让迷宫 HUD 上那条血条成为真实资源。
> **D161 的执行结果**：战斗结束即回满 ⇒ 迷宫 HUD 的血条**恒满** ⇒ **共享了也没有意义**。这不是"实现方式变了"，是**D1 的设计目的被取消**。
> 连带影响（都已确认过，不是推测）：
> 1. **支柱 B 被削弱**：§1.2 的「迷宫探索与风险管理」从此只剩**陷阱**与**迷路**两个风险源，战斗不再有累积成本；
> 2. **消耗品价值重估**：治疗药水只在**单场战斗内**有用，D113 商店卖的药水价值大幅下降；
> 3. **存档点（每层 `saveCount`）失去回血职能**，只剩"保存进度"一个用途；
> 4. **D17 团灭惩罚仍然成立**（团灭 = 单场战斗全灭），但"带着残血往下走"这种紧张感消失；
> 5. **D20/D23 的经济核算不受影响**（金币/科技点产出与此无关）。
>
> **三种可能的口径（下一轮确认）**：
> - **(a) 确认推翻 D1**：HP 只在战斗内有效，迷宫 HUD 的血条改为"上一场结束时的剩余"或干脆不显示；支柱 B 的表述同步修订（风险源改为陷阱+迷路）
> - **(b) 战斗后回满，但保留"陷阱/事件扣的血"跨场**：即 **战斗**消耗回满、**迷宫**伤害持续 ⇒ 保留一条跨场生命线，且与 D60/D121 的迷宫元素形成呼应
> - **(c) 不整场回满，改为回固定比例（如 50%）**：保留跨场消耗的紧张感，同时不会像 8%/场那样必然团灭
>
> **我倾向 (b)**：它既落实了用户"战斗后自动回满"的意图（消除团灭风险），又保住了 D1 的核心价值与支柱 B —— 迷宫层的伤害仍然是要认真对待的事。

> ⚠️ **D162 必须逐只处理的例外（两只 Boss 有结构性问题，不是数值问题）**
>
> | Boss | 本图族群特性 | 问题 | 建议例外 |
> |---|---|---|---|
> | 碎巢母 | 群体本能（HP 归零分裂 25%） | 1155 HP 再分裂 289 ⇒ Boss 战被推成拖沓 | 分裂继承比例 **25% → 15%**，或 Boss 分裂后**不再获得攻击 ×0.7 的减免** |
> | 菌王·腐甜之主 | 孢子爆发（**被击败时**中毒 2 回合） | **Boss 被击败 = 战斗已结束**，孢子毫无意义 | 改为**阈值触发**：HP 首次跌破 50% 时喷一次孢子（与 D111 的"血量阈值插大招"同一套机制） |
> | 不眠的司祭 | 亡骸重整（**受到治疗**时受伤） | 它的专属技「育巢/菌毯/亡骸重整」里有自愈 ⇒ **自愈变自伤，技能自废** | 二选一：① 司祭**不吃**本特性；② 保留特性，但把它的自愈技改为"护膜 / 减伤"（不吃治疗路径） |
> | 王座上的等待者 | 深渊吞噬（每回合 8% 削血） | 无结构性问题（且「深渊汲食」是它对玩家的输出技，不是自愈） | 无需例外 |
>
> **下一轮请对两只（菌王 / 司祭）各拍一次板。**

### 第 43 轮

| # | 议题 | 裁决 |
|---|---|---|
| D164 | **生命池口径（D1 的修订）** | ✅ **战斗回满、迷宫伤害跨场** ⇒ **D1 修订为"双层账"**，见下 |
| D165 | **菌王的孢子例外** | ✅ **改为阈值触发**：HP 首次跌破 **50%** 时喷一次孢子（全队中毒 2 回合）。与 D111「血量阈值插大招」同一套机制 |
| D166 | **司祭的自愈例外** | ✅ **司祭不吃「亡骸重整」** —— Boss 是族群的"首领"，不该被自己的族规伤到；它的自愈技正常生效 |
| D167 | **锻造的金币费与饰品定价** | ✅ **锻造 = 材料 + 金币费**（`price × 0.9`）。⚠️ `price` 数值**待经济核算**，暂用 ★3 = 600 / ★4 = 1500 / ★5 = 4000 作起点 |

> ⚠️ **D167 勘误（第 101 轮 · 落地实测）**：锻造费**现值 = 材料 + `price` × 0.6**（**不是 0.9**）。0.9 会让"自造"恒比"直购"**贵 15–21%**（材料另算），与 D167「反哺锻造主力」的原意**方向相反**，故被 **§11.A.1 正文**那条经济修正覆盖（正文写「材料 + `price` × 0.6（原 0.9 会让"自造"比"直购"贵，方向反了）」）。⇒ 12 件饰品的 `forgeCostRate` 配表值**全部为 0.6**（`src/data/tables/accessories.json`，12 条全为 0.6）。**看到 0.6 是正确值，不要"改回 0.9"。** 同轮 **D171** 的"锻造费 = `price × 0.9`"同属被覆盖的旧值。

> 📐 **D164 定案：D1 修订为「双层生命账」**（这是本轮最重要的结构性裁决）
>
> | 账本 | 来源 | 是否跨场 |
> |---|---|---|
> | **战斗 HP** | 每场战斗开始 = **当前迷宫 HP**；战斗内的伤害只扣这本账 | **不跨场**（战斗结束即丢弃，不写回） |
> | **迷宫 HP** | 陷阱 / 事件扣血 | **跨场持续**，直到回营地或被治疗 |
>
> 等价叙述：**战斗不消耗迷宫血量，只有迷宫元素消耗**。
> - ✅ 落实了 D160/D161「战斗后自动回满」（战斗伤害不带走 ⇒ 不存在"带残血连打 35 场"）
> - ✅ 保住了 **D1 的核心价值**：迷宫 HUD 的血条是真的资源，陷阱与事件必须躲
> - ✅ 保住了**支柱 B「迷宫探索与风险管理」** —— 风险源 = **陷阱 + 事件 + 迷路**（战斗不再是累积成本）
> - ⚠️ **待确认**：迷宫 HP 归零时发生什么？（现状代码里没有"迷宫内死亡"路径）⇒ **下一轮拍板**
> - ⚠️ **连带**：`GameState.setVitals / vitalsOf` 的接通（§10 已知问题 #3）现在是**必需的**，不再是可选优化
> - ⚠️ **连带**：`R1 自愈镀层`（每回合回 4% maxHp）在双层模型下只在**战斗内**有意义 ⇒ 它与迷宫 HP 无关，数值无需再改

> ⚠️ **D165 / D166 之后还剩一条 Boss 例外未定**：**碎巢母的分裂**（1155 HP → 残躯 289，可能把 Boss 战推成拖沓）。**下一轮拍板。**

### 第 44 轮

| # | 议题 | 裁决 |
|---|---|---|
| D168 | **迷宫 HP 归零的后果** | ✅ **归零即团灭**（沿用 D17 惩罚：本图进度消失 + 丢 50% 价值物品）⇒ 陷阱从此**真正有威胁** |
| D169 | **碎巢母的分裂例外** | ✅ **继承比例 25% → 15%**（1155 → 173）。与普通敌的 25% 拉开，避免 Boss 战被推成拖沓 |
| D170 | **是否补新风险维度** | ✅ **不加**。支柱 B 的风险源就是 **陷阱 + 事件 + 迷路**（D164 后的最终形态） |
| D171 | **饰品 `price` 起点** | ✅ **★3 = 600 / ★4 = 1 500 / ★5 = 4 000**（与既有物品量级 药水 80 / 大药水 260 明显拉开）。锻造费 = `price × 0.9` |

> ⚠️ **D171 勘误（第 101 轮 · 落地实测）**：D171 末句"锻造费 = `price × 0.9`"与 **D167** 同源，**现值 = 材料 + `price` × 0.6**（0.9 会让"自造"比"直购"贵、方向反，已被 **§11.A.1 正文**覆盖；`accessories.json` 12 条 `forgeCostRate` 全为 0.6）。**看到 0.6 是正确值，不要改回 0.9。** 措辞与 D167 勘误一致。

> 🔴 **D168 引出一个必须立刻处理的数值问题**
> 陷阱伤害现状：`round(maxHp × v/100) + v`，`v = int(8, 20)`（`DungeonSystem.ts:286-290`、`MazeGenerator.ts:570`）
> ⇒ 单次陷阱 = **最大生命的 8%–20% 再加 8–20 点**。
> 在 D164 的双层账下（**迷宫伤害跨场、战斗不再回补**），一趟 6 层会遇到 `trapCount` = 1 / 3 / 4 / 5 个陷阱（按图），**且陷阱是"踩到就吃"**：
> ```
> 假设中位 v = 14：单次约 maxHp × 14% + 14
> 踩 5 次 ≈ 最大生命的 70%+ 70 点  ⇒ 再踩一次就团灭（D168）
> ```
> ⇒ **陷阱从"扣点血"变成了"半管血的彩票"**。而且 D57 已删「迟缓」陷阱，现存只有**伤害 / 传送**两种。
> 三个方向（**下一轮拍板**）：
> - **(a)** 下调伤害系数（如 `v = int(4, 8)` ⇒ 4%–8% + 4–8 点）
> - **(b)** 保持数值，但**降低陷阱数量**（`trapCount` 1/3/4/5 → 1/1/2/2）
> - **(c)** 保持数值与数量，但**给玩家"看到陷阱"的能力**（陷阱在已探索区域可见 / 或加"排雷"消耗品），把随机踩雷变成可规避的风险

### 第 45 轮

| # | 议题 | 裁决 |
|---|---|---|
| D172 | **陷阱的致命化** | ✅ **不降伤害，降数量**：`trapCount` 1/3/4/5 → **1/1/2/2**（伤害系数与"已探索可见"均不改） |
| D173 | **深渊之王的归属** | ✅ **改名「深渊之王的虚影」**，作为**稀有奖励怪**：**每张图的最后一层**，当该出现精英怪时**有概率替换原精英怪**；战胜后给 **科技点 + 精英怪 2 倍的金币与经验 + 更高稀有度物品**。⚠️ 四处待定，见下 |
| D174 | **28 只新敌人 + 2 只新精英的推进** | ✅ **派队员批量产出**（已派，见 §0.8） |
| D175 | **92 条技能文案的推进** | ✅ **派队员批量写**（已派，见 §0.8） |

> ❓ **D173 的四处待定（下一轮补）**
> 1. **替换概率的具体数值**（原文只说"有概率"）；
> 2. **"每张图的最后一层"的读法**：字面是**四张图都可出现**，但「**深渊之王**的虚影」出现在「**无根回廊**」（新手图）的第 6 层会很突兀 —— 两种口径：
>    - (i) 四图通用（按原文），把名字理解为"深渊力量的投影"，代价是命名与场景错位；
>    - (ii) **仅腐朽王座第 6 层**出现，名字自洽；
> 3. **"更高稀有度物品"的具体掉落表**（需与 D49 的"必得专属掉落"区分）；
> 4. **与 D152「每图 1 只精英」的关系**：是**取代**精英位（腐朽王座第 6 层可能没有普通精英），还是**额外追加**？我按"**取代**"记录。

> ⚠️ **D172 的执行后果**：`trapCount` 从合计 13 个降到 6 个（1/1/2/2），**单次伤害仍是最大生命的 8–20% + 8–20 点**。
> ⇒ 一趟 6 层最多踩 2 次 ⇒ 最坏约 **40% 血量**，可承受但仍有威胁，符合 D168「归零即团灭」下"既要危险又不能必死"的定位。
> ⚠️ `trapCount` 是**每图固定值**（不按层区分），所以本次是配表层一次性调整，不涉及分层逻辑。

### 第 46 轮

| # | 议题 | 裁决 |
|---|---|---|
| D176 | **虚影的出现范围** | ✅ **四图通用**，但**不同图的数值要做衰减**（越浅的图越弱）—— 具体衰减系数待定 |
| D177 | **虚影的替换概率** | ✅ **取代**精英位，概率 **10%**（更稀有） |
| D178 | **虚影的"更高稀有度物品"** | ✅ **② + ③ 都要**：必掉**秘药（`item_elixir`，永久属性道具）**＋**大量稀有材料**（魔核 / 深渊结晶） |
| D179 | **迷宫 HP 的回复手段** | ✅ **四选四**：① 战斗后回满 ② **通关迷宫后回满** ③ **团灭回营地后回满** ④ **迷宫里可用道具回复** |

> 🔴 **D179 与 D164 的冲突（必须确认）**
> **D164** 定的是「战斗回满、**迷宫伤害跨场**」（第 3 轮的双层账，(b) 方案）。
> **D179** 的第 ① 条「战斗后回满」会让**陷阱扣的血在下一场战斗结束时也被抹掉** ⇒ 迷宫伤害实际上**不跨场** ⇒ 等价于 D164 的 **(a) 方案**。
> 两种可能的最终口径：
> - **(i) 严格按 D164**：陷阱伤害**不因战斗而回满**，只在「通关迷宫 / 团灭回营地 / 用道具」时恢复 ⇒ 陷阱真正累积，D168「归零即团灭」有真实威胁
> - **(ii) 按 D179 字面**：战斗后也回满 ⇒ 陷阱只在"踩到 → 下一场战斗结束"这段区间内有威胁
>
> 量化差距：`trapCount` = 1/1/2/2，单次伤害 = 最大生命 8–20% + 8–20 点
> - 口径 (ii)：一趟最多踩 2 次 ≈ 最坏 **40%**，且**下一场战斗后就清零** ⇒ 几乎不可能触发 D168 的团灭
> - 口径 (i)：伤害整趟累积，**后续叠上"从营地出发"的血量起点**才有意义 ⇒ 陷阱是真威胁
>
> **我倾向 (i)** —— 它才是 D164 的原始裁决，且让 D168 不至于成为一条空条款。**请确认。**

> ❓ **D176 待补**：四图数值衰减的系数（例如 无根 ×0.5 / 苔痕 ×0.7 / 静谧 ×0.85 / 腐朽 ×1.0）。**下一轮定。**

### 第 47 轮（队员产物审阅：我核对出的代码事实与更正）

| # | 议题 | 我的裁定 |
|---|---|---|
| D180 | **更正：`combo_bonus` 并非 no-op** | ✅ **推翻 §10 的旧结论**。`skillComboBonus()`（`DamageCalculator.ts:50-52`）确实从 `effects` 读 `combo_bonus` 并加进 `comboRate`（`:129`），且命中数早已是"可追加"（`totalHits += baseHits`，`:133`）⇒ **D56 的前提（"逻辑不可达"）已过期**，该修正项可从待办中移除 |
| D181 | **更正：D146 的字段名** | ✅ **不新增 `desc`**。配表**已有 `description` 字段且被前端读取**（`DataLoader.ts:176` 的 `★${rarity} ${i.description}`）⇒ D146 的实际动作是「**停止程序拼接、改读 `description`**」，并核对 80 条技能该字段是否已填充 |
| D182 | **新增两条代码发现 → 归入清理批次** | 见下。两条都归入 **D42 的清理批次**（与死字段一起处理），但**必须在 D5 分档落地前修**，否则分档会放大问题 |

> 🔴 **D182 详述：两条新发现（我已独立核对，附证据）**
>
> **① 70 条"伤害 + `effects`"技能的 `effects` 完全不结算**
> 证据：`DamageCalculator.ts:117-123` —— `isDamaging = power > 0 && targetsEnemy`，而 `applySupportEffects()` **只在 `!isDamaging` 时被调用**（`:120-122`）。
> ⇒ 只要技能 `power > 0` 且打敌人，它自带的 `effects`（`debuff` / `damage` / 加 HP 之类）**一个都不生效**，只有 `enhanced`（通过 `applyEnhancedEffects`）会生效。
> 规模：**88 条技能里 70 条**属于这一类（`power > 0` 且有非空 `effects`）。
> ⇒ 这不是"少数技能没做好"，是**配表里大半个技能库的附带效果处于静默失效状态**。玩家看到的文案与实战不符。
>
> **② 11 条 `target: 'ally_all'` 技能只作用于施法者本人**
> 证据：`CombatSystem.resolveTargets:477-479` —— `ally_all` / `self` 一律返回 `[actor]`（我方侧）。
> 而伤害公式把**出手者的 buff 当作全队增益**读（`teamWithBuffs()`，`CombatSystem.ts:218-232` 的"全体增益视为队伍增益"）。
> ⇒ 后果有两层：
> 1. "全队增益"只挂在**施法者一人**身上；**换人出手时增益不在那个单位身上**，于是"全队 buff"实际只对施法者本人有效；
> 2. 若按 D1 改成真共享 HP 池，**治疗**那半会自动变得正确（池子只有一个），但**增益**那半**仍然错**（buff 是挂在单位身上的数组）。
> ⇒ 所以 **① ② 必须一起修，且不能靠 D1 顺带解决。**

### 第 48 轮

| # | 议题 | 裁决 |
|---|---|---|
| D183 | **类 2 的降幅** | ✅ **不降**（保留 +4pp / +10pp / +10pp）⇒ **我定的 15% 硬约束放宽到 ~22%**，伤害轴最坏 **+21.98%** 被接受。⚠️ D108 重算时**必须把 +21.98% 计入** |
| D184 | **AP 类饰品** | ✅ **取消 AP 类饰品**（T1 过载抑制器 / T2 冗余电容 / R3 终末采样器 **三件整体删除**）⇒ 12 件变 **9 件**，见下方连带 |
| D185 | **陷阱口径** | ✅ **按 D179 字面：战斗后回满** ⇒ **D164 修订为「战斗回满、迷宫伤害不跨场」**（等价于原 (a) 方案） |
| D186 | **Boss 技能数量** | ✅ **D51 由"每个 2–3 个"改为"2–4 个"**，司祭保留 4 个技能（总数 13 条） |

> 🔴 **D184 的连带：饰品从 12 件掉到 9 件，必须补**
> 删掉三件 AP 件后，四类的分布变成：
>
> | 类 | 原 3 件 | 删除后剩余 | 缺口 |
> |---|---|---|---|
> | 1 属性加成 | A1 / A2 / A3 | 3 件 | — |
> | 2 连击·暴击·强化率 | P1 / P2 / P3 | 3 件 | — |
> | 3 战术效果 | T1（AP 升耗）❌ / T2（AP 上限）❌ / T3 | **1 件** | **缺 2** |
> | 4 每回合 1 次 | R1 / R2 / R3（AP）❌ | **2 件** | **缺 1** |
>
> ⇒ **需要新设计 3 件非 AP 的饰品**才能维持 D122 的"4 类 × 3 件"。
> 若不想再设计，则应把 **D122 从"4 类 × 3 件"修订为"9 件（3/3/1/2）"**。**下一轮拍板。**

> ⚠️ **D184 的另一处遗漏：`R2 相位反射片` 也是"我方输出来源"**
> 它的效果是「每大回合我方首次受击时，对敌人造成**队伍攻击力 × 30%** 的固定伤害」——**这是一条伤害**，属 D108 禁令范围内的"新输出源"（虽只约 +2.9%）。
> 取消 AP 类饰品的动机是"不要加行动次数"，但 R2 加的是**直接伤害**，同一逻辑下是否也该删/改？**下一轮一并拍板。**

> ⚠️ **D185 的后果（已知并接受）**：D168「迷宫 HP 归零即团灭」**几乎不可能触发** —— `trapCount` = 1/1/2/2，~~一趟最多踩 2 次（最坏约 40%）~~，且**下一场战斗结束就清零**。
> 🔴 **【勘误（第 79 轮回填）】上面划掉的那句是错的**：`MazeGenerator.ts:141-167` 的 `place('trap', config.trapCount, …)` 定义在 `generate()` 内，而 **`generate()` 每层调用一次** ⇒ `trapCount` 是**每层**语义，**一趟 6 层实际是 6 / 6 / 12 / 12 次**（见 **D349**）。
> ⇒ 结论（"几乎不可能触发"）**仍然成立，但理由换了**：不是"一趟只有 2 个"，而是 **D185 的战斗后回满把暴露区间压缩到"两场战斗之间"，而一趟约 35 场战斗 vs 6–12 个陷阱 ⇒ 平均每个间隔不到 1 个**。
> ⇒ 陷阱实际退化为"**在踩到之后、下一场战斗之前**那一段里的轻微风险"。
> ⇒ 与 **D170「不补新风险维度」**叠加后，支柱 B 的三条风险源里，**事件与迷路成为主要威胁，陷阱基本被架空**。
> 若日后认为支柱 B 太软，第一优先的补救是 **D172 的备选 (c)：让陷阱在已探索区域可见**（把"随机踩雷"变成"可规划的绕路"），而不是加回伤害。

### 第 49 轮

| # | 议题 | 裁决 |
|---|---|---|
| D187 | **饰品的件数** | ✅ **补足到 12 件**：类 3 补 2 件、类 4 补 1 件，**全部必须是非 AP 效果**，以维持 D122「4 类 × 3 件」。已派队员设计 |
| D188 | **R2 相位反射片** | ✅ **保留**（只约 +2.9%，且需"敌方打得到我"才触发，属**天然受限**的输出，不算 D108 意义上的"新输出源"） |
| D189 | **陷阱是否补救** | ✅ **不补救，接受** —— 陷阱退化为轻微惩罚，支柱 B 靠**事件与迷路**支撑 |
| D190 | **虚影的数值衰减** | ✅ **无根 ×0.5 / 苔痕 ×0.7 / 静谧 ×0.85 / 腐朽 ×1.0** |

> 📐 **D184 + D187 后的饰品最终结构（12 件）**

| 类 | 档 I（★3，魔铁矿） | 档 II（★4，深渊结晶） | 档 III（★5，魔核） |
|---|---|---|---|
| **1 属性加成** | A1 沉铁配重环 | A2 静默轴承组 | A3 深核供能芯 |
| **2 概率加成** | P1 节拍同步器（连击 +4pp） | P2 准星校准环（暴击 +10pp） | P3 脉冲放大器（强化 +10pp） |
| **3 战术效果** | T3 装甲解算器（单次受伤 ≤25% maxHp） | **待设计（非 AP）** | **待设计（非 AP）** |
| **4 每回合 1 次** | R1 自愈镀层（每回合回 4%） | R2 相位反射片（首击反击） | **待设计（非 AP）** |

> ⚠️ **D184 的删件记录（保留可追溯）**：`T1 过载抑制器`（取消临时 AP 升耗）、`T2 冗余电容`（AP 上限 +2 / 初始 +1）、`R3 终末采样器`（每回合 +2 AP）**三件整体作废**。
> 作废理由：它们加的是**行动次数**，而伤害类饰品加的是**每次行动的伤害**，两轴**相乘** ⇒ 六槽最坏组合 **+148%**（R3 单件即 +73%），远超 D108 的禁令量级。
> ⚠️ 副作用：`T1` 原本是**唯一一件正面回应 D123/D124「AP 耗尽没有兵底」的饰品**，删除后那条冲突**回到无缓解状态**。若日后觉得"AP 耗尽就结束阶段"太硬，第一优先是回到 T1 的设计，而不是改 D124。

> ⚠️ **D189 的现状记录（写清楚，便于日后翻案）**：支柱 B 的三条风险源里，**陷阱已被架空**：
> `trapCount` 1/1/2/2 + 战斗后回满（D185）⇒ 一趟最多踩 2 次（最坏约 40%），且**下一场战斗就清零**。
> 剩下两条：**事件**（`eventCount` 1/2/2/3）与**迷路**（D142 明确不做小地图）。这是有意的取舍，不是遗漏。

### 第 50 轮（第 40 轮后的检查点）

| # | 议题 | 裁决 |
|---|---|---|
| D191 | **迷宫 HUD 的血条** | ✅ **迷宫 HUD 不再显示血条**，只在战斗界面显示。理由：D185 后迷宫 HP 几乎总是满的，常驻显示是噪声 |
| D192 | **事件系统** | ✅ **扩充事件**（种类为主，数量未加。支柱 B 现在主要靠事件 + 迷路支撑） |
| D193 | **消耗品体系** | ✅ **大幅扩充到 8–10 种**（回血 / 解毒 / 护盾 / 增益 / 陷阱探测 / 传送等，做成一条道具线） |
| D194 | **剩余 50 轮的重点** | ✅ **先做经济核算** —— 金币产出口径至今没有实测支撑（D23 明确记过"金币产出尚未标定"）。其余待办顺延 |

> 📊 **第 50 轮检查点**
> - **累计裁决：194 条（D1–D194）**，共 50 轮
> - **新增草案 3 份**（在飞或已完成）：`content-draft-v1.md`（饰品/Boss/族群特性，已到 v3）、`enemies-draft-v1.md`（28 敌 + 2 精英）、`skill-texts-v1.md`（93 条技能文案）
> - **本轮新增的代码事实**（均已独立核对，见 §10 #12b / #12c）：
>   1. **70 条**"伤害 + `effects`"技能的附带效果**静默失效**
>   2. **11 条** `ally_all` 技能**只作用于施法者本人**（且 D1 只能修好治疗那一半）
>   3. **更正**：`combo_bonus` **不是** no-op（§10 旧结论与 D56 的前提都已过期）
> - **本轮被推翻/修订的既有裁决**：D164（陷阱不跨场）、D122→件数维持但删 3 件 AP 饰品、D51（2–3 → 2–4）、D146（字段名 `description` 已存在）、D56（不修）
> - **当前最硬的两个开放风险**：
>   1. **§10 #32 输出 ∝ N²**（D108 延后到内容落地后整体重算，且 D183 已接受饰品再叠加 +22%）
>   2. **经济核算空白**（D194 定为下一步）：金币产出、饰品 `price`、训练场汇率、商店与招募价格之间**没有任何实测支撑**

> 📌 **下一步（D194 的执行）**：先用项目里已验证的蒙特卡洛脚手架（`scripts/mc-difficulty.mjs` + `scripts/_phaser-stub.cjs`）实测**一趟 6 层的金币/道具产出**，再回头校准 D113/D167/D171/D120 这一串价格。

### 第 51 轮

| # | 议题 | 裁决 |
|---|---|---|
| D195 | **类 3 的档位缺口** | ✅ **接受不齐**：类 3 = **★4 ×1 + ★5 ×2**，缺口不加件 ⇒ **12 件不变**。**"档位"从此只代表材料档**（决定配方与掉落曲线），不再承诺"每类三档各一" |
| D196 | **"全队筋力 −12"的口径** | ✅ **全队**合计** −12**（= 当前代码语义：`teamWithBuffs()` 只把**出手者**的 buff 加进队伍合计属性）。⇒ **但四个 Boss 的 debuff 数值必须按图分级重定**，见下 |
| D197 | **伤害轴的真实上界** | ✅ **接受 +24.13%** ⇒ **D183 的上界正式改写为「伤害轴 ≤ +25%」**，并**同时记录两个口径**：实操 **+21.96%**（R2 在现行元游戏下不触发）/ 理论上界 **+24.13%** |
| D198 | **类 3 的乌龟流** | ✅ **接受三件同装，不加减伤叠加上限**。在裁决记录里写明代价：「**类 3 三件满装 = 放弃全部输出饰品**」 |

> 📐 **D196 的执行细则（必须照此落地，否则 Boss 数值会两极分化）**
> 口径定为"全队合计"后，**一个固定的 −12 在不同等级下差异极大**（因为 `attack = 队伍筋力合计 × 5`）：
>
> | 场景 | 队伍筋力合计 | −12 的占比 |
> |---|---|---|
> | L4（无根回廊） | ≈ 78 | **−15.4%** |
> | L8（苔痕回廊） | ≈ 151 | −7.9% |
> | L11（静谧墓所） | ≈ 191 | −6.3% |
> | L17（腐朽王座） | ≈ 325 | **−3.7%** |
>
> ⇒ Boss 的 debuff **不能四图统一用一个绝对值**。按图分级（草案原值 −12 保留在最终图）：
> **碎巢母 −4 / 菌王 −6 / 不眠的司祭 −9 / 王座上的等待者 −12**
> ⇒ 四处一律改写成「我方**合计**筋力 −N」的措辞（**不是**"全队筋力"，避免再被读成"每人 −N"）。
>
> ⚠️ 连带：**D187 的 `逆向解离针`（我方阶段开始清 1 条属性削减）的收益上界**——按 D196 口径，它一次最多恢复 **队伍筋力合计的 3.7%–15.4%**（按图与等级），属可接受区间；若当初按"每人 −12"（合计 −72）解读，则洗一次等于**队伍输出翻 12 倍**，属失控。**这也是 D196 必须定死的理由。**

> ⚠️ **D197 的口径差异必须留档**：v2 算 +21.98% 时**漏了一槽** —— R2 取代 A1 更优（因 A1 与 A2 的筋力都是 +2，A1 被 A2 覆盖）。真最优 6 槽 = `A2+A3 + P1+P2+P3 + R2` = **+24.13%**。
> 两个数字都要留在文档里，否则以后对不上账。**D108 重算时按 +24.13% 计入。**

> 📌 **D195–D198 后饰品定稿为 12 件**（类 3 的档位不齐是有意接受的结果）：

| 类 | 档 I（★3，魔铁矿） | 档 II（★4，深渊结晶） | 档 III（★5，魔核） |
|---|---|---|---|
| **1 属性加成** | A1 沉铁配重环（筋 2/耐 1） | A2 静默轴承组 | A3 深核供能芯 |
| **2 概率加成** | P1 节拍同步器（连击 +4pp） | P2 准星校准环（暴击 +10pp） | P3 脉冲放大器（强化 +10pp） |
| **3 战术效果** | —（缺口，D195 接受） | 空转惰轮（每回合首次受伤 ×0.5） | 装甲解算器（单次 ≤25% maxHp）/ 压舱锚针（比例伤害 ×0.5） |
| **4 每回合 1 次** | R1 自愈镀层（回 4%） | R2 相位反射片（首击反击） | 逆向解离针（清 1 条属性削减） |

### 第 52 轮

| # | 议题 | 裁决 |
|---|---|---|
| D199 | **技能文案与代码现状的对齐** | ✅ **先修代码，再填文案**。文案按**设计意图**写（配表声明的效果），但 **§10 #12b（70 条 effects 不结算）/ #12c（`ally_all` 只作用施法者）/ #10（治疗用 attack 而非魔力）列为文案落地的前置条件** —— 未修前不填，否则文案就是在说谎 |
| D200 | **15 条 `power=0` 纯辅助技（D10 遗留）** | ✅ **整套 buff 体系改为「战术」体系**（原文见下）：同种效果统一为同一战术、**每战术上限 5 层**、**持续 3 回合**、**每次释放后该战术的"CD"重置**。⚠️ 两处需澄清 |
| D201 | **剩余内容工作的推进** | ✅ **打包派给队友**：消耗品 8–10 种（D193）+ 事件扩充（D192）+ 15 条辅助技改造（D200） |
| D202 | **经济参数的授权范围** | ✅ **价格与产出都授权**我直接调整（据实测），只向用户汇总结果 |

> 📌 **D200 定案：buff → 「战术」体系（用户原文）**
> > 「所有 buff 类全部改成战术，所有同种效果但数值不同的 buff 技能全部统一成相同的战术，每个战术上限 5 层，每次释放战术后战术的 CD 重置，所有战术持续 3 回合」
>
> 拆成可执行的规则：
> 1. **收敛**：所有 `buff` 类效果统一改称**战术**（不再有"灵性 +5 / 灵性 +8"这种同效果不同数值的技能 —— 统一成同一个战术）；
> 2. **层数**：每个战术**最多 5 层**（同一角色身上的同一战术叠到 5 层封顶）；
> 3. **持续**：每个战术**持续 3 回合**；
> 4. **刷新**：每次再次释放该战术 ⇒ 持续回合数**重置为 3**（而非叠加时长）。
>
> 🔴 **两处必须澄清（下一轮）**
> - **(a) "CD 重置"的措辞**：冷却系统**已在早前的改造中被完全废除**（`Skill.cooldown` 标 `@deprecated`、`BattleUnit.cooldowns` 字段已删、D42 判它进死字段清理）。所以"战术的 CD"在现行系统里**不存在**。两种可能：**(i)** 用户说的就是"**持续时间刷新为 3 回合**"（我按此记录）；**(ii)** 真的要为战术**重新引入一个冷却**（那会与 D42 的清理冲突）。
> - **(b) 层数的效果**：5 层是**线性叠加**（每层给同一数值）还是**有衰减**？以及每层给多少（需要一张"战术清单"）。
>
> ⚠️ **D200 与 D10 的关系**：D10 原裁"**buff 只留攻击类，其余删除**"。D200 实际上**改写了 D10** —— 不再按"保留哪一类"取舍，而是**把 buff 全部收敛进战术体系并统一数值**。⇒ §10 里"D10 掏空一批技能"的待办风险**就此关闭**（技能不再因为"buff 被删"而变成耗 AP 无效果的废技）。
>
> ⚠️ **D200 的连带**：战术是**挂在单位身上的 buff 数组**（`BattleUnit.buffs`），而伤害公式**只读出手者的 buff**（`teamWithBuffs()`）⇒ `ally_all` 类战术技能即使修好目标解析，**仍要逐单位挂载**才能让每个出手者都吃到。这条与 §10 #12c 是同一个根，必须一起修。

---

## 0.9 经济核算实测（队员产出，2026-09-17）

**路径**：`design/drafts/economy-report-v1.md`（D194 的执行）。

### 🔴🔴 最重要的发现：我此前的成长曲线定标建立在错误的战斗数上

**证据（我已独立核对）**：`DungeonScene.ts:443`
```ts
enemyDensity: config.enemyDensity * (1 + (nextFloor - 1) * 0.12),
```
⇒ **敌人密度随层数放大**（第 N 层 ×(1+0.12(N−1))，第 6 层 ×1.60）。

而 §4.5 的成长蒙特卡洛（以及由它推出的 **D33 `expExponent 1.9`** 与 **D35 苔痕 density 0.064**）**没有计入这一项** ⇒ **战斗次数被低估 30–43%。**

| | 旧口径（漏了密度缩放） | 实测（含该缩放） |
|---|---|---|
| 无根回廊 | 2.00 级 | **2.69 级** |
| 苔痕回廊 | 2.75 级 | **3.43 级** |
| 静谧墓所 | 3.64 级 | **4.67 级** |
| 腐朽王座 | 5.88 级 | **7.33 级** |
| 连打四图 | — | **L4 → L14.7** |

⇒ **D33 / D35 的"台阶形状"依然成立（相邻差值仍递增），但绝对量级整体上移约 30%。** 需要重定标（下一轮）。

### 其余实测结论

| 项 | 实测值 |
|---|---|
| **一趟 6 层金币** | 无根 **2 234** / 苔痕 **3 388** / 静谧 **4 810** / 腐朽 **8 007**（合计 18 439；每层均值 372 / 565 / 802 / 1 335） |

> 🔴 **【第 92 轮更正】上表的绝对值是错的，真实现值更低**
> 队员**自报并订正**：那张表的基线用的是 `mc-economy.src.ts` 里 `DECIDED_DENSITY` 的**"决后密度"（D33/D35 的决策值）**，而 **D257 已经把 D33/D35 的密度动作全部作废** ⇒ 那是**一个从未落地的反事实**。
> ⇒ **正确现值**：一趟四图 **14 524**（**−18.8%** 相对 17 894 的旧基线）；逐图 **1 452 / 2 501 / 3 687 / 6 884**。
>
> **凡是引用了下列数字的地方，按此更正**：
> | 旧（错） | **正确** |
> |---|---|
> | 18 439（现值基线） | **17 894** |
> | 15 069（D350 之后） | **14 524** |
> | −18.3%（D350 的降幅） | **−18.8%** |
> | 招募 **3.0 次/趟** | **2.9 次/趟** |
> | 训练场出口 **36.6%** | **39.8%** |
> | 40 219（招募出口面对的金币池） | **≈39 100**（同步 −3%） |
>
> ✅ **结论全部不变**：**七项价格 0 项需要改**、循环 **13.3**（极端 **33.4**）、**宝箱与钱袋相关的量不受密度影响**（宝箱金 8 359 → 5 573、钱袋 1 750 → 1 166、材料 −4.86、科技点 −5.40）。
> ⇒ **这正是"绝对值会漂、比例不会漂"的实证** —— 与 D372 的结论（价格锚在比例上）同源。
| 金币来源结构 | **宝箱 + 钱袋占 55%**（与敌人强度无关）⇒ 前 3 层"小怪没用"的结构性原因 |
| 升级速度 | 2.69 / 3.43 / 4.67 / 7.33 级（见上） |

### 🔴 最失衡的一项：科技点超发 20 倍

- 一把打完四图 ≈ **228 点**，而 **D41 的科技树总价只有 79 点**（D23 原本只给战斗留 11 点）
- ⇒ **首次全清可点满 289%**，与 D23「首次全清点满约 80%」的目标相差一个数量级
- **建议的砍法**：普通怪 `techPoints` **归 0**、精英 **1 点**、Boss **重复击杀 2–3 点**（首通仍走 `firstClearTechPoints`，D156 已定为 10/12/14/16）

### 其余 4 项失衡（均为我按 D202 授权可直接处理的"价格类"）

| 问题 | 现象 | 处理方向 |
|---|---|---|
| **锻造费方向反了** | `price × 0.9` 让"自造"恒比"直购"**贵 15–21%**（材料另算），**违背 D167「反哺锻造主力」的原意** | 系数 **0.9 → 0.6** |
| **钱袋无限套利** | `item_coin_pouch` 买价 100 / 卖价 120 ⇒ 低买高卖 | `value` **120 → 60** |
| **训练场偏高** | 1 金 = 2 经验时，一趟战斗经验 ≈ 训练场花费的 90%（训练场几乎可替代下迷宫） | 系数 **0.5 → 1.0**（即 1 金 = 1 经验） |
| **招募偏便宜** | 固定 500 金相对单趟 2k–8k 的金币收入过易 | **500 → 1 500** |

> ⚠️ 另有两条**前提失效**的记录：
> 1. **D60 的前提已不成立** —— 宝箱金币**已随层上涨**（45 → 170），D60 想要修的"恒为 1 的 `config.floor`"问题已经不存在；
> 2. **魔核产出**：一趟约能凑 3 件的材料，与 12 件饰品的配方需求是否匹配仍需核算（待办）。

### 第 53 轮

| # | 议题 | 裁决 |
|---|---|---|
| D203 | **成长曲线重定标** | ✅ **接受实测为新基准**：四图 **2.69 / 3.43 / 4.67 / 7.33**，L4 → **L14.7**。**D33 的目标值据此改写，配表不动**（台阶形状仍成立：相邻差值 +0.74 / +1.24 / +2.66 严格递增） |
| D204 | **科技点超发** | ✅ **按建议砍**：普通怪 `techPoints` **归 0**、**精英 1 点**、**Boss 重复击杀 2–3 点**；首通仍走 `firstClearTechPoints`（D156 已定 10/12/14/16） |
| D205 | **战术的 CD** | ✅ **战术系统独立引入 CD**，**技能系统保持无冷却** —— 即两套系统从此**各自独立**：技能只有 AP 成本与升耗，战术只有 CD 与层数/时长。⚠️ CD 回合数未定，见下 |
| D206 | **精英怪的奖励** | ✅ **放大 ×2**（金币与经验按 ×2 结算，与 D118 的"属性 ×1.5 + 必掉材料"匹配） |

> 📌 **D205 定案：两套系统彻底分离**

| | 技能系统 | **战术系统（新）** |
|---|---|---|
| 成本 | **AP**（含 D5 分档 + 升耗） | —（不耗 AP？待确认） |
| 冷却 | **无**（冷却已被废除，D42 判进死字段清理） | **有 CD**（释放后 CD 重置/重新计时） |
| 叠层 | — | **每战术上限 5 层** |
| 持续 | — | **3 回合**，每次释放刷新 |
| 承载 | `Skill` + `Skill.effects` | **待定**（是否复用 `BattleUnit.buffs`，还是新增独立字段） |

> ❓ **D205 留下的三个待定（下一轮）**
> 1. **战术 CD 的具体回合数**（例如 2 / 3 / 5 回合）；
> 2. **释放战术是否消耗 AP** —— 若不耗，它就成了"免费行动"，与 D123「AP 分配是核心决策」直接冲突；若耗，则与技能的 AP 竞争就成立；
> 3. **战术清单**：有哪些战术、每层给多少、5 层是否**线性叠加**（这些是"同种效果统一"之后的必然产物 —— 原本 15 条 `power=0` 技能的不同数值要收敛成统一值）。

> ⚠️ **D203 的连带（重要）**：D33 / D35 的**具体数值动作**（`expExponent` 1.6→1.9、无根 density 0.035→0.060、苔痕 0.045→0.064、三小怪 exp 14/12/16→20/17/23）**本身仍然有效**（台阶形状是靠它们造出来的），失效的只是"目标值"这个标尺。
> ⇒ 但它们**从未落地过**，所以现在**需要重新判断**：按实测值（2.69/3.43/4.67/7.33），这套改动是否**仍然必要**？特别是无根回廊的 density 0.035→0.060 —— 那笔改动的目的是"填前期成长缺口"，而实测显示无根已经有 2.69 级，**缺口可能本就不存在**。**下一轮一并确认。**

> ⚠️ **D204 的连带**：普通怪 `techPoints` 归 0 后，**科技点的唯一来源变成"首通 + Boss 重复击杀 + 精英 + 任务奖励（D92）"** ⇒ D92 的任务奖励权重上升，它不再是"补充"，而是**主力来源之一**。

### 第 54 轮

| # | 议题 | 裁决 |
|---|---|---|
| D207 | **shrine「祈祷」白送永久属性** | ✅ **不再给属性，换其它奖励**（并保留"选择与代价"，不做纯白送）。理由：36 点/趟 = 秘药产出的 **10.6 倍**，把 `item_elixir` 整条线架空了 |
| D208 | **药水数值口径** | ✅ **改为百分比**（小药 **8% maxHp** / 大药 **20% maxHp**）。理由：队伍 maxHp 从 L4 的 1560 涨到 L17 的 6500（4.2 倍），固定值后期必然废掉 |
| D209 | **ambush「逃跑」支配「迎战」** | ✅ **埋伏事件不允许逃跑** —— 直接**删掉该选项**（不是加代价），玩家只能迎战 |
| D210 | **D185 让回血类事件失效** | ✅ **回血事件改为"送药水"事件** —— 直接给消耗品，由玩家自己决定何时使用 |

> 📌 **第 54 轮的输入：两份新草案（已审）**
> - `design/drafts/consumables-draft-v1.md` —— **9 种消耗品**（60–320 金，覆盖回血/解毒/护盾/增益/陷阱探测/传送）
> - `design/drafts/events-draft-v1.md` —— **11 种事件**（纯收益 3 / 风险抉择 5 / 纯负面 3）
> - 实测落盘：`scripts/_mc-economy-consumables.mjs` + `_mc-economy-consumables.txt`
>
> **队员交回的 3 个新发现（我已核对逻辑，均为真问题）**：
> 1. **shrine 白送 +3 永久属性**（→ D207）
> 2. **ambush「逃跑」严格支配「迎战」**（→ D209）
> 3. **spring 回血在 D185 后恒为 0 收益**（→ D210）
>
> **我作为 lead 直接落的 4 条**（D202 授权范围内）：
> - 事件净额取 **+757**（队员推荐）
> - **新建 `events.json`**：事件硬编码在 `MazeGenerator` 里是历史遗留，项目是数据驱动的（8 张配表 + `gen-tables`），应当配表化
> - **归途石保留** —— 队员以为"团灭代价未裁"，其实 D17 / D21 / D22 / D145 / **D168** 已裁全
> - **#17（重写 README）暂不授权** —— GDD 还在逐轮裁决（目标 100 轮），现在重写等于拿一份还在动的东西覆写另一份；**等定稿后一次性做**

> 🔴 **我在第 54 轮的第三次派单错误（已更正）**：我上一条派单里写了"**迷宫伤害跨场**"，那是照抄已被覆盖的 **D164** 旧文。**生效口径是 D185：战斗 HP 每场开满、迷宫伤害不跨场**（影响区间 = 踩到之后 ~ 下一场战斗结束）。
> ⇒ 这条错误的直接后果是：队员按"跨场"推算了事件扣血的权重，需要重算。**已发更正。**
> ⇒ **教训（第三次同类）**：我在把裁决转述给队友时，**引用了被后续裁决覆盖的旧文**。以后派单前必须先核对"这条裁决是否已被更新"。

### 第 55 轮

| # | 议题 | 裁决 |
|---|---|---|
| D215 | **CD=3 与 5 层的冲突** | ✅ **层数靠"反复释放技能"堆叠** —— **技能本身无 CD**（D205 已定技能系统无冷却），所以同一个技能可以重复释放，每次授予 1 层；**饰品给的是一次性的**。节流由 **AP 成本 + 升耗**承担，而不是靠战术自己的 CD |
| D216 | **"战术与饰品合流"的含义** | ✅ **饰品类 3 就是「授予战术的饰品」**（三件即授予机制型战术者）。⇒ 与 D211「饰品给战术」完全合流 |
| D217 | **战术的两个来源** | ✅ **饰品：战斗开局授予 1 次（免费、不耗 AP）；技能：每个技能各自绑定 1 个战术，释放该技能时附带授予（耗该技能的 AP）** |
| D218 | **是否加机制型战术** | ✅ **加**（除六维属性战术外，另有一批机制型战术） |

> 📌 **第 55 轮后的战术系统全貌**

| 维度 | **属性战术（6 个）** | **机制型战术（新增）** |
|---|---|---|
| 内容 | 六维各一（筋力/耐力/敏捷/幸运/灵性/魔力） | 非属性机制（回血 / 减伤 / 回 AP 之类） |
| 来源 | **技能**（每个技能绑定 1 个） | **饰品**（D216：饰品类 3 即授予者） |
| 授予频率 | 每次释放该技能 **+1 层** | **战斗开局一次性** |
| 节流 | **AP 成本 + 升耗**（连放同一技能越来越贵） | 无（一次性） |
| 叠层上限 | 5 层 | 待定 |
| 持续 | 3 回合，每次释放刷新 | **待定**（一次性授予若也只持续 3 回合，饰品会非常弱） |

> 🔴 **D215 实际取代了 D205 的"战术引入 CD"，必须确认**
> - **D205** 原裁：「战术系统独立引入 CD，技能系统保持无冷却」
> - **D215** 现裁：「**技能无 CD 所以可以叠加战术效果**」+「饰品给的是一次性的」
>
> 两者不能同时成立 —— 若战术有自己的 CD，同一个技能连续释放就不该叠加。
> **两种口径（下一轮确认）**：
> - **(i)** **战术没有独立 CD**，节流完全由技能的 **AP 成本 + 升耗**承担（同一技能连放会逐次涨价，这本身就是一种软冷却）⇒ **D205 的"战术 CD"作废**
> - **(ii)** 战术**保留** CD，则"技能无 CD 所以能叠加"仅在 **CD 已过**的前提下成立（CD 决定叠层速度上限）
>
> **我倾向 (i)**：它天然复用了既有的升耗机制（D12/D18），不需要为战术再造一套冷却，且与 D42「清理冷却」的方向一致。

> ❓ **本轮留下的三个待定（下一轮）**
> 1. 上述 **(i)/(ii)** 的口径确认；
> 2. **机制型战术的持续与层数上限**（一次性授予 + 只持续 3 回合 = 饰品几乎无价值）；
> 3. **机制型战术的清单**（有哪些、数值多少）—— 需要派活设计。

### 第 56 轮

| # | 议题 | 裁决 |
|---|---|---|
| D219 | **战术是否有 CD** | ✅ **取消战术 CD，改为"持续回合"** ⇒ **D205「战术独立引入 CD」作废**。**D200 的"CD 重置"正式解释为"持续时间刷新"**。节流由技能的 **AP 成本 + 升耗**承担 |
| D220 | **机制型战术的持续** | ✅ **饰品给予的机制型战术持续时间无限**（整场生效），与 D80「饰品被动生效」一致 |
| D221 | **机制型战术清单** | ✅ **派队员设计**（已派） |
| D222 | **属性战术每层的数值锚** | ✅ **按百分比**（每层给"该维的 x%"，自动适配等级，而不是固定小值） |

> 📐 **第 56 轮后的战术系统定稿口径**

| 维度 | **属性战术（6 个）** | **机制型战术** |
|---|---|---|
| 内容 | 六维各一 | 非属性机制（回血 / 减伤 / 回 AP 等） |
| 来源 | **技能**（每技能绑定 1 个） | **饰品**（D216：饰品类 3 即授予者） |
| 授予频率 | 每次释放该技能 +1 层 | **战斗开局一次性** |
| 节流 | **AP 成本 + 升耗**（无独立 CD） | 无（一次性） |
| 叠层上限 | **5 层** | 1 层（一次性） |
| 持续 | **3 回合**，每次释放**刷新**（不是叠加时长） | **无限**（整场） |

> ⚠️ **D219 的术语清理（写文档时要一致）**：从此**不要再用"战术 CD"这个说法**。
> - 属性的节流 = **AP 成本 + 升耗**；
> - 时长的控制 = **持续回合**（属性战术 3 回合 / 机制型战术无限）。
> ⇒ D200 原文里的"每次释放战术后战术的 CD 重置" = **"每次释放后持续时间刷新为 3 回合"**。

> ⚠️ **D222 的取值还需一次标定**：每层给"该维 x%" —— `x` 未定。
> 参考锚点：**饰品类 1 的单件 = 队伍该维的 3.3%**（D187/①.5 定标），而属性战术是**可由技能反复叠加到 5 层**的 ⇒ 两者必须不互相架空。
> **建议**：单层 `x = 2%`，**满 5 层 = 10% 该维**（略高于 3 件饰品的合计 10%，因为它需要消耗 5 次技能释放的 AP 与升耗成本）。**下一轮定。**

### 第 57 轮

| # | 议题 | 裁决 |
|---|---|---|
| D223 | **叠层在当前战斗长度下永不生效** | ✅ **上限先降到 3 层**（3 回合内可达），**5 层作为 D108 重算后再上调的预留**。文档要写明"战术系统在现行战斗长度下只有 1–2 层的体验" |
| D224 | **烟丸 vs D50「Boss 战禁用逃跑」** | ✅ **Boss 战禁用烟丸**（D50 优先）—— Boss 战就是"必须打完"，烟丸只在普通战可用 |
| D225 | **D33/D35 是否执行** | ✅ **按新基准重新标定一套**（重跑含密度缩放的 MC，产出新的参数组合）—— 已派 economy-analyst |
| D226 | **魔力战术是空壳** | ✅ **删掉魔力战术** ⇒ 属性战术 **6 → 5 个**（筋力/耐力/敏捷/幸运/灵性） |

> 🔴 **我在第 57 轮发现自己的第 4 个错误：把"升耗"描述错了**
> 我在给队员的派单里写"同一技能连放会**逐次涨价**，这本身就是软冷却"。核对 `CombatSystem.skillCost()`：
> ```ts
> const base = skill.apCost ?? 1;
> if (skill.escalation === 'permanent') return base + (unit.permStack[skill.id] ?? 0);  // ← 这才是累加
> return base + (unit.usedLastRound.has(skill.id) ? 1 : 0);                              // ← 恒定 +1，不累加
> ```
> ⇒ **`temp` 升耗是"恒定 +1"，不是累加**；只有 `escalation: 'permanent'` 才是累加，**而全部 88 条技能里 `permanent` 一条都没启用**（§10 已记录）。
> ⇒ **D219 的"节流由 AP 成本 + 升耗承担"这个前提比实际弱**：连放同一技能的边际成本恒定在 `base + 1`，不会越来越贵。真正的限制器只有**每回合 AP 预算（3 → 8，上限 8）**。
> ⇒ **下一轮需要重新判断**：是否要把 `temp` 升耗改成累加（那是唯一能让"连放同一技能"真正变贵的机制），或接受当前这样。

> ⚠️ **队员在战术 v2 里报的其他几项**：
> - `x = 2.3%/层`，第 4/5 层 ×0.67，满层 ≈10%（与我的候选 2% 同量级，**采纳**）；
> - **机制型战术 6 条**（类 3 三件 → 空转卸载 / 装甲解算 / 压舱配平；类 4 三件 → 镀层闭合 / 相位反射 / 逆向解离），受"优先级"约束**恒只 1 条生效**；
> - ⚠️ **必须做"合流"**：饰品类 3/4 的效果与机制型战术是**同一件事的两种表述**，**结算只留一处真源**，否则会双重结算；
> - ⚠️ **Q5 冲突**：D217「每个技能绑定 1 个战术」与 v1 里那"2 条双战术技能"矛盾 ⇒ **裁定：每个技能只绑定 1 个战术**，两条双战术技能改为"1 战术 + 1 直接效果"。

### 第 58 轮

| # | 议题 | 裁决 |
|---|---|---|
| D227 | **`temp` 升耗是否改累加** | ✅ **不改，保持恒定 +1**。每回合 AP 预算（3 → 8）就是唯一限制器，叠层因此在 AP 内**很快**（约 2–3 回合叠满） |
| D228 | **机制型战术与饰品的真源** | ✅ **战术侧是真源** —— 饰品只声明"授予哪个战术"，数值全在战术表里，避免双重结算 |
| D229 | **3 处"战斗长度导致机制空转"** | ✅ **继续问答，把这类机制统一标为「待 D108 重算后验证」**，不因它们去改战斗长度 |
| D230 | **剩余轮次规划** | ✅ **继续问答**（不预设分块，按当轮缺口走） |

### 第 58 轮（lead 裁定：技能文案 v2 暴露的实现级问题）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D231 | **战术授予：加字段还是机械推导** | ✅ **一个字段都不加** —— 授予关系由现有 `effects[].stat` + `enhanced[].stat` **机械推导**（≈6 行）。理由：单数字段 `tactic?: string` 装不下实测的"1 技能授 3 条战术"与"2 层 + 1 层"；且加字段会制造**第二个真源**（正是 D228 最忌讳的）。**例外**：鹿角祝福 / 天使赐福**只有 heal、无任何 `stat`** ⇒ 显式兜底"纯治疗技 → 授予相位增幅"，否则这两条一条战术都授不出 |
| D232 | **文案字数上限** | ✅ **放宽到 ≤55 汉字**（不选"交 UI 渲染"，否则 #3/#11 两条纯授予技的 `description` 会变成空串） |
| D233 | **8 条"削其它属性"的本体 debuff** | ✅ **一律改为削弱敌方 `attack` 或 `defense`**。依据：**敌方单位只使用 `attack` 与 `defense` 两个轴**（`DamageCalculator` 的敌方伤害 = `attacker.attack × power`；`speed` 已不参与出手顺序，魔力/幸运/灵性/敏捷在敌方侧无任何读取点）⇒ 削这四维**在设计上就是空效果**，不是数值问题 |
| D234 | **Boss 侧 4 条我方 debuff 是否同源** | ✅ **同源**，按 **D196** 口径：写成"我方**合计** XX −N"，且按图分级（−4 / −6 / −9 / −12）。我方侧这些轴**是有战斗效果的**（筋力→attack、耐力→maxHp、幸运→暴击率、灵性→强化率），所以照常生效 |
| D235 | **治疗量口径（§10 #10 结案）** | ✅ **正式定为"攻击力派生"**（承认现状并写进设计）：`治疗量 = 队伍攻击力 × 倍率 + 灵性 × 0.5`。理由：**魔力在战斗内完全无效**（D226 已因此删掉魔力战术），若坚持"魔力派生"会让治疗量直接崩塌。**§10 第 10 条就此关闭** |
| D236 | **两条挂起技能的收尾** | ✅ 黑裙礼赞 / 绯冠加冕的 `enhanced: buff magic +10` **改轴为灵性 +10**（并入相位增幅）—— 因为 D226 已删除魔力战术 |

> 📌 **§10 #12c 降级**：`ally_all` 只作用于施法者这条，被 **tactics 草案 §3.2 的"队伍级 `state.tactics`"** 绕过了 **buff 那一半**（战术挂队伍级，不挂单位）⇒ **只剩"治疗"那一半仍需 D1 修**。**#12c 从"整条 bug"降级为"半个问题"**，记录在案。

### 第 59 轮

| # | 议题 | 裁决 |
|---|---|---|
| D237 | **清完四图之后玩什么** | ✅ **不加终局内容** —— 四图清完就是"玩完"，玩家靠**刷材料 / 科技点 / 饰品**继续（呼应 D2「不做全局结局」） |
| D238 | **任务系统的具体清单** | ✅ **派队员设计**（主线 4 条 + 支线 8–12 条，含名称、达成条件、奖励数值） |
| D239 | **新手引导的文案与形式** | ✅ **派队员写文案**（D43/D98 的四个触发点：首次进战 / 首次下探 / 首次踩陷阱 / 首次招募） |
| D240 | **落地顺序** | ✅ **先做不依赖战斗长度的内容**（数据 / 配表 / UI：任务面板、商店、锻造、事件、难度选择、统计），**把"需长战才生效"的一律标为待 D108** |

> 📌 **D237 的连带（写清楚，避免以后误以为漏了）**
> 「四图清完 = 玩完」意味着**终局体验就是"刷"**。它带来三个必须在落地时保证的前提：
> 1. **D105 的统计面板**要有可展示的长期凭证（通关次数 / 击败数 / 最高层），否则"刷"没有反馈；
> 2. **D204 砍掉普通怪科技点后，任务奖励（D92）成为科技点主力来源** ⇒ 主线 / 支线任务必须能**重复完成**（至少支线），否则一次性任务做完后科技点就断供；
> 3. **饰品的锻造材料必须可重复获取**（精英 15% / Boss 重复击杀 / 事件给材料）⇒ 否则 12 件饰品做完后终局无目标。

> 📌 **D240 的两批清单（落地时照此分）**

| 批次 | 内容 | 理由 |
|---|---|---|
| **批次 1（不依赖战斗长度）** | 任务面板 / 商店 / 锻造 / 事件（`events.json`）/ 难度选择界面 / 统计面板 / 消耗品 / 装备与饰品 UI / 训练场 / 教程引导 / 死字段清理（D42） | 价值与"战斗打几回合"无关 |
| **批次 2（待 D108 重算后验证）** | **战术叠层**（D223）、**深渊吞噬**（D159）、**饰品防御类效果**、**精英二动**（D151）、**R2 相位反射片**、**D108 的 N² 失衡本身** | 这些机制在 1–2 回合的战斗里数值为 0 或近 0 |

---

### ⚠️ 第 60 轮的输入：队员 v3 **推翻了 D223 的前提**（我的第 5 处错误）

我在派单里让队员写下结论「战术系统在现行战斗长度（1–5 回合）下只有 1–2 层的体验」。**队员拒绝写入，并给出反证**：

| 项 | 队员实测口径 |
|---|---|
| **叠到 3 层** | **1 回合 / 3 AP** |
| **叠到 5 层** | **2–3 回合 / 5–7 AP** |
| 真正的瓶颈 | 不是"叠不叠得起来"，而是**回本要 ≈23 回合**（远长于 1–5 回合的实际战斗）|

**为什么叠得这么快** —— 两条我此前都没注意到的机制事实：
1. **`temp` 升耗是恒定 +1，且只在 `endRound()` 滚动** ⇒ **同一个回合内重复释放同一技能完全不涨费**（`usedLastRound` 要到本回合结束才更新）；
2. 代码里**没有"每单位每回合限出手 1 次"这个锁**（UI 与 `size` 都没有）⇒ 一个单位在一回合内可以连放多次。

⇒ **D223 的"降到 3 层"理由不成立**（上限设几层其实无差别，因为瓶颈是回本而非叠层）。**结论是否保留需重新拍板。**

> 🔴 **这条还牵出一个更硬的矛盾：D9 至今未实现**
> **D9** 明确裁过「**同一单位每回合限出手 1 次**」，而代码**没有这个锁**，队员的"1 回合 3 层"正是建立在此之上。
> ⇒ **两套口径下叠层速度差一个数量级**：
> - **若落实 D9**：一个单位一回合最多出手 1 次 ⇒ 叠 3 层要靠**不同单位绑定同一战术**（而 D217 是"1 技能绑 1 战术"，不同角色的技能绑定未必相同）⇒ 叠层会显著变慢；
> - **若不落实 D9**：一回合连放 3 次同一技能 ⇒ 3 层。
>
> ⇒ **D9 是否真正落实，是战术系统数值的前置条件**，必须一并定掉。

### 第 60 轮（第 50 轮后的检查点）

| # | 议题 | 裁决 |
|---|---|---|
| D241 | **战术层数上限** | ✅ **维持 3 层** —— 但**理由改写**："不是叠不上去，而是**回本要 ≈23 回合**，远长于 1–5 回合的实际战斗" ⇒ 上限设几层无实质差别，取小值以免误导 |
| D242 | **D9 的落实口径** | ✅ **改为「每个技能每回合限使用 1 次」** —— 这是对 **D9「同一单位每回合限出手 1 次」的修订**：同一单位一回合内**可以放多个不同技能**，但**同一技能不能重复** |
| D243 | **剩余 40 轮的节奏** | ✅ **保持原速**（每轮 4 问） |
| D244 | **6 份草案的归档** | ✅ **并入 GDD 正文**（各成一节，带完整数值表），草案 md 保留为"推导过程"引用 |

> 📐 **D242 定案：D9 的修订版（这条会连锁影响战术叠层）**

| | 旧口径（D9） | **新口径（D242）** |
|---|---|---|
| 限制对象 | **单位**：每单位每回合限出手 1 次 | **技能**：每（单位，技能）对每回合限 1 次 |
| 一回合能放几次 | 1 次 | **等于该单位拥有的不同技能数**（通常 2） |
| 对连招的影响 | 完全禁止 | **允许"技能 A → 技能 B"的两连**，禁止"A → A" |

> ⚠️ **D242 的连锁（必须重算）**：战术叠层速度**直接取决于这条**。
> 队员 v3 算的"3 层 = 1 回合 / 3 AP"**建立在"同一技能可连放 3 次"之上**，而新口径**禁止了 A→A**。
> ⇒ 同一战术要叠 3 层，需要**3 个不同技能都绑定该战术**（或跨单位叠加）⇒ **叠层速度显著变慢，需重算**。已发更正给队员。

> ⚠️ **D241 的口径说明**：所谓"**回本 ≈23 回合**"是指 —— 把一个战术叠到满层所花的 AP，若改用于直接打伤害技能，需要约 23 回合才能在伤害上追平。**这是战术系统真正的价值瓶颈**，也是它被列入"待 D108 重算"（D240 批次 2）的原因。

> 📌 **D244 的执行方式（由 lead 亲自做，不派队员）**
> 6 份草案的数值表要并入 GDD 正文，**风险是 clobber**（草案还在被队员改、GDD 也在被我改）。因此采取：
> 1. 等 6 份草案**全部冻结**（目前：饰品/Boss ✅、消耗品 ✅、事件 ✅、技能文案 ⏳、战术 ⏳、敌人 ⏳）；
> 2. 由我按 **§11 内容附录** 的编号一次性并入（每份一节 + 数值表 + 指向草案文件的"推导过程"链接）；
> 3. 并入后草案 md **不再修改**（改 GDD）。

### 第 61 轮

| # | 议题 | 裁决 |
|---|---|---|
| D245 | **科技点总量失衡** | ✅ **宝箱不再产科技点**（宝箱应给金币/道具），其余来源不动。⚠️ **但算术仍不达目标，见下** |
| D246 | **两处口径重叠** | ✅ ① **首通 52 拆成「首通 38 + 主线任务 14」**（每图仍是 10/12/14/16，总量不变，消除重复计费）；② **Boss 重复击杀以 D204 为准（2–3 点）**，D156 的 4/5/6/8 **作废** |
| D247 | **迷宫濒死提示** | ✅ **用氛围提示代替数值条**：**全屏边缘渐晕 + 心跳音效**（纯氛围，不给数字）。⇒ D191「迷宫 HUD 无血条」的配套 |
| D248 | **引导文案的数字** | ✅ **用模板占位**（如 `{RECRUIT_PRICE}`），落地时从配表取 —— 避免重演 `CD 3` 式的过期文案 |

> 🔴 **D245 之后仍不达标（必须下一轮解决）**
> 砍掉宝箱科技点后，首清四图的科技点供给 = **121 点**（首通 38 + 主线 14 + 精英 31 + 支线 24 + Boss 重复），而 **D41 的树总价仍是 79 点**
> ⇒ **首清可点满 153%**，D23 的「约 80%」仍差一倍。
> ⇒ 你连续两次都选了"砍来源"（D204 砍普通怪、D245 砍宝箱）而不是"改价格"，但这个方向**算不到 80%**：
> - 若要靠砍来源达标，需要把 **精英 31 → 0**、**主线+支线 38 → 0**，那任务系统与精英就都不能给科技点了；
> - 若接受"首清即可点满"，等价于**正式废掉 D23 的 80% 目标**。
> **下一轮三选一（见 §第 62 轮）。**

### 第 61 轮（lead 裁定：技能文案 v2 的 3 项 + 1 处我的勘误）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D249 | **11 条 debuff 的量纲** | ✅ **一律改为百分比**。队员实测：沿用绝对值 12/14/15 时，**浅层削 −33%~−60%、深层只 −5%** —— 与 D196 完全同构的问题（绝对值的意义随等级漂移） |
| D250 | **"自代价"类 debuff** | ✅ **保留原样，不改为削敌**。**我 D233 的一刀切误伤了这一条**：`清雪居合` 的"自身敏捷 −8"是**设计上的取舍代价**，不是"削敌方无战斗效果"问题 ⇒ D233 的适用范围**限定为"作用于敌方"的 debuff** |
| D251 | **11 条伤害句里的「魔力」** | ✅ **去掉该表述**。依据：伤害公式只有 `team.attack × power`（`DamageCalculator.ts:126`），且**魔力在战斗内完全无效**（D226 已因此删掉魔力战术）。文案不得出现公式里没有的量 |
| D252 | **我的第 6 个错误（勘误）** | ❌ 我在派单里说"黑裙礼赞的 `enhanced` 是 `magic`" —— **不成立**。队员核对：它的 `magic +14` 在 **`effects`**，`enhanced` 是敏捷；**只有绯冠加冕的 `enhanced` 是 magic**。已更正 |

> ⚠️ **D247 的落地注意**：氛围提示**没有数字**，所以玩家的判断依据只有"渐晕强度 + 心跳频率"。
> ⇒ 建议把渐晕做成**三档**（HP < 50% 轻度 / < 30% 中度 / < 15% 重度+心跳加快），否则"看不见数字又分不清轻重"会让 D168（归零即团灭）显得像随机惩罚。

### 第 62 轮

| # | 议题 | 裁决 |
|---|---|---|
| D253 | **科技点总量尾差** | ✅ **扩树节点而非涨价**（16 项 → 约 30 项，节点单价维持）。⚠️ 但 D254/D255 又把供给砍了一截，算术需再校，见下 |
| D254 | **精英的科技点** | ✅ **完全不给科技点**（精英只给金币 ×2 / 经验 ×2 / 必掉材料） |
| D255 | **Boss 重复击杀的科技点** | ✅ **重复击杀不给科技点**（首通仍走 `firstClearTechPoints`，D246 拆为首通 38 + 主线 14） |

> 🔴 **D254/D255 之后，科技点供给被砍到只剩三处（这是第三次数值对不上，必须收口）**
>
> | 来源 | 首清四图的量 |
> |---|---|
> | 首通（38） | 38 |
> | 主线任务（14） | 14 |
> | 支线任务（24，**可重复**） | 24 |
> | 普通怪 | **0**（D204） |
> | 精英 | **0**（D254） |
> | Boss 重复 | **0**（D255） |
> | 宝箱 | **0**（D245） |
> | **合计** | **76 点** |
>
> 而 D253 选的"扩节点"方案是按**当时算的 121 点**评估的（结论"= 树的 78%"）。**供给已变成 76**，所以：
>
> | 树的定位 | 总价 | 首清 76 点占 |
> |---|---|---|
> | **a. 维持 D41 的 79 点**（不扩节点） | 79 | **96%** —— 首清基本点满，"终局靠刷"落空 |
> | **b. 扩到约 95 点**（≈ +4~6 个节点） | 95 | **80%** ✅ **恰好命中 D23 的目标，且留 20% 给终局刷** |
> | c. 扩到 155 点（原 D253 评估值） | 155 | 49% —— 半棵树点不动，需恢复某个产出源 |
>
> **我强烈倾向 (b)**：它是三个数字里唯一同时满足「扩节点（D253 的方向）」「不涨价」「80% 目标」「留刷的空间」的。
> **下一轮确认。**

> ⚠️ **D254/D255 的连带（必须记录）**：**战斗不再产出任何科技点**（普通怪 / 精英 / Boss / 宝箱全部为 0）⇒
> **终局刷科技点的唯一途径是"重复完成支线任务"**（D237 说终局靠刷，而 D91/D92 的支线恰好可重复）。
> ⇒ 支线任务的**数量与重复价值**因此变成了终局体验的承重结构，10 条是否够需要评估。**下一轮一并问。**

### 第 62 轮（lead 裁定：D225 成长重标定结案）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D256 | **成长曲线采纳 Z 方案** | ✅ **`expExponent` 1.6 → 1.9；三小怪 exp → 25 / 21 / 29；密度不动** ⇒ ★4 四图 **2.01 / 2.88 / 4.10 / 6.30**（台阶 **+0.87 / +1.22 / +2.20**，严格递增）；★5 = 1.41 / 2.04 / 3.26 / 5.41；连打四图 **L4 → L13.3** |
| D257 | **D35 的密度动作作废** | ✅ **不执行**（无根 density 0.035→0.060 / 苔痕 0.045→0.064 全部撤销）。理由：它的达标**靠"无根 32 → 54 场"的战斗数暴涨买单**，会让**前两图的战斗数超过腐朽王座** —— 一个"越浅的图打得越多"的荒谬结构 |
| D258 | **腐朽偏快的正确修法** | ✅ **把 3 只旧 Boss 移出 `enemyPool`**（它们变成精英/Boss 后本就不该留在普通池里）⇒ 腐朽 7.32 → **6.30**；D2 的"第 6 层固定 Boss"落地后为 **6.85**。⇒ **不再靠抬指数去压后期** |

> 📌 **D256 的意义：这修正了我在 §4.5 的一整套定标**
> 我原来的 D33 组合是「`expExponent` 1.9 + 无根密度 0.060 + 苔痕 0.064 + 三小怪 exp 20/17/23」。
> 现在拆开看：**指数那半是对的（保留并加强到 25/21/29），密度那半是错的（撤销）**。
> ⇒ **教训：达标方式本身要审查**。D35 当初"台阶恢复"的漂亮结果，是靠一个**不合理的手段**（把浅层图的战斗数堆到比深层还多）换来的 —— 我当时只看了"差值是否递增"，没有检查**绝对值是否合理**。
>
> ⚠️ **敏感度已核**（可接受的边界）：精英 15% 概率替换会让每场经验 ×1.15，升级数 **+7.7% ~ +12.1%**（腐朽 ≈6.8），可接受；**真风险是 D148 的换池**（28 只敌人分浅/深两组会改变加权经验/场），但关系式已写入 `growth-recalibration-v1.md`，**按 ±5% 验收即可，不必重跑蒙特卡洛**。

### 第 62 轮（lead 裁定：战术 v4 的实测收紧）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D259 | **`power=0` 技不掷强化** | ✅ **按"修代码"处理**（不是删文案）。理由：**配表里已经写了 `enhanced`，那是设计意图**；删句等于把 15 条技能的效果直接砍掉（内容损失），而修代码是让配表意图生效。⇒ 列入 §10 与 D42 的清理批次，**文案保留强化句** |
| D260 | **层数硬上限 = 承载技能数** | ✅ **接受"上限 = min(3, 承载技能数)"**，不补承载技能。⇒ "预留 5 层"的表述改为"**须先补承载技能才可启用**"（否则是空头支票） |
| D261 | **D242 是否约束敌方** | ✅ **只约束我方**。敌方按 **D151** 的类别规则（普通 1 次 / 精英 2 次且每 3 轮 1 次大招 / Boss 按 D111 的循环） |
| D262 | **D242 后的叠层实测** | 记录在案：**叠 3 层 = 1–2 回合 / 3–5 AP / 需要 3 个不同技能 × 3 名不同角色**（因每角色只有 1 条战术技）。第 1 回合可达的只有 **火力校准 / 结构加固 / 相位增幅**；机动节拍 4 AP、弹道校准 5 AP 需 2 回合 |
| D263 | **各战术的承载数** | 机动节拍 **6** / 相位增幅 **6** / 结构加固 **5** / 火力校准 **4** / **弹道校准 3（零冗余：可可 + 琳 + 白雪 三人必须全上阵）** |

> 🔴 **D259 是一条实质性的代码缺口（记入 §10）**：强化（enhance）判定发生在**伤害路径**内 ⇒ **`power=0` 的纯辅助技永远不会触发强化** ⇒ 配表里为它们写的 `enhanced` 效果**永不生效**。
> 这与 §10 #12b（70 条"伤害+effects"的 effects 不结算）是**同一类"配表写了但代码不读"**的问题。

> ⚠️ **D260 的一条真实约束（写清楚，避免以后当成 bug）**：层数上限**不是**"策划定的 3"，而是**被承载技能数物理限制**的（弹道校准只有 3 条技能能授予它）。⇒ 若要给某个战术提高上限，**必须先增加承载它的技能**，否则调上限无效。

### 第 63 轮

| # | 议题 | 裁决 |
|---|---|---|
| D264 | **科技树的规模** | ✅ **扩到 ~95 点**（新增 4–6 个节点，节点单价维持）⇒ 首清 76 点 = **80%**，恰好命中 D23 目标，且留 **20%** 给终局刷 |
| D265 | **终局循环的定义** | ⏸ **先不定义**（保留 D237 的"清完四图 = 玩完"表述，不展开"刷什么、刷到什么时候"） |
| D266 | **精英的价值锚** | ✅ **精英再加"必掉高稀有度物品"**（魔核 / 秘药）—— 补强刷精英的驱动力（原仅"金币经验 ×2 + 必掉材料"） |
| D267 | **存档 schema 规格** | ✅ **派队员出规格**（含新字段清单、版本号、迁移拒绝规则） |

> 📐 **D263 + D264 之后的科技点经济（待 analyst 复核后定稿）**

| 项 | 值 |
|---|---|
| **供给（首清四图）** | **76 点** = 首通 38 + 主线 14 + 支线 24（普通怪 / 精英 / Boss 重复 / 宝箱 **全部为 0**） |
| **树的规模** | **~95 点**（16 项 → 约 20–22 项） |
| **首清占树** | **80%** ✅ |
| **终局刷来源** | **仅"重复完成支线任务"**（D254/D255 之后战斗不再产出科技点） |

> ⚠️ **D265 的已知代价（记录在案，等日后翻案）**：不定终局循环 ⇒ 玩家清完四图后的体验**没有任何被设计过的目标**。
> 与 D237 的连带三条（统计要有凭证 / 支线可重复 / 材料可重复获取）**仍然成立**，它们保证"能刷"，但**不保证"有理由刷"**。
> ⇒ 落地时若发现玩家在第 4 张图通关后立即流失，**第一优先的补救是补一条终局循环**（D265 翻案），而不是加新内容。

### 第 64 轮

| # | 议题 | 裁决 |
|---|---|---|
| D268 | **D108 重算的定义** | ✅ **定义为"把战斗从 1–2 回合拉到 5–10 回合"（TTK 中位）**，并要求给出 **TTK/TTD 的目标区间 + 验收脚本**（直接复用 `scripts/mc-difficulty.mjs` 跑回归）。⇒ 所有挂在"待 D108"上的机制从此有了明确的解锁条件 |
| D269 | **技能解锁** | ✅ **开局全解锁**（每个角色 2 个技能一开始就都能用）⇒ **`slot.unlocked` 归档为死字段**，并入 **D42** 的清理清单 |
| D270 | **装备槽的开放时机** | ✅ **两类槽位开局就有**（每角色 1 饰品 + 1 消耗品）；"用得上"靠**内容解锁**（先得锻造出饰品），不靠系统解锁 |
| D271 | **潜力是否可提升** | ✅ **不可提升** —— 潜力是角色的"底子"，生成时随机一次即固定；它是 D66「稀有度价值靠技能倍率体现」的一部分 |

> 📐 **D268 的验收标准（写清楚，这是"待重算"批次 2 的解锁闸门）**
> - **TTK 中位**：从现状 **1.0 – 2.1** 提升到 **5 – 10 回合**；
> - **TTD**：全 96 格不得出现"1 回合团灭"；
> - **难度倍率**（D73）在重算后必须**可感知**（简单与噩梦的 TTK 差值不再被淹没）；
> - **验收脚本**：直接跑 `scripts/mc-difficulty.mjs`（它已能输出 TTK/TTD/胜率/`HP×N→75%胜`四列），**回归阈值写进脚本**。
>
> ⚠️ **D268 的连带**：批次 2 的所有机制（战术叠层 / 深渊吞噬 / 饰品防御类 / 精英二动 / R2 反击 / 饰品 `+24.13%` 的伤害轴）**在重算完成前不得验收**。
> 而 **D183 已接受的 +24.13%** 与 **D256 的成长曲线**都要在重算时**一并纳入**（它们都是"我方更强"方向的改动，会抵消拉长战斗的努力）。

### 第 64 轮（lead 裁定：`applySupportEffects` 的落点 bug —— 本轮最严重的一条）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D272 | **buff / debuff 的落点** | 🔴 **必修，且是 D233 / D249 的前置条件**。我已核对 `DamageCalculator.ts:276-287`：`applySupportEffects` 把 `buff`/`debuff` **直接 push 到 `caster`**，而该函数的签名里**根本没有 targets**（只有 `{caster, skill, team, log}`）⇒ **所有增减益的落点恒为施法者，与 `skill.target` 无关** |

> 🔴🔴 **D272 详述（这是 §10 里最严重的一条，等级高于 #12b）**
>
> **现实后果（三条，全部荒谬）**：
> 1. **Boss 的削我方技能全部作用在 Boss 自己身上** —— 母巢尖啸 / 蜜腐诱导 / 墓前静默 / 等待者的凝视（`target: enemy_all`）⇒ **Boss 在给自己上 debuff**（队员实测发现）；
> 2. **我方的"削弱敌方"技能会削弱自己** —— ⇒ **D233 / D249 让 8 条技能改削敌方 `attack`/`defense`，在修好之前会造出"自伤技能"**；
> 3. **§10 #12c 的真因不是 `resolveTargets`** —— 那是表象；真因是这个函数**收不到 targets**。
>
> ⇒ **对 §10 的修正**：
> - **#12c 从"`ally_all` 只作用于施法者"升级为"所有增减益落点恒为施法者"**；
> - 它与 **#12b（70 条"伤害 + effects"的 effects 不结算）** 合起来意味着：**配表里几乎所有 `buff`/`debuff` 的落点语义都不成立**。
> - 修复顺序（**必须照此排队**）：**先修落点 → 再修 #12b 的结算路径 → 再落 D233/D249 的数值改动**。顺序错了就会往配表里写入"自伤技能"。
>
> ⚠️ **一条缓和**：我方"全队增益"那半**碰巧能用** —— 因为 `teamWithBuffs()` 把**出手者的 buff 当作全队增益**读（`CombatSystem.ts:218-232`）。所以"buff 落在施法者身上"在"施法者自己出手时"效果正确；**换人出手时增益就不在了**（这正是 D231 把它绕开的原因：战术改挂队伍级 `state.tactics`）。

---

## 0.10 敌人草案 v1（队员产出，已审）

**路径**：`design/drafts/enemies-draft-v1.md`。

**规模**：**28 只普通敌**（每图 8 = 浅层组 4 + 深层组 4）+ **4 只专属精英**（无根/苔痕各新增 1，静谧沿用守护者、腐朽沿用暴君）+ **4 只「深渊之王的虚影」**（四图通用、按 D190 衰减 0.5/0.7/0.85/1.0）+ **新增 9 条敌方技能**。

**三条自检（都已做，值得肯定）**：
1. 现有怪**全留原 id 与现有技能**，**未撞 88 条既有技能 id**；
2. 1 级与第 6 层的攻/HP/防**三轴均 ≤ 现役最强（骸骨暴君）**；
3. **每族特性都是"单侧收益"**，因此**不会 ×队伍人数**。

> 🔴 **队员的"关键改口"与 D121 冲突（必须拍板）**
> 队员把 4 条族群特性**重新定义为"敌方对敌方的收益/代价"**，不再"我方全队掉血"。
> ⇒ 它的理由是**真实且重要**的：在 **HP 池未真共享**（§10 #4）的前提下，"作用于我方全队"的效果会**×队伍人数**（6 人 = 6 倍），而 **D272（增减益落点恒为施法者）** 更让"敌方给我方上 debuff"根本无法表达 —— 那条 debuff 会**落在敌方自己身上**。
> ⇒ 但这样一改，**D121 / D159 / D164 / D185 的整条推导链就失效了**（那四轮讨论全部建立在"特性作用于我方全队"之上，包括"深渊吞噬导致必然团灭 → 战斗后回满 HP"这个因果）。
>
> **两种口径（下一轮拍板）**：
> - **(a) 保留 D121 的四条语义**，接受它们是"**依赖 D1 真共享 HP 池 + 序列钩子**"的机制，并承认它们在 D272 修好前无法落地；
> - **(b) 接受队员的改口**，四族特性改为"敌方内部"的单侧效果 ⇒ **D159/D164/D185 的推导链需要重新审视**（尤其"深渊吞噬 → 必然团灭 → 战斗后回满"这条因果会失去前提）。

> ⚠️ **另一处定位重复（同样需要拍板）**：**深渊之王（900 HP / 攻 76）与 Boss「王座上的等待者」（基础 1328 / 攻 107）定位重叠**，而 D173 又把深渊之王降为"虚影"稀有怪。
> ⇒ 三者的归属需要二选一（把深渊之王留在图 Boss 位、或彻底降为虚影、或调整等待者的数值与之拉开）。

### 第 65 轮

| # | 议题 | 裁决 |
|---|---|---|
| D273 | **族群特性的语义（D121 是否保留）** | ✅ **保留 D121 的四条语义**，并**用一条口径消解"×队伍人数"**：**战斗时视作 1V1，"作用于全队"的效果只结算 ×1**（队伍在战斗中被视为一个整体）。⇒ **队员的"敌方单侧"改口不采纳** |
| D274 | **深渊之王 vs 王座上的等待者** | ✅ **彻底降为虚影**（D173 维持）—— 深渊之王不再占 Boss 位，图 Boss 就是「王座上的等待者」 |
| D275 | **团灭惩罚 vs 存档回退** | ✅ **惩罚立即写盘**（失去进度 + 丢 50% 物品**直接落存档**）⇒ **重载存档躲不掉惩罚**。这条同时把 **D69「战斗中不存档」**的边界钉死 |
| D276 | **开局流程** | ✅ **主菜单 → 新游戏 → 难度选择（含噩梦 4 机制勾选）→ 营地**（难度在角色生成之前；因 D269 技能全开放，不需要角色创建步骤） |

> 📐 **D273 是一条"消解型"裁决（值得记方法论）**
> 队员提出的问题是真问题（HP 池未真共享 ⇒ 全队效果 ×6），但它给的两条出路都要推翻既有设计（改语义 / 改 D159-D164-D185 的推导链）。
> 用户给的第三条路**把问题本身消掉了**：**"战斗时视作 1V1，作用于全队就是 ×1"**。
> ⇒ 这与 §3 的既有设定**完全一致**：`team.attack` 是队伍合计、HP 是一条共享池、敌方"一个单位" —— **队伍本来就是一个整体**，所以"全队效果结算 1 次"是自洽的，不是补丁。
> ⇒ **推论**：D272（落点 bug）修好后，敌方对我方的"全队"效果应当**只落一次**（落在共享池/队伍级字段上），而不是逐单位挂载。
> ⇒ **教训**：遇到"某个机制的两种实现都有代价"时，先问"**这个机制的前提假设是不是本身就不需要**"，往往能消解而不是取舍。

> ⚠️ **D273 对 D272 的修正要求（写清给实现）**：修 `applySupportEffects` 的落点时，**"全队"类效果的语义是"结算 1 次到队伍级"**（HP 池 / `state.tactics`），**不是"逐单位各挂一份"**。否则会从"落错人"变成"落 6 倍"。

### 第 65 轮（lead 裁定：战术 v5 的 4 项 + 存档规格的现状）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D277 | **概率三轴的百分比化（Q3）** | ✅ **接受**：百分比在全等级都干净；固定值必然随等级漂移（L4 只 +2.2pp 是因为基线低，这是**正确**的行为） |
| D278 | **升耗是否要"堵"（Q6）** | ✅ **不堵，并据此改写 D219 的表述**：战术的节流**只由 AP 成本承担**（零代码改动）。"轮换技能以规避升耗"正是 **D242** 想要的玩法 |
| D279 | **防御与战术的 AP 竞争（Q7）** | ✅ **不动**。若允许"防御也叠层"，会绕过 D242 的"3 技能 × 3 角色"限制，§2.2 的全表都要重算 |
| D280 | **`tech_ap_cap` 的中文名（Q8）** | ✅ **改为「行动扩容」**（1 条文案、零代码），解决 D212 的三方撞名 |

> 🔴 **存档规格 v1 的现状（队员实测，已核）**
> - **`SAVE_VERSION = 4`**（真源在 **`src/systems/GameState.ts:14`** —— 我派单时写成 `src/state/GameState.ts`，**这是我的第 7 处勘误**）
> - `migrateSave` 只覆盖 v1→v4 且**无拒绝分支**；**`load()` 与 `importJson()` 均无条件迁移** ⇒ **D134「旧档不可续档」在代码里根本没实现，导入路径是完全敞开的**
> - **建议新版本号 5，只做纯闸门**（`version !== 5` 即拒，**读档与导入两个入口都拦**）
> - **必须新增 12 个字段**（难度 2 / 角色双槽 2 / 饰品资产 2 / 任务计数 1 / 统计 4 / 引导 flag 1）；另有 2 个待拍板
> - **队员指出的"最易漏"三条**：① `totalGoldEarned` **不能复用 `gold`**（花费/损失会算错累计统计）；② **`firstTimeFlags`**（引导跨会话只弹一次 —— **我的派单清单里漏了这个字段**）；③ `ownedAccessories` 必须与 `forge.forgedCount` **分开**（D82 有三条获取途径）

> 📌 **战术 v5 的一条正面连带**：**D259（修 `power=0` 技术不掷强化）修好后，8 条 1-AP 技能也会掷强化** ⇒ "结构加固"的捷径从 **0 → 2 条**，5 条战术**全部有了捷径**。⇒ **这反过来说明 D259 该修**（不修则 5 条战术里有一条是残的）。

### 第 66 轮

| # | 议题 | 裁决 |
|---|---|---|
| D281 | **`maxFloorEver` 的粒度** | ✅ **每图一个**（四个数字）—— 能看出玩家在哪张图探得最深，统计面板信息量更大 |
| D282 | **锻造配方是否初始全知** | ✅ **开局全知**（12 件配方一开始就在锻造界面可见，只是材料不够）—— 与 D237「刷就是终局」契合，给玩家明确的长期目标 |
| D283 | **引导开关** | ✅ **新游戏时让玩家自己选是否开启引导**（不是默认开、也不是默认关）⇒ 这是 **D276 开局流程**里新增的一步；该选择**存进存档** |
| D284 | **难度是否影响任务奖励** | ✅ **不受影响**（与 D143 一致：倍率只管**战斗**奖励）。避免 D204/D254/D255 砍完产出后又被难度二次扰动 |

> 📌 **开局流程的最终版（经 D296 / D359 修订，此处已回填）**
> ```
> 主菜单 → 新游戏 → ① 选存档槽（D296：强制选槽，不允许默认槽）
>                  → ② 是否开启引导（D283）
>                  → ③ 难度选择 + 噩梦 4 机制勾选（D115/D276）
>                  → 营地
> ```
> ⚠️ 三步都**在角色生成之前**（D269 已定技能全解锁 ⇒ 无角色创建步骤）；且**三步的选择都要落存档**（槽位 + 引导开关 + 难度不可改）。
> 🔴 **① 的位置是 D359 补的裁决**：D276 原文只有 ② ③ —— 补它的理由是"`guideEnabled` 与难度**都是随档数据**，必须先有槽才有落点"。
> ⚠️ **D360（G19）是 ① 的前置**：`activeSlot === null` 时 `save()` 必须**拒绝写盘**，否则"强制选槽"在代码层不成立。

> ⚠️ **D281 引出一个必须澄清的问题（下一轮）**：现在同时存在**两个"最高层"概念**：
> - **`maxFloor`**（D21：团灭后**归零**）—— 代表当前进度；
> - **`maxFloorEver`**（D137/D281：团灭后**不重置**，每图一个）—— 代表历史统计。
> ⇒ "**本图进度全部消失**"（D17）到底归零的是哪一本账？若只归零 `maxFloor`，那玩家**重进该图要从第 1 层重走**吗？还是从 `maxFloorEver` 继续？**这条直接决定团灭惩罚的实际痛感。下一轮拍板。**

> 📌 **第 66 轮附：技能文案 v4 完成 + D249 的条数更正**
> 队员已交付 `skill-texts-v1.md` **v4**，并**用 Node `fs.unlinkSync` 复核删除**了重复文件（`existsBefore/After` 均为 false，全仓库递归扫描只剩一份）。
> **更正（措辞级，不新增裁决）**：我在 D249 里写的"**11 条**"其实是"**全部带 debuff 的玩家技能集合**"；真正**需要改数值的是 10 条**（Q4 提出的 8 条 + 队员延伸的 3 条 − **清雪居合**（D250 豁免））。
> 最终百分比区间：**本体**削敌方 `attack` **−15% ~ −22%**；**`enhanced`** 削敌方 `defense` **−12% ~ −15%**。
> §0.5「前置代码修复（D272）」已写入 `DamageCalculator.ts:254-259`（签名无 targets）、`:270-287`（push 到 caster）、`CombatSystem.ts:218-232`；§3.2 十条 + §4 六条**逐条标了 `⚠️ 依赖 D272 的代码修复`**。

### 第 67 轮

| # | 议题 | 裁决 |
|---|---|---|
| D285 | **团灭归零的是哪本账** | ✅ **只归零该图的 `maxFloor`** ⇒ **重进该图从第 1 层重走**；`maxFloorEver` 只用于统计。与 D17「本图进度全部消失」字面一致 |
| D286 | **引导开关的默认与可改性** | ✅ **默认开启，可在设置里随时改**，并存进存档（D283） |
| D287 | **能否手动存档** | ✅ **自动（D68）+ 营地手动**并存：玩家可在营地里**手动存到指定命名槽**（D70），但**迷宫内与战斗中不可手动存**。⇒ 与 D69 不矛盾（手动存档只在营地开放） |
| D288 | **战斗内的血条** | ✅ **战斗内双方都显示精确数值血条**（我方共享池 + 敌方）—— 与 D191/D247 的"迷宫不显示、改用氛围提示"形成**分工**：迷宫靠氛围，战斗靠数值 |

> 📌 **D285 + D287 合起来定义的"进度"模型（写清，避免实现歧义）**

| 概念 | 归零时机 | 用途 |
|---|---|---|
| `maxFloor[图]` | **团灭时归零**（D21/D285） | 进度：能进该图的第几层 |
| `maxFloorEver[图]` | **永不归零** | 统计（D137/D281） |
| `cleared[图]` | 永不归零 | 首通凭证（D21） |
| 手动存档 | 营地内可存到命名槽（D287） | 存档分叉 |

> ⚠️ **D287 引出一个需要在 UI 规格里处理的点**：既然"营地可手动存、迷宫不可"，那**玩家在迷宫里想存档必须撤回来走** ⇒ 这与 **归途石**（D179/归途石 55 金 + 限购 1）的定位**重合**了。两者都是"撤回营地"的手段。⇒ 需要在 UI 上区分清楚（归途石 = 临时撤离但保留进度；手动存档 = 纯粹的存档分叉）。**已记入待办。**

### 第 68 轮

| # | 议题 | 裁决 |
|---|---|---|
| D289 | **逃跑机制** | ✅ **只能靠烟丸**（普通战也不做"概率逃跑"）：有烟丸 = 100% 脱战，没有就只能硬打。**Boss 战两者都禁用**（D50/D224） |
| D290 | **虚影的命名** | ✅ **四图同名「深渊之王的虚影」**（靠 D190 的数值衰减 0.5/0.7/0.85/1.0 区分强弱） |
| D291 | **精英的可见性** | ✅ **显式标识**：名字前缀 + 配色不同 + 战前预警 —— 与 D119「强特性需针对性应对」同构（玩家必须知道自己在打精英） |
| D292 | **饰品槽挂在谁身上** | ✅ **挂在出战位上**（不是挂在角色上）：换人后槽内的饰品**保留在新上阵者身上**，不需要卸下 |

> ⚠️ **D290 勘误（第 101 轮 · 落地实测）**：D290 原文的口径是"四图**一律叫「深渊之王的虚影」，id 共用 `enemy_abyss_king_shadow`**，靠 D190 的数值衰减 0.5/0.7/0.85/1.0 区分强弱"。**实际配表落地为 4 个独立 id**（`enemy_abyss_king_shadow_root` / `enemy_abyss_king_shadow_moss` / `enemy_abyss_king_shadow_tomb` + `enemy_abyss_king`），**按图给不同 `level`**。原因：**"共用单 id"在配表层做不到**（同一 id 不能有两个 `level`）；而"共用 id"的**原始立论**是"一个 id 一套技能 ⇒ 天然挂不上图独占的族群技"，**该立论已由 D438 的 `family: 'echo'` 取代** —— 现在"虚影不挂族群技"靠 `family` 表达，不再依赖共用 id。⇒ "`family: 'echo'` 恰好 4 条"已由 `scripts/verify.mjs` 的断言固定（12.2 节 / `FAMILY_HISTOGRAM`）。

> ⚠️ **D292 的语义修订（写清楚）**：D141 原文是"**每个角色** 1 个饰品槽"，**实际语义应为"每个出战位 1 个"**。
> 影响：① 饰品不是"角色的资产"而是"队伍的配置"；② D80/D82 的"获得途径"只影响**拥有与否**，不绑定到人；③ **换人零成本**（不会因为换下某人而丢饰品）。
> ⇒ 与 **D270「槽位开局就有」**一致；与 **D271「潜力不可提升」**配合后，**饰品的价值完全落在"配装"而不是"养人"**上。

### 第 68 轮（lead 裁定：存档规格 v1 的 16 条缺口，全部收口）

> 队员的 `save-schema-spec-v1.md` 列了 **16 条"依据不足"**的缺口（E-1 ~ E-16），**一条都没有自行补设定** —— 这是我要的工作方式。
> 其中 **E-1 / E-2 已被 D281 / D282 答掉**，其余条目裁定如下：

| # | 缺口 | 裁定 |
|---|---|---|
| **E-1** | `maxFloorEver` 层级 | ✅ **per-地牢**（D281） |
| **E-2** | 配方是否初始全知 | ✅ **初始全知**（D282）⇒ `unlockedRecipes` **不进存档**（真源是配方配表） |
| **E-3** | 任务的"首次"口径 | ✅ **支线用"每次下探的首次"**（否则会变成一次性，与 D237「终局靠刷」直接矛盾）；**主线用"全局一次"**（击败该图 Boss） |
| **E-4** | 任务计数的存续范围 | ✅ **两层表**：永久表（跨会话）+ **`activeRun.runCounters`**（随下探重置，**团灭时随进度一起清**） |
| **E-5** | 新游戏如何不覆盖旧档 | ✅ **新游戏写入新的空槽，不覆盖既有槽**；槽位满时提示玩家选择要覆盖的槽 |
| **E-6** | 拒绝旧档的提示文案 | ✅ 由 **quest-writer** 出稿（与其它 UI 文案一并），本文不发明 |
| **E-7** | 统计口径 | ✅ `totalDefeated` **含 Boss 与精英**；`totalClears` **含重复通关**；`totalGoldEarned` **含卖出道具所得**（收入侧统一收口在 `addGold`） |
| **E-8** | 落盘节流策略 | ✅ **脏标记 + 事件点统一落盘**（不做每次立即 `JSON.stringify`） |
| **E-9** | `autosave()` / `tick()` 死代码 | ✅ **改造保留**：`tick()` 复活用于累计 `playTime`，`autosave()` 改为脏标记驱动（**不恢复定时**，遵守 D68） |
| **E-10** | 战斗中落盘的显式守卫 | ✅ **加"战中锁"**（D69 目前靠"没有触发点"隐式成立，但 `save()` 全域可调用，真实可绕过） |
| **E-11** | `SAVE_VERSION` 的真源位置 | ✅ **移到 `config/saveKeys.ts`**（不改行为，纯整洁度：现在 `SaveSystem` 反向 import `GameState` 只为拿版本号） |
| **E-12** | `activeRun.vitals` | ✅ **改为单一 `poolHp: number`**（D1 已定真共享一份，`vitals` 是第二个真源） |
| **E-13** | `rngSeed` | ✅ **删除**（只在新游戏写一次、从不更新，实际是常量，属死字段，并入 **D42**） |
| **E-14** | 引导 key 清单以哪份为准 | ✅ **以 `tutorial-draft-v1.md` 的 11 条为准**（D98 的 4 条是最初草案 → D239 扩到 10 → **D422 加第 11 条**） |
| **E-15** | "重看全部提示"入口 | ✅ **做** ⇒ `firstTimeFlags` 变为**可清空**（不只只增） |
| **E-16** | 命名存档槽上限 | ✅ **6 槽**（现状 3 槽偏少，"新游戏不覆盖"也需要空位） |

> 📌 **E-3 / E-4 是这 16 条里最要紧的两条**：它们决定"支线能不能重复做"，而 **D204/D245/D254/D255 把战斗科技点清零后，支线是终局刷科技点的唯一来源**。
> ⇒ 若按"全局首次"实现，支线做完就断供，**D237「终局靠刷」和 D265「不加终局内容」都会当场失效**。

### 第 69 轮

| # | 议题 | 裁决 |
|---|---|---|
| D293 | **存档槽数** | ✅ **10 槽**（现状 3 槽偏少） |
| D294 | **"重看全部提示"入口** | ✅ **做** ⇒ `firstTimeFlags` **可清空**（不只只增） |
| D295 | **统计口径** | ✅ **全部计入**：`totalDefeated` 含 Boss/精英；`totalClears` 含重复通关；`totalGoldEarned` 含卖出所得（只记"入账"不记"花费"） |
| D296 | **新游戏与槽位的关系** | ✅ **强制让玩家选槽**（不允许"默认槽"），因此**不存在"覆盖当前槽"这个动作** ⇒ D134「旧档只读」不会被绕过 |

### 第 69 轮（lead 裁定：科技点重算 + 材料来源收窄）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D297 | **科技树定为 99 点** | ✅ **采纳队员的 99**（细化了 D264 的"~95"）：**79 + 5 个节点 × 4**，新增「疾行训练 II / 幸运徽记 II / 秘法研习 II / 灵性启迪 II / 混战节拍」，**全部使用现有 `effect.type`，零代码**。⇒ 首清 76（严格口径）= **77%**、中位 81 = **82%** |
| D298 | **材料的来源收窄** | ✅ **D266 修正为"精英必掉的是【该图档位材料】"**（不是"魔核 / 秘药"通吃）。理由：若精英直接掉魔核与秘药，会变成**魔核 ×8 / 秘药 ×16** 的量级，把整个锻造与永久道具经济冲垮 |
| D299 | **`item_core` 的产量修正** | ✅ **王座上的等待者的 `item_core` 掉落 2 → 4** ⇒ 造满 12 件饰品从 **7.5 趟降到 3.75 趟**。⚠️ **必须与 D258（3 只旧 Boss 移出敌人池）同批次落地**，否则**魔核来源为 0、★5 饰品永远造不出** |
| D300 | **任务草案的 4 处修正** | ✅ 采纳：① **`S3 净空回廊` 恒不达成**（第 1 层无支路 ⇒ 陷阱/事件/宝箱必踩，"未触发 X"型条件在物理上不可能）⇒ **改为比例型**；② `S9` 阈值 1200 → **2500**（低于四图宝箱期望）；③ `S4` 满金币能一结算 4 次/趟 ⇒ **收紧**；④ **招募给的 2 点科技点归 0** |

> 🔴 **D299 是一条"顺序依赖"警告（比数值本身重要）**
> 魔核的来源在三刀（D204 普通怪 / D245 宝箱 / D254 精英不给科技点）之后**只剩 Boss**，
> 而 **D258 又把 3 只旧 Boss 移出普通池** ⇒ 若 D258 先落地、D299 的掉落修正后落地，**中间会有一段"魔核 = 0"的窗口**。
> ⇒ **落地时这两条必须在同一批次**（写进 D240 的批次 1）。

> 📌 **第 69 轮的两份冻结确认**
> - **战术草案 v6 已冻结**（`design/drafts/tactics-draft-v1.md`）：Q3/Q6/Q7/Q8 全采纳、D219 改写为"节流只由 AP 成本承担"、悬空引用清空（一律改指 GDD 正文，不抄数值）
> - **技能文案 v4 已完成**（`design/drafts/skill-texts-v1.md`），重复文件已用 `fs.unlinkSync` 删除并复核
> - ⚠️ 队员报了一处**路径勘误**：草案真身在 **`H:\work\maze-rpg\design\drafts\`**（我一直用的就是这个路径，无影响）

### 第 70 轮（第 60 轮后的检查点）

| # | 议题 | 裁决 |
|---|---|---|
| D301 | **剩余 30 轮的走法** | ✅ **继续问答** |
| D302 | **引导触发点** | ✅ **不加，10 条够**（再加就变成弹窗轰炸，与 D43「不集中说明」的初衷相反）。⚠️ **本条已被 D422 部分覆盖**：实际**加了第 11 条**（事件提示），因为 D336 把事件界面改成全文披露后，"迷宫里有事件这回事"成了独立的一课 |
| D303 | **GDD 的最终形态** | ✅ **保持单文件** `design/gdd.md`（草案降为附录引用，推导过程留在独立 md）。⇒ **D244 的并入是"把数值表搬进正文"，不是拆文件** |
| D304 | **D108 的验收门（细化 D268）** | ✅ **按敌人类型分级设硬门**：**普通敌 TTK 2–4 回合 / 精英 5–10 回合 / Boss ≥10 回合** |

> 📐 **D304 取代 D268 的单一阈值（这是更精确的验收标准）**

| 敌人类型 | TTK 硬门 | 现状（§0.6 实测） | 差距 |
|---|---|---|---|
| **普通敌** | **2–4 回合** | 1.0 – 2.1 回合 | 需拉长约 2 倍 |
| **精英** | **5–10 回合** | 1.0 – 2.1 回合 | 需拉长约 4 倍 |
| **Boss** | **≥10 回合** | 1.0 – 2.1 回合 | 需拉长约 6 倍 |

> ⚠️ **D304 比 D268 更难达标，且把"分层"写进了验收**：若要同时满足"普通 2–4 / 精英 5–10 / Boss ≥10"，**单靠调敌方 HP 做不到**（那会让普通敌也变长）。
> ⇒ 必须有**分层的强度轴**：普通敌（浅层低 HP）→ 精英（×1.5 + 二动）→ Boss（专属机制 + 高 HP）。这条恰好支持 **D151 的"普通 1 动 / 精英 2 动 / Boss 特殊"**与 **D118 的精英 ×1.5**。
> ⇒ **D108 重算必须按这三档分别验收**，不能用中位数概括。已同步给队员。

> 📌 **第 70 轮补充：D294 与 D293 的口径冲突（以用户裁决为准）**
> 我在 D294~D296 那一批里曾裁"存档 **6 槽**"（E-16），而用户随后裁 **D293：10 槽** ⇒ **以 10 槽为准**，E-16 的 6 槽作废。

### 第 70 轮（lead 裁定：任务草案 v2 的 3 项待拍板）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D305 | **S7/S8 给科技点是否违反 D254/D255** | ✅ **不违反**。**D254/D255 砍的是"敌人掉落轨"（`enemies.json` 的 `techPoints`），不是"任务奖励轨"**；而 **D92 明文就是"发金币 + 科技点"**。⇒ 队员的"两轨"处理**正确**，不采纳严格口径（否则终局收益砍掉 37%，1.7 小时 → 2.7 小时） |
| D306 | **招募价** | ✅ **1 500 → 3 500**（据实测定：一趟四图 18 439 + 支线 11 680 + 主线 10 100 = **40 219 金**，而招满 20 名只需 30 000 ⇒ 原价使招募几乎免费） |
| D307 | **S9 的 2500 阈值缺实测支撑** | ⏸ **挂起**，等 economy-analyst 补"**宝箱金币分项**"（它此前只给了宝箱科技点） |

> 🔴 **队员的支线重复价值自检暴露了一个终局问题（必须处理）**
> 实测：稳态支线 = **32.1 科技点/趟 = 13.8 点/小时**；而树的剩余量只有 **99 − 76 = 23 点**
> ⇒ **刷满剩余科技点只要 ≈1.7 小时**，之后**支线科技点归零价值**。
> 叠加"支线科技点是终局唯一来源"（D254/D255/D245/D204 四刀之后），**终局体验 = 1.7 小时后失去科技点目标**。
> 这与 **D265「先不定义终局循环」**叠加后，流失风险的量化值就是 **1.7 小时** —— 这个数字必须由用户认领或改写。
> **下一轮拍板。**

> ⚠️ **另一处：10 条支线里只有 7 条是"引擎型"** —— **S4 / S5 / S6 绑在有限资源池上**（永久上限合计约 9 次，**头 4 趟就耗尽**）。
> ⇒ 实际长期可重复的支线只有 **7 条**，"重复价值"的估算建立在这 7 条上。**下一轮一并确认是否改造这 3 条。**

### 第 71 轮

| # | 议题 | 裁决 |
|---|---|---|
| D308 | **终局只有 1.7 小时科技点目标** | ✅ **不改，接受** —— 不翻案 D265、不扩树；玩家刷完科技点后自行转向 12 件饰品（3.75 趟）与精英材料 |
| D309 | **S4/S5/S6 绑在有限资源池** | ✅ **改造这 3 条**，让它们也变成可重复 ⇒ **10 条支线全部为引擎型** |
| D310 | **招募价** | ✅ **1 500 → 5 000** ⇒ 招满 20 名需 **100 000 金**，成为真正的长期目标（原价 30 000 相对 40 219 金的总收入几乎免费） |
| D311 | **D244 的并入时机** | ✅ **等全部草案冻结后一次性并**（避免并到一半又来改） |

### 第 71 轮（lead 裁定：存档 v2 的 5 条新缺口 + UI 规格的 4 项）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D312 | **E17 消耗品槽是否也改"出战位"** | ✅ **是**（与 D292 的饰品槽保持一致：两槽都挂在出战位上） |
| D313 | **E18 `partyEquip` 的容器形态** | ✅ **`party` 改为对象数组**（`{ charId, accessory, consumable }`）—— 因为 D292 已把槽位与角色**解耦**，必须显式绑定"出战位 → 角色 + 饰品 + 消耗品"，并行数组会制造三个需要同步的真源 |
| D314 | **E19 自动存档的键与槽位** | ✅ **自动存档用独立键，不占用 10 个命名槽**（否则自动存会挤掉玩家的命名存档） |
| D315 | **E20 引导开关放哪** | ✅ **随档 `state.settings`**（它影响存档内的引导行为）；而 **文本缩放 / 高对比（D100）属全局无障碍偏好 ⇒ 独立键 `maze_rpg_settings`** ⇒ **两套设置并存是有意的** |
| D316 | **E21 迷宫"存档点"是否属 D287 禁令** | ✅ **不属**：存档点元素 = **D68 的"事件触发式自动存档"**，属允许范围；D287 禁的是"**玩家手动存**" |
| D317 | **G1 战术栏容几条** | ✅ **9 条**（以 tactics v6 冻结的"类 4 只授予优先级最高的一件"为准）⇒ **5 属性 + 4 机制型**，UI 按 9 条设计（派单时我说 11 条，作废） |
| D318 | **G3 引导默认值（D283 vs D286 字面冲突）** | ✅ **新游戏时让玩家选，默认高亮"开启"** —— 两句都成立（D283 给的是"让玩家选"，D286 给的是"默认开"） |
| D319 | **G11 噩梦机制 2 是否作用于营地商店** | ✅ **作用于全部商店（含营地）** —— 否则玩家能在营地**规避**机制 2 的代价（物价 ×2），机制就形同虚设 |

> ⚠️ **D317 勘误（第 101 轮 · 落地实测）**：D317 的"**9 条按 UI 设计**"**已被 D328 作废** —— **战斗 HUD 只放属性战术（每侧 5 条）**，机制型战术**不占该 HUD**，另放"战斗界面另一处"（D332）。故三段口径为：**内容 = 11（5 属性 + 6 机制型）／战斗 HUD = 每侧 5 条（只属性战术）／非属性战术 = 另一处（D332）**。另：D317 所据前提"以 tactics v6 冻结的'类 4 只授予优先级最高的一件'为准"**亦已过时**（tactics v6 §1.5.1 实为"类 3 三件 + 类 4 三件，一一对应零剩余"，见 §11.A.1）。

> 🔴 **存档 v2 又挖出第 4 条"配表写了但代码不读"（比 §10 已记录的多一条）**
> **G6：强化附加效果（`applyEnhancedEffects`）的落点也是错的** —— 它**一律落在"被击中的敌人"身上**（而不是按 `skill.target` 解析）。
> ⇒ 与 **D272（`applySupportEffects` 落点恒为施法者）是同一个根、但相互独立的两个函数**。
> ⇒ **§10 的"D272 家族"现共 4 条**：① `applySupportEffects` 落点恒为施法者（D272）；② 70 条"伤害+effects"的 effects 不结算（#12b）；③ `power=0` 技不掷强化（D259）；④ **`applyEnhancedEffects` 落点恒为被击中的敌人（G6，新增）**。
> ⇒ **修复顺序（已定）**：先修两个落点 → 再修 #12b 的结算路径 → 再落 D233/D249 的数值改动。

> 📌 **UI 规格 v1 已交付**（`design/drafts/ui-spec-additions-v1.md`）：11 个系统逐项给了入口/尺寸/坐标/字号色值/动效/空状态/可访问性联动；末尾 **6 条阻断级 + 9 条影响面 + 6 条知会**。
> 队员自评：**信息充足可直接实现**的是战术显示、难度+引导、存档、配装、锻造、商店、营地；**缺口最大**的是事件界面（失败后果是否全文披露）、濒死渐晕（三档阈值只是"建议"）、商店营地档位。**后两项下一轮拍板。**

### 第 72 轮

| # | 议题 | 裁决 |
|---|---|---|
| D320 | **事件的失败后果是否全文披露** | ✅ **不在事件界面内披露**，改为**首次进入迷宫时弹窗提示**事件的机制与风险（与 D98 的上下文引导合流）。⇒ 事件界面本身**不写具体数值** |
| D321 | **濒死渐晕的三档阈值** | ✅ **50% / 30% / 15%** 正式定案（轻度渐晕 / 中度 + 心跳 / 重度 + 心跳加快），替代 D247 里"建议值"的表述 |
| D322 | **"零渐变"的适用范围** | ✅ **放宽为"只约束 UI 面板"** —— **全屏遮罩（渐晕）允许 alpha 渐变**。⇒ 修正 §8.1 的绝对表述 |
| D323 | **敌方 debuff 的可见性** | ✅ **在敌人名字/血条旁显示 debuff 图标 + 剩余回合**（D272 修好后"削敌方 attack/defense"才真正生效，玩家必须能看到） |

> 📌 **D322 是一条对美术规则的正式修订（值得单独记）**
> 原规则：**零渐变、零发光**（§8.1，来自 D8）。这条规则是**为扁平 UI 面板**定立的 —— 它的目的是"不要拟物化的立体感"。
> 但**全屏遮罩（渐晕）不是面板**：它没有边框、没有圆角、不参与布局，用 6 层平色环带反而会露出**色阶断层**（比渐变更丑）。
> ⇒ **修订后的规则**：**UI 面板零渐变；全屏效果层（遮罩/渐晕）允许 alpha 渐变**。这是规则**适用范围**的澄清，不是放弃美术方向。

> ⚠️ **D320 的连带（需要补一条 UI 入口）**：既然事件界面不披露后果，**玩家判断错误后无法回查"当时为什么会这样"** ⇒ 需要给一个**可回查的入口**（候选：战斗/事件日志、或迷宫内的事件记录页）。**下一轮确认。**

### 第 73 轮

| # | 议题 | 裁决 |
|---|---|---|
| D324 | **"首次进迷宫弹窗"的粒度** | ✅ **全局只弹一次**（第一次进任意迷宫）—— "不弹窗轰炸"的最彻底做法 |
| D325 | **无根 / 苔痕的新精英命名** | ✅ **「巢守」**（无根回廊）+ **「孢母」**（苔痕回廊） |
| D326 | **战术栏的呈现** | ✅ **新做一个 HUD 显示在血量 HUD 上方**：**有战术存在时才显示**、**每个战术独立 HUD**、**敌方战术左对齐 / 我方战术右对齐**、**最多 5 条战术** |
| D327 | **浅层组 / 深层组的经验金币** | ✅ **不拉开** —— 两组只区分技能与难度，经验金币由 D14 的按层缩放统一决定 |

> 🔴 **D326 与 D317 的条数冲突（必须澄清）**
> - **D317**（我据 tactics v6 裁定）：战术栏按 **9 条**设计 = **5 属性 + 4 机制型**
> - **D326**（你刚才裁的）：这个 HUD **最多 5 条战术**
>
> ⇒ 我方若同时存在 9 条战术，**装不进 5 条的 HUD**。两种可能：
> - **(a) HUD 是"每侧 5 条"**（我方 5 + 敌方 5），而我方的 9 条里**只显示优先级最高的 5 条**（其余藏在详情页）；
> - **(b) 战术总数本就该 ≤5**（即 D317 的 9 条理解错了，机制型战术不占这个 HUD，另有去处）。
>
> **下一轮拍板。**

> 📌 **七名队友全部交回（第 73 轮）**
> | 交付物 | 状态 |
> |---|---|
> | `content-draft-v1.md`（饰品/Boss/族群特性） | ✅ 冻结（v4.1） |
> | `tactics-draft-v1.md`（战术系统） | ✅ 冻结（v6） |
> | `skill-texts-v1.md`（93 条文案） | ✅ 完成（v4） |
> | `enemies-draft-v1.md`（36 个敌人） | ⏳ v2 |
> | `quests-draft-v1.md` + `tutorial-draft-v1.md` | ⏳ v2 |
> | `ui-spec-additions-v1.md`（11 个系统） | ⏳ v2 |
> | `save-schema-spec-v1.md`（存档规格） | ✅ **v3**（13 份草案全部冻结，可启动 D244 合并） |
> | `economy-report-v1.md` / `growth-recalibration-v1.md` / `techpoint-economy-v1.md` | ✅ 已交 |
> | `combat-length-recalibration-v1.md`（D108） | ⏳ **第一步根因诊断已出，回归未跑** |

> 🔴 **D108 重算的根因诊断（队员已交第一步，**推翻了我的假设**）**
> 它的结论：**"我方输出 ∝ N²"不是主因**（N=4 时只贡献 **1.3 倍**）；
> **真正的主因是"轮（round）太小"** —— 每个大回合我方有 **4 个行动**，敌方只有 **1 个**。
> ⇒ 并且它给出了一个**保守方案可在不动任何伤害公式的前提下达成 TTK 5–10**，与 **D304 的三档硬门**（普通 2–4 / 精英 5–10 / Boss ≥10）兼容。
> ⇒ **这条对我的 §10 #32 是一次修正**：我此前把 N² 列为"根因"并据此延后处理（D108），**方向对但归因不完整**。等它的回归数据出来后一并更正 §10。

### 第 74 轮

| # | 议题 | 裁决 |
|---|---|---|
| D328 | **战术栏的条数（D317 vs D326 冲突）** | ✅ **HUD 上最多 5 条** ⇒ **D317 的"9 条按 UI 设计"作废**：**战斗 HUD 只放属性战术（5 条）**，机制型战术**不占该 HUD**，另找去处 |
| D329 | **终局形态（每趟重做任务 27 分钟 / 自循环 3.0–3.7 小时）** | ✅ **接受**（与 D265「不加终局内容」一致）；支线金币占一趟收入 **29%** 也接受 |
| D330 | **S3 与 S10 重复** | ✅ **删 S3** ⇒ 支线从 **10 条 → 9 条** |
| D331 | **精英必掉材料与档位设定的矛盾** | ✅ **改口径为"该图 `drops` 池里的稀有位"**（不写死"档位材料"）—— 因为档 II 材料**从静谧墓所才开始出现**，苔痕精英若必掉"档位材料"只能掉浅层材料，与世界设定冲突 |

> ⚠️ **D328 的连带（机械型战术需要一个去处）**
> 机制型战术（4 条）本质上是**饰品效果的显性化**（D216）⇒ 既然不占战斗 HUD，**最合理的去处是「装备/配装界面」**：
> 玩家在那里看到自己装备的饰品时，**顺带看到它授予的机制型战术及其数值**。
> ⇒ 这条与 **D119/D121 的"强特性需针对性应对"**同类：**玩家必须能在决策的时候看到它**（配装就是决策时刻），而不是在战斗中被 HUD 挤掉。
> **下一轮确认。**

> 📌 **第 74 轮：D266 经三次修订后定稿**
> `D266`（精英必掉魔核/秘药）→ `D298`（收窄为"该图档位材料"）→ **`D331`（改为"该图 `drops` 池里的稀有位"）**。
> ⇒ 这是同一件事的**第三次修订**，每次都是因为下一层的设定矛盾被挖出来（魔核 ×8 → 苔痕无档 II 材料）。
> ⇒ **方法论**：涉及"跨图分级"的设定，必须**逐图验一遍材料/敌人的档位是否存在**，否则会在最弱的图上露馅。

### 第 75 轮

| # | 议题 | 裁决 |
|---|---|---|
| D332 | **机制型战术的显示位置** | ✅ **放在战斗界面的另一处**（不占战术 HUD），具体位置由 UI 规格定 |
| D333 | **事件是否标出代价** | ✅ **有代价的选项用红色加粗标出** ⇒ 部分覆盖 D320 的"完全不披露"（D320 仍管"是否给具体数值"） |
| D334 | **存档槽与命名的共存** | ✅ **固定 10 槽 + 每个槽可自定义名称**（不命名则显示"存档 N"）—— 固定槽保证"强制选槽"可实现，命名满足 D70 |
| D335 | **任务奖励是否因招募涨价而重核** | ✅ **不改**（已按 D297 的树价 99 标定过，且 D284 已定"任务奖励不受难度影响"） |

> 🔴 **D333 引出一个残留歧义（下一轮确认）**
> D320（用户裁）：事件界面**不写具体数值**，改为首次进迷宫弹窗提示机制。
> D333（用户裁）：**有代价的选项用红色加粗标出**。
> ⇒ "红色加粗"标的是**"这个选项有代价"这个事实**，还是**连代价的数值一起写出**？
> 前者与 D320 相容；后者实际上回到了"全文披露"。**需一句确认。**

> 📌 **第 75 轮的另一处结构性缺口（从 D326 暴露出来）**
> **D326 说"敌方战术左对齐"** —— 但**敌方目前没有任何战术来源**：
> 战术的两个来源是 **技能授予**（D217）与 **饰品授予**（D216），**两者都是我方系统**；敌方的"每回合出手次数"由 **D151** 按类别决定（普通 1 次 / 精英 2 次 / Boss 特殊）。
> ⇒ 要么 **(a) 敌方没有战术**（那 D326 的"左对齐"就是预留位，UI 永远空着），
> 要么 **(b) 敌方也需要战术系统**（那要新增"敌人授予战术"的机制，与 D151 的调度规则协调）。
> **下一轮拍板。**

### 第 76 轮

| # | 议题 | 裁决 |
|---|---|---|
| D336 | **事件的代价是否给数值** | ✅ **连数值一起给**（红色加粗 + 具体数值）⇒ **推翻 D320 的"不写具体数值"** —— 事件界面**全文披露** |
| D337 | **敌方是否有战术** | ✅ **敌方也有战术** ⇒ 需新增"**敌人授予战术**"机制，并与 **D151**（普通 1 次 / 精英 2 次 / Boss 循环）协调；**Boss 强度需重算** |
| D338 | **`maze_rpg_settings` 整体覆写的 bug** | ✅ **改为"读旧值 → 合并 → 写回"**（升为必须修，否则 D100 的可访问性设置会被改分辨率的动作静默抹掉） |
| D339 | **存档名是否允许重复** | ✅ **不允许重名**（重名时报错）⇒ 保证玩家能靠名字区分槽位 |

> ⚠️ **D336 与 D320 的关系（要写清，避免两处矛盾）**
> - **D320** 的两半：「不在事件界面披露**具体数值**」+「首次进迷宫弹窗提示机制」
> - **D336** 推翻了前半 ⇒ **事件界面全文披露（红字加粗 + 数值）**
> ⇒ **D320 只剩后半有效**：首次进迷宫**仍弹一次总览**（讲三态迷雾 / 无小地图 / 团灭代价 / 事件有代价），但**它不再承担"教事件风险"的职责**（界面上已经写明了）。
> ⇒ 这个改动同时消掉了 **D320 的连带待办**（"回查入口"）—— 队员已选的「迷宫日志」仍然保留（它的价值从"补披露"变成"排时间线"）。

> 🔴 **D337 的连带（这是本轮最重的一条，会改战斗系统）**
> 敌方一旦有战术，**战术系统就不再是"我方专属"**，而 **D200–D222 的全部设计默认它是我方系统**。必须补齐：
> 1. **来源**：敌人靠什么获得战术（自身技能授予？每 N 回合自动获得？进入战斗时自带？）
> 2. **叠层与持续**：是否沿用我方规则（上限 3 / 持续 3 回合 / 每次刷新），还是另有一套；
> 3. **可见性**：D326 已预留"敌方左对齐 5 条"——与 D328 的"HUD 最多 5 条"合并后，**我方 5 + 敌方 5 = 10 条**；
> 4. **强度重算**：Boss 与精英的数值要按"它也会叠战术"重算（挂 **D108 / D304 的三档硬门**）。
> **下一轮逐条拍板。**

### 第 77 轮

| # | 议题 | 裁决 |
|---|---|---|
| D340 | **敌方战术的来源** | ✅ **靠敌人自己的技能授予**（与 D217 对称） |
| D341 | **敌方战术的叠层与持续** | ✅ **与我方完全同规则**：上限 **3 层** / 持续 **3 回合** / 每次释放刷新 |
| D342 | **敌方战术的内容** | ✅ **属性共用（5 个）+ 每图族群 1 个专属** ⇒ 敌方共 **9 个战术** |
| D343 | **Boss / 精英的强度重算** | ✅ **纳入 D108 一起算**（与 D304 的三档硬门同批：普通 2–4 / 精英 5–10 / **Boss ≥10**） |

> 📐 **D342 + D328 合并后的战术 HUD 最终口径（双方对称，写清）**

| | 我方 | 敌方 |
|---|---|---|
| **战术池** | 5 属性 + **6 机制型** = **11**（⚠️ 原文作 4 / 9，勘误见下） | 5 属性 + **4 族群专属** = **9** |
| **HUD（每侧 5 条）** | **属性战术 5 条**（右对齐） | **属性战术 5 条**（左对齐） |
| **非属性战术** | 机制型 **6 条** → **战斗界面另一处**（D332） | 族群专属 4 条 → **同样放"另一处"** |

⇒ 两侧的 HUD 规则**完全对称**，且"非属性战术"的来源也对称（我方来自饰品、敌方来自族群）—— 这让玩家能形成一致的阅读习惯。

> ⚠️ **本表勘误（第 101 轮 · 落地实测）**：我方"机制型"**原文作 4**（战术池"5 + 4 = 9"、"非属性战术 机制型 4 条"），**实为 6**（类 3 三件 + 类 4 三件，见 §11.A.1 / §11.B.2）⇒ 我方战术池 = **11**。**敌方侧"5 属性 + 4 族群专属 = 9"不变**（敌方无机制型）。

> ⚠️ **D340 的一个隐忧（记录在案）**：敌方靠技能授予战术 ⇒ **它的强度与 D151 的出手次数直接耦合**：
> 精英 **2 次/轮**（每 3 轮 1 次大招）⇒ 叠层速度是我方的 2 倍；Boss 按 D111 的循环 + 阈值插大招。
> ⇒ **拉长战斗会同时拉高敌方战术层数**（正反馈），这与 D304「Boss ≥10 回合」的目标**方向相反**。⇒ 已并入 D343 的重算范围。

### 第 78 轮

| # | 议题 | 裁决 |
|---|---|---|
| D344 | **我方清除敌方战术的手段** | ✅ **新增若干我方技能**：① **造成伤害的同时、按概率清除敌方 1 条战术**；② **不造成伤害但必定清除敌方 1 条战术**。（不新增消耗品，不给 D187 的逆向解离针加双向） |
| D345 | **族群专属战术的强度** | ✅ **同档但特色化**（数值与属性战术同档，靠效果类型立差异，不靠更高数值） |
| D346 | **敌方层数的正反馈矛盾** | ✅ **允许差异化**（不再严格要求敌我完全对称：敌方战术的持续时间可以更短 / 上限可以更低） |
| D347 | **第 80 轮检查点** | ✅ **不设体检，直接继续问答** |

> 🔴🔴 **第 78 轮的重大更正：`trapCount` / `treasureCount` / `eventCount` / `saveCount` 都是【每层】语义**
>
> **证据（我已独立核对）**：`MazeGenerator.ts:141-167` —— `place('trap', config.trapCount, …)` 定义在 `generate()` **内部**，而 `generate()` **每层调用一次**（`DungeonScene.ts:445` 用 `nextFloor` 生成）。
> ⇒ 这四个字段都是**每层数量**，**不是每趟总数**。
>
> | 字段 | 现值（每层） | **一趟 6 层的总数** |
> |---|---|---|
> | `trapCount` | 1 / 3 / 4 / 5 | **6 / 18 / 24 / 30** |
> | （D172 改后） | 1 / 1 / 2 / 2 | **6 / 6 / 12 / 12** |
> | `treasureCount` | 3 / 4 / 5 / 6 | **18 / 24 / 30 / 36** |
> | `eventCount` | 1 / 2 / 2 / 3 | **6 / 12 / 12 / 18** |
> | `saveCount` | 1 / 1 / 2 / 2 | **6 / 6 / 12 / 12** |
>
> **这直接推翻我在 D172 里的算术**：我写"一趟最多踩 2 次（最坏约 40%）"——**实际是 6–12 次**，按单次 8–20% 算，**一趟累计可达 48%–240%**。
>
> **但结论未必翻转**，因为 **D185（战斗后回满 HP）** 会把陷阱伤害在**下一场战斗结束时清零**：
> ⇒ 真正的暴露区间是"**两场战斗之间**"，而不是整趟。一趟约 35 场战斗 vs 6–12 个陷阱 ⇒ **平均每个战斗间隔不到 1 个陷阱**。
> ⇒ 所以"陷阱不是主要威胁"的结论**仍然成立，但理由从"一趟只有 2 个"变成了"每个战斗间隔最多 1 个"**。
>
> ⚠️ **仍有两个必须处理的连带**：
> 1. **D172 的改动（1/3/4/5 → 1/1/2/2）是在错误算术下做的** ⇒ 需用正确语义**重新评估**是否还该改；
> 2. **D60（宝箱金币跟层数挂钩）与 D127（每层 ≥1 商店）**的表述本身与"每层"语义**一致**，无需改；但 **D245（宝箱不产科技点）**是正确方向的。
> ⇒ **已派队员用正确语义重算陷阱与宝箱的暴露量。下一轮给结论。**

### 第 79 轮

| # | 议题 | 裁决 |
|---|---|---|
| D349 | **陷阱数量（D172 是否回滚）** | ✅ **保持 `trapCount` = 1 / 1 / 2 / 2**（一趟 6 / 6 / 12 / 12）—— 虽然 D172 的理由算错了，但"每层少量"在正确语义下**反而是对的**；等实测确认后再微调 |
| D350 | **宝箱数量** | ✅ **`treasureCount` 3/4/5/6 → 2/3/3/4**（一趟 12 / 18 / 18 / 24）—— 原值一趟 18–36 个偏密（平均每 1–2 场战斗一个） |
| D351 | **清敌方战术的技能从哪来** | ✅ **改造现有技能**（从 80 条里挑 **2–3 条**改造成"清敌方战术"），**不新增技能位**（保持 D55 的"每角色 2 个"） |
| D352 | **GDD 合并的启动** | ✅ **先合已冻结的 4 份**（饰品/Boss、战术、文案、存档），其余等冻结后再合 |
| D353 | **E-25：两个不可推导的计数器** | ✅ **采纳，字段表 14 → 16**：新增 **`stats.eliteKills`**（供支线 S7）与 **`stats.clearStreak`**（供支线 S10）。理由：`totalDefeated` 按 D295 不含类型信息、拆不出"精英"；`totalClears` 单调递增、表达不了"连击被打断"，且不能放 `runCounters`（会随 `activeRun` 一起清） |

> 📌 **存档规格的收口情况（队员已完成）**
> - **E-24 关闭**：10 条支线**逐条审计**过，**无一条依赖"本层元素总数"这类读档后无法复算的量** ⇒ 不加计数字段。S3 的分母已被 quests-draft 自行去掉（改「绝对次数 ≤ N」、N 逐图写死在配表）
> - **E-23 写入**：槽满时的正确行为 = **列出槽（名称/时间/进度）→ 玩家主动点选 → 二次确认 → 先确认再写入**
> - ⚠️ **队员第四次纠正我的路径引用**：我写的"写进 §A.2"是错的（§A.2 是**装备槽**），E-23 属**存档槽** ⇒ 已在 §A.9，并注明。

> ⚠️ **两处待同步（已派 quest-writer）**
> 1. **D330 已删 S3，但 quests-draft 正文里 S3 仍在**（结论不受影响，草案需同步删）⇒ 支线最终 **9 条**；
> 2. **S10 的"未使用归途石"**：若"用了归途石但仍能通关"，还需要一个"**本次下探作废**"的标记 —— 由 quest-writer 定义。

### 第 79 轮（lead 裁定：D108 战斗长度重算结案）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D353 | **D242 限次的归属** | ✅ **per-unit**（每个单位各自算"该技能本回合是否用过"），**不是全队共享一个限次**。依据：D242 修订的 D9 原文就是**单位**语义（"同一单位每回合限出手 1 次"），修订只换了限制对象（出手→技能），没换主体 |
| D354 | **Boss 图的 DPR 修正** | ✅ 采纳队员的更正：**Boss 图（只有 1 个敌人）应使用 3.90 的 DPR，而不是 5 敌图的 6.92**（Boss 图修正 ×1.5） |

> 🟢🟢 **D108 结案：D304 的三档硬门【已达成】，且零数值改动**
>
> | 敌人类型 | 硬门（D304） | **实测** | 判定 |
> |---|---|---|---|
> | 普通敌 | 2–4 回合 | **2.29 – 3.53** | ✅ |
> | 精英 | 5–10 回合 | **5.51 – 8.39** | ✅ |
> | Boss | ≥10 回合 | **10.25 – 15.34** | ✅ |
> | 团灭 | — | **0 / 528** | ✅ |
> | 噩梦对普通/精英的 TTK | 需可感知 | **仅 ×1.05** | ⚠️ 仍不可感知 |
>
> **根因的三层结构（队员的归因，我认）**：
> | 层 | 结论 |
> |---|---|
> | ① **配置层** | **不是主因** —— 饰品单件只 **+1.07%**、D183 的三件合计约 **+9.7%**、成长曲线 **<1%** |
> | ② **结构层（主因）** | **每轮我方有 4 个行动，敌方只有 1 个** |
> | ③ **决定性变量** | **D242（每技能每回合限 1 次）** ⇒ TTK 直接拉到 **6.15 回合** |
>
> ⇒ **换句话说：让战斗从 1–2 回合变成 5–10 回合的，是 D242 这一条规则，而不是任何数值改动。**
> ⇒ **批次 2 的解锁条件（D240）就此满足** ⇒ **战术叠层 / 深渊吞噬 / 饰品防御类 / 精英二动 / R2 反击 全部可以验收。**
> ⇒ **§10 #32（输出 ∝N²）需要修正**：N² 是**真实但次要**的因素（N=4 时约 1.3 倍），我此前把它列为"结构性主因"**归因过度**。

> 🔴 **两处必须记录的警告（队员自己的诚实标注）**
> 1. **`+24.13%` 与 `+9.7%` 是两套口径，不能混用**（D197 记的是前者、本次实测是后者）。
>    ⇒ 我此前在 D197 里把 **+24.13% 写成"伤害轴的上界"**，而本次实测 D183 三件约 **+9.7%** —— **这说明两者对"饰品是否全队生效"的假设不同**（前者按"每个饰品都作用于全队"，后者按"只作用于佩戴者"）。
>    ⇒ **D197 的 +24.13% 是保守上界（安全性用途），本次的 +9.7% 是实际预期值（制定目标用途）**，两者**都要留，但必须标明用途**。**下一轮请确认这个记账方式。**
> 2. **敌方战术的 `+19.4%` 与我方饰品/战术同源**（都作用于同一个 `team.*`）⇒ **同时生效时会计入同一处，效果会互相吸收**。
>    ⇒ 这是真实的**同源性限制**，不是 bug；但它意味着**两边不能各自按独立加成估算**。

### 第 80 轮（第 60 轮后的检查点）

| # | 议题 | 裁决 |
|---|---|---|
| D355 | **饰品的两套口径** | ✅ **只留实测值 +9.7%**，**推翻 D197 的 +24.13%**（后者建立在"饰品作用于全队"的假设上，与实测不符） |
| D356 | **批次 2 是否解锁** | ✅ **立即解锁**（D108 的三档硬门已达成，团灭 0/528） |
| D357 | **第 80 轮之后怎么走** | ✅ **边合并边问答**（我把已冻结的并入 GDD §11，同时每轮继续问缺口） |
| D358 | **README 的"每回合自动存档"** | ✅ **取消每回合自动存档**（维持 D68 的"仅事件触发"）⇒ README 那句是过期描述，随 D44 的重写一并修掉 |

### 第 80 轮（lead 裁定：G19 与两处口径）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D359 | **开局流程的最终版（含选槽位置）** | ✅ `主菜单 → 新游戏 → **① 选槽（D296）** → ② 引导开关（D283）→ ③ 难度 + 噩梦勾选（D115/D276）→ 营地`。**① 的位置原本是我的解释、不是裁决**（D276 原文只有 ② ③），**现正式补为裁决**：`guideEnabled` 与难度**都是随档数据**，必须先有槽才有落点 |
| D360 | 🔴 **G19：`save()` 的 null 回落会静默毁存档** | ✅ **成立，最高优先级必修**。修法：**`activeSlot === null` 时 `save()` 直接拒绝写盘**（不回落 `SAVE_KEYS.main`）；且 **`newGame()` 必须要求先选槽**。⇒ 这是 **D296「强制选槽」的前置**，与 E-23「先确认后写入」是同一刀 |
| D361 | **S7 的阈值口径** | ✅ **以草案的「累计击败 15 只精英」为准**（我在 E-25 裁定里随手写的"≥12"是记错的） |
| D362 | **`stats.totalRecruits`** | ✅ **采纳**（支线 S4 需要它，而 `roster` 长度表达不了"累计招募次数"——退货/替换会让它减少）⇒ 字段表 **16 → 17** |

> 🔴🔴 **D360 详述（这是"存档类"里最严重的一条，比 E-23 更硬）**
> **两条独立路径都会违反 D296「强制选槽」并造成静默覆盖**：
>
> | 路径 | 现状 | 后果 |
> |---|---|---|
> | **主菜单的「保存设置」** | `SaveSystem.save(slot = activeSlot)`，`activeSlot === null` 时 key 回落 **`SAVE_KEYS.main`**（`SaveSystem.ts:40-41`）；而 `listSlots()` 把 **`slot 0` 也映射到同一 `main` 键**（`:95-96`、`keyForSlot()` `:139-142`）⇒ **"main 键"在 UI 上显示为第 01 槽** | **未读档时点一下"保存设置"，就把整份 `gameState.s`（`:42-48`）写进显示为「01」的那一槽** —— 覆盖的是一个命名槽的**完整存档**，而不只是设置 |
> | **新游戏** | `SaveSystem.newGame()`（`:289-299`）**不设置 `activeSlot`**，而 `MainMenuScene.startNewGame()` 结尾直接 `save()`（`:167`） | **未选槽直接开新档 = 直接写 01 槽** —— 正是 D296 明令禁止的"默认槽"行为 |
>
> ⇒ **修复要求（并入必修清单）**：① `activeSlot === null` ⇒ `save()` **拒绝写盘**；② `newGame()` 必须**先有槽**；③ 加一条**回归断言**："未选槽时点保存设置 → 第 01 槽的存档内容不变"。
> ⇒ **这条与 D272（落点家族）同级**：都是"代码行为与已裁规则相反、且会造成不可逆损失"。

> 📌 **第 81 轮附：任务/引导草案 v4 完成**
> - **首清科技点 76.3 / 99 = 77.1%**（38 首通 + 14 主线 + 24.3 支线）——**与 D264/D297 的树价 99 自洽，树价不动**
> - **支线金币 11 410 → 8 270 金/趟**（= 战斗金币的 **72.5%**，592 金/10 分钟）
>   ⚠️ **我引给用户的"41.9%"是队员 v3 的算错值，正确占比是 44.9%**（D363 的目标带 ~42–45% 不变）
> - **S9 已改 1 700**，并补了**可行性验收方法**（死支线 < 0.25 次/趟 / 发钱机 > 2.0 / **目标带 0.5–1.5**）—— 这是本轮沉淀的方法论的可执行版本
> - **引导总开关**（D364）已落：**关闭 ≠ 清空**（关闭期照常记账，重开只补缺的）
> - **D365 严格逐字节**已落：不 trim、不归一化（v3 的"忽略大小写"作废）

### 第 81 轮（lead 裁定：G19 升级为正式裁决 + 同根第二颗雷）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D368 | **G19 正式立项** | ✅ **正式 D 号 + 进 D240 的批次 1（不依赖 D108）+ 优先级最高**（全场唯一"会静默不可逆毁档"的缺陷）。修法：`activeSlot === null` ⇒ `save()` **直接 `return false`**，不回落 `SAVE_KEYS.main` |
| D369 | 🔴 **同一 ternary 的第二颗雷** | ✅ **`delete()` / `load()` / `hasSave()` 用的是同一个 ternary** ⇒ **`delete(null)` 会删掉第 01 槽**（当前无调用点，但"删档"功能迟早要做）。**必须与 D368 一起修**，不能只修 `save()` |
| D370 | **`SAVE_SLOT_COUNT` 常量** | ✅ **`config/saveKeys.ts:18` 现值是 3 ⇒ 改为 10**（D293），并入批次 1 |

> 🔴 **D368 的真实后果（队员复核后的结论比"覆盖设置"重得多）**
> 启动后**未读档**时 `gameState.s` 就是 `defaultGameState()`（`party: []`、`gold: 0`、`techPoints: 0`）
> ⇒ 此时点一下"保存设置"，**实际是"拿一份空档覆盖掉第 01 槽的完整存档，不可逆"** —— 不是"设置覆写设置"。
> ⇒ **症状 ②**（第二路径）：`newGame()`（`SaveSystem.ts:289-299`）不设 `activeSlot`，而 `startNewGame()`（`MainMenuScene.ts:167`）结尾**裸调 `save()`** ⇒ **未选槽直接开新档，同样写进 01 槽**。
> ⇒ 波及面：**13 处无参 `save()` 调用**都要复查。
> ⇒ **验收断言（必须写进测试）**：① 未选槽时点"保存设置" ⇒ **第 01 槽内容不变**；② 未选槽时开新档 ⇒ **拒绝或强制弹选槽**；③ `delete(null)` 不产生任何删除。

### 第 82 轮（lead 裁定：D350 之后的金币经济 + 陷阱立案）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D371 | **钱袋套利** | ✅ **现在就修**（`item_coin_pouch.value` **120 → 60**），并入批次 1。理由：**买 100 / 用 120 是无上限印钞**，而 D350 让钱袋在收入中的占比上升 ⇒ 不修会削弱 D350 的全部效果 |
| D372 | **七项价格** | ✅ **0 项需要改数值**（队员实测：★5 = 0.58 趟腐朽、招募 **2.9 次/趟**、训练场出口 **39.8%**、归途石 1.5%）⇒ 此前在旧收入下定的一整套价格在 **−18.8% 的新收入下仍然成立**。⚠️ 本行的绝对值已按**第 92 轮更正**更新（原记 3.0 / 36.6% / −18.3% 是用了**已作废的决后密度**） |
| D373 | **陷阱单独立案** | ✅ **立案，不用调 `trapCount` 去救**。新案号 **T-1「陷阱的致死路径」**：它的前置是 **D1（真共享 HP 池）+ D168（归零即团灭）**，两者都**尚未实现** |
| D374 | **科技点的 80% 复核** | ✅ **派队员复核**（D350 同时改变了金币与科技点的收入结构） |

> 🔴🔴 **第 82 轮最重要的代码事实：`D168` 当前【不可达】**
> 队员用真实代码实测，给出三条硬事实：
> 1. 陷阱伤害有 `Math.max(1, …)` ⇒ **不可能致死**；
> 2. **`startBattle` 不传 HP** ⇒ **每场战斗都是满血开局** —— 也就是说 **D164 的「双层生命账」在代码里根本没实现**（迷宫 HP 与战斗 HP **仍然不连通**，即 §10 #3 的那条老问题）；
> 3. **全仓 `grep 团灭` 零命中** ⇒ **D168「迷宫 HP 归零即团灭」没有任何代码路径**。
>
> ⇒ **结论：在 D1 落地之前，"陷阱太致命/太温和"这个讨论本身是空转的** —— 因为迷宫 HP 既不会致死、也不会带进战斗。
> ⇒ 这解释了为什么 D172（不降伤害、降数量）与 D349（保持 1/1/2/2）怎么调都"感觉无差别"：**它们调的是一本不参与任何判定的账**。
> ⇒ **T-1 的落地顺序**：`D1（真共享池）→ D168（致死路径）→ 才是 D172/D349 的数值微调`。

> ⚠️ **D350 的一个副作用（需要补偿）**：宝箱在**第一张图**的收入里占 **75%**（腐朽仅 42%）
> ⇒ 砍宝箱让**前期被砍得比后期重**（无根第 1 层只剩 **107 金**）。
> ⇒ 与"无根是新手图、玩家最穷"叠加后，**新手期的挫败感会被放大**。**补偿方案下一轮拍板。**

### 第 83 轮

| # | 议题 | 裁决 |
|---|---|---|
| D375 | **D350 的前期补偿** | ✅ **接受，不补偿** —— 新手期本来就没多少东西要买（★3 饰品 600 金 ≈ 6 层收入），而 D350 砍的是"密度"不是"单价" |
| D376 | **T-1 的验收带** | ✅ **每 10 趟 0–1 次**（≤10%）—— 陷阱是"意外"而非"常态"，与 D4「不做硬核生存压力」一致 |
| D377 | **循环次数的目标** | ✅ **接受 13.1 循环**（招满 20 名 3.7 + 12 件饰品 0.91 + 6 名主力 L4→L20 共 13.1）；**不追求极端 32.7 循环**（其 86% 花在训练场） |
| D378 | **陷阱的当前处置** | ✅ **冻结 `trapCount` 不动**，等 **T-1**（前置 D1 真共享池 + D168 致死路径）再统一调 |

### 第 84 轮（lead 裁定）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D379 | **勘误包的落点** | ✅ **允许队员在草案原文后追加"勘误包 v6.1"**（不改既有段落，只新增），与 GDD 侧的"勘误回填"是同一套留痕纪律 |
| D380 | **存档字段的账目** | ✅ **最终 17 个字段**（其中只有 **D362 的 `stats.totalRecruits`** 是第 17 个；**D364 的"关闭全部引导"不新增字段** —— 它在 `state.settings` 内复用 `guideEnabled` 的语义） |
| D381 | **D307 的解挂** | ✅ **解挂**：队员确认 S9 的"宝箱金币分项"**早已存在**于 `techpoint-economy-v1.md` §4.3（四图 **1 393 / 1 858 / 2 322 / 2 786**，四图合计 8 359）⇒ **D300 ② 的 2 500 已有实测支撑**，只待 D350 之后同步为 **1 700**（D367） |
| D382 | 🔴 **`enemies-draft-v1.md` 丢失事件** | ⚠️ **已确认：该文件在磁盘上不存在**（我先后两次列 `design/drafts/` 对比：**之前 15 个 md 里有它，现在没有**）⇒ 需**重新产出**。详见下方专项记录 |

> 🔴🔴 **D382 专项记录：一份队员交付物丢失（必须记住的流程教训）**
>
> **事实**（我的证据，不是转述）：
> - 我在并入 §11.A 之前列过 `H:\work\maze-rpg\design\drafts\*.md`，返回 **14 个文件，其中含 `enemies-draft-v1.md`**；
> - 本轮我再列，返回 **15 个文件（多了 `gold-economy-v2.md`），`enemies-draft-v1.md` 已不在**；
> - 另跑 `enemies*` 通配，**零命中**；
> - 而 **general-purpose-2 两次报告"该文件不存在"**（H 盘与 C 盘都查过）—— **它是对的，我此前反驳它说"文件在"是错的**（我引用的是**更早的**目录快照）。
>
> **丢失的内容**：**28 只普通敌 + 4 只专属精英 + 4 只「深渊之王的虚影」 + 9 条新增敌方技能**（含每只的双列数值、族群归属、技能分配、掉落）—— 这是 §11 里"敌人"一节的**唯一来源**。
>
> **原因（待查）**：本会话里已有**两次队友主动删文件**的记录（general-purpose-1 用 `fs.unlinkSync` 删掉了自己的重复稿 `skill-texts-v2.md`）。**需确认是不是**某次"清理"扫到了不该删的文件。
>
> **处置**：① 已让 **general-purpose-3 用 `fs.writeFileSync` 重新落盘**（它的上下文里应当还留着这份草稿）；② 若上下文已被压缩，则按已裁的约束重新生成（每图 8 只 / 浅 4 深 4 / 普通 2 技能 / 精英独立条目 / 强度 ≤ 骸骨暴君 / 族群归属按 D121）。
>
> **流程教训（写给以后的自己）**：
> - **队员的交付物落盘后，lead 要立刻记下"文件路径 + 生成时间"**，不能只依赖"它说交了"；
> - **任何"清理/删除"指令必须写明白名单**（本会话我已经在派单里写过"只改你这两个 md"，但**没有写"不要删别人的 md"**）；
> - ⇒ **新纪律：派单时必须加一句"禁止删除 `design/drafts/` 下任何文件"**（除非明确授权删某个指定文件）。

### 第 84 轮

| # | 议题 | 裁决 |
|---|---|---|
| D383 | **D342 vs D366 的冲突（敌方用不了概率轴）** | ✅ **为敌方另做 3 个替代战术** —— 用 `attack` / HP / 护盾实现，**效果对齐敏捷 / 幸运 / 灵性但走不同机制** ⇒ 敌方共 **9 个战术**（5 属性位 + 4 族群专属，其中 3 个属性位是"敌方版"） |
| D384 | **敌人草案丢失的兜底** | ✅ **并行重写一版**（gp3 从上下文恢复原文 + 新派一名队员独立重写）⇒ 双保险，最后取完整的那份并**对账差异** |
| D385 | **存档字段账目的落点** | ✅ **并入 §11.D**（并在顶部标明"**17 个字段，其中第 17 个是 D362 的 `stats.totalRecruits`**；D364 的引导总开关**不新增字段**，复用 `state.settings` 的 `guideEnabled` 语义"） |
| D386 | **最后 16 轮怎么走** | ✅ **继续问答** |

> 📌 **D383 的落地含义（写清，避免与 D366 再撞）**
> 敌方的 5 个"属性位"战术是：**火力校准（筋力）/ 结构加固（耐力）/ 3 个敌方版**。
> ⇒ **3 个敌方版不能复用我方的名字**（因为我方那三个走的是概率轴，机制不同），必须**另起名 + 另写效果**。
> ⇒ **HUD 左侧仍是 5 条**（D326/D328 不变），只是其中 3 条的内容与我方不同名 —— **"左/右对齐"的对称性保留，但两侧战术不再完全同名**。
> ⇒ 这也意味着 **D342 的"属性共用"实际只对 2 个战术成立**（火力校准 / 结构加固），需在 §11.B.2 的表格里注明。

> ⚠️ **D384 的双路重写要防的一件事**：两份稿子可能给出**不同的敌人名字与数值**。
> ⇒ 收齐后必须**逐项对账**（数量 / 名字 / 三轴强度 / 族群归属 / 技能 id），差异项由我裁定，**不允许两份并存**（否则会变成第二个真源）。

### 第 85 轮（lead 裁定：§11.D 并入 + 一处命名空间事故）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D387 | 🔴 **两套 G 编号是不同命名空间（我的第 5 处"混引"错误）** | ✅ **以 `save-schema-spec-v1.md` 的 G 表为准：G1–G18 = 18 条，加 G19 ⇒ 19 条**。我在派单里说的"23 条"是**把 `ui-spec-additions-v1.md` 的 G 编号空间混进来了** —— 而 UI 规格**自己在文首写过**"本文 G 编号与 save-schema G 编号是**两套命名空间，勿混引**"（我读了却没照做）。⇒ **今后引用 "G 编号" 必须带文件名前缀**（如 `save-G19` / `ui-G15`） |
| D388 | **存档规格的并入** | ✅ **并入 §11.D**（见下，含 17 个字段与 19 条 G 缺口 + 勘误包） |

---

### 11.D 存档规格

> 🧭 **§11 导航（重要）**：`§11 内容附录`的**物理顺序尚未重排**（当前是 D → C → B → A），**规范顺序是 A → B → C → D**：
> **A 饰品·Boss·族群特性** → **B 战术系统** → **C 技能文案** → **D 存档规格**
> ⇒ **待办（第 90 轮一致性体检时统一重排）**：把 `## 11` 标题与 §11.A 从文档尾部移到 §11.B 之前。
> ⇒ 目前**不影响内容正确性**（各节自洽、互不引用行号），只是阅读顺序异常。
>
> 🗺️ **坐标映射（给"按 D398 去查真源"的人）**：`§11.D` 的内容来自 `save-schema-spec-v1.md` 的 **§A + 勘误包**，**不建立新的子编号**。其中：
> - **"存档槽规则"**（E-23 三步覆盖 + D365 逐字节比较 + 四步顺序）= 规格 **§A.9 + 勘误 #5**（**这是 D398 第③条指向的坐标，不得改名**）
> - **"字段定义 / 默认值 / 写入时机"** = 规格 **§A + 勘误包**
> - **引导 key 清单与全部文案** = `tutorial-draft-v1.md` / `quests-draft-v1.md`
> ⚠️ 我此前在对话里写过"名册编号统一为 §A.8 / §A.9.1 / §A.9.2" —— **那两个子号是我编的，规格里没有**（本会话第 4 次转述失真）。

> **来源**：`design/drafts/save-schema-spec-v1.md` **v6.1（含勘误包，冻结）**
> **对应裁决**：D46 / D68–D71 / D100 / D105 / D115 / D134–D137 / **D283–D296** / D337 / **D360 / D362 / D368–D370 / D379–D381 / D385 / D387**

#### 11.D.1 版本与迁移

| 项 | 值 |
|---|---|
| **当前 `SAVE_VERSION`** | **4**（真源 `src/systems/GameState.ts:14`） |
| **目标版本** | **5 —— 纯闸门**：`version !== 5` 即拒，**读档与导入两个入口都拦**（D134） |
| 现状缺陷 | `migrateSave` 只覆盖 v1→v4 且**无拒绝分支**；**`load()` 与 `importJson()` 都无条件迁移** ⇒ **D134 在代码里根本没实现，导入路径完全敞开** |
| 槽位 | **10 槽 + 可命名 + 不允许重名（严格逐字节比较，不 trim 不归一化）**（D293/D334/D365/D339） |
| 自动存档 | **独立键，不占 10 个命名槽**（D314/D319） |
| 手动存档 | **仅营地可手动存**（迷宫内与战斗中不可）；**迷宫"存档点"元素属 D68 的事件触发式自动存档，不受此限**（D287/D316） |

#### 11.D.2 新增字段（共 17 个）

| 来源 | 字段 | 说明 |
|---|---|---|
| D115 + D109 | `difficulty`、`nightmareMods[]` | 难度开局选定不可改；噩梦机制可叠加 ≤4 |
| D141 / D292 + D140 / D312 | **出战位的双槽**（饰品 + 消耗品） | **挂在出战位**上，不是挂在角色上（换人不丢）；容器 = `party` **对象数组** `{ charId, accessory, consumable }`（D313/D318） |
| D82 / D88 | 饰品资产与锻造进度 | `ownedAccessories` **必须与 `forge.forgedCount` 分开**（D82 有三条获取途径） |
| D92 / D237 / D300 | 任务进度 | **两层表**：永久表 + `activeRun.runCounters`（随下探重置、**团灭时随进度清**） |
| D105 + D135 | 统计 4 项 + `maxFloorEver` | `maxFloorEver` **per-地牢**（D281/D285）；口径见下 |
| D362 | **`stats.totalRecruits`（第 17 个）** | **默认 0，开局 `createStarters()` 白送的角色不计入**；判据 = "累计**花金币**招募 6 次（重复角色计入、退货后再招也 +1）" |
| E-25 | `stats.eliteKills`、`stats.clearStreak` | 二者都**不可推导**（`totalDefeated` 不含类型信息；`totalClears` 单调递增、表达不了"连击被打断"），且不能放 `runCounters`（会被团灭清掉） |
| D283 / D364 | `settings.guideEnabled` | **不新增字段**（复用既有 #12）；语义三条：**关闭 ≠ 清空** / **关闭期照常写 `firstTimeFlags`** / **恢复时只补缺的** |
| D294 | `firstTimeFlags` | 引导 **11 条**的 key 集合（原写 10，**已按 D422 更正**），**可清空**（"重看全部提示"入口） |

#### 11.D.3 统计口径（D295）

`totalDefeated` **含 Boss 与精英**｜`totalClears` **含重复通关**｜`totalGoldEarned` **含卖出所得**（收入侧统一收口在 `addGold`，**只记入账不记花费**）

#### 11.D.4 🔴 必修的代码缺口（19 条 G + 落点家族 + 顺序约束）

| 序 | 缺陷 | 位置 |
|---|---|---|
| **1** | `applySupportEffects` 增减益落点**恒为施法者**（D272） | `DamageCalculator.ts:270-287` |
| **2** | `applyEnhancedEffects` 落点**恒为被击中的敌人**（save-G6） | 签名 `:301-308`、落点 `:332` |
| **3** | **70 条"伤害 + `effects`"的 `effects` 不结算** | `DamageCalculator.ts:117-123` |
| **4** | **`power=0` 的纯辅助技不掷强化**（D259） | — |
| **5** | 🔴 **`save()` 在 `activeSlot === null` 时回落 `SAVE_KEYS.main` = UI 的"01 槽"**（D368） | `SaveSystem.ts:40-41` + `:95-96` + `:139-142`；受害路径 `MainMenuScene.ts:637` / `:167`；`newGame()` `:289-299` |
| **6** | **同一 ternary 也在 `delete()` / `load()` / `hasSave()` 里** ⇒ `delete(null)` 会**删掉第 01 槽**（D369） | `SaveSystem.ts:65-90` |
| **7** | `slot.unlocked` **无人置 true**（D269 ⇒ 归档为死字段） | — |
| **8** | `maze_rpg_settings` **整体覆写**会静默抹掉 D100 的可访问性设置（G18） | `graphics.ts:174` |
| **9** | `SAVE_SLOT_COUNT` 现值 **3 ⇒ 改 10**（D370） | `config/saveKeys.ts:18` |

**必须遵守的三条顺序约束（顺序错了会造出更坏的后果）**：
1. **修两个落点（①②）→ 再修 effects 结算路径（③）→ 再修强化（④）→ 最后才落 D233/D249 的数值改动**（否则会往配表写入"自伤技能"）
2. **版本闸门（G1）必须先于 `party` 改对象数组（G15）**（否则旧档的 `string[]` 会被读成对象、`party[i].accessorySlot` 得 `undefined`，表现是"编队界面全空"）
3. **`party` 结构变更（G15）必须先于 `activeSlot` 修复（G19/D368）**（否则修 G19 时面对的仍是"用数组下标当角色身份"的旧结构）

### 第 86 轮

| # | 议题 | 裁决 |
|---|---|---|
| D389 | **最终交付物** | ✅ **五件套**：① **GDD 单文件**（含 §11 内容附录）② **风险清单** ③ **落地顺序**（批次 1/2/3 + 顺序约束）④ **验收矩阵**（每条硬门 → 对应脚本/断言）⑤ **代码缺口单**（19 条 G + 落点家族 4 条） |
| D390 | **给实现者的交接文档** | ✅ **单独出一份一页纸**（必修代码缺口 + 顺序约束 + 批次清单 + 验收命令）—— 因为已经有人问"从哪里开始改" |
| D391 | **一致性总检** | ✅ **第 90 轮做一次全形体检**：把 386 条裁决按系统分组，重扫跨组依赖与内部矛盾，**并专查"被后续裁决覆盖的旧裁决"**（本会话已抓到 5 处以上） |
| D392 | **验收矩阵的产出** | ✅ **派 economy-analyst**（它手里有全部脚本：`mc-difficulty` / `mc-combat-length` / `mc-gold-economy` / `mc-techpoint`） |

> 📌 **第 86 轮：`save-schema-spec-v1.md` 已完成 v6.1 并**彻底收工**（含勘误包）
> ⇒ **§11.D 的并入是本会话第 4 份并入**（A 饰品/Boss/族群特性 → B 战术 → C 技能文案（待）→ **D 存档**）
> ⇒ ⚠️ 队员同时纠正了我一处**命名空间混引**（D387）：我把 UI 规格的 G 编号空间与存档规格的混成了一谈。
> ⇒ **新纪律（已生效）**：引用 "G 编号" **必须带文件名前缀**（`save-G19` / `ui-G15`）。

### 第 87 轮

| # | 议题 | 裁决 |
|---|---|---|
| D393 | **商店那件"随机稀有饰品"按哪一档** | ✅ **按本层档位 + 小概率上浮**（如 N5 有 15% 出 ★5）—— 不会用商店跳过锻造链，但保留"惊喜" |
| D394 | **商店卖的材料怎么分层** | ✅ **只卖本档材料**（N1–N4 魔铁矿 / N5–N6 深渊结晶）；**魔核不在商店售卖**（只能从 Boss / 精英 / 事件获得） |
| D395 | **消耗品 9 种与事件 11 种的审核方式** | ✅ **我逐条审**（约多花 2–3 轮） |
| D396 | **敌人双路重写后的数值基准** | ✅ **交齐后由我对账裁定**（取一份，差异项我定），不允许两份并存 |

### 第 87 轮（lead 裁定：三份队友回传的收口）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D397 | 🔴 **出战位容量（我的第 6 处混引）** | ✅ **`\|party\|` = 4 → 6**（初始 **4** 人，科技树「队伍扩编 I/II」→ 5 → 6）。我在派单里写的"3 → 6"是**把 `SAVE_SLOT_COUNT = 3` 当成了出战位容量** —— 同一个 3，用途完全不同 |
| D398 | **合并时的"真源优先级"（三条）** | ✅ 采纳：**引导 key 清单与文案**以 quest-writer 的 tutorial / 任务草案为真源；**字段定义 / 默认值 / 写入时机**以存档规格（§A + 勘误包）为真源；**存档槽规则**（E-23 三步覆盖 + 逐字节比较 + 四步顺序）以存档规格 §A.9 + 勘误 #5 为真源 |
| D399 | **存档命名的 UI 时序** | ✅ 采纳两条推论：① **命名与查重必须发生在"清空并开始？"这次不可逆确认之前** ⇒ UI 上**先输名、再问清空**；② **查重只能是"纯读校验、禁止写盘"**（否则玩家点取消后，槽里会残留一个他从未确认过的名字） |
| D400 | **查重的"自身豁免"** | ✅ 采纳 quest-writer 补的那条：查重必须**排除正在被覆盖 / 正在改名的那个槽自身**（`isNameTaken(name, exceptSlotIndex?)`），否则"沿用该槽旧名"会被**它自己的旧名**假报错 |
| D401 | 🔴 **D350 与 D367 是"互相补偿的一对"** | ✅ 记录：**D350 单独 −3.23 点**（S9 阈值锁 1700 时 2.71→1.09 次/趟）｜**D367 单独 +3.12 点**（2500→1700）｜**合计 −0.11 点**（树 99 达成率 77.1% → **77.0%**，差 0.1pp）。⇒ **将来只回退其中一个会出问题**，必须成对处理 |
| D402 | **D374 结案：科技点卡线不破** | ✅ **树价 99 维持**。队员证实我上轮的担心是**假警报**：80% 口径 = 首通 38 + 主线 14 + 支线（24.3 / 29.3），**宝箱从来不在这个和里**（D245 已裁"宝箱不产科技点"）；配表残留的 0.15 点/箱是**未落地旧值**。⇒ **D307 解挂**（宝箱金币分项早已交付：929 / 1 394 / 1 393 / 1 858），**S9 实测 1.09 次/趟 ✅ 落在 0.8–1.4 带内** |

> 📌 **本会话我被队员纠正的清单（现共 8 处，全部是"数字/位置/命名空间"类）**
> ① 类 2 的数值上界；② 类 3 的档位缺口（我的规格错）；③ R4 上界用了旧值；④ 事件净额口径；
> ⑤ 拒绝写入我的错误结论（战术"1–2 层体验"）；⑥ `SAVE_VERSION` 的位置；⑦ 草案真身目录；
> ⑧ **§A.2 vs §A.9 的章节引用**；⑨ G 编号命名空间混引；⑩ **`|party|` 与 `SAVE_SLOT_COUNT` 混引**。
> ⇒ **归纳：我的错误集中在"把某个数字用到别的语境"**，而队员的错误集中在"口径与假设"。
> ⇒ **有效对策（已生效）**：给队员的每条数字断言都注明**来源与用途**；引用编号必须带文件名前缀。

### 第 88 轮

| # | 议题 | 裁决 |
|---|---|---|
| D403 | **科技树能否洗点** | ✅ **不能洗**（与 D115「难度不可改」同类的"选择有重量"原则） |
| D404 | **训练场的次数上限** | ✅ **不限次** —— 兑换率（1 金 = 1 经验）本身就是限制，不再叠一层 |
| D405 | **营地的设施清单** | ✅ **刚好够**：招募 / 队伍 / 背包 / 科技 / 难度（已锁定，仅显示）/ **训练场**（D107）/ **手动存档**（D287） |
| D406 | **难度的"推荐"提示** | ✅ **标推荐**：难度选择界面标注"**推荐：普通**"，并在噩梦上标"**不推荐首次游玩**" |

---

### 11.C 技能文案

> **来源**：`design/drafts/skill-texts-v1.md` **v4（已完成）**
> **对应裁决**：D11 / D146 / D175 / D181 / D199 / D231–D236 / D242 / D249–D251 / D259 / D272

#### 📌 **并入口径（对 D244 的一处**收窄**，记录理由）**

**93 条文案的正文留在草案**（`skill-texts-v1.md`），**不进 GDD 正文**。理由：
> D244 的初衷是"把散落的设计**数值表**收进 GDD，避免读者要开一堆文件"。但**文案本身就是文本内容**（不是可核算的数值）—— 把它复制进 GDD 只会让 GDD 变成**第二份文案库**，与"单一真源"的原则相悖。
⇒ **GDD 收的是"规则 + 账目 + 受影响清单"**；**逐条文案以草案为真源**。这条已在 §11 开头登记。

#### 11.C.1 条数账目（可核对）

| 范围 | 条数 | 状态 |
|---|---|---|
| **玩家技能** | **80** | 其中 **35 条**受战术系统与 debuff 改轴影响被改写，**45 条一字未动** |
| **Boss 专属技能** | **13** | 一字未动（4 个 Boss：3 + 3 + 4 + 3，D186 把 D51 的"2–3 个"改为"**2–4 个**"） |
| **合计** | **93** | **需文案 93 条 = 80 角色专属 + 13 Boss 专属**；⚠️ 原文"与 `skills.json` 的现役条数一致"**不准确**，勘误见表后 |

> ⚠️ **§11.C.1 勘误（第 101 轮 · 落地实测）**：**「93」这个数是对的，错的是"与现役条数一致"这句措辞**。口径应写成：**93 条需文案 = 80 条角色专属 + 13 条 Boss 专属**；而 `src/data/tables/skills.json` 现役 **101** 条 = 80 + **8 条普通敌方技** + 13，那 **8 条普通敌方技沿用旧文案、不计入 93**。（80 + 13 = 93 才是需文案量，切勿按"93 = 现役"去反推删掉 8 条。）

#### 11.C.2 五条硬规则

| # | 规则 | 依据 |
|---|---|---|
| 1 | **字段沿用 `description`**，**不新增 `desc`**（该字段已存在且被前端读取：`DataLoader.ts:176`） | D181 |
| 2 | **字数上限 ≤55 汉字**（原 ≤40 保不住"战术授予句"，而放弃战术句会让两条纯授予技的 `description` 变空串） | D232 |
| 3 | **禁止出现已删/已废弃字段**：`cooldown` / `CD` / `cost` / `element`（属性克制已删） | D11 / D42 |
| 4 | **不写 AP 数值**（留给 UI 渲染，避免与 D5 分档后的值不一致） | D5 |
| 5 | **不写公式里没有的量**：伤害句**不得出现"魔力"**（伤害公式只有 `team.attack × power`，且魔力战斗内无效） | D251 |

#### 11.C.3 三条数值口径

| 项 | 值 | 依据 |
|---|---|---|
| **治疗量口径** | **攻击力派生**：`队伍攻击力 × 倍率 + 灵性 × 0.5`（**§10 第 10 条就此关闭**） | D235 |
| **本体 debuff（削敌方）** | **10 条**改为削 **敌方 `attack`**，幅度 **−15% ~ −22%** | D233 / D249 |
| **`enhanced` 的 debuff** | 削 **敌方 `defense`**，幅度 **−12% ~ −15%** | 同上 |
| **`清雪居合` 例外** | 恢复"**自身敏捷 −8、1 回合**"的**自代价**设计（我的 D233 一刀切误伤） | D250 |

> ⚠️ **D233/D249 的适用范围（D250 收窄后）**：只针对**作用于敌方**的 debuff。
> ⇒ 依据是代码事实：**敌方单位只读 `attack` 与 `defense` 两个轴**（`speed` 不参与出手顺序；魔力/幸运/灵性/敏捷在敌方侧**零读取点**）⇒ 削这四维**在设计上就是空效果**，不是"缺授权"。

#### 11.C.4 三条前置依赖（文案落地前必须先做）

1. **D272**：`applySupportEffects` 的增减益落点**恒为施法者** ⇒ 修好前这几条 debuff 的文案**与实际不符**（我方"削敌"技能会削弱自己）
2. **`applyEnhancedEffects` 的落点也错**（`save-G6`，恒为被击中的敌人）
3. **`power=0` 的纯辅助技不掷强化**（D259 ⇒ 配表已写 `enhanced`，应修代码而非删文案）
⇒ **文案里已逐条标注 `⚠️ 依赖 D272 的代码修复`**（§0.5 前置修复节 + §3.2 十条 + §4 六条）。

### 第 89 轮（lead 裁定：敌人独立重写稿的 5 条关键项）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D407 | 🔴 **敌人总数（我的第 11 处算术错）** | ✅ **定为 32 只**（4 图 × 8）—— **D148 原文写的"共 28 只"是错的**（4 × 8 = 32）。⇒ 修正 D148 的数字；若坚持 28 则须改为"每图 7 只"（**下一轮由用户拍板**，因为它改变内容量） |
| D408 | 🔴 **基础防御上限（我的第 12 处混引）** | ✅ **上限 = 基础 0.20**（**不是 0.14**）。我把「**骸骨暴君重定标后的防御 0.14**」误当成了"全表上限"写进派单。⇒ **骸骨暴君的防御提到 0.20**，并准予**现役怪保留高防辨识度**（第 6 层 0.20 × 2.5 = 0.50，距 `clamp(0.95)` 仍有 **1.9 倍**余量） |
| D409 | 🔴 **AI 永不释放多段技（真 bug）** | ✅ **改为按 `power × hits` 排序**（现按 **raw power** 排序，`CombatSystem.ts:643-645`）⇒ 否则多段技**永远排不上**：含**现役的 `enemy_sweep`** + 本稿新增 2 条 = **3 条死技能**。**并入必修清单** |
| D410 | **`heal_max_ratio` 的配套修复** | ✅ **必须同步改 `CombatSystem.ts:639`**（它只认 `type === 'heal'`）⇒ 否则新效果永不释放（"续髓"直接是死技能）。**并入必修清单** |
| D411 | **精英"必掉"的口径（D118 vs D331）** | ✅ **精英必掉 = "该图能产出的最高档锻造材料"**：无根 / 苔痕 → **魔铁矿**｜静谧 → **深渊结晶**（+ 魔铁矿）｜腐朽 → **魔核**（+ 深渊结晶）。⇒ 同时满足 **D118（必掉锻造材料）**与 **D331（不写死"档位材料"，因为苔痕没有档 II 材料）** |
| D412 | **`family` 挂哪 + 跨图复用** | ✅ **仍挂在敌人身上**（D231），**保留跨图复用**（骷髅兵同时在苔痕与静谧）⇒ **不接受"取消复用"**：D148 的浅/深分组会让同一只怪在两张图承担不同角色，取消复用会把内容量翻倍 |

> 📌 **必修清单从 9 条增到 11 条（第 89 轮又 +2）**
> | 新增 | 现象 | 位置 |
> |---|---|---|
> | **AI 按 raw power 排序** | 多段技（含现役 `enemy_sweep`）**永远不会被 AI 释放** | `CombatSystem.ts:643-645` |
> | **`heal_max_ratio` 不被识别** | 只认 `type === 'heal'` ⇒ 新效果永不释放 | `CombatSystem.ts:639` |
> ⇒ 这已是**第 6 次**发现"**配表/设计写了，但代码路径读不到**"这一类缺陷（前四条见 §11.D.4 的落点家族）。

> ⚠️ **敌人稿其余 12 条（Q6–Q17）里，以下四条需与另一路稿对账后再裁**：**Q7（精英 gold 与定稿不符）/ Q8（虚影 family 与孢母被动）/ Q9（腐朽虚影超上限）/ Q14（现役怪 exp·gold 小幅变动）** ⇒ 归入 §11 的"敌人"节，**两稿齐了统一处理**。

### 第 89 轮（用户裁决）

| # | 议题 | 裁决 |
|---|---|---|
| D413 | **敌人总数** | ✅ **32 只（每图 8）** —— **D148 的"共 28 只"是算术错**（4 × 8 = 32），已更正 |
| D414 | 🔴 **基础防御上限（修订 D408）** | ✅ **上限 = 0.30**（以现役最高的**石魔像**为准）—— **现役 4 只一只都不降**。⇒ 修订我在 D408 里写的"0.20"。⚠️ 连带：**石魔像在第 6 层是 0.75 减伤**（我方只能打出 25% 伤害），这条风险**被接受** |
| D415 | **虚影的 `family`** | ✅ **独立族群 `echo`** ⇒ 虚影**不吃任何族群被动**，四图之间只有数值差异（0.5/0.7/0.85/1.0） |
| D416 | **现役怪的 exp / gold** | ✅ **允许调整**（因为 D204/D254/D255 砍掉了大量产出，现役怪的奖励需要重对）；**但三轴强度不得上调** |

> ⚠️ **D414 的连带（写清楚，避免以后当成 bug）**
> | 敌人 | 基础防御 | **第 6 层（×2.5）** | 我方实际伤害 |
> |---|---|---|---|
> | 石魔像 | 0.30 | **0.75** | **25%** |
> | 黯甲骑士 | 0.24 | 0.60 | 40% |
> | 虚空祭司 | 0.20 | 0.50 | 50% |
> ⇒ 这是**有意保留**的"重甲辨识度"，代价是**第 6 层的重甲怪极其难打**。
> ⇒ 与 **D14 的防御缩放 ×(1+0.3(N−1))** 叠加后，**第 6 层的防御轴是全局最陡的一轴**（HP ×3.5、攻 ×2.25、**防 ×2.5**）。
> ⇒ ⚠️ **须在 D108 的验收里补一条**："第 6 层重甲怪（石魔像）的 TTK 不得越过 Boss 的硬门（≥10 回合）"，否则重甲怪会变成比 Boss 更耐打的路边怪。

> 📌 **D415 对 §11.A.3 的影响**：族群表需要加一行 **`echo`（深渊回响）**，特性栏写"**无被动特性**"（因为虚构影只吃数值衰减）。已回填至 §11.A.3。

### 第 90 轮（一致性体检的入口）

| # | 议题 | 裁决 |
|---|---|---|
| D417 | **敌人总量** | ✅ **接受 44 个条目**（32 普通 + 4 精英 + 4 虚影 + 4 Boss）—— 与 D72「先做模板后续可扩建」一致 |
| D418 | **体检报告的形态** | ✅ **只写进 GDD**（在 §0.5 末尾加一节「**裁决覆盖台账**」），不另建文件 |
| D419 | **重甲怪在第 6 层的边界** | ✅ **接受 0.75 减伤，但必须同步拉高击杀奖励**（否则玩家会绕开重甲怪）；且其 **TTK 不得越过 Boss 的硬门（≥10 回合）**—— 已并入 D108 的验收 |
| D420 | **最后十轮的分块** | ✅ **四块**：① **第 90 轮体检** ② 90–95 把两路敌人稿对账并并入 §11 ③ 95–98 出**风险清单 / 验收矩阵 / 交接文档** ④ 99–100 全文收口与交付 |

> 📊 **第 90 轮检查点：累计 420 条裁决、90 轮**
> | 项 | 状态 |
> |---|---|
> | **设计覆盖** | 战斗 / 成长 / 经济（金币·科技点·材料）/ 迷宫 / 敌人 / 装备饰品 / 战术（双方）/ 消耗品 / 事件 / 任务 / 引导 / 难度 / 存档 / UI |
> | **11 份草案** | 内容（饰品·Boss·族群特性）/ 战术 / 敌方战术 / 技能文案 / 存档规格 / 消耗品 / 事件 / 任务 / 引导 / UI 规格 / 经济与重算系列 |
> | **§11 已并入** | **A 饰品·Boss·族群特性** ✅ / **B 战术** ✅ / **C 技能文案（规则+账目）** ✅ / **D 存档规格** ✅ |
> | **必修清单** | **11 条**（4 条落点家族 + AI 多段技 + `heal_max_ratio` + 70 条 effects + 强化不掷 + `slot.unlocked` + `maze_rpg_settings` 覆写 + `SAVE_SLOT_COUNT`） |
> | **顺序约束** | **3 条 + 2 对同批次**（落点家族顺序 / G1→G15→G19 / D258+D299 / D350+D367） |
> | **仍开放** | 敌人两稿对账（Q6–Q17）/ 消耗品 9 种与事件 11 种的逐条审（D395）/ 敌方 3 个替代战术 / UI 的 G2·G5·G6 三条阻断项 |

### 第 91 轮（lead 裁定：三处收口，其中一处修我自己的不自洽）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D421 | 🔴 **存档命名的四步顺序（修我自己的不自洽）** | ✅ **以 `校验/替换 → 查重 → 确认 → 落盘` 为准**。我在 D398/D399 里先写"查重 → 落盘 → 确认"、又写"查重必须在不可逆确认之前" ⇒ **两句自相矛盾**（前者让"确认"变成空操作）。⇒ 队员的取法正确：**"确认"必须是写入的闸门**，而"查重"必须在它之前 |
| D422 | **引导提示的条数** | ✅ **`firstTimeFlags` = 11 个 key**（原写 10；新增 `T11 tip_dungeon_event`，47 字，触发 = 事件界面首次弹出）⇒ **不新增字段**（只是集合元素 +1） |
| D423 | **事件的三个未定义设定（T11 的三件）** | ✅ **一律按"最不宽容"处理**：① **事件弹出后必须选一个选项，不能拒绝/走开**；② 因此"拒绝的代价"这个问题**不存在**；③ **不能重掷**（一次性）。⇒ 理由：与 **D209「埋伏事件不允许逃跑」**同构 —— 事件是支柱 B 的主要承载体（D189/D210），**可回避就等于没有风险** |

> ⚠️ **D421 的教训（记一笔）**：我**在同一条消息里给了两处顺序描述**，一处是"四步落地顺序"、一处是"UI 时序" —— 它们**指向同一个动作链**，我却用了不同的排列。
> ⇒ **规律：只要两条约束描述的是同一个动作，就必须合并成一条写**，否则必然出现自相矛盾。这是我在本项目第 13 处表述问题（前 12 处是数字/命名空间类，这处是**顺序类**）。

> 📌 **对已冻结的 `save-schema-spec-v1.md` 的两处交叉勘误（记在 GDD 侧，不回头改冻结文件）**
> | 项 | 冻结稿写的 | **以 GDD 为准** |
> |---|---|---|
> | 四步顺序 | "校验/替换 → 查重 → 落盘"（未把"确认"插入） | **校验/替换 → 查重 → 确认 → 落盘**（D421） |
> | 引导 key 数 | §A.6 的「key 清单以 10 条为准（T1–T10）」与「基线规模 = 10 个 key」 | **11 个**（D422）。⚠️ 我此前把原文转述成"「共 10 个」"**不准确**，此处按**原文措辞**引用 |
> ⇒ 冻结稿**只读**；**以 GDD 为准**（这条规则在 §11 开头已登记）。

### 第 92 轮（lead 裁定：队员报的 4 处问题 + 经济口径二次更正）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D424 | 🔴 **残躯继承比例的真源自相矛盾（我的错）** | ✅ **普通敌 = 25%**（D121 原值）／**碎巢母 = 15%**（D169 的 Boss 例外）。我在 §11.A.3 把**"碎巢母的例外值"当成了全体值**写进汇总表 ⇒ **已修正**（这是第 14 处"把某个数字用到别的语境"） |
| D425 | **敌方族群专属战术的承载（不打散 D153）** | ✅ **改挂"该图的精英 / 虚影 / Boss"，普通敌不挂** ⇒ 保住 **D153（普通敌 2 技能）**，同时避免 82 条敌方技能的预算再膨胀。⇒ 这比"替换式（普通敌 2 技能里换掉 1 条）"更好：不牺牲普通敌的技能多样性，且族群战术**只出现在"首领级"敌人身上**更符合主题 |
| D426 | **深渊吞噬的"不致死保护"** | ✅ **保留** —— 成本为零，且能避免 **1 HP 边角下"0 点伤害 vs 反复触发"的语义 bug** |
| D427 | **深渊吞噬的量级说明（只记录，不改数值）** | ✅ 记录：旧文写的"**每回合 8%**"在 **D159 阈值化**之后实为"**每场 1 次 8%**"⇒ 它已不再是 D119 意义上的"强特性"。**该族的压制力应由技能「深渊汲食」（6%/回合）承担** —— 这是**有意的分工**，不是缺陷 |
| D428 | **玄铁因 D344 而失去自增益** | ✅ **接受**：D344 已定"从现有技能里改造 2–3 条"，**代价必然落到某几条技能上**。并**批准「沉默之势」继续保留**（玄铁是**默认解锁**的 ★4，砍掉一个会让新手队伍少一个技能）⇒ skill-texts §7.5-2 的两条候选**冲突按"保留沉默之势"解** |

> 📌 **经济口径的第二次自报更正（记录，不影响任何结论）**
> 队员订正：上一条"稳态 **−1.1 点**、降幅来自招募上限 12.3→10.0 次"**是错的** —— 那是因为它 v2 基线里填的"旧一趟金币"是 **18 439**（已作废的决后密度值）。改成 **17 894** 后，招募上限是 **12.3 → 11.9 次**，**没跨过 `÷3` 档**，所以那 −1.1 点不存在。
> **修正后**：稳态/趟 **32.3 / 40.3 → 32.2 / 40.2**，**−0.11 点全部来自 `S9`**；距点满 **0.57 / 0.44 趟**。
> ⇒ **结论全部不变**：树 99 仍 **77.0% / 82.0%**、S9 实测 **1.09 次/趟**（带 0.8–1.4）仍成立。
> ⚠️ 这是**同一个（已作废的密度基线）在本会话里造成的第 3 次**数字漂移 ⇒ 已要求队员**删掉脚本里的 `DECIDED_DENSITY` 常量**（删 + 注释，不是标"已作废"）。

### 第 93 轮

| # | 议题 | 裁决 |
|---|---|---|
| D429 | **「探针」的显形范围** | ✅ **按草案原意：本层剩余陷阱与宝箱全部显形（含未探索区域）**。⚠️ **这是对 D142「迷路是玩法」的一处有意例外** —— 见下 |
| D430 | **复活类道具** | ✅ **删掉**（`item_feather` 不死鸟之羽 / `item_last_candle` 残烛）—— 因为现设计里**角色不会单独"死"**，只有全队共享池的团灭 |
| D431 | **「铜灯」的"已探索锁定"** | ✅ **接受，但限定"本层内"**（走完一层效果消失）—— 它只是把 D142 **本来就保留的"三态迷雾已探索档"**强化一下，不构成变相小地图 |
| D432 | **信息类道具的使用上限** | ✅ **每层每种限用 1 次**（同一层不能连点两盏灯/两根针）—— 保留道具意义但防刷 |

> ⚠️ **D429 的口径记录（这是有意放宽，不是矛盾）**
> **D142** 定的是「不做小地图、不做方向箭头 —— **迷路本身是玩法**」，它的三条缓解手段是：地标 / 已探索档 / **出口不隐藏**。
> **D429** 让「探针」**一次性揭示整层的陷阱与宝箱（含未探索区域）** ⇒ **它把"迷路风险"变成了"可花钱购买的"**。
> ⇒ 这不是推翻了 D142，而是**给支柱 B 开了一个明码标价的出口**：
> - **它不知道出口在哪**（探针只显陷阱与宝箱，**不显出口、不显路径**）⇒ **"走哪条路"仍然要靠玩家自己判断**，"迷路"的核心（**找不到出口**）**未被架空**；
> - 它只解决"**别踩雷、别漏宝箱**"，而 D189/D378 之后**陷阱本来就被架空**（T-1 落地前无致死路径）⇒ 探针的实际价值**当前偏低**，等 **T-1** 落地才会兑现。
> ⇒ **一句话**：**探针给的是"资源雷达"，不是"地图"** —— 与 D142 的核心（找不到出口）不冲突。已按此记录。
>
> ⚠️ **D430 的连带（必须做）**：删掉复活类道具后，**`item_feather`（★4，300 金）腾出了一个稀有度位**。
> ⇒ 两条路（**下一轮定**）：① 该位空着，`items.json` 的 ★4 只剩其它物品；② **补一件机制不同的 ★4 消耗品**（候选方向：**跨层保留的一次性增益** / **团灭保险**）。
> ⇒ 另：D430 之后，**"团灭"是唯一失败形态**，所以任何"保险类"道具都必须明确它**对冲的是 D17/D21/D22 的惩罚**（丢进度 + 丢 50% 物品价值），不能含糊。

### 第 94 轮（lead 裁定：D304 的 Boss 档无法验收 + 不致死保护的显式定义）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D433 | 🔴 **D304 的「Boss ≥10 回合」当前无法验收** | ✅ **登记为"待 Boss 落地后补验"**。队员实测：**96 格的 TTK 全部落在 [2,10]，从未到过 11** ⇒ 那 96 格是**现有 14 只敌人的池子**，而 **4 只 Boss 还没进配表**（D40 全部是新增）。⇒ **Boss 档标 ⏸**，且**明令不得用普通怪的 `rounds === 10` 当替身** |
| D434 | **"不致死保护"的显式定义** | ✅ 采纳建议：**"深渊吞噬"每场战斗只结算 1 次**（这本就是 D159 阈值化的结果）⇒ **不存在"反复触发"**；再加一条保险：**同一大回合内多次满足触发条件也只结算 1 次**。⇒ 由此"1 HP 边角下永远打不死"的语义 bug **在设计层被排除**，不致死保护成为**零成本的兜底** |

> 📐 **D433 的执行口径（可执行判据，写给验收矩阵）**

| 档 | 硬门 | **可执行判据** | 状态 |
|---|---|---|---|
| 普通敌 | 2–4 回合 | 96 格中**普通敌**格子的 TTK 中位落在 [2,4] | ✅ 可验（D108 已过） |
| 精英 | 5–10 回合 | 同上，**精英格子**落在 [5,10] | ✅ 可验 |
| **Boss** | **≥10 回合** | **必须实测 Boss 战的 `rounds ≥ 10`**（≥10 是硬门下限） | ⏸ **待 4 只 Boss 进配表** |

> ⚠️ **D433 暴露的一个通病（值得记）**：我们在 D304 里定下"Boss ≥10"时，**Boss 还完全不存在**（D40 是新增）。
> ⇒ 这是本会话**第 2 次**"验收标准先于被验收对象存在"（第 1 次是 D268 的 TTK 5–10，当时战斗只有 1–2 回合）。
> ⇒ **教训：定硬门时必须同时确认"被验收对象是否已存在"**，否则会留下"永远待验"的档位。

### 第 94 轮（lead 裁定：引用纪律 + 我的第 3 类表述问题）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D435 | 🔴 **引用 GDD 位置的硬规则** | ✅ **一律用「小节号 + 原文措辞」，禁止用行号**。理由：**行号会漂、措辞不会** —— 本轮我在 §0.9 加更正块、在 §11.D 加导航块之后，整体下移了几十行，导致队员报的三个行号里**只有一个是准的**。⇒ 行号仅允许"当轮对话内临时定位"且必须附原句 |
| D436 | **"转述失真"的第三例（记录并定纪律）** | ✅ 记录：我把存档规格原文「key 清单以 10 条为准（T1–T10）」转述成 **"「共 10 个」"** —— 后者**原文里根本没有**。已按原文措辞回填。⇒ **规律：我在压缩引用时会把限定语丢掉**（前两例：`SAVE_SLOT_COUNT=3` 被当成出战位容量；骸骨暴君的防御 0.14 被当成全表上限）。⇒ **纪律：引用必须**逐字**，压缩时保留限定语** |

> 📌 **本会话我被队员纠正的表述问题已累计 16 处，分三类**
> | 类 | 例 | 数量 |
> |---|---|---|
> | **数字用错语境** | 类 2 上界 / R4 旧值 / 骸骨暴君 0.14 当全表上限 / `SAVE_SLOT_COUNT` 当出战位 / 残躯 15% 当全体值 | **多数** |
> | **命名空间混引** | `ui-G` 与 `save-G` / 两套 G 表 | 2 |
> | **转述丢限定语 + 顺序自相矛盾** | "共 10 个" / 四步顺序写成两套 | 3 |
> ⇒ **共同根因：我在"压缩"信息时丢失约束条件。** ⇒ 对策已成型：**引用逐字 + 编号带文件前缀 + 同一动作链只写一处**。

### 第 95 轮（lead 裁定：D425 被自己破掉 ⇒ 改判"占位式"）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D437 | 🔴 **改判 D425：族群授予技改用「占位式」** | ✅ **采纳队员的反驳**。D425 写"挂到精英/虚影/Boss 上、普通敌不挂"——**但我没核对那些档位的技能位是否已满**：**精英 3 位 / 虚影 3 位 / Boss 3–4 位已经排满**（`enemies-draft-v2-independent.md` 的 ":303 / :470" + content-draft 的 12 条 Boss 技）⇒ "挂上去"就是加第 4/5 条 ⇒ **D153 照样破**。<br>⇒ **改为"占位式"**：族群授予技**占既有技能位**（**普通敌第 2 位**；精英/虚影/Boss 占 3–4 位之一），**任何档位都不新增第 N+1 条**。<br>⇒ 好处：**D153 逐档成立（2/3/3–4）**· **82 条预算不膨胀**（它按 **(敌人,技能) 对**计数）· **普通战仍有战术可叠**（32 只普通敌 vs 12 只首领级，原方案会让绝大多数战斗恒 0 层、敌方 HUD 空槽）· **32 只普通敌零返工** |
| D438 | **虚影是否挂族群战术** | ✅ **不挂**。理由：**D415 已定虚影 `family = 'echo'`**（不吃任何族群被动）⇒ 给一只"不属于本图族群"的虚影挂本图族群战术，**读感是错的** |
| D439 | **敌方战术的存储字段** | ✅ **两个字段，都不落盘**：`enemyTacticGainThisRound: Set`（**每战术每轮 +1 层的闸门**）与 `state.enemyTactics: Record<tacticId, {stacks, turnsLeft}>`（**层数真源**）。⇒ 与我方对称：**我方 `state.tactics` 一份 + 敌方 `state.enemyTactics` 一份**（D3 永久单敌 ⇒ 敌方只需一份）。⇒ **战术不落存档**（战斗内状态） |
| D440 | 🔴 **`clear_tactic` 不能走 `effects`** | ✅ **采纳**：把"清除敌方战术"做成**伤害之后的独立结算钩子**，**不塞进 `effects`**。<br>依据：**§10 #12b —— 70 条"伤害 + `effects`"的 `effects` 完全不结算**（`applySupportEffects()` 只在 `!isDamaging` 时调用，`DamageCalculator.ts:117-123`）⇒ 若塞进 `effects`，**D344 的通道① 会静默失效**。<br>⇒ **并入 D272 修复批次的顺序约束**（与"落点家族"同批） |
| D441 | **`enemies-draft-v1.md` 的引用标注** | ✅ **把 GDD 里三处对它的引用标注为"已被 `enemies-draft-v2-independent.md` 取代"**（该文件已确认不在磁盘上）。⇒ **`enemies-draft-v2-independent.md` 与本图 Boss 的 `content-draft-v1.md §②/§③` 是敌人侧的两个真源** |

> 🔴 **D437 是 D425 的自我否决，也是本会话第 3 次"我的裁决被自己/队员用边界条件推翻"**
> 前两次：**D414 推翻 D408**（防御上限 0.20→0.30）、**D424 修正 §11.A.3**（残躯 15%→普通敌 25%）。
> ⇒ **共同点：我在定"挂到哪/上限是多少"这类结构性口径时，没有先把"承载方的容量"数一遍。**
> ⇒ **新纪律：凡"把 X 挂到 Y 上"的裁决，必须同时给出 Y 的剩余容量**（技能位 / 字段位 / 槽位 / 预算）。

### 第 96 轮

| # | 议题 | 裁决 |
|---|---|---|
| D442 | **★4 消耗品位（D430 删掉复活类后空出）** | ✅ **补一件**：效果 = **群体升一级**。⚠️ 语义与数值需细化，见下 |
| D443 | **商店稀有饰品的"上浮概率"** | ✅ **5%** |
| D444 | **实现避坑清单** | ✅ **独立成 GDD 的 §12**（把 16 处纠正 + 11 条必修缺口 + 3 条顺序约束 + 3 类引用纪律集中在一处，**给实现者当第一份要读的东西**） |
| D445 | **最后 5 轮的分法** | ✅ 96–97 敌人两稿对账并入 §11 / 98 出风险清单 / 99 验收矩阵 + 交接文档收口 / **100 全文一致性复查 + 交付** |

### 第 96 轮（lead 裁定：敌人侧的两处收口）

| # | 议题 | 我的裁定与依据 |
|---|---|---|
| D446 | **族群授予技的承载（队员 Q18 的三选一）** | ✅ **取 (a) 维持「占位式」**（= D437 的裁决）。队员的量化我采信：**85% 的战斗应有承载者**（族群技挂该图 8 只普通敌），而 D425 的字面会让**普通战恒 0 层、85% 的战斗 HUD 空槽**，与 **D337 + D328/D342 的 HUD 规格**正面相抵。<br>⚠️ 它另外指出一个**我漏掉的坑**：**虚影带不了族群技** —— 虚影是**四图共用同一个 id**（D290），不可能同时持有 4 条图独占的 `enemy_grp_*` ⇒ 承载者只有 **每图 1 精英 + 1 Boss = 8 个**，不是 12 个。<br>⇒ **结论：零改动**（三份文件已自然对齐：`gdd` / `enemy-tactics` / `enemies-draft-v2-independent` 的 §0.9 都按"1 条主攻技 + 1 条族群授予技"） |
| D447 | **三条新技能的 `apCost`** | ✅ **维持 1**（采纳队员倾向）：**把"40% 的概率"当作第一旋钮**，而不是靠涨价调节（因为涨价只影响"能不能放"，不影响"放了多少收益"） |
| D448 | **`沉默之势` vs `家务全役`** | ✅ **维持 `沉默之势`**（已在 D428 裁定）—— 玄铁是**默认解锁的 ★4**，砍掉它会让新手队伍少一个技能 |

> ⚠️ **D442 待细化（下一轮）**：「**群体升一级**」这个效果需要钉死三件事：
> 1. **语义**：**全队各升 1 级（永久）** 还是 **本场战斗内等级 +1**？（两者强度差一个数量级）
> 2. **价格与限购**：★4 的价位（现有 ★4 参照：`item_feather` 300 / `item_last_candle` 320）+ 是否限购。
> 3. **与 D107（训练场 = 金币换经验）的关系**：若它永久升 1 级，会与训练场**功能重叠** —— 一个是用金币买经验、一个是**道具直接跳级**。需要明确谁是主路径。
> ⇒ **注意 D403「科技点不可洗」体现的"选择有重量"原则**：**用道具跳级会绕过经验曲线**，这条要有意识地下。

> 📌 **第 96 轮的两条队友回传**
> - **`tutorial-draft-v1.md` v6**：T11 补完（52 字，三条边界 = 必须选一个选项／不能走开／选完定死），**提示仍是 11 条**
> - **`skill-texts-v1.md` v6**：`clear_tactic` 已按 **D440** 改为"**伤害结算之后的独立钩子**"（v5 的"新增 `SkillEffect` 变体"**作废**），并钉死 `value` = 清除条数（=1）、`chance` 省略=必定 / `0.4`=概率；两个字段按 **D439** 落位
>   ⚠️ 它还替我校正了一处**引文归属**：关于残躯 25%/15% 的"真源自相矛盾"，**真正的错在 GDD 侧**（旧 §11.A.3 把碎巢母的例外值当成了全体值），**不是两条真源互斥** ⇒ 已在 D424 修好，GDD 现存四处**均已正确**。
>   ⚠️ 并指出**唯一仍留旧文的是 `content-draft-v1.md`**（仍写"统一 15%"）⇒ 由其作者自改（**不越界改他人文件**）。

---

### 11.B 战术系统

> **来源**：`design/drafts/tactics-draft-v1.md` **v6（冻结）** + `design/drafts/enemy-tactics-draft-v1.md`（敌方侧，v2 迭代中）
> **对应裁决**：D200 / D205（作废）/ D211 / D215–D222 / D226 / D231–D232 / D241–D242 / D259–D263 / D277–D280 / D337–D346 / D366

#### 11.B.1 两套系统彻底分离（D205 的继承 + D219 的修订）

| | **技能系统** | **战术系统** |
|---|---|---|
| 成本 | **AP**（含 D5 分档 + 升耗） | 无独立成本；**由授予它的技能承担 AP** |
| 冷却 | **无**（冷却已废除） | **无 CD**（D219：**D200 的"CD 重置"= 持续时间刷新**） |
| 节流 | AP 预算 + 升耗 | **只由 AP 成本承担**（D278；升耗恒定 +1，不累加） |

#### 11.B.2 双方战术总览

| | **我方** | **敌方** |
|---|---|---|
| **属性战术** | 5 个（筋力 / 耐力 / 敏捷 / 幸运 / 灵性）—— **魔力已删**（D226：战斗内完全无效） | **共用同一套 5 个**（D342） |
| **非属性战术** | **机制型 6 个**（由饰品类 3/类 4 授予，D216；⚠️ 原文作 4，勘误见表后） | **族群专属 4 个**（每图 1 个，D342） |
| **来源** | 技能授予（D217）+ 饰品开局授予（D217） | **靠敌人自己的技能授予**（D340） |
| **叠层上限** | **3 层**（D241；"预留 5 层"须**先补承载技能**才可启用，D260） | 同我方（D341），但 **D346 允许差异化**（更短持续 / 更低上限） |
| **持续** | **3 回合**，每次释放**刷新** | 同我方（同上） |
| **HUD** | **每侧 5 条、我方右对齐**（D328；**槽位口径**，仅属性战术 —— 机制型 6 个**不占**此 HUD，见 D332） | **每侧 5 条、敌方左对齐**（D326/D328；同上**槽位口径**，仅属性战术 —— 族群专属 4 个**不占**此 HUD） |
| **非属性战术的位置** | 战斗界面另一处（D332） | 同处（D328 的对称处理） |

> ⚠️ **§11.B.2 勘误（第 101 轮 · 落地实测）**：本表"机制型"**原文作 4 个，实为 6 个** —— §11.A.1 的 12 件饰品中 **类 3 三件 + 类 4 三件 = 6 件**授予机制型战术（`design/drafts/tactics-draft-v1.md` §1.4 列 6 条、§1.5.1 明写「一一对应，零剩余」；已落地的 `src/data/tables/tactics.json` 亦为 6 条：`mec_idle` / `mec_solve` / `mec_ballast` / `mec_plate` / `mec_reflect` / `mec_dissoc`）。**「4」是陈旧计数；「12 件饰品」是更强的证据**，D216 的授予关系不变。

#### 11.B.3 属性战术的数值口径

| 项 | 值 | 依据 |
|---|---|---|
| 每层加成 | **2.3%**（百分比，自动适配等级） | D222 + 队员定标 |
| 第 4 / 5 层 | **×0.67**（前 3 层线性） | 队员定标（满层 ≈10%） |
| 层数上限 | **3** | D241 |
| 持续 | **3 回合**，刷新 | D200/D219 |
| 承载数（每个战术由几条技能授予） | 机动节拍 **6** / 相位增幅 **6** / 结构加固 **5** / 火力校准 **4** / **弹道校准 3（零冗余）** | D263 |

> ⚠️ **"层数上限被承载技能数物理限制"（D260）**：**上限不是策划定的 3，而是 min(3, 承载技能数)**。
> 例：弹道校准只有 3 条技能能授予它 ⇒ **必须让可可 + 琳 + 白雪三人全部上阵**才能叠满 3 层 —— **第 3 格点不亮不是 bug**。
> ⇒ 要抬高某个战术的上限，**必须先增加承载它的技能**，光调上限无效。

#### 11.B.4 敌方侧的三条硬约束

1. **D366：不补读取点，改设计** ⇒ 敌方战术的效果集被限制为：
   ✅ 直接改 **HP**（治疗 / 护盾）｜✅ 改**出手次数**｜✅ 改**敌方自己的 `attack` / `defense`**｜✅ 直接伤害 / `damage_cut`
   ❌ 概率轴（combo / crit / enhance，**敌方是硬编码的**）｜❌ `speed`｜❌ 魔力 / 幸运 / 灵性 / 敏捷
2. **我方有清除手段（D344）**：新增 / 改造 **2–3 条我方技能**——① 造成伤害的同时**按概率清除敌方 1 条战术**；② **不造成伤害但必定清除 1 条**。**不新增技能位**（保持 D55 的"每角色 2 个"）
3. **"拉长战斗 → 敌方层数升高"是正反馈**（队员实测：精英会满层）⇒ **D343** 已把 Boss/精英的强度重算**并入 D108**；**D346** 允许对敌方做差异化

#### 11.B.5 实现口径（三条，避免"两个真源"）

| 项 | 裁决 |
|---|---|
| **授予关系** | **不加字段**，由现有 `effects[].stat` + `enhanced[].stat` **机械推导**（≈6 行）。**例外**：鹿角祝福 / 天使赐福**只有 heal、无任何 `stat`** ⇒ 显式兜底为"授予相位增幅"（否则这两条一条都授不出） | D231 |
| **真源** | **战术侧是真源**（饰品只声明"授予哪个战术"，数值全在战术表） | D228 |
| **承载方式** | **新增队伍级字段 `state.tactics`** / `state.tacticCd`，**不复用 `BattleUnit.buffs`** ⇒ `ally_all` 不需要逐单位挂载 | 队员方案 |

#### 11.B.6 技能侧的改造（D200 / D259 的落地）

- **15 条 `power=0` 纯辅助技**：改为"**授予战术**"，并按 D13 的精神补一个直接效果（**9 条治疗 / 4 条削敌方 `attack` / 2 条双战术**）
- **文案上限放宽到 ≤55 汉字**（D232，否则两条纯授予技的 `description` 会变空串）
- **⚠️ 全部依赖两条前置代码修复**：`power=0` 技**不掷强化**（D259，配表已写 `enhanced` ⇒ 应修代码而非删文案）+ **增减益落点**（D272）

---

### 第 81 轮

| # | 议题 | 裁决 |
|---|---|---|
| D363 | **支线金币占比（29% 是误读，实际 61.9%）** | ✅ **采纳组合 B，降到 41.9%**（保持 ★3 饰品锚点 600 金/10 分钟，把支线金币压到战斗的 72%）⇒ 让"下迷宫"仍然是主要赚钱方式 |
| D364 | **引导的总开关** | ✅ **加一个"关闭全部引导"总开关**（设置里），比逐条关更实用；`firstTimeFlags` 可清空（D294） |
| D365 | **存档重名的比较口径** | ✅ **严格逐字节比较**（连空格都算不同） |
| D366 | **敌方侧缺 4 个读取点** | ✅ **不补读取点，改设计** —— 敌方战术只做"能用现有公式表达"的（如直接改 HP / 出手次数），**不碰概率轴**（敌方 combo/crit/enhance 是硬编码的） |
| D367 | **支线 S9 的阈值** | ✅ **2 500 → 1 700**（随 D350 的宝箱下调同步；验收口径 **0.8–1.4 次/趟**）。⚠️ 这是**第二个因阈值失效而需要修的支线**（第一个是 S3，已删） |

> ⚠️ **D366 的连带（写清楚，避免以后误以为漏了）**：既然不补读取点，**敌方战术的可选效果集被限制为**：
> ✅ 直接改 **HP**（治疗/护盾）｜✅ 改**出手次数**｜✅ 改**敌方自己的 `attack`/`defense`**（这两个轴有读取点）｜✅ 直接伤害/`damage_cut`
> ❌ **不能**做：概率轴（combo/crit/enhance）、`speed`、魔力/幸运/灵性/敏捷相关
> ⇒ **4 个族群专属战术必须落在这个集合内**。已发回给队员重做。

> 📌 **第 81 轮的两处"同类缺陷"（值得记方法论）**
> **S3 与 S9 是同一类问题：阈值/条件在**真实数值**下恒不达成或恒达成**。
> - S3：**"未触发 X"型条件**，在**无支路**的第 1 层**物理上不可能**（分母恒为 0）⇒ 已删
> - S9：阈值 **2 500 高于实际期望** ⇒ 变成**恒不达成**
> ⇒ **教训：任务/成就类条件必须在"真实产出/真实布局"下验一遍可行性**，而不是拍一个"看起来合理"的阈值。
> ⇒ 这条与 **D331（苔痕无档 II 材料）**是同一个方法论：**分级/阈值类设定必须逐项验边界**。

---

## 0.6 D99 难度系统蒙特卡洛（实测，2026-09-17）

**结论先行**：D73 全局难度倍率与 D14 按层缩放**乘算之后没有触发任何一条警戒线**——但不是因为数值调得好，而是因为**战斗本身处于"我方一回合清场"的状态**，0.75×～1.875× 的差异完全被淹没了。

### 方法

- 脚本：`scripts/mc-difficulty.mjs`（esbuild 打包 `scripts/mc-difficulty.src.ts`）＋ `scripts/_phaser-stub.cjs`（Phaser 空壳，否则 `import Phaser` 在 Node 里因 `window` 未定义而崩）。
- 运行的是**真实 `CombatSystem`**，不是复刻的公式：`"H:/node.js/node.exe" scripts/mc-difficulty.mjs`（必须用 Node 24）。
- 每格 **120 场**，共 96 格（4 图 × 6 层 × 4 难度）。
- 敌人数量按现状恒为 **1**（`DungeonScene.startBattle` 强制截断，`count` 折算成单体）；敌人按各图 `enemyPool` 权重抽取。
- 我方：**每单位每大回合出手 1 次**（D9 的裁决），AP 不足则防御；4 名初始角色（rarity 4 → 起始等级 4）。
- 等级来自此前成长曲线蒙特卡洛：四图通关各升 2.00 / 2.75 / 3.64 / 5.88 级，图内按层线性插值。

### 三条警戒线

| 判据 | 含义 |
|---|---|
| TTK ≥ 15 | 击杀拖沓 |
| TTD ≤ 1 | 见面即死 |
| 预期等级胜率 < 60% | 过难 |

### 结果（TTK = 胜局平均回合数；HP×N = 敌方 HP 要放大几倍，胜率才降到 75%）

| 图 | 层 | 等级 | 简单 | 普通 | 困难 | 噩梦(+机制3) |
|---|---|---|---|---|---|---|
| 无根回廊 | 1 | 4 | 1.0 / >200 | 1.0 / 181 | 1.0 / 116 | 1.0 / 116 |
| 无根回廊 | 3 | 4.7 | 1.0 / >200 | 1.0 / 162 | 1.0 / 120 | 1.0 / 74 |
| 无根回廊 | 6 | 5.7 | 1.0 / 162 | 1.0 / 114 | 1.0 / 87 | 1.0 / 54 |
| 苔痕回廊 | 3 | 6.9 | 1.0 / >200 | 1.0 / 178 | 1.0 / 138 | 1.0 / 79 |
| 苔痕回廊 | 6 | 8.3 | 1.0 / 163 | 1.0 / 112 | 1.0 / 85 | 1.1 / 37 |
| 静谧墓所 | 3 | 10 | 1.0 / >200 | 1.0 / 188 | 1.0 / 123 | 1.3 / 30 |
| 静谧墓所 | 6 | 11.8 | 1.0 / 166 | 1.0 / 79 | 1.4 / 28 | 2.1 / **16** |
| 腐朽王座 | 3 | 14.4 | 1.0 / >200 | 1.0 / >200 | 1.0 / 198 | 1.4 / 55 |
| 腐朽王座 | 6 | 17.3 | 1.0 / >200 | 1.1 / 135 | 1.4 / 51 | 2.0 / **22** |

（完整 96 格明细见 `scripts/_mc-difficulty-out.txt`）

汇总：**96 格胜率全部 100%**，TTK 范围 **1.00 – 2.13**，团灭 0 场，1 回合团灭率 0%。
`HP×N` 的中位数 **112**、范围 16–198，另有 **29 格即使放大 200 倍也打不输**。

### 三条结论

1. **D73 / D14 目前是"装饰性"的。** 简单与噩梦的 TTK 差异只在第 3 层之后才出现，且最大只到 1.0 vs 2.1 回合。只要战斗在第 1 回合就结束，敌人属性 ×0.75 还是 ×1.875 对玩家都不可感知。
2. **真正的失衡在"我方单回合输出 ∝ N²"。** `team.attack` 已经是**全队筋力之和**（∝ N），而**每名成员还能各出手一次**（再 ∝ N），一回合总输出因此随队伍人数**平方级**膨胀；敌方却永远是 1 个单位。这是结构性的，不是配表能修的。
3. **噩梦的"机制 3"（敌方属性额外 ×1.5）是四档里唯一有感知的**，也是唯一让深层 TTK 突破 1.5 回合的杠杆（静谧 6 层 2.1 / 腐朽 6 层 2.0）。

### 假设与局限（复核请重点看这里）

- 「每单位每回合出手 1 次」是 **D9 的裁决，代码尚未实现**；现行代码允许同一单位反复出手（受 AP 上限 8 约束），**真实爆发只会更高**，所以本结果偏保守。
- 未计入饰品 / 消耗品 / 锻造（D80–D85 尚未落地），落地后我方只会更强。
- 未计入科技加成（`TECH_BONUS_CACHE` 在 headless 下为空）。
- 「预期等级」是成长曲线蒙特卡洛的插值结果，非实测存档。
- `HP×N` 只是**定位失衡量级**用的探针，不是建议真的把敌人 HP 乘 100。

---

## 1. 游戏概览

### 1.1 一句话

玩家带领一支 4–6 人小队，在程序化生成的迷宫中探索、与敌人回合制交战、收集资源、用科技点解锁更深的迷宫与更强的小队。

### 1.2 核心体验支柱 ✅ 已定（D4）

定为 **3 条**：

| 支柱 | 含义 | 当前实现状态 |
|---|---|---|
| **① AP 战术分配** | 每回合把有限的 AP 分配给「谁出手、用什么技能、出手几次」——战斗决策密度的唯一来源 | ⚠️ **实际失效**：`apCost` 80 条全为 1，AP 退化成"行动次数"（§3.2.1） |
| **② 迷宫探索与风险管理** | 迷雾推进、陷阱与事件、要不要继续下探、生命池还够不够 | ⚠️ **前提不成立**：迷宫 HP 与战斗 HP 原本不连通，风险无法传导（D1 已裁决修正，待实现） |
| **③ 收集与养成** | 招募角色、搭配技能组合、堆科技点解锁更深内容与更强小队 | ✅ 基本成立 |

**明确排除**：「硬核生存压力」（资源稀缺 / 高失败代价 / 谨慎规划）**不是**本作支柱 → 团灭代价可以偏轻，不作为主要设计约束。

此外仍成立的两条实现层特质（不是支柱，但是事实）：**队伍即整体**（伤害与生命按队伍聚合计算）、**数据驱动可扩展**（加内容只改配表）。

### 1.3 目标受众 / 平台 / 时长 ✅ 已定（D16）

| 项 | 定义 |
|---|---|
| **受众定位** | **中度核心**——愿意为一次地牢下探做完整规划（队伍编成、技能搭配、AP 分配、下探深度取舍）的玩家 |
| **单次游玩时长** | **30–60 分钟** ✅ D29（原定 40–90 与实测冲突：一次 6 层下探实测 25–60 分钟，中位 35–40） |
| **平台** | 浏览器（Web），亦可离线单文件运行；逻辑分辨率 720p–4K |
| **商业化** | ❌ 未定义（纯前端单机、无后端，目前无任何商业化设想） |

> ⚠️ 「中度核心」与 D8（团灭不扣金币）存在张力，见 §0.5 第 4 轮下方警示。

### 1.4 内容规模（现状实测）

| 内容 | 数量 |
|---|---|
| 角色 | **40**（★5×9 / ★4×21 / ★3×10；默认解锁 4 人） |
| 技能 | **101**（角色专属 80 + 普通敌方 8 + Boss 专属 13）（改前 88） |
| 敌人 | **44**（32 普通 + 4 精英 + 4 虚影 + 4 Boss）（改前 14） |
| 道具 | **19**（改前 9） |
| 科技 | **21**（合计 99 科技点）（改前 16 / 166 点） |
| 地牢 | **4**（每个最多 6 层） |
| 世界地图节点 | **5**（1 营地 + 4 地牢） |
| 难度档 | **6**（N1–N6） |
| 产出规模 | 生产构建 `index 242.88 kB (gzip 76.51)` + phaser 1.2 MB |

### 1.4 全局难度 ✅ 新增（D73）

提供 **4 档难度**，影响敌人属性与资源获取（详见 §0.5 第 20 轮）。

| 难度 | 敌人属性 | 金币 / 经验 |
|---|---|---|
| 简单 | ×0.75 | ×1.25 |
| 普通 | ×1.0 | ×1.0 |
| 困难 | ×1.25 | ×0.75 |
| 噩梦 | 困难基础上叠加 **4 个可选机制** | 同左 |

> ⚠️ 此前的 GDD 版本记载为"**无难度选择**"，D73 推翻了这一点。难度倍率与 D14 的按层线性缩放是**乘算**关系。

---

## 2. 核心循环

```
主菜单 ──┬── 新游戏 / 继续游戏（3 存档槽）
         └── 设置 / 配表预览 / 数值曲线

世界地图（默认落点 = 营地）──┬── 营地：科技树 / 招募 / 编队 / 背包 / 升级预览 / 设置
                            └── 地牢（4 个，需科技解锁）
                                  └─ 迷宫探索（移动 · 迷雾 · 元素）
                                       └─ 踩到敌人 → 战斗
                                            └─ 胜利：经验/金币/科技点/掉落
                                            └─ 失败：扣 20% 金币 + 清 activeRun
                                       └─ 出口 → 下一层（最多 6 层）或返回营地
                                                     ↑
                                        ┌────────────┘
                                        └──→ 成长循环：等级 / 科技 / 队伍扩张
```

### 2.1 各场景职责

| 场景 | 文件 | 职责 |
|---|---|---|
| BootScene | `scenes/BootScene.ts` | 极简引导（音频 pause/resume） |
| PreloadScene | `scenes/PreloadScene.ts` | 载 8 张配表 + 跨表校验 + 40×3 立绘纹理 |
| MainMenuScene | `scenes/MainMenuScene.ts` | 新游戏/续档/设置/配表预览/数值曲线 |
| WorldMapScene | `scenes/WorldMapScene.ts` | 节点选择、出发确认、存读档 |
| CampScene | `scenes/CampScene.ts` | 科技、招募、编队、背包、升级预览、设置 |
| DungeonScene | `scenes/DungeonScene.ts` | 迷宫移动、三态迷雾、元素触发、层数下探 |
| BattleScene | `scenes/BattleScene.ts` | 回合制战斗、技能栏、结算 |
| UIScene | `scenes/UIScene.ts` | 常驻覆盖层：toast、调试信息（**独立相机**） |

### 2.2 ❌ 缺失的循环要素

- **无通关/结局**：没有"游戏胜利"的定义（§10-3）。
- **无新手引导**：AP、升耗、阶段制、迷雾这些**反直觉的核心机制没有任何教学入口**；玩家只能靠技能 tooltip 自己悟。
- **无目标引导**：没有任务列表 / 下一目标提示。通关第 6 层后没有任何指向。
- **营地「出发探索」按钮**只是回到世界地图（不是直接进地牢）。

---

## 3. 战斗系统

> 核心文件：`systems/CombatSystem.ts`（规则）、`systems/DamageCalculator.ts`（伤害）、`systems/ProbabilitySystem.ts`（概率）

### 3.1 回合结构：双方阶段制 ✅

一个大回合 = **我方阶段 + 敌方阶段**。

| 阶段 | 规则 |
|---|---|
| **我方阶段** | **全部存活我方单位都可行动**，没有"轮到谁"。同一单位只要 AP 够**可多次出手**。玩家点「结束回合」切换；或 AP 耗尽且全员无技能可放时**自动切换**。 |
| **敌方阶段** | 敌方单位按 `enemyUnits` **定义顺序**（不排序）依次自动出手。 |
| 回合边界 | 敌方全部出手完 → `endRound()`：轮次+1、AP 回复、升耗滚动、增益递减、胜负与超时判定。 |

- `currentActor()` 在我方阶段**恒返回 null**（旧"当前行动者"语义已反转，UI 一律用 `state.phase`）。⚠️ 注释仍残留旧描述。
- 🪦 `turnIndex` 语义已废弃；🪦 `speed` 不参与任何顺序计算。

### 3.2 AP 体系 ✅（但配表不可调）

| 项 | 值 | 位置 |
|---|---|---|
| 初始 AP | **3** | ❓硬编码 `CombatSystem.ts:42` |
| 每回合回复 | **+2** | ❓硬编码 `:43` |
| 上限 | **8** | ❓硬编码 `:44` |
| 科技「战术扩容」 | 上限 **+2 → 10** | `tech.json` |
| 科技「奇袭节拍」 | **奇数回合额外 +1** | `tech.json` |

- 回复发生在 `endRound()` 内、`round` 已 +1 **之后**，按 `min(apCap, ap+gain)` 截断（溢出浪费）。
- **AP 池是队伍共享的**；HP 不是（见 §10-1）。

#### 3.2.1 升耗机制（取代旧冷却）✅ 代码完整 / ⚠️ 配表未启用

唯一真源 `CombatSystem.skillCost()`：

```ts
base = skill.apCost ?? 1
if (skill.escalation === 'permanent') cost = base + permStack[skill.id]
else                                  cost = base + (上回合用过该技能 ? 1 : 0)
// 两种模式【互斥】，绝不相加
```

| 模式 | 语义 | 现状 |
|---|---|---|
| `temp`（缺省） | 上回合用过 → +1；**不累加**（连用恒为 base+1） | ✅ 生效 |
| `permanent` | 每次使用 `permStack` +1，**只增不减** | 🔧 **D12 + D18 已裁决启用**：**人工挑 5–8 条最强技能**配 `permanent`，保持稀缺感（候选：花魁杀 `power 200`、死神镰刀 `205` 等）。当前 88 条**无一条配置** |

- 滚动时机：**大回合边界一次**（`endRound()`），`usedLastRound ← usedThisRound` 后清空（原地 `clear`+`add`，不换 Set 引用）；**down 单位也滚动**。
- 同回合内连放同一技能**不自我升耗**（Set 去重）。
- 战斗结束随 `BattleUnit` 销毁而恢复，**不落盘**。
- ⚠️ `apCost` **80 条角色技能全部 = 1**，零分化 → AP 实际退化为"每回合行动次数"，而队伍有 4–6 人 × 2 技能。**这是最影响战斗手感的数值问题。**

**✅ 分档规则（D15 已定）**：

| 技能强度 | `apCost` |
|---|---|
| `power ≤ 60` | **1 AP** |
| `power 61–120` | **2 AP** |
| `power > 120` | **3 AP** |
| `hits ≥ 2` | 按**等效 power**（`power × hits`）折算到上述档位 |

> 配合 D9（每单位每回合限出手 1 次）与 AP 池 3 / +2 / 上限 8：一回合大约只能放 2 次中技或 1 次大招，取舍真实存在。

#### 3.2.2 AP 使用细则 ✅ 已裁决（D9）

| 项 | 裁决 | 现状 → 目标 |
|---|---|---|
| `submitAction` 校验 AP | **补校验**（AP 不足直接拒绝） | ⚠️ 现不校验，把关只在 UI 层 → 修为系统层强制 |
| 同一单位每回合出手次数 | **限 1 次** | 现可无限次（只要 AP 够）→ 修为每单位每回合仅一次 |
| AP=0 时能否防御 / 道具 / 逃跑 | **不允许**（保留现状，不修） | ✅ 保持：AP 归零即**自动让出我方阶段**，玩家无法手动防御 |

> **设计含义**：AP 是唯一资源 —— 没 AP 就什么都做不了，包括防御。这是有意的取舍（与 D4 支柱① 一致）。
> **连带待办**：UI 的自动切阶段逻辑需与"每单位限 1 次"配合；`canAllyAct()` 的判定需重写（现只看"有没有买得起的技能"）。

### 3.3 技能

#### 3.3.1 字段语义

| 字段 | 含义 | 现状 |
|---|---|---|
| `apCost` | AP 消耗，缺省 1 | 角色技能 **80 条全是 1** |
| `escalation` | `'temp' \| 'permanent'` | ❌ 88 条全 undefined |
| `power` | 倍率百分比（100 = 攻击力×1.0） | 0–205，中位数 66；**18 条为 0**（纯辅助） |
| `hits` | 多段攻击段数，缺省 1 | 33 条有值（2 段 32 条 / 3 段 1 条） |
| `target` | `enemy` 47 / `enemy_all` 25 / `ally_all` 11 / `self` 5 | ⚠️ **无单体 `ally`**，单奶在数据结构上无法表达 |
| `effects[]` | `damage` 70 / `buff` 28 / `debuff` 14 / `combo_bonus` 9 / `heal` 9 | |
| `enhanced[]` | 强化（灵性触发）时**追加**的效果 | 80 条角色技能全非空 |
| `type` | `active \| passive` | 🪦 **88 条全 active**，被动体系只有类型没有数据 |
| `cooldown` | 🪦 **@deprecated**，无逻辑读取 | 87 条仍带着 3–8 的旧值 |
| `cost` | 🪦 旧 MP 消耗，无逻辑读取 | 12–30 摆设 |
| `element` | 7 种取值 | 🔧 **D11 已裁决删除**：不做属性克制，字段与 UI 展示一并移除（原为死字段） |

#### 3.3.2 群体技能 —— 已裁决清理（D3）

**一场战斗恒只有 1 个敌方单位**（多处硬编码）。因此 `target:'enemy_all'` 的 **25 条技能全部退化为单体**（代码注释已承认"现已改造为 2 段高伤单体技"）。

**D3 裁决**：**永久单敌，清掉群体概念**。待办：

1. 把 25 条 `enemy_all` 技能改写为**明确的单体技**（保留其"2 段高伤"的实际数值表现）。
2. 从 `SkillTarget` 类型中**删除 `enemy_all`**。
3. **删除 `EnemyDef.count` 字段**及全部折算逻辑（含 `0.35` 系数），敌人只剩"一只"概念。

> `ally_all` 的 11 条**不受影响**——我方有 4–6 个单位，它本来就是真的群体。

#### 3.3.3 ⚠️ 技能描述与实现脱节

- `skill_mes_death_waltz` 描述"对**单体**造成 2 段"，字段 `target:'enemy_all'`。
- `skill_vln_unbreakable` 描述"为全队**分担承受**"，但效果里**没有任何 taunt/redirect 机制**——描述承诺了不存在的功能。
- 多条辅助技描述"回复生命"，实际**只治施法者自己**（见 §10-2）。

### 3.4 伤害与概率公式

#### 3.4.1 队伍聚合（`TeamStats`）

```
队伍属性     = Σ 出战角色六维（+ 科技 stat_bonus）
队伍攻击力   = round(Σ筋力 × strengthToAttack(5))
队伍最大生命 = round(Σ耐力 × enduranceToHP(20))
经验倍率     = 1 + Σ魔力 × magicToExpRate(0.02)
```

**我方每个战斗单位**：`hp = 队伍最大生命`（同一数值各持一份）、`attack = 0`、`defense = 0`。
→ 伤害**只用 `team.attack`**；个体只差 2 个技能。**"角色 = 技能载体"是事实上的设计。**

#### 3.4.2 三概率曲线（`P(x) = P_max × (1 − e^(−x/K))`）

| 轴 | 驱动属性 | 上限 | K |
|---|---|---|---|
| 连击 | Σ敏捷 | 0.6 | 80 |
| 暴击 | Σ幸运 | 0.5 | 80 |
| 强化 | Σ灵性 | 0.5 | 80 |

参考值（`verify.mjs` 逐项断言）：属性总和 10 → 7.1%/5.9%/5.9%；60 → 31.7%/26.4%/26.4%；200 → 55.1%/45.9%/45.9%。

⚠️ 概率在**战斗构造时一次性定为固定值**，战斗内**不再随属性变化**（buff 也无法影响，见 §10-2）。

#### 3.4.3 完整伤害链（`DamageCalculator`）

```
① baseDamage = 队伍攻击力 × (power / 100)          // 无技能时 power = 100
② 连击段数：
     baseHits  = max(1, skill.hits ?? 1)
     P_combo   = min(0.95, 基础连击率 + comboBonus + 技能连击加成)
     while (totalHits < 8 && rng.chance(P_combo)) totalHits += baseHits
     totalHits = min(totalHits, 8)                 // MAX_HITS_PER_ACTION = 8，先累加后截断
③ 逐段独立结算：
     暴击： crit ? (1.5 + 暴击率) : 1              // 暴击率同时进倍率，双重收益
     强化： enhanced 且 rng < min(0.95, 强化率 + 加成)
     浮动： variance = U(0.95, 1.05)
     减伤： defenseRate = clamp(目标defense + (目标防御姿态 ? 0.5 : 0), 0, 0.95)
     damage = max(1, round(baseDamage × critMul × variance × (1 − defenseRate)))
④ 强化命中后追加：damage/heal 追加【不过防御/暴击/浮动】；buff/debuff 施加到目标
⑤ 辅助技（power=0 或非敌方目标）：
     heal = round(队伍攻击力 × value/100 + 队伍灵性 × 0.5)   // ⚠️ 只治【施法者自己】
     buff/debuff 施加到【施法者自己】
```

- 我方防御恒 0 → **减伤只对敌方生效**，双方不对称；我方唯一减伤手段是「防御姿态」的 0.5。
- 敌方伤害走同一套公式，但 `probs` 固定为 `{combo 0.08, crit 0.1, enhance 0}`，**不吃任何队伍概率加成**。

### 3.5 敌人

#### 3.5.1 数据

14 个（Lv1–15）。字段：`id/name/level/hp/attack/defense/speed/skillIds/exp/gold/techPoints/color/scale/drops/count`。
`defense` 是 **0–0.38 的减伤比例**（不是护甲值）。

| 敌人 | Lv | HP | 攻 | 减伤 | exp | 金币 | 科技点 | count |
|---|---|---|---|---|---|---|---|---|
| 黏液怪 | 1 | 42 | 8 | .05 | 14 | 10 | 0 | **3** |
| 穴居蝙蝠 | 1 | 30 | 11 | 0 | 12 | 8 | 0 | **3** |
| 巨鼠 | 2 | 36 | 10 | .02 | 16 | 12 | 0 | 2 |
| 骷髅兵 | 3 | 78 | 18 | .12 | 30 | 20 | 1 | 2 |
| 哥布林战士 | 3 | 68 | 20 | .08 | 28 | 26 | 1 | 2 |
| 食尸鬼 | 4 | 96 | 22 | .10 | 38 | 30 | 1 | 1 |
| 怨灵 | 5 | 88 | 26 | .15 | 46 | 34 | 1 | 1 |
| 石魔像 | 6 | 180 | 30 | .30 | 70 | 55 | 2 | 1 |
| 黯甲骑士 | 7 | 160 | 36 | .24 | 84 | 70 | 2 | 1 |
| 深渊猎犬 | 8 | 140 | 42 | .18 | 92 | 76 | 2 | 1 |
| 虚空祭司 | 9 | 170 | 44 | .20 | 110 | 92 | 3 | 1 |
| 迷宫的守护者 | 10 | 420 | 46 | .28 | 220 | 200 | 4 | 1 |
| 骸骨暴君 | 12 | 560 | 58 | .32 | 330 | 320 | 5 | 1 |
| 深渊之王 | 15 | 900 | 76 | .38 | 600 | 600 | 8 | 1 |

#### 3.5.2 `count` = "N 只合体" ⚠️

| 项 | 折算 |
|---|---|
| `hp` / `maxHp` | **直接 × count** |
| `attack` | `round(atk × (1 + (count−1) × 0.35))` ← ❓**0.35 无出处** |
| `name` | 加 ` ×N` 后缀 |
| `exp` / `gold` / `techPoints` / `drops` | **一律不变**（奖励不随数量放大） |

#### 3.5.3 ❌ 敌人相关缺失

- **无 `isBoss` / `isElite` 字段**（实测 0/14）。最终 Boss「深渊之王」只是 `dungeon_abyss` 敌人池里 **weight=2 的随机杂兵** → 约每 53 次遇敌撞见一次，也可以一次都不遇到就通关。
- **敌人属性完全不随层数成长**：`rollEnemy` 只按 `enemyPool` 加权抽，不看难度/层数。下到第 6 层遇到的**还是 Lv1–2 的黏液怪**。❓"越深越难"目前只体现为"路更长、怪更多"。

**✅ 已裁决的修正（D7 + D14）**：**数值缩放 + 按层切池 两者都做**。

| 随层增长 | 公式（第 N 层） |
|---|---|
| HP | `×(1 + 0.5 × (N−1))` → N6 时 **×3.5** |
| 攻击 | `×(1 + 0.25 × (N−1))` → N6 时 **×2.25** |
| 防御 | `×(1 + 0.3 × (N−1))` → N6 时 **×2.5** |
| 奖励 | 小幅上浮（具体系数待定） |

- 系数**写在 `mazeDifficulty.ts`** 作为可调项（与尺寸/拓扑同源，改它不需要跑 gen-tables）。
- 同时按层切换敌人池：深层只出高阶兵，与数值缩放叠加。
- ⚠️ 两者叠加会**同时抬难度**，调平衡时需注意变量翻倍（D7 选项里已提示）。
- 敌人池权重不归一（tomb Σ89 / abyss Σ106），策划无法用百分比直觉改。
- 🪦 `speed` 是死字段；`MapNodeType` 预留的 `boss` 节点类型从未使用。

### 3.6 胜负与结算

| 结果 | 条件 | 后果 |
|---|---|---|
| `victory` | 敌人全灭 | 立即结算奖励；经验只发**出战成员** |
| `defeat` | **我方全部 down** | **✅ D17 + D21 + D22 定稿**：<br>① 本图进度清除——`maxFloor` **归零**、`activeRun` 清空，但 **`cleared` 保留**作为成就<br>② 背包物品损失——随机挑物品直到累计价值达背包总价 **50%**（**材料类计入**）<br>③ 送回营地（修正原文案"送回营地/实回世界地图"的矛盾）<br>原设计（扣金币 20%）**已废弃** |
| `fled` | 主动逃跑成功，或 `round > maxRounds(60)` 超时 | 清 `activeRun` |

- 逃跑成功率 `min(0.95, 0.45 + 已尝试次数 × 0.15)` → **3 次失败后即 90%+**，任何非 Boss 战都可无成本脱战。
- ⚠️ 失败文案写「被送回**营地**」，代码实际回**世界地图**；文案说"探索进度已清空"，但 `maxFloor` / `cleared` 实际**保留**。
- ❓ 角色**永不死亡**（`down` 只存在于本场 BattleUnit，战斗结束即销毁）。

### 3.7 自动战斗

我方 AI 优先级：治疗技（队伍血 <45%）→ `enemy_all` 技 → power 最高技 → 兜底防御；并先过滤"买得起"的技能以防死循环。
敌方 AI：能放技能则 65% 概率放（选 power 最高）；`self` 治疗技在自身血 <40% 时优先；否则普攻。目标恒为**血量最低的我方单位**。

### 3.8 战斗 UI（详见 §8.6）

技能栏显示 `apCost` 动态值；升耗时数字转金色并带 `+N` 角标；不可用时置灰；买不起时数字转红。**已取消选敌交互**（敌方单体技自动锁定唯一敌人）。

---

## 4. 角色与成长

### 4.1 六维属性

筋力 / 耐力 / 敏捷 / 幸运 / 魔力 / 灵性。每项 = `{ potential: A–F, value: number }`。

| 属性 | 作用 |
|---|---|
| 筋力 | → 队伍攻击力（×5 线性） |
| 耐力 | → 队伍最大生命（×20 线性） |
| 敏捷 | → 连击率（指数，K=80） |
| 幸运 | → 暴击率（指数，K=80） |
| 灵性 | → 强化率（指数，K=80） |
| 魔力 | → 经验倍率（+2%/点 线性） |

### 4.2 潜力 A–F（只影响**升级成长**，不影响初始值）

| 潜力 | 倍率 | 每级实际增量 |
|---|---|---|
| A | 2.0 | 3~5 |
| B | 1.6 | 2~4 |
| C | 1.3 | 2~4 |
| D | 1.1 | 1~3 |
| E | 0.9 | 1~3 |
| F | 0.7 | 0~2 |

**生成约束**：`rarityFactor = max(0.2, (rarity+1)/6)`；A 抽 `int(0, round(2×rf))`（上限 2），B 抽 `int(1, min(3, round(3×rf)))`（上限 3），其余从 `['C','C','D','D','E','F']` 均权抽。
→ ★3 至多 1 个 A；★4 至多 2 个 A；**只有 ★5 能完全触达上限**；B 恒 ≥1（不可能出现全 C-F 白板）。

### 4.3 生成规则

```
初始六维总和 = 60 + max(0, rarity − 1) × 3      // ★3=66 / ★4=69 / ★5=72
每项 ≥ 1，用 Rng.distribute 精确凑总和（同种子可复现）
起始等级     = clamp(rarity, 1, 9)              // ★3→Lv3 / ★4→Lv4 / ★5→Lv5
技能栏       = 恰好 2 个，来自角色模板的专属主动技
```

⚠️ 初始总和只差 6 点（±4.5%），而潜力 A vs F 差 **2.86 倍**成长 → **稀有度的价值几乎全押在随机潜力上**，★3 的成长可能优于 ★5。

### 4.4 升级

```
需求经验： expForLevel(L) = round(20 × L^1.6)      // L1→2 需 20；L10 需 796；L20 需 2570
升级成长： 每项 Δ = max(0, round(2 × 潜力倍率) + U(−1,1))
```

- 经验**只发给出战成员**，备战栏角色永远 0 经验 → 换人即等级断层。**✅ D28 已裁决：维持不改**（有意强化"出战才有成长"的取舍）；⚠️ 与 D19（升级变慢）叠加后换人成本更高。
- ⚠️ **经验倍率被计算两次**：先在战斗结算乘 `team.expMultiplier`，再在 `gainExp` 乘个体 `expMultiplierFor(card)`。
  🔧 **D19 已裁决**：**只保留个体倍率**（`expMultiplierFor(card)`），**去掉队伍层**。
  → 连带影响：`team.expMultiplier`（= `1 + Σ魔力 × 0.02`）变为**死数据**；**魔力成为纯个体属性**（不再有队伍级作用）。
  → ⚠️ 升级节奏会**明显变慢**（原先是两级放大），需重算 D16 下"单次 40–90 分钟能升几级"。
- ❌ **无手动加点**（`LevelUpPanel` 只是成长预览）。
- 🪦 `player.level` / `player.exp` 从未读写；`levelUpTechPointBonus` 无人读取（升级不给科技点）。

### 4.5 招募

| 项 | 规则 |
|---|---|
| 解锁 | 科技「招募许可 I」（3 点） |
| 花费 | 金币 平价 **5 000**（D67 取消递增，`recruitCostGrowth` = 1）+ 科技点 **2**（改前：`120 × 1.35^n`，n = 本次会话已招募次数） |
| 金币序列 | **平价，不再递增**（5 000 → 5 000 → …）；改前：120 → 162 → 219 → 295 → 399 → 538 → 727 → 981 → 1325 … |
| 抽取 | 按 `rarityWeights` 加权；★≥4 模板权重 ×(1+rarityBonus)；`unlockedByDefault` 剧情角色 **×0.35** |
| 重复 | **允许重复角色**（id 带时间戳+随机数） |

⚠️ `rarityWeights` 里 `{1:40, 2:30, 3:18, 4:9, 5:3}` 的 **★1/★2 权重要 70 是死权重**（无 ★1/★2 模板）。实际概率：★3 = 60% / ★4 = 30% / ★5 = 10%（再乘剧情惩罚与加成）。**按表解释招募概率会全错。**

### 4.6 队伍

- 上限 `min(6, unlockedPartySlots)`；初始 4；科技「队伍扩编 I/II」各 +1。
- 至少保留 1 人；`movePartyMember` 只改展示顺序（不影响数值）。
- 迷宫内可换人（按比例缩放生命池）。
- 开局成员 = `unlockedByDefault` 按稀有度降序取 4（char_01–04）。

### 4.5 成长曲线数值核算 ✅（蒙特卡洛，2000 次/组）

> 2026-09-17 用**真实代码**跑的模拟：真调 `MazeGenerator.generate()`、`CharacterFactory.gainExp` / `applyLevelUp`、真实 grid 上 BFS 寻路模拟探索。**不是解析估算。**

**代码事实澄清**（顺带修正两处易错认知）：

- 经验**只发出战成员**（`gameState.partyCards()` = `state.party`，`roster` 是另一个集合），且**每人拿全额、不分摊**。
- 宝箱 / 事件 / 陷阱 **本来就不给经验**（只给金币/道具/科技点），所以"不计入"不是保守假设。
- 每场战斗**恒 1 个敌人**（`DungeonScene.ts:598` 强制 `slice(0,1)`）。

**D19 的实测影响**（一次完整 6 层下探，探索型，每成员升级数）：

| 地牢 | ★3 | ★4 | ★5 | (旧两级倍率 · ★4) |
|---|---|---|---|---|
| 无根回廊 | 1.74 | **1.05** | **0.89** | 2.37 |
| 苔痕回廊 | 3.28 | 2.58 | 2.00 | 5.06 |
| 静谧墓所 | 6.02 | 5.16 | 4.38 | 10.68 |
| 腐朽王座 | 9.49 | 8.56 | 7.64 | 19.83 |

- **D19 砍掉了 49–57%（4 人队）/ 56–63%（6 人队）。**
- **D19 修对了方向**：旧方案下"多带 2 人 = Σ魔力更大 = 白拿 +26% 经验"是 bug 级激励（6 人队在腐朽王座一次升 **26 级**）。D19 之后**人数不再是经验杠杆**（每人全额、无队伍倍率）。
- ⚠️ **真问题在第一个地牢**：无根回廊 ★4 只升 1.05 级、★5 **0.89 级**（**10% 的人零成长**）；速通打法下 ★5 有 **46% 概率零成长**。而腐朽王座 8.56 级偏快 —— 是**前后期不均**，不是整体太慢。
- ⚠️ **经验高度压在深层**：N5+N6 占总量的 **75–79%** →「打到第 3 层就撤」只能拿到约 25%，是比 D19 更严重的风险。

**⚠️ D16 需要修正**：实测一次 6 层下探约 **25–60 分钟**（中位 35–40）；**速通只需 16–20 分钟**。即"40–90 分钟"只在「探索型 + 慢节奏 + 打满 6 层」时成立。

**✅ `expExponent` 补扫结果（2000 次/组 × 4 角色，真调真实代码）**：

| `expExponent` | 无根★4 | 无根★5 | 苔痕★4 | 静谧★4 | **腐朽★4** | ★5 零成长率 |
|---|---|---|---|---|---|---|
| **1.6（现状）** | 1.11 | 1.00 | 2.87 | 5.33 | **8.59** | ~0% |
| 1.7 | 1.00 | 0.97 | 2.42 | 4.70 | 7.51 | 2.6% |
| 1.75 | 1.00 | 0.88 | 2.20 | 4.40 | 7.06 | 12.5% |
| 1.8 | 1.00 | 0.70 | 2.06 | 4.15 | **6.64**（未达标） | 30.2% |
| 1.85 | 1.00 | 0.44 | 2.01 | 3.89 | 6.23 | 56.4% |
| **1.9** | 1.00 | 0.19 | 1.97 | 3.64 | **5.88 ✅** | **81.0%** ❌ |

- **1.8 压不到 5–6 级目标**（只到 6.64）；**1.9 才够**（5.88，P10=5 / P90=7）。
- **但 1.9 单独会崩掉前期**（★5 均值 0.19、81% 零成长）→ **必须搭配方案 A**。
- ⚠️ 无根回廊 ★4 的均值在 1.7–1.9 **全程恒为 1.00**（总经验被卡在"卡4→5 所需"这一个区间里），所以**别用 ★4 均值判断前期，要看 ★5**。

**✅ 推荐组合：`expExponent = 1.9` + 方案 A**

| 方案（均在 1.9 下） | 无根★4 | 苔痕★4 | 静谧★4 | **腐朽★4** | 需改代码 |
|---|---|---|---|---|---|
| 仅曲线 | 1.00 | 1.97 | 3.64 | 5.88 | — |
| **+ A：无根 `enemyDensity` 0.035→0.060，黏液怪/蝙蝠/巨鼠 exp 14/12/16→20/17/23** | **2.00** | 2.01 | 3.64 | **5.88（不变）** | ❌ 纯配表 |
| + A 变体（density 0.050） | 1.84 | 2.01 | 3.64 | 5.88 | ❌ |
| + B：楼层奖励 `round(4N²)` | 1.98 | 2.69 | 4.18 | **6.23（削掉成果）** | ✅ |

- **A 是外科手术式的**：只影响第一张图，静谧/腐朽**一个数字都不变**；全档位零成长率归 0。
- **不推荐 B**：全局抬升会把腐朽从 5.88 推回 6.23，**等于削掉 D32 的成果**，且要写代码。
- ⚠️ **口径提示**：补扫实测现状（1.6）★5 零成长率为 **~0%**，与本文档早前记录的 10% 不符 —— 那个 10% 是从上一份报告的 `p10=0` 推断的，而本次是直接统计。两次跑分在腐朽王座★4 上一致（8.59 vs 8.56），模型可信，**是"零成长率"指标的口径差异**，不影响 1.9 + A 的结论。
- ⚠️ **待决副作用**：1.9 + A 后四图成长为 **2.00 / 2.01 / 3.64 / 5.88** —— 第一张与第二张图**几乎一样**，"越往后成长越快"的台阶在前期丢了一格（见 C3-29c）。

---

## 5. 经济

### 5.1 货币与资源

| 资源 | 获取 | 消耗 |
|---|---|---|
| **金币** | 新游戏 120；战斗掉落；宝箱；事件（卖材料）；钱袋道具 | 招募（平价 **5 000**（D67 取消递增，`recruitCostGrowth` = 1）；改前：120×1.35ⁿ）；事件商人 80；神龛 50；**团灭扣 20%** |
| **科技点** | 战斗掉落；宝箱（10%）；**首通奖励**（**8/9/10/11，合计 38**；改前 3/3/5/10 = 21）；研究手记 +3；神龛（50 金币→2 点） | 科技树 **99 点**（改前 166 点）；招募每次 2 点 |
| **经验** | 仅战斗结算 | 升级 |

### 5.2 掉落

每个敌人**逐条独立** `rng.chance(drop.chance)`，命中后 `rng.int(min,max)`。
⚠️ 掉落完全不随 `count` 放大。

### 5.3 ❌ 经济缺失

- **没有任何产出/消耗预算表**：没有"玩家在 LvN 应该有多少金币/科技点"的目标值。
- **科技点缺口无人验证**：科技树需 **99 点**（改前 166 点），而四图首通只给 **38 点**（改前 21 点）→ 缺口 **≈62%**（改前 87%）必须靠反复击杀（原按每敌 1–8 点估约 60–80 次精英击杀，**该估值未随总价下调重算**）。这个等式从未被验证。
  🔧 **D20 已裁决**：**三管齐下** —— ① **下调科技树总价** ② **提高科技点产出** ③ **产出与难度挂钩**（深层给得更多）。具体数值待 Round 6 定（需与 D14 的敌人奖励上浮合并计算）。
- **升级曲线 vs 推荐等级无对照表**：L1→L10 需 ≈2702 exp；`dungeon_moss` 最强杂兵 28–30 exp → 约需 **90 次遇敌**，而推荐等级只写 Lv6。三者之间没有换算关系。
- **永久属性道具产出无上限**：`item_elixir`（★5 永久 +2 属性）可从骸骨暴君(0.3)/深渊之王(0.6)反复刷取。

### 5.4 道具（现役 **19** 种）

| 道具 | ★ | kind | value | 营地 | 迷宫 | 堆叠 | 售价 | 限购 |
|---|---|---|---|---|---|---|---|---|
| 治疗药水（小） | 1 | heal | **8%（maxHp）** | ✅ | ✅ | 99 | 30 | — |
| 治疗药水（大） | 2 | heal | **20%（maxHp）** | ✅ | ✅ | 99 | 80 | — |
| 续命散 | 1 | heal | 12% + 6%×2（maxHp） | ✅ | ✅ | 99 | 70 | — |
| 铜灯 | 1 | vision | 本层视野 +2 格 | ✅ | ✅ | 20 | 60 | — |
| 探针 | 2 | detect | 本层陷阱 / 宝箱显形 | ❌ | ✅ | 20 | 90 | — |
| 净毒粉 | 2 | cure | 解全部减益 + 8%（maxHp） | ✅ | ✅ | 50 | 90 | — |
| 铁誓符 | 2 | shield | 护盾 25%（maxHp） | ✅ | ✅ | 50 | 120 | — |
| 烟丸 | 2 | escape | 必定脱战（Boss / 精英无效） | ✅ | ✅ | 30 | 150 | — |
| 灰纱 | 3 | mitigation | 受伤 −35% · 3 回合 | ✅ | ✅ | 30 | 150 | — |
| 归途石 | 1 | teleport | 立即回营地（进度不回退） | ✅ | ✅ | 10 | 55 | **1 / 趟** |
| 残烛 | 4 | revive | 战斗内复活 + 40%（maxHp） | ✅ | ✅ | 20 | 320 | — |
| 升阶谕令 | 4 | level_up | 全队 +1 级（永久） | ✅ | ❌ | 20 | 600 | **1 / 趟** |
| 不死鸟之羽 | 4 | revive | 营地复活 + 50%（maxHp） | ✅ | ❌ | 20 | 300 | — |
| 古代秘药 | 5 | stat | 永久 +2 随机属性 | ✅ | ❌ | 20 | 800 | — |
| 研究手记 | 3 | tech_point | +3 科技点 | ✅ | ❌ | 99 | 150 | — |
| 钱袋 | 2 | gold | +120 金币 | ✅ | ✅ | 99 | 100 | — |
| 魔铁矿 | 1 | material | 档 I 锻造主料 | — | — | 999 | 25 | — |
| 深渊结晶 | 3 | material | 档 II 锻造主料 | — | — | 999 | 120 | — |
| 魔核 | 5 | material | 档 III 锻造主料 | — | — | 999 | 500 | — |

> 📌 **口径（D208）**：两瓶药水已是 **% maxHp** 口径 —— 小 **8%**、大 **20%**（**不再是固定点数**；故 `value` 由旧表的 80 / 260 改为 8 / 20）。
> 📌 **限购口径**：`归途石` / `升阶谕令` 的 `perRunPurchaseLimit = 1`（**每趟限购 1**）。升阶谕令的 **D450 原文作"每店"**，落地裁决为"**每趟**"，**以配表为准**。
> 📌 本表 = 现役 **19 条**（真源 `src/data/tables/items.json`；10 种新增消耗品的价格推导见 `design/drafts/consumables-draft-v1.md` v3）。


- 售价一律**按 price 的 5 折**。
- ⚠️ **不死鸟之羽在任何场景都用不了**：配表标 `usableInDungeon=false` + `use()` 对 revive 直接拒绝"请在战斗中使用" + 战斗道具菜单只列 `usableIn('dungeon')` → 三重封死。且战斗调用时不传 `target`，**"复活指定队友"的实现根本不存在**。
- ⚠️ **药水在营地里喝不了**：配表标 `usableInCamp=true`，但 heal 分支要求传入生命池，而营地打开背包不传 → 与配表声明矛盾。
- ⚠️ 战斗中使用道具**不消耗 AP / 行动、无次数限制** → AP 经济可被道具绕过。
- ✅ **（改前作 ❌）**3 个 material 描述写"锻造材料" —— **锻造系统已就位**：配方表 = `accessories.json` 的 **12 件 + 12 套配方**（**开局全知**，D282）、锻造面板已实现（`src/ui/ForgePanel.ts`）、锻造费 = 材料 + `price × 0.6`（D167；§11.A.1）。
- ✅ **（改前作 ❌）**装备槽**已实现** —— 挂**出战位**的**双槽**（饰品 + 消耗品），容器 `partyEquip` 与 `party` **等长、下标一一对应**（`src/types/save.ts`），**换人不丢**（D292 / D313）。
- ❌ **饰品的数值目前没有任何读取点** ⇒ **能锻造、能装备，但装上什么都不发生**（属 §13.1 **R4**"配表写了但代码读不到"，见 **R4 第 14 类**）：
  - **类 1（属性加点）**：队伍属性派生 `TeamStats.teamDerived()` **完全不吃 `partyEquip`** ⇒ 类 1 三件（"筋力 +2 / 耐力 +1"之类）零效果 —— **已在修**（已派单）。
  - **类 2（概率轴）**：`probability: { axis, pp }` 的连击 / 暴击 / 强化 pp **无人读** —— **已在修**（已派单）。
  - **类 3 / 类 4（机制型战术）**：`mec_*` 六条未实现 ⇒ **归批次 2**（§14 批次 2 原文列有"饰品防御类 / R2 相位反射片"）。

---

## 6. 科技树

21 个节点，合计 **99 科技点**（改前：16 节点 / 166 点），**全部单次不可重复、不可洗点**。

> 🔧 **D20 + D23 已裁决调整（总价与首通数已按 D246 / D297 落地）**：
> - **目标节奏：首次全清（4 图）即可点满约 80%** —— 此 **80 是"达成率目标"（75–85%），不是树的总价**；实测首清 **76.3 / 99 = 77.1%**
> - 总价：**166 → 99 点（21 节点）**（改前"约 80 点"即把上述达成率目标误当总价）
> - 四图首通合计：**21 → 38 点**（D246 / D255；`dungeons.json` = 8 / 9 / 10 / 11）
> - 其余靠战斗掉落，`techPoints` **随层缩放**（与 D14 奖励上浮**合并计算**，避免双重放大）

| 效果类型 | 数量 | 生效方式 |
|---|---|---|
| `unlock_party_slot` | 2 | 出战位 +1（第 5 / 第 6 位） |
| `unlock_dungeon` | 3 | 解锁苔痕回廊 / 静谧墓所 / 腐朽王座 |
| `stat_bonus` | **11** | **全队永久**六维加成（改前 8；含新增 4 条「II」级 + `远征装备`） |
| `unlock_recruit` | 1 | 开放招募 |
| `recruit_rarity_bonus` | 1 | ★4/★5 权重翻倍 |
| `ap_cap_bonus` | 1 | AP 上限 +2 |
| `ap_regen_odd` | **2** | 奇数回合 AP +1（「奇袭节拍」+「混战节拍」；改前 1） |
| **合计** | **21** | 与「21 个节点」自洽（改前表合计 17 ≠ 16，本次一并校正） |

**关键节点**（成本取自 `tech.json` 实测）：`队伍扩编 I`(2) → `队伍扩编 II`(6)；`战术扩容`(5) → `奇袭节拍`(7) → `混战节拍`(6，可重复 ×2)；地牢链 `深层探索`(4) → `深渊入口`(9，需 +筋力科技) → `深渊核心`(14，需 +远征装备)；另有 `远征装备`(13，全队 +10 筋力 / +10 耐力)。

⚠️ **定价失衡（改前口径）**：**（改前）**筋力/耐力科技 **6 点**（经 ×5 / ×20 放大为 +30 攻 / +200 HP），而敏捷/幸运/魔力/灵性 **7 点**（走指数曲线，80 点仅达上限 63%）⇒ **筋力/耐力更便宜又更强，最优解永远是最后买敏捷系。定价方向反了。**
⇒ **已由第 101 轮的定价反转修正**（`tech.json` 为唯一真源；节点 16 → 21、总价 166 → 99）。现值对比如下 —— **线性维现在恒比饱和维贵**：

| 维 | 单节点成本 | 每点属性成本 |
|---|---|---|
| **线性维**（筋力 / 耐力，+6 → ×5 / ×20） | **5 点 / +6** | **≈0.83 点/属性**；最便宜的线性项 = `远征装备` **13 点 / +20 = 0.65** |
| **饱和维**（敏捷 / 幸运 / 魔力 / 灵性，走指数曲线） | **2 点 / +6**（II 级 **3 点 / +10**） | **≈0.33 点/属性**（四系 I 级 = **最贵的饱和项**；II 级 **0.30** 更低） |

**结论（重判）**：**"线性又便宜又强"的失衡已解除** —— **最便宜的线性项（0.65）仍是最贵的饱和项（0.33）的约 2 倍**。同时 **"最优解永远是最后买饱和系"也一并失效**：饱和维走指数曲线、边际递减（Σ=80 处每点仅 **+0.23~0.28%**，Σ=200 处跌到 **+0.05~0.06%**），故**中后期是否补饱和维取决于当前 Σ 的边际收益**，不再是一刀切结论。⇒ 该历史结论由 D297 的 99 点定价落地取代。

⚠️ `tech_recruit_2`（**现 2 点/层、可重复 ×2**；改前写"10 点"）把 ★4/★5 权重 +1.0 —— 属辅助向，与同价位的属性科技不构成直接竞争。

🪦 `repeatable` / `maxRank` 机制完整实现（含 `cost × 1.6^rank` 复购），但 21 条**没有一条配置** → 分档成长路径空置，UI 分支永不可达。

---

## 7. 地牢与迷宫

### 7.1 难度阶梯 N1–N6 ✅（唯一真源）

**难度 = 层数**（第 N 层 = N_N）。真源 `src/config/mazeDifficulty.ts`。

| | 尺寸 | 通关步数 | 支路数 | 支路长度 |
|---|---|---|---|---|
| N1 | 15×15 | 20 | **0** | — |
| N2 | 19×19 | 32 | 2 | 4~6 |
| N3 | 23×23 | 48 | 4 | 6~10 |
| N4 | 27×27 | 58 | 6 | 8~14 |
| N5 | 31×31 | 74 | 8 | 10~18 |
| N6 | 35×35 | 90 | 10 | 12~24 |

- 长度单位是**玩家移动格数**（必须偶数）；内部房间格数 = `步数/2 + 1`。
- N1 的 `branchCount = 0` → 整图只有一条主干，**完全没有支路**。
- 每档 +4 尺寸；⚠️ 通关步数增量 12/16/**10**/16/16 —— N3→N4 是唯一的节奏凹点。

### 7.2 生成算法 ✅

```
外圈设为「虚空」(2)
  → 深度受限随机 DFS 找恰好 mainPathSteps 玩家步的简单路径（出口固定末端）
  → 按 branchCount 开凿死胡同（起点必须"有地可挖 且 非死胡同"，保证不产生捷径、不吞掉已有死胡同）
  → 放置元素（宝箱/存档/事件/陷阱按配置，敌人按密度，上限 floorTiles×0.12）
  → validate 自检（连通 / 步数 / 支路数 / 元素合法）
```

同种子可复现（⚠️ 例外：`teleportRandom` 用了 `Math.random()`，破坏可复现承诺）。

### 7.3 探索与迷雾 ✅

- 移动：4 向，键盘 ↑↓←→/WASD + 屏幕十字键（透明度 0.15–1）。
- **三态迷雾**：未探索(0) / 已探索(1) / 当前可见(2)。
  - `SIGHT_RADIUS = 5`，**曼哈顿距离** ≤5；距离 >2 时做 Bresenham 视线检查，撞墙即不可见。
  - 旧可见降级为已探索；已探索的通道/墙 alpha 0.3。
- 相机：不夹取、队伍恒居中、迷宫外纯黑；缩放按难度自适应（见 §8.6）。

### 7.4 元素类型与触发

| 类型 | 触发结果 |
|---|---|
| `start` | 🪦 无功能（`consumed=true` 且返回 null） |
| `exit` | 不消耗，弹「进下一层 / 返回营地」 |
| `enemy` | 消耗 → 进战斗（**恒 1 个敌方单位**） |
| `treasure` | 消耗 → `gold`(45%) / `item`(45%) / `tech`(10%) —— ❓**权重硬编码在代码里** |
| `trap` | `damage`(50%：`round(maxHp×value/100)+value`，value∈[8,20]) / `slow`(25%) / `teleport`(25%) |
| `save` | 队伍 HP 池回满 + 立即存档 |
| `event` | 泉水 / 商人 / 神龛 / 埋伏 4 种，效果**硬编码在场景里**（无 events.json） |

⚠️ **「迟缓」陷阱实际无任何效果**（`slowTurns` 只自减无人读取）→ 25% 概率白给。

### 7.5 层数与通关（已裁决 D2）

**通关定义**：每地牢**第 6 层设固定 Boss**（不可回避），**击败 Boss 即该地牢通关**。
**不做全局结局**——4 图全清后不触发结束演出，玩家可继续刷。

待办：
1. 敌人表新增 **Boss 标记字段**（如 `isBoss`），并把 3 个高 HP 敌人（迷宫的守护者 / 骸骨暴君 / 深渊之王）显式标为 Boss；**普通遇敌池必须排除 Boss**（当前最终 Boss 是 weight=2 的随机杂兵）。
2. 第 6 层的 `exit` 改为「Boss 房」：先击败 Boss 才能通关；Boss 未击败时不可离开。
3. `dungeonProgress.cleared` 由「击败 Boss」触发，而**不再是"清完第 6 层出口选返回营地"**。
4. 非 Boss 图的 `cleared` 语义随之统一（每图应有自己的 Boss）。

- 出口在非第 6 层 → 可进下一层；第 6 层 → 击败 Boss 后通关并返回营地。
- 下探规则：保留生命池（**不回满**）、敌人密度 `×(1+(层−1)×0.12)`、层数 +1、记录 `maxFloor`。
- **每层元素全部重新生成**（宝箱/存档/事件/陷阱重生），只保留 HP 池。
- ⚠️ **宝箱金币按 `config.floor` 计算，而 4 个地牢的 `floor` 全是 1** → 首层与后续层金币口径不一致，且越深越不值。

### 7.6 世界地图

5 个节点：1 营地（默认落点）+ 4 地牢。坐标手工排布，**无图论连通性**（纯视觉）。
⚠️ dungeon 节点的 `unlocked` 字段**从不被读取**（改查科技解锁）→ 改表无效。
🪦 `event` / `shop` / `boss` 三种节点类型从未使用。

### 7.7 地牢配置

| 地牢 | 敌人密度 | 宝箱 | 陷阱 | 事件 | 存档 | 推荐 Lv | 首通科技点 | 解锁科技 |
|---|---|---|---|---|---|---|---|---|
| 无根回廊 | 0.035 | 3 | 1 | 1 | 1 | 1 | 3 | 初始 |
| 苔痕回廊 | 0.045 | 4 | 3 | 2 | 1 | 3 | 3 | 深层探索 |
| 静谧墓所 | 0.050 | 5 | 4 | 2 | 2 | 6 | 5 | 深渊入口 |
| 腐朽王座 | 0.055 | 6 | 5 | 3 | 2 | 10 | 10 | 深渊核心 |

---

## 8. 美术 / UI / UX 规格

> 详细规格见 `docs/art/ui-style-spec.md`（16.9 KB）与 `design/ux/battle-ap-ux-spec.md`（16.3 KB）。

### 8.1 视觉铁律

- **明日方舟式扁平科幻**；唯一主强调色为亮黄 `0xFFD400`。
- **切角代替圆角**；**零渐变、零发光**。
- 全程序化绘制，无外部美术资源（立绘除外）。

### 8.2 配色（`Palette`，`src/config/constants.ts`）

| 名称 | 值 | 用途 |
|---|---|---|
| `bg` | `0x0B0E14` | 背景 |
| `panel` / `panelLight` | `0x14181F` / `0x1C212B` | 面板底板 |
| `border` | `0x3A4150` | 描边 |
| `text` / `textDim` | `0xEDEFF2` / `0x8A919D` | 正文 / 次要文字 |
| **`accent`** | **`0xFFD400`** | 唯一强调色 |
| `gold` | `0xF0A824` | **升耗提示**（`BattleScene.ts:905/924`）。⚠️ **不再是宝箱色**——迷宫改黄底后宝箱已改用深色墨线 `bg`（`DungeonScene.ts:905-926`），但 `ui-style-spec.md:241` 未同步 |
| `tech` | `0x2ED6C8` | 科技 |
| `danger` | `0xE5483F` | 危险 / 敌人 |
| `hp` | `0x3FD08C` | **单色** HP / 正面反馈（`Widgets.ts:169/196`、`HUD.ts:207`、`UIScene.ts:64`）。**不是**渐变端点 |
| `hpLow` / `hpMid` / `hpHigh` | `0xE5483F` / `0xFF8C1A` / `0xFFD400` | 血条渐变三锚点（红→橙→黄），`BattleScene.ts:126/1660-1661` |
| `exp` | `0xC9A54C` | 经验 |
| `fogUnknown` / `fogExplored` | `0x04060A` / `0x7C8492` | 迷雾：未探索 / 已探索（`DungeonScene.ts:839` 渐隐遮罩用后者色） |
| **迷宫专用** `mazeRoad` / `mazeWall` / `mazeVoid` | `0xFFD700` / `0x00008B` / `0x000000` | 通道黄 / 墙深蓝 / 虚空黑（相机背景） |

> ⚠️ **`floor` / `floorAlt` 不要改**（`0x1A1F28` / `0x151A21`）—— 它们是各 UI 面板的深色底板（`BattleScene:308`、`InventoryPanel:345`、`PartyPanel:259/400`、`RecruitPanel:145/270`、`TechPanel:163`），改了会把所有面板一起染。
>
> ⚠️ **`wall`（`0x2E3540`）全项目零使用** —— 只存在于定义处。而 `ui-style-spec.md:36-38` 仍把它定义为"迷宫墙体"、§5（`:238-239`）还要求用 `floor/floorAlt/wall` 重绘迷宫。这是**真实的规格书 vs 代码冲突**：迷宫实际用 `mazeWall`。详见 §8.7。

### 8.3 布局尺寸

| 项 | 值 |
|---|---|
| 逻辑分辨率 | 可热改写：1280×720 / 1600×900 / 1920×1080 / 2560×1440 / 3840×2160 + 自适应 |
| 顶部信息带 `TOPBAR_H` | **64px**（叠加层，不挤压地图） |
| 横条下渐隐遮罩 | 34px 逐行降 alpha |
| 迷宫格子 `TILE` | **34px**（世界坐标恒定，`DungeonScene.ts:48`） |
| 相机缩放 | `Clamp(min(视口宽,高) × 1.15 ÷ (max(迷宫宽,高) × 34), 1, 2.2)`，**再保留两位小数**（`DungeonScene.ts:752-758`） |
| 屏幕方向键按钮 | 54px + 4px 间隙；距左缘 28px；透明度 0.15–1（`DungeonScene.ts:278-283/335`） |
| 顶部渐隐遮罩 | 34px，`0.62 × (1 − i/34)`（`DungeonScene.ts:836-841`） |
| 立绘三档 | 头像 256×256 / 半身 384×512 / **全身 512×1024（透明底）** |

> ⚠️ 相机缩放公式的易错点（第一版本文档写错过）：① 分母是**迷宫最大边长** `max(宽,高)`，不是"格子总数"；② `round` 是**保留两位小数**（注释：非整数倍率下格子边缘出现半像素接缝），不是取整；③ **夹取在取整之前**。
>
> **布局常量的归属**（`§0.3` 的补充）：`TILE=34` / `DPAD_MARGIN=28` / 渐隐 `FADE=34` 都在 **`DungeonScene.ts`**（:48 / :45 / :836），**不在 `constants.ts`**；控件热区 `ICON_HIT_MAX=40`、`DRAG_CANCEL_DISTANCE=8` 在 **`Widgets.ts`**（:350 / :348）。改这些要动对应文件，不是改 constants。

### 8.4 相机与渲染分工 ✅

- **双相机**：`cameras.main`（世界，带缩放 + **队伍恒居中**，只画 `tileLayer`/`spriteLayer`）与 `uiCam`（1:1，只画 UI）。
  > 措辞注意：实现**没有用 `startFollow`**（全 src 零命中），而是每次移动后 `pan(…, 140, 'Sine.easeOut')` 或 `centerOn`（`DungeonScene.ts:817-826`）。效果等同"恒居中"，但说"相机跟随（follow）"作为机制描述不准确。
- 判定规则：「除两个世界图层外全是 UI」，`update()` 每帧把首次见到的对象移出世界相机（`WeakSet` 去重）。
- 迷宫外为**纯黑**（相机背景色）。

### 8.5 交互约定

- **点击热区**：普通按钮严格等于绘制尺寸；图标按钮 `iconMode` 取字形+padding 与指定尺寸的较大者（上限 40px）；圆形按钮圆形热区。
- **滑动列表**：滚轮 + 拖拽（带惯性）+ 滚动条；拖拽超阈值**取消条目点击**；键盘 ↑↓/PageUp/PageDown/Home/End。
- ESC：对话框打开时接管，避免"一次 ESC 既关对话框又开菜单"。
- 按下后移出按钮（或移动 >8px）再松手**不触发点击**。

### 8.6 战斗 AP 的 UX 规格

- AP 池显示在 **C3 指令列顶部**（`battle-ap-ux-spec.md:50`；代码 `BattleScene.ts:389/638`），**不是**"技能栏上方"。
- 技能数字为**动态消耗值**；升耗时转 `gold` 金色 + **右上角** `+N` 角标；AP 不足时转 `danger` 红色并加斜线（`BattleScene.ts:905-928`）。

### 8.7 ⚠️ 规格书与实现的真实冲突（已核实）

第三方复核（2026-09-17）推翻了我第一版写在这里的两条"冲突"——那两条（「AOE 2 段写法 3 种」「`+N` 角标落点 2 种」）在两份规格书里**零命中**，是我张冠李戴。真正的冲突是下面这三条：

| # | 冲突 | 双方 | 现状 |
|---|---|---|---|
| 1 | ~~**角色卡尺寸**~~ | ~~规格书二值互斥 + 代码第三值~~ | ✅ **已决 D24**：采用 **168×340**（BUX 规格）。`ui-style-spec.md:164` 的 96×128 **应标注废弃**；代码需从 168×400 回调，并同步调 `CONSOLE_H` 与卡内元素坐标 |
| 2 | **卡牌抬升时长** | 120ms vs 200ms | ✅ **已决 D27 + D30**：**120ms**，但改为「**已出手**瞬时反馈」（出手时抬升后即回落），**不再**表示"当前行动者"。原三件套（常驻抬升 8px / 切角描边 2px / 行动角标 16×16）**废弃** |
| 3 | **技能栏形态** | 三形态互斥 | ✅ **已决 D26**：**156×46 横向条**（代码现值）。`ui-style-spec.md` 的 36×36+冷却扇形应标废弃（冷却已废除） |
| 4 | **AP pip** | 形状与槽位数互斥 | ✅ **已决 D25**：**菱形 20×20，两排 5+5 共 10 槽**⚠️ 代码现值形状是平行四边形，需改 |
| 5 | **迷宫色名** | 规格书用 `floor/floorAlt/wall`，代码已改 `mazeRoad/mazeWall/mazeVoid`（`wall` 零使用） | ✅ **已决 D36**：改规格书对齐代码；`floor`/`floorAlt` 保留（面板底板），`wall` 标废弃 |

### 8.8 动效（规格书已成文项）

| 场景 | 时长 | 缓动 | 出处 |
|---|---|---|---|
| 镜头对准队伍 | 140ms | `Sine.easeOut` | 代码 `DungeonScene.ts:822` |
| 卡牌抬升 / 回落 | **120ms**（回落 150ms）/ 另有 **200ms** | `Quad.easeOut` | ⚠️ 两规格冲突，见 §8.7-2 |
| AP pips 逐个亮起间隔 | 80ms | — | `battle-ap-ux-spec.md:55` |
| 点击不可用技能抖动 | 120ms | — | `battle-ap-ux-spec.md:98` |
| 图鉴扫描线 | 2400ms（yoyo） | `Linear` | `ui-style-spec.md:212`，条目错开 index×300ms |
| 营地全息信标浮动 | 1800ms（yoyo） | `Sine.easeInOut` | `ui-style-spec.md:258` |
| toast 存活 | 2000ms | — | 代码 `UIScene.ts:12` |

> 我第一版曾写「面板入场 → 按规格」，但两份规格书里**没有任何"面板入场"动效规格**（grep 零命中）——该行已删除，属于我编造。

### 8.9 立绘

`public/assets/portraits/`，每个角色**三档**（`constants.ts:105-112`、`PreloadScene.ts:41/50-53`）：

| 档 | 尺寸 | 用途 |
|---|---|---|
| `portrait_XX.png/.webp` | 256×256 | 列表缩略图、角色行 |
| `bust_XX.png/.webp` | 384×512 | 图鉴详情大图 |
| **`full_XX.png/.webp`** | **512×1024（透明底）** | 详情栏大立绘，**优先级高于 bust**（`PortraitCard.ts:25/76-85`） |

> ⚠️ 我第一版只写了两档，与本文档 §2.1 的"**40×3** 立绘纹理"自相矛盾；`ui-style-spec.md` 也没提 `full` 档 —— 属文档与规格**同时遗漏**，证据只在代码里。
> 加载失败降级为占位图（`PortraitCard`），不抛异常。

---

## 9. 存档

| 项 | 值 |
|---|---|
| 版本 | `SAVE_VERSION = 4` |
| 结构 | `{ version, timestamp, state }`；state = player / party / roster / characters / tech / techPoints / inventory / gold / dungeonProgress / activeRun / settings / playTime / rngSeed |
| 存储键 | 主存档 `maze_rpg_save`；3 槽 `maze_rpg_save_slot_N`；轻量设置 `maze_rpg_settings` |
| `activeRun` | dungeonId / floor / seed / playerX / playerY / consumed[] / vitals{} |

**迁移链**：v1→v2 补 settings+activeRun；v2→v3 补 `unlockedDungeons`；v3→v4 删除 MP 遗留。任何版本读完强制 v4；非法 state 整份回落默认并告警。

**存档时机**：**全部是事件驱动** —— 新游戏 / 改设置 / 踩存档点 / 离开地牢 / 进战斗前 / 战斗胜利 / 失败 / 撤退 / 营地按钮 / 大地图存读档 / 返回主菜单。

⚠️ 所谓"每回合自动存档（30 秒节流）"**未接线**：`AUTOSAVE_INTERVAL` + `SaveSystem.autosave()` + `GameState.tick()` 三者全部无人调用。
⚠️ `playTime` 只在无人调用的 `tick()` 里累加 → **游戏时长永远是 0**，但 UI 处处显示它。
⚠️ 主菜单**不自动续档**，必须手点「继续游戏」。战斗中不能存档。
🪦 `exportJson` / `importJson` 无 UI 入口。

---

## 10. 已知问题清单（实现与意图不符）

> 按严重度排序。每条都在附录 C 有对应的待决问题编号。

| # | 问题 | 影响 |
|---|---|---|
| **1** | **迷宫 HP 池与战斗 HP 池完全不连通**。迷宫 pool 只在 DungeonSystem 内部消耗；战斗用 `teamDerived(party).maxHp` **满血开局**；`GameState.setVitals/vitalsOf` **无人调用**。 | 战斗掉血不影响探索，探索掉血不影响战斗 → **团灭只扣 20% 金币几乎无成本**<br>🔧 **D1 已裁决**：改为真共享一份，本项转为「待实现」 |
| **2** | **"共享生命池"名不副实**。实现是每个我方单位各持一份**相同**的 hp；伤害只落在单个单位上；血条只读 `ally[0]`。因敌人恒打血最少者 + 开局全满，**恰好**伪装成共享池。 | 一旦 `ally[0]` 倒下，血条显示 `0/N` 却仍在战斗；其它角色放治疗**完全无效**（自己满血）<br>🔧 **D1 已裁决**：改为真共享一份，本项转为「待实现」 |
| **3** | **无通关 / 结局定义**。第 6 层出口选"返回营地"即算 `cleared`，与战斗胜负无关。最终 Boss 是随机杂兵，可不遇到。 | 游戏没有终点，也没有目标感<br>🔧 **D2 已裁决**：每图第 6 层固定 Boss，击败即通关；不做全局结局（见 §7.5 待办） |
| **4** | **全队 buff 语义不成立**。`self` 与 `ally_all` 一律返回 `[actor]`，buff 只落在施法者身上；且只有"该角色出手"时才被当作队伍属性。 | 11 条 `ally_all` 技能描述承诺的"全队增益"实际只在施法者那一击生效<br>🔧 **D10 已裁决**：非攻击类 buff 全部删除，本项随之消解 |
| **5** | **buff 生效路径四种结果**：筋力有效；**耐力无效**（maxHp 只影响显示）；敏捷/幸运/灵性**只按 0.004/点**折算且概率已冻结；**魔力完全无效**。 | 大量技能描述与结果不符<br>🔧 **D10 已裁决**：只留攻击类，其余删除 → 本项转为「待实现」，但**须先做技能影响面普查**（见 §0.5 副作用警告） |
| **6** | **敌人属性不随层数成长** → "越深越难"只体现为路更长怪更多，敌人还是 Lv1–2。 | 难度曲线断裂 |
| **7** | **Boss 无标记，会当路边怪随机刷出**（最终 Boss 权重 2）。 | 叙事与难度双崩 |
| **8** | `apCost` 全部 = 1 → AP 退化为"行动次数"。 | 战斗决策深度大幅下降 |
| **9** | **经验倍率双重计算**（队伍魔力 + 个体魔力各乘一次）。 | 数值虚高 |
| **10** | 经验**只发给出战成员** → 备战栏角色永远 0 经验。 | 换人即断层 |
| **11** | `submitAction` 不校验 AP；AP=0 时无法防御/道具/逃跑；同一单位可无限次行动。 | 规则漏洞 + 手感困惑 |
| **12** | 强化 `combo_bonus` **完全空转**（只写日志），且逻辑上不可达（命中数在循环前已定）。 | ❌ **此条已被 D180 推翻为误判**：`skillComboBonus()`（`DamageCalculator.ts:50-52`）确实读 `effects` 里的 `combo_bonus` 并加进 `comboRate`（`:129`），命中数也早已可追加（`totalHits += baseHits`，`:133`）⇒ **功能是好的**。 |
| **12b** | 🔴 **70 条"伤害 + `effects`"技能的 `effects` 完全不结算**（`applySupportEffects` 只在 `power === 0` 时被调用，`DamageCalculator.ts:117-123`） | 88 条技能里 **70 条**的附带效果静默失效，文案与实战不符 |
| **12c** | 🔴 **11 条 `target: 'ally_all'` 技能只作用于施法者本人**（`resolveTargets:477-479` 一律返回 `[actor]`），而伤害公式把出手者的 buff 当全队增益读 | "全队增益"只对施法者本人有效；**D1 改真共享池只能修好治疗那一半，增益那一半仍错** |
| **13** | `element` 7 种取值**无任何战斗逻辑**，无克制表。 | 属性系统不存在<br>🔧 **D11 已裁决**：删除字段，不做克制 |
| **14** | 「迟缓」陷阱无效果；「不死鸟之羽」全场景不可用；「魔铁矿」描述的锻造系统不存在。 | 若干内容为空壳 |
| **15** | 传送用 `Math.random()`，破坏"同种子可复现"。 | 可复现承诺失效 |
| **16** | 宝箱金币按 `config.floor`（恒 1）而非实际层，首层与后续层口径不一致。 | 越深越不值 |
| **17** | 文案与行为不符：失败文案"送回营地"实回世界地图；"探索进度已清空"但 `maxFloor` 保留。 | 误导玩家 |
| **18** | `entities/` **整个目录无人 import**（4 个孤儿模块）。 | 代码债 |
| **19** | `src/entities/BattleUnit.ts` 还留着与阶段制冲突的速度逻辑。 | 混淆 |
| **20** | **治疗量用「筋力派生」而非「魔力」**。多条治疗技文案写"恢复生命"，数值却取 `team.attack`（= Σ筋力×5）。花语祝福 / 茶点侍奉 / 天使赐福 / 银盾庇护 等全部如此。 | 文案与实现脱节；魔力只在经验倍率上有效 |
| **21** | **宝箱金币基数恒为 45**。`goldBase = 20 + 25 × config.floor`，而 4 张地牢配表的 `floor` **全是 1** → 层数增长实际未生效。 | 越深越不值（同 §10-16） |

---

## 11. 内容附录（并入的草案数值表）

> **本节的存在理由（D244 / D303 / D352）**：项目的设计产出分散在 **13 份草案**里。按 D303「GDD 保持单文件」，这些草案的**数值表与规则**在此并入正文；草案 md **降为"推导过程"的引用**（改 GDD，不改草案）。
> **并入顺序**（D352：先合已冻结的）：**A 饰品 / Boss / 族群特性** ✅ → **B 战术系统** ✅ → **C 技能文案** ✅ → **D 存档规格** ✅ → 其余待冻结后并入。

---

### 11.A 饰品 · Boss · 族群特性

> **来源**：`design/drafts/content-draft-v1.md` **v4.1（定稿冻结）**
> **对应裁决**：D80–D85 / D88 / D113 / D121 / D122 / D128 / D130 / D141 / D156–D159 / D162–D171 / D183–D198 / D216 / D266 / D298（修正）/ D331（再修正）

#### 11.A.1 十二件饰品（4 类 × 3 档）

**档位 = 材料档**（D195 之后不再承诺"每类三档各一"）：

| 档 | 适用等级带 | 材料 | 掉落源 |
|---|---|---|---|
| **档 I** | Lv1–8 | `item_ore` 魔铁矿 | 无根回廊 / 苔痕回廊 |
| **档 II** | Lv8–14 | `item_crystal` 深渊结晶 | 静谧墓所 |
| **档 III** | Lv14+ | `item_core` 魔核 | 腐朽王座 |

| # | id | 名字 | 类 | 档 | ★ | 数值 | 配方 | 输出增量 |
|---|---|---|---|---|---|---|---|---|
| 1 | `acc_a1_ironweight` | 沉铁配重环 | 1 属性 | I | 3 | 筋力 +2、耐力 +1（3 点） | 魔铁矿 ×12 | +2.8% 攻 / +1.4% HP |
| 2 | `acc_a2_bearing` | 静默轴承组 | 1 属性 | II | 4 | 筋力 +2、耐力 +4（6 点） | 深渊结晶 ×6 + 魔铁矿 ×8 | — |
| 3 | `acc_a3_deepcore` | 深核供能芯 | 1 属性 | III | 5 | 筋力 +5、耐力 +6（11 点） | 魔核 ×3 + 深渊结晶 ×6 | — |
| 4 | `acc_p1_metronome` | 节拍同步器 | 2 概率 | I | 3 | **连击率 +4pp** | 魔铁矿 ×12 | +6.8% |
| 5 | `acc_p2_crosshair` | 准星校准环 | 2 概率 | II | 4 | **暴击率 +10pp** | 深渊结晶 ×6 + 魔铁矿 ×8 | +5.9% |
| 6 | `acc_p3_amplifier` | 脉冲放大器 | 2 概率 | III | 5 | **强化率 +10pp** | 魔核 ×3 + 深渊结晶 ×6 | +2.3% |
| 7 | `acc_t1_freerun` | 空转惰轮 | 3 战术 | II | 4 | 每回合首次受伤 ×0.5 | 深渊结晶 ×6 + 魔铁矿 ×8 | 0（纯防御） |
| 8 | `acc_t2_solver` | 装甲解算器 | 3 战术 | III | 5 | 单次受伤 ≤ 队伍最大生命 25% | 魔核 ×3 + 深渊结晶 ×6 | 0（纯防御） |
| 9 | `acc_t3_ballast` | 压舱锚针 | 3 战术 | III | 5 | 比例类伤害（削血/中毒）×0.5 | 魔核 ×3 + 深渊结晶 ×6 | 0（纯防御） |
| 10 | `acc_r1_plating` | 自愈镀层 | 4 每回合 | I | 3 | 每个大回合开始回复 4% maxHp | 魔铁矿 ×12 | 续航 |
| 11 | `acc_r2_reflector` | 相位反射片 | 4 每回合 | II | 4 | 每回合首次受击时反击「队伍攻击力 × 30%」 | 深渊结晶 ×6 + 魔铁矿 ×8 | +2.9% |
| 12 | `acc_r3_dissociator` | 逆向解离针 | 4 每回合 | III | 5 | 我方阶段开始清除 1 条属性削减 | 魔核 ×3 + 深渊结晶 ×6 | 0 |

**关键数值口径（都是裁决，不是建议）**：

| 项 | 值 | 裁决 |
|---|---|---|
| 槽位归属 | **每个出战位 1 个**（不是挂在角色上，换人不丢） | D292 |
| 槽位开放 | **开局就有** | D270 |
| 类 1 的取值规则 | 单件 = **该档队伍该维均值的 3.3%**（3 件满装 ≈ 10%） | D187 定标 |
| 类 1 只放线性维 | 只加筋力/耐力（敏捷/幸运/灵性是**饱和曲线**，+40 也只有 +3pp） | ①.3 |
| 类 3 三件可同装 | **接受**，代价写明：三件满装 = 放弃全部输出饰品 | D198 |
| **伤害轴最坏上界** | **≤ +25%**（实操 +21.96% / 理论上界 +24.13%） | D197 |
| 获取途径 | **锻造（主力）+ 商店每店限购 1 件** | D128 |
| 配方是否全知 | **开局全知** | D282 |
| `price` | ★3 = **600** / ★4 = **1 500** / ★5 = **4 000** | D171 |
| 锻造费 | **材料 + `price` × 0.6**（原 0.9 会让"自造"比"直购"贵，方向反了） | D167 + 经济修正 |

**⚠️ 已作废的三件（D184，保留可追溯）**：`T1 过载抑制器`（取消临时 AP 升耗）、`T2 冗余电容`（AP 上限 +2 / 初始 +1）、`R3 终末采样器`（每回合 +2 AP）。
作废理由：它们加的是**行动次数**，与伤害轴**相乘** ⇒ 六槽最坏组合 **+148%**（R3 单件即 +73%）。

#### 11.A.2 四只专属 Boss

**写进 `enemies.json` 的是「基础值」**；第 6 层实际值 = 基础值 ×（HP ×3.5 / 攻 ×2.25 / 防 ×2.5）。

| | 碎巢母 | 菌王·腐甜之主 | 不眠的司祭 | 王座上的等待者 |
|---|---|---|---|---|
| 地牢 | 无根回廊 | 苔痕回廊 | 静谧墓所 | 腐朽王座 |
| 族群主题 | 杂居兽群 | 真菌孢子 | 亡骸军团 | 深渊眷属 |
| `id` | `enemy_boss_swarm_mother` | `enemy_boss_spore_king` | `enemy_boss_sleepless_priest` | `enemy_boss_throne_waiter` |
| `level` | 5 | 7 | 10 | 14 |
| **`hp` 基础** | 330 | 551 | 965 | 1 328 |
| **`attack` 基础** | 29 | 49 | 71 | 107 |
| **`defense` 基础** | 0.06 | 0.10 | 0.14 | 0.18 |
| `exp` | 140 | 250 | 450 | 800 |
| `gold` | 120 | 230 | 400 | 780 |
| `techPoints`（重复击杀） | **0** | **0** | **0** | **0** |
| **第 6 层实际 HP** | 1 155 | 1 929 | 3 378 | 4 648 |
| **第 6 层实际攻击** | 65 | 110 | 160 | 241 |
| **第 6 层实际防御** | 0.15 | 0.25 | 0.35 | 0.45 |
| 必掉（`chance` 1.0） | `item_ore` ×15 | `item_crystal` ×3 | `item_crystal` ×4 | `item_core` **×4** |

**口径与推导**：
- **等级 = 该图推荐等级 + 4**（D48）；**HP = 同层普通敌实际 HP × 9**（D48 的 8–10 倍取中位）；**攻击 = 同层普通敌攻击均值 × 3.0**（D48 的"不秒人"）
- **防御硬性封顶基础 0.18** —— 因为第 6 层 ×2.5，基础 > 0.38 会被 `clamp(0.95)` 吃掉（`DamageCalculator.ts:231`）
- **`techPoints` 全为 0**：D254/D255 之后战斗不产科技点（首通 38 + 主线 14 走 `firstClearTechPoints` 与任务）
- **`item_core` ×4**（D299，原 2）⇒ 造满 12 件饰品从 7.5 趟降到 3.75 趟。⚠️ **必须与 D258 同批次落地**，否则魔核来源为 0
- **专属技能**：每个 3–4 条（D51 改为"2–4"），共 **13 条**；释放顺序按 **D111「固定循环 + 血量阈值插大招」**
- **Boss 也享受本图族群特性，但有逐只例外**（D162 + D165/D166/D169）：菌王的孢子**改为"HP 首次跌破 50% 时触发"**；**司祭不吃亡骸重整**；碎巢母的分裂**继承 15%**（非 25%）

#### 11.A.3 四条族群被动特性

| 图 | 族群 | `family` | 特性 | 效果 |
|---|---|---|---|---|
| 无根回廊 | 杂居兽群 | `swarm` | **群体本能** | HP 归零后分裂成「残躯」：**继承 25% HP、攻击 ×0.7**，**只触发 1 次**、残躯不继承特性。⚠️ **碎巢母为例外（继承 15%，D169）** |
| 苔痕回廊 | 真菌孢子 | `fungus` | **孢子爆发** | 被击败时我方全队**中毒 2 回合**（每回合 **maxHp 的 3%**）；**死于连击第 2 段及以后则不触发** |
| 静谧墓所 | 亡骸军团 | `undead` | **亡骸重整** | 受到**治疗**时反而受到**等量伤害**（按实际恢复量结算，满血时溢出部分不计） |
| 腐朽王座 | 深渊眷属 | `abyss` | **深渊吞噬** | 战斗进入**第 3 个及以后的我方阶段**时**触发 1 次**，我方全队损失**当前生命 8%** |

**三条硬口径**：
- **作用于"我方全队"的效果只结算 ×1**（战斗被视作 1V1，队伍是一个整体）—— D273
- **特性挂在敌人身上**（新增 `family` 字段），**不是挂在地牢上** —— 因为 D148 之后同一地牢有 8 只怪、且敌人可能跨图复用（骷髅兵同时在苔痕与静谧）
- **`family` 字段是新增字段**（D231/D272 的落点修复之前，"敌方给我方上 debuff"无法表达）

---

### 第 97 轮

| # | 议题 | 裁决 |
|---|---|---|
| D449 | **"群体升一级"的语义** | ✅ **全队各升 1 级（永久）** |
| D450 | **价格与限购** | ✅ **600 金 + 每店限购 1 件**（高于其它 ★4 的 300–320，因为"跳级"是稀缺效果） |
| D451 | **与 D107 训练场的关系** | ✅ **共存，但跳级受限**（限购 1 + 600 金）⇒ 训练场仍是"细水长流"的主路径，跳级是"偶得的爆发" |
| D452 | **§12 的范围** | ✅ **四块全放**（见下） |

> 📌 **D442 落地：新 ★4 消耗品（名称暂定，可改）**
> | 字段 | 值 |
> |---|---|
> | `id` | `item_ascension` |
> | 名字 | **升阶谕令** ★4 |
> | 效果 | **使用后全队各升 1 级**（永久，走正常升级流程） |
> | `price` | **600** |
> | 限购 | **每店限购 1 件**（D450） |
> | 使用场景 | 待定（营地必可用；**战斗内是否可用**需与 D140「使用占出手 + 1 AP」协同，**下一轮定**） |
> ⇒ ⚠️ 它**直接绕过经验曲线**：与 **D403「科技点不可洗」**体现的"选择有重量"原则有张力 —— **这是有意识下的**，靠限购 + 高价控制总量。

---

## 12. 实现避坑清单（给实现者的第一份要读的东西）

> **依据**：D444 / D452。**范围**：① 必修代码缺口 ② 顺序约束 ③ 16 处纠正归纳出的规律 ④ 引用纪律与环境坑。
> ⚠️ **本节的每一条都是本会话实测出来的**，不是推测。

### 12.1 必修的代码缺口（11 条，按修复顺序）

| 序 | 缺陷 | 位置 | 后果 |
|---|---|---|---|
| **1** | `applySupportEffects` 增减益落点**恒为施法者** | `DamageCalculator.ts:270-287`（签名 `:254-259` 无 targets） | 我方"削敌"技能会削弱自己；Boss 的削我方技能作用在 Boss 自己身上 |
| **2** | `applyEnhancedEffects`（**伤害路径**）落点**恒为被击中的敌人** | 签名 `:301-308`、落点 `:332` | 强化附加效果的落点与 `target` 无关；**同函数"辅助技路径"的另一处缺口见 §13.1 的 R4 第 8 类（勿合并）** |
| **3** | **70 条"伤害 + `effects`"的 `effects` 完全不结算** | `DamageCalculator.ts:117-123` | 88 条技能里 70 条的附带效果静默失效 |
| **4** | **`power=0` 的纯辅助技不掷强化** | 强化判定在伤害路径内 | 配表写的 `enhanced` 永不生效 |
| **5** | 🔴 **`save()` 在 `activeSlot === null` 时回落 `SAVE_KEYS.main`（= UI 的"01 槽"）** | `SaveSystem.ts:40-41` / `:95-96` / `:139-142`；受害路径 `MainMenuScene.ts:637` / `:167` | **未读档点一下"保存设置"，就用空档覆盖第 01 槽（不可逆）** |
| **6** | **同一 ternary 也在 `delete()` / `load()` / `hasSave()`** | `SaveSystem.ts:65-90` | `delete(null)` 会**删掉第 01 槽** |
| **7** | **AI 按 raw power 排序** | `CombatSystem.ts:643-645` | **多段技永远不会被 AI 释放**（含现役 `enemy_sweep`） |
| **8** | **`heal_max_ratio` 不被识别** | `CombatSystem.ts:639`（只认 `type === 'heal'`） | 新效果永不释放 |
| **9** | `maze_rpg_settings` **整体覆写** | `graphics.ts:174` | **改分辨率会静默抹掉 D100 的可访问性设置** |
| **10** | `slot.unlocked` **无人置 true** | — | 归档为死字段（D269） |
| **11** | `SAVE_SLOT_COUNT` = 3 | `config/saveKeys.ts:18` | 需改为 **10**（D370） |

### 12.2 顺序约束（顺序错了会造出更坏的后果）

1. **先修 ①②（两个落点）→ 再修 ③（effects 结算）→ 再修 ④（强化）→ 最后才落 D233/D249 的数值改动**
   ⇒ 否则会往配表里写入"**自伤技能**"；且 ③ 会把 ①② 的缺陷面从"15 条纯辅助技"放大到"88 条里的绝大多数"
2. **版本闸门（`save-G1`）必须先于 `party` 改对象数组（`save-G15`）** ⇒ 否则旧档的 `string[]` 被读成对象、`party[i].accessorySlot` 得 `undefined`，表现是"**编队界面全空**"（极难归因）
3. **`party` 结构变更（`save-G15`）必须先于 `activeSlot` 修复（`save-G19`/D368）**
4. **`clear_tactic` 必须走"伤害之后的独立结算钩子"**，**不许塞进 `effects`**（否则会踩 #3 的坑，D344 的通道① 静默失效）

**必须同批次落地的两对（只回退其中一个会出问题）**：
- **D258（3 只旧 Boss 移出 `enemyPool`）↔ D299（等待者 `item_core` 2→4）** ⇒ 否则**魔核来源有一段为 0**
- **D350（宝箱 3/4/5/6 → 2/3/3/4）↔ D367（S9 阈值 2500 → 1700）** ⇒ 两者 −3.23 / +3.12 几乎抵消（净 −0.11）

### 12.3 本会话 16 处纠正归纳出的三条规律（**都是"lead 压缩信息时丢约束"**）

| 类 | 典型例 | 对策 |
|---|---|---|
| **数字用错语境** | 骸骨暴君的防御 0.14 被当成"全表上限"；`SAVE_SLOT_COUNT=3` 被当成"出战位容量"；碎巢母的 15% 被当成"全体值"；类 2 上界用了旧口径 | **凡引用某个数，必须同时写出它的来源与适用范围** |
| **命名空间混引** | `ui-G` 与 `save-G` 混成一谈；两套 G 表 | **编号必须带文件名前缀**（`save-G19` / `ui-G15`） |
| **转述丢限定语 / 同一动作链写两处** | 把原文"key 清单以 10 条为准"转述成"共 10 个"；存档命名的四步顺序写成两套 | **引用逐字**；**同一动作链只在一处写** |

### 12.4 引用纪律与环境坑

- 🗺️ **引用 GDD 位置一律用「小节号 + 原文措辞」，禁用行号**（D435：行号会漂、措辞不会）
- 🧪 **必须用 Node 24**（系统版 `H:/node.js/node.exe`）：托管 Node 22 跑 `verify.mjs` 会**假红 1 项**（缺 `phaser3spectorjs`）
- 🧪 **`smoke.mjs` / `debug.mjs` 不自起 dev server**：必须先在 `127.0.0.1:5173` 起 `vite`，否则 54 项全红是**假失败**
- 🧪 **跑 smoke/debug 时不要编辑 `src/`**：Vite HMR 会刷新页面 ⇒ `Inspected target navigated or closed`
- 🔧 **改 `src/data/tables/*.json` 后必须跑 `node scripts/gen-tables.mjs`**；但改 `src/config/mazeDifficulty.ts` **不用跑**（它是 TS 模块）
- 🖼️ **`setScrollFactor(0)` 挡不住相机缩放** ⇒ 迷宫内的 UI 必须画在 **UI 相机图层**（双相机方案）
- 🖼️ **不要在 `create()` 里用 `cam.worldView`** 算可见范围（首帧前是空矩形 ⇒ 整个迷宫全黑）
- 🚫 **禁止删除 `design/drafts/` 下的任何文件**（本会话发生过一次交付物被误删）
- 🐚 本机 **bash 近乎不可用**（`ls`/`cat`/`rm`/`grep` 全 not found、管道会失败）；**PowerShell stdout 不回显** ⇒ 稳妥做法是脚本内 `fs.writeFileSync` 落盘再读；删文件用 Node 的 `fs.unlinkSync`

---

## 13. 风险清单

> **依据**：D389（五件套之②）/ D391（第 90 轮体检）/ D420。**只列"真风险"**（已确认存在、且有后果），不含推测。

### 13.1 结构性风险（已确认，部分已化解）

| # | 风险 | 现状 | 缓解 / 状态 |
|---|---|---|---|
| **R1** | **迷宫 HP 与战斗 HP 不连通** | 代码事实：`startBattle` 不传 HP ⇒ **每场战斗满血开局**；D164 的"双层生命账"**未实现** | 属 **§10 #3** 的老问题；**D1 是其前置**。⇒ **T-1 的落地顺序第一位** |
| **R2** | **陷阱致死路径不可达** | `Math.max(1, …)` 不致死 + 全仓 `grep 团灭` 零命中 ⇒ **D168 无代码路径** | 已立案 **T-1**（D373）；`trapCount` **冻结不动**（D378）；验收带 **每 10 趟 0–1 次**（D376） |
| **R3** | **我方输出 ∝ N²** | **已化解**：D108 实测显示它不是主因（N=4 时约 1.3 倍），真正的主因是**每轮我方 4 行动 vs 敌方 1**，而 **D242** 已把 TTK 拉到 6.15 | ✅ **§10 #32 的归因已更正**（D354/D356） |
| **R4** | **"配表写了但代码读不到"共 14 类**（**原始 6 类 + 本轮新增 8 类**） | 原始 6 类：落点家族 2 条 + 70 条 effects + 强化不掷 + AI 多段技 + `heal_max_ratio`；**本轮新增 8 类 = R4 第 7–14 类**（见下表） | 原始 6 类全部列在 **§12.1**、修复顺序 **§12.2**；新增 8 类的逐条现状见下表 |

**R4「本轮新增」逐条（R4 第 7–14 类）**

> **筛选口径**：**配表字段 / 已裁决规格已存在，而代码缺读取 / 承载点**（静默失效）⇒ 计入；**"整个系统未实现"不计**（那是"没写"，不是"配表空转"）。来源：`§12.1`（原始 6 类）＋ `design/drafts/gap-table-round3.md` §1（本轮实测，只读）。

| 编号 | 缺口 | 现状 |
|---|---|---|
| **R4 第 7 类** | `difficulty` 在战斗侧零引用 ⇒ 三档难度 / 噩梦机制完全无效 | 在修（1a：`CombatSystem` 按 `difficulty` 修敌方 HP / 攻击，**不碰 `defense`**，D453） |
| **R4 第 8 类** | `enhanced` 的落点**在辅助技路径**缺 `buffTargets` ⇒ 强化段落在施法者身上（**与 §12.1 第 2 类同为 `applyEnhancedEffects`：那条 = 伤害路径、本条 = 辅助路径，勿合并**） | **已修**（`buffTargets: alive`） |
| **R4 第 9 类** | 汲食 `..._siphon` 缺吸血读取点 ⇒ 等待者少一条核心机制 | 在修（4a） |
| **R4 第 10 类** | 育巢 dot 落点被读成 `skill.target='self'` ⇒ 毒到菌后自己 | 在修（5a） |
| **R4 第 11 类** | `firstTimeFlags` 无写入点 ⇒ D283 / D364 两条语义无法端到端验证 | 在修（7a） |
| **R4 第 12 类** | 商店限购 `perRunPurchaseLimit` 无存档宿主 ⇒ 关页重进限购归零 | 在修（8a：挂 `activeRun.runCounters`） |
| **R4 第 13 类** | 事件商人 / 迷宫内商店无宿主 ⇒ `event_merchant` 的 `perRunPurchaseLimit` 读不到 | 在修（12a） |
| **R4 第 14 类** | 饰品数值无读取点（类 1 `teamDerived()` 不吃 `partyEquip`／类 2 `probability.axis/pp` 无人读／类 3·4 `mec_*` 未实现） | 类 1 / 类 2 在修；类 3 / 类 4 待排（批次 2） |

**未计入 R4（"整个系统未实现"类，`gap-table-round3.md` §1 余项）**：任务引擎（第 6 项）、精英位机制（第 9 项）、战斗内道具出手闸门（第 10 项，属 D9 / D242 同批）、`MainMenuScene` 难度入口（第 11 项，属 UI 未接）；`damage_cut` / `dot` 时长递减挂 `endRound`（第 3 项，属"定序"，数据已被读取）。§12.1 的存档 3 条、`slot.unlocked` 死字段（D269）、`SAVE_SLOT_COUNT` 常量亦不属 R4。

### 13.2 验收风险

| # | 风险 | 现状 | 缓解 / 状态 |
|---|---|---|---|
| **R5** | 🔴 **D304 的「Boss ≥10 回合」当前无法验收** | 96 格 TTK 全落在 **[2,10]**，从未到过 11 —— 因为**那 4 只 Boss 还没进配表** | 登记为"**待 Boss 落地后补验**"（D433）；**明令禁止拿普通怪的 10 当替身** |
| **R6** | **D304 的三档依赖 D242 已实现** | 实测证实"三档硬门达成"**完全建立在 D242 之上** | ⇒ **D242 是实现侧的硬前置**，不是可选项 |

### 13.3 内容量与工作量风险

| 项 | 量 | 说明 |
|---|---|---|
| 敌人条目 | **44 个**（32 普通 + 4 精英 + 4 虚影 + 4 Boss） | 含族群归属 / 浅深分组 / 掉落 |
| 敌方技能 | **约 82 条"（敌人,技能）对"** | 按 D153 逐档计数 |
| 饰品 | 12 件 + 12 套独立配方 | 4 类 × 3 档 |
| 技能文案 | **93 条**（80 玩家 + 13 Boss） | 已到 v6 |
| 消耗品 | **10 种**（9 + 新 ★4 升阶谕令） | |
| 事件 | 11 种 | 需新建 `events.json` |
| 战术 | 我方 11（5 属性 + 6 机制型）/ 敌方 9（5 属性位 + 4 族群） | 敌方 3 个属性位是"敌方版"；⚠️ 我方"9（5+4）"原文有误，勘误见表后 |
| 引导 | 11 条 | 上下文触发 |
| **必修代码缺口** | **11 条** | §12.1 |

> ⚠️ **§13.3 勘误（第 101 轮 · 落地实测）**：「战术」一行的**我方原文作 9（5 属性 + 4 机制型），实为 11（5 属性 + 6 机制型）** —— 依据同 §11.B.2：12 件饰品中 **类 3 三件 + 类 4 三件 = 6 件**授予机制型战术（`tactics-draft-v1.md` §1.4/§1.5.1；`src/data/tables/tactics.json` 6 条）。「敌方 9（5 属性位 + 4 族群）」不变（`family` 为 4 族）。

### 13.4 终局与体验风险

| # | 风险 | 现状 | 缓解 / 状态 |
|---|---|---|---|
| **R7** | **终局只有 ≈1.52 小时科技点目标** | 稳态支线 34.7 点/趟、树的剩余 22.7 点 ⇒ 1.52 小时刷满 | **D308 已裁"不改、接受"**；之后靠 12 件饰品（3.75 趟）与精英材料续命 |
| **R8** | **终局循环未定义** | D265 已裁"先不定义" | 与 R7 叠加 ⇒ **流失风险量化值 ≈1.52 小时**；若实测流失，**第一优先是补终局循环**（D265 翻案） |
| **R9** | **重甲怪在第 6 层极难打** | 石魔像 **0.75 减伤**（我方只能打 25%） | **D414/D419 已接受**，但要求**同步拉高击杀奖励**，且其 TTK 不得越过 Boss 硬门 |
| **R10** | **★4「升阶谕令」绕过经验曲线** | 全队各升 1 级永久 | 靠 **600 金 + 每店限购 1** 控总量（D450）；与 D403「科技点不可洗」的"选择有重量"有张力，**有意识下的** |

### 13.5 数据一致性与流程风险

| # | 风险 | 现状 | 缓解 / 状态 |
|---|---|---|---|
| **R11** | **冻结草案与 GDD 可能不一致** | 已有实例（`firstTimeFlags` 10 vs 11、残躯 15% vs 25%） | **规则已定：以 GDD 为准**（§11 开头登记）；草案只读 |
| **R12** | **交付物被误删** | 已发生 1 次（`enemies-draft-v1.md` 消失） | 已立纪律：**派单必须加"禁止删除 `design/drafts/` 下任何文件"**；且已并行重写一版双保险 |
| **R13** | **lead 的"压缩式引用"会丢约束** | 16 处纠正，三类规律 | 对策见 **§12.3**：引用逐字 + 编号带前缀 + 同一动作链只写一处 |

### 13.6 仍在飞 / 未决（交付前必须收口）

| 项 | 状态 |
|---|---|
| **敌人两稿对账** | 两稿都在（`enemies-draft-v2-independent.md` v2.1 + 恢复稿）；**D396 已授权我对账取一** |
| **敌方 3 个"属性位"替代战术** | v4 已出（占位表 11 行），**整场增益 +9~12%（中值 +10.5%）**，抑制建议 G-c+G-d+G-g，**G-c 须补"层数最低优先"**否则回落 +8% |
| **UI 的 G2 / G5 / G6 三条阻断项** | 队员未答（已两次索要一行版） |
| **验收矩阵 / 交接文档** | 均已派出，**待交** |
| **§11 的物理顺序重排** | 记为**第 100 轮收口待办** |

---

## 14. 落地顺序（五件套之③）

### 批次 1 —— 不依赖战斗长度（可立即开工）

| 组 | 内容 |
|---|---|
| **A 先修代码（阻塞其它一切）** | **§12.1 的 11 条**，按 **§12.2 的顺序约束**执行 |
| **B 配表化** | 新建 `events.json`、`tactics.json`（机制型 + 族群型）、饰品 12 件、消耗品 10 种、敌人 44 条、Boss 技能 13 条、科技树 16 → 约 21 节点（99 点）、`enemies.json` 的 `subject` 字段（`family`）、`techPoints` 普通怪归 0 |
| **C 规格与文案** | 技能文案 93 条入 `description`、引导 11 条、事件 11 种、任务 9 支线 + 4 主线 |
| **D UI** | 任务面板 / 商店 / 锻造 / 装备（出战位双槽）/ 难度选择 + 引导开关 / 存档 10 槽 / 统计面板 / 训练场 / 战术 HUD（每侧 5 条）/ 事件界面（全文披露）/ 濒死三档渐晕 |
| **E 存档** | 版本 5 纯闸门 + 17 字段 + `party` 改对象数组 + `activeSlot` 拒绝写盘 |

### 批次 2 —— 依赖 D108 的验收（**已解锁**，D356）

战术叠层 / 深渊吞噬 / 饰品防御类 / 精英二动 / R2 相位反射片。

### 批次 3 —— 待对象落地后补验

**Boss 档的 TTK ≥10**（R5）/ **T-1 陷阱致死链**（R2，前置 D1+D168）/ **敌方 3 个替代战术的强度**。

### 第 99–100 轮（收口）

| # | 议题 | 裁决 |
|---|---|---|
| D453 | 🔴 **难度倍率不作用于防御** | ✅ **采纳队员建议**：D73 的难度倍率（简单 ×0.75 / 困难 ×1.25 / 噩梦 ×1.875）**只作用于 HP 与攻击，不作用于 `defense`**。<br>理由同 **D118（精英的 ×1.5 不吃防御）**：防御是**率**且带 `clamp(0.95)`，乘上去会**顶上限**，把"更危险"变成"**打不动**"。⇒ 与 **D414（上限 0.30）**叠加后，噩梦第 6 层石魔像会到 **0.75 × 1.5 = 1.125 → 被 clamp 到 0.95**（我方只能打 5%），这是必须避免的 |
| D454 | **两份交付物已落盘** | ✅ `design/drafts/acceptance-matrix-v1.md`（**验收矩阵**：12 条硬门 → 脚本/命令/判据/依赖 + 无法自动验收清单）与 `design/drafts/handoff-to-implementer-v1.md`（**给实现者的一页纸**）<br>⚠️ **这两份原派给队员但未落盘，为保第 100 轮交付由 lead 直接撰写** |
| D455 | **敌人稿 v2.2 已对齐** | ✅ 四项回填完成：**D414（7 只 def）** / **D411（腐朽精英掉 `item_core`）** / **D415+D438（虚影 `echo` 不挂族群技）** / **D413（=32 只）**；**D437 占位式零改动** |
| D456 | **五件套齐备** | ✅ ① GDD 单文件（含 §11 内容附录）② §13 风险清单 ③ §14 落地顺序 ④ `acceptance-matrix-v1.md` ⑤ `handoff-to-implementer-v1.md`（含 11 条必修缺口） |

> 🧭 **§11 的物理位置索引（收口：不做危险的大块搬迁，改为精确索引）**
> 尝试把 §11.B/C/D 搬到 §11.A 之后会涉及**约 700 行的整块搬移**，在文档已达 3900+ 行的规模下**风险高于收益**。⇒ **改为把导航做实**：
> | 小节 | 物理位置 |
> |---|---|
> | `§11 标题 + §11.A`（饰品·Boss·族群特性） | **§10 之后**（§12 之前） |
> | `§11.B`（战术系统） | 在**各轮决策记录区内**（约在文档中段） |
> | `§11.C`（技能文案） | 同上 |
> | `§11.D`（存档规格，**含本导航块**） | 同上（**最先被读到的一节**） |
> ⇒ **规范阅读顺序仍是 A → B → C → D**；各节自洽、互不引用行号，**内容正确性不受影响**。

> 📊 **第 100 轮 · 终局盘点**
> | 项 | 值 |
> |---|---|
> | **裁决总数** | **456 条**（D1–D456） |
> | **轮次** | **100 轮** |
> | **草案** | **18 份**（16 份队员产出 + 2 份 lead 补写） |
> | **必修代码缺口** | **11 条**（§12.1） |
> | **顺序约束** | **4 条 + 2 对同批次**（§12.2） |
> | **风险** | **R1–R13**（§13） |
> | **硬门** | **12 条**（8 条有脚本 / 4 条待写断言 / 5 类人工核对） |
> | **已知问题** | §10 共 **34 条**（含 6 类"配表写了但代码读不到"）〔**勘误指针**：该处"6 类"已由第 101 轮重计为 **14 类**，见 §13.1 的 R4 与本节「落地勘误（第 101 轮）」表〕 |
> | **未被实现的设计** | 全部 —— **本 GDD 是纯设计文档，一行游戏代码都未改**（用户从第 1 轮起即要求"都先不做"） |

### 落地勘误（第 101 轮 · 2026-09-18）

> 本节是**实施阶段实测**出来的 **18 处**"真源打架"的**收口记录**（①–⑥ 为第一轮，⑦–⑯ 为 team-lead 复核后追加，⑰–⑱ 为后续复核补入）。每一条都已在正文对应位置**就地留下勘误**（本节只做索引，不重复定义真源）。**"位置"一律用「小节号 / 裁决号 + 原文措辞」定位，不用行号**（D435）。

| # | 位置 | 原文 | 勘误 | 依据 |
|---|---|---|---|---|
| ① | §11.B.2「双方战术总览」表「非属性战术」行；§13.3「内容量与工作量风险」表「战术」行 | 「机制型 **4** 个（由饰品类 3/类 4 授予，D216）」；「我方 **9**（5 属性 + **4** 机制型）」 | 机制型为 **6** 个；我方为 **11**（5 属性 + 6 机制型） | §11.A.1 的 12 件饰品类 3 三件 + 类 4 三件 = **6 件**；`tactics-draft-v1.md` §1.4 / §1.5.1；`tactics.json` 6 条 |
| ② | §11.C.1「条数账目」表「合计」行 | 「合计 **93** —— 与 `skills.json` 的现役条数一致」 | 93 是**需文案**量（80 角色专属 + 13 Boss 专属）；`skills.json` 现役 **101**（80 + **8 条普通敌方技** + 13），那 8 条沿用旧文案、**不计入 93** | 配表 `skills.json` 现役 101 条 |
| ③ | D23「科技树经济定标」 | 「总价降至 **约 80 点**」 | **约 80 = 首清四图的达成率目标**，非总价；树总价落地 **99 点（21 节点）**，实测达成率 **76.3 / 99 = 77.1%** | §13.3 / §14；`tech.json` 21 条合计 99；D297 |
| ④ | D290「虚影的命名」 | 「四图同名，id **共用 `enemy_abyss_king_shadow`**」 | 落地为 **4 个独立 id**（`_root` / `_moss` / `_tomb` + `enemy_abyss_king`），按图给不同 `level`；立论已由 D438 的 `family: 'echo'` 取代 | `enemies.json`；`verify.mjs` 12.2 / `FAMILY_HISTOGRAM` |
| ⑤ | `docs/formulas.md` §11「首次通关奖励」 | 四图首通「**`3 / 3 / 5 / 10`**（合计 21）」 | 实为 **`8 / 9 / 10 / 11`（合计 38）**，口径「首通 38 + 主线 14」 | `dungeons.json` 的 `firstClearTechPoints`；D246 / D255 / D297 |
| ⑥ | D167「锻造的金币费与饰品定价」 | 锻造费「材料 + `price` × **0.9**」 | 现值 **× 0.6**（0.9 使"自造"比"直购"贵、与 D167 原意反向，已被覆盖） | §11.A.1 正文；`accessories.json` 12 条 `forgeCostRate` 全 **0.6** |
| ⑦ | D317「G1 战术栏容几条」 | 「**9 条**（5 属性 + **4 机制型**），UI 按 9 条设计」 | 「9 条按 UI 设计」**已被 D328 作废**：战斗 HUD 每侧只放 **5 条属性战术**，机制型不占 HUD（D328/D332）；内容 = **11** | D328；§11.B.2 |
| ⑧ | D342 旁「D342 + D328 合并后的战术 HUD 最终口径」表 | 「战术池 5 属性 + **4 机制型** = 9」「非属性战术：机制型 **4** 条」 | 我方机制型 **6** 条 ⇒ 战术池 **11**；敌方 5 + 4 族群 = 9 **不变** | §11.A.1 / §11.B.2 |
| ⑨ | §6 科技树（开头 + 🔧 框）；同源数亦见 §5.1 / §5.3 | 「16 个节点，合计 **166 科技点**」；「总价 166 → **约 80 点**」；「四图首通合计 21 → **约 45–50 点**」 | 现状 **21 节点 / 99 点**；**80 是"达成率目标"（75–85%），非树的总价**；四图首通 **21 → 38** | `tech.json` 21 条合计 99；`dungeons.json` 8/9/10/11；D246 / D255 / D297 |
| ⑩ | §1.4 内容规模（现状实测） | 「技能 88 / 敌人 14 / 道具 9 / 科技 16（166 点）」 | 现状 **技能 101 / 敌人 44 / 道具 19 / 科技 21（99 点）** | 各配表 `skills.json` / `enemies.json` / `items.json` / `tech.json` |
| ⑪ | 附录 B 死字段清单「位置」列旧快照 | 「`skills.json（88 条）`」「`enemies.json（14 条）`」「`tech.json（16 条）`」「`items.json`：9 条全无」 | 现状 **`skills.json` 101 / `enemies.json` 44 / `tech.json` 21 / `items.json` 19** | 同上（附录 B 把计数当证据，旧数即错证据） |
| ⑫ | D171 饰品 `price` 起点 | 「锻造费 = `price × 0.9`」 | 与 D167 同源，现值 **× 0.6**（见 ⑥） | §11.A.1；`accessories.json` 12 条全 0.6 |
| ⑬ | §6 科技树「关键节点」段 + 「⚠️ 定价失衡」段 | 「`tech_party_5`(5) → `tech_party_6`(12)…」；「筋力/耐力 6 点…最优解永远是最后买敏捷系。定价方向反了」 | 成本按 `tech.json` 改写；**定价已反转** —— 线性维单价 **≈0.83 点/属性**（最便宜线性项 `远征装备` **0.65**）vs 饱和维 **≈0.33**（最贵饱和项 = 四系 I 级）⇒ **"线性又便宜又强"已解除，"永远最后买饱和系"亦失效** | `tech.json` 21 条实测成本；D297 |
| ⑭ | §6「效果类型」表 | 「`stat_bonus` 8」「`ap_regen_odd` 1」 | 现 **`stat_bonus` 11** / **`ap_regen_odd` 2**，并加「合计 **21**」行（原表合计 17 ≠ 16） | `tech.json` 按 `effects[].type` 计数 |
| ⑮ | §5.4 道具表 | 仅列 9 行；药水 `value` 80 / 260 | 补足 **现役 19 行**；药水改 **% maxHp（8 / 20）**；新增「限购」列（归途石 / 升阶谕令 = **1/趟**） | `items.json` 19 条；`consumables-draft-v1.md` v3 |
| ⑯ | 附录 B `skills.json…（87 条仍带值）` | 「87 条仍带值」 | **已核＝正确**（101 条中 `cooldown` 非零者确为 **87** 条），**无需改** | `skills.json` 实测 |
| ⑰ | §5.4 表尾三条 ❌（「锻造系统」「装备系统」「饰品无数值读取点」） | 「❌ 3 个 material…无锻造系统 / 配方表」「❌ 无装备系统（无装备槽、无 `equip`）」 | 前两条**已过时**（配方表 + `src/ui/ForgePanel.ts` 已就位、出战位双槽 `partyEquip` 已实现，D292/D313）⇒ ❌ 转 ✅；**新增第 3 条 ❌**：饰品数值**无任何读取点**（类 1 / 类 2 在修；类 3 / 类 4 归批次 2） | `accessories.json` / `src/ui/ForgePanel.ts` / `src/types/save.ts`；§13.1 R4 |
| ⑱ | §4.5 招募表「花费」行 + §4.5「金币序列」行 + §5.1 货币「金币」行；附录 C 第 56 条；`docs/formulas.md` §6（公式行 + 算例表 + 常量来源表） | 「金币 `120 × 1.35^n` + 科技点 2」「120 → 162 → 219 …」「招募（120×1.35ⁿ）」「已决 D67：改为固定 500 金币」「`120`、`1.35`」 | 现行为**平价 5 000**（D67 取消递增、`recruitCostGrowth` = 1；D306 → 3 500、D310 → 5 000）；**四处落点**原值均以「改前」保留；附录那条补后续指针、原句不删 | D67 / D306 / D310；配表 `recruitGoldCost` / `recruitCostGrowth`（**待改**；非 R4 类） |

> **共性归类（便于回溯）**：①⑦⑧⑩⑪⑬⑭⑮ 是"**陈旧计数 / 旧快照未回填**"；② 是"**措辞口径**"；③⑨ 是"**同一数字用在两个语境**"（达成率 vs 总价 / 内容 vs HUD）；④ 是"**配表能力边界 → 旧立论被新机制（`family`）取代**"；⑤⑥⑫ 是"**后续修正（D246 / 经济修正）覆盖了早期数值**"；⑯ 是"**核实后确认原值正确、无需改**"；⑰ 是"**过时缺口已落地（❌ 转 ✅）+ 暴露现役新缺口**"；⑱ 是"**裁决链未落地 + 现状节描述已废模型**"（非 R4 类）。这些正是本项目反复出现的失效模式，记此收口。

---

---

## 附录 A · 已废弃设计（README v1.0 存档）

---

---

`README.md` 描述的是 v1.0，与现状冲突。**保留此表是为了避免后人再被误导。**

| v1.0（README 描述） | 现状 |
|---|---|
| 10 个职业 + `classes.json` | 职业表已从配表**删除**（全项目零命中） |
| 15 个角色 | **40 个角色** |
| 60 个固有技能（passive） | 固有/被动技能**全部删除** |
| 每角色从 5 技能池随机抽 2 | 每角色**固定 2 个专属主动技** |
| 普攻 / 技能 / 防御 / 道具 / 逃跑 | **普攻已从玩家 UI 移除**（`kind:'attack'` 仅作兼容按防御处理） |
| MP（`vitals.mp`） | v3 迁移已删除 |
| 按速度单独行动 | **双方阶段制**；`speed` 是死字段 |
| 技能冷却 | **已删除**，改为 AP 升耗 |
| 图鉴 `CodexPanel` | **已删除** |
| 引用《迷宫 RPG 游戏设计文档 v1.0》 | **该文档不存在** |

---

## 附录 B · 死字段 / 孤儿模块清单

| 位置 | 字段/模块 | 证据 |
|---|---|---|
| skills.json（101 条） | `cooldown` | `@deprecated`，无逻辑读取（87 条仍带值） |
| skills.json（101 条） | `cost` | 旧 MP 消耗，无读取方 |
| skills.json（101 条） | `element` | 无战斗逻辑读取 |
| skills.json（101 条） | `escalation` | 全 undefined → `permanent` 分支永不进入 |
| skills.json | `type:'passive'` | 101 条全 active |
| skills.json | `target:'ally'` | 0 次使用 |
| balance.json | `baseSpeed` | 无读取方 |
| balance.json | `levelUpTechPointBonus` | 无读取方 |
| enemies.json（44 条） | `speed` | 不参与任何计算 |
| mapNodes.json（4 条） | dungeon 节点的 `unlocked` | 改查科技，字段无效 |
| tech.json（21 条） | `repeatable` / `maxRank` | 一条未配，复购分支死代码 |
| items.json | `stat` | 19 条全无 |
| save.ts | `rngSeed` | 只写不读 |
| save.ts | `player.level` / `player.exp` | 从未读写 |
| `src/entities/` | 全部 4 个文件 | **无人 import** |

---

## 附录 C · 待决问题池（100 轮问答）

> 每轮解决 4 条，逐轮把答案写回正文并标记「已决」。

### C1 顶层设计（阻塞级）

| # | 问题 | 状态 |
|---|---|---|
| 1 | **生命池语义**：共享一份？每人一份？战斗与迷宫是否同一份？ | ✅ **已决 D1**：真共享一份，战斗与迷宫同一份 |
| 2 | **通关与结局**：要不要必打 Boss？要不要结局演出？第 6 层之后是什么？ | ✅ **已决 D2**：每图第 6 层固定 Boss，击败即通关；不做全局结局 |
| 3 | **团灭代价**：只扣 20% 金币是否太轻？角色要不要真的会死？丢哪部分进度？ | ✅ **已决 D17 + D21 + D22（定稿）**：`maxFloor` 归零 + `activeRun` 清空、**`cleared` 保留**；背包物品按总价值凑满 **50%** 随机损失（**材料计入**）；送回营地 |
| 3b | D17 澄清三项 | ✅ **已决**：① 全清但保留 `cleared` ② 按总价值凑一半 ③ 材料计入 |
| 4 | **核心体验支柱** | ✅ **已决 D4**：AP 战术分配 / 迷宫探索与风险管理 / 收集与养成 |
| 4b | **目标受众 / 单局时长** | ✅ **受众：中度核心**（D16）；✅ **时长已修正为 30–60 分钟**（D29，原 40–90 与实测冲突） |
| 5 | **多敌是否永久废弃**（决定 `enemy_all` 与 `count` 的命运） | ✅ **已决 D3**：永久单敌，清理 `enemy_all` 与 `count` |
| 6 | **属性克制要不要做**（7 种 element 现在纯装饰） | ✅ **已决 D11**：删除 `element` 字段，不做克制 |
| 7 | **敌人是否随层数变强**（当前断裂） | ✅ **已决 D7 + D14**：数值缩放 + 按层切池都做；线性系数已定（HP ×(1+0.5(N−1)) 等） |
| 8 | **音频 / 新手引导 / 目标引导** | ✅ **已决 D31 + D38 + D39 + D43** |
| 8a | **新手引导**（AP/升耗/阶段制/迷雾无说明入口） | ✅ **已决 D43**：**上下文触发提示**（首次遇到某机制时弹一次） |
| 8b | **目标引导** | ✅ **已决 D38**：任务列表面板 |
| 8c | **音频** | ✅ **已决 D39**：BGM 3 首 + 5 类核心音效 |
| 41 | **README 重写** | ✅ **已决 D44**：按 GDD 重写 |
| 42 | **根目录调试产物**（约 25 个文件） | ✅ **已决 D45**：全部清理 |
| 43 | **存档导入/导出** | ✅ **已决 D46**：接 UI 入口 |
| 44 | **定时自动存档 / `playTime` 恒 0** | ✅ **已决 D47 + D68**：不做定时存档；但 `playTime` 计时需接线 |
| 45 | **战斗中存档** | ✅ **已决 D69**：不支持，退回战斗前 |
| 46 | **存档槽** | ✅ **已决 D70**：改为命名存档列表 |
| 47 | **`combo_bonus` 空转** | ❌ **已由 D180 推翻（误判）**：功能正常，`skillComboBonus()` 真的读 `effects` 并加进 `comboRate`；D56 的"逻辑不可达"前提已过期。**但 D56 的"修复"因此无需执行，改判为"不修"** |
| 48 | **敌方 AI 参数** | ✅ **已决 D57**：只放技能；普通 1–2 次/轮、精英 2 次且每 3 轮 1 次大招、Boss 专属释放顺序；并搬进配表 |
| 49 | **「迟缓」陷阱无效** | ✅ **已决 D58**：删除类型，改伤害/传送 50-50 |
| 50 | **战斗道具无限制** | ✅ **已决 D59**：改为消耗 1 AP |
| 51 | **宝箱金币恒为 45** | ✅ **已决 D60**：改跟难度档 N1–N6 挂钩 |
| 52 | **埋伏恒取最弱敌** | ✅ **已决 D61**：改为按权重随机 |
| 53 | **传送破坏可复现** | ✅ **已决 D62**：改用项目 Rng |
| 54 | **重复角色无价值** | ✅ **已决 D64**：重复 → 该角色升 1 级 |
| 55 | **`rarityWeights` 死权重 70%** | ✅ **已决 D65**：补上 1★ / 2★ 角色模板 |
| 56 | **招募成本递增 1.35^n** | ✅ **已决 D67**：改为固定 500 金币（**后续 D306 抬到 3 500、D310 定为 5 000 ⇒ 现行为平价 5 000**） |
| 57 | **三个材料无用途 + 锻造缺失** | ✅ **已决 D54**：加锻造系统 |
| 58 | **敌人数量** | ✅ **已决 D52**：扩充到 30+ |
| 59 | **大地图 event/shop/boss 节点** | ✅ **已决 D53**：补上商店与事件；删除 boss 类型 |
| 60 | **内容量目标** | ✅ **已决 D72**：先做模板，后续可随意扩建 |
| 61 | **多结局 / 周目** | ✅ **已决 D73**：不做结局，改为加**全局难度系统**（4 档 + 噩梦 4 机制） |
| 62 | **本地化 i18n** | ✅ **已决 D74**：不做 |
| 63 | **4 个新 Boss 的具体数值与技能** | ❓ 待定（D48/D51 已定定位与"2–3 个全新技能"，具体数值未定） |

### C2 战斗与数值

| # | 问题 | 状态 |
|---|---|---|
| 9 | `apCost` 是否分化（现在全 1） | ✅ **已决 D5 + D15**：按 `power` 阈值分档 ≤60→1 / 61–120→2 / >120→3 AP（`hits≥2` 按等效 power 折算） |
| 10 | `escalation:'permanent'` 是否启用、给哪些技能 | ✅ **已决 D12 + D18**：启用，**人工挑 5–8 条最强技能**（保持稀缺感）；具体书目待挑 |
| 11 | `submitAction` 是否补 AP 校验 | ✅ **已决 D9**：补校验 |
| 12 | AP=0 时是否允许防御/道具/逃跑 | ✅ **已决 D9**：**不允许**（保留现状，有意取舍） |
| 13 | 同一单位一回合是否限制出手次数 | ✅ **已决 D9**：限 1 次 |
| 14 | 暴击率同时进倍率（双重收益）是否有意 | ❓ |
| 15 | buff 生效路径统一（耐力/魔力/敏捷系） | ✅ **已决 D10**：只留攻击类，其余删除 |
| 15b | **D10 的连带问题**：被掏空的纯辅助技（如「白金礼赞」`power=0` 只剩 spirit buff）是**改造**还是**删除**？ | ✅ **已决 D13**：改造成攻击 / 治疗 / 削弱敌方 `attack`·`defense` |
| 15c | **D8 与 D16 的张力复议**：中度核心定位是否需要更重的失败代价？ | ❓ **下一轮优先** |
| 16 | 经验双重倍率是否有意 | ✅ **已决 D19**：只保留个体倍率，去掉队伍层（魔力转为纯个体属性；`team.expMultiplier` 变死数据；升级变慢需重算） |
| 17 | 备战成员是否应获得经验 | ✅ **已决 D28**：**不分**（维持现状）。与 D19（升级变慢）叠加，换人成本进一步升高，属有意取舍 |
| 15d | **阶段制下是否保留"行动者高亮"** | ✅ **已决 D30**：只保留**「已出手」瞬时反馈**（120ms 抬升即回落），不表示"轮到谁"；原三件套废弃 |
| 18 | 0.35 / 0.004 / 回气 10 / 1.5+暴击率 等魔法数字的出处 | ❓ |
| 19 | 敌方 AI 的 65% / 40% / 45% 血线是否有意 | ❓ |
| 20 | Boss/精英的定义与标记 | ✅ **已决 D6 + D40**：**全部新增 4 个专属 Boss**（每图 1 个，含专属技能与掉落，打 `isBoss`，普通池排除）；现有守护者/暴君/深渊之王**降为精英怪** |

### C3 经济与成长

| # | 问题 | 状态 |
|---|---|---|
| 21 | 科技点产出与总价的匹配 | ✅ **已决 D20 + D23 + D41**：目标「首次全清点满约 80%」→ 总价 **166 → 79 点**（等比 ×0.48，逐条值见 §0.5 第 12 轮）、首通合计 **≈45–50 点**、其余靠战斗（`techPoints` 随层缩放，与 D14 合并算） |
| 29 | **后期升级过快**：腐朽王座一次下探升 8.59 级 | ✅ **推荐 `expExponent = 1.9`**（1.8 只到 6.64，不达标），**但必须搭配方案 A**；待你最终确认 |
| 29b | **前期成长缺口**（无根回廊 ★5 均值 0.19、零成长 81%） | ✅ **推荐方案 A**（无根 `density` 0.035→0.060 + 三小怪 exp 20/17/23）→ 拉回 **2.00 级**、零成长归 0、且后期不变；待确认 |
| 29c | **前期台阶丢失** | ✅ **已决 D35**：苔痕回廊 `enemyDensity` **0.045 → 0.064** → 四图 2.00 / 2.75 / 3.64 / 5.88，差值严格递增。**备选（上调中阶敌人 exp）已实测否决**——静谧墓所共享骷髅兵/食尸鬼各 22%，会把静谧从 3.64 推到 3.83–4.40 |
| 29d | D35 副作用：苔痕回廊成为战斗数最多的图（41.9 场 vs 无根 38.2 / 静谧 31.9 / 腐朽 34.7） | ✅ 已接受并记录 |
| 30 | **D16 时长修正** | ✅ **已决 D29**：改为 **30–60 分钟** |
| 22 | 科技定价失衡（筋力/耐力更便宜更强） | ❓ |
| 23 | 升级曲线 vs 推荐等级对照 | ❓ |
| 24 | 稀有度价值（初始只差 6 点，潜力随机） | ❓ |
| 25 | `rarityWeights` 死权重清理 | ❓ |
| 26 | 永久属性道具（秘药）产出上限 | ❓ |
| 27 | 要不要锻造/装备系统（3 个 material 目前无用途） | ✅ **已决 D54 + D80–D85**：要。装备仅两槽（**饰品 + 消耗品**）；消耗品按背包数量、每次使用 **1 AP**；饰品被动，分 4 类（属性加成 / 连击·暴击·强化率 / 战术效果 / 每回合触发 1 次） |
| 28 | 金币回收手段是否足够 | 🟡 **部分已决 D107**：新增**营地训练场**（金币换经验）作为第二条主要出口；其余出口（商店 D53/D90、招募 D65、锻造 D88）尚待定价核算 |
| 31 | **`defendReduction` 名不副实** | ✅ **已决 D104**：当前 0.5 是「加到防御率」的加法项（`DamageCalculator.ts:231`）且 `clamp(0,0.95)`，实际减伤随自身防御率递减、日志"降低 50%"夸大。改为独立乘区「本回合受伤 ×0.8」并耗 1 AP |
| 32 | **我方单回合总输出 ∝ N²（战斗 1 回合结束）** | ⏸ **已实测确认（§0.6），已决 D108：延后整体重算**。根因是 `team.attack` = 全队筋力之和（∝ N）**且**每名成员各出手一次（再 ∝ N）。重算须排在 D40 / D52 / D80–D85 之后，并纳入饰品加成与 D5 的 `apCost` 分档 |

### C4 表现与规格

| # | 问题 | 状态 |
|---|---|---|
| 29 | **角色卡尺寸**：规格书二值互斥（96×128 vs 168×340），代码第三值（168×400） | ✅ **已决 D24**：采用 **168×340**；`ui-style-spec.md:164` 的 96×128 标注废弃；代码需从 400 回调 |
| 30 | **AP pip** | ✅ **已决 D25**：菱形 20×20 · 两排 5+5 共 10 槽（代码现值形状是平行四边形，**需改**） |
| 31 | **技能栏形态** | ✅ **已决 D26**：156×46 横向条（代码现值）；`ui-style-spec.md` 的 36×36+冷却扇形标废弃 |
| 32 | **卡牌抬升时长** | ✅ **已决 D27 + D30**：120ms，用于**「已出手」瞬时反馈**（非"当前行动者"），原三件套废弃 |
| 33 | **升耗提示规范** | ✅ **已决 D37**：补写进 `battle-ap-ux-spec.md` —— gold 数字 + 右上角 9px `+N` + AP 不足时 danger 红 + 2px 斜线 + 技能条 `SPENT_FILL #272C35`（色条 alpha 1→0.35）。**零代码改动** |
| 33b | **迷宫色名** | ✅ **已决 D36**：改规格书对齐代码（`mazeXxx` 为真源，`wall` 标废弃） |
| 34 | **动效清单**：规格书已成文项已整理进 §8.8；待补的是"哪些界面完全没有视觉规格" | ❓ |

### C5 工程与流程

| # | 问题 | 状态 |
|---|---|---|
| 35 | AP / 宝箱 / 事件概率是否移入配表 | ❓ |
| 36 | 是否补 `engines.node` 与 Node 版本声明 | ✅ 已加 `>=24` |
| 37 | 死字段是清理还是保留 | ✅ **已决 D42**：**全部清理** —— `skills.cooldown`/`cost`、`enemies.speed`、`balance.baseSpeed`/`levelUpTechPointBonus`，以及 **`src/entities/` 整个孤儿目录** |
| 38 | `README.md` 是否按本 GDD 重写 | ❓ |
| 39 | 是否清理根目录散落的 scan-*/tmp-* 调试产物 | ❓ |
| 40 | 存档导出/导入是否接 UI 入口 | ❓ |

---

## 附录 D · 相关文档

| 文档 | 内容 | 何时看它 |
|---|---|---|
| `docs/formulas.md` | **公式速查表 + 常量来源对照**：全项目所有实际参与计算的公式（含文件:行号与代入实数的算例）；以及「改某个数值要去哪里改」的三列对照（配表 / TS 常量 / **硬编码在逻辑里**），末尾附"把硬编码项纳入配表的最小改动方案" | 调数值、查公式、判断某个数能不能改 |
| `docs/art/ui-style-spec.md` | 美术与 UI 视觉规格（配色、切角、字号、组件） | 做 UI 前 |
| `design/ux/battle-ap-ux-spec.md` | 战斗 AP 的交互规格 | 改战斗界面时 |
| `README.md` | **已过期**，见附录 A | ⚠️ 仅供了解 v1.0 历史 |

---

*本文档由代码与配表逆向整理生成。标 ❓ 处等待作者裁决；裁决后请更新对应章节并把附录 C 该条标为「已决」。*
