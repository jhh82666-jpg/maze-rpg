# 迷宫 RPG · 技术手册

> **本手册是项目的技术总文档。** 合并日期 **2026-09-22**；由原先 38 份 md / 1810 KB 提炼而成。
> 裁决原文（D1–D456）在 `design/gdd.md`；旧文档全量归档在 `.backup/docs-v1-2026-09-22/`（逐字节校验 38/38）。
> 冲突时**以 `design/gdd.md` 为准**。章节来源见各章开头。


---

## 0. 本手册与项目概览

> 本章来源：`README.md` + 仓库实况扫描（2026-09-22）。
> **本手册是项目唯一的技术总文档**，由原先 38 份散落文档提炼合并而成（合并范围与归档位置见 §0.1）。

### 0.1 这份手册是什么、旧文档在哪

2026-09-22 之前，项目里有 **38 份 Markdown / 1810 KB**，分为：设计裁决总账、22 份实施草案、若干规格书、脚本实测报告、证据记录。
它们之间存在大量重复（同一规则在多份草案里各写一遍）与版本堆叠（同一系统有 v1…v6.2 多个草案），但没有一份能"单独读懂整个项目"。

本手册把它们的**定稿结论**提炼成一份连贯文档，并**保留 `D` 编号**作为与源码互引的锚点。
处理原则：

| 处理 | 对象 |
|---|---|
| **并入手册后删除** | `design/drafts/` 全部 22 份草案；`scripts/_mc-*-out.md` 3 份脚本实测产物；`_portrait-swap/*.md` 3 份过程报告；`docs/portrait-swap-2026-09-22.md` |
| **保留原文件**（被源码直接引用／是仓库门面／是既往证据） | `README.md`、`design/gdd.md`、`docs/formulas.md`、`docs/art/ui-style-spec.md`、`design/ux/battle-ap-ux-spec.md`、`docs/evidence/**`、`image/_work/design-matrix.md` |
| **全量备份**（逐字节校验 38/38） | `.backup/docs-v1-2026-09-22/` —— 保留原目录结构，任何删除都可从这里还原 |

> 🔴 **回溯约定**：本手册每章开头都写明"本章来源"，指向已归档/已删除的原件名；原件在 `.backup/docs-v1-2026-09-22/` 里可找到同名文件。
>
> **关于残留的旧文档名**：`design/gdd.md` 与本手册中仍会出现 `xxx-draft-v1.md` 这类**旧文件名**（合计约 250 处）。它们是**来源注记**（说明"这条结论出自哪份草案"），**不是**指向现存文件的链接 —— 那些文件已归档到 `.backup/docs-v1-2026-09-22/` 的对应相对路径下。**不要按这些名字去找现存文件。**

### 0.2 项目是什么

一个**数据驱动的 2D 迷宫探索回合制 RPG**，纯前端单机：

- **技术栈**：Phaser **3.90** + TypeScript **5.9** + Vite **7.3**；Node **≥ 24**。
- **无后端、无外部美术资源**：所有 UI 与画面由 Phaser 的 `Graphics` **程序化绘制**；唯一的位图资产是 40 个角色的立绘（见 §50）。
- **数据驱动**：角色 / 技能 / 敌人 / 道具 / 科技 / 地牢 / 大地图节点 / 事件 / 任务全部来自 `src/data/tables/*.json`，改配表即可扩展内容。
- **核心循环**：营地准备（编队 / 商店 / 锻造 / 招募 / 科技）→ 选图与难度 → 迷宫中探索（战斗 / 事件 / 宝箱 / 商店）→ 通关或撤退 → 回营地结算。
- 🔴 **改 `src/data/tables/*.json` 之后必须重跑 `node scripts/gen-tables.mjs`**，否则 `*.gen.ts` 不更新、改动不生效。改 `src/config/*.ts` 不需要。

### 0.3 目录结构

```
maze-rpg/
├─ src/
│  ├─ config/     常量与绑定：constants(Palette/尺寸/纹理 key)、graphics、balance、
│  │              mazeDifficulty(迷宫难度阶梯唯一真源)、accessibility、saveKeys、campLayout
│  ├─ data/
│  │  ├─ tables/  *.json 配表 + gen-tables 产出的 *.gen.ts 镜像
│  │  └─ loaders/ DataLoader（配表校验）
│  ├─ entities/   实体（队伍、单位等）
│  ├─ scenes/     MainMenu / Preload / Camp / WorldMap / Dungeon / Battle / UIScene
│  ├─ systems/    纯逻辑系统：Combat / Damage / Probability / Maze / Character / Tech /
│  │              Save / Inventory / Quest / Event / ShopPolicy / TeamStats …
│  ├─ types/      共享类型定义
│  ├─ ui/         面板与控件：Widgets(唯一遮罩实现) / ScrollList / ShopPanel / HUD …
│  └─ utils/      eventBus、rosterSort、listHit 等
├─ public/assets/portraits/   240 个立绘产物（40 角色 × 3 档 × PNG/WebP）
├─ image/  image2/            立绘源图（**文件名即角色名**）
├─ scripts/                   自检、探针、生成器、蒙特卡洛模拟（见 §0.4）
├─ design/                    gdd.md（裁决总账）+ ux/（UX 规格）
├─ docs/                      本手册 + formulas.md + art/ + evidence/
└─ .backup/                   历次备份（含文档全量归档 docs-v1-2026-09-22/）
```

### 0.4 命令速查

| 命令 | 作用 |
|---|---|
| `npm install` | 安装依赖 |
| `npm run dev` | 开发服务器（默认 `http://127.0.0.1:5173`） |
| `npm run typecheck` | `tsc --noEmit`（strict + noUnusedLocals） |
| `npm run verify` | **纯 Node 逻辑自检（不依赖浏览器）**：配表闭合 / 概率 / 伤害 / 迷宫 / 角色 / 存档 / 立绘资源 … |
| `npm run gen:tables` | 改完 JSON 配表后生成类型化 TS 镜像 |
| `npm run build` | 类型检查 + 生产构建 → `dist/` |
| `npm run build:offline` | 产出 `dist/index.offline.html`（IIFE 内联，可 `file://` 双击游玩） |
| `npm run smoke` / `debug` / `shots` | **headless 运行时**测试／交互调试／UI 截图（**都要求 dev server 已在跑**） |
| `npm run check` | `verify` + `typecheck` |

### 0.5 验证链（改完代码按此顺序）

1. `npm run typecheck` → **必须 exit 0**
2. `npm run gen:tables`（**只在改了 `src/data/tables/*.json` 时**）
3. `npm run verify` → **全部项通过**（当前基线见 §0.6）
4. **先起 `npm run dev`**，再跑 `npm run smoke` / `scripts/zz-*.mjs` 探针
5. `npm run build`

🔴 环境铁律（违反会得到**假红**，很容易误判成代码回归）：

- **必须用系统 Node 24**（本项目实测：托管 Node 22 跑 `verify.mjs` 会红 1 项 —— `phaser3spectorjs` 缺失）。
- 本机 bash **缺 `ls`/`cat`/`grep`/`head`/`tail`/`sleep`/`dirname` 且无管道**；一律用 Read/Grep/Glob 工具或 Node/Python 脚本。**非 ASCII 路径下不要写 `.ps1`/`.bat`**（编码会把路径搞乱）。
- **探针不自起 dev server**。没起 server 就跑探针 ⇒ 满屏 `ERR_CONNECTION_REFUSED` + 断言全红，那是**环境假失败**。
- 不要用 `( cmd &)` 子 shell 起常驻进程（命令返回时被回收）；用工具的后台运行能力。
- 跑批产物要核对**时间戳晚于被测脚本**，否则陈旧输出造成假绿。
- 🔴 **`scripts/verify.mjs` 同一时刻只许一个写者**：并发往文件末尾追加断言会把前一节的标题覆盖掉（断言还在、也计数，但归属错乱）。新增只在**末尾**加节号。

### 0.6 当前基线（2026-09-22）

| 项 | 值 |
|---|---|
| `tsc --noEmit` | **exit 0** |
| `scripts/verify.mjs` | **250 项 / 失败 0** |
| 立绘产物 | **240 个**（尺寸 256²/384×512/512×1024，全 RGBA，alpha 覆盖 33.7%–90.6%） |
| D 裁决 | **D1–D456**（全文见 `design/gdd.md`；提炼见 §10） |

### 0.7 文档权威顺序（冲突时怎么办）

1. **`design/gdd.md`** —— 已冻结的裁决总账（D1–D456）。**任何文档与它冲突，以它为准。**
2. **本手册** —— 对 gdd.md 与各草案的提炼。若本手册与 gdd.md 有出入，以 gdd.md 为准，并请顺手修正本手册。
3. **`docs/formulas.md`**（GDD 附录 D：公式速查与常量来源对照）、`docs/art/ui-style-spec.md`、`design/ux/battle-ap-ux-spec.md` —— 专项规格，被源码直接引用。
4. **草稿** —— 已归档至 `.backup/docs-v1-2026-09-22/`，仅作历史参考；其中**可能保留着已被否决的方案**，不可当结论用。

### 0.8 `D` 编号索引

`D` 编号（`D1`–`D456`）是设计与源码之间的**互引锚点**，全仓有 **1500+ 处**引用（`src/systems/CombatSystem.ts` 65 处、`src/systems/DamageCalculator.ts` 38 处、`src/types/save.ts` 37 处、`scripts/verify.mjs` 153 处 …）。
- **全部 456 条规则的定稿内容**见 §10（按主题分组，每条带编号）。
- **编号 → 章节** 的定位表见 **§99 附录：D 编号索引**。
- 🔴 本项目文档纪律：**引用位置一律用「小节号 + 原文措辞」，禁用行号**（裁决 D435：行号会漂、措辞不会）；**编号必须带文件名前缀**（曾经发生过"两套 G 表混为一谈"的事故）。

---

## 10. 设计规则总账（GDD D1–D456）

> 本章来源：`design/gdd.md`（4037 行 / 24 小节 / 4 轮裁决）。原文保留在仓库中，本表为定稿规则的结构化提炼。
> 编号纪律：D 编号与原文**一一对应、未重排、未重新编号**；每条恰好出现一次。原文实际出现的去重编号为 **453 个**（**D213 / D214 / D348 在原文中无对应条目**；**D353 被两条不同裁决共用**，本表合并为一条并注明）。凡原文标「已作废 / 已被取代」者，在该条后注 `（已作废，见 Dxxx）`。

### 10.1 顶层定位、通关定义与交付节奏

- **D2** 只做单图通关，**不做全局结局**：每地牢第 6 层设固定 Boss（不可回避），击败即该图通关；4 图全清不触发结束演出。
- **D4** 核心体验支柱定为 3 条：① AP 战术分配 ② 迷宫探索与风险管理 ③ 收集与养成；**明确不含**「硬核生存压力」。
- **D16** 目标受众「中度核心」，愿为一次下探做完整规划（原定单次 40–90 分钟，被 D29 修正）。
- **D29** 单次时长改为 **30–60 分钟**（实测一次 6 层下探 25–60 分钟，中位 35–40；速通仅 16–20 分钟）。
- **D72** 内容量目标：先做模板 / 结构，后续可随意扩建，不为当前数量纠结。
- **D74** 不做本地化 i18n，文案继续硬编码在代码与配表。
- **D75** 第 20 轮检查点后继续提问（推进第 21–30 轮）。
- **D138** 最大内容缺口由队员起草、lead 审核（12 件饰品 / 4 个 Boss 专属技能与数值 / 4 条族群特性数值），不碰游戏代码。
- **D154** 不碰代码，继续问答到第 100 轮，设计做完再一次性实现。
- **D194** 剩余 50 轮先做经济核算 —— 金币产出口径至今没有实测支撑。
- **D201** 剩余内容工作打包派给队友：消耗品 8–10 种（D193）+ 事件扩充（D192）+ 15 条辅助技改造（D200）。
- **D230** 剩余轮次不预设分块，按当轮缺口走。
- **D237** 四图清完即「玩完」，**不加终局内容**；终局靠刷材料 / 科技点 / 饰品。连带三条：统计要有长期凭证、支线必须可重复、锻造材料必须可重复获取。
- **D240** 落地顺序：先做不依赖战斗长度的内容（批次 1：数据 / 配表 / UI），把「需长战才生效」的标为待 D108（批次 2）。
- **D243** 剩余 40 轮保持原速（每轮 4 问）。
- **D265** 终局循环（刷什么、刷到什么时候）先不定义；若实测玩家流失，第一优先是补终局循环（D265 翻案），不是加新内容。
- **D301** 剩余 30 轮继续问答。
- **D303** GDD 保持单文件 `design/gdd.md`；草案降为附录引用（推导过程留独立 md）。
- **D308** 终局只有 ≈1.7 小时科技点目标：**不改、接受** —— 不翻案 D265、不扩树；刷完后玩家转向 12 件饰品（3.75 趟）与精英材料。
- **D311** D244 的草案并入等全部草案冻结后一次性做（避免并到一半又来改）。
- **D329** 接受终局形态：每趟重做任务 27 分钟 / 自循环 3.0–3.7 小时；支线金币占一趟收入 29% 也接受。
- **D347** 第 80 轮检查点不设体检，直接继续问答。
- **D352** GDD 合并先合已冻结的 4 份（饰品 / Boss、战术、文案、存档），其余等冻结后再合。
- **D357** 边合并边问答（把已冻结的并入 §11，同时每轮继续问缺口）。
- **D386** 最后 16 轮继续问答。
- **D389** 最终交付物五件套：① GDD 单文件（含 §11 内容附录）② 风险清单 ③ 落地顺序（批次 1/2/3 + 顺序约束）④ 验收矩阵 ⑤ 代码缺口单（19 条 G + 落点家族）。
- **D390** 给实现者的交接文档单独出一份一页纸（必修缺口 + 顺序约束 + 批次清单 + 验收命令）。
- **D391** 第 90 轮做一次全形一致性总检：按系统分组重扫跨组依赖与内部矛盾，并专查「被后续裁决覆盖的旧裁决」。
- **D392** 验收矩阵派 economy-analyst 产出（它手里有 `mc-difficulty` / `mc-combat-length` / `mc-gold-economy` / `mc-techpoint`）。
- **D395** 消耗品 9 种与事件 11 种由 lead 逐条审（约多花 2–3 轮）。
- **D396** 敌人双路重写稿交齐后由 lead 对账裁定（取一份，差异项 lead 定），**不允许两份并存**（否则成第二个真源）。
- **D418** 体检报告只写进 GDD（§0.5 末加一节「裁决覆盖台账」），不另建文件。
- **D420** 最后十轮分四块：① 第 90 轮体检 ② 90–95 敌人两稿对账并并入 §11 ③ 95–98 风险清单 / 验收矩阵 / 交接文档 ④ 99–100 全文收口与交付。
- **D444** §12 实现避坑清单独立成节（16 处纠正 + 11 条必修缺口 + 3 条顺序约束 + 3 类引用纪律集中一处），作为实现者第一份要读的东西。
- **D445** 最后 5 轮分法：96–97 敌人对账并入 §11 / 98 风险清单 / 99 验收矩阵 + 交接文档收口 / 100 全文一致性复查 + 交付。
- **D452** §12 的范围四块全放。
- **D454** 两份交付物已落盘：`design/drafts/acceptance-matrix-v1.md`（12 条硬门 → 脚本 / 命令 / 判据 / 依赖）与 `design/drafts/handbook-to-implementer` 类的 `design/drafts/handoff-to-implementer-v1.md`（含 11 条必修缺口）；原派队员但未落盘，由 lead 补写。
- **D456** 五件套齐备：GDD 单文件 / §13 风险清单 / §14 落地顺序 / 验收矩阵 / 交接文档。

### 10.2 战斗结构、回合与 AP

- **D3** 永久单敌，清掉群体概念：承认 `enemy_all ≡ enemy`，25 条群体技改为明确单体技，**删除 `enemy_all` 目标类型与 `count` 字段**（含 0.35 系数折算逻辑）。
- **D5** 技能 AP 消耗按强度分档：弱技 1 AP / 中技 2 AP / 大招 3 AP（按 `power` 与 `hits` 分档）。
- **D9** 补 AP 校验（`submitAction` 拒绝 AP 不足）+ **同一单位每回合限出手 1 次**；但**保留**「AP=0 时无法防御 / 道具 / 逃跑」（不修，属有意取舍）。（出手次数口径后被 D242 修订）
- **D12** 启用 `escalation:'permanent'` 永续升耗，配给 power 最高的几个技能，形成「越用越贵」的取舍。
- **D15** `apCost` 分档阈值：`power ≤60 → 1 AP`；`61–120 → 2 AP`；`>120 → 3 AP`；`hits ≥2` 按等效 power（`power × hits`）折算。
- **D18** 永续升耗只给少数最强技能（人工挑 **5–8 条**），保持稀缺感（候选：花魁杀 200 / 死神镰刀 205）。
- **D56** `combo_bonus` 空转**不修** —— 前提已过期（见 D180：功能本来就是好的）。
- **D59** 战斗中使用道具改为**消耗 1 AP**（与技能统一进 AP 经济，堵住无限嗑药）。
- **D104** 防御保留指令但改为「**消耗 1 AP + 本回合受到的伤害 ×0.8**」；⚠️ 必须**新增一个独立的伤害乘区**（在 `clamp(0,0.95)` 之后），否则 `defendReduction`(0.5) 作为「加到防御率的加法项」会被 clamp 吞掉、玩家感知为零。
- **D108** 我方输出 ∝ N² **暂不修**，等 Boss（D40）/ 敌人扩充（D52）/ 装备饰品（D80）全部落地后一次性整体重算；重算必须排在 D80–D85 之后，并把饰品加成与 D5 的 `apCost` 分档一并纳入。在此之前不再引入新的我方输出来源。
- **D123** D5 分档**不补偿**，「防御就是兵底」，让 AP 分配成为真正的核心决策（该措辞后被 D124 修正）。
- **D124** **承认没有兵底**：AP 耗尽就结束我方阶段；D123 的「防御就是兵底」改为「防御是**有 AP 时**的最优保底，不是无 AP 时的退路」，D9 的「AP=0 不能防御」原样保留。连带：`chooseAllyAutoAction()` 在 AP<1 时必须改为返回 null / 跳过该单位。
- **D140** 消耗品槽**每角色 1 个、装 1 种**；使用消耗品**占用该角色本回合的出手机会**（与 D9 一致）并消耗 1 AP ⇒ 防御 / 技能 / 道具三者共享同一个「每回合 1 次」行动额度。
- **D147** 自动战斗保留且**无限制**（含 Boss 战）；前置必修：无可用技能时不得无条件返回 `defend`（否则会提交 AP 不足的非法动作）。
- **D227** `temp` 升耗**不改累加、保持恒定 +1**（只有 `permanent` 才累加，而 88 条技能里 `permanent` 一条都没启用）；每回合 AP 预算（3 → 8，上限 8）是唯一限制器。
- **D229** 3 处「战斗长度导致机制空转」继续问答，统一标为「**待 D108 重算后验证**」，不因它们去改战斗长度。
- **D242** D9 修订为「**每个技能每回合限使用 1 次**」：同一单位一回合内可放多个不同技能（通常 2 个），但禁止「A → A」；允许「技能 A → 技能 B」两连。
- **D261** D242 **只约束我方**；敌方按 D151 的类别规则（普通 1 次 / 精英 2 次且每 3 轮 1 次大招 / Boss 按 D111 循环）。
- **D262** D242 后的叠层实测：叠 3 层 = **1–2 回合 / 3–5 AP / 需 3 个不同技能 × 3 名不同角色**；第 1 回合可达的只有火力校准 / 结构加固 / 相位增幅（机动节拍 4 AP、弹道校准 5 AP 需 2 回合）。
- **D268** D108 重算定义为「把战斗从 **1–2 回合拉到 5–10 回合**（TTK 中位）」，并要求给出 TTK / TTD 目标区间 + 验收脚本（复用 `scripts/mc-difficulty.mjs`）；全 96 格不得出现「1 回合团灭」。
- **D278** 升耗**不堵**（轮换技能以规避升耗正是 D242 想要的玩法）；战术的节流**只由 AP 成本承担**（零代码改动）。
- **D279** 防御**不参与**战术叠层（若允许会绕过 D242 的「3 技能 × 3 角色」限制），战术与防御的 AP 竞争不动。
- **D304** D108 验收改按敌人类型分级设硬门：**普通敌 TTK 2–4 回合 / 精英 5–10 回合 / Boss ≥10 回合**（取代 D268 的单一阈值）。
- **D354** Boss 图（只有 1 个敌人）应使用 **3.90 的 DPR**，而不是 5 敌图的 6.92（Boss 图修正 ×1.5）。
- **D356** 批次 2（战术叠层 / 深渊吞噬 / 饰品防御类 / 精英二动 / R2 相位反射片）**立即解锁**（三档硬门已达成，团灭 0/528）。
- **D433** D304 的「Boss ≥10 回合」登记为「**待 4 只 Boss 进配表后补验**」（96 格 TTK 全落在 [2,10]，从未到过 11）；**明令不得用普通怪的 `rounds === 10` 当替身**。

### 10.3 技能、文案与 debuff 口径

- **D10** buff **只保留攻击类，其余全部删除**（耐力 / 魔力 / 敏捷 / 幸运 / 灵性类 buff 及其技能效果一并移除）。（后被 D200 改写为战术体系）
- **D11** 删除 `element` 字段：不做属性克制，技能表移除该字段及其 UI 展示，`SkillElement` 类型与相关 UI 一并清理。
- **D13** 被 D10 掏空的技能**改造成有效技能**：改成攻击 / 治疗 / **削弱敌方 `attack`·`defense`**（敌人只有这两个属性参与计算，只有这两种减益真实有效）。
- **D55** 角色技能数保持 **2 个**（共 80 条），角色差异靠技能组合与属性，不加第 3 技。
- **D146** 技能 / 物品描述改为配表手写（字段名后被 D181 更正为既有 `description`）。
- **D175** **92 条技能文案的推进** ⇒ ✅ **派队员批量写**（见 GDD §0.8）。
- **D181** **不新增 `desc`** —— 配表已有 `description` 且被前端读取（`DataLoader.ts`）；实际动作是「停止程序拼接、改读 `description`」并核对 80 条是否已填充。
- **D182** 两条代码发现归入 D42 清理批次、且**必须在 D5 分档落地前修**：① 88 条技能里 **70 条**「伤害 + `effects`」的 `effects` 完全不结算；② **11 条** `ally_all` 技能只作用于施法者本人；两者不能靠 D1 顺带解决。
- **D199** 技能文案**先修代码、再填文案**：文案按设计意图写，但 #12b / #12c / #10（治疗用 attack 而非魔力）列为落地前置条件，未修前不填，否则文案就是在说谎。
- **D233** 8 条「削其它属性」的本体 debuff **一律改为削弱敌方 `attack` 或 `defense`**（敌方只使用这两个轴，`speed` 不参与出手顺序，魔力 / 幸运 / 灵性 / 敏捷在敌方侧零读取点）。
- **D234** Boss 侧 4 条我方 debuff **同源**，按 D196 口径写成「我方**合计** XX −N」并按图分级 **−4 / −6 / −9 / −12**（我方这些轴有战斗效果，照常生效）。
- **D235** 治疗量口径正式定为**攻击力派生**：`治疗量 = 队伍攻击力 × 倍率 + 灵性 × 0.5`（魔力在战斗内完全无效，坚持魔力派生会让治疗量崩塌）。§10 第 10 条就此关闭。
- **D236** 黑裙礼赞 / 绯冠加冕的 `enhanced: buff magic +10` **改轴为灵性 +10**（并入相位增幅），因 D226 已删除魔力战术。
- **D249** 11 条 debuff 的量纲**一律改为百分比**（沿用绝对值 12/14/15 时浅层削 −33%~−60%、深层只 −5%）；最终区间：本体削敌方 `attack` **−15% ~ −22%**，`enhanced` 削敌方 `defense` **−12% ~ −15%**（真正需改数值的是 **10 条**）。
- **D250** 「自代价」类 debuff **保留原样、不改为削敌**（`清雪居合` 的自身敏捷 −8 是设计上的取舍代价）；D233 的适用范围**限定为「作用于敌方」的 debuff**。
- **D251** 11 条伤害句里的「魔力」表述**全部去掉**（伤害公式只有 `team.attack × power`，魔力战斗内无效，文案不得出现公式里没有的量）。
- **D252** lead 勘误：黑裙礼赞的 `magic +14` 在 **`effects`**、`enhanced` 是敏捷；**只有绯冠加冕的 `enhanced` 是 magic**（原派单说反了）。
- **D269** 技能**开局全解锁**（每角色 2 个技能一开始就都能用）⇒ `slot.unlocked` 归档为死字段，并入 D42 清理清单。
- **D351** 清除敌方战术的技能**从现有 80 条里挑 2–3 条改造**，不新增技能位（保持 D55 的每角色 2 个）。
- **D428** 玄铁因 D344 而失去自增益**接受**；批准「**沉默之势**」继续保留（玄铁是默认解锁的 ★4，砍掉会让新手队伍少一个技能）。
- **D447** 三条新技能的 `apCost` **维持 1**（把「40% 的概率」当第一旋钮 —— 涨价只影响能不能放，不影响放了的收益）。
- **D448** 维持「沉默之势」而非「家务全役」（玄铁是默认解锁 ★4）。

### 10.4 战术系统（我方 / 敌方）

- **D200** 15 条 `power=0` 纯辅助技的**整套 buff 体系改为「战术」体系**：同种效果统一为同一战术、**每战术上限 5 层**、**持续 3 回合**、每次释放后该战术的持续回合数**重置为 3**（而非叠加时长）。这条实际改写了 D10，§10「D10 掏空一批技能」的待办风险就此关闭。
- **D205** 战术系统独立引入 CD、技能系统保持无冷却 —— 两套系统从此各自独立（**已作废，见 D219**）。
- **D211** ⚠️ 待澄清：原文仅在 D216 与 §11.B 中被引用为「饰品给战术」，**无独立裁决行**（其含义由 D216 / D217 承接：饰品可授予战术）。
- **D212** ⚠️ 待澄清：原文仅在 D280 中被引用为「`tech_ap_cap` 的三方撞名」，**无独立裁决行**（由 D280 改为「行动扩容」解决）。
- **D215** 层数靠「**反复释放技能**」堆叠（技能本身无 CD，可重复释放，每次授予 1 层）；**饰品给的是一次性的**；节流由 AP 成本 + 升耗承担，不是靠战术自己的 CD。
- **D216** 「战术与饰品合流」的含义 = **饰品类 3 就是「授予战术的饰品」**（三件即授予机制型战术者），与 D211 完全合流。
- **D217** 战术的两个来源：**饰品**战斗开局授予 1 次（免费、不耗 AP）；**技能**每个各自绑定 1 个战术，释放该技能时附带授予（耗该技能的 AP）。
- **D218** 加**机制型战术**（除六维属性战术外，另有一批非属性机制型战术）。
- **D219** **取消战术 CD，改为「持续回合」**（D205 作废）；D200 的「CD 重置」正式解释为「**持续时间刷新为 3 回合**」。⚠️ 术语纪律：从此禁用「战术 CD」说法 —— 属性战术的节流 = AP 成本 + 升耗，时长的控制 = 持续回合。
- **D220** 饰品给予的机制型战术**持续时间无限（整场生效）**，与 D80「饰品被动生效」一致。
- **D221** 机制型战术清单派队员设计。
- **D222** 属性战术每层数值锚**按百分比**（每层给「该维的 x%」，自动适配等级），不用固定小值（定标结果：2.3%/层，第 4/5 层 ×0.67，满层 ≈10%）。
- **D223** 叠层上限**先降到 3 层**（3 回合内可达），5 层作为 D108 重算后再上调的预留（理由后被 D241 改写）。
- **D226** **删掉魔力战术** ⇒ 属性战术 6 → **5 个**（筋力 / 耐力 / 敏捷 / 幸运 / 灵性），因魔力在战斗内完全无效。
- **D228** 机制型战术与饰品合流时**战术侧是真源**（饰品只声明「授予哪个战术」，数值全在战术表），避免双重结算。
- **D231** 战术授予**一个字段都不加**，由现有 `effects[].stat` + `enhanced[].stat` **机械推导**（≈6 行）；**例外**：鹿角祝福 / 天使赐福只有 heal、无任何 `stat` ⇒ 显式兜底「纯治疗技 → 授予相位增幅」。
- **D232** 文案字数上限**放宽到 ≤55 汉字**（≤40 保不住战术授予句，会导致两条纯授予技的 `description` 变空串）。
- **D241** 层数上限**维持 3 层**，但理由改写为：瓶颈不是叠不上去，而是**回本需 ≈23 回合**（把一个战术叠满所花的 AP 改用于直接打伤害，要约 23 回合才追平），远长于实际战斗 ⇒ 上限设几层无实质差别，取小值以免误导。
- **D259** `power=0` 技不掷强化按「**修代码**」处理（配表已写 `enhanced`，那是设计意图；删句等于砍掉 15 条技能的效果），**文案保留强化句**，列入 §10 与 D42 清理批次。
- **D260** 层数硬上限 = **承载技能数**（`min(3, 承载技能数)`），不补承载技能；「预留 5 层」改为「**须先补承载技能才可启用**」。
- **D263** 各战术的承载技能数：机动节拍 **6** / 相位增幅 **6** / 结构加固 **5** / 火力校准 **4** / **弹道校准 3（零冗余：可可 + 琳 + 白雪三人必须全上阵）**。
- **D277** 概率三轴（连击 / 暴击 / 强化）的**百分比化**接受：百分比在全等级都干净，固定值必然随等级漂移（L4 只 +2.2pp 是因为基线低，这是**正确**行为）。
- **D317** 战术栏容 **9 条**（5 属性 + 4 机制型），UI 按 9 条设计（**已作废，见 D328**；且「4 机制型」为陈旧计数，实为 6）。
- **D326** 战术栏**新做一个 HUD 显示在血量 HUD 上方**：有战术存在时才显示、每个战术独立 HUD、**敌方左对齐 / 我方右对齐**、**最多 5 条战术**。
- **D328** HUD 上**最多 5 条** ⇒ D317 的「9 条按 UI 设计」作废：**战斗 HUD 只放属性战术（每侧 5 条）**，机制型战术不占该 HUD（另见 D332）。三段口径：内容 = 11（5 属性 + 6 机制型）/ 战斗 HUD = 每侧 5 条（只属性战术）/ 非属性战术 = 另一处（D332）。
- **D332** 机制型战术放在**战斗界面的另一处**（不占战术 HUD），具体位置由 UI 规格定。
- **D337** **敌方也有战术** ⇒ 需新增「敌人授予战术」机制，并与 D151（普通 1 次 / 精英 2 次 / Boss 循环）协调；Boss 强度需重算。
- **D340** 敌方战术来源 = **靠敌人自己的技能授予**（与 D217 对称）。
- **D341** 敌方战术的叠层与持续与**我方完全同规则**：上限 3 层 / 持续 3 回合 / 每次释放刷新。
- **D342** 敌方战术内容 = **属性共用（5 个）+ 每图族群 1 个专属** ⇒ 敌方共 **9 个战术**（⚠️ 属性位中只有火力校准 / 结构加固真正共用，另 3 个是「敌方版」，见 D383）。
- **D344** 我方清除敌方战术的手段 = **新增若干我方技能**：① 造成伤害的同时**按概率清除敌方 1 条战术**；② **不造成伤害但必定清除敌方 1 条战术**。不新增消耗品，不给逆向解离针加双向。
- **D345** 族群专属战术的强度 = **同档但特色化**（数值与属性战术同档，靠效果类型立差异，不靠更高数值）。
- **D346** 敌方层数的正反馈矛盾**允许差异化**（不再严格要求敌我完全对称：敌方战术的持续时间可以更短 / 上限可以更低）。
- **D366** 敌方侧缺 4 个读取点：**不补读取点，改设计** —— 敌方战术只做能用现有公式表达的：✅ 直接改 HP（治疗 / 护盾）✅ 改出手次数 ✅ 改敌方自己的 `attack` / `defense` ✅ 直接伤害 / `damage_cut`；❌ 概率轴（combo / crit / enhance 敌方是硬编码的）❌ `speed` ❌ 魔力 / 幸运 / 灵性 / 敏捷。
- **D383** 为敌方**另做 3 个替代战术**（用 `attack` / HP / 护盾实现，效果对齐敏捷 / 幸运 / 灵性但走不同机制）⇒ 敌方仍共 **9 个战术**（5 属性位 + 4 族群专属，其中 3 个属性位是「敌方版」，须另起名另写效果，不能复用我方名字）。
- **D425** 敌方族群专属战术改挂「该图的精英 / 虚影 / Boss」，普通敌不挂 —— **已作废，见 D437**。
- **D437** 改判 D425 为「**占位式**」：族群授予技**占既有技能位**（普通敌第 2 位；精英 / 虚影 / Boss 占 3–4 位之一），**任何档位都不新增第 N+1 条** ⇒ D153 逐档成立（2 / 3 / 3–4）、82 条预算不膨胀（按「(敌人,技能) 对」计数）、普通战仍有战术可叠、32 只普通敌零返工。
- **D438** **虚影不挂族群战术**（D415 已定虚影 `family = 'echo'`，不吃任何族群被动；给不属于本图族群的虚影挂本图族群战术，读感是错的）。
- **D439** 敌方战术的存储 = **两个字段，都不落盘**：`enemyTacticGainThisRound: Set`（每战术每轮 +1 层的闸门）与 `state.enemyTactics: Record<tacticId, { stacks, turnsLeft }>`（层数真源）；与我方 `state.tactics` 对称（D3 永久单敌 ⇒ 敌方只需一份）。
- **D440** `clear_tactic` **不能走 `effects`**：必须做成「**伤害结算之后的独立结算钩子**」（否则踩 #12b，D344 的通道① 静默失效），并入 D272 修复批次的顺序约束。
- **D446** 族群授予技维持「占位式」（= D437）；承载者 = **每图 1 精英 + 1 Boss = 8 个**（虚影带不了族群技，因其四图共用 id），三份文件已自然对齐，零改动。

### 10.5 数值、成长曲线与伤害上界

- **D19** 经验倍率**只保留个体倍率**（`expMultiplierFor(card)`），**去掉队伍层**倍率（原两级放大砍掉 49–63%）；副作用：`team.expMultiplier`（Σ魔力 × 0.02）变为死数据，魔力成为纯个体属性，升级节奏明显变慢。
- **D28** 备战成员**不分经验**（维持现状，永远 0 经验，换人即等级断层）；与 D19 叠加后换人成本进一步升高，属有意取舍。
- **D32** 升级曲线调整：`expExponent` **1.6 → 1.8**（待实测复核），目标把腐朽王座一次下探的 8.56 级压下来；⚠️ 已知副作用是前期缺口恶化（无根 ★4 降到约 0.8、★5 约 0.7）。（**已被 D33 定案取代**）
- **D33** 升级曲线定案：**`expExponent = 1.9` + 方案 A**（无根回廊 `enemyDensity` 0.035 → 0.060，黏液怪 / 蝙蝠 / 巨鼠 exp 14/12/16 → 20/17/23）⇒ 腐朽 8.59 → **5.88**、无根 ★4 1.00 → 2.00、★5 0.19 → 1.25、全档位零成长率 **0%**、静谧 / 腐朽数字不变，纯改配表零代码（密度那一半后被 D257 作废）。
- **D34** 前期台阶靠**提高苔痕回廊密度**（而非降低无根密度），让第二张图重新明显高于第一张（具体数值由 D35 定）。
- **D35** 苔痕回廊 `enemyDensity` **0.045 → 0.064**（★4 升级数 2.01 → 2.75；四图 2.00 / 2.75 / 3.64 / 5.88，相邻差值 +0.75 / +0.89 / +2.24 严格递增，可接受带 0.062–0.066）；备选「上调中阶敌人 exp」已实测否决（静谧共享骷髅兵 / 食尸鬼各 22%，会把 3.64 推到 3.83–4.40）。（**已作废，见 D257**）
- **D99** 数值复核已完成：D73 × D14 乘算**未触发任何警戒线**，但原因是战斗本身 1 回合结束、难度倍率被淹没；真正的结构性问题是「我方单回合输出 ∝ N²」。
- **D183** 饰品类 2（连击 / 暴击 / 强化率）的降幅**不降**（保留 **+4pp / +10pp / +10pp**）⇒ 15% 硬约束放宽到 ~22%，伤害轴最坏 **+21.98%** 被接受（后被 D355 改写）。
- **D197** 伤害轴真实上界接受 **+24.13%**（实操 +21.96%），D183 的上界正式改写为「**伤害轴 ≤ +25%**」，两个口径都要留档（前者按「饰品全队生效」，后者按「只作用于佩戴者」）。（**已作废，见 D355**）
- **D203** 成长曲线重定标：接受实测为新基准 —— 四图 **2.69 / 3.43 / 4.67 / 7.33**，L4 → **L14.7**；D33 的目标值据此改写，**配表不动**（台阶形状仍成立：+0.74 / +1.24 / +2.66 严格递增）。
- **D225** D33 / D35 是否执行 ⇒ **按新基准重新标定一套**（重跑含密度缩放的 MC，产出新的参数组合）。
- **D256** 成长曲线采纳 **Z 方案**：`expExponent` **1.6 → 1.9**；三小怪 exp → **25 / 21 / 29**；**密度不动** ⇒ ★4 四图 **2.01 / 2.88 / 4.10 / 6.30**（台阶 +0.87 / +1.22 / +2.20 严格递增），★5 = 1.41 / 2.04 / 3.26 / 5.41，连打四图 L4 → **L13.3**。
- **D257** **D35 的密度动作作废**（无根 0.035 → 0.060 / 苔痕 0.045 → 0.064 全部撤销）：它的达标靠「无根 32 → 54 场」的战斗数暴涨买单，会造成「越浅的图打得越多」的荒谬结构。
- **D327** 浅层组 / 深层组的经验金币**不拉开** —— 两组只区分技能与难度，经验金币由 D14 的按层缩放统一决定。
- **D355** 饰品的两套口径**只留实测值 +9.7%**，**推翻 D197 的 +24.13%**（后者建立在「饰品作用于全队」的假设上，与实测不符）。
- **D374** D350 之后的科技点 80% 卡线派队员复核（D350 同时改变了金币与科技点的收入结构）。
- **D402** D374 结案：**树价 99 维持**；80% 口径 = 首通 38 + 主线 14 + 支线（24.3 / 29.3），**宝箱从来不在这个和里**（D245 已裁「宝箱不产科技点」）；配表残留的 0.15 点/箱是未落地旧值；S9 实测 **1.09 次/趟**，落在 0.8–1.4 带内。

### 10.6 经济：金币、科技点、招募与训练场

- **D20** 科技点经济**组合方案**：① 下调科技树总价 ② 提高科技点产出 ③ 产出与难度挂钩（深层给得更多）。
- **D23** 科技树经济定标：**首次全清点满约 80%** —— ⚠️ 该「80」是**首清四图的达成率目标**（带 75–85%），**不是树的总价**；树总价落地为 **99 点（21 节点）**，实测 **76.3 / 99 = 77.1%**（原文「约 80 点」与 D264 的「约 95」均为被取代的旧值，见 D297）。
- **D41** 科技树定价**等比缩放 ×0.48**（166 → 79 点），保留原有相对价值关系（16 条逐条新价：队伍扩编 I 2 / 招募许可 I 1 / 深层探索 4 / 战术扩容 5 / 队伍扩编 II 6 / 奇袭节拍 7 / 兵刃淬炼 3 / 重甲锻造 3 / 疾行训练 3 / 幸运徽记 3 / 秘法研习 3 / 灵性启迪 3 / 招募许可 II 5 / 深渊入口 9 / 远征装备 8 / 深渊核心 14）。（**已被 D297 的 99 点取代**）
- **D64** 获得重复角色 ⇒ **该角色升 1 级**（非分解、非突破）。
- **D65** 补上 **1★ / 2★ 角色模板**，让 `rarityWeights` 的死权重（70%）完整生效。
- **D66** 稀有度价值**不动数值**，靠**技能倍率**体现（接受 3★ 可能强于 5★ 的随机性）。
- **D67** 招募定价**固定 500 金币一次**，取消 `1.35^n` 递增（科技点 2 沿用）。（后续由 D306 → 3 500、D310 → 5 000）
- **D92** 任务奖励**发金币 + 科技点**（成为 D23 科技点产出的一部分，缓解「只能靠刷」）。
- **D107** 营地新建设施「**训练场**」（**金币换经验**），成为 D23 之外金币的第二条主要出口。
- **D110** 训练场汇率：**固定汇率，金币 → 经验**（直接注入角色经验条，走正常升级曲线）；候选 1 金币 = 1 / 2 / 3 经验（数值由 D112 / D116 / D120 逐步定案）。
- **D112** 训练场汇率改为**随等级递增（越后期越贵）**，非固定值：每点经验的金币成本 = `1 + floor((当前等级 − 1) / 3)`（等级 1–3 → 1 金/经验，4–6 → 2，7–9 → 3…）。（**已被 D116 / D120 取代**）
- **D116** 训练场汇率改为「**系数固定，靠经验曲线自然变贵**」：`升 1 级总价 = expForLevel(当前等级) × 固定系数`，不再按等级分档。
- **D120** 训练场汇率系数定为 **0.5（1 金币 = 2 经验）**：5→6 级 **210 金**，11→12 级 **740 金**（关闭 D116 的口径冲突）。（⚠️ 实测后调整为 **1 金 = 1 经验**，见 D404）
- **D143** 难度经济倍率**只管战斗奖励**（敌人掉落的金币与经验）；**宝箱、任务奖励、训练场不受影响**。
- **D144** 科技点**也受难度倍率影响**（简单 ×1.25 / 困难 ×0.75），口径统一为「难度 = 收益与风险的交换比」；处理：79 点作普通难度基准，倍率**只作用于产出端不作用于价格端**（否则双重调节），「首清点满约 80%」限定为普通难度。
- **D156** Boss 科技点预算**并入 `firstClearTechPoints`**：四图从 3/3/5/10 上调到 **10/12/14/16（合计 52）**；`enemies.json` 的 `techPoints` 只表示**重复击杀**的小额值（其 4/5/6/8 已由 D246 作废）。
- **D202** 经济参数（价格与产出）**授权 lead 据实测直接调整**，只向用户汇总结果。
- **D204** 科技点超发（一把四图 ≈228 点 vs 树 79 点）按建议砍：**普通怪 `techPoints` 归 0、精英 1 点、Boss 重复击杀 2–3 点**；首通仍走 `firstClearTechPoints`。
- **D245** **宝箱不再产科技点**（宝箱应给金币 / 道具），其余来源不动。
- **D246** ① 首通 52 拆成「**首通 38 + 主线任务 14**」（每图仍是 10/12/14/16，总量不变，消除重复计费）；② Boss 重复击杀**以 D204 为准（2–3 点）**，D156 的 4/5/6/8 **作废**。
- **D253** 科技点总量尾差：**扩树节点而非涨价**（16 项 → 约 30 项，节点单价维持）。
- **D254** **精英完全不给科技点**（只给金币 ×2 / 经验 ×2 / 必掉材料）。
- **D255** **Boss 重复击杀不给科技点**（首通仍走 `firstClearTechPoints`，D246 拆为首通 38 + 主线 14）⇒ 终局刷科技点的唯一途径是「重复完成支线任务」。
- **D264** 科技树**扩到 ~95 点**（新增 4–6 个节点，节点单价维持）⇒ 首清 76 点 = **80%**，留 20% 给终局刷（最终由 D297 定为 99 点）。
- **D271** **潜力不可提升** —— 潜力是角色的「底子」，生成时随机一次即固定；它是 D66「稀有度价值靠技能倍率体现」的一部分。
- **D284** **难度不影响任务奖励**（与 D143 一致：倍率只管战斗奖励），避免 D204 / D254 / D255 砍完产出后又被难度二次扰动。
- **D297** 科技树定为 **99 点**（79 + 5 个节点 × 4，新增「疾行训练 II / 幸运徽记 II / 秘法研习 II / 灵性启迪 II / 混战节拍」，全部使用现有 `effect.type`，零代码）⇒ 首清 76（严格口径）= **77%**、中位 81 = **82%**。
- **D300** 任务草案四处修正采纳：① **`S3 净空回廊` 恒不达成**（第 1 层无支路 ⇒ 陷阱 / 事件 / 宝箱必踩）⇒ 改为比例型；② `S9` 阈值 1200 → **2500**；③ `S4` 满金币能一趟结算 4 次 ⇒ 收紧；④ **招募给的 2 点科技点归 0**。
- **D305** S7 / S8 给科技点**不违反 D254 / D255**（那两刀砍的是「敌人掉落轨」`enemies.json` 的 `techPoints`，不是「任务奖励轨」，而 D92 明文就是发金币 + 科技点）。
- **D306** 招募价 1 500 → **3 500**（一趟四图 18 439 + 支线 11 680 + 主线 10 100 = 40 219 金，原价使招募几乎免费）。
- **D310** 招募价 1 500 → **5 000**（招满 20 名需 **100 000 金**，成为真正的长期目标）。
- **D335** 任务奖励**不因招募涨价而重核**（已按 D297 的树价 99 标定过，且 D284 已定任务奖励不受难度影响）。
- **D363** 支线金币占比（29% 是误读，实际 61.9%）⇒ 采纳**组合 B 降到 41.9%**（保持 ★3 饰品锚点 600 金 / 10 分钟，支线金币压到战斗的 72%）。
- **D371** **钱袋套利现在就修**：`item_coin_pouch.value` **120 → 60**（买 100 / 用 120 是无上限印钞，且 D350 让钱袋占比上升，不修会削弱 D350 的全部效果）。
- **D372** 七项价格 **0 项需要改数值**（★5 = 0.58 趟腐朽、招募 **2.9 次/趟**、训练场出口 **39.8%**、归途石 1.5%）⇒ 此前在旧收入下定的一整套价格在 **−18.8%** 的新收入下仍成立（**价格锚在比例上：绝对值会漂、比例不会漂**）。
- **D375** D350 的前期补偿：**接受，不补偿** —— 新手期本来就没多少东西要买（★3 饰品 600 金 ≈ 6 层收入），D350 砍的是「密度」不是「单价」。
- **D377** 接受 **13.1 循环**（招满 20 名 3.7 + 12 件饰品 0.91 + 6 名主力 L4→L20 共 13.1）；不追求极端 32.7 循环（其 86% 花在训练场）。
- **D401** **D350 与 D367 是互相补偿的一对**：D350 单独 −3.23 点、D367 单独 +3.12 点、合计净 **−0.11 点**（树 99 达成率 77.1% → 77.0%）⇒ **将来只回退其中一个会出问题，必须成对处理**。
- **D403** 科技树**不能洗点**（与 D115「难度不可改」同类的「选择有重量」原则）。
- **D404** 训练场**不限次** —— 兑换率（**1 金 = 1 经验**）本身就是限制，不再叠一层。

### 10.7 商店、装备、饰品、锻造与消耗品

- **D54** 三个材料（魔铁矿 / 深渊结晶 / 魔核）**加锻造系统**（按配方合成道具或强化），消除「描述写锻造材料但无锻造」的空头承诺。
- **D77** 锻造产出 = **材料打造装备 —— 引入装备系统**（不是合成消耗品）。
- **D78** 商店**卖消耗品为主 + 随机折扣**（每次进店随机 1–2 个折扣商品），与 D73 噩梦机制 2 的「+4 栏位 / 稀有度上升」兼容。
- **D80** 装备槽**只有 2 个：饰品槽 + 消耗品槽**，**不设武器 / 防具**等其它装备类型；角色的攻防仍完全来自六维（攻击 = Σ筋力×5、生命 = Σ耐力×20），饰品只做「加成与特效」，不改变攻防的基本结构。
- **D81** 装备属性按 D80 的分类设定。
- **D82** 装备获取 = **锻造 + Boss 掉落 + 商店** 三条途径 ⇒ `ownedAccessories` 必须与 `forge.forgedCount` 分开。
- **D83** 装备**不可强化**（属性固定，想更强就去打更强的 Boss 或锻造更好的）。
- **D84** 饰品**先做 12 件**（四类各 3 件），建立模板，后续按 D72 随意扩建。
- **D85** 饰品**沿用现有 1–5 星**体系（与角色、道具一致）。
- **D86** 消耗品槽每角色一个，**每场战斗使用次数不限**（受背包数量与 1 AP/次 约束）。（后被 D140 修订为「装 1 种 + 占出手」）
- **D88** 锻造配方**每件饰品独立配方**（指定材料与数量），不按稀有度套模板。
- **D90** 商店**按标价原价卖**（买价 = `price` × 1.0，背包卖出 = `price` × 0.5），形成 2 倍买卖差价。
- **D94** 商店**每次进店刷新**（商品与折扣都刷新）。
- **D95** 战斗道具 UI：**角色卡上显示携带的消耗品与剩余数量，点击即用**（消耗 1 AP），与技能横条并列。
- **D113** 商店商品构成 = **消耗品 + 锻造材料 + 随机 1 件稀有饰品（每店限购 1 件）**。
- **D122** 12 件饰品分布 = **4 类各 3 件**（属性加成 / 连击·暴击·强化率 / 战术效果 / 每回合触发 1 次），每类 3 件供玩家组 build。
- **D126** 饰品**只靠锻造**（D88 每件独立配方）。（**已作废，见 D128**）
- **D128** 保留商店限购、**改 D126** ⇒ 饰品**两条来源**：**锻造（主力，属性更强）+ 商店每店限购 1 件随机稀有饰品**；落地必须保证锻造产出的饰品属性明显强于商店随机件。
- **D129** 商店商品档次**跟层数档位 N1–N6 挂钩**（与 D60 宝箱金币同一口径）—— 第 6 层不会卖第 1 层的药水。
- **D133** 锻造件 vs 商店件的强度差 = **无区别**（不给差异，靠「商店限购 1 件」克制）；风险已接受并留复核点（若刷店成为主路径，再给锻造件加 2 类效果）。
- **D141** 饰品槽**每个角色 1 个**（6 人队 = 最多 6 件同时生效；饰品可自由装卸）。（语义由 D292 修订为「每个出战位 1 个」）
- **D150** 消耗品槽**实时从背包取**（槽位指定「用哪一类消耗品」，数量实时扣除；背包用完则槽位变灰）。
- **D158** 12 件的 **3 档 × 4 类网格**接受（角色级槽下装满同类 3 槽必然是 I + II + III 各一，天然封住「堆同一件」）。
- **D167** 锻造 = **材料 + 金币费**（`price × 0.9`）。（⚠️ **现值 = 材料 + `price` × 0.6**；0.9 会让「自造」比「直购」贵 15–21%、方向反了，已由 §11.A.1 正文的经济修正覆盖，`accessories.json` 12 条 `forgeCostRate` 全为 **0.6** —— 看到 0.6 是正确值，不要改回 0.9）
- **D171** 饰品 `price` 起点：**★3 = 600 / ★4 = 1 500 / ★5 = 4 000**（与既有物品量级药水 80 / 大药水 260 明显拉开）；锻造费 = `price × 0.9`。（⚠️ 同 D167，**现值 × 0.6**）
- **D184** **取消 AP 类饰品**（`T1 过载抑制器` / `T2 冗余电容` / `R3 终末采样器` **三件整体删除**）⇒ 12 件变 9 件；作废理由：它们加的是**行动次数**，与伤害轴**相乘**，六槽最坏组合 **+148%**（R3 单件即 +73%）。⚠️ 副作用：T1 原本是唯一正面回应 D123 / D124「AP 耗尽没有兵底」的饰品，删除后该冲突回到无缓解状态。
- **D187** 饰品**补足到 12 件**：类 3 补 2 件、类 4 补 1 件，**全部必须是非 AP 效果**，以维持 D122「4 类 × 3 件」。
- **D188** **R2 相位反射片保留**（只约 +2.9%，且需「敌方打得到我」才触发，属**天然受限**的输出，不算 D108 意义上的「新输出源」）。
- **D193** 消耗品体系**大幅扩充到 8–10 种**（回血 / 解毒 / 护盾 / 增益 / 陷阱探测 / 传送等，做成一条道具线）。
- **D195** 类 3 的**档位缺口接受不齐**（类 3 = ★4 ×1 + ★5 ×2），**12 件不变**；「档位」从此只代表**材料档**（决定配方与掉落曲线），不再承诺「每类三档各一」。
- **D196** 「全队筋力 −12」的口径 = **全队合计 −12**（= 当前代码语义：`teamWithBuffs()` 只把出手者的 buff 加进队伍合计属性），**不是每人 −12**；四个 Boss 的 debuff 必须按图分级重定（碎巢母 −4 / 菌王 −6 / 司祭 −9 / 等待者 −12），四处一律写成「我方**合计**筋力 −N」。
- **D198** 类 3 的**乌龟流接受三件同装**，**不加减伤叠加上限**；在裁决记录里写明代价：「**类 3 三件满装 = 放弃全部输出饰品**」。
- **D208** 药水数值口径**改为百分比**（小药 **8% maxHp** / 大药 **20% maxHp**）—— 队伍 maxHp 从 L4 的 1560 涨到 L17 的 6500（4.2 倍），固定值后期必然废掉。
- **D210** D185 让回血类事件失效 ⇒ **回血事件改为「送药水」事件**（直接给消耗品，由玩家自己决定何时使用）。
- **D270** 装备槽**开局就有**（每角色 1 饰品 + 1 消耗品）；「用得上」靠**内容解锁**（先得锻造出饰品），不靠系统解锁。
- **D282** 锻造配方**开局全知**（12 件配方一开始就在锻造界面可见，只是材料不够）⇒ `unlockedRecipes` **不进存档**（真源是配方配表）。
- **D292** 饰品槽**挂在出战位上**（不是挂在角色上）：换人后槽内的饰品**保留在新上阵者身上**，不需要卸下 ⇒ 饰品是「队伍的配置」而非「角色的资产」，换人零成本（修订 D141）。
- **D312** 消耗品槽**也改为出战位**（与 D292 的饰品槽保持一致：两槽都挂在出战位上）。
- **D319** 噩梦机制 2（商店物价 ×2）**作用于全部商店（含营地）** —— 否则玩家能在营地规避机制 2 的代价，机制就形同虚设。
- **D393** 商店那件「随机稀有饰品」**按本层档位 + 小概率上浮**（如 N5 有 15% 出 ★5）—— 不会用商店跳过锻造链，但保留「惊喜」。
- **D394** 商店只卖**本档材料**（N1–N4 魔铁矿 / N5–N6 深渊结晶）；**魔核不在商店售卖**（只能从 Boss / 精英 / 事件获得）。
- **D429** 「探针」的显形范围 = **本层剩余陷阱与宝箱全部显形（含未探索区域）** —— 这是对 D142「迷路是玩法」的**有意例外**：探针**不显出口、不显路径**（「走哪条路」仍靠玩家判断），属「**资源雷达**」不是「地图」。
- **D430** **删掉复活类道具**（`item_feather` 不死鸟之羽 / `item_last_candle` 残烛）—— 现设计里角色不会单独「死」，只有全队共享池的团灭；此后「团灭」是唯一失败形态，任何保险类道具都必须明确它对冲的是 D17 / D21 / D22 的惩罚。
- **D431** 「铜灯」的「已探索锁定」**接受，但限定「本层内」**（走完一层效果消失）—— 只是强化 D142 本来就保留的「三态迷雾已探索档」，不构成变相小地图。
- **D432** 信息类道具**每层每种限用 1 次**（同一层不能连点两盏灯 / 两根针）。
- **D442** D430 删掉复活类后空出的 **★4 消耗品位补一件**：效果 = **群体升一级**（`item_ascension` 升阶谕令）。
- **D443** 商店稀有饰品的**上浮概率 = 5%**。
- **D449** 升阶谕令的语义 = **全队各升 1 级（永久）**（不是本场战斗内等级 +1，两者强度差一个数量级）。
- **D450** 升阶谕令 **600 金 + 每店限购 1 件**（高于其它 ★4 的 300–320，因为「跳级」是稀缺效果）。
- **D451** 升阶谕令与 D107 训练场**共存，但跳级受限**（限购 1 + 600 金）⇒ 训练场仍是「细水长流」的主路径，跳级是「偶得的爆发」；与 D403「选择有重量」的张力是有意识下的。

### 10.8 敌人、Boss、精英、虚影与族群

- **D6** **每图一个专属 Boss**：4 个地牢各指定一个，打 `isBoss` 标记，**普通遇敌池必须排除 Boss**（修复「最终 Boss 当路边怪」）。
- **D7** 「越深越难」**两者都做**：既按层数缩放敌人数值（HP / 攻 / 防），也按层数切换敌人池（两者叠加会同时抬难度，调平衡时注意变量翻倍）。
- **D14** 敌人随层缩放的形状 = **线性**：第 N 层 HP `×(1 + 0.5×(N−1))`（N6 ×3.5）、攻 `×(1 + 0.25×(N−1))`（×2.25）、防 `×(1 + 0.3×(N−1))`（×2.5），奖励小幅上浮；系数写在 `mazeDifficulty.ts` 做成可调项。
- **D40** Boss 来源 = **全部新增 4 个专属 Boss**（每个含专属技能与掉落）；现有 3 个高 HP 敌人（迷宫的守护者 / 骸骨暴君 / 深渊之王）**降为精英怪**（落地后旧 Boss 须砍数值：第 6 层 ×3.5 ×1.5 后路边精英会比该图 Boss 还硬）。
- **D48** Boss 强度定位：**明显更长但不秒人** —— 等级 ≈ 该图推荐等级 **+4**，HP ≈ 同层普通敌人的 **8–10 倍**（落地取中位 9 倍；攻击 = 同层普通敌攻击均值 ×3.0）。
- **D49** Boss 掉落 = **必得专属掉落**（该图「核心」材料 + 大额科技点），作为通关里程碑的实质回报。
- **D50** **Boss 战禁用逃跑**（现任何战斗 3 次失败后成功率即 90%+，会让 Boss 战形同虚设）。
- **D51** Boss 技能 = **每个 Boss 2–3 个全新专属技能**（不复用现有 8 个敌方技能），辨识度优先（数量后由 D186 改为 2–4）。
- **D52** 敌人数量**大幅扩充到 30+**（现 14 个；配合 D7 按层切池与 D48 的 4 个 Boss）。
- **D57** 敌方 AI **重做并搬进配表**：**只放技能、不再普攻**；普通敌人每轮 1–2 次技能、精英每轮 2 次且每 3 轮额外 1 次威力巨大的技能、Boss 有独一无二的专属技能释放顺序（次数口径后由 D151 明确为按类别）。
- **D76** 4 个新 Boss **各自呼应地图主题**（藤蔓 / 孢子 / 亡骸 / 深渊），彼此独立，不做统一叙事。
- **D87** 4 个 Boss **先定名字与技能释放顺序，数值后补**（待 D52 敌人扩充与 D73 难度系统确定后统一填）。
- **D89** 30+ 敌人的分配 = **每图独立敌人池 + 池内按层分两组**（浅层组 / 深层组），配合 D7 的按层切池（与 D148 是同一件事的两种说法，落地只做一次实现）。
- **D93** 新敌人设计方向 = **每图一个主题族群**（丛林兽群 / 真菌孢子 / 亡骸军团 / 深渊眷属），每族 **7–8 只**，视觉与技能呼应地图。
- **D114** **每族加 1 条被动特性**（具体 4 条效果待定）；不做「同族共鸣」那种需要同场判定的复杂联动。
- **D118** 精英怪**按概率随机出现**（建议 **15%** 替换普通遇敌），属性 **×1.5**，**必掉锻造材料**。
- **D119** 族群特性强度 = **强特性，需要针对性应对**（明确改变打法，而非 ±10% 的 flavor）。
- **D121** 4 条族群特性定案：无根「**群体本能**」HP 归零后分裂成「残躯」（25% HP、攻击 ×0.7）；苔痕「**孢子爆发**」被击败时我方全队中毒 2 回合（**死于连击第 2 段及以后则不触发**，奖励精准爆发）；静谧「**亡骸重整**」受到治疗时反而受到等量伤害；腐朽「**深渊吞噬**」每回合开始对我方全队造成当前生命 **8%** 的伤害（第 4 条由「AP 上限 −1」改来，因现行一回合清场而无感）。
- **D125** 4 个新 Boss 的命名由 lead 拟定，与四个族群主题呼应（亡骸军的 Boss 同样带治疗反转等）；数值与专属技能后续再落。
- **D130** 4 个 Boss 命名采用：**碎巢母**（无根回廊）/ **菌王·腐甜之主**（苔痕回廊）/ **不眠的司祭**（静谧墓所）/ **王座上的等待者**（腐朽王座）。
- **D148** 敌人扩编 = **每图 8 只普通敌（浅层组 4 只第 1–3 层 + 深层组 4 只第 4–6 层）**；⚠️ 原文「共 28 只」是**算术错**（4 × 8 = 32，见 D407 / D413）。
- **D149** 精英怪的配表实现 = **独立配表条目**（有自己的名字、技能、掉落，像 Boss 一样定制），而非普通怪 ×1.5 的变体。
- **D151** 敌方每回合出手次数 = **普通敌 1 次 / 精英 2 次且每 3 轮 1 次大招 / Boss 按 D111 的固定循环 + 血量阈值插大招**（把 D57 的「1–2 次」明确为按类别而非随机）。
- **D152** 精英怪数量 = **每图 1 只专属精英，共 4 只**：无根、苔痕各新增 1 只；静谧墓所沿用降级的**迷宫的守护者**，腐朽王座沿用**骸骨暴君**；**深渊之王**同为精英，另作他用（见 D173）。
- **D153** 敌人技能数量 = **普通敌 2 个 / 精英 3 个 / Boss 3–4 个**（含专属技能）。
- **D157** **新增 `heal_max_ratio` 效果类型**（按自身最大生命比例治疗）—— 不新增则四个 Boss 的自愈技在配表层就是废技（现公式只能回 4.7% 最大生命）。
- **D159** 深渊吞噬的触发点改挂「**我方阶段开始**」的强制钩子 `onAllyPhaseStart()`（此举同时救活 D122 的类 4 三件饰品，它们原本也永不触发）。
- **D162** **Boss 享受族群特性**，但必须逐只处理例外（否则有两只 Boss 会出现「无意义」或「自废技能」）。
- **D163** 亡骸重整的定位 = **诱饵**：刻意给亡骸族 7–8 只里的 **3 只配自愈技**，做成「识别与优先级的谜题」。
- **D165** 菌王的孢子例外 = **改为阈值触发**：HP 首次跌破 **50%** 时喷一次孢子（全队中毒 2 回合），与 D111「血量阈值插大招」同一套机制。
- **D166** **司祭不吃「亡骸重整」** —— Boss 是族群的「首领」，不该被自己的族规伤到；它的自愈技正常生效。
- **D169** 碎巢母的分裂例外 = **继承比例 25% → 15%**（1155 → 173），与普通敌的 25% 拉开，避免 Boss 战被推成拖沓。
- **D173** 深渊之王**改名「深渊之王的虚影」**，作为**稀有奖励怪**：**每张图的最后一层**，当该出现精英怪时**有概率替换原精英怪**；战胜后给 **科技点 + 精英怪 2 倍的金币与经验 + 更高稀有度物品**（与 D152 的关系按「**取代**」精英位记录）。
- **D174** **28 只新敌人 + 2 只新精英的推进** ⇒ ✅ **派队员批量产出**（见 GDD §0.8）。
- **D176** 虚影**四图通用**，但不同图的数值要做**衰减**（越浅的图越弱）。
- **D177** 虚影**取代**精英位，概率 **10%**（更稀有）。
- **D178** 虚影的「更高稀有度物品」= **② + ③ 都要**：必掉**秘药（`item_elixir`，永久属性道具）**＋**大量稀有材料**（魔核 / 深渊结晶）。
- **D186** Boss 技能数量 **D51 由「2–3 个」改为「2–4 个」**，司祭保留 4 个技能（总数 **13 条**：3 + 3 + 4 + 3）。
- **D190** 虚影的数值衰减 = **无根 ×0.5 / 苔痕 ×0.7 / 静谧 ×0.85 / 腐朽 ×1.0**。
- **D206** 精英怪的奖励**放大 ×2**（金币与经验按 ×2 结算，与 D118 的「属性 ×1.5 + 必掉材料」匹配）。
- **D258** 腐朽偏快的正确修法 = **把 3 只旧 Boss 移出 `enemyPool`**（它们变成精英 / Boss 后本就不该留在普通池）⇒ 腐朽 7.32 → **6.30**，D2 落地后为 6.85 ⇒ 不再靠抬指数压后期。⚠️ **必须与 D299 同批次落地**，否则中间会有一段「魔核 = 0」的窗口。
- **D266** 精英的价值锚再加「**必掉高稀有度物品**」（魔核 / 秘药）—— 补强刷精英的驱动力（后被 D298 / D331 两次修订）。
- **D273** **保留 D121 的四条语义**，并用一条口径消解「×队伍人数」：**战斗时视作 1V1，「作用于全队」的效果只结算 ×1**（队伍在战斗中被视为一个整体）；队员的「敌方单侧」改口不采纳 ⇒ 修 D272 时「全队」类效果的语义是「结算 1 次到队伍级」（HP 池 / `state.tactics`），不是逐单位各挂一份。
- **D274** **深渊之王彻底降为虚影**（D173 维持）—— 深渊之王不再占 Boss 位，图 Boss 就是「王座上的等待者」。
- **D290** 虚影**四图同名「深渊之王的虚影」**，靠 D190 的数值衰减区分强弱。（⚠️ 落地勘误：实际配表为 **4 个独立 id** —— `enemy_abyss_king_shadow_root` / `_moss` / `_tomb` + `enemy_abyss_king`，按图给不同 `level`；「共用单 id」在配表层做不到，其原始立论已由 D438 的 `family: 'echo'` 取代）
- **D291** 精英**显式标识**：名字前缀 + 配色不同 + 战前预警 —— 与 D119「强特性需针对性应对」同构（玩家必须知道自己在打精英）。
- **D298** **D266 修正为「精英必掉的是【该图档位材料】」**（不是魔核 / 秘药通吃）—— 否则会变成魔核 ×8 / 秘药 ×16 的量级，把整个锻造与永久道具经济冲垮。
- **D299** **王座上的等待者的 `item_core` 掉落 2 → 4** ⇒ 造满 12 件饰品从 **7.5 趟降到 3.75 趟**。⚠️ **必须与 D258 同批次落地**，否则魔核来源为 0、★5 饰品永远造不出。
- **D325** 无根 / 苔痕的新精英命名 = 「**巢守**」（无根回廊）+「**孢母**」（苔痕回廊）。
- **D331** 精英「必掉材料」的口径改为「**该图 `drops` 池里的稀有位**」（不写死「档位材料」）—— 因为档 II 材料从静谧墓所才开始出现，苔痕精英若必掉档位材料只能掉浅层材料，与世界设定冲突。
- **D343** Boss / 精英的强度重算**纳入 D108 一起算**（与 D304 的三档硬门同批：普通 2–4 / 精英 5–10 / Boss ≥10）。
- **D384** 敌人草案丢失的兜底 = **并行重写一版**（从上下文恢复原文 + 新派一名队员独立重写）⇒ 双保险，最后取完整的那份并**对账差异**（数量 / 名字 / 三轴强度 / 族群归属 / 技能 id），差异项由 lead 裁定。
- **D407** 敌人总数**定为 32 只（4 图 × 8）** —— D148 原文写的「共 28 只」是算术错（4 × 8 = 32）；若坚持 28 则须改为每图 7 只（最终由 D413 确认为 32）。
- **D408** 基础防御上限 = **0.20**（不是 0.14 —— 「骸骨暴君重定标后的防御 0.14」被误当成全表上限）。（**已作废，见 D414**）
- **D411** 精英「必掉」= **该图能产出的最高档锻造材料**：无根 / 苔痕 → **魔铁矿**；静谧 → **深渊结晶**（+ 魔铁矿）；腐朽 → **魔核**（+ 深渊结晶）⇒ 同时满足 D118 与 D331。
- **D412** `family` **仍挂在敌人身上**（不挂地牢），**保留跨图复用**（骷髅兵同时在苔痕与静谧）—— 取消复用会把内容量翻倍。
- **D413** 敌人总数 **32 只（每图 8）**，D148 的「28」是算术错，已更正。
- **D414** 基础防御上限 = **0.30**（以现役最高的**石魔像**为准），**现役 4 只一只都不降**（修订 D408 的 0.20）；⚠️ 连带：石魔像在第 6 层是 **0.75 减伤**（我方只能打出 25%），黯甲骑士 0.60 / 虚空祭司 0.50 —— 这是有意保留的「重甲辨识度」，风险被接受。
- **D415** 虚影的 `family` = **独立族群 `echo`** ⇒ 虚影**不吃任何族群被动**，四图之间只有数值差异（0.5 / 0.7 / 0.85 / 1.0）；§11.A.3 族群表需加一行 `echo`（深渊回响），特性栏写「无被动特性」。
- **D416** 现役怪的 **exp / gold 允许调整**（D204 / D254 / D255 砍掉了大量产出，现役怪的奖励需要重对），但**三轴强度不得上调**。
- **D417** 敌人总量**接受 44 个条目**（32 普通 + 4 精英 + 4 虚影 + 4 Boss），与 D72「先做模板后续可扩建」一致。
- **D419** 重甲怪在第 6 层的边界：**接受 0.75 减伤，但必须同步拉高击杀奖励**（否则玩家会绕开重甲怪）；且其 **TTK 不得越过 Boss 的硬门（≥10 回合）** —— 已并入 D108 的验收。
- **D424** 残躯继承比例的真源：**普通敌 = 25%**（D121 原值）／**碎巢母 = 15%**（D169 的 Boss 例外）—— §11.A.3 曾把「碎巢母的例外值」误当成全体值，已修正。
- **D426** 深渊吞噬的「**不致死保护**」**保留** —— 成本为零，且能避免 1 HP 边角下「0 点伤害 vs 反复触发」的语义 bug。
- **D427** 深渊吞噬的量级说明（只记录、不改数值）：旧文写的「每回合 8%」在 D159 阈值化之后实为「**每场 1 次 8%**」⇒ 它已不再是 D119 意义上的「强特性」；该族的压制力应由技能「**深渊汲食**」（6%/回合）承担 —— 这是**有意的分工**，不是缺陷。
- **D434** 「不致死保护」的显式定义：**「深渊吞噬」每场战斗只结算 1 次**（D159 阈值化的结果）⇒ 不存在「反复触发」；再加一条保险：**同一大回合内多次满足触发条件也只结算 1 次** ⇒ 「1 HP 边角下永远打不死」的语义 bug 在设计层被排除。
- **D441** GDD 里三处对 `enemies-draft-v1.md` 的引用**标注为「已被 `enemies-draft-v2-independent.md` 取代」**（该文件已确认不在磁盘上）；敌人侧的两个真源 = `enemies-draft-v2-independent.md` + `content-draft-v1.md` §②/§③。
- **D453** 🔴 **难度倍率不作用于 `defense`**：D73 的倍率（简单 ×0.75 / 困难 ×1.25 / 噩梦 ×1.875）**只作用于 HP 与攻击** —— 防御是「率」且带 `clamp(0.95)`，乘上去会顶上限，把「更危险」变成「打不动」（与 D414 叠加后，噩梦第 6 层石魔像会到 0.75 × 1.5 = 1.125 → clamp 到 0.95，我方只能打 5%）。
- **D455** 敌人稿 v2.2 已对齐：四项回填完成（**D414** 的 7 只 def / **D411** 腐朽精英掉 `item_core` / **D415 + D438** 虚影 `echo` 不挂族群技 / **D413** = 32 只）；**D437 占位式零改动**。

### 10.9 迷宫、难度档、生命池与陷阱

- **D1** **生命池真共享一份**：全队共用一条 HP，**战斗与迷宫是同一份**，伤害扣它，任何角色的治疗都回它。（后被 D164 / D185 修订为双层账）
- **D8** 团灭代价**减轻：不扣金币**，只清 `activeRun`（探索进度）并送回营地（修正原文案「送回营地 / 实回世界地图」的矛盾，与 D4「无硬核压力」一致）。（**已被 D17 取代**）
- **D17** 团灭代价复议（调和 D8 与 D16）：**本图进度全部消失 + 背包物品损失（约合总价值 50%）**；不再只清本层 —— 整张地牢的进度全清；资产层面从「不扣金币」升级为「损失背包物品」。
- **D21** D17 澄清①：进度消失范围 = **全清，但保留「已通关」记录** —— `maxFloor` 归零、`activeRun` 清空，**`cleared` 保留**作成就（下次进这张图从第 1 层重来）。
- **D22** D17 澄清②：物品损失算法 = **按总价值凑一半**（随机挑物品直到累计价值达背包总价 **50%**），**材料类计入**（未排除）⇒ 损失额稳定可预期（不是每件 50% 概率的二项分布）。
- **D53** 大地图节点**补上商店与事件节点**（`shop` / `event` 两种从未使用）；`boss` 类型因 D2 已无用，**应删除**。
- **D58** **删除「迟缓」陷阱类型**（陷阱改为 伤害 / 传送 **各 50%**）—— 原「迟缓」实际无任何效果（`slowTurns` 只自减无人读取）。
- **D60** 宝箱金币**跟难度档 N1–N6 挂钩**（与迷宫尺寸 / 拓扑同源，改 `mazeDifficulty.ts`），弃用 `config.floor`（4 张图全是 1，导致金币恒为 45）。
- **D61** 埋伏选敌**改为按权重随机**（现恒取 `pool[0]`，永远是最弱的怪）。
- **D62** 可复现性：**传送改用项目 Rng**（现用 `Math.random()`，破坏「同种子同结果」）。
- **D63** 层元素**保持下层重生**（宝箱 / 陷阱 / 事件 / 存档点重新生成，保证每层能补给）。
- **D73** **新增全局难度系统**（推翻此前「无难度选择」的记载）：4 档 —— 简单（敌人属性 **×0.75** / 金币经验 **×1.25**）、普通（×1.0 / ×1.0）、困难（**×1.25** / **×0.75**）、噩梦（困难基础上叠加）。噩梦 4 个可自选、可组合机制：① 埋伏事件敌人先动 ↔ 埋伏金币经验 **×1.5**；② 商店物价 **×2** ↔ **+4 商品栏位**且稀有度上升；③ 敌方属性**额外 ×1.5** ↔ 己方每回合 AP 回复变为 **3**（基础 2）；④ 招募价 **×2** ↔ 重复角色等级提升 **1 → 3**。⚠️ 「敌人属性 ×0.75 / ×1.25」与 D14 的按层线性缩放是**乘算**关系。
- **D79** 难度**在新游戏时选择，之后锁定**（含噩梦 4 机制也一并锁定）。
- **D109** 噩梦机制**可任意叠加，最多 4 个全开**（每个机制自带代价 ↔ 补偿，玩家自由组合；不额外设全开奖励）。
- **D111** Boss 技能释放顺序 = **固定循环 + 血量阈值插入大招**（平时按配表顺序循环，血量跌破阈值时强制插入大招）。
- **D115** 难度**开局选择，选定后不可更改**（存档记录 `difficulty` 与 `nightmareMods[]`；噩梦 4 机制同样在开局一并选定并锁定）⇒ ① 需要难度选择界面；② 存档 schema 新增两个字段；③ 统计面板应展示当前难度；④ 回归测试要覆盖「全关」与「全开」两端。
- **D127** 商店的位置与数量 = **营地 1 个 + 迷宫每层最少 1 个、较低概率出现第 2 个**（不是只在大地图）；⚠️ 商店成为迷宫内元素，`MazeGenerator` 需新增放置规则。
- **D131** 商店在迷宫中的位置 = **支路末端**（与宝箱 / 存档点同机制，不占主干），鼓励探支路。
- **D132** N1 没有支路放商店（N1 `branchCount = 0`）⇒ **给 N1 加 1 条支路**（0 → 1）；⚠️ 需改 `src/config/mazeDifficulty.ts` 并**重跑迷宫难度探针**；**主干长度不变 ⇒ 通关步数 20 不变**。
- **D136** N1 支路长度上限 = **4–6 步（与 N2 同档）** ⇒ 完整阶梯：N1 `1 条 / 4–6`、N2 `2 条 / 4–6`、N3 `4 条 / 6–10`、N4 `6 条 / 8–14`、N5 `8 条 / 10–18`、N6 `10 条 / 12–24`；支路**条数**仍严格递增。改它**不需要**跑 `gen-tables`（TS 模块），但必须重跑迷宫难度探针与 `verify.mjs`。
- **D142** **不做小地图、不做方向箭头** —— 迷路本身是玩法（呼应支柱②）。三条缓解手段：① 地标（存档点 / 商店）；② 三态迷雾的「已探索」档要真的保留；③ **出口不随迷路隐藏**。若日后实测「迷路到弃游」，第一优先是加「出口方向箭头」，不是小地图。
- **D145** 团灭惩罚**不分难度，四档统一**按 D17 / D21 / D22 执行。
- **D160** 深渊吞噬导致必然团灭的解法 = **HP 改为战斗后自动恢复**（用「HP 不入跨场账」一次性消解：8% 永远只是单场内的一次削血）。
- **D161** 场外回血手段 = **战斗结束后自动回满**（不设存档点回血、不做每层通关回复）。
- **D164** **D1 修订为「双层生命账」**：**战斗 HP**（每场战斗开始 = 当前迷宫 HP；战斗内伤害只扣这本账）**不跨场**（战斗结束即丢弃，不写回）；**迷宫 HP**（陷阱 / 事件扣血）**跨场持续**，直到回营地或被治疗。等价叙述：战斗不消耗迷宫血量，只有迷宫元素消耗。（「迷宫伤害跨场」那一半被 D185 修订）
- **D168** **迷宫 HP 归零即团灭**（沿用 D17 惩罚：本图进度消失 + 丢 50% 价值物品）⇒ 陷阱从此真正有威胁。（⚠️ 当前代码不可达 —— 陷阱伤害有 `Math.max(1,…)` 不致死、`startBattle` 不传 HP、全仓无团灭代码路径；见 D373 / T-1）
- **D170** 是否补新风险维度 ⇒ **不加**。支柱 B 的风险源就是**陷阱 + 事件 + 迷路**（D164 后的最终形态）。
- **D172** 陷阱的致命化 = **不降伤害，降数量**：`trapCount` 1/3/4/5 → **1/1/2/2**（伤害系数与「已探索可见」均不改）；⚠️ 原「一趟最多踩 2 次（最坏约 40%）」的算术是错的 —— `trapCount` 等四字段是**每层**语义（见 D349）。
- **D179** 迷宫 HP 的回复手段**四选四**：① 战斗后回满 ② 通关迷宫后回满 ③ 团灭回营地后回满 ④ 迷宫里可用道具回复。
- **D185** 陷阱口径**按 D179 字面：战斗后回满** ⇒ **D164 修订为「战斗回满、迷宫伤害不跨场」**（等价于原 (a) 方案）；后果已知并接受：D168 几乎不可能触发（暴露区间被压缩到「两场战斗之间」，一趟约 35 场战斗 vs 6–12 个陷阱 ⇒ 平均每个间隔不到 1 个），陷阱退化为轻微风险。
- **D189** 陷阱**不补救，接受** —— 陷阱退化为轻微惩罚，支柱 B 靠**事件与迷路**支撑；若日后认为支柱 B 太软，第一优先是 D172 的备选 (c)（让陷阱在已探索区域可见，把随机踩雷变成可规划的绕路），而不是加回伤害。
- **D191** **迷宫 HUD 不再显示血条**，只在战斗界面显示（D185 后迷宫 HP 几乎总是满的，常驻显示是噪声）。
- **D224** **Boss 战禁用烟丸**（D50 优先）—— Boss 战就是「必须打完」，烟丸只在普通战可用。
- **D247** 迷宫濒死提示**用氛围提示代替数值条**：**全屏边缘渐晕 + 心跳音效**（纯氛围，不给数字），是 D191 的配套。
- **D289** **逃跑只能靠烟丸**（普通战也不做「概率逃跑」）：有烟丸 = 100% 脱战，没有就只能硬打；**Boss 战两者都禁用**（D50 / D224）。
- **D321** 濒死渐晕的三档阈值正式定案 = **50% / 30% / 15%**（轻度渐晕 / 中度 + 心跳 / 重度 + 心跳加快），替代 D247 里「建议值」的表述。
- **D322** 「**零渐变**」的适用范围**放宽为「只约束 UI 面板」** —— **全屏遮罩（渐晕）允许 alpha 渐变**（6 层平色环带会露出色阶断层，比渐变更丑）；这是规则**适用范围**的澄清，不是放弃美术方向。
- **D323** 敌方 debuff 的可见性 = **在敌人名字 / 血条旁显示 debuff 图标 + 剩余回合**（D272 修好后「削敌方 attack / defense」才真正生效，玩家必须能看到）。
- **D349** 陷阱数量：**保持 `trapCount` = 1 / 1 / 2 / 2**（每层语义 ⇒ 一趟 6 / 6 / 12 / 12）—— 虽然 D172 的理由算错了，但「每层少量」在正确语义下反而是对的；等实测确认后再微调。
- **D350** 宝箱数量 `treasureCount` **3/4/5/6 → 2/3/3/4**（每层语义 ⇒ 一趟 12 / 18 / 18 / 24）—— 原值一趟 18–36 个偏密（平均每 1–2 场战斗一个）。⚠️ 副作用：宝箱在第一张图收入里占 75%（腐朽仅 42%），前期被砍得比后期重（由 D375 裁定不补偿）。
- **D373** **陷阱单独立案 T-1「陷阱的致死路径」**，不用调 `trapCount` 去救；其前置是 **D1（真共享 HP 池）+ D168（致死路径）**，两者都尚未实现 ⇒ 在 D1 落地前「陷阱太致命 / 太温和」的讨论是空转；落地顺序 `D1 → D168 → 才是 D172 / D349 的数值微调`。
- **D376** T-1 的验收带 = **每 10 趟 0–1 次（≤10%）** —— 陷阱是「意外」而非「常态」，与 D4「不做硬核生存压力」一致。
- **D378** 陷阱的当前处置 = **冻结 `trapCount` 不动**，等 T-1（前置 D1 + D168）再统一调。

### 10.10 事件与探索辅助

- **D192** **扩充事件**（种类为主，数量未加；支柱 B 现在主要靠事件 + 迷路支撑）。
- **D207** shrine「**祈祷**」**不再给永久属性，换其它奖励**（并保留「选择与代价」，不做纯白送）—— 36 点/趟 = 秘药产出的 **10.6 倍**，会把 `item_elixir` 整条线架空。
- **D209** 埋伏事件**不允许逃跑** —— 直接**删掉该选项**（不是加代价），玩家只能迎战（原「逃跑」严格支配「迎战」）。
- **D320** 事件的失败后果**不在事件界面内披露**，改为**首次进入迷宫时弹窗提示**事件的机制与风险（与 D98 的上下文引导合流）；事件界面本身不写具体数值。（**前半已作废，见 D336**）
- **D324** 「首次进迷宫弹窗」的粒度 = **全局只弹一次**（第一次进任意迷宫）—— 「不弹窗轰炸」的最彻底做法。
- **D333** **有代价的选项用红色加粗标出** ⇒ 部分覆盖 D320 的「完全不披露」（D320 仍管「是否给具体数值」，后被 D336 推翻）。
- **D336** 事件的代价**连数值一起给**（红色加粗 + 具体数值）⇒ **推翻 D320 的「不写具体数值」**，事件界面**全文披露**；D320 只剩后半有效（首次进迷宫仍弹一次总览，讲三态迷雾 / 无小地图 / 团灭代价 / 事件有代价，但不再承担「教事件风险」的职责）。
- **D423** 事件的三个未定义设定**一律按「最不宽容」处理**：① 事件弹出后**必须选一个选项，不能拒绝 / 走开**；② 因此「拒绝的代价」这个问题**不存在**；③ **不能重掷**（一次性）。理由与 D209 同构 —— 事件是支柱 B 的主要承载体，可回避就等于没有风险。

### 10.11 任务与新手引导

- **D38** 目标引导 = **加任务列表面板**（完整任务 / 目标列表，如「通关无根回廊 3/6 层」）。
- **D43** 新手引导 = **上下文触发提示**：首次遇到某机制时弹一次说明（第一次开战斗讲 AP、第一次踩陷阱讲陷阱…），比开场集中说明更有效。
- **D91** 任务面板内容 = **主线 + 支线**（主线 = 击败该图 Boss；支线 = 首次通关第 N 层、招募 N 名角色、锻造 N 件饰品等）。
- **D98** 上下文引导触发点**覆盖四项反直觉机制**：首次进入战斗（AP 与升耗）、首次下探（迷雾与层）、首次踩陷阱、首次招募（潜力）。
- **D238** 任务系统的具体清单**派队员设计**（主线 4 条 + 支线 8–12 条，含名称、达成条件、奖励数值）。
- **D239** 新手引导的文案与形式**派队员写**（D43 / D98 的四个触发点）。
- **D248** 引导文案里的数字**用模板占位**（如 `{RECRUIT_PRICE}`），落地时从配表取 —— 避免重演「CD 3」式的过期文案。
- **D283** 新游戏时**让玩家自己选是否开启引导**（不是默认开、也不是默认关）⇒ 这是 D276 开局流程里新增的一步，该选择**存进存档**。
- **D286** 引导**默认开启，可在设置里随时改**（与 D283 合并：新游戏时让玩家选，默认高亮「开启」）。
- **D294** 「**重看全部提示**」入口**做** ⇒ `firstTimeFlags` **可清空**（不只只增）。
- **D302** 引导触发点**不加，10 条够**（再加就变成弹窗轰炸，与 D43 的初衷相反）。（⚠️ **已被 D422 部分覆盖**：实际加了第 11 条事件提示）
- **D307** S9 的 2500 阈值缺实测支撑，**挂起**等 economy-analyst 补「宝箱金币分项」（后被 D381 解挂）。
- **D309** S4 / S5 / S6 绑在有限资源池（永久上限合计约 9 次，头 4 趟就耗尽）⇒ **改造这 3 条**让它们也可重复 ⇒ **10 条支线全部为引擎型**。
- **D318** 引导默认值（D283 vs D286 字面冲突）= **新游戏时让玩家选，默认高亮「开启」** —— 两句都成立。
- **D330** **删 S3**（与 S10 重复）⇒ 支线从 **10 条 → 9 条**。
- **D361** S7 的阈值口径 = 以草案的「**累计击败 15 只精英**」为准（不是 ≥12）。
- **D364** 加一个「**关闭全部引导**」总开关（设置里），比逐条关更实用；⚠️ **关闭 ≠ 清空**（关闭期照常写 `firstTimeFlags`，重开只补缺的），**不新增字段**（复用 `state.settings` 的 `guideEnabled` 语义）。
- **D367** 支线 S9 的阈值 **2 500 → 1 700**（随 D350 的宝箱下调同步；验收口径 **0.8–1.4 次/趟**）—— 这是第二个因阈值失效而需要修的支线（第一个是 S3，已删）。
- **D381** D307 解挂：队员确认 S9 所需的「宝箱金币分项」**早已存在**于 `techpoint-economy-v1.md` §4.3（四图 **1 393 / 1 858 / 2 322 / 2 786**，合计 8 359）⇒ **D300 ② 的 2 500 已有实测支撑**，只待 D350 之后同步为 1 700（D367）。
- **D422** `firstTimeFlags` = **11 个 key**（原写 10；新增 `T11 tip_dungeon_event`，触发 = 事件界面首次弹出）⇒ **不新增字段**（只是集合元素 +1）。

### 10.12 存档、统计与设置

- **D46** 存档导入 / 导出**接 UI 入口**（主菜单或营地设置；`exportJson` / `importJson` 已实现）。
- **D47** 不做定时自动存档，但 **`playTime` 计时仍需接线**（现恒为 0，而 UI 处处显示「游戏时长」）。（表述后被 D68 修正）
- **D68** 存档频率 = **仅事件触发，不做定时自动存档**（修正 D47）；但 `playTime` 计时仍需接线。
- **D69** **战斗中不支持存档**，退出后退回战斗开始前（保持现状）。
- **D70** 存档槽**改为命名存档列表**（不再是固定 3 槽）。
- **D71** 主菜单续档**保持现状**（不自动续档，无进度提示）。
- **D105** **只做统计，不做成就**；统计项：游戏时长 / 通关次数 / 击败数 / 金币 / 最高层。
- **D134** **旧档不可续档**（升版本；旧档只读，提示开新档）—— 因为 D115 定了「难度选定后不可改」，没有难度的旧档本身就不合法；⚠️ D46 的导入导出必须按版本号拒绝旧档。
- **D135** 统计面板入口 = **主菜单新增一个入口**，与「游戏说明 / 读档」并列（不依赖游戏内场景，随时可看）。
- **D137** 「最高层」统计口径 = **历史最高，团灭不重置**（D21 只归零 `maxFloor`，不动历史字段）⇒ 存档需新增 `maxFloorEver`。
- **D267** 存档 schema 规格**派队员出规格**（含新字段清单、版本号、迁移拒绝规则）。
- **D275** **团灭惩罚立即写盘**（失去进度 + 丢 50% 物品**直接落存档**）⇒ **重载存档躲不掉惩罚**；这条同时把 D69「战斗中不存档」的边界钉死。
- **D276** 开局流程 = 主菜单 → 新游戏 → **难度选择（含噩梦 4 机制勾选）** → 营地（难度在角色生成之前；因 D269 技能全开放，不需要角色创建步骤）。（① 选槽由 D359 补）
- **D281** `maxFloorEver` 的粒度 = **每图一个**（四个数字）—— 能看出玩家在哪张图探得最深。
- **D285** 团灭**只归零该图的 `maxFloor`** ⇒ **重进该图从第 1 层重走**；`maxFloorEver` 只用于统计，`cleared` 永不归零。
- **D287** **自动存档（D68）+ 营地手动并存**：玩家可在营地里手动存到指定命名槽（D70），但**迷宫内与战斗中不可手动存**（与 D69 不矛盾）。
- **D293** 存档槽数 = **10 槽**（现状 3 槽偏少；E-16 曾裁 6 槽，以本条为准）。
- **D295** 统计口径**全部计入**：`totalDefeated` 含 Boss / 精英；`totalClears` 含重复通关；`totalGoldEarned` 含卖出所得（收入侧统一收口在 `addGold`，**只记入账不记花费**）。
- **D296** 新游戏**强制让玩家选槽**（不允许「默认槽」）⇒ 不存在「覆盖当前槽」这个动作，D134「旧档只读」不会被绕过。
- **D313** `partyEquip` 的容器形态 = **`party` 改为对象数组** `{ charId, accessory, consumable }`（因 D292 已把槽位与角色解耦，必须显式绑定「出战位 → 角色 + 饰品 + 消耗品」，并行数组会制造三个需要同步的真源）。
- **D314** **自动存档用独立键，不占用 10 个命名槽**（否则自动存会挤掉玩家的命名存档）。
- **D315** 引导开关放**随档 `state.settings`**（它影响存档内的引导行为）；而**文本缩放 / 高对比（D100）属全局无障碍偏好 ⇒ 独立键 `maze_rpg_settings`** ⇒ **两套设置并存是有意的**。
- **D316** 迷宫「存档点」**不属** D287 禁令：存档点元素 = D68 的「事件触发式自动存档」，属允许范围；D287 禁的是「玩家手动存」。
- **D334** 存档槽与命名的共存 = **固定 10 槽 + 每个槽可自定义名称**（不命名则显示「存档 N」）—— 固定槽保证「强制选槽」可实现，命名满足 D70。
- **D338** `maze_rpg_settings` 的**整体覆写**改为「**读旧值 → 合并 → 写回**」（必须修，否则 D100 的可访问性设置会被改分辨率的动作静默抹掉）。
- **D339** 存档名**不允许重名**（重名时报错）⇒ 保证玩家能靠名字区分槽位（比较口径见 D365）。
- **D353** ⚠️ 待澄清：**同一 D 号被两条不同裁决共用** —— ① E-25：新增 **`stats.eliteKills`**（供支线 S7）与 **`stats.clearStreak`**（供支线 S10）两个不可推导的计数器（字段表 14 → 16；`totalDefeated` 不含类型信息拆不出「精英」，`totalClears` 单调递增表达不了「连击被打断」，且都不能放 `runCounters`）；② D242 限次的归属 = **per-unit**（每个单位各自算「该技能本回合是否用过」），不是全队共享一个限次。
- **D358** **取消每回合自动存档**（维持 D68 的「仅事件触发」）⇒ README 那句是过期描述，随 D44 的重写一并修掉。
- **D359** 开局流程最终版 = `主菜单 → 新游戏 → ① 选槽（D296）→ ② 引导开关（D283）→ ③ 难度 + 噩梦勾选（D115 / D276）→ 营地`；① 的位置正式补为裁决（`guideEnabled` 与难度**都是随档数据**，必须先有槽才有落点）。
- **D360** 🔴 **`save()` 的 null 回落会静默毁存档**（后立项为 D368，见下）：`activeSlot === null` 时 `save()` **直接拒绝写盘**（不回落 `SAVE_KEYS.main`）；且 `newGame()` 必须要求先选槽 ⇒ 这是 D296「强制选槽」的前置。
- **D362** **`stats.totalRecruits` 采纳**（支线 S4 需要它，而 `roster` 长度表达不了「累计招募次数」）⇒ 字段表 **16 → 17**；默认 0，**开局 `createStarters()` 白送的角色不计入**。
- **D365** 存档重名的比较口径 = **严格逐字节**（连空格都算不同，不 trim、不归一化；v3 的「忽略大小写」作废）。
- **D368** 🔴 **G19 正式立项**（最高优先级必修，全场唯一「会静默不可逆毁档」的缺陷）：`activeSlot === null` ⇒ `save()` **直接 `return false`**，不回落 `SAVE_KEYS.main`；进 D240 的批次 1（不依赖 D108）。⚠️ 真实后果 = 未读档时点「保存设置」，是**拿一份空档覆盖第 01 槽的完整存档，不可逆**；另 `newGame()` 不设 `activeSlot` 而 `startNewGame()` 裸调 `save()` ⇒ 未选槽直接开新档同样写进 01 槽；**13 处无参 `save()` 调用都要复查**。
- **D369** 🔴 **同一 ternary 的第二颗雷**：`delete()` / `load()` / `hasSave()` 用的是同一个 ternary ⇒ **`delete(null)` 会删掉第 01 槽**；**必须与 D368 一起修**，不能只修 `save()`。
- **D370** `SAVE_SLOT_COUNT`（`config/saveKeys.ts`）现值 **3 ⇒ 改为 10**（D293），并入批次 1。
- **D379** **允许队员在草案原文后追加「勘误包 v6.1」**（不改既有段落，只新增），与 GDD 侧的「勘误回填」是同一套留痕纪律。
- **D380** 存档字段的账目 = **最终 17 个字段**（其中只有 **D362 的 `stats.totalRecruits`** 是第 17 个；**D364 的「关闭全部引导」不新增字段** —— 它在 `state.settings` 内复用 `guideEnabled` 的语义）。
- **D385** 存档字段账目的落点 = **并入 §11.D**（顶部标明「17 个字段，其中第 17 个是 D362 的 `stats.totalRecruits`」）。
- **D388** 存档规格（v6.1 含勘误包）**并入 §11.D**（含 17 个字段与 19 条 G 缺口）。
- **D397** 🔴 出战位容量 `|party|` = **4 → 6**（初始 **4** 人，科技树「队伍扩编 I/II」→ 5 → 6）；⚠️ `SAVE_SLOT_COUNT = 3` **不是**出战位容量（同一数字、用途完全不同）。
- **D398** 合并时的「真源优先级」三条：**引导 key 清单与文案**以 quest-writer 的 tutorial / 任务草案为真源；**字段定义 / 默认值 / 写入时机**以存档规格（§A + 勘误包）为真源；**存档槽规则**（E-23 三步覆盖 + 逐字节比较 + 四步顺序）以存档规格 §A.9 + 勘误 #5 为真源。
- **D399** 存档命名的 UI 时序：① **命名与查重必须发生在「清空并开始？」这次不可逆确认之前** ⇒ UI 上**先输名、再问清空**；② **查重只能是「纯读校验、禁止写盘」**（否则玩家点取消后，槽里会残留一个他从未确认过的名字）。
- **D400** 查重的「**自身豁免**」：查重必须**排除正在被覆盖 / 正在改名的那个槽自身**（`isNameTaken(name, exceptSlotIndex?)`），否则「沿用该槽旧名」会被它自己的旧名假报错。
- **D421** 存档命名的四步顺序以「**校验/替换 → 查重 → 确认 → 落盘**」为准（修 D398 / D399 里两处自相矛盾的顺序描述）：「确认」必须是写入的闸门，而「查重」必须在它之前。

### 10.13 UI、美术与可访问性

- **D24** 战斗角色卡尺寸 = **168×340**（`battle-ap-ux-spec.md` 值）；⚠️ 代码现值 168×400 需回调，并同步调 `CONSOLE_H` 与卡内元素坐标（半身像、名字带、buff 行、技能条）；`ui-style-spec.md` 的 96×128 应标注废弃。
- **D25** AP pip 形态 = **菱形 20×20，两排 5+5 共 10 槽**（BUX 规格）；未解锁的第 9/10 槽画锁形 + 虚线描边；⚠️ 代码现值 10 槽用的是**平行四边形**，需改。
- **D26** 战斗技能栏形态 = **156×46 横向条**（代码现值）：左技能名 + 右动态 AP 消耗 + 左 3px 语义色条，**无图标格、无扇形、无 MP 角标**；`ui-style-spec.md` 的 36×36 + 冷却扇形应标废弃（**冷却已废除**）。
- **D27** 卡牌抬升时长 = **120ms** `Quad.easeOut`（回落 150ms）。
- **D30** 阶段制下的高亮 = **只保留「已出手」瞬间反馈**（单位出手时短暂闪一下，120ms 抬升后立即回落），**不表示「轮到谁」**；原「当前行动者」三件套（常驻抬升 8px / 切角描边 2px / 行动角标 16×16）**废弃**，D27 的 120ms 应用于这个瞬时反馈。
- **D31** 音频 vs 新手引导 = **先做音频，引导暂缓**（设置里已有音量滑杆但无音频资源）。
- **D36** 迷宫色名冲突：以 **`mazeRoad` / `mazeWall` / `mazeVoid`** 为唯一真源，把规格书描述迷宫的 `floor/floorAlt/wall` 改成 `mazeXxx`，并标注 **`wall` 废弃**（全项目零使用）；`floor` / `floorAlt` **保留**（它们是 UI 面板底板）。
- **D37** 升耗视觉提示**补写成正式规格**：数字转 `gold` 金色 + 右上角 **9px `+N`** 角标 + AP 不足时转 `danger` 红并加 **2px 斜线** + 技能条降为 `SPENT_FILL #272C35`（左侧色条 alpha 1 → 0.35）；**零代码改动**（当前实现已如此，只补文档）。
- **D39** 音频范围 = **BGM + 核心音效**：地牢 / 战斗 / 营地 **3 首 BGM** + **UI 点击 / 技能释放 / 受击 / 升级 / 获得道具** 5 类音效。
- **D96** BGM 风格 = **氛围 / 压迫向**（低频脉冲 + 稀疏音型，强调迷宫的孤独与压迫），与「零渐变零发光」的冷峻视觉一致。
- **D97** 音效清单**按 D39 的五类**：UI 点击 / 技能释放 / 受击 / 升级 / 获得道具。
- **D100** 可访问性 = **文本缩放 + 高对比模式**（全局字号 **100% / 125% / 150%**；高对比增强描边与文字对比）；⚠️ 与「切角代替圆角、零渐变」不冲突，只增强可读性。
- **D101** 文本呈现 = **逐字显示，可跳过**（战斗日志与对话；点击或按键立即全出）。
- **D102** 战斗结算界面 = **完整结算**：获得经验（含每人升级情况）、金币、科技点、掉落道具。
- **D103** 背包**不设总格数上限**（继续按 `stackLimit` 堆叠；D54 锻造会增加材料囤积，但不设限）。
- **D106** 图鉴 `CodexPanel` **不恢复**（维持现状，不投入修复或重做）。
- **D117** **不做资料查看入口**（不建图鉴、不并入面板），信息只靠战斗内的数值条与技能描述临时查看；⚠️ 但「饰品槽位上的效果说明」不算资料入口，属槽位自身的必要标注，落地时必须保留。
- **D139** 音频资源**先只接接口与事件**（D97 五类音效 + D96 三首 BGM），资源用占位；⚠️ `AudioSystem` 需要「静默模式」（资源缺失时不报错、不阻塞），音量滑杆要真正接到主音轨。
- **D280** `tech_ap_cap` 的中文名**改为「行动扩容」**（1 条文案、零代码），解决 D212 的三方撞名。
- **D288** **战斗内双方都显示精确数值血条**（我方共享池 + 敌方）—— 与 D191 / D247 的「迷宫不显示、改用氛围提示」形成分工：**迷宫靠氛围，战斗靠数值**。
- **D405** 营地的设施清单 = **刚好够**：招募 / 队伍 / 背包 / 科技 / 难度（已锁定，仅显示）/ **训练场**（D107）/ **手动存档**（D287）。
- **D406** 难度的「推荐」提示 = **标推荐**：难度选择界面标注「**推荐：普通**」，并在噩梦上标「**不推荐首次游玩**」。

### 10.14 工程纪律、代码缺口与环境坑

- **D42** 死字段与孤儿模块**全部清理**：删 `skills` 的 `cooldown` / `cost`、`enemies` 的 `speed`、`balance` 的 `baseSpeed` / `levelUpTechPointBonus`，并删除无人 import 的 **`src/entities/` 整个目录**；同步清理 `types/` 里对应字段与读取它们的死逻辑。
- **D44** `README.md` **按 GDD 重写**设计章节（现为 v1.0：10 职业 / 15 角色 / 60 固有技能 / 普攻 / MP / 速度单动 / 图鉴，与现状全面冲突）。
- **D45** 根目录调试产物**全部清理**（`scan-*` / `tmp-*` / `answer52.txt` / `task28-*` / `gen-out.txt` / `zz-*.log` 等约 25 个文件）。
- **D155** 内容草案**等队员交回后由 lead 逐条审核，再让用户拍板**（草案路径 `design/drafts/content-draft-v1.md`，**只读不落地**）。
- **D180** **更正：`combo_bonus` 并非 no-op**（推翻 §10 的旧结论）—— `skillComboBonus()` 确实从 `effects` 读 `combo_bonus` 并加进 `comboRate`，且命中数早已可追加（`totalHits += baseHits`）⇒ **D56 的前提「逻辑不可达」已过期，该修正项从待办中移除，改判为「不修」**。
- **D244** 6 份草案**并入 GDD 正文**（各成一节，带完整数值表），草案 md 保留为「推导过程」引用；⚠️ 并入须等 6 份草案**全部冻结**，且并入后草案 md **不再修改**（改 GDD）。
- **D272** 🔴 **必修，且是 D233 / D249 的前置条件**：`applySupportEffects` 把 `buff` / `debuff` **直接 push 到 `caster`**，而该函数签名里**根本没有 targets** ⇒ **所有增减益的落点恒为施法者，与 `skill.target` 无关**（后果：Boss 的削我方技能作用在 Boss 自己身上；我方「削弱敌方」技能会削弱自己）。⇒ **修复顺序：先修两个落点 → 再修 #12b 的 effects 结算路径 → 再修强化（D259）→ 最后才落 D233 / D249 的数值改动**（顺序错了会往配表里写入「自伤技能」）。
- **D382** 🔴 **`enemies-draft-v1.md` 丢失事件已确认**（该文件在磁盘上不存在）⇒ 需**重新产出**；⚠️ 流程教训：**派单时必须加一句「禁止删除 `design/drafts/` 下任何文件」**，且队员交付物落盘后 lead 要立刻记下「文件路径 + 生成时间」。
- **D387** 🔴 **两套 G 编号是不同命名空间**：以 `save-schema-spec-v1.md` 的 G 表为准（**G1–G18 = 18 条，加 G19 ⇒ 19 条**）；UI 规格自己的 G 编号空间是另一套。⇒ **今后引用「G 编号」必须带文件名前缀**（`save-G19` / `ui-G15`）。
- **D409** 🔴 **必修**：敌方 AI 按 **raw power** 排序 ⇒ **多段技永远不会被 AI 释放**（含现役 `enemy_sweep` + 新增 2 条 = 3 条死技能）⇒ **改为按 `power × hits` 排序**。
- **D410** 🔴 **必修**：`heal_max_ratio`（D157）的配套 —— 必须同步改 `CombatSystem`（它只认 `type === 'heal'`），否则新效果永不释放（「续髓」直接是死技能）。
- **D435** 🔴 **引用 GDD 位置的硬规则：一律用「小节号 + 原文措辞」，禁止用行号**（行号会漂、措辞不会）；行号仅允许「当轮对话内临时定位」且必须附原句。
- **D436** 「转述失真」第三例记录并定纪律：把存档规格原文「key 清单以 10 条为准（T1–T10）」转述成「共 10 个」是错的（后者原文里根本没有）⇒ **引用必须逐字，压缩时保留限定语**。

---

## 20. 战斗 / 技能 / 战术 / 敌人

> 本章来源：`design/drafts/tactics-draft-v1.md`、`enemy-tactics-draft-v1.md`、`skill-texts-v1.md`、`combat-length-recalibration-v1.md`、`enemies-draft-v2-independent.md`（原文已归档）

本章讲四件事：**战斗怎么跑**（回合/AP/出手规则）、**技能怎么算**（伤害/治疗/概率/文案）、**战术怎么叠**（我方 11 条 + 敌方 9 条）、**敌人是什么**（32 普通 + 4 精英 + 4 虚影）。
凡本章与原始草案冲突处，一律以本节所写的裁定版为准；草案中被否决的方案只在"已被否决"的标注里出现，不作为规则。

---

### 20.1 战斗骨架：大回合、阶段与行动力

**一次战斗 = 若干个大回合**，每个大回合 = 我方阶段 → 敌方阶段 → 大回合边界结算。

| 项 | 口径 | 依据 |
|---|---|---|
| 敌人数量 | **永久单敌**（`enemy_all ≡ enemy`，群体概念已删） | **D3** |
| 生命池 | **真共享一份**：全队共用一条 HP，战斗与迷宫是同一份 | **D1** |
| "作用于我方全队"的效果 | **只结算 ×1**（战斗视作 1V1，队伍是一个整体） | **D273** |
| 行动力 AP | 开局 **3** / 每回合 **+2** / 上限 **8**（科技「行动扩容」**D280** +2 ⇒ 10）；AP 池**队伍共享** | `CombatSystem` 常量 `AP_INITIAL / AP_REGEN_BASE / AP_CAP_BASE` |
| 奇数回合额外 AP | 研究「奇袭节拍」后奇数回合再 +1（`apOddBonus`） | 科技表 |
| 噩梦机制 3 | 开启时 `apRegen` 由 2 变 **3** | `CombatSystem` 常量 |
| 防御 | **1 AP**，本回合受到伤害 **×0.8**，不推进战斗 | **D104**；`balance.defendApCost=1`、`defendDamageMultiplier=0.8` |
| 出手锁（我方） | **每个（单位 × 技能）对每大回合限 1 次**：允许 A→B，**禁止 A→A** | **D242** |
| 出手锁的归属 | **per-unit**（每个单位各算各的"该技能本回合是否用过"），**不是全队共享一个限次** | **D353** |
| 出手锁的适用范围 | **只约束我方**；敌方按 **D151** 的类别规则走 | **D261** |
| 敌方出手次数 | 普通 **1 次** / 精英 **2 次且每 3 轮 1 次大招** / Boss 按 **D111** 固定循环 + 血量阈值插大招 | **D151 / D111** |

**D242 的四条实现纪律**（它是"新加的闸门"，不是"改一个开关"）：

1. 判据复用现成的 `BattleUnit.usedThisRound`（`Set<string>`），**不需要新字段**；读侧加闸门即可。
2. 闸门加在**我方三处**：`submitAction()` / `executeAction()` 的 skill 分支、UI 的 `skillState()`、`canAllyAct()` 与 `chooseAllyAutoAction()` 的可用性过滤。
3. `canAllyAct()` 的判据必须同步改成"存在存活我方单位，**且存在一个既未用过、又付得起 `skillCost` 的技能**"——否则"AP 够但技能全用过"时会**卡死**（恒 true，自动结束永不触发）。
4. 敌方出手**本来就不写** `usedThisRound` ⇒ 闸门天然只截我方，**不要**再加 `if (side === 'ally')` 分支。

> ⚠️ **不要把 D242 实现成"每单位每回合限出手 1 次"**（那是被它修订的 **D9**）。那会让"每角色 2 技能"退化成 1 个可用，与 **D55** 冲突。

**大回合边界（`endRound()`）的顺序**（顺序错了会改变"持续 N 回合"的实际长度）：

1. `usedThisRound → usedLastRound` 滚动（**D242 的"技能恢复可用"就在这里**）
2. AP 回复（`apRegen + 奇数回合奖励`，按 `apCap` 截断）
3. 我方阶段开始的结算 `onAllyPhaseStart()`（**D159**）
4. `round += 1` → `phase = 'ally'`

**`onAllyPhaseStart()`（D159）的结算顺序**（"第 N 个我方阶段"以此计数）：

1. 时长类效果老化：`damage_cut` → 治疗反转 → 增益
2. 我方 DoT（中毒）
3. **深渊吞噬**（腐朽王座族群特性）：战斗进入**第 3 个及以后的我方阶段**时触发 **1 次**，我方全队损失**当前生命 8%**（不是"每回合 8%"；由 D159/D160/D185 修订 D121 的原始意图），且**只结算 ×1**（D273）
4. 战术递减（属时长类，见 §20.5）——**必须挂在 AP 回复之前**，否则"持续 3 回合"会变成 4 个我方阶段

---

### 20.2 伤害、概率与治疗

**伤害（唯一公式）**

```
baseDamage = team.attack × power / 100
team.attack = round(Σ 全队筋力 × 5)      // balance.strengthToAttack = 5
实伤 = baseDamage × (1 − defenseRate) × 暴击倍率 …，下限 minDamage = 1
defenseRate = clamp(target.defense + (防御姿态 ? 0 见下 : 0), 0, 0.95)
```

- 多段技按 `hits` 逐段结算，每段独立掷连击 / 暴击。
- 暴击倍率 = `balance.critDamageBase(1.5) + probs.crit`。
- 防御姿态走 `defendDamageMultiplier = 0.8`（D104），不是加 `defense`。

**三条概率轴（我方）**

```
P(x) = P_max × (1 − e^(−x / K))        x = 队伍该维合计
连击：P_max 0.6 / K 80  ← 敏捷
暴击：P_max 0.5 / K 80  ← 幸运
强化：P_max 0.5 / K 80  ← 灵性
掷点时上限 clamp 0.95
```

| 档位（队伍每维合计） | 连击 | 暴击 | 强化 |
|---|---|---|---|
| L4（78） | 37.4% | 31.1% | 31.1% |
| L8（151） | 50.9% | 42.4% | 42.4% |
| L11（191） | 54.5% | 45.4% | 45.4% |
| L17（325） | 59.0% | 49.1% | 49.1% |

- 战斗内 buff 对概率的加成走 `collectBonus`，线性 **+0.004/点**（敏捷→连击 / 幸运→暴击 / 灵性→强化）。
- 类 2 饰品提供的概率加成是**掷点加成**，**刻意不写进 `probs`**（否则 `critMultiplier = 1.5 + probs.crit` 会把暴击 pp 再吃一次，最坏组合越过 D197 的"伤害轴 ≤ +25%"上限）。

> 🔴 **敌方概率是硬编码常量，不读属性**：`balance.enemyComboRate = 0.08`、`enemyCritRate = 0.1`、**`enemyEnhanceRate = 0`**。
> ⇒ **敌方强化率恒为 0**。所以：
> ① 敌方技能**一律不填 `enhanced`**（填了也永不触发）；
> ② 敌方任何"战术类 / 附带效果"必须写在 `effects` 里，**不能写进 `enhanced`**；
> ③ 敌方侧无法做"概率轴"类战术（见 §20.7 的 D366 效果集）。

**治疗（D235，攻击力派生）**

```
治疗量 = 队伍攻击力 × 倍率/100 + 灵性 × 0.5
上限：被施法者 maxHp 截断
```
文案口径：一律写"`X%` 攻击力生命"，**不再出现"魔力值"**（魔力在战斗内完全无效，D226 已据此删掉魔力战术）。

**技能表的数值纪律**

- `apCost` 是**用户设定的配表值，程序不得改写**（含升耗只改"本次成本"，不改 `apCost`）。
- 玩家技能（`skill_*`）的比值区间：**单段 `power / apCost ∈ [100, 130]`**、**多段 `power × hits / apCost ∈ [80, 120]`**（按 D15 分档后的值核对）。
- 敌方技能**不吃 AP**（扣费整段被 `actor.side === 'ally'` 包住），`apCost` 只是按 D15 照填的展示值，不参与上述比值校验。

---

### 20.3 技能：分档、升耗与文案

**AP 分档（D5 / D15）与升耗**

```
skillCost = (apCost ?? 1) + (该单位上回合用过该技能 ? 1 : 0)
```

- 升耗是 **Set 成员判断**，**恒定 +1，绝不累加**；`escalation:'permanent'` 才累加，但配表 **88 条技能无一启用**（**D227**）。
- **跳回合 / 换技能 / 换单位都能规避升耗** ⇒ 正确打法下 `usedLastRound` 长期为空，升耗几乎不触发。
- ✅ **D278**：这正是设计想要的（"轮换技能以规避升耗"是 **D242** 鼓励的打法）⇒ **不堵**。

**文案口径（D146 / D232）**

| 项 | 口径 |
|---|---|
| 字数上限 | **≤55 汉字**（按 `[\u4e00-\u9fff]` 计，排除数字 / `%` / `×` / 标点） |
| 禁止出现 | `cooldown` / `CD` / `cost` / `element` / 属性克制 / **AP 数值** |
| 目标范围 | 一律按配表 `target` 写 |
| 治疗句 | `全队恢复 X% 攻击力生命`（不写"灵性 ×0.5"那一项） |
| 伤害句 | `X% 攻击力`（**不写"魔力"**） |
| 战术句 | 5 条**属性**战术名必须出现；6 条**机制型**战术名一个都不出现（它们由饰品授予） |

**改造范围（80 条玩家技能）**：改 **46 条**（24 条战术句 + 11 条本体 debuff 改百分比 + 11 条"魔力"→"攻击力"），**34 条一字未动**；Boss 13 条里改 6 条。

**本体 debuff 的百分比口径（D233 + D249 + D250）**

| 位置 | 新口径 | 原值档映射 |
|---|---|---|
| 作用于**敌方**的本体 debuff | 敌方 `attack` **−15% / −18% / −22%** | 12 → −15%、14 → −18%、15 → −22% |
| 作用于**敌方**的 `enhanced` 半句 | 敌方 `defense` **−12% / −15%**（**相对值**：`def ← def × (1 − r)`） | 10 → −12%、12 → −15% |
| 作用于**自身**的 debuff | **保留原样，不百分比化** | 唯一一条：**清雪居合「自身敏捷 −8、持续 1 回合」**（190% 最高单体倍率的对价） |

> ⚠️ `defense` 是 **[0, 0.95] 的率**（`enemies.json` 写 `0.18` / `0.45`），不是属性点 ⇒ 写"防御 −10"是**量纲错误**（会打到负数并顶满 clamp）。

**Boss 侧 4 条我方 debuff（D234 + D196 按图分级）**：碎巢母 −4 / 菌王 −6 / 司祭 −9 / 等待者 −12；口径一律"**我方合计 X −N**"（全队合计扣减，**不是每人各扣**）。这 4 条在我方侧都是有效轴（筋力→攻击 / 敏捷→连击 / 幸运→暴击 / 灵性→强化），**不做** D233 的改轴处理，也**不做** T1 百分比化。

**两条已修复的前置代码事实（落笔前请确认现状）**

| 编号 | 问题 | 现状 |
|---|---|---|
| **D272** | `applySupportEffects` 签名里没有 `targets`，`buff`/`debuff` 一律 push 到 `caster.buffs` ⇒ **所有增减益落点恒为施法者** | 已修：改为按解析后的 `targets`（`alive`）落点；"全队"类效果按 **D273** 只结算 **1 次**到队伍级 |
| **D259** | `power = 0` 的纯辅助技**不掷强化**（强化判定只写在伤害路径），配表里写的 `enhanced` 全是空头承诺 | 已修：辅助路径同样掷强化，语义与伤害路径一致；`buffTargets` 必须传，否则落点回落到施法者 |

> 修复顺序（不可调换）：**先修落点（D272）→ 再修"伤害路径下也要结算非伤害 `effects`"→ 最后才落 D233/D249 的数值**。

---

### 20.4 战术系统总览（我方 11 条 = 属性 5 + 机制型 6）

| 项 | **属性战术（5 条）** | **机制型战术（6 条）** |
|---|---|---|
| 授予者 | **技能**（每技能绑定 1 个战术，D217） | **饰品**（开局授予 1 次、免费，D216/D217） |
| 层数上限 | **`min(3, 承载技能数)`**（D241 + D260） | **1 层**（不可叠，D220） |
| 持续 | **3 回合**，每次释放**刷新**（D200 + D219） | **整场**，不参与递减（D220） |
| 叠层 | 释放绑定该战术的技能 **+1 层**；`enhanced` 命中再 **+1 层** | 开局即满 |
| 归零 | `duration` 归零 ⇒ **整条连层一起清空**（不是掉 1 层） | 永不清空 |
| 节流 | **只由 AP 成本承担**（D278） | 无（免费、被动） |
| HUD | 占战术 HUD（最多 5 条，**D328**） | **不占战术 HUD**，放战斗界面另一处（**D332**） |

**三条"不要两个真源"的纪律（D228 / D231）**

1. **战术侧是真源**：饰品只声明"授予哪个战术"（配表新增 `tactic?: string` 一个字段），数值与结算全在战术侧；饰品结算函数里**不得**再写一份 `if (hasAccessory(...))` 分支。
2. **技能侧不加字段**：战术授予由 `effects` / `enhanced` 的 `stat` **机械推导**（D231）。加 `tactic?: string` 会造出第二个真源。
3. **承载是队伍级一份**：`state.tactics`（不是 `BattleUnit.buffs`）。`teamWithBuffs()` 只读出手者的 buff，若走 buffs 复用路线覆盖率只有 1/N；队伍级字段天然覆盖全队，**且不会"N 份叠乘"**（读取口只有出手者一处）。

**机制型战术的合流真源（饰品类 3 / 类 4）**：类 3 三件 + 类 4 三件 = **6 件，一一对应零剩余**。
⚠️ 类 4「全队每大回合只生效 1 件、优先级 档 III ＞ 档 II ＞ 档 I」⇒ **类 4 只授予当前生效的那一件** ⇒ 机制型那一组**最多 4 条**（类 3 三件 + 类 4 一件）。

---

### 20.5 属性战术：数值、承载与叠层

**数值口径**

| 项 | 值 |
|---|---|
| 每层加成 `x` | **该维的 2.3%**（百分比，自动适配等级；D222） |
| 第 4 / 5 层 | **×0.67** ⇒ **1.54%**（前 3 层线性） |
| 层数上限 | **3**（D241），实际生效值 = **`min(3, 承载技能数)`**（D260） |
| 持续 | 3 回合，每次释放刷新 |
| CD | **无 CD**（D219 废除战术 CD，D200 的"CD 重置"正式解释为"持续时间刷新"） |

```
系数表 = [1.00, 1.00, 1.00, 0.67, 0.67]  ⇒ Σ = 4.34
x = 10% / 4.34 = 2.304%  ⇒ 取 2.3%      （满层目标值 = 该维 10%，对标"3 件类 1 饰品 ≈ 10%"）
```

| 层 | 每层 | 累计 | 状态 |
|---|---|---|---|
| 1 | +2.3% | 2.3% | ✅ 可达 |
| 2 | +2.3% | 4.6% | ✅ 可达 |
| **3** | +2.3% | **6.9%** | ✅ **当前满层（≈ 2 件类 1 饰品 6.6%）** |
| 4 | +1.54% | 8.44% | ⏸ 须先补承载技能才可启用 |
| 5 | +1.54% | 9.98% | ⏸ 须先补承载技能才可启用（≈ 3 件类 1 饰品 10%） |

> 🔴 **"预留 5 层"这一表述作废**，一律写「**须先补承载技能才可启用**」。
> **层数上限不是策划定的，而是被配表里的承载技能数物理限制的** —— 想抬高上限只能去配表加承载技能，**改常量无效**。

**5 条属性战术表**

| # | `id` | 名字 | 我方落点通道 | 承载技能 / 角色 | **层数硬上限** |
|---|---|---|---|---|---|
| 1 | `tac_fire` | 火力校准 | 筋力 → `team.attack`（筋力 ×5） | 4 / 4 | **4**（5 层不可达） |
| 2 | `tac_armor` | 结构加固 | 耐力 → `team.maxHp`（耐力 ×20） | 5 / 5 | **5** |
| 3 | `tac_tempo` | 机动节拍 | 敏捷 → 连击率 | 6 / 6 | **6** |
| 4 | `tac_aim` | 弹道校准 | 幸运 → 暴击率 | **3 / 3（零冗余）** | **3**（可可 + 琳 + 白雪必须全上阵） |
| 5 | `tac_amp` | 相位增幅 | 灵性 → 强化率 + 治疗（+0.5/点） | 6 / 6 | **6** |
| ~~—~~ | ~~`tac_arc`~~ | ~~秘法增幅~~ | ~~魔力 → 经验倍率~~ | — | **D226 已删除**（魔力战斗内无效） |

**3 层满层的绝对值（L4 / L8 / L11 / L17）**：筋力等 +5.4 / +10.4 / +13.2 / +22.4 点；攻击恒 **+6.9%**；连击 / 暴击 / 强化 **+2.2 / +4.2 / +5.3 / +9.0 pp**（相位增幅另加治疗 +2.7 / +5.2 / +6.6 / +11.2）。

> ✅ **D277**：L4 只 +2.2pp 是**基线低**导致的正确行为，不是口径错（固定值必然随等级漂移：草案实测 v1 的固定值在四档上是 +19.2% / +9.9% / +7.9% / +4.6%）⇒ **不改**。

**叠 3 层的真值表（D242 之后）**

| 战术 | 1-AP 技数 | 确定性最短路径 | 回合 | AP | 第 1 回合可达？ |
|---|---|---|---|---|---|
| 火力校准 | 3 | R1：沉默之势 + 绯冠加冕 + 家务全役（各 1 AP） | 1 | 3 | ✅ |
| 结构加固 | 5 | R1：任 3 条（全 1 AP） | 1 | 3 | ✅ |
| 相位增幅 | 4 | R1：白金礼赞 + 鹿角祝福 + 蓝月帷幕（或任 3 条 1-AP 技） | 1 | 3 | ✅ |
| 机动节拍 | 2 | R1：巷影疾走 + 银狐追迹（2 AP）→ R2：暗影突进（2 AP） | 2 | 4 | ❌ |
| 弹道校准 | 1 | R1：花语祝福（1）+ 猎食扑击（2）→ R2：剑镰双面（2） | 2 | 5 | ❌ |

- **概率捷径**：1-AP 技（1 层）+ 伤害技触发 `enhanced`（当次 +2 层）= **3 层 / 3 AP / 1 回合 / 2 名角色**。D259 修好后，5 条战术**全部**有捷径（结构加固从不落壁垒 / 枪阵立ち 的 0 条变 2 条；弹道校准不增，因花语祝福的 `enhanced` 是 `heal`，不给层）。
- **维持成本**：`duration = 3` + 释放刷新 ⇒ **1 AP / 2 回合**（可以换技能换角色，升耗永不触发）。
- ⚠️ **回本门槛 ≈ 23 回合（3 层）/ 27 回合（5 层）**，与实际战斗长度（D304：普通 2–4）不在一个量级 ⇒ **属性战术不是"要专门投资"的模块，而是"顺带产生的复利"**；专门凑人叠层在数学上是亏的（**D241** 的结论句）。UI **不得**提示"距满层还差 N 层"或做叠层进度奖励。

**授予关系的机械推导（D231，零新增字段）**

```ts
tacticsOf(skill):
  const stat = (skill.effects ?? []).find(e => e.type === 'buff' && e.stat)?.stat; // ① 第一条带 stat 的轴
  const id   = stat ? STAT2TAC[stat] : 'tac_amp';                                  // ② 无 stat ⇒ 兜底
  const up   = (skill.enhanced ?? []).some(e => e.type === 'buff' && e.stat);       // ③ enhanced 是否含 buff
  return { id, layers: 1, enhancedLayers: up ? 1 : 0 };                            // ④ 命中 ⇒ 本战术 +1 层（R2）
```

四条规则：
- **R1**：**1 技能 = 1 战术**。副轴（`effects[1]`）**不再生成第二条战术**。主轴 = "`effects` 里**第一条带 `stat` 的轴**"（不是 `effects[0].stat`——有 3 条技的 `effects[0]` 是 `heal`）。
- **R2**：`enhanced` 命中 ⇒ **只给本技能已绑定的那 1 条**额外 +1 层，**不产生第二个战术**。
- **R3（D13 精神）**：能补一个直接效果的就必须补（治疗 / 削弱敌方 attack / 削弱敌方 defense）。
- **R4（D242）**：每（单位，技能）每回合限 1 次 ⇒ 叠 N 层需要 N 个不同技能 × N 名不同角色。

**两条必须显式写进代码的兜底**

| 兜底 | 技能 | 处理 | 理由 |
|---|---|---|---|
| **B1a / B1b** | 鹿角祝福 `skill_alst_healing_antler` / 天使赐福 `skill_srn_angel_blessing` | **显式授予 `tac_amp` 相位增幅 ×1**（推导会输出空集） | 它们的 `effects` 只有 `heal`、无任何 `stat`；灵性是唯一与治疗有数据关联的轴（治疗第二项 = 灵性 × 0.5） |
| **B2（措辞修正，非兜底）** | 花语祝福 / 茶点侍奉 / 蓝月帷幕 | 取"`effects` 里第一条带 `stat` 的轴" | 这 3 条 `effects[0]` 是 `heal`，`stat` 在 `effects[1]` ⇒ 弹道校准 / 结构加固 / 相位增幅 |

> ⚠️ B1a / B1b 必须写成代码里的一个 `if`，**不能靠推导函数顺手兜住**。

**24 条承载技能（按战术分组；合计 24 = 15 条 `power=0` + 9 条"伤害 + buff"）**

| 战术 | 承载技能（持有角色） |
|---|---|
| `tac_fire` 火力校准（4） | 沉默之势（玄铁）· 绯冠加冕（艾尔薇拉）· 家务全役（芙兰）· 暗金锋刃（卡蜜拉，power 120） |
| `tac_armor` 结构加固（5） | 不落壁垒（瓦伦蒂娜）· 茶点侍奉（玛德琳）· 粉盾壁垒（罗莎莉）· 枪阵立ち（蓝缇雅）· 银盾庇护（露娜） |
| `tac_tempo` 机动节拍（6） | 巷影疾走（凛）· 银狐追迹（雪铃）· 暗影突进（妮克丝，105）· 龙尾回旋（缇娅，44×2）· 赤白疾驱（莉泽，46×2）· 双马尾突进（蒂法，132） |
| `tac_aim` 弹道校准（3） | 花语祝福（可可）· 猎食扑击（琳，110）· 剑镰双面（白雪，50×2） |
| `tac_amp` 相位增幅（6） | 白金礼赞（塞拉菲娜）· 鹿角祝福（艾莉丝缇雅，B1a）· 蓝月帷幕（伊莲）· 天使赐福（塞蕾娜，B1b）· 有礼之一击（梅莉，49×2）· 黑裙礼赞（艾丝黛，128） |

- 24 条战术技分布在 **24 个不同角色**上（每名角色恰好 1 条战术技）⇒ **单角色每回合对战术系统的推进量 ≤ 1 层**（`enhanced` 命中时该次为 2 层）。
- 出战位默认 4，`tech_party_5` / `tech_party_6` 各解锁 1 位 ⇒ **上限 6**；叠 3 层要求队里同时有 3 名持该战术技能的角色。
- 15 条 `power = 0` 技按 D13 精神各补了直接效果：**9 条治疗 + 6 条削弱敌方**（−8% / −10%），**没有一条是"耗 AP 但无效果"的废技**。

---

### 20.6 机制型战术（6 条，饰品授予、整场、1 层）

| # | `id` | 名字 | 效果 | 数值 | 授予饰品 | 类·档 |
|---|---|---|---|---|---|---|
| 1 | `mec_idle` | 空转卸载 | 每大回合我方**首次**受到的伤害减半（保底 ≥ `minDamage` = 1） | **×0.5** | 空转惰轮 `acc_t4_idler` | 类 3 · 档 II |
| 2 | `mec_solve` | 装甲解算 | 我方**单次**受到的伤害钳制在"队伍最大生命 × 25%"以内 | **≤ 25% maxHp** | 装甲解算器 `acc_t3_solver` | 类 3 · 档 III |
| 3 | `mec_ballast` | 压舱配平 | 我方受到的**比例类**伤害减半（深渊吞噬 8% / 孢子中毒 3% / 深渊汲食 6%） | **×0.5**（保底 1） | 压舱锚针 `acc_t5_ballast` | 类 3 · 档 III |
| 4 | `mec_plate` | 镀层闭合 | 每大回合开始时，队伍恢复最大生命的 4% | **+4% maxHp / 大回合** | 自愈镀层 `acc_r1_plating` | 类 4 · 档 I |
| 5 | `mec_reflect` | 相位反射 | 每大回合我方首次受击时，对敌人造成"队伍攻击力 × 30%"的固定伤害 | **0.30 × team.attack**（≈ +2.9% 输出/回合） | 相位反射片 `acc_r2_reflector` | 类 4 · 档 II |
| 6 | `mec_dissoc` | 逆向解离 | 我方阶段开始时，清除我方当前生效的 **1 条**"临时属性削减"（剩余时间最长优先，同长取削减总量最大者） | **1 条 / 大回合** | 逆向解离针 `acc_r4_dissoc` | 类 4 · 档 III |

- **零新增数值**：6 条里唯一的输出项是 `mec_reflect` 的 +2.9%/回合，已在饰品伤害轴预算内。
- 饰品栏与战术栏读**同一个 `state.tactics`**；饰品文案里的效果说明写「→ 见战术栏」，**不允许两处各写一份数值**。
- 验收护栏：`mec_plate` 的回血日志条数 == 回合数（不得 2×）；`mec_solve` 的钳制单次受击只出现一次；`mec_reflect` 每大回合至多 1 次。

---

### 20.7 敌方战术（9 条 = 属性 5 + 族群 4）

| 项 | 我方 | 敌方 |
|---|---|---|
| 战术池 | 5 属性 + 6 机制型 = **11** | 5 属性 + **4 族群专属** = **9** |
| 来源 | 技能授予 + 饰品授予 | **技能授予**（D340，**无饰品侧**） |
| 上限 / 持续 | `min(3, 承载技能数)` / 3 回合刷新 | **3 层 / 3 回合 / 每次释放刷新**（D341）；**D346 允许差异化** |
| 节流 | AP 成本 | **无 AP** ⇒ 靠**出手次数**节流（D151）+ §20.7 的闸门 |
| 效果的落点通道 | 属性轴 + 概率轴全部有读取点 | 🔴 **只有 D366 的 6 个格子** |

**D366：不补读取点，改设计 ⇒ 敌方战术的可用效果集（4 类 / 6 个落点）**

| ✅ 可用 | ❌ 不可用（及原因） |
|---|---|
| ① 直接改 HP（治疗 / **护盾**） | ❌ 概率轴（combo / crit / enhance）—— 敌方是常量，且 enhance = 0 |
| ② 改出手次数 | ❌ `speed`（已不参与出手顺序） |
| ③ 改敌方自己的 `attack` / `defense` | ❌ 魔力 / 幸运 / 灵性 / 敏捷（这四个属性在敌方数据里**不存在**） |
| ④ 直接伤害 / `damage_cut` | ❌ 任何需要新字段或新读取通道的效果 |

> **落地方式**：`resolveEnemyAction` 已经把 `attacker.attack` 与 `target.defense` **当字面值读** ⇒ 把层数**原地乘算写进 `unit.attack` / `unit.defense` 这两个数字本身**，就不需要任何新读取点。结构加固也改用**护盾**表达（避免碰 `maxHp`）。

**敌方 5 条属性战术的落点与数值**（配表 `src/data/tables/tactics.json` 已按此落）

| 战术 | 与我方的关系 | 敌方落点 | 每层 | 3 层满层 | 授予技能 | 该技 power |
|---|---|---|---|---|---|---|
| `tac_fire` 火力校准 | **同落点** | `unit.attack` 原地乘算 | **+2.3%** | +6.9% | `enemy_heavy_blow` 重击 | 140 |
| `tac_armor` 结构加固 | 换通道（`maxHp` → 护盾） | 叠加护盾（`maxHp × 2.3%/层`，先扣盾再扣 HP） | **+2.3%** | +6.9% maxHp 的盾 | `enemy_sweep` 横扫 | 43×2 |
| `tac_tempo` 机动节拍 | 换通道（连击率 → 出手次数） | **满 3 层才启动节拍器**：每轮 `tempoCounter += 1`，累计 ≥3 时该轮额外出手 1 次并清零；**整场上限 1 次** | — | 整场 **+1 次出手** | `enemy_bite` 撕咬 | 100 |
| `tac_aim` 弹道校准 | 换通道（暴击率 → 附加真实伤害） | 命中附加 `attack × 1.5%/层` 的**真实伤害**（不吃防御） | **+1.5%** | +4.5% | `enemy_venom` 毒液 | 80 |
| `tac_amp` 相位增幅 | 换通道（强化率 → 减伤） | `damage_cut` −1.5%/层 | **−1.5%** | −4.5% | `enemy_dark_flame` 冥火 | 115 |

- 属性 5 条**零新增技能**，挂到现有的 5 条 `power > 0` 攻击技上（AI 只挑 `power > 0` 的技，`power = 0` 的技永不被选）。
- ⚠️ **不要把任何战术挂到 `enemy_curse` / `enemy_dread_roar` 上**（`power = 0` ⇒ AI 永不释放；且降的 `spirit` / `strength` 在敌方侧无读取点）。`enemy_regen` 同理（只在 HP < 40% 时放）。
- 🔴 **`tac_tempo` 的计数器必须写"满 3 层才 +1"**，不能写 `tempoCounter += 层数` —— 后者会让普通敌 1 层就在 3 回合战里换到 1 次额外出手（≈ +25~33% DPS），与"普通敌叠不到 3 层 ⇒ 0 效果"自相矛盾。
- **D383** 已裁：机动节拍 / 弹道校准 / 相位增幅这 3 个属性位在敌方侧是**替代机制**（效果对齐敏捷 / 幸运 / 灵性但走不同机制）⇒ **D342 的"属性共用"实际只对火力校准 / 结构加固两条同字面成立**；"共用"的语义 = **共用 id / 名字 / HUD 条位**，不共用落点。

**4 条族群专属战术（每图 1 条，图独占）**

| 图 | `id` | 名字 | D366 格子 | 每层 | 3 层满层 | 授予技能（新增） |
|---|---|---|---|---|---|---|
| 无根回廊 | `tac_grp_scab` | **残躯预演** | ① HP（护盾） | 该敌 HP **首次跌破 50%** 时获得一次性护盾 `maxHp × 4% × 层数`（触发后不再重算） | **+12% maxHp** 一次性护盾 | `enemy_grp_scab` 舐创（power 95） |
| 苔痕回廊 | `tac_grp_spore` | **孢裂预支** | ④ 直接伤害 | 每次出手后，对我方队伍池造成「**当前**总生命 × 1%」的直接伤害（×1，D273） | 每次出手 **3%** | `enemy_grp_spore` 吐孢（85） |
| 静谧墓所 | `tac_grp_penance` | **缄口以血** | ③ `defense` + ④ 直接伤害 | ① 敌方 `defense` 原地乘算 **+1.5%**；②「亡骸重整」反转伤害中 **15% × 层数** 追加为我方队伍池真实伤害 | ① `defense +4.5%`；② **反噬 45%** | `enemy_grp_penance` 缄口（90） |
| 腐朽王座 | `tac_grp_tithe` | **潮位下移** | ③ `attack` + ④ 直接伤害 | ① 敌方 `attack` 原地乘算 **+1.5%**；② 深渊吞噬阈值 **−1 阶段**；③ 3 层时吞噬伤害 `8% → 12%` | ① `attack +4.5%`；② 阈值 **3 → 1**；③ **12%** | `enemy_grp_tithe` 汲食（110） |

三条必须写清的边界：

- **司祭不吃「亡骸重整」**（D166，Boss 是族群首领）⇒ **司祭战中 `tac_grp_penance` 的反噬半条恒为 0**，只剩 `defense` 半条。**必须写进配表与 UI 备注**，否则玩家会看到"满层却没反噬"而以为是 bug。
- **残躯预演的护盾只给本体，残躯不带**（`splitDone` 之后族群特性整体失效）⇒ 残躯阶段不可能再触发第二次护盾。
- **潮位下移的三层阶梯**（避免 2 层与 3 层撞在同一阈值）：1 层 = 阈值 3→2；2 层 = 阈值 2→1；3 层 = 阈值保持 1，但伤害 `8% → 12%`。

**族群授予技怎么挂 —— 占位式（D437）**

> 每只敌人的 `skillIds` **长度不变**（D153：普通 2 / 精英 3 / 虚影 3 / Boss 3–4），族群授予技**占既有技能位**，**任何档位都不新增第 N+1 条**。

| 档位 | 占哪一位 | 该位原先放什么 | 其余位 |
|---|---|---|---|
| **普通敌**（32 只） | **第 2 位** | 第二条主攻技 | 第 1 位 = 该敌最高 raw `power` 的主攻技（AI 65% 放它） |
| **精英**（4 只） | 第 1 或第 2 位 | 一条常规技 | 第 3 位 = 大招位（不动） |
| **虚影**（4 只） | ❌ **不挂（D438）** | — | `enemy_abyss_lash` + 图内常规技 + 大招位 |
| **Boss** | 3–4 位之一 | 一条常规技 | 其余位不动 |

- **D438**：虚影 `family = 'echo'`（D415，不吃任何族群被动）⇒ 给"不属于本图族群"的虚影挂本图族群战术，读感是错的；且虚影**四图共用同一个 id**，天然带不了图独占技。
- 4 条 `enemy_grp_*` 的 `power` **刻意低于该图主攻技**（95 < 撕咬 100/腐肉穿刺 130；85 < 重击 140/孢刺 115；90 < 重击 140；110 < 重击 140/冥火 115）⇒ **不抢 AI 的"最高 power"位**，只能靠"战术优先"分支被选到。

**叠层算式与实测层数**

```
层数(t) = min(3, ceil(P × a × t))      满层时间 T_full = 3 / (P × a)
  T = 该场回合数；a = 该敌每轮出手次数（普通 1 / 精英 2 / Boss 1）
  P = 该次出手使用"授予目标战术的技能"的概率（建议 0.5，可调 0.35–0.65）
```

| 敌人类别 | `a` | TTK 目标（D304） | 满层时间 | 该场实际层数 |
|---|---|---|---|---|
| 普通敌 | 1 | 2–4 回合 | 6 回合 | **1–2 层**（典型 1，永远到不了 3） |
| 精英 | 2 | 5–10 回合 | 3 回合 | **3 层**（第 3 回合满，之后刷新维持） |
| Boss | 1 | ≥10 回合 | `3n / (P × k)`（循环长 `n`、授予技出现 `k` 次） | **1–2 层** |

- 🔴 **敌方不受 D242 约束（D261）⇒ 可以反复用同一条技能叠层** ⇒ **"承载技能数"这个硬上限对敌方不成立**；敌方上限只由"出手次数 × 回合数"决定。
- ⚠️ 占位式下**每只普通敌恒有 2 个战术来源**：族群战术（32/32 都能叠）+ 属性战术（只有第 1 位主攻技恰好是 5 条属性授予技之一的那些，约占一半）⇒ **每只普通敌至少亮 1 条战术**，HUD 不会空槽。

**三条抑制手段（都不动 D341 / D151 / D304）**

| # | 手段 | 做法 | 效果 |
|---|---|---|---|
| **G-c ⭐** | AI「战术优先」 | `chooseEnemyAction` 加前置分支：若存在"授予某条**未满层**战术"的可用技能 ⇒ 以概率 **P**（建议 0.5，不宜 > 0.65）使用其中一条；**排序 = 层数最低优先，同层按 `power` 降序** | `P` 是唯一的全局节流旋钮（不改裁决、不改数值） |
| **G-d ⭐** | 每战术每轮最多 +1 层 | `enemyTacticGainThisRound: Set<string>`，`endRound()` 清空（与我方 `usedThisRound` 同一时机） | 堵住"精英二动 → 同一战术 2 层/轮" |
| **G-g ⭐** | 机动节拍额外出手整场上限 | `ENEMY_EXTRA_ACTION_CAP = 1`，**前置条件 = 满 3 层才启动节拍器** | 普通敌 / Boss（1–2 层）恒 0 效果；精英 ×1.05 |
| G-d2 | `damage_cut` 与护盾叠加上限 | `ENEMY_DAMAGE_CUT_CAP = 0.20` | 防止未来减伤源叠加把敌方变成无敌 |

> ⚠️ **明确反对**：不要把"只有大招才授予"当主闸门（普通敌没有大招 ⇒ 普通战恒 0 层，D337 白裁）。

**整场强度影响（给重算用的输入）**

| 项 | 值 |
|---|---|
| 满层上界 | 输出面 **×1.173**（1.069 × 1.045 × 1.05）、耐久面 **×1.119**（1.069 × 1.047） |
| 精英（会满层） | 折算整场 **+12%** |
| Boss（1–2 层） | 折算整场 **+4%** |
| 普通敌 | 综合压力 **×1.055 ~ ×1.16**（属性层 + 族群层，不再可忽略） |
| **整场（加权 82 / 15 / 3）** | **+9% ~ +12%，中值 +10.5%** |
| 建议的重算标定 | 精英 DPS / 耐久 **×1.12**；Boss 输出 / 耐久 **×1.04** |

**敌方 HUD（D326 / D328 / D342）**：血量 HUD **上方、左对齐**；**最多 5 条、只放属性战术**；**按该敌实际持有的授予技能动态显示（1–5 条），不预置空槽**；槽内容 `战术名 ▮▮▮ +X% · 剩 N 回合`，有层数才亮。族群专属 4 条**不占**该 HUD。
⚠️ 两处"满层但效果为 0"必须打标：司祭战的 `tac_grp_penance`；`tac_tempo` 的额外出手已用尽。

**两个字段、都不落盘（D439）**

```ts
state.enemyTactics: Record<tacticId, { stacks: number; turnsLeft: number }>  // 层数真源
enemyTacticGainThisRound: Set<string>                                        // 每战术每轮 +1 层的闸门
```
- 与我方 `state.tactics` 对称（D3 永久单敌 ⇒ 敌方只需一份）。
- **只在战斗内存在**，战斗结束 / 团灭即丢弃 ⇒ **存档里不出现这两个键，零迁移逻辑**。
- 递减挂 **`onEnemyPhaseStart()`**（各自阶段递减自己），否则敌方在第 N 轮末尾叠的层会在第 N+1 轮开头（我方阶段）立刻 −1 ⇒ "持续 3 回合"实际只剩 2 回合。

---

### 20.8 我方清除敌方战术（D344）

**3 条改造技能（不新增技能位，保 D55「每角色 2 个」）**

| 通道 | 技能 | 角色 | `desc` 新增半句 |
|---|---|---|---|
| ② **必定清除** | `skill_xt_iron_silence` 沉默之势 | `char_04` 玄铁（★4，**默认解锁**） | 「…敌方攻击 −10%，**必定清除敌方 1 条战术**；授予火力校准 ×1。…」 |
| ① 概率清除 | `skill_ofl_battlefield_collapse` 压塌战场 | `char_22` 欧菲莉亚（★5） | 「…敌方攻击 −18%，**并有 40% 概率清除敌方 1 条战术**；强化：敌方防御 −15%。」 |
| ① 概率清除 | `skill_srn_feather_judgment` 羽刃审判 | `char_31` 塞蕾娜（★5） | 「…敌方全体 2 段 58% 攻击力，**并有 40% 概率清除敌方 1 条战术**。」 |

- 分散在三个不同角色，每名角色只动 1 条技能；**②（必定清除）压在默认解锁的 ★4 上** ⇒ 新玩家开局就有反制手段。
- **D428**：沉默之势由此从"给自己加筋力/耐力"变成"削敌方攻击 + 清除" ⇒ 玄铁少一个耐久向 buff，**已裁定接受**（"沉默"最贴题，且保住"默认解锁"这条可用性）。
- `skill_xt_iron_silence` 的 `target` 由 `'self'` 改为 `'enemy'`：因选敌交互已删 + D3 永久单敌 ⇒ 单体技自动锁定唯一敌人，**零交互成本**。

**7 条实现规则**

| # | 规则 | 值 / 语义 |
|---|---|---|
| 1 | 清除粒度 | **整条移除**（`delete state.enemyTactics[id]`，O(1)；层数 + 持续一起清零）。**不要**改成"清 1 层再连清至 0" —— 只削 1 层会被"每次释放刷新"立刻补回 |
| 2 | 选条 | **层数最高优先**；同层取 HUD 第一条（**确定性，不要随机**） |
| 3 | 0 条可清时 | 允许空放，不拦、不报错、不额外消耗（与"AP 用尽"同一取舍） |
| 4 | 判定次数 | **单次行动只 roll 一次**（`hits: 2` 的两条**合计判定 1 次**） |
| 5 | 判定时机 | **先结算伤害 → 目标仍存活 → 再 roll**（目标已死则清层无意义） |
| 6 | 概率常数 | 通道 ① 统一读 **`balance.tacticClearChance = 0.4`**（不要在技能里写死）；通道 ② 恒 1 |
| 7 | 当轮不回补 | 清除**只写 `state.enemyTactics`**，**不同步** `enemyTacticGainThisRound` ⇒ 同一轮内被清掉的战术**当轮无法重新叠起** |

> 🔴 **D440：`clear_tactic` 做成"伤害之后的独立结算钩子"，严禁塞进 `effects` 数组**。
> 理由：`resolveAction` 的 `isDamaging` 短路让"伤害 + `effects`"的 `effects` 完全不结算 ⇒ 塞进 `effects` 会让**通道 ① 静默失效**（比"不生效"更糟：它是坏的、但看起来是好的）。
> 签名：`{ type: 'clear_tactic'; value: number; chance?: number }` —— **`value` 钉死为"清除的目标条数"= 1**（**不是层数**）；`chance` 省略 = 必定（读不到时取 `balance.tacticClearChance`）。
> 并入 **D272 修复批次**的顺序约束。

---

### 20.9 战斗长度：D108 的结案与验收门

**三档硬门（D268 定义、D304 细化）**

| 敌人类型 | 硬门 | **第 79 轮实测（已达成）** |
|---|---|---|
| 普通敌 TTK | **2–4 回合** | **2.29 – 3.53** ✅ |
| 精英 TTK | **5–10 回合** | **5.51 – 8.39** ✅ |
| Boss TTK | **≥10 回合** | **10.25 – 15.34** ✅ |
| 1 回合团灭（D268） | **0** | **0 / 528** ✅ |
| 难度可感知（噩梦 ÷ 简单） | ≥1.5 | **仅 ×1.05** ⚠️ **仍不可感知** |

**根因的三层结构（D108 实测结论）**

| 层 | 结论 |
|---|---|
| ① 配置层 | **不是主因**（饰品单件只 +1.07%，三件合计约 +9.7%，成长曲线 <1%） |
| ② 结构层（主因） | **每轮我方有 4 个行动，敌方只有 1 个** |
| ③ **决定性变量** | **D242（每技能每回合限 1 次）⇒ TTK 直接拉到 6.15 回合** |

> 🟢 **结论：让战斗从 1–2 回合变成 5–10 回合的是 D242 这一条规则，不是任何数值改动。**
> ⇒ **D242 是实现侧的硬前置，不是可选项**（R6）；批次 2 的解锁条件就此满足（**D356**：战术叠层 / 深渊吞噬 / 饰品防御类 / 精英二动 / R2 反击 全部可验收）。
> ⇒ **§10 #32「我方输出 ∝ N²」的归因已更正**：∝N² 真实但次要（N=4 时约 1.3 倍），此前把它列为结构性主因是**归因过度**。

**⛔ 已被否决的草案方案（`combat-length-recalibration-v1.md` 的★推荐，未采纳）**
草案曾提出 5 条改动：`A（我方伤害改用每单位自身筋力，等效 ÷N）` + `D1 真共享 HP 池` + `defLayerScale 0.30 → 0.20` + `敌方 HP ×4` + 验收阈值写进 `mc-difficulty.mjs`。
**已被第 79 轮的 D108 结案否决** —— 三档硬门在不做任何数值改动（仅靠 D242）的前提下即已达成。备选的"纯配表版（`defLayerScale 0.20` + 敌方 HP ×16）"同样未采纳。
草案里的"现状 TTK = 1.00、96 格全饱和"是**D242 落地前**的测量结果，读草案时不要把它当现状。

**残留的两条**

1. `GATES.winMinByDiff.nightmare = 0.40` 是草案自定的阈值，未被正式裁定；实测噩梦胜率已远超它。
2. **Boss 只有下限 `≥10`，没有上限**。零改动世界里的实测是 10.25–15.34，暂无"磨"的问题；若将来 Boss 数值上调，需补 `bossTtkMax`（草案建议 ≤25）。

---

### 20.10 敌人：口径与全表

**按层缩放（D14，配表写基础值）**

```
第 N 层：HP × (1 + 0.5×(N−1))   ⇒ 第 6 层 ×3.5
        攻击 × (1 + 0.25×(N−1)) ⇒ 第 6 层 ×2.25
        防御 × (1 + 0.3×(N−1))  ⇒ 第 6 层 ×2.5
```

**强度硬上限（基础值）**：**HP ≤ 220 / 攻击 ≤ 53**（锚点 = 重定标后的骸骨暴君）· **防御 ≤ 0.30**（**D414**，以现役最高的石魔像为准）。
⚠️ 石魔像第 6 层 `0.30 × 2.5 = 0.75` ⇒ 我方只能打出 25% 伤害，**该风险已在 D414 明确接受**；**D419** 要求它的 TTK **不得越过 Boss 硬门（≥10 回合）**。

**池位与分组（D89 / D148 / D327）**：每图 `enemyPool` 分 `shallow`（第 1–3 层）与 `deep`（第 4–6 层）两组，各 4 只；浅层组 = 全池均值 ÷1.1，深层组 = 全池均值 ×1.1（**两组只用"技能与机制"拉开，经验金币不拉开**）。
**敌人总数 = 32（4 图 × 8）**——**D413** 已裁定：D148 写的"共 28 只"是算术错（4 × 8 = 32）。

**池均值被刻意锚死在现役值**（保住 4 个已定稿 Boss 的承重条件：`Boss HP = 同层普通敌 HP × 9`、攻击 ×3.0、防御 ×1.6）：

| 图 | 池均值 HP | 池均值攻击 | 池均值防御 | Boss 血量比（D48 要求 8–10×） |
|---|---|---|---|---|
| 无根回廊 | 36.38 | 9.63 | 0.025 | 碎巢母 **9.07×** |
| 苔痕回廊 | 61.38 | 16.38 | 0.069 | 菌王 **8.98×** |
| 静谧墓所 | 107.75 | 23.75 | 0.140 | 司祭 **8.96×** |
| 腐朽王座 | 148.38 | 35.88 | 0.160 | 等待者 **8.95×** |

**奖励推导**：`exp = round(hp × 图比率)`、`gold = round(hp × 图比率)`；四图 `exp/hp` = **0.380 / 0.404 / 0.419 / 0.545**，`gold/hp` = **0.270 / 0.317 / 0.315 / 0.444**（**D416** 允许现役怪的 exp/gold 随统一曲线微调，**三轴强度不得上调**）。36 条敌人的 `techPoints` 一律 **0**（D254 / D255）。

**精英口径（D118 / D149 / D206 / D411 / D254）**

```
精英 HP   = 该图全池普通敌 HP 均值 × 1.5
精英 攻击 = 该图全池普通敌 攻击均值 × 1.5
精英 防御 = 该图普通敌的防御最大值（不吃 ×1.5 —— 防御是率）
精英 奖励 = 池均值 ×2（D206）；科技点 0（D254）
精英 必掉 = 该图能产出的最高档锻造材料（D411）：
           无根 / 苔痕 → item_ore ；静谧 → item_crystal（+ item_ore）；腐朽 → item_core（+ item_crystal）
出现方式 = 15% 概率替换普通遇敌（D118）；不进 enemyPool
```

**敌方 AI 的三条硬事实（决定"2 技能"怎么读）**

| # | 事实 | 后果 |
|---|---|---|
| A1 | AI 只挑 `power > 0` 的技，按 **`power × hits`** 降序取第 1 条，65% 概率放它、否则普攻 | 低于同池最高的技能，AI 永不释放 |
| A2 | 自愈技只在 `hp/maxHp < 0.4` 时被选，判定须同时认 `heal` 与 **`heal_max_ratio`** | 见 **D410**（否则新技永不释放） |
| A3 | `hits ≥ 2` 按 `power × hits` 参与排序 | 见 **D409**（原按 raw `power` 排序 ⇒ `enemy_sweep` 等 3 条多段技是死技能） |

> **D409 / D410 均已裁为必修**：① 排序键改 `power × (hits ?? 1)`；② 自愈判定改 `e.type === 'heal' || e.type === 'heal_max_ratio'`。

**四图族群被动（D121，保留原语义；D273 ⇒ 全队效果 ×1）**

| 图 | `family` | 被动 | 关键口径 |
|---|---|---|---|
| 无根回廊 | `swarm` | **群体本能**：HP 归零后分裂成残躯 | **普通敌继承 25%**（D121 原值）／**碎巢母 15%**（D169 的 Boss 例外）—— **D424** 勘误；攻击 ×0.7；只触发 1 次；**残躯不继承特性** |
| 苔痕回廊 | `fungus` | **孢子爆发**：被击败时我方全队中毒 2 回合（每回合 `maxHp 3%`） | **死于连击第 2 段及以后则不触发**；Boss（菌王）为"HP 首次跌破 50% 触发"（D165） |
| 静谧墓所 | `undead` | **亡骸重整**：受到治疗时反而受到**等量**伤害 | **司祭不吃这条**（D166，Boss 是族群首领） |
| 腐朽王座 | `abyss` | **深渊吞噬**：第 3 个及以后的我方阶段触发 **1 次**，损失**当前生命 8%** | 不是"每回合 8%"（D159 取 (b) / D160 / D185 修订 D121） |
| 虚影（四图） | **`echo`**（D415） | **无被动特性** | 四图共用 id `enemy_abyss_king_shadow`，只靠数值衰减区分 |

> ⚠️ 静谧墓所**刻意给 3 只敌配自愈技**（灰烬守夜 / 枯骨诵者 / 空腔司墓）：自愈 → 被亡骸重整反转成等量自伤 ⇒ "会自愈的敌人反而更脆"，是一道**识别与优先级的谜题**。**盾**（怨灵 / 石魔像 / 石棺守望）不带自愈，是纯硬度题。

**32 只普通敌全表**（数值 = 配表基础值；斜杠后为第 6 层核对值，不入库；技能列第 2 位 = 本图族群授予技）

**无根回廊 `swarm`（浅层组均值 HP 33.00 / 攻 8.75 / 防 0.023；深层组 39.75 / 10.50 / 0.030）**

| id | 名字 | 档 | lv | HP 基础/第6层 | 攻 基础/第6层 | 防 基础/第6层 | exp | gold | 技能 | 掉落 |
|---|---|---|---|---|---|---|---|---|---|---|
| `enemy_slime` | 黏液怪 | 浅 | 1 | 42 / 147 | 8 / 18 | 0.05 / 0.125 | 16 | 11 | `enemy_bite` · `enemy_grp_scab` | potion_s 0.30/1-1 · ore 0.25/1-2 |
| `enemy_bat` | 穴居蝙蝠 | 浅 | 1 | 30 / 105 | 11 / 24.8 | 0 / 0 | 11 | 8 | `enemy_bite` · `enemy_grp_scab` | ore 0.20/1-1 |
| `enemy_rat` | 巨鼠 | 浅 | 2 | 36 / 126 | 10 / 22.5 | 0.02 / 0.05 | 14 | 10 | `enemy_venom` · `enemy_grp_scab` | coin_pouch 0.15/1-1 · ore 0.30/1-2 |
| `enemy_rift_lizard` | 裂脊蜥 | 浅 | 2 | 24 / 84 | 6 / 13.5 | 0.02 / 0.05 | 9 | 6 | `enemy_swarm_rend` · `enemy_grp_scab` | ore 0.25/1-1 · potion_s 0.20/1-1 |
| `enemy_shard_hyena` | 碎齿鬣兽 | 深 | 4 | 34 / 119 | 13 / 29.3 | 0.03 / 0.075 | 13 | 9 | `enemy_carrion_gore` · `enemy_grp_scab` | ore 0.35/1-2 |
| `enemy_brood_bearer` | 断巢孕体 | 深 | 5 | 50 / 175 | 7 / 15.8 | 0.03 / 0.075 | 19 | 14 | `enemy_swarm_rend` · `enemy_grp_scab` | ore 0.40/1-3 · potion_s 0.30/1-1 |
| `enemy_root_prowler` | 无根巡狩 | 深 | 5 | 30 / 105 | 12 / 27 | 0.02 / 0.05 | 11 | 8 | `enemy_swarm_rend` · `enemy_grp_scab` | coin_pouch 0.12/1-1 · ore 0.30/1-2 |
| `enemy_pack_alpha` | 混群领头 | 深 | 6 | 45 / 158 | 10 / 22.5 | 0.03 / 0.075 | 17 | 12 | `enemy_carrion_gore` · `enemy_grp_scab` | coin_pouch 0.18/1-1 · ore 0.35/1-2 |

**苔痕回廊 `fungus`（浅 55.50 / 14.75 / 0.063；深 67.25 / 18.00 / 0.075）**

| id | 名字 | 档 | lv | HP | 攻 | 防 | exp | gold | 技能 | 掉落 |
|---|---|---|---|---|---|---|---|---|---|---|
| `enemy_goblin` | 哥布林战士 | 浅 | 3 | 68 / 238 | 20 / 45 | 0.08 / 0.20 | 27 | 22 | `enemy_heavy_blow` · `enemy_grp_spore` | coin_pouch 0.25/1-1 · ore 0.30/1-2 |
| `enemy_bud_grub` | 孢芽幼体 | 浅 | 2 | 60 / 210 | 14 / 31.5 | 0.06 / 0.15 | 24 | 19 | `enemy_spore_lance` · `enemy_grp_spore` | ore 0.30/1-2 · potion_s 0.25/1-1 |
| `enemy_sweet_grazer` | 腐甜采食者 | 浅 | 3 | 48 / 168 | 13 / 29.3 | 0.06 / 0.15 | 19 | 15 | `enemy_spore_lance` · `enemy_grp_spore` | ore 0.30/1-2 |
| `enemy_moss_skulker` | 苔斑行者 | 浅 | 3 | 46 / 161 | 12 / 27 | 0.05 / 0.125 | 19 | 15 | `enemy_swarm_rend` · `enemy_grp_spore` | potion_s 0.30/1-1 · ore 0.25/1-1 |
| `enemy_ghoul` | 食尸鬼 | 深 | 4 | 96 / 336 | 22 / 49.5 | 0.10 / 0.25 | 39 | 30 | `enemy_carrion_gore` · `enemy_grp_spore` | potion_l 0.20/1-1 · ore 0.35/1-2 |
| `enemy_rot_bloom` | 腐孢绽体 | 深 | 5 | 62 / 217 | 18 / 40.5 | 0.07 / 0.175 | 25 | 20 | `enemy_spore_lance` · `enemy_grp_spore` | ore 0.40/1-3 · potion_l 0.12/1-1 |
| `enemy_mycelial_husk` | 菌丝壳骸 | 深 | 6 | 56 / 196 | 16 / 36 | 0.07 / 0.175 | 23 | 18 | `enemy_spore_lance` · `enemy_grp_spore` | ore 0.45/1-3 |
| `enemy_sweet_root` | 甜腐根须 | 深 | 5 | 55 / 193 | 16 / 36 | 0.06 / 0.15 | 22 | 17 | `enemy_swarm_rend` · `enemy_grp_spore` | potion_l 0.15/1-1 · ore 0.35/1-2 |

**静谧墓所 `undead`（浅 97.50 / 21.50 / 0.090；深 118.00 / 26.00 / 0.190）**

| id | 名字 | 档 | lv | HP | 攻 | 防 | exp | gold | 技能 | 掉落 |
|---|---|---|---|---|---|---|---|---|---|---|
| `enemy_skeleton` | 骷髅兵 | 浅 | 3 | 78 / 273 | 18 / 40.5 | 0.12 / 0.30 | 33 | 25 | `enemy_heavy_blow` · `enemy_grp_penance` | potion_s 0.30/1-2 · ore 0.35/1-3 |
| `enemy_grave_acolyte` | 墓前侍从 | 浅 | 4 | 112 / 392 | 24 / 54 | 0.10 / 0.25 | 47 | 35 | `enemy_heavy_blow` · `enemy_grp_penance` | potion_l 0.15/1-1 · ore 0.35/1-2 |
| `enemy_ash_vigil` | 灰烬守夜 | 浅 | 4 | 100 / 350 | 22 / 49.5 | 0.07 / 0.175 | 42 | 32 | `enemy_grp_penance` · `enemy_regen` | ore 0.40/1-3 |
| `enemy_bone_chanter` | 枯骨诵者 | 浅 | 4 | 100 / 350 | 22 / 49.5 | 0.07 / 0.175 | 42 | 32 | `enemy_grp_penance` · `enemy_marrow_knit` | potion_l 0.12/1-1 · ore 0.35/1-2 |
| `enemy_wraith` | 怨灵 | 深 | 5 | 88 / 308 | 26 / 58.5 | **0.15** / 0.375 | 37 | 28 | `enemy_heavy_blow` · `enemy_grp_penance` | crystal 0.18/1-1 |
| `enemy_golem` | 石魔像 | 深 | 6 | **180** / 630 | 30 / 67.5 | **0.30** / 0.75 | 75 | 57 | `enemy_heavy_blow` · `enemy_grp_penance` | ore 0.70/2-4 · crystal 0.30/1-1 |
| `enemy_grave_warden` | 石棺守望 | 深 | 5 | 104 / 364 | 24 / 54 | **0.20** / 0.50 | 44 | 33 | `enemy_heavy_blow` · `enemy_grp_penance` | crystal 0.12/1-1 · ore 0.40/1-3 |
| `enemy_hollow_sexton` | 空腔司墓 | 深 | 6 | 100 / 350 | 24 / 54 | 0.11 / 0.275 | 42 | 32 | `enemy_grp_penance` · `enemy_regen` | crystal 0.15/1-1 · potion_l 0.20/1-1 |

**腐朽王座 `abyss`（浅 134.25 / 32.50 / 0.125；深 162.50 / 39.25 / 0.195）**

| id | 名字 | 档 | lv | HP | 攻 | 防 | exp | gold | 技能 | 掉落 |
|---|---|---|---|---|---|---|---|---|---|---|
| `enemy_abyss_hound` | 深渊猎犬 | 浅 | 8 | 140 / 490 | **42** / 94.5 | **0.18** / 0.45 | 76 | 62 | `enemy_abyss_lash` · `enemy_grp_tithe` | crystal 0.15/1-1 |
| `enemy_abyss_thrall` | 渊缚从者 | 浅 | 8 | 138 / 483 | 30 / 67.5 | 0.11 / 0.275 | 75 | 61 | `enemy_heavy_blow` · `enemy_grp_tithe` | crystal 0.12/1-1 · potion_l 0.20/1-1 |
| `enemy_tide_scavenger` | 潮底拾骸 | 浅 | 9 | 132 / 462 | 29 / 65.3 | 0.11 / 0.275 | 72 | 59 | `enemy_dark_flame` · `enemy_grp_tithe` | crystal 0.12/1-1 · potion_l 0.20/1-1 |
| `enemy_deep_lurker` | 深喉潜猎 | 浅 | 9 | 127 / 445 | 29 / 65.3 | 0.10 / 0.25 | 69 | 56 | `enemy_abyss_lash` · `enemy_grp_tithe` | crystal 0.10/1-1 |
| `enemy_dark_knight` | 黯甲骑士 | 深 | 10 | 160 / 560 | 36 / 81 | **0.24** / 0.60 | 87 | 71 | `enemy_heavy_blow` · `enemy_grp_tithe` | potion_l 0.40/1-2 · crystal 0.35/1-2 |
| `enemy_tithe_knight` | 献祭骑士 | 深 | 11 | 165 / 578 | 39 / 87.8 | **0.22** / 0.55 | 90 | 73 | `enemy_heavy_blow` · `enemy_grp_tithe` | crystal 0.30/1-2 · potion_l 0.30/1-1 |
| `enemy_womb_of_tides` | 潮腔孕体 | 深 | 11 | 155 / 543 | 38 / 85.5 | 0.12 / 0.30 | 84 | 69 | `enemy_dark_flame` · `enemy_grp_tithe` | crystal 0.30/1-2 · tome 0.10/1-1 |
| `enemy_void_priest` | 虚空祭司 | 深 | 12 | 170 / 595 | 44 / 99 | **0.20** / 0.50 | 93 | 75 | `enemy_dark_flame` · `enemy_grp_tithe` | tome 0.40/1-1 · crystal 0.50/1-2 |

**4 只精英（每回合 2 次 + 每 3 轮 1 次大招；3 技能；不进 `enemyPool`）**

| # | id | 名字 | 图 | lv | HP | 攻 | 防 | exp | gold | 必掉 | 技能（第 3 条 = 大招） |
|---|---|---|---|---|---|---|---|---|---|---|---|
| E1 | `enemy_elite_nest_warden` | 巢守 | 无根 | 4 | 55 / 193 | 14 / 31.5 | 0.05 / 0.125 | 28 | 20 | `item_ore` ×8 | `enemy_bite` · `enemy_grp_scab` · **`enemy_carrion_gore`** |
| E2 | `enemy_elite_spore_matriarch` | 孢母 | 苔痕 | 6 | 92 / 322 | 25 / 56.3 | 0.10 / 0.25 | 50 | 39 | `item_ore` ×8 | `enemy_grp_spore` · `enemy_spore_lance` · **`enemy_heavy_blow`** |
| E3 | `enemy_maze_guardian` | 迷宫的守护者 | 静谧 | 10 | 160 / 560 | 36 / 81 | 0.12 / 0.30 | 90 | 85 ⚠️ | `item_crystal` ×2 + `item_ore` ×4 | `enemy_grp_penance` · `enemy_sweep` · **`enemy_heavy_blow`** |
| E4 | `enemy_bone_tyrant` | 骸骨暴君 | 腐朽 | 12 | **220** / 770 | **53** / 119.3 | 0.20 / 0.50 | 130 | 120 ⚠️ | `item_core` ×1 + `item_crystal` ×2 | `enemy_grp_tithe` · `enemy_abyss_lash` · **`enemy_heavy_blow`** |

- E3 / E4 沿用 `content-draft` 的重定标值；用扩充后的池均值重算（`222.6 / 53.8`）与定稿 `220 / 53` 只差 1% ⇒ 扩编没有破坏定标。
- ⚠️ E3/E4 的 `gold` 与"池均值 ×2"口径不符（定稿 85 vs 69、120 vs 132）；当前**保留定稿值**（见 §20.11 待澄清）。
- E3 的自愈技已被换成 `enemy_sweep` ⇒ 精英不会因亡骸重整自残。

**4 只「深渊之王的虚影」（D173 / D176 / D190 / D290 / D415 / D438）**

四图**同名同 id**（`enemy_abyss_king_shadow`），`family: echo`，**不挂族群战术**；出现在每张图的最后一层（有概率替换原精英）；奖励 = 该图精英 ×2；技能四图共用一套。

| 图 | 衰减 | HP | 攻 | 防 | lv | exp | gold | 掉落 |
|---|---|---|---|---|---|---|---|---|
| 无根回廊 | **×0.5** | 130 / 455 | 31 / 69.8 | 0.08 / 0.20 | 5 | 56 | 40 | ore ×5 + coin_pouch ×2 |
| 苔痕回廊 | **×0.7** | 182 / 637 | 43 / 96.8 | 0.11 / 0.275 | 7 | 100 | 78 | ore ×6 + potion_l ×2 |
| 静谧墓所 | **×0.85** | 221 / 773.5 | 53 / 119.3 | 0.14 / 0.35 | 10 | 180 | 170 | crystal ×3 + tome ×1 |
| 腐朽王座 | **×1.0** | 260 / 910 | 62 / 139.5 | 0.16 / 0.40 | 15 | 260 | 240 | crystal ×4 + feather ×1 |

技能（四图统一）：`enemy_abyss_lash` · `enemy_dark_flame` · **`enemy_heavy_blow`（大招）**。基准 ×1.0 = 深渊之王重定标值（Lv15 · 260 HP / 62 攻 / 0.16 防）。
⚠️ 静谧（221/53）与腐朽（260/62）**故意超出 HP 220 / 攻击 53 的普通敌上限**，按 **D173「稀有奖励怪」例外**处理（D409/D414 只把防御上限拆出，两行防御均在 0.30 内）。

**9 条新增敌方技能**（敌方技能一律 `type: active`、`cost: 0`、**不填 `enhanced`**；`apCost` 按 D15 照填但**敌方不读取**）

| # | id | 名字 | power | hits | target | effects | 等效 power | apCost | 归属 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | `enemy_grp_scab` | 舐创 | 95 | 1 | `enemy` | damage 95 | 95 | 2 | **无根全部 8 只**（族群授予技） |
| 2 | `enemy_grp_spore` | 吐孢 | 85 | 1 | `enemy` | damage 85 | 85 | 2 | **苔痕全部 8 只** |
| 3 | `enemy_grp_penance` | 缄口 | 90 | 1 | `enemy` | damage 90 | 90 | 2 | **静谧全部 8 只** |
| 4 | `enemy_grp_tithe` | 汲食 | 110 | 1 | `enemy` | damage 110 | 110 | 2 | **腐朽全部 8 只** |
| 5 | `enemy_swarm_rend` | 群噬 | 45 | **2** | `enemy` | damage 45 | 90 | 2 | 无根 3 只 · 苔痕 2 只 |
| 6 | `enemy_carrion_gore` | 腐肉穿刺 | 130 | 1 | `enemy` | damage 130 | 130 | 3 | 无根 2 只 · 苔痕 1 只 |
| 7 | `enemy_spore_lance` | 孢刺 | 115 | 1 | `enemy` | damage 115 | 115 | 2 | 苔痕 4 只 |
| 8 | `enemy_marrow_knit` | 续髓 | 0 | 1 | `self` | **`heal_max_ratio` 12** | 0 | 1 | 静谧：枯骨诵者 |
| 9 | `enemy_abyss_lash` | 渊鞭 | 60 | **2** | `enemy` | damage 60 | 120 | 2 | 腐朽 2 只（含虚影） |

- 只有 1 条需要新效果类型：**`heal_max_ratio`**（**D157** 已裁新增，按自身最大生命 12% 自愈 ⇒ 不再随攻击力漂移，与现役 `enemy_regen` 量级一致）。
- `enemy_grp_*` 的 id 与 power 与敌方战术草案**完全同源**（不产生第二套定义）。

**`enemyPool` 结构（D89 的"同一次实现"）**

```jsonc
"enemyPool": { "shallow": [{ "enemyId": "...", "weight": 30 }, ...],   // 4 条，第 1–3 层
               "deep":    [{ "enemyId": "...", "weight": 30 }, ...] }  // 4 条，第 4–6 层
```
权重建议：**图标怪（最强）30 · 基础怪 30 · 基础怪 25 · 极端值怪 15**。
**必须移出 `enemyPool` 的 7 条（D258 / D6）**：`enemy_maze_guardian`、`enemy_bone_tyrant`（→ 15% 精英位）、`enemy_abyss_king`（→ 虚影）+ **4 个图 Boss 一律不进池**（第 6 层固定出现）。移除后权重需重归一化。
⚠️ **上线顺序**：这三条移除与 **D299**（王座上的等待者 `item_core` 2 → 4）**强烈建议同批次**，否则会出现一段"魔核 = 0"的窗口（**D411** 让骸骨暴君必掉 `item_core` ×1，把该风险从"硬要求"降为"强烈建议"）。

---

### 20.11 落地状态与已知未决

**已落 / 已修（写代码前可据此判断现状）**

| 项 | 状态 |
|---|---|
| `src/data/tables/tactics.json` | **已落 15 条**：5 属性（`sides: [ally, enemy]`，含 enemy 落点与 perStackPct）+ 6 机制型 + 4 族群 |
| D242 出手闸门 | 已实现（`usedThisRound` 读侧闸门），是 D304 三档硬门达成的前提 |
| D272 落点 / D259 辅助技掷强化 | 已在 `DamageCalculator` 修复（`applySupportEffects` 收 `targets`；辅助路径也掷强化） |
| 敌方概率常量 | `balance` 已参数化：`enemyComboRate 0.08` / `enemyCritRate 0.1` / **`enemyEnhanceRate 0`** |
| `enemies.json` | **44 条**（32 普通 + 4 精英 + 4 虚影 + 4 图 Boss），`family` 分布 swarm/fungus/undead/abyss 各 10、echo 4 |

**尚未落（草案已定稿，配表/代码待补）**

- 4 条族群授予技 `enemy_grp_*` **尚未进入** `skills.json`，也未挂到 `enemies.json` 的 `skillIds` 上 ⇒ **敌方族群战术当前不生效**。
- `state.enemyTactics` / `enemyTacticGainThisRound` 两个运行时字段与 `onEnemyPhaseStart()` 递减挂点尚未实现 ⇒ **敌方战术整体尚未生效**，连带 D344 的 3 条清除技能是"写了不生效的承诺"。
- `clear_tactic` 结算钩子与 `balance.tacticClearChance` 尚未实现（**D440** 要求它独立于 `effects`，并入 D272 批次）。

以下 7 条是草案间 / 草案与实现间的真冲突，**GDD 中未见对应裁定**，逐条标 `⚠️ 待澄清`：

1. ⚠️ **待澄清** —— **敌方族群战术的 HUD 显示位**未定（D328 只规定"战术 HUD 最多 5 条、只放属性战术"，敌方那"另一处"指向未定）。草案建议与 **D323** 的"敌方 debuff 图标行"共用。
2. ⚠️ **待澄清** —— **敌方 AI「战术优先」的 `P` 与排序口径**：草案取 `P = 0.5`、排序"层数最低优先，同层按 power 降序"。若沿用 v2/v3 的"power 最高"，占位式下每只普通敌只亮 1 条战术，整场增益从 +10.5% 降到约 +8%。
3. ⚠️ **待澄清** —— **`tac_tempo` 计数器的写法**：草案 v4 自查后改为"满 3 层才启动节拍器"（v2/v3 的 `+= 层数` 会让普通敌 1 层就在 3 回合战里换到 1 次额外出手）。
4. ⚠️ **待澄清** —— **困难难度 ×1.25 是否作用于防御**（敌人稿 R1）：石魔像在困难 · 第 6 层 `0.30 × 2.5 × 1.25 = 0.9375` ⇒ 有效血量 ≈ 该图 Boss，是该图精英的 4.9 倍。草案建议"难度倍率只作用于 HP 与攻击，不作用于防御"。
5. ⚠️ **待澄清** —— **E3/E4 的 `gold` 与"池均值 ×2"口径不符**（守护者 85 vs 69；暴君 120 vs 132）。
6. ⚠️ **待澄清** —— **虚影奖励是否随 D190 一同衰减**（D190 只写了"数值衰减"；当前按"只衰减数值、不衰减奖励"出表）。
7. ⚠️ **待澄清** —— **`enemy_curse` / `enemy_dread_roar` 两条死技能的处置**（`power = 0` ⇒ AI 永不释放，且降的轴在敌方侧无读取点）：删 / 标注永不释放 / 改 `power > 0`（后者需并入重算）。

**⛔ 本章明确排除的已否决方案（读草案时遇到请跳过）**

1. **`combat-length-recalibration` 的★推荐 5 条改动**（我方伤害 ÷N + D1 共享池 + `defLayerScale 0.30→0.20` + 敌方 HP ×4 + GATES 落脚本）—— **第 79 轮 D108 结案否决**：三档硬门靠 D242 零数值改动即达成（详见 §20.9）。
2. **D425「族群战术只挂精英/虚影/Boss、普通敌不挂」** —— **已被 D437 推翻**（现为占位式）。理由：精英/虚影/Boss 的技能位已排满，"挂上去"就是加第 4/5 条 ⇒ 照样破 D153；且会让 85% 的普通战敌方 HUD 空槽。
3. **敌方族群战术的 v1 初版落点**（残躯继承比例 `+3pp`/层、孢子豁免阈值 `+2 段`/层 等） —— **已被 D366 否决**（它们是规则参数 / 需新读取点），v2 重做为"护盾 / 每次出手泄漏 / 反噬 / 吞噬提前"。
4. **`clear_tactic` 做成 `SkillEffect` 变体塞进 `effects`** —— **已被 D440 否决**（会让通道 ① 静默失效）。
5. **战术独立 CD（D205）** —— **已被 D219 废除**（改为"持续时间刷新"）；**"节流由 AP 成本 + 升耗承担"** 已被 **D278** 改写为"**只由 AP 成本承担**"。
6. **魔力战术 `tac_arc` 秘法增幅** —— **D226 已删除**（魔力战斗内完全无效），两条 `magic` 轴改轴灵性并入相位增幅。
7. **"预留 5 层"** —— **D260**：改为 `min(3, 承载技能数)`，5 层须先补承载技能才可启用，不得再写"预留"。
8. **"我方单回合总输出 ∝ N² 是战斗过短的结构性主因"** —— D108 实测更正：∝N² 真实但次要（N=4 约 1.3 倍），主因是"每轮我方 4 行动 vs 敌方 1"，决定性变量是 D242。
9. **饰品伤害轴上界 `+24.13%`** —— **D355 已推翻**（它建立在"饰品作用于全队"的假设上）；实际预期值约 **+9.7%**（前者作保守上界留档，**两者不可混用**）。

---

## 30. 经济与数值

> 本章来源：`design/drafts/techpoint-economy-v1.md`、`gold-economy-v2.md`、`growth-recalibration-v1.md`、`consumables-draft-v1.md`、`economy-report-v1.md`、`combat-length-recalibration-v1.md`、`scripts/_mc-*-out.md`（原文已归档）

> **标注约定**：本章把数字分成两类 —— `实测` = 蒙特卡洛脚本真调 `MazeGenerator` / `CombatSystem` / `CharacterFactory` 跑出来的值；`设计` = 裁决给的目标值或尚未落地的推定。两者不一致时**两个都写**。
> 复现入口（Node 24）：`scripts/mc-economy.mjs`、`mc-gold-economy.mjs`、`mc-techpoint.mjs`、`mc-techpoint-d350.mjs`、`mc-growth-recalibration.mjs`、`mc-combat-length.mjs`、`mc-economy-consumables.mjs`。

### 30.1 口径与模型地基

这一节是全文的地基。**所有经济数字都建立在下面这 7 条代码事实上**，其中任何一条变了，后面所有表都要重跑。

| 项 | 生效口径 | 出处 / 相关裁决 |
|---|---|---|
| **楼层密度加成** | 第 N 层有效密度 = 配表密度 × `(1 + 0.12×(N−1))`，第 6 层 ×1.60 | `DungeonScene.descend()`。**GDD §4.5 的旧成长 MC 漏了这一项**（D203 采信时已认定），漏了会让战斗数低估 30–43% |
| **敌人密度** | **0.035 / 0.045 / 0.05 / 0.055（配表 = 现值）** | **D257 已撤销 D35 的密度动作**（0.060/0.064），理由是它会让无根 32→54 场、超过腐朽王座 ⇒ 不得再引用"决后密度"口径 |
| 每场战斗 | **恒定 1 个敌人**；`exp` / `gold` / `techPoints` / `drops` 都不随 `count` 放大 | `DungeonScene.startBattle` 的 `slice(0,1)` |
| 掉落判定 | 每条 `drops` **独立**判定一次；`rng.int(min,max)` 是闭区间 ⇒ 期望 = `chance × (min+max)/2` | `CombatSystem.calculateReward` |
| 宝箱内容 | 45% 金币 / 45% 道具（**5 件池等概率**：小药水·大药水·魔铁矿·深渊结晶·钱袋）/ 10% 科技点；道具分支每箱 `int(1,2)` 件 | `MazeGenerator.rollTreasurePayload`。**宝箱池里没有 `item_core`** ⇒ 魔核 100% 来自敌人/Boss |
| 宝箱金币基数 | `goldBase = 20 + floor × 25` ⇒ 第 1–6 层 = **45 / 70 / 95 / 120 / 145 / 170**；**只跟层数走，与地牢无关** | 修正 D60 的前提（"金币恒为 45"只对第 1 层成立） |
| 敌池抽取 | 按 `enemyPool` 权重，**与层数无关**（D148 的浅/深分组尚未落地） | `MazeGenerator.rollEnemy` |

**每箱期望金币（实测）**

| 层 | 1 | 2 | 3 | 4 | 5 | 6 | 全趟均值 |
|---|---|---|---|---|---|---|---|
| 金币分支 | 32.4 | 50.4 | 68.4 | 86.4 | 104.4 | 122.4 | 77.4 |
| 钱袋折算 | 16.2 | 16.2 | 16.2 | 16.2 | 16.2 | 16.2 | 16.2 |
| **每箱合计** | **48.6** | **66.6** | **84.6** | **102.6** | **120.6** | **138.6** | **93.6** |

**每场战斗的加权产出（实测，按现值敌池；D258 之后静谧/腐朽已下修）**

| 图 | 权重分布 | 金币/场 | 经验/场 | 科技点/场 |
|---|---|---|---|---|
| 无根回廊 | 黏液 40 / 蝙蝠 35 / 巨鼠 25 | **9.80** | 13.80 | 0 |
| 苔痕回廊 | 蝙蝠 15 / 巨鼠 20 / 哥布林 30 / 骷髅 25 / 食尸鬼 10 | **19.40** | 24.70 | 0.65 |
| 静谧墓所 | 骷髅 22 / 食尸鬼 22 / 怨灵 22 / 石魔像 18 / 守护者 5 | **38.38** | 54.70 | 1.371 |
| 腐朽王座 | 怨灵 18 / 石魔像 18 / 黯甲 24 / 猎犬 22 / 祭司 16 / 骸骨暴君 6 / 深渊之王 2 | **95.46** | 104.42 | 2.264 |

> 🔴 **两个"看起来对但已经失效"的旧数**：
> 1. `economy-report-v1.md` 里的 **18 439 / 2 234 / 3 388 / 4 810 / 8 007** 是 **D33/D35 决后密度**（206.9 场）下的值，**D257 之后作废**。正确现值见 §30.2。
> 2. 同报告 §4.3 第 4 条「`item_ore` 的 **42%** 来自宝箱」**是错的**，正确是 **14.3%**（现值密度下 **11.4%**）—— 宝箱那份只有 `108 箱 × 0.135 = 14.58` 个。**敌人掉落才是材料主力（魔铁矿 83% / 深渊结晶 85%）**。

### 30.2 金币产出：一趟 6 层

**D350 前后对照（实测，`_mc-gold-out.md` S2；现值密度 + 含楼层加成）**

| 图 | 箱/层 前→后 | 战斗数 | 战斗金 | 宝箱金 前→后 | 钱袋 前→后 | 事件金 | **合计 前→后** | 变化 |
|---|---|---|---|---|---|---|---|---|
| 无根回廊 | 3 → **2** | 32.1 | 314 | 1 393 → **929** | 292 → **195** | 15 | **2 014 → 1 452** | **−27.9%** |
| 苔痕回廊 | 4 → **3** | 40.8 | 792 | 1 858 → **1 393** | 389 → **292** | 24 | **3 063 → 2 501** | −18.3% |
| 静谧墓所 | 5 → **3** | 45.0 | 1 942 | 2 322 → **1 393** | 486 → **292** | 60 | **4 810 → 3 687** | −23.4% |
| 腐朽王座 | 6 → **4** | 49.8 | 4 485 | 2 786 → **1 858** | 583 → **389** | 153 | **8 007 → 6 884** | −14.0% |
| **四图合计** | 108 → **72** | 167.7 | 7 533 | 8 359 → **5 573** | 1 750 → **1 166** | 252 | **17 894 → 14 524** | **−18.8%** |

**分来源拆解（实测）**

| 来源 | D350 前 | D350 后 | 变化 | 占旧收入 |
|---|---|---|---|---|
| ① 战斗掉落 | 7 533 | 7 533 | **不变** | 42.1% |
| ② 宝箱金币分支 | 8 359 | 5 573 | **−2 786** | 46.7% |
| ③ 宝箱钱袋折算 | 1 750 | 1 166 | **−583** | 9.8% |
| ④ 事件（埋伏） | 252 | 252 | **不变** | 1.4% |
| **合计** | **17 894** | **14 524** | **−3 370** | 100% |

> **宝箱 + 钱袋：10 109（占旧收入 56.5%）→ 6 739（占新收入 46.4%）**。宝箱+钱袋合计约占收入的一半，而它**与敌人强度完全无关，只跟 `treasureCount` 与层数走** ⇒ 在最弱的无根回廊刷宝箱的金币效率并不低（无根 : 腐朽 = 1 : 4.7，强度差 10 倍以上）。

**D350 的三条连带结论（实测）**

1. **宝箱数减少没有挤掉任何其它元素。** 真调生成器验证：陷阱 / 事件 / 存档 / 敌人数 / 地板格**全部不变**（`place()` 的顺序是 宝箱→存档→事件→陷阱→敌人，而 `enemyCount` 只看 `floorTiles.length`）。
2. **这一刀砍前期比砍后期重。** 宝箱在该图收入里的占比：无根 **83.6%** / 苔痕 73.4% / 静谧 58.4% / 腐朽 42.1% ⇒ 降幅 −27.9% / −18.3% / −23.4% / −14.0%。**GDD D375 裁定：接受，不补偿**（砍的是密度不是单价，新手期本来也没多少东西要买）。
3. **第 1 层是全流程最穷的一格**（实测，D350 后）：无根 1 层 **107 金**（−31.4%）/ 苔痕 165 / 静谧 189 / 腐朽 284。带 4 人队打一层赚 107 金，连一瓶小药水（30）都要算着买。

**逐层金币（D350 后，实测，节选）**

| 图 | 层 | 战斗数 | 战斗金 | 宝箱金 | 钱袋 | 本层合计 |
|---|---|---|---|---|---|---|
| 无根回廊 | 1 | 1.00 | 10 | 65 | 32 | **107** |
| 无根回廊 | 6 | 12.13 | 119 | 245 | 32 | 396 |
| 静谧墓所 | 6 | 17.40 | 750 | 367 | 49 | 1 166 |
| 腐朽王座 | 6 | 19.13 | 1 723 | 490 | 65 | **2 277** |

**事件的金币净额**：埋伏（毛收入 252/趟四图）− 神龛/商人期望支出 ⇒ **`economy-report-v1.md` 口径净额 −1 116 金**（占 18 439 的 6.1%）；**事件草案 v3 口径净额 +392 金/趟四图**。两个口径不一致，本章 §30.3/§30.4 采用后者（v3 是较新口径）。
> ⚠️ **D61（埋伏按权重选敌）未落地**：现在取 `enemyPool[0]`（各图最弱的一只），使埋伏收益只有普通遇敌的 36%–52%。D61 落地后埋伏收入从 252 涨到 ≈618 金。

### 30.3 金币出口与七项价格

**D372 裁决：D350 之后七项价格 0 项需要改数值**（分母换成新收入 14 524 后逐项判定，实测）。

| # | 项 | 现值 | 依据 | 新收入下的判定 | 结论 |
|---|---|---|---|---|---|
| 1 | 饰品 `price` | ★3 **600** / ★4 **1 500** / ★5 **4 000** | D171 | ★3 = 一趟无根(1 452) 的 **41%**；★4 = 一趟静谧(3 687) 的 **41%**；★5 = 一趟腐朽(6 884) 的 **58%** | 🟢 **不动**（仍全在"一趟以内"，D171 的"★5 ≈ 半趟"立意未破） |
| 2 | 锻造费系数 | `price × **0.6**` | D167（**0.9 → 0.6** 的经济修正） | 12 件金币费 **16 680** + 材料机会成本 **7 840** = **24 520** = 新四图 **1.7 趟** | 🟢 **不动**（是 `price` 的内部比例，与收入无关） |
| 3 | 招募价 | **5 000**（平价，无递增）⇒ ⚠️ **现值已被裁决为 500**，见 §30.14 | D310 / D67（旧）→ **现值 500** | 按 5 000：一趟四图 **2.9 次**、招满 20 名 = 100 000 = **6.9 趟**；按现值 500：一趟四图 **29 次**、招满 20 名 = 10 000 = **0.7 趟** | 🟢 判定（"0 项需要改"）在 5 000 口径下成立；**换 500 后招募不再是"唯一能吃掉整趟收入的大出口"** |
| 4 | 训练场 | **1 金 = 1 经验** | D120（原 0.5）→ 经济修正 1.0 → **D404 确认不限次** | 一趟四图金币全投入 = 该趟主路径战斗经验池的 **39.8%** | 🟢 **不动** |
| 5 | 商店 | 原价买 / 半价收（D90 的 2 倍差价） | D90 | 大药水 80 = 一趟四图的 **0.55%** | 🟢 **不动** |
| 6 | 归途石 | **55**，限购 **1 / 趟** | 消耗品草案 v3 | 四图共 220 = 一趟四图的 **1.5%** | 🟢 **不动** |
| 7 | 事件净额 | **+392 / 趟四图** | 事件草案 v3 | 占新收入 **2.7%** | 🟢 **不动** |

**为什么锻造费是 0.6 而不是 D167 原文的 0.9（实测不变式）**

| 档 | 锻造费（×0.6） | 材料机会成本 | 自造总成本 | 直购价 | 自造 vs 直购 |
|---|---|---|---|---|---|
| ★3 | 360 | 150 | **510** | 600 | **便宜 15%** ✅ |
| ★4 | 900 | 460 | 1 360 | 1 500 | 便宜 9% ✅ |
| ★5 | 2 400 | 1 110 | **3 510** | 4 000 | **便宜 12.3%** ✅ |

按 0.9 落地则三档全部**贵 15%–21%**，与 D167「让直购永远略贵于自造、反哺锻造」的立意相反（会架空锻造系统，即 D133 预警的结局）。**看到 0.6 是正确值，不要改回 0.9**（`accessories.json` 12 条 `forgeCostRate` 全为 0.6）。

**唯一的硬套利：钱袋（D371）**

```
items.json: item_coin_pouch  price = 100, kind = 'gold', value = 120, 营地/迷宫均可用
D90 买价 = price × 1.0 = 100  ⇒  买 100 → 用掉 → +120  ⇒  净赚 20/次，无冷却、无上限、stackLimit 99
```
持有 14 524 金 → 买 145 个 → 17 400 金 → 再买 174 个 → …**无上界**。
**D371 裁定：`value` 120 → 60**（保留 40% 折损）。改 `value` 而不是 `price`，是因为 `price` 还与"卖出 50 金 / 事件商人卖材料"两条挂钩。
> ⚠️ **配表仍未落地**：`items.json` 现值 `value = 120`。

### 30.4 一次完整循环的收支

**收入（稳态 / 每循环，实测 + 引用）**

| 收入项 | D350 前 / 循环 | D350 后 / 循环 |
|---|---|---|
| 四图各一趟（含楼层加成，现值密度） | 17 894 | **14 524** |
| 支线任务（quests-draft §4.3 稳态，⚠️ 见下方口径警告） | 11 410 | 11 410 |
| 事件净额 | +392 | +392 |
| **稳态合计** | 29 696 | **26 326** |
| 主线任务（**一次性，仅第 1 循环**） | +10 100 | +10 100 |
| **第 1 循环合计** | 39 796 | **36 426** |

**支出与循环次数（实测）**

| 支出项 | 金额 | = 几个循环（新口径） | 旧口径 |
|---|---|---|---|
| 招满 20 名（20 × 5 000） | 100 000 | **3.8** | 3.4 |
| 12 件饰品锻造（含材料机会成本） | 24 520 | **0.93** | 0.83 |
| 6 名主力 L4→L20 训练场（`expExponent` 1.9） | 226 302 | **8.6** | 7.6 |
| **【现实目标】三项合计** | **350 822** | **13.3** | 11.8 |
| 20 人**全部** L4→L20（1.9） | 754 340 | **28.7** | 25.4 |
| **【极端目标】招满 + 饰品 + 全员满级** | **878 860** | **33.4** | **29.6** |

**训练场单项（实测，它是唯一的大头）**

| 场景 | 金额 | = 几个循环 |
|---|---|---|
| 6 名主力 L4 → L20（`expExponent` **1.9**） | 226 302 | 8.60 |
| 6 名主力 L4 → L20（`expExponent` **1.6**） | 103 074 | 3.92 |
| 20 名全员 L4 → L20（1.9） | 754 340 | **28.66** |
| 20 名全员 L4 → L20（1.6） | 343 580 | 13.05 |
| 6 名主力从 L14.7 → L20（1.9） | 131 388 | 4.99 |

**三条读法**

1. **D350 把"毕业"成本抬高 12.8%**（29.6 → 33.4 循环），不是 19% —— 循环收入里有 11 410 的支线任务金币**不受 D350 影响**（任务奖励不来自宝箱）。
2. **训练场是唯一的大头**（28.66 / 33.4 = 86%）。招满 20 名只要 3.8 循环、12 件饰品只要 0.93 循环 —— 内容向目标很便宜，**"把 20 个人都练满"才是纯数值向的无限刷**。若抱怨"毕业太慢"，该动的是训练场或等级上限，不是 D350。
3. **折成时间**（按 2.33 小时/循环，⚠️ quest-writer 的数，未独立复验）：现实目标 13.3 循环 ≈ **31.1 小时**；极端目标 33.4 循环 ≈ **77.8 小时**。

> **D377 裁决**：接受 **13.1 循环**（招满 3.7 + 饰品 0.91 + 6 名主力满级）；不追求极端 32.7 循环（其 86% 花在训练场）。⚠️ GDD 该行记的 13.1 / 32.7 是**旧口径**，第 92 轮更正后为 **13.3 / 33.4**。

> ⚠️ **支线金币口径警告（本章最需要留意的一处）**：§30.4 两张表用的 **11 410 金/循环**来自 `quests-draft-v1.md` §4.3，**未被 economy 侧独立复验其频次假设**；GDD 第 81 轮已把它下调为 **8 270 金/趟**（= 战斗金币的 72.5%）。**若按 8 270 重算，稳态收入与全部循环次数都要变** —— 本章保留 11 410 是因为所有已交付的循环表都建在它上面，改用 8 270 需要重跑一次 `mc-gold-economy`。

### 30.5 科技点经济

**四刀之后科技点的来源只剩三处（D204 / D245 / D254 / D255）**

| 来源 | 值 | 裁决 |
|---|---|---|
| 普通怪 `techPoints` | **0** | D204 |
| 精英 | **0** | D254 |
| Boss 重复击杀 | **0** | D255 |
| 宝箱 | **0** | D245 |
| **首通 `firstClearTechPoints`** | **38**（8 / 9 / 10 / 11） | D246 |
| **主线任务 4 条** | **14**（2 / 3 / 4 / 5） | D246 |
| **支线任务** | 见下表 | D92 / D305（任务奖励轨不受 D254/D255 影响） |

**首清四图的科技点供给（实测，逐条）**

| 来源 | 严格 | 中位 | 乐观 | 依据 |
|---|---|---|---|---|
| 首通 `firstClearTechPoints` | 38.0 | 38.0 | 38.0 | D246 |
| 主线任务 4 条 | 14.0 | 14.0 | 14.0 | D246 |
| `S1 三层止步` | 4 | 4 | 4 | 4 图 × 1 点（必达） |
| `S2 六层之底` | 8 | 8 | 8 | 4 图 × 2 点（必达） |
| `S3 净空回廊` | **0** | **0** | **0** | 恒不达成 → **D330 已删除**（见下） |
| `S4 三人成行` | 0 | 1 | 3 | 招募上限 = 一趟金币 14 524 ÷ 5 000 = 2.9 次…（满金币口径 12.3 次）⇒ D300 ③ 已收紧 |
| `S5 自铸三器` | 0 | 1 | 2 | 一趟可造 ≥3 件 ★3 |
| `S6 炉火不熄` | 0 | 3 | 3 | 一趟可造 1 件 ★5（魔核瓶颈） |
| `S7 猎杀精英` | 4 | 4 | 4 | 25.6 只精英/趟 ⇒ 2 次 × 2 点 |
| `S8 重返王座` | 0 | 0 | 0 | 首杀归主线 M1–M4 |
| `S9 满载而归` | 2.2 | 2.2 | 2.2 | 阈值 1 700 时四图合计达成 **1.09 次**（实测） |
| `S10 全身而退` | 6 | 6 | 6 | 四图连续通关 ⇒ 第 2/3/4 图后各 1 次 |
| **支线小计** | **24.3** | **29.3** | **38.2** | |
| **总供给** | **76.3** | **81.3** | **90.2** | D350+D367 后实测 **76.2 / 81.2 / 84.2** |

> **S3 的处置（草案与 GDD 不一致）**：草案实测证明它**恒不达成**（第 1 层 `branchCount = 0`，21 个地板格就是起点→出口的必经路径，陷阱/事件/宝箱**踩上即触发**），并建议改成"比例型"（未触发陷阱 ≥ 已触发陷阱）⇒ **D300 ① 采纳了"改比例型"，但 D330 最终裁定删除 S3** ⇒ **支线 10 条 → 9 条**。本章按"已删"记。
> 派生规则：**任何"未触发 X"型条件在本作里都不可能成立**（X ∈ 陷阱 / 事件 / 宝箱），因为第 1 层必然踩到。

**树价与达成率（实测，`_mc-techpoint-d350.md` S3）**

| 树价 | 严格 | 中位 | 乐观 | 说明 |
|---|---|---|---|---|
| **99（D297 裁定，现值）** | **77.0%** | **82.0%** | 85.0% | 79（D41）+ 5 个节点 × 4 |
| 95（D264 原值） | 80.2% | 85.5% | 88.6% | 让"80%"落在严格口径的**下界** |
| 89 | 85.6% | 91.2% | 94.6% | — |

**D297：科技树定为 99 点**（细化 D264 的"~95"）= 79 + 5 节点 × 4，新增「疾行训练 II / 幸运徽记 II / 秘法研习 II / 灵性启迪 II / 混战节拍」，**全部只用现有 `effect.type`，零代码**。`tech.json` 现值 **21 节点 / 99 点** ✅ 已落地。

> 🔴 **科技点天生是"一次性消费品"，不是终局货币（实测）**
> 稳态支线 = **严格 32.2 / 中位 40.2 点/趟** ⇒ 首清 76.2（严格）+ 一趟刷 40.2 = 116.4 点 > 99 ⇒ **距点满还差 0.57 趟 / 0.44 趟 ⇒ 第 2 趟就毕业**。
> 根因不是数值而是性质：树有限且不可洗点（D403），撑不住 D237 的"终局靠刷科技点"。
> ⇒ **D308 裁定：不改，接受**。终局循环锚在**材料与饰品**（12 件饰品 3.75 趟）与精英材料上。GDD 已把科技树明确定位为「首清 + 约 1 趟」的短周期目标。

**`S9 满载而归` 的阈值演化（这是全章最典型的一处"数值失效"）**

| 阶段 | 阈值 | 实测达成次数/趟 | 结论 |
|---|---|---|---|
| 原值 | **1 200** | **3.58 次** | 🔴 形同虚设（四图宝箱期望 1 393/1 858/2 322/2 786 全部 > 1 200）⇒ 每趟自动多领 7.2 点 + 4 800 金 |
| D300 ② | **2 500** | **1.15 次** | ✅ 变成"腐朽王座专属"（69.1%）+ 静谧偶尔（36.7%） |
| **D367（现值）** | **1 700** | **1.09 次**（实测） | ✅ 落在 D367 给的验收带 **0.8–1.4 次/趟** 内 |

**D401：D350 与 D367 是"互相补偿的一对"，必须成对处理**

| 单变量口径 | 次数/趟 前 → 后 | `S9` 科技点净变化 |
|---|---|---|
| ① 只动 **D350**（宝箱数），阈值都锁 1 700 | 2.71 → 1.09 | **−3.23 点** |
| ② 只动 **D367**（2 500 → 1 700），宝箱数取旧值 | 1.15 → 2.71 | **+3.12 点** |
| **合计（v2 基线 → 现在）** | 1.15 → 1.09 | **−0.11 点** |

> ⚠️ **比法警告**：在"同阈值 1 700"下比对会得到"−3.23 点"的大幅下降 —— 那是**忽略 D367 的错误比法**。正确基线是 v2 的 @2 500 = 1.15 次。

**D374 / D402：D350 之后"首清 ≈80%"这条卡线不破**

- 关键澄清：**宝箱科技点从来就不在这条线上**（D245 已裁"宝箱不产科技点"）⇒ `gold-economy-v2.md` 报的 **−5.40 科技点/趟是"这条线之外"的量**，不要在 80% 卡线的语境里引用。
- 实测：树 99 的达成率 **77.1% / 82.1% → 77.0% / 82.0%**（差 0.1pp，四舍五入级别）⇒ **树价 99 与 §30.5 全部任务裁定维持不变**。
- 结论：**当初"要不要复核"的担心是假警报**（作者自己留痕）。

**招募的科技点成本：归 0（D300 ④，✅ 已落地）**
原 `recruitTechPointCost = 2`（D67）会让"首清点满 80%"变成一句无法验收的话（每招募 1 人吃掉 2 点）。裁定归 0，理由是科技点已是**里程碑货币**、招募是**日常行为**，不该共用同一个池子。配表现值 `recruitTechPointCost = 0` ✅。

**顺带发现：秘法研习是树里性价比最高的成长节点，但它的名字骗人**

- `DamageCalculator` 的伤害只用 `team.attack × power`，**魔力在战斗内完全无效**。
- 但 `CharacterFactory.expMultiplierFor` = `1 + 魔力 × balance.magicToExpRate(0.02)` ⇒ **魔力 +6（秘法研习，D41 价 3 点）= 全队经验 +12%**；**含 N3 的 +16 魔力 ⇒ ×1.32**。
- ⇒ 4 点买近 18% 的全局成长加速。**建议**：把描述与效果对齐（改名或明写"全队战斗经验 +12%"），否则玩家会以为它是废节点。

### 30.6 成长曲线（D225 的重定标）

**D256 采纳方案 Z（设计值，未落地）**

| 参数 | 现值（配表） | **D256 裁决值** | 说明 |
|---|---|---|---|
| `balance.expExponent` | **1.6** | **1.9** | 唯一能压后期的旋钮 |
| 黏液怪 exp | 16 | **25** | 比 D33 的 20 更激进，用来补前期 |
| 穴居蝙蝠 exp | 11 | **21** | D33 是 17 |
| 巨鼠 exp | 14 | **29** | D33 是 23 |
| 四图 `enemyDensity` | 0.035 / 0.045 / 0.05 / 0.055 | **全部不动** | **D257 撤销 D35 的 0.060 / 0.064** |
| 三只旧 Boss 是否留在 `enemyPool` | 已移出 ✅ | **移出**（D258）✅ | 结构性压后期 |

**四图每趟升级数（★4 队，起始 L4，实测）**

| 方案 | 无根 | 苔痕 | 静谧 | 腐朽 | 台阶差值 | 连打四图 | 四图总战斗数 |
|---|---|---|---|---|---|---|---|
| **A 什么都不改（现值 1.6）** | 1.98 | 3.71 | 6.78 | **10.73** | +1.72 / +3.08 / +3.94 | L4 → **18.9** | 167 |
| **B 旧 D33+D35 全套** | 2.68 | 3.38 | 4.68 | **7.32** | +0.70 / +1.29 / +2.64 | L4 → **14.7** | **206** |
| **Z（D256 采纳）** | **2.01** | **2.88** | **4.10** | **6.30** | +0.87 / +1.22 / +2.20 | L4 → **13.33** | **167** |
| Q6（若要求腐朽 ≤ 6.1） | 2.16 | 3.02 | 3.94 | 6.07 | +0.85 / +1.13 / +2.35 | L4 → 13.1 | 206 |

★5 队：**1.41 / 2.04 / 3.26 / 5.41**。**零成长率：全部场景 × 全部图 = 0.0%**（1 600 人次/格）。

> **"台阶必须递增"只能当否决条件，不能当选择条件** —— 真实口径下"什么都不改"的 A 也是严格递增 ⇒ 这个约束在新标尺下已经没有鉴别力。真正要修的是**绝对量级**与**跑图长度**。方案 N（1.9 + 无根密度 0.060）的台阶 +0.05 倒挂，应被否决。

**为什么不用"加密度"补前期缺口（实测代价）**

| 图 | 一趟战斗数（现值密度） | 一趟战斗数（D33/D35 密度） | 变化 |
|---|---|---|---|
| 无根回廊 | **32.0** | **54.2** | **+69%** |
| 苔痕回廊 | 40.6 | 57.4 | +41% |
| 静谧墓所 | 44.9 | 44.9 | 0 |
| 腐朽王座 | 49.7 | 49.7 | 0 |
| **四图合计** | **167.2** | **206.4** | **+23%** |

⇒ D33/D35 之后**教学图（54/57 场）比最终图（50 场）还长**。⇒ **用"低阶怪 exp"补前期，不用"密度"补**（零新增战斗）。

**`expForLevel` 实测值（`expForLevel(L) = round(20 × L^expExponent)`）**

| `expExponent` | L4 | L5 | L6 | L8 | L10 | L12 | L15 | L20 |
|---|---|---|---|---|---|---|---|---|
| **1.6（配表现值）** | 184 | **263** | 352 | 557 | **796** | 1 066 | **1 523** | 2 414 |
| **1.9（D256 裁决值）** | 279 | **426** | 602 | 1 040 | **1 589** | 2 246 | **3 432** | 5 929 |

累计经验：1→10 = 2 676（1.6）/ 4 710（1.9）；1→15 = 8 041 / 16 075；1→20 = **17 376 / 37 973**。

> ⚠️ **D120 的第二个示例数字（11→12 级 740 金）用错了等级**：按 1.9 复算应为 **952 金**（`expForLevel(11) = 1 904` × 1.0）。5→6 级的 210 金是对的（实测 213）。

**对未落地改动的敏感度（实测弹性：升级数对"每场经验"）**

| 图 | 弹性 |
|---|---|
| 无根回廊 | **≈ 1.0**（量化严重） |
| 苔痕回廊 | **≈ 0.80** |
| 静谧墓所 | **≈ 0.63** |
| 腐朽王座 | **≈ 0.59** |

⇒ **越深的图越"钝"**：经验涨 15% 会被整数升级台阶吸收掉一部分。

| 未落地改动 | 影响 |
|---|---|
| **D118 精英 15% + D206 奖励 ×2** | 每场期望经验 = `0.85×E + 0.15×2E = **1.15 E**` ⇒ 升级数 **+10.2% / +12.1% / +9.4% / +7.7%**（不是均匀 15%）。腐朽从 6.30 涨到 ≈ 6.8；若要抵消，`expExponent` 需 +0.07（1.9 → 1.97） |
| **D148 / D413 敌人扩到 32 只（每图 8，浅/深分组）** | **唯一会真正推翻这套参数的改动** —— 它换的是"每场加权经验 `E`"。⚠️ 组合效应：`B + 精英15% + Boss 移出池` 实测 2.96/3.79/4.50/6.86 —— 精英把 Boss 出池省下的又吃回去大半，净变化只有 −6% |
| **D204 普通怪 `techPoints` 归 0** | **0 影响**（科技点与经验是两条独立奖励通道） |

**D148 落地时的换算式（不用重跑 MC）**

```
E'_d = E_d × (1 + p_elite) × (1 − bossExpShare_d) × (1 + deepBoost × w_deep)
ΔL_d ≈ L_d × 弹性_d × (E'_d / E_d − 1)

p_elite = 0.15（D118）   w_deep = 0.82（N4–N6 名义经验占比，实测 0.807–0.826）
bossExpShare_d = 无根 0 / 苔痕 0 / 静谧 0.226 / 腐朽 0.287（Boss 出池前）
弹性_d = [1.0, 0.80, 0.63, 0.59]
```
⇒ **建议把"每张图的加权经验/场"写成 D148 的显式验收指标，保持在现值 ±5% 以内**（现值：无根 13.80 / 苔痕 24.70 / 静谧 54.70 / 腐朽 104.42）。

### 30.7 材料、魔核与 12 件饰品

**12 件饰品的需求（配方来自 `accessories.json`，已落地）**

| 档 | 件数 | 配方（单件） | 小计 |
|---|---|---|---|
| ★3（档 I） | 3 | `item_ore` × 12 | 魔铁矿 36 |
| ★4（档 II） | 4 | `item_crystal` × 6 + `item_ore` × 8 | 结晶 24 / 铁矿 32 |
| ★5（档 III） | 5 | **`item_core` × 3** + `item_crystal` × 6 | **魔核 15** / 结晶 30 |
| **合计** | **12** | | **魔铁矿 68 / 深渊结晶 54 / 魔核 15** |

材料单价：魔铁矿 **25** / 深渊结晶 **120** / 魔核 **500**（卖出价一律 5 折）。

**魔核供给的八个场景（实测）**

| 场景 | 魔铁矿/趟 | 结晶/趟 | **魔核/趟** | 12 件需刷趟数 |
|---|---|---|---|---|
| **P0 配表现状**（三 Boss 在池内） | 88.7 | 41.9 | **5.15** | **2.91 趟** |
| **P1 D258 落地、D2 未落地** | 91.8 | 43.9 | **0.00** | **∞** 🔴 |
| P2 D258 + D2/D40（`core` ×2） | 105.0 | 50.7 | **2.00** | **7.50 趟** |
| P3 P2 + 精英必掉材料 | 171.8 | 108.5 | 2.00 | 7.50 趟 |
| **P4 P3 + 王座等待者 `core` ×4（D299）** | 171.8 | 108.5 | **4.00** | **3.75 趟** ✅ |
| P5 P3 + 精英也给魔核（静谧/腐朽 100%） | 171.8 | 108.5 | **16.46** | **0.91 趟** 🔴 |
| P7 P2 + 降级旧 Boss 保留原 `core` 掉率 | 105.0 | 50.7 | **16.25** | **0.92 趟** 🔴 |

**魔核的关系式（改配置时按此换算，不用重跑 MC）**

```
Core_per_trip = B_core + 0.15 × Σ_d ( battles_d × q_d )
B_core = 腐朽王座一趟的 Boss 魔核总量（D2 落地后 = 1 只 × 每只必掉数）
battles_d = 32.6 / 41.5 / 45.8 / 50.6（现值密度）
⇒ 趟数 = 15 / Core_per_trip
```
**D299：王座上的等待者 `item_core` 2 → 4** ⇒ 12 件饰品 **7.5 趟 → 3.75 趟**（单字段改动，语义干净：「只有王座上的东西体内才有魔核」）。

> 🔴 **最重要的顺序依赖：D258 与 D299 必须同批次**
> 现状魔核全部来自三只旧 Boss（迷宫的守护者 0.5 / 骸骨暴君 0.6 / 深渊之王 0.9×2.5），而 **D258 已把它们移出 `enemyPool`**（✅ 已落地）。新 Boss 只靠 **D2/D40 的"第 6 层固定 Boss"**才出现 —— 那段生成器代码**目前不存在**。
> ⇒ **若 D258 已落地而 D2 未落地，魔核产出 = 0，★5 饰品的 5 条配方永久无法完成。** 验收清单里应加一句机检：**「若 `dungeon_abyss.enemyPool` 内已无 `enemy_bone_tyrant` / `enemy_abyss_king`，则第 6 层固定 Boss 必须已生效，且 `enemy_boss_throne_waiter.drops` 里 `item_core` 的 `min = max = 4`」**。

**精英"必掉"的口径经过三次修订（本章以 GDD 现值口径为准）**

| 阶段 | 口径 | 问题 |
|---|---|---|
| D266 | 精英必掉"高稀有度物品"（**魔核 / 秘药**） | 若按字面执行：魔核 **×8.2**（2.00 → 16.46/趟）、秘药最多 **×15.9**（1.72 → 27.3/趟）⇒ 魔核与 D28 的"永久属性道具产出无上限"同时崩 |
| D298 | 收窄为"**该图档位材料**"，魔核与秘药都不进精英表 | 苔痕没有档 II 材料，写死"档位"会出问题 |
| **D411（现值）** | **精英必掉 = 该图能产出的最高档锻造材料**：无根/苔痕 → **魔铁矿**；静谧 → **深渊结晶**（+ 铁矿）；**腐朽 → 魔核**（+ 结晶） | ✅ 同时满足 D118（必掉锻造材料）与 D331 |

> ⚠️ **草案与 GDD 的冲突**：`techpoint-economy-v1.md` §5.6 裁定"魔核与秘药都不进精英掉落表"，**已被 D411 修订**（腐朽精英掉魔核）。⇒ 草案 §5.2 的 P4 场景（魔核 4.00/趟、3.75 趟）**需要按 D411 重算**。

**其它材料的产出（实测，现值密度，D350 前 → 后）**

| 材料 | 一趟四图 | 12 件需几趟 | 备注 |
|---|---|---|---|
| 魔铁矿 | 85.5 → **80.6** | 0.80 → **0.84 趟** | 相对过剩（一趟无根就够造 1.7 件 ★3）⇒ ★3 的材料成本 ≈ 0 |
| 深渊结晶 | 36.5 → **31.6** | 1.48 → **1.71 趟** | 在腐朽爆产（石魔像 + 黯甲 + 祭司都在池里） |
| **魔核** | **4.11 → 4.11（不变）** | **3.65 趟（瓶颈）** | **不在宝箱池里 ⇒ D350 对它零影响** |

⇒ **魔核是真瓶颈，12 件全造 ≈ 3.7 趟四图**。金币不是瓶颈（12 件锻造费 16 680 ≈ 0.9 趟）。

**秘药 `item_elixir`（永久 +2 随机属性）**：现状 **1.69 件/趟**，且**只从腐朽王座出**（骸骨暴君 0.3 / 深渊之王 0.6，合计权重仅 8/106）⇒ D28 的"产出无上限"风险**在当前配表下被权重压住**。D266 落地后 ≈ **1.40/趟**（不眠的司祭 0.35 + 王座等待者 1.05）。
**D207**：shrine「祈祷」不再给永久属性（36 点/趟 = 秘药产出的 10.6 倍，会把秘药整条线架空）。

### 30.8 消耗品体系（D193）

**9 种消耗品（v3 定稿；现役 `items.json` 共 19 条）**

| # | id | 名字 | ★ | 场景 | 效果 | 价格 | 堆叠 | 限购 |
|---|---|---|---|---|---|---|---|---|
| 1 | `item_return_stone` | **归途石** | 1 | 迷宫 | 立即回营地，金币/道具/经验/科技点全保留，`maxFloor` 不回退 | **55** | 10 | **1 / 趟** |
| 2 | `item_lantern` | **铜灯** | 1 | 迷宫 | 本层视野 +2 格，已探索区不再退回迷雾 | **60** | 20 | — |
| 3 | `item_regrowth` | **续命散** | 1 | 战斗 | 立即回 **12% maxHp**，之后 2 个大回合各回 **6%**（合计 24%） | **70** | 99 | — |
| 4 | `item_probe` | **探针** | 2 | 迷宫 | 本层剩余陷阱与宝箱**全部显形**（含未探索区） | **90** | 20 | — |
| 5 | `item_antidote` | **净毒粉** | 2 | 战斗 | 解除我方**全部减益** + 回 **8% maxHp** | **90** | 50 | — |
| 6 | `item_ward` | **铁誓符** | 2 | 战斗 | 战斗内获得 **25% maxHp 护盾**，先于 HP 扣除，持续到战斗结束 | **120** | 50 | — |
| 7 | `item_smoke` | **烟丸** | 2 | 战斗 | **必定脱战**（无战利品）；**对 Boss 与精英无效** | **150** | 30 | — |
| 8 | `item_veil` | **灰纱** | 3 | 战斗 | **3 大回合内我方受到伤害 −35%** | **150** | 30 | — |
| 9 | `item_last_candle` | **残烛** | 4 | 战斗 | 战斗内立即复活 1 名倒下队友 + 40% maxHp | **320** | 20 | — |

> **D430 裁定：删掉复活类道具（`item_feather` 不死鸟之羽 / `item_last_candle` 残烛）** —— 因为现设计里角色不会单独"死"，只有全队共享池的团灭。⚠️ **配表两条仍在**（未落地）。
> **D224 / D289**：烟丸是**唯一的脱战手段**（普通战也不做概率逃跑），**Boss 战禁用**（D50 优先）。

**定价方法**

```
price = round( E × S )  再取整到 5 或 10
  E（等效效果值）：
    回复 / 护盾类   E = 百分点数 × 4 金        （锚 = 小药水 30 金 / 8% = 3.75 金/百分点，取整 4）
    迷宫情报类     E = 避免 1 次伤害陷阱（15 百分点 × 4 = 60）+ 找回宝箱期望
    脱战 / 撤离类   E = 被规避掉的风险 × 触发概率
    归途石（保险）  E = 一趟下探的期望损失 × 团灭概率，不乘溢价
  S（场景系数）：战斗型 1.0（要付 1 AP + 1 次出手）；迷宫型 1.5（零机会成本）
```

**归途石的三档灵敏度表（实测推导）**：单次团灭损失 **1 860 金**（丢 50% 物品 1 260 + 进度重打隐性成本 600）

| 假设的单图团灭概率 p | 期望损失 | 应定价 | 占一趟无根 |
|---|---|---|---|
| **3%（现值采用，最保守）** | 56 金 | **55** | 2.5% |
| 8% | 149 金 | 150 | 6.7% |
| 15% | 279 金 | 280 | 12.5% |

**占收入比例自检（v3，按 D350 前的旧收入 2 234 / 8 007 计）**：9 件落在 **2.5%–14.3%**，其中 8 件在 2.5%–6.7% 的"随手买"带，只有残烛（14.3%）是"咬咬牙"档。**价格带跨度 5.8 倍**（55 → 320）。

**无套利检查（结构性，4 条）**：本批 9 件**全部不产出金币 / 材料 / 科技点 / 永久属性**，效果（HP / 护盾 / 减伤）无法变现 ⇒ **结构上不可能出现"低买高卖"**。
> 建议写成通用规则：**新增消耗品或事件奖励，若要产出金币 / 材料 / 科技点 / 永久属性，必须先过这四条换算检查。**（D207 砍掉 shrine 属性白送用的正是同一条逻辑。）

**分层掉落表（避免深层还在掉续命散）**

| 层 | 可掉落的消耗品 |
|---|---|
| N1–N2 | 续命散 / 铜灯 / 小药水 / 大药水 |
| N3–N4 | 探针 / 烟丸 / 净毒粉 / 续命散 / 铜灯 |
| N5–N6 | 铁誓符 / 归途石 / 灰纱 / 净毒粉 / 大药水 |
| Boss 掉落 | 残烛（唯一来源） |

> **供给量提示（实测推导）**：宝箱渠道每趟四图只给约 **73 件**道具（`108 箱 × 45% × 1.5`）。物品池扩到 14 件后，**每件每趟只有约 5 件** ⇒ **新道具的主渠道必须是商店购买，不是掉落**；`price` 的定位是"商店常备价"，不是"掉落物估值"。

**队伍生命池（实测，4 名 ★4 初始角色，400 次采样；`maxHp = Σ耐力 × 20`）**

| 等级 | 生命池均值 | P10 | P90 | 每级 +HP |
|---|---|---|---|---|
| **L4（起始）** | **923** | 620 | 1 240 | — |
| L6.69（无根通关） | 1 515 | — | — | +220 |
| L8.68（静谧通关） | 1 954 | — | — | +220 |
| L11.33（腐朽通关） | 2 536 | — | — | +220 |
| L15 | 3 348 | 2 760 | 3 960 | +220 |

**药水的等效回复率（D208 后，实测）**：小药水 **8% maxHp**（L4 ≈ 74 点 / L15 ≈ 268 点）、大药水 **20% maxHp**（L4 ≈ 185 / L15 ≈ 670），价 **30 / 80**。
⇒ **D208 之后"会退化的一方变成了治疗技能"**：`skills.json` 8 条 `heal` 均值 **82 点**（固定值），在 L4 是 8.9%，在 L15 只剩 **2.4%** ⇒ 药水与技能不是替代关系，而是"何时必须付 1 AP 换爆发"。

**一次伤害陷阱 ≈ 生命池的 14.5%–14.9%**（实测，四图一致）—— 这是所有"伤害 / 护盾 / 减伤"数值的标尺。

### 30.9 陷阱与迷宫伤害：对经济的贡献为 0

**三条从代码里读出来的硬事实（比任何蒙特卡洛都重要）**

| # | 事实 | 位置 |
|---|---|---|
| 1 | **陷阱不可能致死。** `pool.hp = Math.max(1, pool.hp - damage)` —— **HP 地板是 1** | `DungeonSystem.triggerAt` |
| 2 | **陷阱对战斗零影响。** `startBattle` 只传 `enemyIds / dungeonId / floor / seed`，**不传 HP**；`buildAllyUnit` 一律 `hp = maxHp = teamHp` ⇒ **每场战斗都是满血开局** | `DungeonScene.startBattle` / `CombatSystem.buildAllyUnit` |
| 3 | **D168「迷宫 HP 归零即团灭」在当前代码里不可达。** 全项目只有 5 处写 `pool.hp`，全部是 `max(1, …)` 或回满；`src/` 里**没有任何"迷宫团灭"判定** | 全仓扫描 |

⇒ **陷阱既不产金币，也不强制消耗金币**（永远不需要为了活命喝药：死不了，且下一场回满）。它现在只是"拖时间的减速/传送 + HUD 上一行红字"。

**同一战斗间隔内的陷阱暴露量（实测，公式 = `陷阱数 ÷ (战斗数 + 1)`）**

| 图 · 层 | 战斗数 | 陷阱/层（配表现值） | 每间隔期望 | 陷阱/层（D172/D349） | 每间隔期望 |
|---|---|---|---|---|---|
| 无根回廊 1 层 | 1.00 | 1 | 0.500 | 1 | 0.500 |
| 苔痕回廊 1 层 | 1.00 | **3** | **1.500** | 1 | 0.500 |
| 静谧墓所 1 层 | 1.00 | **4** | **2.000** | 2 | 1.000 |
| 腐朽王座 1 层 | 1.00 | **5** | **2.500** | 2 | 1.000 |
| 腐朽王座 6 层 | 19.13 | 5 | 0.248 | 2 | 0.099 |

> **关键读法（与直觉相反）**：**第 1 层的每间隔陷阱数最高（0.5–2.5），第 6 层最低（0.076–0.248）** —— 第 1 层只有 1 场战斗，而陷阱数与层数无关 ⇒ **风险集中在前几层**。

**一趟四图陷阱数**

| 口径 | 无根 / 苔痕 / 静谧 / 腐朽 | 合计 |
|---|---|---|
| **配表现值 1/3/4/5** | 6 / 18 / 24 / 30 | **78** |
| **D172 = D349 裁决值 1/1/2/2** | 6 / 6 / 12 / 12 | **36** |

> ⚠️ **配表仍是 1/3/4/5，裁决值 1/1/2/2 未落地。** 引用时必须写明是"配表现值"还是"裁决值"。
> D172 最初的理由（"一趟最多踩 2 次"）算错了（实际是 6–12 次），但 **D349 复核后仍裁定保持 1/1/2/2** —— 在正确语义下"每层少量"反而是对的。

**最坏情况的数值上界（腐朽第 6 层，生命池 3 640；陷阱 payload = 伤害 50% / 减速 25% / 传送 25%）**

| 口径 | 单层最多陷阱 | 伤害型（期望个数） | 最坏伤害 | 占生命池 | **全为伤害型** | 占生命池 | 能否打空 |
|---|---|---|---|---|---|---|---|
| 配表现值 1/3/4/5 | **5** | 2.5 | 2 244 | 62% | **3 740** | **103%** | **能**（打到 1 HP） |
| **D172 的 1/1/2/2** | **2** | 1.0 | 748 | 21% | 1 496 | **41%** | 不能 |

⇒ **D172 的直接效果不是"少扣点血"，而是把"能把你打空"变成"最多扣 41%"**。但即便在配表现值下，HP 地板也让它永远停在 1 HP，且下一场战斗满血开局。
⇒ **若设计意图是"陷阱要承担风险与抉择"，问题不在数量，而在"伤害不进战斗"这条链断了**（硬事实 #2）。**落地顺序：D1（真共享池）→ D168（致死路径）→ 才是 D172/D349 的数值微调。**

### 30.10 战斗长度对数值体系的影响（只取经济/数值结论）

**草案（`combat-length-recalibration-v1.md`，D108 v1）的实测**

| 项 | 实测 |
|---|---|
| 现状 TTK 中位 | **1.00**（P10 1.00 / P90 1.00；96 格全部 1.00） |
| 根因 | 12 个"图×层"采样点里 **10 个**"单次技能伤害 ÷ 敌方有效 HP" ≥ 1 ⇒ 被 `ceil` 钉成 1 回合 |
| 输出对 N 的幂次 | **α = 0.22 / 0.53 / 0.67**（N=4/5/6），**不是 2**；被 AP 闸门压住（出手 1.12–1.19 次/回合） |
| 生命池对 N 的幂次 | **α = 2.02 / 2.02 / 1.91**（N=2/3/4）⇒ 「∝N²」在**生命池轴**上，不在输出轴上 |
| 完全无效的 5 个旋钮 | AP 参数、D242 出手结构、敌方单位数、敌方出手次数（对 TTK）、饰品（对 TTK，它只会让 TTK **更短**） |
| 等价性 | **A（我方伤害 ÷N）≡ B（敌方 HP ×N）** 在 TTK 上完全等价（硬度 4.00 的三组给出完全相同的 1.00 / 1.00 / 3.00）⇒ "选 A 还是 B"是工程题不是数值题 |

**🔴 GDD 第 79 轮结案：三档硬门已达成，且零数值改动**

| 敌人类型 | 硬门（D304） | **实测** | 判定 |
|---|---|---|---|
| 普通敌 | 2–4 回合 | **2.29 – 3.53** | ✅ |
| 精英 | 5–10 回合 | **5.51 – 8.39** | ✅ |
| Boss | ≥10 回合 | **10.25 – 15.34** | ✅ |
| 团灭 | — | **0 / 528** | ✅ |
| 噩梦对普通/精英的 TTK | 需可感知 | **仅 ×1.05** | ⚠️ 仍不可感知 |

> **（草案曾提出 ★推荐 = A（我方伤害 ÷N）+ D1（真共享 HP 池）+ `defLayerScale` 0.30→0.20 + 敌方 HP ×4（实测 2.00 / 5.00 / 16.05，噩梦胜率 71%），已被 GDD 第 79 轮结案取代 —— 最终零数值改动，决定性变量是 D242。）**
> 结案的归因三层：① 配置层**不是主因**（饰品单件 +1.07%、D183 三件合计约 +9.7%、成长曲线 <1%）；② 结构层（我方每轮 4 个行动 vs 敌方 1 个）；③ **决定性变量 = D242「每技能每回合限 1 次」**（D353 定为 per-unit 语义），TTK 直接拉到 **6.15 回合**。
> ⇒ **批次 2 解锁条件（D240）满足**（D356 立即解锁）⇒ 战术叠层 / 深渊吞噬 / 饰品防御类 / 精英二动 / R2 反击 全部可以验收。

**饰品伤害轴的两套口径（不能混用）**

| 口径 | 值 | 用途 |
|---|---|---|
| **D197 的 +24.13%** | 保守**上界**（假设每个饰品作用于全队） | **安全性用途**（判断"最坏能到多强"） |
| **实测 +9.7%** | 实际预期值（只作用于佩戴者） | **制定目标用途** |

⇒ **D355 裁定：只留实测值 +9.7%，推翻 D197 的 +24.13%**。另注意敌方战术的 +19.4% 与我方饰品/战术**同源**（都作用于同一个 `team.*`）⇒ 同时生效会互相吸收，**两边不能各自按独立加成估算**。

> 🔴 **顺序依赖（对经济数值最要紧的一条）**：**凡是"每回合 / 持续 N 回合 / 第 N 次"型的机制，在 D108 落地前一律视为"未验证"** —— 现值战斗只有 1 个回合，战术叠层（D241 的 3 层 / 23 回合）、饰品「每回合首次受伤 ×0.5」、精英二动（D151）全部是纸面效果。**D108 应排在批次 2 的最前面，而不是与批次 2 并行。**

### 30.11 顺序依赖：必须先做 A 才能做 B

| # | 必须先做（A） | 才能做（B） | 颠倒的后果 | 状态 |
|---|---|---|---|---|
| 1 | **第 6 层固定 Boss（D2/D40）的生成器代码** | **三只旧 Boss 移出 `enemyPool`（D258）** | 🔴 **魔核产出归零** ⇒ ★5 饰品的 5 条配方永久无法完成（宝箱池里也没有魔核）；12 件从 3.75 趟变成 **∞** | ⚠️ D258 已落地，**D2 未落地 ⇒ 处于"魔核 = 0"窗口** |
| 2 | **D350（宝箱 2/3/3/4）** | **D367（`S9` 阈值 1 700）** | 🔴 单独只做 D350 会让 `S9` 掉 **−3.23 点**；两者 −3.23 / +3.12 几乎抵消（净 −0.11）⇒ **只回退其中一个会出问题** | ✅ 均已落地 |
| 3 | **D108（战斗长度）** | **批次 2 全部机制**：战术叠层 / 深渊吞噬 / 饰品防御类 / 精英二动（D151）/ R2 | 🔴 1 回合清场 ⇒ 所有"每回合结算 / 持续 N 回合 / 二动"机制没有第 2 个回合可用 | ✅ 已结案，D356 解锁 |
| 4 | **D1（真共享 HP 池）** | **D168（致死路径）** → 才是 **D172/D349 的 `trapCount` 数值微调** | 🔴 陷阱 HP 现在是一本"不参与任何判定的账"，怎么调都"感觉无差别" | ⬜ 未落地 |
| 5 | **D272（增减益落点修正）** | **D233 / D249（8 + 10 条 debuff 的数值改动）** | 🔴 `resolveTargets` 把 `ally_all` / `self` 一律返回 `[actor]` ⇒ 全队削减实际只打在施法者身上，数值改完会造出"自伤技能" | ⬜ 未落地 |
| 6 | **D259（`power=0` 辅助技也要掷强化）** | 战术「结构加固」的第 2 条捷径 | 🔴 强化判定发生在伤害路径内 ⇒ 纯辅助技永不触发 ⇒ 捷径在代码里永不生效 | ⬜ 未落地 |
| 7 | **树价 99 ⇒ 先改 `tech.json` 再验算 80%** | 一次性把 16 条改成 D41 值 + 新增 5 条（共 21 条 / 99 点） | 🔴 只加新节点不改旧价格 ⇒ 树 = 186 点，首清达成率掉到 41%–44% | ✅ 已落地（21 节点 / 99 点） |

### 30.12 配表现值 vs 裁决值对照（落地状态）

改经济数值前**先看这张表** —— 本章大量数字是"裁决值"，而配表里可能还是旧值。

| 项 | 裁决值 | 配表现值 | 是否落地 |
|---|---|---|---|
| `treasureCount` | **2 / 3 / 3 / 4**（D350） | 2 / 3 / 3 / 4 | ✅ 已落地 |
| `trapCount` | **1 / 1 / 2 / 2**（D172 = D349） | **1 / 3 / 4 / 5** | ❌ **未落地** |
| `firstClearTechPoints` | **8 / 9 / 10 / 11**（D246） | 8 / 9 / 10 / 11 | ✅ 已落地 |
| `enemyPool` 移除 3 只旧 Boss | 移除（D258） | 已移除（无根 3 / 苔痕 5 / 静谧 4 / 腐朽 5 只） | ✅ 已落地 |
| `enemyDensity` | **全部不动**（D257 撤销 D35） | 0.035 / 0.045 / 0.05 / 0.055 | ✅ 现值即裁决值 |
| `expExponent` | **1.9**（D256） | **1.6** | ❌ **未落地** ⇒ 所有 1.9 口径的训练场金额是"设计值" |
| 三小怪 `exp`（黏液/蝙蝠/巨鼠） | **25 / 21 / 29**（D256） | **16 / 11 / 14** | ⚠️ 见 §30.13 #1 |
| `recruitGoldCost` | **5 000**（D310） | **500** | ❌ **未落地** |
| `recruitCostGrowth` | **1**（D67 取消递增） | 1 | ✅ 已落地 |
| `recruitTechPointCost` | **0**（D300 ④） | 0 | ✅ 已落地 |
| 科技树 | **21 节点 / 99 点**（D297） | 21 条 / 合计 99 | ✅ 已落地 |
| `forgeCostRate` | **0.6**（D167 经济修正） | 0.6（12 条全为 0.6） | ✅ 已落地 |
| 药水口径 | **8% / 20% maxHp**（D208） | `value` 8 / 20，`valueUnit: pctMaxHp`，价 **30 / 80** | ✅ 已落地 |
| 消耗品 9 件 | 见 §30.8 | 全部在 `items.json`，含 `perRunPurchaseLimit` | ✅ 已落地 |
| 复活类道具 | **删除**（D430：残烛 / 不死鸟之羽） | **仍在配表**（320 / 300） | ❌ **未落地** |
| `item_coin_pouch.value` | **60**（D371） | **120** | ❌ **未落地** ⇒ **套利仍开着** |
| 王座等待者 `item_core` | **4**（D299） | 该敌人尚未进配表 | ❌ **未落地**（配合 D2） |
| `item_core` ×3（★5 配方） | 3 | 3 | ✅ 已落地 |

### 30.13 ⚠️ 待澄清清单

| # | 议题 | 两个口径 | 影响 |
|---|---|---|---|
| 1 | **三小怪 exp** | D256 裁决 **25 / 21 / 29**；配表现值 **16 / 11 / 14**（D416 授权"现役怪 exp/gold 允许调整"后） | 找不到把 25/21/29 改成 16/11/14 的显式裁决记录。**若按配表现值，无根 ★4 的升级数偏离 D256 的 2.01** ⇒ 需要补一条裁决或重跑一次 MC |
| 2 | **支线金币** | §30.4 全部循环表用 **11 410 金/循环**（quests-draft §4.3，未独立复验其频次假设）；GDD 第 81 轮改为 **8 270 金/趟** | 若按 8 270 重算，稳态收入、13.3 / 33.4 循环、31.1 / 77.8 小时**全部要变** |
| 3 | **起始队伍生命池口径** | 实测（4 名 ★4，400 采样）**L4 = 923 → L15 = 3 348**（3.6 倍）；GDD/lead 引用 **L4 = 1 560 → L17 = 6 500**（4.2 倍） | 相差 1.7 倍（起点人数或稀有度假设不同）。**不影响 `% maxHp` 口径的任何数值**，但会影响 UI 上显示的**绝对点数**（"回复 138 点" vs "回复 234 点"）与战斗伤害标定 |
| 4 | **精英"必掉"的最终口径** | 草案（D298 口径）"魔核不进精英表"；**D411（现值）腐朽精英掉魔核** | 魔核/趟 与 12 件饰品的趟数（3.75 趟）需按 D411 重算；章节里两个值并存 |
| 5 | **事件的金币净额** | `economy-report-v1.md` 口径 **−1 116 金/趟四图**；事件草案 v3 口径 **+392 金/趟四图** | 符号相反。本章循环表用 +392（较新口径），但两者未对账 |
| 6 | **战斗型消耗品的首版数值** | 护盾 **25%** / 减伤 **35%** / 续命散 12%+6%×2 是"先出的首版值" | 承伤曲线取决于敌方实际伤害（D73 难度倍率 × D14 按层缩放），D108 结案后**应复标一次** |
| 7 | **烟丸"脱战不触发 D185 的战斗后回满"** | 草案按"不触发"设计（这是 5 条代价里最关键的一条）| 若触发回满，烟丸变成"150 金买一次无伤脱身"，净成本从 239 金降到 150 金 ⇒ 必须涨价。**需与 `BattleScene` 实现对齐** |
| 8 | **D304 的 Boss 档** | 硬门"≥10 回合"；实测 96 格 TTK 全落在 **[2,10]**，从未到过 11 —— 4 只 Boss 尚未进配表（D40 全部是新增） | **D433：Boss 档标 ⏸，待 Boss 落地后补验**；明令不得拿普通怪的 `rounds === 10` 当替身 |
| 9 | **噩梦难度的可感知度** | D73 要求难度倍率可感知；实测噩梦对普通/精英的 TTK **仅 ×1.05** | 与 ★推荐的参数选择直接相关，至今未达标 |
| 10 | **循环→小时折算** | 2.33 小时/循环（quest-writer 的数） | **未独立复验**。31.1 / 77.8 小时两个数建立在其上 |

---

## 40. 内容与系统规格

> 本章来源：`design/drafts/content-draft-v1.md`、`events-draft-v1.md`、`quests-draft-v1.md`、`tutorial-draft-v1.md`、`save-schema-spec-v1.md`、`ui-spec-additions-v1.md`、`unimplemented-effects-registry.md`、`gap-table-round1/2/3.md`、`acceptance-matrix-v1.md`、`handoff-to-implementer-v1.md`（原文已归档）

> **读法约定（D435 / D436）**：本章所有引用一律用「小节号 + 原文措辞」定位，**不使用行号**。要求他人改动某段文案时，必须**逐字引用原文**，不得转述。
> **裁决优先级**：本章所有 `D###` 编号均指 `design/gdd.md` 中的裁决条目，**编号一律保留、不重排**。凡草案与 GDD 冲突处，**一律写 GDD 版本**，并在括号内注明「（草案曾提出 X，已被 GDD 否决）」；GDD 中查不到裁决的，标 `⚠️ 待澄清`。
> **G 编号是两套命名空间（D387）**：`save-schema-spec-v1.md` 的 **G1–G19** 与 `ui-spec-additions-v1.md` 的 **G1–G18 + N-1…N-12** 互不相干。本章一律带文件名前缀书写，如 `save-G19`、`ui-G15`。

---

### 40.1 数值基线：层级缩放与队伍强度带

**层级缩放（每层相对第 1 层基础值）**：

| 维度 | 第 N 层系数 | 第 6 层实际系数 |
|---|---|---|
| HP | ×(1 + 0.5 × (N − 1)) | **×3.5** |
| 攻击 | ×(1 + 0.25 × (N − 1)) | **×2.25** |
| 防御 | ×(1 + 0.3 × (N − 1)) | **×2.5** |

**队伍强度基线（用于校准饰品点数、Boss 阈值、事件收益）**：取样等级 **L4 / L8 / L11 / L17**，分别对应「无根回廊 / 苔痕回廊 / 静谧墓所 / 腐朽王座」的推荐进入带。

**防御口径（D104，第 2 轮缺口表裁决）**：`defendApCost = 1`；防御减伤为**在 clamp 之后独立作用的 ×0.8 乘区**（不是并入 clamp 前的加法项）；`defendReduction` 字段**已废弃**（deprecated，仅为兼容保留）。

---

### 40.2 饰品系统（12 件 · 4 类 × 3 档）

> 来源 `content-draft-v1.md` v4.1（定稿冻结），已并入 GDD §11.A.1。
> 对应裁决：D80–D85 / D88 / D113 / D121 / D122 / D128 / D130 / D141 / D156–D159 / D162–D171 / D183–D198 / D216 / D266 / D298（修正）/ D331（再修正）。

#### 40.2.1 档位 = 材料档

**D195 之后不再承诺「每类三档各一」**——档位由材料档决定，不是由类别决定。

| 档 | 适用等级带 | 材料 | 掉落源 |
|---|---|---|---|
| 档 I | Lv1–8 | `item_ore` 魔铁矿 | 无根回廊 / 苔痕回廊 |
| 档 II | Lv8–14 | `item_crystal` 深渊结晶 | 静谧墓所 |
| 档 III | Lv14+ | `item_core` 魔核 | 腐朽王座 |

#### 40.2.2 十二件饰品表

| # | id | 名字 | 类 | 档 | ★ | 数值 | 配方 | 输出增量 |
|---|---|---|---|---|---|---|---|---|
| 1 | `acc_a1_ironweight` | 沉铁配重环 | 1 属性 | I | 3 | 筋力 +2、耐力 +1（3 点） | 魔铁矿 ×12 | +2.8% 攻 / +1.4% HP |
| 2 | `acc_a2_bearing` | 静默轴承组 | 1 属性 | II | 4 | 筋力 +2、耐力 +4（6 点） | 深渊结晶 ×6 + 魔铁矿 ×8 | — |
| 3 | `acc_a3_deepcore` | 深核供能芯 | 1 属性 | III | 5 | 筋力 +5、耐力 +6（11 点） | 魔核 ×3 + 深渊结晶 ×6 | — |
| 4 | `acc_p1_metronome` | 节拍同步器 | 2 概率 | I | 3 | **连击率 +4pp** | 魔铁矿 ×12 | +6.8% |
| 5 | `acc_p2_crosshair` | 准星校准环 | 2 概率 | II | 4 | **暴击率 +10pp** | 深渊结晶 ×6 + 魔铁矿 ×8 | +5.9% |
| 6 | `acc_p3_amplifier` | 脉冲放大器 | 2 概率 | III | 5 | **强化率 +10pp** | 魔核 ×3 + 深渊结晶 ×6 | +2.3% |
| 7 | `acc_t1_freerun` | 空转惰轮 | 3 战术 | II | 4 | 每回合首次受伤 ×0.5 | 深渊结晶 ×6 + 魔铁矿 ×8 | 0（纯防御） |
| 8 | `acc_t2_solver` | 装甲解算器 | 3 战术 | III | 5 | 单次受伤 ≤ 队伍最大生命 25% | 魔核 ×3 + 深渊结晶 ×6 | 0（纯防御） |
| 9 | `acc_t3_ballast` | 压舱锚针 | 3 战术 | III | 5 | 比例类伤害（削血 / 中毒）×0.5 | 魔核 ×3 + 深渊结晶 ×6 | 0（纯防御） |
| 10 | `acc_r1_plating` | 自愈镀层 | 4 每回合 | I | 3 | 每个大回合开始回复 4% maxHp | 魔铁矿 ×12 | 续航 |
| 11 | `acc_r2_reflector` | 相位反射片 | 4 每回合 | II | 4 | 每回合首次受击时反击「队伍攻击力 × 30%」 | 深渊结晶 ×6 + 魔铁矿 ×8 | +2.9% |
| 12 | `acc_r3_dissociator` | 逆向解离针 | 4 每回合 | III | 5 | 我方阶段开始清除 1 条属性削减 | 魔核 ×3 + 深渊结晶 ×6 | 0 |

> ⚠️ **id 冲突已按 GDD 定案**：`content-draft-v1.md` 曾把 7/8/9/12 写作 `acc_t4_idler` / `acc_t3_solver` / `acc_t5_ballast` / `acc_r4_dissoc`；**GDD §11.A.1 与 `src/data/tables/accessories.json`（12 键）一致采用上表的 `acc_t1_freerun` / `acc_t2_solver` / `acc_t3_ballast` / `acc_r3_dissociator`**（战术类按 t1/t2/t3 重排、每回合类按 r1/r2/r3 重排）。**以 GDD / 落地 id 为准**。

#### 40.2.3 关键数值口径（均为裁决，非建议）

| 项 | 值 | 裁决 |
|---|---|---|
| 槽位归属 | **每个出战位 1 个**（挂在出战位，不是挂在角色上 ⇒ 换人不丢） | D292 |
| 槽位开放 | **开局就有** | D270 |
| 类 1 取值规则 | 单件 = **该档队伍该维均值的 3.3%**（3 件满装 ≈ 10%） | D187 定标 |
| 类 1 只放线性维 | 只加筋力 / 耐力；敏捷 / 幸运 / 灵性是**饱和曲线**，+40 也只有 +3pp | §①.3 |
| 类 3 三件可同装 | **接受**，代价写明：三件满装 = 放弃全部输出饰品 | D198 |
| **伤害轴最坏上界** | **≤ +25%**（实操 **+21.96%** / 理论上界 **+24.13%**） | D197 |
| 获取途径 | **锻造（主力）+ 商店每店限购 1 件** | D128 |
| 配方是否全知 | **开局全知** | D282 |
| `price` | ★3 = **600** / ★4 = **1 500** / ★5 = **4 000** | D171 |
| 锻造费 | **材料 + `price` × 0.6** | D167 + 经济修正 |

> **锻造费口径（草案 Q3 曾提出 (b) `price × 0.9`，已被 GDD 否决）**：0.9 会让「自造」比「直购」贵，方向反了；GDD §11.A.1 定为 **×0.6**。

#### 40.2.4 已作废的三件（D184，保留可追溯）

| 原 id 名 | 效果 | 作废理由 |
|---|---|---|
| T1 过载抑制器 | 取消临时 AP 升耗 | 加的是**行动次数**，与伤害轴**相乘** |
| T2 冗余电容 | AP 上限 +2 / 初始 +1 | 同上 |
| R3 终末采样器 | 每回合 +2 AP | 同上 |

作废后六槽最坏组合由 **+148%**（R3 单件即 **+73%**）回落到 §40.2.3 的 ≤ +25%。

---

### 40.3 专属 Boss（4 只）与旧 Boss 降级

> 来源 `content-draft-v1.md` §②，已并入 GDD §11.A.2。

#### 40.3.1 基础值表

**写进 `enemies.json` 的是「基础值」；第 6 层实际值 = 基础值 ×（HP ×3.5 / 攻 ×2.25 / 防 ×2.5）。**

| | 碎巢母 | 菌王·腐甜之主 | 不眠的司祭 | 王座上的等待者 |
|---|---|---|---|---|
| 地牢 | 无根回廊 | 苔痕回廊 | 静谧墓所 | 腐朽王座 |
| 族群主题 | 杂居兽群 | 真菌孢子 | 亡骸军团 | 深渊眷属 |
| `id` | `enemy_boss_swarm_mother` | `enemy_boss_spore_king` | `enemy_boss_sleepless_priest` | `enemy_boss_throne_waiter` |
| `level` | 5 | 7 | 10 | 14 |
| **`hp` 基础** | 330 | 551 | 965 | 1 328 |
| **`attack` 基础** | 29 | 49 | 71 | 107 |
| **`defense` 基础** | 0.06 | 0.10 | 0.14 | 0.18 |
| `exp` | 140 | 250 | 450 | 800 |
| `gold` | 120 | 230 | 400 | 780 |
| **`techPoints`（重复击杀）** | **0** | **0** | **0** | **0** |
| 第 6 层实际 HP | 1 155 | 1 929 | 3 378 | 4 648 |
| 第 6 层实际攻击 | 65 | 110 | 160 | 241 |
| 第 6 层实际防御 | 0.15 | 0.25 | 0.35 | 0.45 |
| 必掉（`chance` 1.0） | `item_ore` ×15 | `item_crystal` ×3 | `item_crystal` ×4 | `item_core` **×4** |

#### 40.3.2 推导口径与硬约束

- **等级 = 该图推荐等级 + 4**（D48）
- **HP = 同层普通敌实际 HP × 9**（D48 的 8–10 倍取中位）
- **攻击 = 同层普通敌攻击均值 × 3.0**（D48 的「不秒人」）
- **防御基础值硬性封顶 0.18**：第 6 层 ×2.5，基础 > 0.38 会被 `clamp(0.95)` 吃掉（见 `DamageCalculator.ts` 的 clamp 分支）
- **`techPoints` 全为 0**：D254/D255 之后战斗不产科技点；首通 38 + 主线 14 走 `firstClearTechPoints` 与任务两条路
  - （草案 §② Q9 曾提出 4 / 5 / 6 / 8，**已被 GDD 否决**）
- **`item_core` 必掉 ×4**（D299，原 2）：造满 12 件饰品从 7.5 趟降到 3.75 趟。⚠️ **必须与 D258 同批次落地**，否则魔核来源为 0
- **专属技能**：每只 3–4 条（D51 由「固定条数」改为「2–4」），共 **13 条**；释放顺序按 **D111「固定循环 + 血量阈值插大招」**
- **分级 debuff 数值**：**−4 / −6 / −9 / −12**（D196）
- **Boss 也享受本图族群特性，但有逐只例外**（D162 + D165 / D166 / D169）：
  - 菌王的孢子**改为「HP 首次跌破 50% 时触发」**（D165）
  - **司祭不吃亡骸重整**（D166）
  - 碎巢母的分裂**继承 15%**（D169，非普通敌的 25%）

#### 40.3.3 旧 Boss 降级

原 4 只通用 Boss 在四只专属 Boss 上线后**降级为精英**（§2.4 降级规则）：保留 `id` 与美术，移出「第 6 层必遇」位置，改由精英刷新池承载；`level` / `hp` / `attack` 按同层精英带重新对齐，**不再享受 Boss 必掉表**。

---

### 40.4 族群被动特性（4 条）

> 来源 `content-draft-v1.md` §③，已并入 GDD §11.A.3。

| 图 | 族群 | `family` | 特性 | 效果 |
|---|---|---|---|---|
| 无根回廊 | 杂居兽群 | `swarm` | **群体本能** | HP 归零后分裂成「残躯」：**继承 25% HP、攻击 ×0.7**，**只触发 1 次**、残躯不继承特性。⚠️ **碎巢母为例外：继承 15%**（D169） |
| 苔痕回廊 | 真菌孢子 | `fungus` | **孢子爆发** | 被击败时我方全队**中毒 2 回合**（每回合 **maxHp 的 3%**）；**死于连击第 2 段及以后则不触发** |
| 静谧墓所 | 亡骸军团 | `undead` | **亡骸重整** | 受到**治疗**时反而受到**等量伤害**（按实际恢复量结算，**满血时溢出部分不计**） |
| 腐朽王座 | 深渊眷属 | `abyss` | **深渊吞噬** | 战斗进入**第 3 个及以后的我方阶段**时**触发 1 次**，我方全队损失**当前生命 8%**（落点 `onAllyPhaseStart()`，D159） |

**三条硬口径**：

1. **作用于「我方全队」的效果只结算 ×1** —— 战斗被视作 1V1，队伍是一个整体（**D273**）
2. **特性挂在敌人身上**（新增 `family` 字段），**不是挂在地牢上** —— D148 之后同一地牢有 8 只怪，且敌人可能跨图复用（如骷髅兵同时出现在苔痕与静谧）
3. **`family` 字段是新增字段**（D231 / D272 的落点修复之前，「敌方给我方上 debuff」无法表达）

**配套字段**：`family`（`swarm` / `fungus` / `undead` / `abyss` / `none`）、`splitRatio`（分裂继承比例）、`traitException`（逐只例外标记）。

> ⚠️ **分裂继承比例的口径**：GDD §11.A.3 定为**普通敌 25% / 碎巢母 15%（D169）**。`content-draft-v1.md` v5 章节曾提出「把两者统一为 15%」，**本章按 GDD 写 25% / 15%**；`⚠️ 未实现` 与 `⚠️ 待澄清` 两节不再重复挂此条。

---

### 40.5 迷宫事件系统（11 类）

> 来源 `events-draft-v1.md`（迷宫事件扩充 v3，D192 定稿）。

#### 40.5.1 事件总表

`src/data/tables/events.json` **实际 11 键**，与草案 id 完全一致：

| id | 名称 | `tone` | `minFloor` | 权重 shallow / mid / deep | 选项数 |
|---|---|---|---|---|---|
| `event_spring` | 微光清泉 | `boon` | 1 | 30 / 18 / 8 | 3 |
| `event_merchant` | 兜帽商人 | `boon` | 1 | 22 / 15 / 10 | 2 |
| `event_old_cache` | 半塌旧库 | `boon` | 2 | 18 / 10 / 6 | 3 |
| `event_shrine` | 无铭神龛 | `risk` | 1 | 12 / 14 / 10 | 4 |
| `event_gambler_altar` | 骰子石桌 | `risk` | 3 | 0 / 12 / 12 | 3 |
| `event_whispering_door` | 低语门扉 | `risk` | 3 | 0 / 9 / 12 | 3 |
| `event_sunken_idol` | 沉水神像 | `risk` | 3 | 0 / 8 / 12 | 3 |
| `event_blood_pact` | 血契石碑 | `risk` | 5 | 0 / 0 / 10 | 3 |
| `event_ambush` | 寂静埋伏 | `bane` | 1 | 18 / 14 / 8 | 2 |
| `event_miasma` | 腐雾 | `bane` | 3 | 0 / 8 / 8 | 3 |
| `event_collapse` | 塌方 | `bane` | 3 | 0 / 4 / 4 | 3 |

#### 40.5.2 数据结构字段

| 结构 | 字段 |
|---|---|
| `EventDef` | `id`、`name`、`text`、`tone`（`boon` / `risk` / `bane`）、`minFloor`、`weight {shallow, mid, deep}`、`options[]`、可选 `runLimit`、可选 `requires` |
| `EventOption` | `label`、可选 `successRate`、可选 `failEffects[]`、`effectsByBand {shallow, mid, deep}` |
| `Effect` 类型 | `item`（`itemId` + `count`）、`gold`、`heal`、`damage`、`buff`、`debuff`、`floorEffect`（含迷雾刷新控制）等 |

**运行时状态**：事件在一次下探（`activeRun`）内按 `runLimit` 限次；已触发记录随 `runCounters` 走，**下探结束或团灭即清**。

**`DataLoader` 校验项（必须落）**：`tone` 枚举合法、`weight` 三键齐全且 ≥0、`effectsByBand` 三键齐全、`successRate` ∈ [0,1]、`requires` 引用的 id 存在。

#### 40.5.3 关键裁决

- **`event_collapse` 第二个选项 = 「迷雾不刷新」**（v3 裁决，不是「迷雾加速刷新」）
- **净金币收益 = +392 / 趟四图**（v3 锁定）
  - （草案 v1 曾测算 +757 / +1 157 / +637 三档，**全部作废**）
- **D320：不在事件对话框里披露失败率**（`successRate` 不呈现给玩家）
- **D185 重新审计**（§5 ④）：事件收益不与商店/锻造定价形成套利闭环
- ⚠️ **唯一遗留开放项**：`event_shrine` 无铭神龛的**随机池分层**尚未定稿（见 §40.11）

---

### 40.6 任务系统（4 主线 + 9 支线）

> 来源 `quests-draft-v1.md` v4。落地表 `src/data/tables/quests.json` **实际 13 键**（4 主 + 9 支，**S3 已按 D330 删除**）。

#### 40.6.1 主线任务（4 条，一次性）

| id | 名称 | 条件 | 金币 | 科技点 | `layer` | `oneShot` |
|---|---|---|---|---|---|---|
| `quest_main_root` | 巢母之死 | 击败 `enemy_boss_swarm_mother` | 800 | 2 | permanent | true |
| `quest_main_moss` | 菌王枯朽 | 击败 `enemy_boss_spore_king` | 1 500 | 3 | permanent | true |
| `quest_main_tomb` | 司祭长眠 | 击败 `enemy_boss_sleepless_priest` | 2 800 | 4 | permanent | true |
| `quest_main_abyss` | 王座易主 | 击败 `enemy_boss_throne_waiter` | 5 000 | 5 | permanent | true |

**主线合计：10 100 金 / 14 科技点**，每个存档恰好各 1 次（分母恒为 1，必达）。

#### 40.6.2 支线任务（9 条，可无限重复）

| id | 名称 | 条件 | 金币 | 科技点 | `layer` |
|---|---|---|---|---|---|
| `quest_sub_floor3` | 三层止步 | 单次下探内首次抵达第 3 层（`runCounters.maxFloor ≥ 3`） | 250 | 1 | run |
| `quest_sub_floor6` | 六层之底 | 单次下探内首次抵达第 6 层（`runCounters.maxFloor ≥ 6`） | 600 | 2 | run |
| `quest_sub_recruit` | 同行不辍 | 累计**花金币**招募 6 次（重复角色计入），每满 6 次计 1 次 | 600 | 1 | permanent |
| `quest_sub_forge` | 铸器不歇 | 累计锻造 8 件饰品（任意档、允许重造），每满 8 件计 1 次 | 480 | 1 | permanent |
| `quest_sub_forge5` | 炉火不熄 | 累计锻造 3 件 ★5 饰品，每满 3 件计 1 次 | 1 200 | 3 | permanent |
| `quest_sub_elite` | 猎杀精英 | 累计击败 15 只精英怪（跨会话） | 600 | 2 | permanent |
| `quest_sub_boss_return` | 重返王座 | 重复击败任一图第 6 层 Boss（**不含首杀**） | 250 | 2 | permanent |
| `quest_sub_hoard` | 满载而归 | 单次下探内从宝箱累计取得金币 **≥ 1 700** | 600 | 2 | run |
| `quest_sub_unscathed` | 全身而退 | 连续 3 张图通关，期间未发生团灭 | 250 | 2 | permanent |

**支线可无限重复**（D237：`quests[questId]` 只累计次数，不设完成上限）。

#### 40.6.3 阈值与可行性口径

- **S9（`quest_sub_hoard`）阈值 2 500 → 1 700**（**D350 定新值，D367 宣布原 2 500 失效**）
  - D350 后宝箱金币期望 929 / 1 394 / 1 393 / 1 858；逐图 P(≥1700) ≈ 1.0% / 22.7% / 22.7% / 63.3% ⇒ **约 1.10 次/趟**，落在验收带 **0.8–1.4**
  - ⚠️ 依赖 `dungeons.treasureCount` 与 `MazeGenerator.rollTreasurePayload`
- **`quest_sub_recruit` 依赖 `stats.totalRecruits`**（D362）：默认 **0**，开局 `createStarters()` 白送的角色**不计入**；判据 = 「累计花金币招募 6 次（重复角色计入、退货后再招也 +1）」
- **`quest_sub_elite` 依赖精英密度**：现值 170.6 场/趟四图 × 15% ≈ 25.6 只 ⇒ 约 1.7 次/趟。⚠️ 依赖 D118 精英出现率与 D14 / D257 `enemyDensity`
- **阈值可行性方法（§4.7）**：以「0.25 死亡 / 2.0 ATM / 0.5–1.5 目标」为基准，**可接受带 0.8–1.4 次/趟**

#### 40.6.4 经济产出

- **支线金币 = 组合 B = 8 270 金 / 趟四图（占 44.9%）**
- **稳态：34.7 科技点 + 8 270 金 / 2.33 小时**

#### 40.6.5 两层任务表（与存档的映射）

| 层 | 容器 | 语义 | 清除时机 |
|---|---|---|---|
| 永久层 | `state.quests: Record<questId, number>` | 主线 = 全局一次性（`count ≥ 1`）；支线 = 可无限重复（D237） | 不随下探/团灭清除 |
| 整趟层 | `state.activeRun.runCounters` | 单次下探内计数（`maxFloor`、购买类 `buy:` 前缀等） | **随下探重置；团灭随进度清** |

---

### 40.7 新手引导与首次标记（11 条）

> 来源 `tutorial-draft-v1.md` v6。

#### 40.7.1 十一条情境提示

真源 = `src/types/save.ts` 的 `FIRST_TIME_FLAG_KEYS` 与 `src/ui/GuidePanel.ts` 的 `GUIDE_ITEMS`（两者**逐条一致**）：

| 序 | flag key | 触发情境 | 该条要讲的机制 |
|---|---|---|---|
| T1 | `tip_combat_ap` | 首次进入战斗（我方阶段开始） | AP 是全队共享资源：开局 3、每回合 +2、上限 8 |
| T2 | `tip_skill_cost` | 首次展开技能列表 | 技能按倍率分三档计价 |
| T3 | `tip_skill_once` | 首次试图重复释放同一技能被拒绝 | 同一技能每单位每回合限 1 次 |
| T4 | `tip_ap_dry` | 首次 AP 不足以做任何事 | AP 见底没有任何兜底，我方阶段直接结束 |
| T5 | `tip_tactics` | 首次获得任意战术层数 | 战术上限 3 层 / 持续 3 回合 / 重复释放刷新 |
| T6 | `tip_dungeon_overview` | 首次在迷宫中迈出第一步之后（全局只弹一次） | 三态迷雾 / 无小地图 / 事件有代价 / 团灭代价 |
| T7 | `tip_trap` | 首次踩到陷阱且伤害被结算 | 陷阱当场扣血，不会自动回满 |
| T8 | `tip_hp_ledger` | 首次带伤进入战斗 | 战斗满血开局，但迷宫伤算到本场战斗结束 |
| T9 | `tip_wipe` | 迷宫内 HP 首次低于 30% | 团灭代价的细节与行动建议 |
| T10 | `tip_recruit` | 首次打开营地招募面板 | 招募花费与「潜力」生成时定死 |
| T11 | `tip_dungeon_event` | 首次与事件格交互、事件界面弹出时 | 事件必须选一个选项：不能走开，选完也定死 |

**`firstTimeFlags` 契约**：**显示前读 → 显示后写**；「重看全部提示」入口**可整体清空**（清空后重新按 T1–T11 顺序补齐）。

#### 40.7.2 引导开关的三条语义（D364）

1. **关闭 ≠ 清空**：关闭引导**不清空** `firstTimeFlags`
2. **关闭期照常写 `firstTimeFlags`**
3. **恢复引导时只补缺的那几条**（已看过的不再弹）

#### 40.7.3 存档槽三步覆盖（D365 + D399 + D421）

- **槽位命名**：10 槽，**不允许重名**，**严格逐字节比较**（不 trim、不归一化），**自身豁免**（改名时与原名相同不算重名）
- **四步顺序（D421）**：`校验 / 替换` → `查重` → `确认` → `落盘`（不得乱序）
- **D399**：纯读取路径**不得写入**
- **`settings.guideEnabled` 默认**：D283（让玩家选）+ D286（默认开）**同时成立** ⇒ D318 定为「开局让玩家选，**默认高亮『开启』**」

#### 40.7.4 UI 文案（`U1–U3` 共 7 条）

覆盖存档槽命名 / 覆盖确认 / 设置面板等 7 条固定文案，逐字以 `tutorial-draft-v1.md` 的 U1–U3 小节为准；**改动时必须逐字引用原文**（D435 / D436）。

---

### 40.8 存档规格（`SAVE_VERSION` 5）

> 来源 `save-schema-spec-v1.md` v6.2（含勘误包），已并入 GDD §11.D。**本章重点。**

#### 40.8.1 版本闸门与槽位

| 项 | 值 | 裁决 |
|---|---|---|
| 当前 `SAVE_VERSION` | **5**（真源 `src/config/saveKeys.ts`；`GameState.ts` import 后重新导出以保持历史路径，E-11） | E-11 |
| 版本语义 | **5 —— 纯闸门**：`version !== 5` 即拒，**读档与导入两个入口都拦** | **D134** |
| 落地状态 | ✅ **已实现**：`SaveSystem.load` 与 `SaveSystem.importJson` 各有一条 `(parsed.version ?? 0) !== CURRENT_VERSION` 拒绝分支 | D134（save-G1 已关闭） |
| 迁移函数的定位 | `migrateSave()` 仍保留（v1→v4 的逐版本补齐 + 补默认值），但**它不再是 `load` 的常规路径**；`load` 先过闸门再调它 | §11.D.1 分工 |
| 槽位 | **10 槽 + 可命名 + 不允许重名**（D293 / D334 / D365 / D339） | D293 / D334 / D365 / D339 |
| 自动存档 | **独立键，不占 10 个命名槽** | D314 / D319 |
| 手动存档 | **仅营地可手动存**（迷宫内与战斗中不可）；**迷宫「存档点」元素属 D68 的事件触发式自动存档，不受此限** | D287 / D316 |

#### 40.8.2 字段清单表（17 个新增字段 + 既有关键字段）

| 字段名 | 类型 | 默认值 | 引入的裁决编号 | 迁移注意 |
|---|---|---|---|---|
| `version` | `number` | **5** | D134 | **纯闸门**。读档与导入两入口都必须拒 `≠5`；✅ **现状已实现** —— `load` 与 `importJson` 各有一条拒绝分支（save-G1 已关闭） |
| `difficulty` | `GameDifficulty` | 开局选定 | D115 | 开档后**不可改**；v4 旧档无此字段 ⇒ 需设定兜底默认值 |
| `nightmareMods` | `NightmareMod[]` | `[]` | D115 / D109 | 可叠加，**上限 `NIGHTMARE_MOD_MAX` = 4**；v4 旧档缺失 ⇒ `[]` |
| `party` | `string[]`（**现行落地**） | 开局 3 名白送角色 | D141 / D292 / D313 / D318 | ⚠️ GDD §11.D.2 定为**对象数组** `{charId, accessory, consumable}`；缺口表裁决 **15b 延后改结构**，改用 `partyEquip` 平行数组。**改结构前必须先落 save-G1 版本闸门**，否则旧档 `string[]` 会被读成对象、表现为「编队界面全空」 |
| `partyEquip` | `PartyEquipSlot[]`（与 `party` **等长平行**） | `{accessory: null, consumable: null}` | 缺口表 15b / D141 / D292 / D140 / D312 | 10 个命名出战位的双槽（饰品 + 消耗品），**挂在出战位上、换人不丢**（D292）；平行数组靠**下标对齐**，任何 `party` 增删必须同步维护 |
| `activeSlot`（`SaveSystem` 私有静态） | `number \| null` | `null` | D368 / D369（save-G5 / G6 / G19） | ✅ **已修复**：`SaveSystem.keyOrNull(slot)` 在 `slot === null` 时返回 `null`，`save` / `load` / `hasSave` / `delete` **四入口共用同一守卫**；`delete(null)` 现为**空操作**，`save(null)` 打 warn 并 `return false` |
| `stats.totalClears` | `number` | 0 | D105 / D295 | **含重复通关**；单调递增 |
| `stats.totalDefeated` | `number` | 0 | D105 / D295 | **含 Boss 与精英**；不含类型信息（这是 `eliteKills` 必须独立存在的原因） |
| `stats.totalGoldEarned` | `number` | 0 | D105 / D295 | **含卖出所得**；收入侧统一收口在 `addGold`，**只记入账不记花费** |
| `stats.eliteKills` | `number` | 0 | E-25 | **不可推导**，且**不能放 `runCounters`**（会被团灭清掉） |
| `stats.clearStreak` | `number` | 0 | E-25 | 同上；`totalClears` 单调递增、表达不了「连击被打断」 |
| `stats.totalRecruits` | `number` | **0** | **D362**（第 17 个字段） | 开局 `createStarters()` 白送的角色**不计入**；判据 = 累计**花金币**招募（重复角色计入、退货后再招也 +1） |
| `maxFloorEver` | `Record<string, number>`（**per-地牢**，键 = `dungeonId`） | `{}` | D105 / D135 / D137 / D281 / D285 | ✅ **已按 per-地牢落地**：`GameStateManager` 的「记录历史最高层」分支按 `dungeonId` 取旧值、只增不减、**永不归零**；与 `dungeonProgress[id].maxFloor`（团灭归零）**是两个量**，UI 必须分开显示 |
| `ownedAccessories` | `Record<string, number>`（键 = 饰品 id，值 = 持有件数） | `{}` | D82 / D88 | **必须与 `forge.forgedCount` 分开**（D82 有三条获取途径：锻造 / 商店 / 掉落）；装备校验依据 = 「对每个饰品 id，被 `partyEquip` 引用的次数 ≤ 这里的持有件数」 |
| `forge.forgedCount` | `number` | 0 | D82 / D88 | 只记**锻造次数**，不表达持有状态 |
| `quests` | `Record<questId, number>` | `{}` | D92 / D237 / D300 | **永久层**。主线一次性（`count ≥ 1`）；支线**可无限重复**（D237）。带 `@raw:` 前缀的键为原始计数，不参与展示 |
| `activeRun.runCounters` | `Record<string, number>` | `{}` | D92 / D237 / D300 | **整趟语义**：随下探重置、**团灭随进度清**。购买限次类键用 **`buy:` 前缀** |
| `firstTimeFlags` | `Record<string, boolean>`（**11 键**） | 全 `false` | D294 / **D422** / D364 | 原写 10 键，**D422 更正为 11**（`tip_combat_ap` … `tip_dungeon_event`）；**可整体清空**（「重看全部提示」） |
| `settings.guideEnabled` | `boolean` | `true` | D283 / D286 / D318 | **不新增字段**（复用既有 #12）；语义三条见 §40.7.2 |
| `activeRun`（整体） | `object \| null` | `null` | D68 | 迷宫内进度；`vitals` 子结构现状保留 |
| `unlocked`（`SaveSlotMeta`） | `boolean` | — | D269 | **死字段**：无人置 `true`，归档保留仅为兼容 |

**统计口径（D295）**：`totalDefeated` 含 Boss 与精英｜`totalClears` 含重复通关｜`totalGoldEarned` 含卖出所得（只记入账不记花费）。

**明确不存档（可推导 ⇒ 不存）**：战术装配（`tactics` **不存**，每局重选）、科技树已购节点的可推导状态、当前 HP / AP（战斗中态）、迷雾已探索格的缓存。

#### 40.8.3 写入时机（15 个触发点）

存档写入触发点共 **15 个**（含：进入新层、拾取、战斗结束、锻造、商店交易、招募、任务达成、手动存档、事件触发式自动存档等）。
> **第 14 号触发点已按 D287 移除**（迷宫内手动存档）。

#### 40.8.4 两套设置命名空间（共存是有意的，E-20 / D315）

| 命名空间 | 载体 | 字段 |
|---|---|---|
| **per-save** | `state.settings` | `guideEnabled`、音量（BGM / SFX）、战斗速度等**随存档走**的设置 |
| **global** | `maze_rpg_settings`（独立 localStorage 键） | 分辨率、文字缩放、高对比度等**跨存档的可访问性**设置（D100） |

> 🔴 **save-G18**：`maze_rpg_settings` **整体覆写**会静默抹掉 D100 的可访问性设置 ⇒ 必须**读-改-写**（read-modify-write），不得整体替换。

#### 40.8.5 存档槽结构

`SaveSlotMeta { version, compatible }`：`version` 记录写入时的 `SAVE_VERSION`；`compatible` 由版本闸门推导（`version === 5`）。`SAVE_SLOT_COUNT` 现状 **3 ⇒ 改 10**（**D370**，save-G9）。

---

### 40.9 UI 规格增补（11 个新系统）

> 来源 `ui-spec-additions-v1.md` v4。**⚠️ 与 §40.8 重叠部分已去重**：槽位结构、存档写入、设置命名空间一律以 §40.8 为准，本节只写 UI 表现层。

#### 40.9.0 通用约束

- **调色板**与圆角按 UI 规格 §0 固定值；**弹窗外框 1280×720 @ (320, 180)**
- **摄像机规则**：`uiCam` 独立相机；🔴 **禁止双 `uiCam`**（重复创建会导致 UI 层被绘制两次）；🔴 **`create()` 内不得读 `cam.worldView`**（此时尚未布局完成，读到的是脏值）
- **暗角（vignette）**：D322 后改为 **alpha 渐变**，不再是硬边遮罩

#### 40.9.1 十一个新系统

| # | 系统 | 关键规格 |
|---|---|---|
| 1 | **战术 HUD** | 9 条条目，`x = 1596`、`w = 300` |
| 2 | **开局流程** | 三段：① 选槽 → ② 引导（默认高亮「开启」） → ③ 难度 |
| 3 | **存档 UI** | 10 槽，`ScrollList` 承载（缺口表 2b） |
| 4 | **任务面板** | 主线 / 支线分栏，展示 `quests` 永久层计数 |
| 5 | **统计面板** | 4 张卡片；**`maxFloorEver` 与 `maxFloor` 必须分开显示**（不可混用） |
| 6 | **装备界面** | 操作对象是**出战位**（不是角色），换人不丢饰品（D292） |
| 7 | **锻造界面** | 锻造费 = 材料 + `price` × **0.6**（D167） |
| 8 | **商店** | 8 / 12 槽位；**噩梦 mod 2 使价格 ×2**（D319）；饰品每店限购 1 件（D128） |
| 9 | **营地** | 含训练场、**手动存档入口**（唯一手动存档点，D287 / D316） |
| 10 | **迷宫事件对话框** | 按 `tone` 着色（boon / risk / bane）；**D320：不披露失败率** |
| 11 | **三级暗角** | **50% / 30% / 15%** 三档 |

#### 40.9.2 追加项

- **§14.1 敌人 debuff 行**（D323）：战斗 UI 需展示敌人身上的增减益条目
- **§14.2 迷宫日志**：滚动文本日志，记录层数推进 / 事件 / 战斗结果
- **§15 设置面板**：同时呈现 §40.8.4 的两套命名空间，UI 上分组不混

#### 40.9.3 阻塞项与非阻塞缺口

- 🔴 **阻塞项**：`ui-G2` / `ui-G5` / `ui-G6`（未解决前相关界面无法落地）
- 其余缺口 `ui-G7` … `ui-G16`、`ui-N-1` … `ui-N-12` 见 §40.11 汇总表

---

### 40.10 验收矩阵

> 来源 `acceptance-matrix-v1.md`。

#### 40.10.1 8 条可自动验证的硬闸门

| 指标 | 目标带 |
|---|---|
| TTK（普通敌） | 2–4 回合 |
| TTK（精英） | 5–10 回合 |
| TTK（Boss） | **≥ 10** 回合 |
| 第 1 回合团灭（`wipeTurn1`） | **恒为 0** |
| 科技树首通点 | **76.3 / 99 = 77.1%**，落在 **75–85%**（D297：科技树总价 **99 点 / 21 节点**；草案「约 80 点」是**达成率**口径，不是总价） |
| 成长曲线 | **2.01 / 2.88 / 4.10 / 6.30**，容差 **±5%** |
| 金币 | **14 524**，容差 **±5%** |
| S9（`quest_sub_hoard`）频率 | **0.8–1.4 次/趟** |

#### 40.10.2 4 条需新增断言

1. `T-1` 陷阱致死率
2. 饰品伤害轴 **≈ +9.7%**
3. 存档版本闸门生效
4. 战术叠加不超上界

#### 40.10.3 非自动验证项

11 个 UI 系统、三级暗角、难度可感知性、「迷路是玩法」、终局 **1.52 小时**。

#### 40.10.4 验证链

`node scripts/gen-tables.mjs` → `npx tsc --noEmit` → `H:/node.js/node.exe scripts/verify.mjs`（**必须 Node 24**；托管 Node 22 会误报 1 项红）→ `scripts/smoke.mjs` / `debug.mjs`（需 dev server 在 `127.0.0.1:5173`）。

---

### 40.11 ⚠️ 未实现 / 未完成项汇总

> 🔴 **本节"状态"列的时效性（务必先读）**
>
> 下表的 ✅/❌ **来自 `gap-table-round3.md`，那是 2026-09-18 的快照**；此后又经过多轮实施（含 09-19、09-20 两次大改）。
> **除 §40.11.9 已逐条实测核实并就地改正的 5 条 `save-G` 之外，其余条目的"状态"尚未逐条对照当前代码复核** ——
> 使用前请自行 Grep 当前代码确认落地点，**不要直接据表施工**。
> （这正是本项目最贵的一类错误：**把快照当成现值** —— 已知至少 5 条 save-G 属于此列，已在 §40.11.9 改正。）

> 三份 `gap-table-round1/2/3.md` 是**同一张表的三轮快照**，本节**只写最终状态**（以 round3 为准），并标注每条的最新轮次与状态。历史已完成轮次不重述。
> `unimplemented-effects-registry.md` 的条目一并并入本节。

#### 40.11.1 🔴 未实现的 `SkillEffect` 类型（19 类中 2 类）

| 类型 | 状态 | 阻塞原因 |
|---|---|---|
| `reflect`（反伤） | ❌ **未实现** | 需要把**受击方队伍的 `attack` 透传**进 `DamageCalculator.resolveSingleHit`；本作 `BattleUnit.attack` 恒为 **0**，攻击力来自 `team.attack` ⇒ **反伤无法计算** |
| `negate`（免疫 / 无效化） | ❌ **未实现** | 设计侧尚未冻结**免疫类别维度**，且没有**逐单位免疫容器** ⇒ **免疫无法判定** |

其余 17 类**已实现**（其中 `shield` / `true_damage` / `extra_action` / `on_hp_below` / `lifesteal` 为模板级实现，另有配置使用方）。
**新增 effect 类型的落地清单（8 步）**见 `unimplemented-effects-registry.md`；**最后核对日期 2026-09-18**。

#### 40.11.2 🔴 round3 快照：整块未开工

| 模块 | 状态 |
|---|---|
| **D 战斗侧**（战术 HUD / 三级暗角 / 战斗内道具结算） | ❌ **未开工** |
| 批次 2 | ❌ 未开工 |
| 批次 3 | ❌ 未开工 |

已完成的模块（round3 §0）：**A ✅**、**E ✅**（17 字段 + `partyEquip`）、**B ✅**（44 敌人 / 13 Boss 技能 / 12 饰品 / 10 消耗品 / 15 战术 / 11 事件 / 21 科技节点）、**C ✅**（93 条技能文案）。

#### 40.11.3 未完成项总表（最新轮次 + 选定方案 + 状态）

| 编号 | 事项 | 最新轮次 | 选定方案 | 状态 |
|---|---|---|---|---|
| #1 | 任务引擎读取点 | round3 | **1a** | ❌ 未落 |
| #2 | （已定案项） | round3 | 已固定 | ✅ |
| #3 | 战斗落点读取点 | round3 | **3a** | ❌ 未落 |
| #4 | effects 结算路径 | round3 | **4a** | ❌ 未落 |
| #5 | 强化掷点 | round3 | **5a**（用户在 5a / 5b 中选 5a） | ❌ 未落 |
| #6 | 任务引擎（第二条） | round3 | **6a** | ❌ 未落 |
| #7 | 精英槽位 | round3 | **7a** | ❌ 未落 |
| #8 | 版本闸门 | round3 | **8a** | ✅ 已落（save-G1 / D134） |
| #9 | 精英槽 | round3 | **9b** | ❌ 未落 |
| #10 | 战斗内道具 AP | round3 | **10b** | ❌ 未落 |
| #11 | 引导 key 读取 | round3 | **11b**（另见 11a 与 #1 组队推进） | ❌ 未落 |
| #12 | 事件商人宿主 | round3 | **12a** | ❌ 未落 |
| #13 | 配表/文档不一致 | round3 | **13a** | ❌ 未落 |
| #14 | 配表/文档不一致 | round3 | **14a** | ❌ 未落 |
| #15 | `party` 结构 | round3 | **15a**（15b 已用于延后改对象数组） | ⚠️ 进行中 |
| #16 | 配表/文档不一致 | round3 | **16a** | ❌ 未落 |
| #17 | 配表/文档不一致 | round3 | **17a** | ❌ 未落 |
| #18 | 配表/文档不一致 | round3 | **18a** | ❌ 未落 |
| #19 | 配表/文档不一致 | round3 | **19a** | ❌ 未落 |
| #20 | 临时文件保留（环境项） | round3 | 保留 | ✅ 环境项 |
| #21 | `vite build` 约 30 分钟卡滞（环境项） | round3 | 已知 | ✅ 环境项 |
| #22 | bash 缺 `ls` / `cat` / `grep` / `head` / `tail` / `dirname` 且无管道（环境项） | round3 | 改用 Read / Grep / Glob 工具 | ✅ 环境项 |
| #23 | SendMessage 身份失败（环境项） | round3 | 已知 | ✅ 环境项 |
| save-G1 | `version !== 5` 纯闸门（读档 + 导入都拦） | round3 | D134 | ✅ **已实现**（主理人实测：`SaveSystem.load` / `importJson` 各有 D134 拒绝分支） |
| save-G5 | `save()` 在 `activeSlot === null` 时回落 01 槽 | round3 | D368 | ✅ **已实现**（主理人实测：未选槽时 warn 并 `return false`，不回落 01 槽） |
| save-G6 | `delete()` / `load()` / `hasSave()` 同一 ternary ⇒ `delete(null)` 删掉 01 槽 | round3 | D369 | ✅ **已实现**（主理人实测：`keyOrNull` 守卫，`delete(null)` 为空操作） |
| save-G9 | `SAVE_SLOT_COUNT` 3 ⇒ 10 | round3 | D370 | ✅ **已落**（主理人实测：`SAVE_SLOT_COUNT = 10`） |
| save-G18 | `maze_rpg_settings` 整体覆写抹掉可访问性设置 | round3 | 读-改-写 | ✅ **已修**（主理人实测：`graphics.ts` 用 `{ ...prev, resolution }` 读-改-写） |
| save-G19 | `activeSlot` 修复 | round3 | D368 | ❌ 未落（须在 #15 之后） |
| ui-G2 / ui-G5 / ui-G6 | UI 阻塞三件套 | round3 | — | 🔴 **阻塞中** |
| ui-G7 … ui-G16、ui-N-1 … ui-N-12 | UI 其余缺口 | round3 | — | ❌ 未落 |
| — | `reflect` / `negate` 两类 `SkillEffect` | registry | 见 §40.11.1 | ❌ **未实现** |

#### 40.11.4 ⚠️ 待澄清（GDD 中查不到裁决）

| # | 事项 | 现状 |
|---|---|---|
| 1 | **`maxFloorEver` 是否 per-地牢** | GDD §11.D.2 依 D281 / D285 定为 **per-地牢**，`src/types/save.ts` 落地为**单值** ⇒ 需澄清 |
| 2 | **`party` 容器形态** | GDD §11.D.2 定为对象数组 `{charId, accessory, consumable}`（D313 / D318）；缺口表 **15b** 裁决延后，现状为 `string[]` + `partyEquip` 平行数组 ⇒ 需澄清何时切换 |
| 3 | **`quest_sub_hoard`（S9）阈值 1 700 需重算** | D350 定 1 700、D367 宣布原 2 500 失效；但可行性依赖 `dungeons.treasureCount` 与 `MazeGenerator.rollTreasurePayload`，配表未复核 |
| 4 | **`quest_sub_forge` / `quest_sub_forge5` 是否允许重造** | 任务文案写「允许重造」，与 `forge.forgedCount` 的计数口径需确认 |
| 5 | **药水价格口径冲突** | D171 定 260，`items.json` 现值为 90 ⇒ 需澄清 |
| 6 | **`quest_sub_elite` 的精英密度** | 依赖 D118 精英出现率与 D14 / D257 `enemyDensity`，配表未复核 |
| 7 | **`event_shrine` 随机池分层** | `events-draft-v1.md` **唯一遗留开放项**，未定稿 |
| 8 | **Q13 深渊吞噬的「不致死保护」** | `content-draft-v1.md` §④ 的 Q13 **仍为开放项**：8% 当前生命是否可能致死，未裁 |

---

### 40.12 落地顺序与交接要点

> 来源 `handoff-to-implementer-v1.md`。「下一步只做一件事」= **批次 1 的 A 组（11 个代码缺口）**。

#### 40.12.1 必须先修的三项

1. `save()` 在 `activeSlot === null` 时的回落（D368 / save-G5）
2. `delete()` / `load()` / `hasSave()` 中**同一个 ternary**（D369 / save-G6）
3. **buff / debuff 的落点**（`applySupportEffects` 恒为施法者、`applyEnhancedEffects` 恒为被击中敌人）

#### 40.12.2 四条顺序约束（顺序错了会造出更坏的后果）

1. **修两个落点（①②）→ 再修 effects 结算路径（③）→ 再修强化（④）→ 最后才落 D233 / D249 的数值改动**（否则会往配表写入「自伤技能」）
2. **版本闸门（save-G1）必须先于 `party` 改对象数组（#15）**
3. **`party` 结构变更（#15）必须先于 `activeSlot` 修复（save-G19 / D368）**
4. **`clear_tactic` 走独立 hook，不要塞进 `effects`**

#### 40.12.3 必须同批次落地的一对

- **D258 + D299**（`item_core` ×4 与魔核来源）：分开落会造成魔核来源为 0
- **D350 + D367**（S9 阈值 1 700 与「原 2 500 已失效」的声明）

#### 40.12.4 五个最容易踩的坑

1. **双 `uiCam`**（重复创建导致 UI 被绘制两次）
2. **`create()` 内读 `cam.worldView`**（读到布局完成前的脏值）
3. **临时升级是常量 +1**（不是按层递增）
4. **敌人只读 `attack` / `defense`**（不要给它加不存在的属性维度）
5. 🔴 **永远不要删除 `design/drafts/` 下的任何文件**（它们是推导过程的归档）

#### 40.11.9 编辑附注（主理人实测核实 · 2026-09-22）

> 本节的返工被 **429 限流中断**，只更新了 §40.8.1 的落地状态，导致本节一度残留旧快照。
> 以下 5 条已由主理人**对照当前代码实测核实**并就地改正（证据写在上表每行内）：
>
> - **save-G1 版本闸门** —— ✅ 已实现。版本常量在 src/config/saveKeys.ts（值 5）；
>   SaveSystem.load 与 SaveSystem.importJson **两个入口各有一条** `(parsed.version ?? 0) !== CURRENT_VERSION`
>   的拒绝分支 —— D134 明确要求「导入路径也必须拦」。
> - **save-G5 save() 未选槽的回落** —— ✅ 已实现。未选槽时打 warn 并 `return false`，
>   注释原话：「已拒绝写盘（防止用空档覆盖第 01 槽）」。
> - **save-G6 delete(null) 删掉第 01 槽** —— ✅ 已实现。keyOrNull() 在 slot === null 时返回 null，
>   save / load / hasSave / delete **四入口共用同一守卫**，delete(null) 现为**空操作**（注释标注 D369）。
> - **save-G9 槽位数** —— ✅ 已落。src/config/saveKeys.ts 里 SAVE_SLOT_COUNT = 10。
> - **save-G18 settings 整体覆写** —— ✅ 已修。src/config/graphics.ts 的写入是 `{ ...prev, resolution: currentId }`，
>   即**读-改-写**，不会抹掉可访问性设置。
>
> ⚠️ **方法教训（写给后来人）**：草案里的「缺口表 / 未实现清单」是**写草案那一刻的快照**，
> 实施之后不会回溯更新。**判定「某缺口是否还在」必须去当前代码里找落地点**，不能照抄缺口表。

---

## 50. 美术 · UI/UX · 公式 · 立绘资产管线

> 本章来源：`docs/art/ui-style-spec.md`、`design/ux/battle-ap-ux-spec.md`、`docs/formulas.md`、`docs/portrait-swap-2026-09-22.md`、`_portrait-swap/*.md`、`image/_work/design-matrix.md`（原件已归档）
>
> 本章的**冲突处置口径**：凡规格书与 `design/gdd.md` 冲突，一律写 GDD 版本，并标注「（草案曾提出 X，已被 GDD 否决 / 未落地）」；GDD 未覆盖的真冲突标 ⚠️ 待澄清（汇总见 §50.13）。

---

### 50.1 视觉铁律与色板（`Palette`）

#### 50.1.1 三条铁律

1. **全平色、零渐变、零发光。** 禁止 `fillGradientStyle`、禁止阴影 / 模糊 / 外发光；层次感只靠「明度阶梯 + 1–2px 细线」。
2. **直角为体、切角为饰。** 所有圆角（`fillRoundedRect` / `strokeRoundedRect`）废除，改用斜切角多边形。
3. **强调色克制。** 亮黄 `0xFFD400` 是唯一主强调色，单屏出现面积 ≤5%，只给「当前可交互 / 当前选中 / 关键数值」；状态色（红 / 绿 / 青）各就其位不混用。

#### 50.1.2 `Palette` 全表（`src/config/constants.ts` 现值）

| 字段 | 值 | 用途 |
|---|---|---|
| `bg` | `0x0B0E14` | 全局底色（近黑微蓝） |
| `panel` | `0x14181F` | 面板底（枪铁灰） |
| `panelLight` | `0x1C212B` | 面板亮层 / 按钮常态底 / 列表行底 |
| `border` | `0x3A4150` | 面板与控件描边（中性灰） |
| `text` | `0xEDEFF2` | 主文本（近白冷灰） |
| `textDim` | `0x8A919D` | 弱文本 / 说明文字 |
| **`accent`** | **`0xFFD400`** | **唯一主强调色**：选中态、主行动、关键角标 |
| `gold` | `0xF0A824` | 金币 / 升耗提示（比 accent 深一档，避免混淆） |
| `tech` | `0x2ED6C8` | 科技点 / 辅助信息（次级强调） |
| `danger` | `0xE5483F` | 危险 / 敌人 / 删除 |
| `hp` | `0x3FD08C` | HP（偏青绿，单色，非渐变端点） |
| `hpLow` / `hpMid` / `hpHigh` | `0xE5483F` / `0xFF8C1A` / `0xFFD400` | 血条渐变三锚点（红→橙→黄） |
| `mp` | `0x3FA9F5` | MP（与 accent 拉开色相距离） |
| `exp` | `0xC9A54C` | 经验（哑光琥珀，刻意比 gold 暗） |
| `floor` / `floorAlt` | `0x1A1F28` / `0x151A21` | **UI 面板深色底板**（不是迷宫地砖，见 §50.4.1） |
| `wall` | `0x2E3540` | 🪦 **全项目零使用**（仅存在于定义处），已被 D36 标废弃 |
| `fogUnknown` / `fogExplored` | `0x04060A` / `0x7C8492` | 迷雾：未探索 / 已探索 |
| `mazeRoad` | `0xFFD700` | 迷宫通道（黄） |
| `mazeWall` | `0x00008B` | 迷宫墙体（深蓝） |
| `mazeVoid` | `0x000000` | 虚空 / 相机背景（纯黑） |

> 🔴 `floor` / `floorAlt` **不要当成迷宫地砖去改**：它们是各 UI 面板的深色底板（战斗、背包、队伍、招募、科技面板共 6 处），改了会把所有面板一起染掉。
> 迷宫色名的**唯一真源**是 `mazeRoad` / `mazeWall` / `mazeVoid`（D36）。

#### 50.1.3 `Widgets.ts` 内部常量

| 常量 | 值 |
|---|---|
| `DISABLED_FILL` | `0x14171D` |
| `PRESSED_FILL` | `0x11141A` |
| `DISABLED_BORDER` | `0x272C35` |
| 禁用文本色（两处） | `0x565D68` |
| `drawBar` 默认 `track` | `0x0C0F15` |
| `Button` 默认 `hoverFill`（含各面板写死的 hover/选中色） | `0x262C37` |
| 主行动按钮文字色 | `0x111417` |

#### 50.1.4 稀有度用色（`rarityColor()`）

| 稀有度 | 值 | 说明 |
|---|---|---|
| 5★ | `0xFFAE1A` | 橙金，唯一高亮稀有度 |
| 4★ | `0x9E7FD8` | 哑光紫 |
| 3★ | `0x4A9FE8` | 工业蓝 |
| 2★ | `0x4CC98A` | 青绿 |
| 1★ | `0x9AA3AF` | 中性灰 |

`PortraitCard.ts` 同步：`SILHOUETTE_TINT = 0x0A0C11`；`RARITY_TINT` = `5:0x2E2410 / 4:0x221A30 / 3:0x14202F / 2:0x12241A / 1:0x1B1F26`；剪影占位文字色 `0x454C58`。

#### 50.1.5 对比度（可访问性自证）

| 组合 | 对比度 |
|---|---|
| `text` / `bg` | 14.8:1 |
| `textDim` / `bg` | 5.1:1 |
| `accent` 黄底 + `0x111417` 黑字 | 12.4:1 |

三档均过 WCAG AA。`accent` 黄在白底上不足 ⇒ **黄底一律配 `0x111417` 黑字**。

---

### 50.2 控件造型语言

#### 50.2.1 切角几何（全项目唯一标准）

标准切角 = **左上 + 右下对角切**，8→6 顶点多边形，Phaser 侧用 `fillPoints / strokePoints`（`closeShape: true`）：

```
矩形 (x, y, w, h)，切角深度 c，顶点顺时针：
(x+c, y) → (x+w, y) → (x+w, y+h-c) → (x+w-c, y+h) → (x, y+h) → (x, y+c) → 闭合
```

**切角深度分档（不允许出现其他值）：**

| 档位 | c | 用于 |
|---|---|---|
| 大面板 | 14px | 全屏弹窗、营地各面板底板（≥600px 宽） |
| 中卡片 | 10px | 战斗角色卡、立绘框 |
| 小控件 | 8px | 列表行、小面板、输入框 |
| 按钮 | 6px | 所有按钮（只切右下一角） |
| 微件 | 4px | 进度条端头、标签 chip |

`drawPanel` 的 `radius` 参数废弃，改为 `chamfer?: number`，默认 **8**；面板 alpha 从 0.96 提到 **1.0**（扁平风格不透底）。

#### 50.2.2 面板规格

| 项 | 值 |
|---|---|
| 填充 | `Palette.panel` `0x14181F`，alpha 1.0 |
| 边框 | 2px `Palette.border` `0x3A4150`，alpha 1.0 |
| 切角 | 按 §50.2.1 分档 |
| 角标装饰 | 主面板**左上角**加 accent L 形角标：横竖各 18px、粗 3px、距面板边 6px；次级面板不加 |
| 标题装饰 | 标题左侧 accent 竖条 **4×20**（原 4×22），距标题 10px |
| 内边距 | 20px |
| 层级分隔 | 只用 1px `border` 色横线 + 12px 留白；必须分层时用 `panelLight` |

#### 50.2.3 按钮三态

按钮形状：**矩形 + 右下角切 6px**（5 顶点）。✕ 关闭钮保留圆形。

**常规按钮：**

| 状态 | 填充 | 边框 | 文本 | 附加 |
|---|---|---|---|---|
| 常态 | `panelLight` `0x1C212B` | 2px `border` `0x3A4150` | `0xEDEFF2` | — |
| hover | `0x262C37` | 2px **`accent` `0xFFD400`** | `0xFFFFFF` | 左侧内缘 3×(h−16)px accent 竖条（距左边 4px） |
| 按下 | `0x11141A` | 2px `accent` | `0xFFD400` | 仅变色，不做位移缩放 |
| 禁用 | `0x14171D` | 2px `0x272C35` | `0x565D68` | 无竖条、不响应 hover |

**主行动按钮**（一屏唯一主 CTA，靠 `fill` 参数识别）：

| 状态 | 填充 | 边框 | 文本 |
|---|---|---|---|
| 常态 | `accent` `0xFFD400` | 无 | `0x111417` bold |
| hover | `0xFFE03A` | 无 | `0x111417` |
| 按下 | `0xD9B400` | 无 | `0x111417` |
| 禁用 | `0x272C35` | 无 | `0x565D68` |

图标按钮（`iconMode`）：✕ 用 22px 字形 `textDim` 色，hover 变 `danger`；＋/− hover 变 `accent`；不画底板圆，只保留圆形热区。

#### 50.2.4 进度条（`drawBar`）

| 项 | 值 |
|---|---|
| 标准高度 | 8px（迷你条 6px，如卡片上的 HP） |
| 槽 | `0x0C0F15` 平色，**直角无圆角**、无描边 |
| 前景 | 对应语义色平色，无渐变 |
| 端头 | 前景右端切 4px 斜角：顶点 `(x,y) (x+fw,y) (x+fw-4,y+h) (x,y+h)` |
| 满值标记 | 槽右端 1×h px `border` 色刻度线 |
| 文字 | 条上方 2px，12px，主色 `text`，不压在条内 |

#### 50.2.5 字体层级

字体族：`"Segoe UI", "Microsoft YaHei", system-ui, sans-serif`（零资源依赖）；英文/数字混排处前置 `Bahnschrift, Consolas`。

| 层级 | 字号 | 字重 | 颜色 | 用途 |
|---|---|---|---|---|
| 场景大标题 | 28px | bold | `text` | `makeHeader` 标题，字符间距 +1 |
| 面板标题 | 20px | bold | `text` | 弹窗标题 |
| 小节标题 | 14px | bold | `accent` | 面板内「简介 / 六维属性」等分节 |
| 正文 | 15px | regular | `text` | 按钮、列表主行 |
| 辅助 | 13px | regular | `textDim` | 元信息、说明 |
| 微注 | 11–12px | regular | `textDim` | 页脚提示、冷却秒数 |
| 关键数值 | 16px | bold | `accent` / 语义色 | 伤害数、已解锁计数 |

原则：**标题更大更重、正文更小更灰**。

---

### 50.3 战斗界面布局与 AP / 卡牌 UX

> 逻辑分辨率 1920×1080 基准；所有坐标按 `GAME_WIDTH / GAME_HEIGHT` 换算，**禁止在逻辑里写死像素常量**。

#### 50.3.1 区域划分（自上而下）

```
┌──────────────────────────────────────────────────────────────┐
│ A 顶部信息带 (y 0..56)                                        │
│   左: 第 N 回合 / combo 数      右: 攻击·生命·连击/暴击/强化   │
├──────────────────────────────────────────────────────────────┤
│ B 战场区 (y 56..700)                                          │
│   敌方单位一排 (y≈196) · 左侧竖向队伍 HP/MP 池条 (x 24/50)    │
│   我方不再画方块小人，由底部卡牌代表                           │
├──────────────────────────────────────────────────────────────┤
│ C 底部指令台 (y 700..1064, 整块深色面板, 上边线高亮)           │
│  ┌─────────┬──────────────────────────────┬───────────────┐  │
│  │ C1 日志 │ C2 角色卡牌 ×6 (核心交互区)   │ C3 AP + 指令  │  │
│  └─────────┴──────────────────────────────┴───────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

| 区域 | 位置（1920 基准） | 内容 |
|---|---|---|
| A 顶部信息带 | 通栏 y 0..56 | 左：回合数 / combo；右：队伍攻击、最大生命、连击/暴击/强化概率、自动战斗标记 |
| B 战场区 | y 56..700 | 敌人单位（圆形占位 + 血条 + 名字）；左侧共享 HP/MP 竖条 |
| C1 日志区 | x 24..420, y 716..900 | 最近 **4 行**日志（13px）；下方一行连击/暴击/强化小字摘要 |
| C2 卡牌区 | x 436..1484, y 700..1064 | 6 个卡牌栏位，单卡 **168×340**，间距 12（6×168+5×12=1068，居中） |
| C3 指令区 | x 1500..1896, y 700..1064 | AP pips + 通用指令按钮 |

> 这里的 `y 0..56` 是**战斗**顶部信息带（`BattleScene` 的 `TOP_BAND_H = 56`）。迷宫的顶部信息带是另一个常量 `TOPBAR_H = 64`（见 §50.7.1）。两者**不是冲突，是不同的两个场景**，不要互相套用。

#### 50.3.2 战斗布局现值（`BattleScene` 常量，1920×1080 基准、纵向锚底）

规格书只给了布局图，下面这组常量是**当前实现的真值**，改战斗界面时以它为准：

| 常量 | 值 | 说明 |
|---|---|---|
| `TOP_BAND_H` | 56 | 顶部信息带（= 布局图的 A 区） |
| `CONSOLE_H` | 424 | 指令台高度（卡牌加高到 400 后同步 +60） |
| `CONSOLE_BOTTOM` | 16 | 指令台距屏幕底边 |
| `CARD_W` / `CARD_H` / `CARD_GAP` / `CARD_SLOTS` | 168 / **400** / 12 / 6 | 卡牌（D24 定为 340，**未落地**，见 §50.15-4） |
| `CARD_ROW_W` | `6×168 + 5×12 = 1068` | 卡牌行总宽，居中 |
| `SKILL_BAR_X` / `_W` / `_H` / `_GAP` | 6 / **156** / **46** / 8 | 技能横条（D26 的 156×46 **已落地**） |
| `SKILL_AREA_Y` | 282 | 技能栏区顶边（卡内绝对 y，名字带下方留给 buff 行） |
| `BUST_H` | 212 | 卡片半身像固定高度 |
| `CHAMFER_PANEL` / `CHAMFER_CARD` | 14 / 10 | 切角深度（对应 §50.2.1 的「大面板 / 中卡片」档） |
| `SPENT_FILL` | `0x272C35` | 已消耗 / 禁用填充（Palette 无对应槽位） |
| `DARK_STRIP` | `0x0B0E14` | 卡底信息带条 |
| `BUFF_FILL` / `BUFF_EDGE` | `0x2ED6C8` / `0x137F76` | 增益底与描边 |
| `DEBUFF_FILL` / `DEBUFF_EDGE` | `0xE5483F` / `0x8F231C` | 减益底与描边 |

> 卡内 buff/debuff 图标行（`CARD_BUFF_Y = 254`）**已废弃**：我方 buff 改挂队伍级 HUD（`allyTacticRoot`）；`buildBuffRow` 仍被敌人头顶那一行复用。敌方行动者角标用 `danger` 红替代 `accent` 黄。

#### 50.3.3 角色卡信息层级（单卡：规格 168×340 / 代码现值 168×400）

1. **半身像区** 168×220（占 65%）：bust 立绘居中裁剪；三态 = 未行动 / 已行动 / 倒下（倒下：灰度 + 暗角 + 「倒下」斜标签）。
2. **当前行动者指示**：见 §50.6 的 D30 裁决（**已废弃常驻抬升三件套**）。
3. **名字行** 高 26：名字 + Lv，居中，13px bold。
4. **技能栏**（底部）：见 §50.6 的 D26 裁决 —— **156×46 横向条**，左技能名 + 右动态 AP 消耗 + 左 3px 语义色条。

> 草案曾提出「36×36 图标格 + 冷却扇形遮罩 + MP 角标 / 44×44 图标格 / 96×128 卡片」，均已被 GDD 否决或废弃，见 §50.6。

#### 50.3.4 AP 数值规则（唯一口径）

| 项 | 值 | 说明 |
|---|---|---|
| 初始 AP | **3** | 每场战斗第 1 回合即为 3 |
| 每回合回复 | **+2** | 第 2 回合起，在**回合开始时**回复（与「—— 第 N 回合 ——」日志同位置） |
| 基础上限 | **8** | 溢出直接截断，不结转、不存储；研究「战术扩容」后 = 10 |
| 单次消耗 | 技能 `apCost` 字段，**默认 1** | 防御 / 道具 / 逃跑 / 自动战斗 = 0 |
| 归属 | **队伍共享池** | 存于 `BattleState.ap / apCap / apRegen / apOddBonus` |

回合回复公式：

```
gain = apRegen + (apOddBonus > 0 且 round 为奇数 ? apOddBonus : 0)
ap   = min(apCap, ap + gain)
```

- **回复时机定为「回合开始」而非回合结束**：避免首轮就多 2 点破坏开局节奏，也与冷却/增益递减同一结算点。
- 判定读的是**递增之后**的 `round`（即回到第 N 回合按 N 的奇偶性）。
- 敌人不消耗 AP；撤退 / 超时 / 战斗结束不清算 AP。

**AP 数值口径（默认，无科技）：**

| 回合 | 回合开始时 AP |
|---|---|
| 1（开局） | 3 |
| 2 | `min(8, 3 − 消耗 + 2)` |
| … | 每回合 +2，封顶 8（研究「战术扩容」后封顶 10） |

**技能动态 AP 消耗（升耗）：**

```
permanent 技能：cost = (skill.apCost ?? 1) + permStack[skill.id]     # 永续叠加，与 temp 互斥
常规技能    ：cost = (skill.apCost ?? 1) + (usedLastRound.has(id) ? 1 : 0)
```

- 两种升耗**只取其一、不相加**（相加会双重计费）。
- `skills.json` 当前**无任何技能声明 `escalation`** ⇒ 全部走「常规 +1」路径，`permanent` 分支空转。
- 🪦 旧字段 `Skill.cooldown` 已废弃，不再被任何逻辑读取。

算例：`apCost = 1` 的常规技能，首回合花 1；同一技能下回合再花 `1 + 1 = 2`；再下回合仍花 2（`usedLastRound` 只记是/否，不累加）。

#### 50.3.5 AP pips（D25 裁决后口径）

- **菱形 pips**：**10 个槽位，每枚 20×20 旋转 45° 菱形，两排 5+5**（容纳科技扩容后的 10 点）。
- 已填充：`accent` 高亮；空槽：暗色描边；**未解锁的第 9/10 槽**：锁形图标 + 虚线描边（未研究 `tech_ap_cap` 时）。
- pips 右侧大号数字 `3 / 8`。
- 回复表现：回合开始时 pips 逐个亮起，**间隔 80ms** + 浮动文字「+2 AP」（科技生效时追加「+1 奇袭节拍」金色字）；被上限截断时改写「+2 AP（已达上限）」。
- 位置：**C3 指令列顶部**（不是技能栏上方）。

> 草案曾提出「斜切平行四边形 14×5px、一行 8 个、超出合并 ×N」，**已被 GDD 否决（D25）**；代码现值形状仍是平行四边形，需改。

#### 50.3.6 通用指令（C3 中下）

竖排 4 个按钮（宽 180，高 44，间距 10）。**现行 AP 成本以本表为准**，规格书 §1.4 的「均不耗 AP」已过时（见下方警示与 §50.14-8）：

| 按钮 | 现行行为（代码现值） | AP |
|---|---|---|
| 防御 | 当前行动者进入防御并结束其行动 | **1 AP**（D104） |
| 道具 | 弹出道具面板 | **占该角色本回合出手 + 1 AP**（D140） |
| 逃跑 | 需持有烟丸 `item_smoke`，**100% 脱战**；无烟丸则该按钮不可用 | **0 AP**（成本是道具本身，D289） |
| 自动战斗 | 开关切换，开启时常亮 | 0 AP（见 §50.3.8） |

> 🔴 `battle-ap-ux-spec.md` §1.4 写的「**均不耗 AP、不占用行动**」**已过时，勿再照它办事**：D104 之后**防御也耗 1 AP**；D140 之后**战斗内用道具占该角色本回合出手 + 1 AP**；只有逃跑（烟丸制，D289）在 AP 侧是 0 成本。
> `BattleScene` 头部注释的原话：⚠️ **旧注释"均不耗 AP"是错的，别再照着写** …… ⇒ **每个通用指令的启用态都必须独立判断**（`refreshCommandButtons()`）。
> BOSS 战禁用逃跑/烟丸见 §50.15-6（D50 / D224，缺 `isBoss` 标记未落地）。

按钮下方：回合数小字「第 N 回合 / 上限 X」。

#### 50.3.6 释放技能链路与禁用态文案

```
[我方角色进入行动]
  → 玩家点击该卡上的技能条
    ├─ target = self / ally_all / enemy_all → 直接结算
    └─ target = enemy(单体) → 进入选敌模式:
         · 底部浮出提示条「选择目标 · 右键/Esc 取消」
         · 存活敌人出现可点击热区 + 呼吸描边
         · 点击敌人 → 结算;  右键/Esc/点击空白 → 返回指令态（不耗 AP）
[结算] → 扣 AP → 扣 MP → 进冷却 → 行动结束，下一人
```

- 一次行动**只能放 1 个技能**（放完即换人）；AP 是全局池，限制「同一回合多人连放」而非「一人连放」。
- 选敌模式保留「自动选敌」（选血量最少者）按钮在提示条右侧。

**点击不可用技能时**：图标抖动 **120ms** + 底部提示条显示原因（取第一个命中）：

| 顺序 | 原因 | 文案 |
|---|---|---|
| 1 | 非当前行动者 | 「现在轮到 {行动者名} 行动」 |
| 2 | 冷却中 | 「{技能名} 冷却中，还需 {n} 回合」 |
| 3 | MP 不足 | 「MP 不足：需要 {cost}，当前 {mp}」 |
| 4 | AP 不足 | 「行动力不足：需要 {apCost} 点 AP，当前 {ap} 点」 |

**禁用态的视觉必须常驻可见**，不依赖点击（灰显 / 红字 / 冷却数字 / 斜线）。禁用提示规范见 D37（§50.6）。

#### 50.3.7 自动战斗 AI

取消普攻后，`chooseAllyAutoAction` 的兜底改为：

1. 优先放可用技能 —— `availableSkills` 需增加 `s.apCost ?? 1 <= state.ap` 过滤；
2. 无可用技能（AP / MP / 冷却全锁）→ **防御**（不再普攻）。

`availableSkills` 的 AP 过滤**对敌人侧不生效**（敌人不看 AP）。

#### 50.3.8 AP 相关科技（`tech.json`）

| id | 名称 | 描述 | 价格 | 前置 | order | effect |
|---|---|---|---|---|---|---|
| `tech_ap_cap` | 战术扩容 | 行动力上限由 8 提升至 10 | 10 | — | 35 | `{ type: 'ap_cap_bonus', value: 2 }` |
| `tech_ap_odd` | 奇袭节拍 | 奇数回合开始时额外获得 1 点行动力 | 14 | `tech_ap_cap` | 45 | `{ type: 'ap_regen_odd', value: 1 }` |

定价依据：功能型科技「队伍扩编 I」5 点、属性科技 6–7 点、「远征装备」16 点；AP 上限抬升每场战斗爆发天花板，定 10 点；奇数 +1 全局期望约 +0.5 AP/回合，定为进阶 14 点并挂前置，形成「AP 支线」。落地方式：参照 `techStatBonuses()` 新增 `techApBonuses()`，在 `CombatSystem` 构造时写入 `apCap / apOddBonus`。

平衡红线：全部技能 1 AP 时「每回合必放最高倍率技」可能成为唯一解 —— 靠 MP 池与冷却对冲；`apCost` 字段必须预留把高倍率技调到 2–3 AP 的调参空间。

---

### 50.4 场景画面（迷宫 / 营地）

#### 50.4.1 迷宫画面扁平化重绘

现状：`DungeonScene.redrawMaze()`，`TILE = 34`，地砖棋盘格 + 墙体顶部 4px 亮条 + 圆角玩家块。

| 元素 | 规格 |
|---|---|
| 地砖 | `mazeRoad` 平色棋盘格，格间留 1px 底色缝（`fillRect(px, py, TILE-1, TILE-1)`） |
| 墙 | `mazeWall` 平色；**去掉顶部 4px 亮条**，改内缩 3px 画 1px `border` 色内框线（工业面板感） |
| 玩家标记 | 删光晕圆；切角方块（内缩 6px，c=4，填 `accent`）+ 中心 4×4 `0x111417` 黑点；朝向用黑点上 2px 宽短线 |
| 宝箱 | 直角箱体 2px 描边**不填充**（线框风），盖线 1 条 |
| 敌人 | 菱形（旋转 45° 方块）`danger` 2px 线框；精英填实 |
| 陷阱 | 三角，2px `danger` 线框不填充 |
| 事件 / 存档 / 终点 | 统一 2px 线框语言：事件 = `tech` 方框内一点；存档 = `hp` 十字；终点 = `accent` 双竖线门形 |
| 迷雾 | 未探索 `fogUnknown` 全盖；已探索 alpha 降到 **0.30** |

原则：元素一律「线框图标化」，填充只留给玩家和精英敌。

> ⚠️ `ui-style-spec.md` 原用 `floor / floorAlt / wall` 描述迷宫，**已被 GDD D36 否决**：改为对齐代码的 `mazeRoad / mazeWall / mazeVoid`，`wall` 标废弃（`floor / floorAlt` 保留为面板底板色）。

#### 50.4.2 营地背景（前哨基地停机坪）

1. **底色**：`bg` 平色全铺，删渐变、删星点。
2. **远景几何**：2–3 个大尺寸斜切多边形色块（`panel` 与 `panelLight` 交替，alpha 1.0），从左右边缘斜插入画面下 1/3；边缘 1px `border` 勾线。
3. **网格地平线**：中下部 1px `border` 横线（y ≈ 62% 屏高），其下间距 48px 的竖向透视线 5–7 条，alpha 0.12。
4. **全息信标（替换篝火）**：底座梯形（上宽 40 / 下宽 64 / 高 18，`panelLight` + `border` 描边）；上方悬浮 18×18 菱形 `tech` 2px 线框，tween 上下浮动 4px（**1800ms `Sine.easeInOut` yoyo**）；菱形外一圈 1px `tech` alpha 0.25 圆环（静态）。替代原 `fireGlow` 补间，动效对象数不变。
5. **顶部装饰**：标题栏下方 4px 高 accent 斜纹警示带（黄黑斜纹，间距 12px，黄纹宽 6px，`accent` + `bg` 交替，长 120px 两段置于左右上角）—— 只此一处用警示纹。
6. 帐篷等写实元素全部删除。

#### 50.4.3 营地 UX 问题清单（优先级顺序）

1. **主操作无层级**：「出发探索」拆出按钮列，做成右下角独立主按钮（420×72 + 高亮描边 + ▶），其余 8 个改 2 列网格小按钮。
2. **资源可见性差**：顶部改为常驻资源栏（图标 + 数字的胶囊 chip，右对齐），数值变化时跳动 + 变色反馈。
3. **备战栏不可交互**：改用 `ScrollList` 全量展示，每行右侧加「编入」快捷按钮（队伍未满时）。
4. **招募未解锁态误导**：按钮置灰 + 🔒 前缀 + 标签直写「招募队友（需科技：招募许可 I）」。
5. **左栏纯文本堆砌**：成员行左端加 40×40 立绘头像，六维缩为「合计 N」一行。
6. **「设置」定位混乱**：营地移除设置按钮，只保留主菜单设置。
7. **就绪提示埋在信息栏**：无出战角色时「出发探索」置灰 + 按钮上方常驻红色警示行。
8. **键盘导航缺失**：统一 Esc 返回大地图，数字键 1–9 映射功能按钮。

---

### 50.5 图鉴与未解锁剪影（已被 GDD 否决，仅存档）

> 🔴 **GDD D106：图鉴 `CodexPanel` 不恢复**（维持现状，不投入修复或重做）；**D117：不做资料入口**（不建图鉴、不并入面板），信息只靠战斗内数值条与技能描述临时查看。
>
> 因此以下两份规格书中的图鉴内容**均已作废**，保留在此仅供溯源，**不得据此施工**：
> - `ui-style-spec.md` §4「图鉴未解锁剪影方案（3 选 1）」：方案 A 纯黑剪影 + 扫描线（推荐）/ 方案 B 静态噪点 / 方案 C 错位轮廓光。
> - `battle-ap-ux-spec.md` §5「图鉴 UX 问题清单」8 条 + §6「未解锁剪影新表现方案」（A 信号未接入 / B 蓝图线框 / C 故障闪烁）。
>
> 相关数值（存档用）：扫描线动效 2400ms `Linear` yoyo，条目错开 index×300ms；列表缩略图 72×96 只留静态底纹，详情栏大立绘约 224×200 才开动效；剪影 `setTint(0x0A0C11)` + alpha 0.92，扫描底纹每 4px 一条 1px `accent` alpha 0.06 横线。

---

### 50.6 规格冲突与 GDD 裁决（UI 侧，全部已决）

| # | 冲突项 | 双方 | **GDD 裁决** |
|---|---|---|---|
| 1 | **角色卡尺寸** | `ui-style-spec.md` 96×128（间距 10、距底 16） vs `battle-ap-ux-spec.md` 168×340（间距 12） vs 代码 168×400 | **D24：168×340**（BUX 值）。96×128 **标注废弃**；代码需从 168×400 回调，并同步调 `CONSOLE_H` 与卡内元素坐标（半身像、名字带、buff 行、技能条） |
| 2 | **AP pip 形态** | 规格书 A：斜切平行四边形 14×5、一行 8 个、超出合并「×N」 vs 规格书 B：菱形 20×20、两排 5+5 共 10 槽 | **D25：菱形 20×20，两排 5+5 共 10 槽**；未解锁第 9/10 槽画锁形 + 虚线描边。⚠️ 代码现值仍是平行四边形，**需改** |
| 3 | **技能栏形态** | 规格书 A：36×36 图标格 + 冷却扇形遮罩 + MP 角标 vs 规格书 B：44×44 图标格、横排最多 4 个 vs 代码：156×46 横向条 | **D26：156×46 横向条**（代码现值）：左技能名 + 右动态 AP 消耗 + 左 3px 语义色条，**无图标格、无扇形、无 MP 角标**。36×36 + 冷却扇形标废弃（**冷却已废除**） |
| 4 | **卡牌抬升语义** | 三件套：常驻抬升 8px + 切角描边 2px + 行动角标 16×16（表示"当前行动者"）；时长 120ms vs 200ms | **D27 + D30**：**只保留「已出手」瞬时反馈** —— 出手时抬升 **120ms `Quad.easeOut`** 后立即回落（回落 150ms），**不再表示"轮到谁"**。原三件套**整段废弃**（阶段制下 `currentActor()` 在我方阶段恒返回 null，没有依附对象） |
| 5 | **迷宫色名** | 规格书用 `floor / floorAlt / wall` vs 代码用 `mazeRoad / mazeWall / mazeVoid`（`wall` 零使用） | **D36**：改规格书对齐代码，`mazeRoad / mazeWall / mazeVoid` 为唯一真源，`wall` 标废弃；`floor / floorAlt` **保留**（UI 面板底板） |
| 6 | **升耗视觉提示**（三处写法不一致） | 规格书未成文 | **D37**：数字转 `gold` 金色 + 右上角 **9px `+N`** 角标 + AP 不足时转 `danger` 红并加 **2px 斜线** + 技能条降为 `SPENT_FILL #272C35`（左侧色条 alpha 1→0.35）。**零代码改动**，当前实现已如此 |

> ⚠️ `battle-ap-ux-spec.md` §7 的「待用户拍板决策项 **D1–D5**」是**该规格书自己的序号**，与 GDD 裁决编号 **不同源、不可混用**（GDD 的 D1 = 共享生命池裁决、D2 = 第 6 层固定 Boss 通关）。该规格书 D1–D5 的原建议：D1 战场上我方方块小人移除 / D2 未解锁图鉴用预告制（**已被 D117 否决**）/ D3 `tech_ap_odd` 串联前置 `tech_ap_cap`（本 §50.3.8 已按串联落地）/ D4 逃跑在 BOSS 战保持全可逃 / D5 道具使用后保持不结束行动。

---

### 50.7 布局尺寸、分辨率与无障碍

#### 50.7.1 布局常量

| 项 | 值 | 归属 |
|---|---|---|
| 逻辑分辨率 | 可热改写 `let`（初值 1920×1080），改走 `setLogicalSize()` | `src/config/constants.ts` |
| 顶部信息带 `TOPBAR_H` | **64px**（叠加层，不挤压地图） | `src/config/constants.ts` |
| 迷宫格子 `TILE` | **34px**（世界坐标恒定） | `src/scenes/DungeonScene.ts` |
| 方向键留白 | 28px；按钮 54px + 4px 间隙 | `src/scenes/DungeonScene.ts` |
| 顶部渐隐遮罩 / 横条下渐隐 | 34px（`0.62 × (1 − i/34)`） | `src/scenes/DungeonScene.ts` |
| 控件热区上限 `ICON_HIT_MAX` | 40px；`DRAG_CANCEL_DISTANCE` 8px | `src/ui/Widgets.ts` |
| 相机缩放 | `clamp(min(视口宽,高) × 1.15 ÷ (max(迷宫宽,高) × 34), 1, 2.2)`，**夹取后再保留两位小数** | `src/scenes/DungeonScene.ts` |

#### 50.7.2 分辨率预设（`src/config/graphics.ts`）

| id | label | 尺寸 |
|---|---|---|
| `auto` | 自适应窗口（推荐，默认） | 按窗口 16:9 校正后**向下取最近的固定档** |
| `hd` | HD | 1280×720 |
| `hdplus` | HD+ | 1600×900 |
| `fhd` | FHD | 1920×1080 |
| `qhd` | QHD | 2560×1440 |
| `uhd` | 4K | 3840×2160 |

- 切换流程：解析预设 → `setLogicalSize()` → `scale.setGameSize()` + `refresh()` → **重启当前场景**让布局重建。
- 直接改 `game.config.width` 在 Phaser 3.90 下**无效**，必须走 `setGameSize`。
- 相机不夹取、队伍恒居中、迷宫外纯黑；缩放取整到两位是为了避免非整数倍率下的半像素接缝。

#### 50.7.3 无障碍（`src/config/accessibility.ts`）

| 项 | 值 |
|---|---|
| 全局字号档 `UI_SCALE_STEPS` | **100 / 125 / 150**，默认 `100` |
| 正文字号 / 字体族 | `BASE_FONT_SIZE = '16px'` / `FONT_FAMILY = '"Segoe UI", "Microsoft YaHei", system-ui, sans-serif'` |
| 高对比文字替换表 | `textDim → #EDEFF2`、`text → #FFFFFF`、`danger → #FF8A82`、`gold → #FFD34D`、`accent → #FFE24D`、`border → #EDEFF2` |
| 高对比文字描边 | `stroke: #05070A`，`strokeThickness: 3` |
| 高对比面板描边 | 仅 `Palette.border` 提亮为 `Palette.text`，线宽一律 ≥2px |
| 濒死渐晕 `vignetteEnabled` | 默认 **true**（D247 / D321） |
| 备战列排序 `rosterSortBy / rosterSortDesc` | 默认 `'rarity'` / `true`（降序） |

🔴 **默认档恒等性铁律**：`uiScale = 100` 且 `highContrast = false` 时，`scaledFontSize` / `highContrastTextColor` / `highContrastTextStroke` / `highContrastPanelStyle` **必须恒等返回入参**（连字符串本身都不变）—— 否则就是一次全项目视觉回归且极难归因。`verify.mjs` §22.1 直接断言这条恒等性与 `DEFAULT_ACCESSIBILITY` 恰好三项。

🔴 **存储（D315 / E-20 冻结裁定）**：「**影响存档内行为**」的项（引导开关、音量、战斗速度、快移）进随档 `state.settings`；「**全局无障碍偏好**」（文本缩放 / 高对比 / 渐晕开关 / 备战排序）**一律进独立键 `maze_rpg_settings`**。两套并存是有意的。

🔴 **写入纪律（D338 / G18）**：`maze_rpg_settings` 是分辨率与无障碍的**共用容器**，写入一律「**读旧值 → 合并 → 写回**」，整体覆写会在玩家改分辨率时把文本缩放 / 高对比静默抹掉（表现为"无障碍选项莫名其妙失效"，不报错、极难归因）。

⚠️ **D100 的连带（登记未做）**：文本缩放会影响固定字号（28/20/14/15/13/11-12/16）与固定布局尺寸（`TOPBAR_H = 64`、卡片 168×340）。本轮**只缩放字号**（经 `makeText`），布局常量**保持固定不联动**；放大后的溢出风险留待后续处理。

---

### 50.8 公式速查表（GDD 附录 D 第一部分 · 保真转载）

#### 50.8.0 记号与统一算例

| 记号 | 含义 |
|---|---|
| `ΣX` | 出战队伍全部角色在属性 `X` 上的取值之和（**含**已解锁科技的永久加成） |
| `team.attack` | 队伍攻击力，由 `Σ筋力` 派生 |
| `team.maxHp` | 队伍共享生命池上限，由 `Σ耐力` 派生 |
| `P(x)` | 边际递减概率曲线 `P_max × (1 − e^(−x/K))` |
| `power` | 技能倍率，配表里的整数百分数（`130` = 130%） |
| `rng.*` | 可复现随机源（mulberry32） |

**全文统一算例队伍**（下文所有「算例」都代入这组数）：

```
出战 4 人：Σ筋力 = 30   Σ耐力 = 40   Σ敏捷 = 80
          Σ幸运 = 60   Σ魔力 = 50   Σ灵性 = 80
技能：skill_srf_rose_pierce（蔷薇刺突，power = 130，hits = 1，
      effects = [damage 130]，enhanced = [damage 45]，apCost = 1）
敌方单位：defense = 0.2（构造值），未处于防御姿态
```

派生：`team.attack = round(30 × 5) = 150`；`team.maxHp = round(40 × 20) = 800`；`expMultiplier = 1 + 50 × 0.02 = 2.0`；`P_combo(80) ≈ 0.379270`；`P_crit(60) ≈ 0.263817`；`P_enhance(80) ≈ 0.316060`。

#### 50.8.1 队伍聚合（攻击 / 生命 / 经验倍率 / 速度）

| 项 | 公式 | 输入来源 | 落点 |
|---|---|---|---|
| 队伍六维 | `stats[X] = Σ(roles.attributes[X].value) + techStatBonuses()[X]` | 角色卡属性 + 已解锁科技缓存 | `src/systems/TeamStats.ts` |
| 科技属性加成 | `bonus[X] += effectValue × max(1, state.rank)` | `tech.json` 的 `stat_bonus` | `src/systems/TeamStats.ts`（rank 乘算在同一函数内） |
| 队伍攻击力 | `team.attack = round(Σ筋力 × strengthToAttack)` | `balance.strengthToAttack = 5` | `src/systems/TeamStats.ts` |
| 队伍最大 HP | `team.maxHp = round(Σ耐力 × enduranceToHP)` | `balance.enduranceToHP = 20` | `src/systems/TeamStats.ts` |
| 队伍经验倍率 | `expMultiplier = 1 + Σ魔力 × magicToExpRate` | `balance.magicToExpRate = 0.02` | `src/utils/math.ts`，调用点 `src/systems/TeamStats.ts` |
| 战斗内队伍攻击力（含增益） | `attack = round((Σ筋力 + buffs) × strengthToAttack)` | 战斗内 buff 汇总 | `src/systems/CombatSystem.ts` |
| 单角色速度 | `speed = baseSpeed + 敏捷` | `balance.baseSpeed = 10` | `src/systems/TeamStats.ts` |
| 出战人数 | `members = cards.length` | 队伍数组长度 | `src/systems/TeamStats.ts` |

> **速度当前不参与任何计算**：`speed` 只是保留字段，行动顺序已改为「我方阶段 / 敌方阶段」**双方阶段制**，敌方内部按定义顺序出手、不排序（见 `src/systems/CombatSystem.ts`）。

#### 50.8.2 三概率曲线（连击 / 暴击 / 强化）

统一曲线：`P(x) = P_max × (1 − e^(−x / K))`；`x ≤ 0` 直接返回 0；`K ≤ 0` 直接返回 `P_max`。

| 概率 | 公式 | 输入 | 参数默认值 |
|---|---|---|---|
| 连击率 | `P_combo = comboMax × (1 − e^(−Σ敏捷 / comboK))` | `Σ敏捷` | `0.6 / 80` |
| 暴击率 | `P_crit = critMax × (1 − e^(−Σ幸运 / critK))` | `Σ幸运` | `0.5 / 80` |
| 强化率 | `P_enhance = enhanceMax × (1 − e^(−Σ灵性 / enhanceK))` | `Σ灵性` | `0.5 / 80` |

判定上限：三种判定在 roll 时都被 `Math.min(0.95, ...)` 截断（叠满加成也最多 95%），连击率另在 `DamageCalculator` 再夹一次。技能可自带 `combo_bonus` 效果补充连击率，配表值 `8` 表示 `+0.08`。

**参考曲线（实算，便于填表核对）：**

| ΣX | P_combo（max 0.6） | P_crit / P_enhance（max 0.5） |
|---|---|---|
| 10 | 0.070502 | 0.058751 |
| 30 | 0.187626 | 0.156355 |
| 60 | 0.316580 | 0.263817 |
| 80 | 0.379270 | 0.316060 |
| 120 | 0.466122 | 0.388435 |
| 200 | 0.550749 | 0.458958 |
| 300 | 0.585889 | 0.488241 |

#### 50.8.3 伤害主干（基础伤害 → 连击段数 → 逐段结算 → 保底）

**（a）基础伤害** `baseDamage = team.attack × power / 100`；普通攻击视为 `power = 100`。
算例：`baseDamage = 150 × 130 / 100 = 195`。

**（b）连击段数（几何式累加，不是判定一次）**

```
comboRate = min(0.95, P_combo + comboBonus + skillComboBonus(skill))
baseHits  = max(1, skill.hits ?? 1)
totalHits = baseHits
while totalHits < 8 且 rng.chance(comboRate):
    totalHits += baseHits          # 每次成功连击追加「一整个技能段数」
    comboTriggered = true
totalHits = min(totalHits, 8)
```

硬上限 `MAX_HITS_PER_ACTION = 8`。含义：`hits: 2` 的 2 段技，连击一次即 +2 段（变 4 段），不是 +1 段。
算例：`comboRate = 0.379270`，`baseHits = 1`，期望段数 `= 1/(1−0.379270) ≈ 1.611`（受 8 段封顶截断）；若本次恰好连击 1 次则 `totalHits = 2`。

**（c）单段结算（暴击 → 浮动 → 减伤 → 保底）**

```
crit           = rng.chance(min(0.95, P_crit + critBonus))
critMultiplier = crit ? (critDamageBase + P_crit) : 1
variance       = rng.range(damageVarianceMin, damageVarianceMax)
defenseRate    = clamp(target.defense + (targetDefending ? defendReduction : 0), 0, 0.95)
rawDamage      = baseDamage × critMultiplier × variance
damage         = max(minDamage, round(rawDamage × (1 - defenseRate)))
```

参数默认值：`critDamageBase = 1.5`、`damageVarianceMin = 0.95`、`damageVarianceMax = 1.05`、`minDamage = 1`、`defendReduction = 0.5`。

算例（`totalHits = 2`，`defense = 0.2`，浮动取中值 1.0）：

| 段 | 暴击 | 计算 | 结果 |
|---|---|---|---|
| 1 | 否 | `round(195 × 1 × 1.0 × (1−0.2))` | **156** |
| 2 | 是 | `raw = 195 × (1.5 + 0.263817) = 343.94`；`round(343.94 × 0.8)` | **275** |
| 合计 | — | `156 + 275` | **431** |

两段都不暴击 = 312；两段都暴击 = 550。保底口径：即使 `power` 极低或减伤拉满，单段也至少 `1` 点。

**（d）强化（灵性触发）追加效果**

```
enhanced = skill.enhanced 非空 ? rng.chance(min(0.95, P_enhance + enhanceBonus)) : false
```

| 强化效果类型 | 公式 |
|---|---|
| `damage` | `extra = round(team.attack × value / 100)`，累加进 `hit.damage` |
| `heal` | `extra = round(team.attack × value / 100)` |
| `buff` / `debuff` | 推入 `target.buffs`，`value` 取 `±abs(value)` |
| `combo_bonus` | **仅写日志**，不改变本次结算 |

算例：`enhanced = [damage 45]` 触发 1 次 → `extra = round(150 × 45 / 100) = 68`，单段伤害由 156 → 224。

**（e）敌方攻击（不享受队伍加成）**：敌人没有队伍合并属性，用「自身 attack 组一个 stub 队伍」，概率固定为 `combo 0.08 / crit 0.1 / enhance 0`。

#### 50.8.4 辅助技（heal / buff / debuff / combo_bonus）

| 效果 | 公式 |
|---|---|
| 治疗（技能 `effects[heal]`） | `amount = round(team.attack × value/100 + Σ灵性 × 0.5)` |
| 治疗对象（`ally_all`） | 取 `[actor]` —— 但 `caster.hp` 是我方共享池，**等价于全队回血** |
| `buff` / `debuff` | `signed = ±abs(value)`，`duration = effect.duration` |
| `combo_bonus` | 作为一条 `source = "XX技能(连击 +N%)"`、`duration = 3`、`value = 0` 的伪 buff 记录 |
| 战斗中属性→概率折算 | `crit += Σbuff(luck) × 0.004`；`enhance += Σbuff(spirit) × 0.004`；`combo += Σbuff(agility) × 0.004` |
| 连击加成 buff 解析 | 从 `source` 正则抠 `连击 +(\d+)%`，累加 `N/100` |
| 被动回血「回气」 | 每大回合对**全体我方**固定回 **10** 点 |

算例：`skill_alst_healing_antler`（`heal value = 90`）→ `amount = round(150 × 0.90 + 80 × 0.5) = 175`。
连击 buff：`skill_rn_dual_draw` 的 `combo_bonus = 12` → `comboRate += 0.12`，持续 3 回合。

> ⚠️ **已核对的文案/实现不一致**：治疗类技能描述写「恢复 90% **魔力**值的生命」，但代码用的是 `team.attack`（由筋力派生）而不是魔力。同类技能（鹿角祝福 / 花语祝福 / 茶点侍奉 / 天使赐福 / 银盾庇护 …）全部如此。**是否修正由策划裁决**（见 §50.13-1）。

#### 50.8.5 升级经验需求与升级成长量

| 项 | 公式 | 参数 |
|---|---|---|
| 升到下一级所需经验 | `expForLevel(level) = round(expBase × level^expExponent)` | `expBase = 20`，`expExponent` 见下方注 |
| 加经验（单角色） | `gained = max(0, round(amount × (1 + 魔力 × magicToExpRate)))` | 魔力 = 角色自身 + 科技加成 |
| 循环升级 | `while exp >= expForLevel(level) { exp -= need; level++; applyLevelUp() }` | — |
| 战斗经验总量 | `reward.exp = round(Σ(敌人 exp) × team.expMultiplier)` | `def.exp` 不随 `count` 放大 |
| 队伍升级入口 | 每个存活成员**独立**按同一 `exp` 值结算（**不是平分**） | — |

**升级成长量**：`delta = max(0, round(levelUpBaseGain × potentialMultiplier[潜力]) + rng.int(-1, 1))`，`levelUpBaseGain = 2`。

| 潜力 | 倍率 | `round(2 × 倍率)` | `delta` 区间 |
|---|---|---|---|
| A | 2.0 | 4 | 3 – 5 |
| B | 1.6 | 3 | 2 – 4 |
| C | 1.3 | 3 | 2 – 4 |
| D | 1.1 | 2 | 1 – 3 |
| E | 0.9 | 2 | 1 – 3 |
| F | 0.7 | 1 | 0 – 2 |

算例：`expForLevel(1) = 20`；`expForLevel(5) ≈ 263`；`expForLevel(10) ≈ 796`。打赢 `enemy_slime`（`exp = 14`）且 `expMultiplier = 2.0` → `reward.exp = 28`，**每个成员各得 28**（不是 7）。某 A 潜力角色升 1 级 → 该属性 +3~+5。

> ⚠️ **经验倍率被应用两次**：第一处 `reward.exp = round(Σ敌人exp × team.expMultiplier)`；第二处 `gained = round(amount × (1 + 该角色魔力 × 0.02))`。调用链把**已乘过队伍倍率**的 `reward.exp` 再交给 `gainExp`。后果：`Σ魔力 = 50` 时队伍倍率 2.0，每个成员还会再乘一次自己的倍率（≥1），实际入账约为设计值的 2 倍以上。**是否视为 bug 由策划裁决**（见 §50.13-2）。
> 实测算例：`reward.exp = 28`，某成员自身魔力 12 → `gained = round(28 × 1.24) = 35`。

> ⚠️ **未使用字段**：`balance.levelUpTechPointBonus`（默认 `0`）在 `src/` 内**没有任何读取点**，仅存在于定义与生成物，当前是死配置。

> ⚠️ **`expExponent` 口径**：本章公式按 `balance.json` 现值 **1.6** 给出；GDD **D33 / D256** 裁决为 **1.9**（D256 定案：1.9 + 三小怪 exp → 25/21/29、密度不动）。配表与代码现值仍为 1.6，**裁决未落地**（见 §50.13-3）。

#### 50.8.6 招募成本与稀有度权重

| 项 | 公式 | 参数 |
|---|---|---|
| 本次招募金币花费 | `round(recruitGoldCost × recruitCostGrowth^recruitCount)` | 平价 **5 000**（D67 取消递增 ⇒ `recruitCostGrowth = 1`；D306 → 3 500、D310 → 5 000）；改前 `120`、`1.35` |
| 本次招募科技点花费 | `max(0, recruitTechPointCost)`，**恒定不递增** | `2` |
| `recruitCount` | 本次营地面板会话内已成功招募的次数（面板成员变量，**读档不累计**） | — |
| 模板权重 | `weight = rarityWeights[rarity] × (rarity ≥ 4 ? 1 + rarityBonus : 1) × (unlockedByDefault ? 0.35 : 1)` | `balance.json` |
| 新角色起始等级 | `clamp(rarity, 1, 9)` | — |
| 初始属性总和 | `initialStatTotal + (rarity − 1) × 3`，每项 ≥ `initialStatMin` | `60` / `1` |
| 可重复科技价格 | `round(cost × 1.6^rank)` | `1.6` |

`rarityWeights = {1:40, 2:30, 3:18, 4:9, 5:3}`。

算例（平价，逐次均为 5 000 金 + 2 科技点）：第 1 / 2 / 3 / 4 / 11 次均为 `round(5000 × 1^n) = 5 000`。
改前（D67 前）`120 × 1.35^n` ⇒ 120 → 162 → 219 → 295 → … → 2413（逐次递增）。

> ⚠️ `rarityWeights` 里 `★1/★2` 权重合计 **70 是死权重**（无 ★1/★2 模板）。实际概率：**★3 = 60% / ★4 = 30% / ★5 = 10%**（再乘剧情惩罚与加成）。**按表解释招募概率会全错。**
> ⚠️ 配表现值（`balance.json`）为 `recruitGoldCost = 500 / recruitCostGrowth = 1 / recruitTechPointCost = 0`，与 GDD 口径（5 000 / 1 / 2）不一致，GDD 勘误条已标注「配表待改」（见 §50.13-5）。

#### 50.8.7 敌人 `count` 折算（一群 = 一个合体单位）

| 字段 | 折算规则 |
|---|---|
| `hp` / `maxHp` | `def.hp × count`（**线性**） |
| `attack` | `round(def.attack × (1 + (count − 1) × 0.35))`（**递减式**，刻意不线性；`0.35` 硬编码） |
| `defense` / `speed` / `exp` / `gold` / `techPoints` / `drops` | **一律不变**（奖励不随数量放大） |
| `name` | `count > 1` 时缀 ` ×N` |

算例：`enemy_slime`（`hp 42`、`attack 8`、`count 3`、`exp 14`、`gold 10`）→ `hp = 126`；`attack = round(8 × 1.7) = 14`；`exp` 仍 `14`、`gold` 仍 `10`。
单敌 `count = 1` → `attack = 8`；`count = 2` → `attack = round(8 × 1.35) = 11`。

#### 50.8.8 AP 系统与逃跑成功率

| 项 | 值 |
|---|---|
| `AP_INITIAL` | **3** |
| `AP_REGEN_BASE` | **2** |
| `AP_CAP_BASE` | **8**（上限 = `8 + TechSystem.apCapBonus()`） |
| 奇数回合额外回复 | `+ TechSystem.apRegenOddBonus()`（「奇袭节拍」= 1） |
| 回合回复判定 | 读的是**递增之后**的 `round` |

**逃跑成功率（`src/systems/CombatSystem.ts`）：**

```
chance = min(0.95, fleeBaseChance + fleeAttempts × fleeChanceStep)
fleeAttempts += 1     # 在判定前自增：每次尝试（含失败）都会推进一档
```

| 第几次尝试 | 成功率 |
|---|---|
| 1 | 0.45 |
| 2 | 0.60 |
| 3 | 0.75 |
| 4 | 0.90 |
| 5 | `min(0.95, 1.05)` = **0.95** |

`fleeBaseChance = 0.45`、`fleeChanceStep = 0.15`。另有**回合超时强制撤退**：`round > balance.maxRounds(60)` 时 `outcome = 'fled'`。

#### 50.8.9 迷宫生成（主干步数 / 房间格换算 / 元素数量）

**（a）难度阶梯（唯一真源 `src/config/mazeDifficulty.ts`）**

| 档 | 尺寸 | `mainPathSteps`（玩家步） | 支路数 | 支路长度（玩家步） |
|---|---|---|---|---|
| N1 | 15×15 | 20 | 0 | 0 / 0 |
| N2 | 19×19 | 32 | 2 | 4 – 6 |
| N3 | 23×23 | 48 | 4 | 6 – 10 |
| N4 | 27×27 | 58 | 6 | 8 – 14 |
| N5 | 31×31 | 74 | 8 | 10 – 18 |
| N6 | 35×35 | 90 | 10 | 12 – 24 |

层 → 档映射：进入第 `floor` 层就用第 `floor` 档，界面显示 `N{spec.level}`；到 `floor ≥ MAZE_MAX_DIFFICULTY = 6` 时出口只剩「返回营地」。

**（b）玩家步数 ⇄ 房间格换算**

```
roomPathLength = max(2, floor(mainPathSteps / 2) + 1)      # 主干
roomLen        = max(1, round(stepLen / 2))                # 单条支路，stepLen 先 rng.int(min, max)
maxSteps（物理上限） = 2 × (rooms - 1)，rooms = floor((w-1)/2) × floor((h-1)/2)
```

换算依据：房间格之间隔一格墙，**走一步房间 = 走两格地图**。出口固定在主干末端 ⇒ **最短通关步数 = 主干步数**。

| 档 | 主干房间格 | 单条支路房间格 | `rooms` | 物理上限 `maxSteps` |
|---|---|---|---|---|
| N1 | `max(2, 10+1) = 11` | 无支路 | 49 | 96 |
| N3 | `max(2, 24+1) = 25` | `round([6,10]/2)` = 3 – 5 | 121 | 240 |
| N6 | `max(2, 45+1) = 46` | `round([12,24]/2)` = 6 – 12 | 289 | 576 |

**（c）元素数量**

```
enemyCap   = max(2, floor(floorTiles × 0.12))
enemyCount = min(enemyCap, max(1, round(floorTiles × config.enemyDensity)) + extraEnemies)
treasureCount / saveCount / eventCount / trapCount   # 直接读配表
```

**层间密度递增**：`enemyDensity × (1 + (nextFloor − 1) × 0.12)`（`0.12` 硬编码于 `src/scenes/DungeonScene.ts`）。
元素摆放顺序：宝箱 → 存档点 → 事件 → 陷阱 → 敌人，从洗牌后的地板格依次取空位。

算例（N1，可精确推算）：地板格数 = 主干 11 + 连接格 10 = **21** ⇒ `enemyCap = max(2, floor(21 × 0.12)) = 2`；`dungeon_root` 的 `enemyDensity = 0.035` → `max(1, round(21 × 0.035)) = 1` ⇒ `enemyCount = min(2, 1 + 0) = 1`。N2 及以上含支路，地板格数随种子变化，需实跑确定。
算例（层间递增）：`dungeon_moss` 的 `enemyDensity = 0.045`，下探到第 3 层时 `0.045 × (1 + 2 × 0.12) = 0.0558`。

#### 50.8.10 宝箱 / 事件 / 陷阱（硬编码权重）

**（a）宝箱内容**

```
goldBase = 20 + config.floor × 25
roll = rng.float()
roll < 0.45          → { kind: 'gold', amount: rng.int(goldBase, round(goldBase × 2.2)) }
0.45 ≤ roll < 0.90   → { kind: 'item', itemId: 从 5 项池任取, count: rng.int(1, 2) }
roll ≥ 0.90          → { kind: 'tech', amount: rng.int(1, 2) }
```

- 概率分界 `0.45 / 0.90`、金币基数 `20 + floor × 25`、上限倍率 `2.2` 全部硬编码。
- 道具池（字面量 5 项）：`item_potion_s` / `item_potion_l` / `item_ore` / `item_crystal` / `item_coin_pouch`。
- ⚠️ 传入的是 `config.floor`（配表字段），而 `dungeons.json` 四张图**都写 `"floor": 1`** ⇒ `goldBase` 恒为 **45**，不随实际层数增长。真正随层数变的是 `enemyDensity`。
- 算例：四张地牢任意一层开箱，金币分支区间 `[45, round(45 × 2.2)] = [45, 99]`。
- payload 缺失时的默认值：金币 50 / 科技点 1 / `item_potion_s × 1`。

**（b）事件点内容**

| 事件 | 权重 | 概率 |
|---|---|---|
| `event_spring` 泉水 | 30 | 30% |
| `event_merchant` 商人 | 20 | 20% |
| `event_shrine` 神龛 | 25 | 25% |
| `event_ambush` 埋伏 | 25 | 25% |

**（c）陷阱内容**

| 类型 | 权重 | 概率 | 参数 |
|---|---|---|---|
| `damage` 伤害 | 50 | 50% | `value = rng.int(8, 20)` |
| `slow` 迟缓 | 25 | 25% | `value = 3`（固定 3 回合） |
| `teleport` 传送 | 25 | 25% | `value = 3`（**未被使用**） |

**（d）陷阱伤害公式**

```
damage = round(pool.maxHp × value / 100) + value
pool.hp = max(1, pool.hp - damage)      # 陷阱不能致死，至少留 1
```

算例：`pool.maxHp = 800`、`value = 15` → `damage = 120 + 15 = 135`。
> 注意公式里那个 **`+ value` 的平地追加项**：陷阱越"高级"（value 越大）既有百分比成长、也有绝对成长，这是硬编码在表达式结构里的，改它必须动代码。

#### 50.8.11 迷宫事件的具体数值

| 事件选项 | 数值 |
|---|---|
| 泉水·饮下 | `heal = round(pool.maxHp × 0.35)` |
| 泉水·灌水袋 | 获得 `item_potion_s × 1` |
| 商人·买药水 | 花 **80** 金币，得 `item_potion_l × 1` |
| 商人·卖材料 | 每份 `max(1, round(price × 0.5))` 金币，素材池 `item_ore / item_crystal / item_core` |
| 神龛·献金 | 花 **50** 金币，得 **2** 科技点 |
| 神龛·祈祷 | 随机一名队员、随机一项属性 **+3** |
| 埋伏·迎战 | 直接开战 |
| 埋伏·逃跑 | `rng.chance(0.5)` 成功则随机传送，否则被迫开战 |

**首次通关奖励**：`gameState.addTechPoints(config.firstClearTechPoints)`，配表 `dungeons.json` 现值 **`8 / 9 / 10 / 11`（合计 38）**。

> 🔧 **勘误（第 101 轮 · 落地实测）**：四图首通原写 **`3 / 3 / 5 / 10`（合计 21）**，那是 **D23** 按**旧的 79 点科技树**标的价，**已被 D246 取代**。现口径为「**首通 38 + 主线任务 14**」（D246 / D255）；验收硬门 #5（D297）用的就是 **38**。GDD §7.7 的地牢表（首通 3/3/5/10）**尚未同步**（见 §50.13-6）。

算例：`maxHp = 800` 时泉水恢复 `round(800 × 0.35) = 280` 点。

#### 50.8.12 视野半径与判定

| 项 | 定义 |
|---|---|
| 视线半径 | `SIGHT_RADIUS = 5`（格，**曼哈顿距离**） |
| 可见范围 | `|dx| + |dy| ≤ 5`，且不越界 |
| 遮挡规则 | `dist > 2` 时做简易视线检查（Bresenham），被墙挡住的格子不点亮 |
| 视线算法 | Bresenham 直线插值，`grid[y][x] !== 0`（墙/虚空）即判定被挡；`guard < 64` 步上限 |
| 视野状态机 | `2 = 当前可见` → 下一帧降级为 `1 = 已探索`；`0 = 未知` |
| 玩家所在格 | 无条件置 `2` |

⇒ 实际可见区域是「半径 5 的曼哈顿菱形」∩「半径 2 以内无视遮挡 + 半径 3–5 需通视」。
算例：玩家在 `(1,1)` 时，`(4,1)`（`dist = 3`）若与 `(1,1)` 之间有墙则不点亮；`(3,1)`（`dist = 2`）无论如何都点亮。
**已探索比例**（HUD 用）：`explored / total`，分母只统计 `grid !== 0` 的地板格。

#### 50.8.13 相机缩放与迷宫像素尺寸

| 常量 / 公式 | 值 |
|---|---|
| 单格世界像素 `TILE` | **34** |
| `ZOOM_TARGET` | **1.15**（视口短边的 1.15 倍） |
| `ZOOM_MIN` | **1**（不缩小） |
| `ZOOM_MAX` | **2.2** |

```
mazeSpan  = max(spec.width, spec.height) × TILE
shortSide = min(GAME_WIDTH, GAME_HEIGHT)
raw       = (shortSide × ZOOM_TARGET) / mazeSpan
zoom      = round(clamp(raw, ZOOM_MIN, ZOOM_MAX) × 100) / 100
```

取两位小数是为了避免非整数倍率下的半像素接缝。相机跟随**不做边界夹取**，队伍恒居中，迷宫外用背景色纯黑填充。
算例（1920×1080，`shortSide × 1.15 = 1242`）：N1 `mazeSpan = 510` → raw 2.4353 触顶 → **2.2**；N3 `782` → **1.59**；N6 `1190` → **1.04**。

---

### 50.9 常量来源对照（GDD 附录 D 第二部分）

#### 50.9.1 A 类 · 配表可改（改 JSON，跑 `scripts/gen-tables.mjs` 即可，不动代码）

| 数值 | 当前值 | 位置 |
|---|---|---|
| 筋力→攻击系数 | 5 | `balance.json` / 回退默认 `balance.ts` |
| 耐力→HP 系数 | 20 | 同上 |
| 魔力→经验倍率 | 0.02 | 同上 |
| 连击率上限 / K | 0.6 / 80 | 同上 |
| 暴击率上限 / K | 0.5 / 80 | 同上 |
| 强化率上限 / K | 0.5 / 80 | 同上 |
| 暴击伤害基数 | 1.5 | 同上 |
| 伤害浮动区间 | 0.95 – 1.05 | 同上 |
| 单段最低伤害 | 1 | 同上 |
| 防御减伤率 | 0.5 | 同上 |
| 升级基础成长值 | 2 | 同上 |
| 初始属性总上限 | 60 | 同上 |
| 初始单项最低值 | 1 | 同上 |
| 升级经验系数 / 指数 | 20 / **1.6**（GDD D33/D256 裁决 1.9，未落地） | 同上 |
| 升级科技点奖励 | 0 **（无读取点，死配置）** | 同上 |
| 队伍上限 / 初始栏位 | 6 / 4 | 同上 |
| 基础速度 | 10 | 同上 |
| 逃跑基础率 / 每回合步进 | 0.45 / 0.15 | 同上 |
| 战斗回合上限 | 60 | 同上 |
| 招募基础金币 / 递增系数 / 科技点 | **5 000 / 1 / 2**（D67 取消递增；改前 120 / 1.35 / 2；**配表待同步**） | 同上 |
| 潜力倍率表 A–F | 2.0 / 1.6 / 1.3 / 1.1 / 0.9 / 0.7 | 同上 |
| A / B 潜力最多项数 | 2 / 3 | 同上 |
| 稀有度权重 1–5 | 40 / 30 / 18 / 9 / 3 | 同上 |
| 技能倍率 / AP 消耗 / 段数 / 强化效果 | 每技能各自 | `skills.json`（`power` / `apCost` / `hits` / `effects` / `enhanced`） |
| 敌人 HP / 攻击 / 减伤 / 经验 / 金币 / 科技点 / 群数 / 掉落 | 每敌人各自 | `enemies.json` |
| 地牢元素数 / 密度 / 首通科技点 / 敌人池权重 | 每图各自 | `dungeons.json` |
| 科技价格 / 效果值（AP 上限 +2、奇数回能 +1、稀有度加成 1.0） | 每科技各自 | `tech.json` |
| 道具价格 / 堆叠上限 | 每道具各自 | `items.json` |
| 角色稀有度 / 技能槽 | 每角色各自 | `characters.json` |

> `balance.json` 允许**部分覆盖**，缺失字段回落到 `balance.ts` 的 `BALANCE` 常量。所以「改 balance.json 不生效」通常意味着键名拼错、回落到了默认值。

#### 50.9.2 B 类 · TS 模块常量（改常量即可，但必须重新构建）

| 数值 | 当前值 | 位置 |
|---|---|---|
| 难度阶梯（尺寸 / 主干步数 / 支路数 / 支路长度） | N1–N6，见 §50.8.9 | `src/config/mazeDifficulty.ts` |
| 单段命中次数上限 | 8 | `src/systems/DamageCalculator.ts` |
| 视线半径 | 5 | `src/systems/DungeonSystem.ts` |
| 单格世界像素 | 34 | `src/scenes/DungeonScene.ts` |
| 缩放目标 / 上下限 | 1.15 / 1 / 2.2 | `src/scenes/DungeonScene.ts` |
| 方向键留白 | 28 | `src/scenes/DungeonScene.ts` |
| 逻辑分辨率（可变 `let`） | 1920×1080 | `src/config/constants.ts`（改走 `setLogicalSize()`） |
| 顶部信息条高度 `TOPBAR_H` | **64** | `src/config/constants.ts` |
| `balance.ts` 的 `BALANCE` 默认值 | 同 A 类 | `src/config/balance.ts`（A 类配表的回退基线） |
| 迷宫自检阈值（可达格 < 20 报警） | 20 | `src/systems/MazeGenerator.ts` |
| 网格尺寸下限 / 奇数化 | ≥11 且为奇数 | `src/systems/MazeGenerator.ts` |
| 配表容量自检（N1 容量 = `floor(15×15/2)` = 112） | 112 | `src/data/loaders/DataLoader.ts` |

#### 50.9.3 C 类 · 硬编码在逻辑里（改它必须动代码）

| 数值 | 当前值 | 位置 | 说明 |
|---|---|---|---|
| AP 初始值 | 3 | `CombatSystem.ts` | 局部 `const`，非配置 |
| AP 每回合回复 | 2 | `CombatSystem.ts` | 同上 |
| AP 上限基数 | 8 | `CombatSystem.ts` | 科技加成叠在它之上 |
| 敌人 `count` 攻击放大系数 | 0.35 | `CombatSystem.ts` | `attack × (1 + (count−1) × 0.35)` |
| 战斗内属性→概率折算 | 0.004 / 点 | `CombatSystem.ts` | 敏捷 / 幸运 / 灵性三处 |
| 「回气」固定回血量 | 10 | `CombatSystem.ts` | 每大回合，全体我方 |
| 敌人 AI：自我治疗血线 | < 0.4 | `CombatSystem.ts` | `hp / maxHp < 0.4` |
| 敌人 AI：使用攻击技能概率 | 0.65 | `CombatSystem.ts` | 否则普攻 |
| 我方自动战斗 AI：治疗血线 | < 0.45 | `CombatSystem.ts` | |
| 逃跑成功率上限 | 0.95 | `CombatSystem.ts` | |
| 三概率判定上限 | 0.95 | `ProbabilitySystem.ts`；连击率另在 `DamageCalculator.ts` 再夹一次 | 叠满也封顶 95% |
| 防御减伤率上限（clamp 上限） | 0.95 | `DamageCalculator.ts` | |
| 敌方固定概率组 | combo 0.08 / crit 0.1 / enhance 0 | `DamageCalculator.ts` | 敌方不吃队伍曲线 |
| 连击 buff 默认持续回合 | 3 | `DamageCalculator.ts` | `combo_bonus` 伪 buff |
| 宝箱概率分界 | 0.45 / 0.90 | `MazeGenerator.ts` | 金币 / 道具 / 科技点 |
| 宝箱金币基数公式 | `20 + floor × 25` | `MazeGenerator.ts` | `floor` 取 `config.floor`（恒 1） |
| 宝箱金币上限倍率 | 2.2 | `MazeGenerator.ts` | |
| 宝箱道具池（5 项字面量） | potion_s / potion_l / ore / crystal / coin_pouch | `MazeGenerator.ts` | |
| 宝箱道具数量 / 科技点数量 | `int(1, 2)` / `int(1, 2)` | `MazeGenerator.ts` | |
| 事件权重 | 30 / 20 / 25 / 25 | `MazeGenerator.ts` | 泉水 / 商人 / 神龛 / 埋伏 |
| 陷阱权重 | 50 / 25 / 25 | `MazeGenerator.ts` | 伤害 / 迟缓 / 传送 |
| 陷阱伤害 `value` 区间 | `int(8, 20)` | `MazeGenerator.ts` | |
| 陷阱 slow / teleport 的 value | 3 | `MazeGenerator.ts` | 传送分支未读取该值 |
| 敌人数硬上限系数 | 0.12 | `MazeGenerator.ts` | `enemyCap = max(2, floor(floorTiles × 0.12))` |
| 敌人上限的下限 / 实际数量下限 | 2 / 1 | `MazeGenerator.ts` | |
| 层间敌人密度递增 | 每层 +12% | `DungeonScene.ts` | `×(1 + (n−1) × 0.12)` |
| 陷阱伤害公式结构 | `round(maxHp×v/100) + v` | `DungeonSystem.ts` | 百分比 + 绝对双段成长 |
| 陷阱伤害默认 `value` | 10 | `DungeonSystem.ts` | payload 缺失时 |
| 陷阱不可致死保底 | 至少留 1 HP | `DungeonSystem.ts` | |
| 宝箱默认金币 / 科技点 / 道具 | 50 / 1 / `item_potion_s × 1` | `DungeonSystem.ts` | payload 缺失时 |
| 默认敌人 id | `enemy_slime` | `DungeonSystem.ts` | |
| 视野遮挡豁免半径 | 2（`dist > 2` 才查视线） | `DungeonSystem.ts` | |
| 视线检查步数护栏 | 64 | `DungeonSystem.ts` | |
| 泉水恢复比例 | 0.35 | `DungeonScene.ts` | `maxHp × 0.35` |
| 商人药水售价 | 80 金币 | `DungeonScene.ts` | |
| 素材回收折扣 | 0.5 | `DungeonScene.ts` | `price × 0.5`，至少 1 |
| 神龛献金 / 回报 | 50 金币 → 2 科技点 | `DungeonScene.ts` | |
| 神龛祈祷加成 | +3 单项属性 | `DungeonScene.ts` | |
| 埋伏逃跑成功概率 | 0.5 | `DungeonScene.ts` | |
| 遇敌数量恒为 1 | 硬截断 | `DungeonScene.ts` | 多敌入参只取第一个 |
| 稀有度每级 +3 初始属性 | 3 | `CharacterFactory.ts` | `initialStatTotal + (rarity−1)×3` |
| 起始等级上限 / 下限 | 9 / 1 | `CharacterFactory.ts` | `clamp(rarity, 1, 9)` |
| A 潜力稀有度修正 | `(rarity+1)/6`，下限 0.2 | `CharacterFactory.ts` | |
| B 潜力抽取基数 | `round(3 × rarityFactor)` | `CharacterFactory.ts` | |
| 低潜力池字面量 | `['C','C','D','D','E','F']` | `CharacterFactory.ts` | |
| 初始解锁角色权重折减 | 0.35 | `CharacterFactory.ts` | `unlockedByDefault` |
| 技能槽固定数量 | 2 | `CharacterFactory.ts`、`DataLoader.ts` | 配表校验也硬性要求 = 2 |
| 可重复科技价格基数 | 1.6 | `TechSystem.ts` | `cost × 1.6^rank` |
| 可重复科技默认层数上限 | 5 | `TechSystem.ts` | `tech.maxRank ?? 5` |
| 战斗日志上限条数 | 200 | `CombatSystem.ts` | |
| 迷宫中传送目标 | 均匀随机地板格 | `DungeonSystem.ts` | `Math.random()`，**非种子随机源** |

#### 50.9.4 改数值的推荐路径（方案，未落地）

| 方案 | 内容 | 落地顺序 |
|---|---|---|
| **方案 1（优先）** | 给 `balance.json` 补键接管战斗侧硬编码：`apInitial` / `apRegenBase` / `apCapBase`、`enemyCountAttackFactor`、`buffStatToChance`、`regenHeal`、`enemyHealHpRatio` / `enemySkillChance` / `allyAutoHealRatio`、`chanceCap`、`enemyBaseProbs`、`maxHitsPerAction` | ① 全在既有 `balance.json` 通道上，改动面最小 |
| **方案 2** | 新建 `src/data/tables/mazeTables.json`，把 `treasure` / `event` / `trap` / `element` / `sight` / `eventNumeric` 六组迷宫侧数值成组外置（塞进 balance.json 会污染"平衡表"语义） | ② 需新增一条配表生成链路，涉及 `gen-tables.mjs`，建议单独一次提交并验证 `DataLoader.validate()` |
| **方案 3** | `TILE` / `ZOOM_TARGET` / `ZOOM_MIN` / `ZOOM_MAX` 提到 `src/config/graphics.ts`，仍作模块常量而非配表（渲染口径而非玩法数值，配表化收益低） | ③ 纯搬迁、零行为变更、回归风险最低 |

三项都不涉及存档结构（`save.ts`），**不影响已有存档兼容性**。

---

### 50.10 立绘资产管线（可操作流程）

#### 50.10.1 三档规格与裁切规则

产物目录 `public/assets/portraits/`，每个角色三档 × PNG/WebP = **240 个产物**（40 角色）。

| 档 | 文件 | 尺寸 | 裁切规则 | 用途 |
|---|---|---|---|---|
| 头像 | `portrait_XX.png/.webp` | **256×256** | 取**头部**：按 `head-windows.mjs` 里该编号的 `h0/h1` 纵向窗口 `crop(0, h0·H, W, h1·H)` → `cover` 等比缩放到 256（纵向居中）→ 横向取 256px（见 `cx`） | 列表缩略图、角色行、HUD 头像 |
| 半身 | `bust_XX.png/.webp` | **384×512** | 取**源图顶部 0–58%** 高度 `crop(0, 0, W, 0.58·H)` → `cover` 到 384×512（`prefer_top = true`，顶部对齐） | 战斗场景立绘 |
| 全身 | `full_XX.png/.webp` | **512×1024** | **整张**源图 `crop(0, 0, W, H)` → `cover` 到 512×1024（纵向居中） | 详情栏大立绘（**优先级高于 bust**） |

编码参数固定：PNG `optimize=True`；WebP `quality=88, method=6`。缩略失败降级为占位图（`PortraitCard`），不抛异常。

加载链（`src/config/constants.ts` 的 `PortraitAssets`）：

| 项 | 值 |
|---|---|
| `dir` | `assets/portraits` |
| `portraitKey` / `bustKey` / `fullKey` | `portrait_${id}` / `bust_${id}` / `full_${id}` |
| `portraitPath` / `bustPath` / `fullPath` | `assets/portraits/{portrait,bust,full}_${id}.png` |
| `all` | `Array.from({length: 40}, …)` → `'01'`–`'40'` |

`PreloadScene` 按 `PortraitAssets.all` 为每个编号加载三张纹理；HUD 用头像、BattleScene 用半身、CampScene / PortraitCard 用全套。运行时**不假设"文件号 = 立绘号"**：`characters.json.portrait → PortraitAssets.*Path(id)`。

#### 50.10.2 🚨 踩过的坑之一：编号错位 —— 立绘编号必须查表

**事故**：源图最初是**长文件名**（形如 `artist_min … s-<数字>.png`），构建脚本用 `sorted(os.listdir(SRC))`（Python 的**字符串/字典序**：`1,10,11,…,2,20,…`）决定立绘编号；后来文件被统一改名成 `1.png…40.png`，**顺序被打乱且无人重跑管线**。

像素级溯源（`full_XX.png` 逐像素 ≡ `build_full(image/M(XX).png)`，**40/40 精确命中**）证明线上产物出自一个错位排列 `M`：

```
01→33  02→15  03→32  04→12  05→13  06→35  07→2   08→3   09→10  10→31
11→40  12→22  13→1   14→25  15→24  16→17  17→37  18→28  19→29  20→30
21→18  22→9   23→19  24→6   25→5   26→11  27→36  28→7   29→14  30→27
31→20  32→16  33→26  34→38  35→23  36→34  37→39  38→21  39→4   40→8
```

即 **`image/1.png` 曾是 13 号凯瑟琳**，`image/15.png` 才是 2 号云漪。

> 🔴 **铁律：立绘编号一律以 `scripts/portrait-sources.mjs` 的 `PORTRAIT_SOURCE` 为准，禁止用文件名 / 排序位置推断编号。** 否则 40 个角色的立绘会**整体错位且不报任何错**（直接按编号复制再重跑就会中招）。
>
> 判定 M 的三条独立证据：① M 推出的 12 组「编号 ← 源图」对照 `characters.json` 称号/简介，**12/12 全中**；② 旧 `head-windows.mjs` 的逐张注释描述的正是"该**编号**的角色"，与之一致；③ 旧表里唯一填了 `cx` 的 40 号，恰是唯一一张"启用 cx 后会与线上不同"的图。

**现状（2026-09-22 起）**：源图文件名已改为**纯角色名**（`image/塞拉菲娜.png`、`image/云漪.png` …，`image2/` 同步），`PORTRAIT_SOURCE` 的值随之变成自解释形式：

```
'01': '塞拉菲娜.png', '02': '云漪.png', '03': '凛.png', … , '40': '蝶.png'
```

于是"编号错位"从**能静默发生**变成**必然变红**：`verify.mjs §38.6` 断言「`PORTRAIT_SOURCE[XX]` 必须等于『portrait 字段为 XX 的那个角色的 `name`』+ `.png`」。

#### 50.10.3 🚨 踩过的坑之二：`cx` 曾是死代码

**事故**：旧实现在 `cover()` **之后**再平移 —— `por = cover(head, 256, 256, …)` 恒为 **256 宽**，于是 `left = max(0, min(por.size[0] − 256, left))` 里 `por.size[0] − 256 == 0`，把横向偏移**钳到 0** ⇒ **`cx` 从不生效、头像永远横向居中**。实测后果：新图 **11 号只剩半张脸、21 号只剩后脑勺**。

**修法**：在**源图**上取一个「边长 = 头部窗口高 `band_h`、横向中心 = `cx·W`」的**正方形窗口**（并 clamp 到图内），再 `cover` 到 256：

```
band_h = hy1 - hy0
x0 = int(round(cx * W - band_h / 2.0))
x0 = max(0, min(W - band_h, x0))
head = src.crop((x0, hy0, x0 + band_h, hy1))
por  = cover(head, 256, 256, prefer_top=False)
```

这与"居中"分支取到的**源区域尺寸完全一致**（居中时可见区也恰是 `band_h × band_h` 的正方形），因此是对居中行为的严格推广；**`cx = null` 的编号像素一位未动**。

**当前 `head-windows.mjs` 全表仅 6 个编号填了 `cx`**（按**立绘编号**索引）：

| 立绘编号 | cx | 角色 |
|---|---|---|
| 04 | 0.620 | 玄铁 |
| 07 | 0.390 | 紫苑 |
| 17 | 0.360 | 千鹤 |
| 26 | 0.680 | 伊莲 |
| 36 | 0.620 | 梅莉 |
| 38 | 0.700 | 艾丝黛 |

> ⚠️ **索引口径要点**：`_portrait-swap/head-windows.v2.md` 的逐张读数表按**源图文件号**（`image/NN.png`）编号，其 6 个 cx 是 `02=0.39 / 11=0.68 / 12=0.62 / 21=0.70 / 34=0.62 / 37=0.36`；交付的 `scripts/head-windows.mjs` 按**立绘编号**索引，两者相差一个 `M` 的逆映射（`源02→07`、`源11→26`、`源12→04`、`源21→38`、`源34→36`、`源37→17`）。**改表前先确认自己在用哪套编号。**

#### 50.10.4 头部窗口表的标定方法

- 头部位置在 40 张图里**不是统一构图**：头顶在 0.01–0.20 之间浮动、肩线在 0.16–0.34 之间浮动，还有宽檐帽 / 斗笠、头纱、巨大蝶翼耳饰、高举武器等特殊构图。
- 自动方案（按行宽找肩线 / 按肤色连通块找脸 / 按肤色滑窗找脸）**三种都在 40 张里翻车**（会被手 / 腿 / 胸口 / 披风抢走）。
- 最终做法：把每张图配 **1% 精读标尺**人工读数，写进 `HEAD_WINDOWS` 表（`{ h0, h1, cx }`，均为占源图的高度/宽度比例）。

**特殊构图处理规则：**

| 类型 | 涉及编号 | 处理 |
|---|---|---|
| 宽檐帽 / 斗笠 | 15（斗笠）、31（女巫帽） | `h0` 取到帽顶上方，保证帽顶不切；**帽檐横向超宽，居中横切会切两侧檐（对称，画幅固有，对中救不了）** |
| 头纱 / 蕾丝罩 | 10、19 | `h0` 含纱顶；薄纱外缘被切可接受 |
| 角 / 荆棘冠 / 鹿角 / 银冠 / 金羽翼饰 | 15、18、24、35、36 | `h0` 一律取到角/冠尖上方（多为 0.005 或 0.000） |
| 兽耳 / 蝶翼耳饰 / 羽翼 | 03、08、17、40 | `h0` 含耳/翼顶；08 蝶翼、17 羽翼横向宽，外翼尖被切（对称） |
| 光环 | 09、16 | `h0` 含光环顶 |
| 高举武器 / 举臂 | 05、06、20、27 | 若武器/手臂在头顶上方，`h0` **不**跟着武器走，只框头部（20 与 30 是特例：头本身在画面中部，需整体下移） |
| 侧身 / 脸偏侧 | 02、11、12、21、34、37（= 立绘 07、26、04、38、36、17） | 用 `cx` 横向对中修复 |

**改动最大的 5 张（新批次 v2 表）**：`30`（0.010–0.235 → 0.120–0.350）、`17`（0.065–0.330 → 0.005–0.220）、`20`（0.020–0.190 → 0.100–0.275）、`16`（0.030–0.315 → 0.010–0.235）、`19`（0.030–0.290 → 0.005–0.220）。旧表在 `20`、`30` 上会**切掉整张脸**。

> ⚠️ **画幅比影响（换批次必须重读表）**：旧批次 704×1472（0.478）→ 新批次 1088×1920（0.567），画面更宽。`cover` 后的横向可视带宽度 = `(h1−h0)·H/W`：**同样窗口在新图上横向可视更窄**，两侧最宽的头发/帽檐/耳饰更易被切。旧表逐张数值**不可直接沿用**，必须在新图上重新读数。

#### 50.10.5 重跑管线的标准操作

**重跑命令：**

```
node scripts/build-portraits.mjs
```

**可选开关**（不传时默认值与历史行为逐字节一致）：

| 开关 | 作用 | 默认 |
|---|---|---|
| `--src=<目录>` | 源立绘目录 | `image`（相对项目根） |
| `--heads=<文件>` | 头部窗口表模块（需导出 `HEAD_WINDOWS`，**按立绘编号索引**） | `./scripts/head-windows.mjs` |
| `--out=<目录>` | 输出目录 | `public/assets/portraits` |
| `--manifest=<文件>` | 写出 `{ nid: { file } }` 血缘清单（供取证） | 关闭 |

示例（写临时目录，**不污染线上资产**）：

```
node scripts/build-portraits.mjs --src=image2 --heads=scripts/head-windows.mjs \
  --out=_portrait-swap/preview --manifest=_portrait-swap/preview-manifest.json
```

**运行时守卫**（任一不满足即报错退出，错误信息带具体缺失/重复项）：

1. 源目录必须**恰好 40 张 PNG**（报错含实际张数 + 目录名）；
2. `PORTRAIT_SOURCE` 键恰好 `01`–`40`；
3. 其值 40 个文件名**互不相同**且**都存在**；
4. 每个编号都能在窗口表里找到标定；
5. 源图画幅比与基准 `704×1472` 相差 >5% 时**仅提示、不退出**（换 1088×1920 属预期）。

依赖：`C:/Python314/python.exe` + Pillow（Node 侧没有图像库）。

#### 50.10.6 完整换图流程（新增批次时照做）

1. **备份**：`.backup/image-v1/`（旧源图）、`.backup/portraits-before-swap/`（线上 240 个产物逐字节副本）、`.backup/head-windows.v1.mjs`、`.backup/build-portraits.v1.mjs`、`.backup/characters.before-desc-fix.json`。
2. **换源图**：按编号把新图覆盖进 `image/`（先临时名再覆盖），逐文件哈希比对 0 处不符。
3. **更新 `PORTRAIT_SOURCE`**：值改成新源图文件名（现为角色名）。
4. **重读头部窗口表**：在新批次上逐张重新读数 → 更新 `scripts/head-windows.mjs`（旧表备份到 `.backup/`）。
5. **换图前回归（关键）**：用**旧图 + 旧窗口表**跑新逻辑，与线上 240 个产物逐像素比 ⇒ 应 **238/240 全等**，唯一差异恰为当时唯一填了 `cx` 的那一张。这一条同时证明"查表逻辑正确复现了历史排列"与"`cx` 只对填了 cx 的编号生效"。
6. **正式重建**：`node scripts/build-portraits.mjs`（可加 `--manifest`）。
7. **取证 + 门禁**：见 §50.10.7；`tsc --noEmit` exit 0；`node scripts/verify.mjs` 失败 0。
8. **文案同步**：对照新图核对 `characters.json` 的 `description`，改完重跑 `gen-tables.mjs`（13 个 `*.gen.ts` 同步更新）。
9. **目检**（不可省）：见 §50.10.7 第 5 层。

#### 50.10.7 核验五层（从硬到软）

| 层 | 内容 | 判据 |
|---|---|---|
| **第 0 层 · 端到端复现**（最强） | 重跑一次管线到临时目录，与线上 240 个文件**逐字节比对** | 实测 **240/240 逐字节相同**（含 120 个 WebP） |
| **第 1 层 · 血缘证明**（唯一能证明"编号没被冲乱"） | 对 40 个编号断言 `full_XX.png ≡ build_full(image/M(XX).png)`（逐像素） | **40/40**。**负对照必须有**：同样算 `build_full(image/XX.png)`（生搬"文件号=立绘号"），命中数须 **≪40**，实测 **0/40** ⇒ 断言有区分力 |
| **第 2 层 · 结构性自检**（`node scripts/verify.mjs`） | §8（40 组 PNG/WebP 齐备、每个 `characters.json.portrait` 有对应文件）；**§38.1** 尺寸与色彩类型（PNG IHDR + WebP VP8X/VP8L/VP8 头）；**§38.2** 每张 PNG 的 alpha 覆盖率 ∈ `(0.10, 0.97)`（下界防空图、上界防忘抠图的白底矩形）；**§38.3** `PORTRAIT_SOURCE` 自洽；**§38.4** 静态禁止 `build-portraits.mjs` 用"排序下标当编号"；**§38.5** 静态禁止旧的 `analyze-portraits.mjs` 再生成图片；**§38.6** 源图文件名 == 角色 `name` | 实测 **250 项 / 失败 0**（原 244 → 加 §38 五条 249 → 加 §38.6 为 250）；alpha 覆盖率实测 **33.7% – 90.6%** |
| **第 3 层 · 源码开关回归**（换图前跑） | 见 §50.10.6 第 5 步 | 238/240 全等 |
| **第 4 层 · `cx` 生效性判别** | 填了 `cx` 的 6 个 → 必须"两变体**不相等**"；未填的 34 个 → 必须"产物 **== 居中变体**" | 实测：6 张全部改变像素（差异 **76–89%**），34 张逐像素等于居中变体，**异常 0** |
| **第 5 层 · 目检**（**不可省**，唯一能验"角色身份"的一层） | 把线上已交付的 `portrait_01..40.png` 排成对照图，配 `characters.json` 的 `name/title/description` 逐张核对"长相 == 设定" | 独立复验 **身份 40/40 全对** |

> 🔴 **不要用剪影 / 色彩相似度判"是不是同一个角色"**：两批图**画幅比不同**（0.478 → 0.567）且新图是**不同姿态的重绘**，自动指标在 40 张里只有 **6/40**（另一条独立路径甚至只有 **4/40**）命中同编号，**纯属假象**。**判定同角色只能靠目检。**
>
> 目检重点样本：02 云漪（斗笠 + 蓝龙角 + 龙尾）、08 雪铃（狐耳银发）、10 可可（巫女帽 + 花束）、17 千鹤（持弓）、20 摩耳斯（黑洛丽塔 + 巨镰）、29 芙兰（女仆装 + 巨斧）、31 塞蕾娜（蓝发天使 + 白羽）、37 蒂法（双马尾）、40 蝶（和服 + 蝶翼）、26 伊莲（紫长发 + 白礼服 + 蓝披肩）、13 凯瑟琳（蓝军装 + 巨弩）。

> 🔴 **不要为验证而重跑浏览器探针**（会新生成约 90 MB 的 Edge profile）；完整性用 `tsc` + `verify.mjs` 证明即可。

#### 50.10.8 回滚与遗留

- **回滚**：`public/assets/portraits` 可从 `.backup/portraits-before-swap`（线上逐字节副本）整体还原；`image/` 可从 `.backup/image-v1` 还原。
- **已修的地雷**：`scripts/analyze-portraits.mjs` 的 `--build` 分支编号来自 `readdirSync().sort()` 的**排序位置**，跑一次会把 240 个产物整体冲乱并用更差的裁剪覆盖 ⇒ 现改为**拒绝执行**（醒目报错 + `exit 1`）并指向真源 `build-portraits.mjs`，保留其色彩分析功能；`README.md` 的过期指引已同步修正。
- **已知代价（已接受）**：5 张图的横向外缘被削 —— `08`（蝶翼耳饰）、`15`（斗笠檐）、`31`（女巫帽檐）、`17`（黑羽翼）、`35`（荆棘冠）两侧同时超宽，新图画幅更宽导致居中裁切的横向可视带更窄，**属对称性限制，横向对中也救不了**；脸与帽/冠主体完整，只削最外侧的薄翼/檐尖。
- **过期中间产物（未删，保留可回溯性）**：`image/_work/`（`raw_01..40.png`、`preview/`、`preview2/`、`_sheet/sheet.png`、`g1..g5.png`、`mapping.json`）与 `assets-src/matted/` 描述的是**旧批次**。特别是 `image/_work/mapping.json` 已过时（记录旧批次长文件名 → 编号），**以 `scripts/portrait-sources.mjs` 为准**。

---

### 50.11 立绘编号 → 角色档案（40 人，`image/_work/design-matrix.md`）

**系统定位（已拍板）**：

- **无职业系统**：`classes.json` 已删除，角色不再有 `classId`；「类型」由**武器 + 属性倾向**表达，不做结构化字段。
- **每角色仅 2 个主动技能**：`CharacterCard.skills` 恰好 2 槽。固有技能栏与固有被动全部删除（`innateSkills` / `innateSkillPool` / `innate_*` / `passive_*` 均不再存在）。
- **无图鉴**（D106 / D117，见 §50.5）。
- 技能 id 前缀 = 角色代号（如 `01 → srf`、`27 → ev`），全部 `type: active` 且带 `enhanced`。
- 六维**总和恒为 60、每项 ≥ 1**。

**稀有度分配**：5★ **9** 名 / 4★ **21** 名 / 3★ **10** 名 = 40 名。

| # | 姓名 | 称号 | 星 | 技能代号 | 武器 | 六维倾向 |
|---|------|------|----|----------|------|----------|
| 01 | 塞拉菲娜 | 白金蔷薇 | 4 | `srf` | 细剑 | MAG↑ SPR↑ LUK↑ |
| 02 | 云漪 | 碧海龙姬 | 4 | `yyu` | 长棍／法器 | MAG↑↑ SPR↑ |
| 03 | 凛 | 现代浪人 | 4 | `rn` | 双刀 | AGI↑↑ LUK↑ |
| 04 | 玄铁 | 黑金武士 | 4 | `xt` | 太刀 | STR↑ END↑ |
| 05 | 瓦伦蒂娜 | 赤凤壁障 | 4 | `vln` | 巨盾+剑 | END↑↑ STR↑ |
| 06 | 艾莉丝缇雅 | 白龙圣女 | 5 | `alst` | 法杖 | MAG↑↑ SPR↑↑ |
| 07 | 紫苑 | 紫栉剑客 | 4 | `sn` | 打刀 | AGI↑ STR↑ |
| 08 | 雪铃 | 银狐剑士 | 4 | `xl` | 长刀 | AGI↑↑ LUK↑ |
| 09 | 罗莎琳德 | 荆棘姬 | 4 | `rsld` | 荆棘鞭 | MAG↑ LUK↑ AGI↑ |
| 10 | 可可 | 花语魔女 | 3 | `kk` | 扫帚 | MAG↑↑ LUK↑ |
| 11 | 玛德琳 | 银盘女仆 | 4 | `mdl` | 短刀+托盘 | AGI↑ LUK↑ END↑ |
| 12 | 薇奥拉 | 星辰占者 | 5 | `vl` | 星盘 | MAG↑↑ SPR↑ |
| 13 | 凯瑟琳 | 苍蓝弩手 | 4 | `kth` | 重型弩 | AGI↑ MAG↑ |
| 14 | 佩可 | 红白战镰 | 3 | `pk` | 长柄镰 | STR↑ AGI↑ |
| 15 | 妮克丝 | 暗精灵枪兵 | 4 | `nx` | 长柄枪 | AGI↑ MAG↑ |
| 16 | 诺克斯 | 黑羽魔姬 | 5 | `nks` | 魔爪 | MAG↑↑ AGI↑ |
| 17 | 千鹤 | 和风弓道 | 4 | `qh` | 长弓 | AGI↑↑ LUK↑ |
| 18 | 灰鸦 | 无音枪手 | 3 | `hy` | 步枪 | AGI↑↑ LUK↑ |
| 19 | 贝亚特丽丝 | 锁链圣女 | 4 | `byt` | 连枷 | STR↑ END↑ |
| 20 | 摩耳斯 | 死神镰刀 | 5 | `mes` | 巨型镰刀 | STR↑↑ MAG↑ |
| 21 | 缇娅 | 龙尾双刃 | 4 | `ty` | 双短剑 | AGI↑↑ LUK↑ |
| 22 | 欧菲莉亚 | 黑翼骑士 | 5 | `ofl` | 重剑 | STR↑↑ END↑ |
| 23 | 索菲娅 | 紫典书士 | 4 | `sf` | 魔导书 | MAG↑↑ SPR↑ |
| 24 | 琳 | 钩爪刺客 | 3 | `ln` | 钩爪 | AGI↑↑ LUK↑ |
| 25 | 幽荧 | 紫焰剑士 | 3 | `yy` | 魔剑 | MAG↑ STR↑ |
| 26 | 伊莲 | 蓝月名媛 | 3 | `yln` | 手袋／扇 | LUK↑↑ MAG↑ |
| 27 | 艾尔薇拉 | 绯冠女帝 | 5 | `ev` | 权杖 | MAG↑↑ SPR↑↑ |
| 28 | 葛蕾丝 | 黑红佣兵 | 4 | `grs` | 巨剑 | STR↑↑ END↑ |
| 29 | 芙兰 | 战斧女仆 | 3 | `fl` | 巨斧 | STR↑ END↑ |
| 30 | 罗莎莉 | 粉盾骑士 | 4 | `rsl` | 战戟 | END↑↑ STR↑ |
| 31 | 塞蕾娜 | 白翼圣女 | 5 | `srn` | 细剑+羽 | MAG↑↑ SPR↑↑ |
| 32 | 卡蜜拉 | 暗金双剑 | 3 | `cml` | 双剑 | AGI↑↑ STR↑ |
| 33 | 蓝缇雅 | 苍枪骑士长 | 4 | `lt` | 长枪 | END↑ STR↑ SPR↑ |
| 34 | 露娜 | 圣银骑士 | 5 | `lna` | 剑+盾 | END↑↑ SPR↑↑ |
| 35 | 莉泽 | 赤白枪骑 | 4 | `lz` | 双刃枪 | AGI↑ STR↑ |
| 36 | 梅莉 | 蓝白执事 | 3 | `ml` | 巨剑 | STR↑ MAG↑ |
| 37 | 蒂法 | 蝙蝠战姬 | 4 | `tf` | 巨斧 | STR↑↑ AGI↑ |
| 38 | 艾丝黛 | 银环剑姬 | 3 | `esd` | 双细剑 | AGI↑↑ MAG↑ |
| 39 | 白雪 | 白之剑姬 | 4 | `bx` | 细剑+镰 | AGI↑↑ LUK↑ |
| 40 | 蝶 | 蝴蝶妖姬 | 5 | `de` | 薙刀 | MAG↑↑ AGI↑↑ |

5★ 名单：06 艾莉丝缇雅、12 薇奥拉、16 诺克斯、20 摩耳斯、22 欧菲莉亚、27 艾尔薇拉、31 塞蕾娜、34 露娜、40 蝶。
3★ 名单：10、14、18、24、25、26、29、32、36、38。

合计 **80 条角色专属主动技能**（每名 2 条）+ 8 条 `enemy_*` = 88 条。

> 换图时与 `characters.json` 冲突并已修订的 3 条 `description`：02 云漪「以斗笠**遮去**龙角」→「斗笠**遮不住**龙角」；09 罗莎琳德「白裙与**黑纱**」→「白裙与**头纱**」；18 灰鸦「**银白**短发」→「**黑色**短发」。其余 37 条逐张核对无冲突、原样未动。

---

### 50.12 动效时长与交互约定（补录）

| 场景 | 时长 | 缓动 | 出处 |
|---|---|---|---|
| 镜头对准队伍 | 140ms | `Sine.easeOut` | `DungeonScene` |
| 卡牌抬升 / 回落 | **120ms** / 150ms（草案曾提 200ms，见 D27） | `Quad.easeOut` | `ui-style-spec.md` §3.4 |
| AP pips 逐个亮起间隔 | 80ms | — | `battle-ap-ux-spec.md` §1.3 |
| 点击不可用技能抖动 | 120ms | — | `battle-ap-ux-spec.md` §2.2 |
| 图鉴扫描线（已作废） | 2400ms（yoyo），条目错开 index×300ms | `Linear` | `ui-style-spec.md` §4 |
| 营地全息信标浮动 | 1800ms（yoyo） | `Sine.easeInOut` | `ui-style-spec.md` §6 |
| toast 存活 | 2000ms | — | `UIScene` |

**交互约定：**

- **点击热区**：普通按钮严格等于绘制尺寸；图标按钮 `iconMode` 取字形+padding 与指定尺寸的较大者（上限 40px）；圆形按钮圆形热区。
- **滑动列表**：滚轮 + 拖拽（带惯性） + 滚动条；拖拽超阈值（8px）**取消条目点击**；键盘 ↑↓ / PageUp / PageDown / Home / End。
- **ESC**：对话框打开时接管，避免"一次 ESC 既关对话框又开菜单"。
- 按下后移出按钮（或移动 >8px）再松手**不触发点击**。

---

### 50.13 ⚠️ 待澄清项汇总

| # | 事项 | 双方口径 | 本章取用 |
|---|---|---|---|
| 1 | **治疗类技能文案 vs 实现** | 文案写「恢复 X% **魔力**值的生命」；实现用 `team.attack`（筋力派生）。同类技能（鹿角祝福 / 花语祝福 / 茶点侍奉 / 天使赐福 / 银盾庇护 …）全部如此 | 按实现给出公式（§50.8.4），**是否修正文案由策划裁决** |
| 2 | **经验倍率被应用两次** | 队伍一次（`reward.exp`）+ 成员一次（`gainExp`），`Σ魔力 = 50` 时实际入账约为设计值 2 倍 | 按实现给出公式（§50.8.5），**是否视为 bug 由策划裁决** |
| 3 | **`expExponent`** | GDD **D33 / D256 裁决 1.9**；`balance.json` 与 `balance.gen.ts` 现值 **1.6** | 公式按现值 1.6 给出并加注；**裁决未落地** |
| 4 | **逃跑在 BOSS 战是否禁用** | `battle-ap-ux-spec.md` §1.4 写「BOSS 战禁用（若有该标记）」，但**代码无 BOSS 标记**（GDD 已知问题第 7 条） | 取「暂保持全可逃，BOSS 标记后续单独立项」（该规格书 D4 的建议值） |
| 5 | **招募定价** | GDD 书面口径 **5 000 金 + 2 科技点**（D67 取消递增、D306 → 3 500、D310 → 5 000）；**但用户后续另行裁决「平价 500」**，`balance.json` 现值 **500 / 1 / 0** | ✅ **以现行为准：500 金、无科技点、无递增**（证据：`src/ui/RecruitPanel.ts` 留有裁决原话「🔴 用户裁决：平价 500」并说明 `recruitCostGrowth` 恒为 1 ⇒ `growth^n = 1`）。GDD 那三个数字**已被后续裁决取代**，勿再照旧值办事 |
| 6 | **四图首通科技点** | 配表与 GDD 货币章 = **8 / 9 / 10 / 11（合计 38）**（D246 / D255，验收硬门 D297 用 38）；GDD §7.7 地牢表仍写 **3 / 3 / 5 / 10（合计 21，D23 旧价）** | 取 **38**；GDD §7.7 表**未同步** |
| 7 | **`battle-ap-ux-spec.md` §7 的 D1–D5** | 是该规格书自有的待拍板序号，与 GDD 裁决编号不同源（GDD D1 = 共享生命池、D2 = 第 6 层固定 Boss） | 本章按「规格书自有序号」标注，不与 GDD D 编号混用 |
| 8 | **技能栏 / AP pip 的代码现值** | D25（菱形 10 槽）、D26（156×46 横向条）已裁决，但代码现值仍是平行四边形与旧形态 | 按 GDD 裁决写，**代码需改** |
| 9 | **角色卡尺寸** | D24 裁决 168×340；代码现值 168×400（`CONSOLE_H` 当初为 400 加高） | 按 168×340 写；**代码需回调并同步卡内元素坐标** |
| 10 | **`levelUpTechPointBonus`** | 存在于定义与生成物，`src/` 内无读取点 | 标为死配置，不参与计算 |

#### 50.13.1 编辑附注（主理人实测核实 · 2026-09-22）

> ⚠️ 本节的返工被 **429 限流中断**，部分条目未能完成"三源核实"（GDD／后续工作日志／当前代码）。以下两点由主理人**实测核实**后就地更正：
>
> - **第 5 条「招募定价」已改判**：5 000 / 3 500 是 GDD 的书面旧值，**用户后续另行裁决「平价 500」**，`RecruitPanel.ts` 里保留裁决原话与 `recruitCostGrowth` 恒为 1 的说明 ⇒ **现行定案是 500 金、无科技点、无递增**。此条**不是待澄清**，是"规格已过时"。
> - **本节其余条目的性质需分层看待**：表中既有**真未决**（如"经验倍率是否视为 bug"、"BOSS 战是否禁逃"），也有**已被后续裁决解决、只是规格没更新**（如首通科技点取 38 —— D246 / D255 已定，GDD §7.7 表未同步）。
>   **使用前请按"旧值 → 现值 + 证据出处"再核一遍**，尤其涉及**数值**的条目。
>
> 🔴 **本项目最贵的一类错误是"把快照当成现值"**：草案与规格书是**当时**的快照，不会回溯更新；而**本项目非 git**，草案之后的决定大量只记在逐日工作日志里 ⇒ **判定"某个冲突是否还在"必须同时查 GDD、工作日志与当前代码注释**，不能只读文档。

---

## 90. 更新日志（按时间顺序）

> 说明：本项目**非 git 仓库**，本日志由逐日工作日志 + GDD 决策记录 + 查漏补缺轮次重建。日期为**文档记录的实际工作日**，未记录的日子不臆造。

### 2026-09-16 —— 修 `file://` 打不开 + 迷宫侧栏改顶部信息带

- **工程**：`index.html` 加 `file://` 兜底提示（2.5s 检测无 canvas 就换文案）；新增 `scripts/build-offline.mjs` 产出单文件内联版 `dist/index.offline.html`（约 1450 KB，立绘仍走 `assets/portraits`）。根因是 `<script type="module">` 在 `file://` 下被 CORS 拦截、`PreloadScene` 从未运行。
- **设计**（用户确认）：**删除 `DungeonScene` 左侧 320px 状态栏**，信息收进**顶部横条 HUD**，地图占满全屏 + 相机重新居中。
- **工程**：`constants.ts` 的 `SIDEBAR_W = 320` → `TOPBAR_H = 64`；`src/ui/HUD.ts` 整文件重写为「地牢名/层数/种子 · HP/MP 细条 · 出战人数/探索度/菜单」三组横向布局，出战成员从 62px 竖排改为 **34px 头像条**。
- **工程**：`DungeonScene` 相机视口改整屏，方向键回左下角、横条下缘加 34px 渐隐遮罩。
- **产物**：`scripts/debug.mjs` 的 1k 小节 5 条断言反转（**102/102**）；`shot-ui.mjs` / `analyze-ui-shots.mjs` 的像素地标从竖向侧栏右缘改为横向信息带下缘；`README.md`「迷宫界面布局」整节重写，断言数 101→102。
- **验证**：`tsc --noEmit` exit 0 · `debug.mjs` **102/102** · `smoke.mjs` exit 0 · `verify.mjs` **44/44** · `analyze-ui-shots.mjs` 全通过 · `vite build` exit 0 · `build-offline.mjs` 产出 1486938 B。
- **教训（已沉淀为排查方法论）**：头像「空框」的真因是**测试注入存档漏了 `portrait` 字段**（`portraitKey(undefined)` → 纹理不存在 → 走降级占位分支），生产代码本来就是对的。判定顺序固定为：先 dump 子对象 `type`/`texture.key`（**「有没有 Image」** 一步分出数据问题还是渲染问题）→ 再做**同帧单变量对照实验**（`A-live` 与 `B-paused` 两张截图**逐字节相同**，证伪了 pause 猜想）。
- **环境坑**：headless 浏览器的 `--user-data-dir` 放在仓库内会让 vite FSWatcher 因 `EBUSY` 整体崩溃；`vite build` 的 `EPERM: dist/assets` 是残留 msedge 进程占目录，**失败的 build 会清掉一半 dist**，须重跑 build + build-offline。

### 2026-09-17 —— GDD v2.0 与 100 轮裁决（D1–D456），同日并行的战斗/迷宫改造

- **设计（主线）**：`design/gdd.md` **GDD v2.0** 落成——§0 术语表与**决策记录**、§1–§9 正文、§10 已知问题、附录 A 废弃设计 / B 死字段 / C 问题池 / D 相关文档。**明确它的定位是对既有实现的逆向整理**：真源优先级为「代码实现 + 配表 JSON > GDD > README（已过期）」。
- **设计（投入量）**：**100 轮问答、累计 456 条裁决 D1–D456**。第 1–13 轮与第 21 轮在 GDD §0.5 里带 **2026-09-17** 日期；第 14–20、22 轮及之后未标日期。里程碑：第 27 轮达 D100、第 30/40/50/60/90 轮为检查点、第 97 轮起收口。
- **设计（早期定案 D1–D43）**：**D1** 真共享生命池（战斗与迷宫同一份）· **D2** 每图第 6 层固定 Boss 即通关、不做全局结局 · **D3** 永久单敌、删 `enemy_all` 与 `count` · **D4** 三支柱（AP 战术 / 迷宫探索与风险管理 / 收集养成，**排除硬核生存压力**）· **D5+D15** `apCost` 按 `power` 分档 `≤60→1 / 61–120→2 / >120→3` · **D7+D14** 敌人数值线性缩放 + 按层切池 · **D11** 删 `element` · **D12+D18** 启用永续升耗（人工挑 5–8 条）· **D42** 死字段全清 + 删 `src/entities/` 孤儿目录。
- **设计（中期定案）**：**D33** 升级曲线 `expExponent` 1.6→**1.9** + 方案 A（无根 density 0.035→0.060、slime/bat/rat exp 14/12/16→20/17/23）· **D35** 苔痕 density 0.045→**0.064**（0.075 会让台阶倒挂）· **D41+D297** 科技树定价等比 ×0.48、最终 **21 节点 / 99 点** · **D73** 新增全局难度系统（简单/普通/困难/噩梦 + 噩梦 4 个可选机制）· **D200** buff 全体系改「战术」· **D219** 取消战术 CD 改「持续回合」（D205 作废）· **D231** 战术授予**不加字段**，由 `effects[].stat` 机械推导 · **D242** 每个技能每回合限 1 次（D9 修订版，D9 原方案代码里根本没有锁）。
- **设计（收口 D421–D456）**：**D437** 族群战术改**占位式**（D425 被推翻：首领级技能位本就排满、虚影四图共用 id ⇒ 原方案会让 85% 的 HUD 空槽）· **D449–D451** 新增 ★4 消耗品「**升阶谕令**」（全队各升 1 级，600 金 + 每店限购 1）· **D453** 难度倍率**不作用于防御**（否则噩梦第 6 层石魔像 = 我方只能打 5%）。
- **产物**：`docs/formulas.md`（公式速查 13 节 + 常量来源对照：配表可改 30 项 / TS 常量 12 项 / **硬编码在逻辑里 45 项**）；`design/drafts/acceptance-matrix-v1.md`（12 条硬门）；`design/drafts/handoff-to-implementer-v1.md`（一页纸交接）；**11 份内容草案**在 `design/drafts/`。
- **工程（战斗改造第二轮）**：彻底删除选敌交互（`enterTargetMode` / `submitTarget` / `pendingSkill` / `targetZone` 等，`Mode` 收敛为 `command|busy|finished`，42 个单体技改自动锁定唯一敌人）；**删除冷却系统**（`BattleUnit.cooldowns`、`CombatSystem.cooldownOf()`、`endRound` 递减循环全删，`Skill.cooldown` 保留但标 `@deprecated`）。
- **工程（AP 升耗取代冷却）**：新增 `Skill.escalation?: 'temp' | 'permanent'` 与 `usedLastRound` / `usedThisRound` / `permStack` 三个字段；扣费唯一真源 `CombatSystem.skillCost()`。修掉**永续升耗双重计费**（原 `base+temp+perm` 对 permanent 会算出 `1,3,4,5`，正解 `1,2,3,4` ⇒ 两种升耗**互斥只取其一**）。
- **工程（迷宫难度 N1–N6）**：新增 `src/config/mazeDifficulty.ts` 作**唯一真源**（TS 模块，不是配表）；N1 15×15 / 20 步 / 0 支路 → N6 35×35 / 90 步 / 10 支路，长度单位统一为**玩家步数**；`MazeGenerator` 重写为「深度受限随机 DFS 找恰好 N 步主干 + 按 branchCount 开凿死胡同 + 外圈设虚空」。修掉 3 个真问题（步数恒为 2×、支路数常少 1–3 条、支路长度测出 30+）。
- **工程（配色与镜头）**：新增 `Palette.mazeRoad 0xFFD700` / `mazeWall 0x00008B` / `mazeVoid 0x000000`（**刻意不复用**面板用的 `floor/floorAlt/wall`）；相机去掉边界夹取改为跟随队伍；**双相机方案**（世界相机 `setZoom`、UI 相机 zoom 恒 1，判据做成「除两个世界图层外全是 UI」+ `WeakSet` 去重）。第一版「按难度自适应格子尺寸」被用户要求推翻。
- **验证**：难度探针（6 档 × 10 种子）**52/52**；`zz-esc-probe.mjs` **8/8**；`ap-edge-probe.mjs` **9 PASS / 1 WARN**（⚠ WARN：`submitAction()` 不校验 AP，属设计取舍）；`debug.mjs` **111/0**；`verify.mjs` 45 → **46**；`tsc` 0；`vite build` exit 0。
- **反推出的重大发现（写进 §10）**：`escalation` 88 条全未配置；`apCost` 80 条角色技能全为 1（AP 退化成行动次数）；迷宫 HP 池与战斗 HP 池**完全不连通**；「共享生命池」名不副实（每人各持一份相同 hp、敌人恒打血最少者才伪装成共享）；**经验倍率被计算两次**；`isBoss` 字段 0/14；`element` 7 种取值无任何战斗逻辑读取；`GameState.tick()/autosave()` 无人调用（**游戏时长永远是 0 但 UI 处处显示**）。
- **数值核算（真跑代码，非估算）**：D19 升级曲线蒙特卡洛 2000 次/组；D99 难度 MC **96 格 × 120 场**，实测 **TTK 仅 1.00–2.13、96 格胜率全 100%、0 场团灭**，根因是**我方单回合总输出 ∝ N²**（`team.attack` ∝ N 且每成员各出手一次），配表修不了 ⇒ 写入 §0.6 与 §10-32。
- **推翻与勘误（同日）**：D8（团灭代价）**被 D17 取代**；D164 **被 D179 实际推翻**（改为「战斗回满、迷宫伤害不跨场」）；§10 旧结论「`combo_bonus` 是 no-op」**被更正为不是**（`skillComboBonus()` 真的读 effects）；**D203** 承认 D33/D35 的定标建立在漏了「敌人密度随层放大 ×1.60」的错误战斗数上，四图升级实为 **2.69 / 3.43 / 4.67 / 7.33**。
- **方法论沉淀**：每轮做**「裁决链一致性检查」**（本日抓到 5 处跨裁决冲突，如 D113↔D126、D110/D112/D116 汇率口径叠成 1 金 = 0.5 经验 ⇒ D120 定回 1 金 = 2 经验）；**「要不要加字段」以「是否制造第二真源」为判据**（D231 由队员提出、否掉了 lead 的 `tactic` 字段方案）；**凡「把 X 挂到 Y 上」的裁决必须同时给出 Y 的剩余容量**（D437）；**同一件事可能被修订多次**（D266→D298→D331）。
- **复核记录**：§0.5.1 **2026-09-17** 对 §8 美术/UI 与 §0.3 常量归属做独立第三方复核，查出 **9 处问题并全部修正**——其中 3 处错在「用途栏」而非「数值栏」，2 条「已知冲突」是**臆造**（跨文件串味），1 处漏了第三档立绘 `full` 512×1024。
- **环境坑**：测试脚本**必须用 Node 24**（托管 Node 22.22.2 跑 `verify.mjs` 必红 1 项，`Cannot find module 'phaser3spectorjs'`）；`taskkill /F /IM msedge.exe` 会误杀用户真实浏览器（本机 26~33 个全是），**禁用**；`vite.config.ts` 的 `server.watch.ignored` 没覆盖根目录 `*.log`，在项目根写日志会让 dev server 以 `EBUSY` 崩服。

### 2026-09-18 —— 实施阶段开工（查漏补缺第 1/2/3 轮）+ 三个输入层 bug + 回合制紧急修复

- **设计/实施**：按 GDD §14 落地顺序开工，**备份先行**（`H:/work/maze-rpg-backup-20260917160952`，3495 项）。基线：gen-tables ✅ / tsc 0 / verify **46 项 0 失败**。
- **工程（实施第 1 轮 · 批次 1 A 组 11 条缺口）**：新增 `BuffStatKey = StatKey | 'attack' | 'defense'`；`types/skill.ts` 新增 **`heal_max_ratio`** 效果类型；**落点家族修复**（`applySupportEffects` 过去只收 `caster`，无论 `target` 写什么都挂施法者身上 ⇒ 改签名 `{caster, targets, skill, team, log, includeComboBonus}`，落点 = `targets.length ? targets : [caster]`）；**伤害技也要结算 effects**（`includeComboBonus: false` 是必须的，否则重复计数）；敌方侧补两个读取点（`resolveSingleHit` 的 `defenseRate` 加 `buffSum(target,'defense')`、`resolveEnemyAction` 的 `stubTeam.attack` 改 `round(attacker.attack × (1 + buffSum(attacker,'attack')))`）；`CombatSystem` AI 两处改 `power × hits`（原按 raw power 排序 ⇒ **多段技永远不会被 AI 释放**）。
- **工程（存档闸门）**：`SaveSystem` 新增 `keyOrNull()`（save/load/hasSave/delete 四处统一）；`SAVE_SLOT_COUNT` 3 → **10**；新游戏**强制选槽**（`openSaveSlots('load'|'new')`）。
- **工程（实施第 2 轮 · D104 防御改造 + G1 版本闸门）**：新增 `defendApCost = 1` + `defendDamageMultiplier = 0.8`；`defendReduction` 从防御率里拿掉，改在 `clamp(0.95)` **之后**乘独立系数（原写法名不副实）；`SAVE_VERSION` 4 → **5** 且真源移到 `src/config/saveKeys.ts`，`load()` 与 `importJson()` **两入口都拦非 v5**，原因写入 `SaveSystem.lastRejection`（UI 才能区分空槽/太旧/损坏）；存档列表改 `ScrollList`（一屏 5 行）+ 覆盖/删除二次确认。
- **产物（查漏补缺表）**：`design/drafts/gap-table-round1.md`（**2026-09-18**，10 个待拍板问题）→ `gap-table-round2.md`（**2026-09-18**，9 个新问题，其中问题 15「`party` 改对象数组的爆破面 20+ 处」定为最大风险项，取 **15b：先加并行数组 `partyEquip`**）→ `gap-table-round3.md`（12 项缺读取点 + 7 项文档不一致）。
- **工程（批次 A/E/B/C/D 推进）**：§12.1 的 11 条必修缺口全落；存档 v5 的 **17 字段**落地；配表扩到 **enemies 44 / skills 101 / accessories 12 / tactics 15 / events 11 / tech 21 节点·99 点 / items 19**；`gen-tables.mjs` 的 TABLES 从 8 扩到 **11**；技能文案 **93 条**入 `description`；新增 6 个营地侧 UI 面板（`DifficultyPanel` / `GuidePanel` / `StatsPanel` / `QuestPanel` / `ShopPanel` / `ForgePanel`）。
- **设计（第 101 轮 · 落地勘误 18 处）**：GDD 新增「**落地勘误（第 101 轮 · 2026-09-18）**」收口表。其中明确记着两条**被推翻的原文**：🔴 **D23 勘误**——「科技树总价约 80 点」的 80 是**首清四图的达成率目标**而非总价，树总价落地 **99 点（21 节点）**，实测达成率 77.1%；🔴 **D167 勘误**——锻造费「`price × 0.9`」现值 **× 0.6**（0.9 会让「自造」比「直购」贵、方向反），`accessories.json` 12 条 `forgeCostRate` 全为 0.6，**看到 0.6 是正确值，不要改回 0.9**（同族 **D171** 一并勘误）。另含机制型战术 4→**6**、我方战术池 9→**11**、`skills.json` 现役 **101** 条（93 是需文案量，勿据此反推删 8 条）、四图首通 3/3/5/10→**8/9/10/11** 等。
- **工程（输入层 bug 族 A/B：点空白关弹窗 + 热区半身位偏移）**：14 个面板手抄同一段遮罩样板，点在面板空白处命中的其实是遮罩 ⇒ 弹窗被关（在「选难度」这种必答向导里 = 丢弃整条新游戏流程）。收敛为 `createModalScrim(scene, {alpha, panel, onOutsideClick, center})` **三种语义**（关闭 / `() => undefined` 只挡不关 / 缺省只压暗）。查 Phaser 源码确认 `pointWithinHitArea()` 会把 `displayOrigin` **加到**局部坐标 ⇒ 凡 `setSize()` 过的容器写 `Rectangle(-w/2,-h/2,w,h)` 会让热区**左上偏移半个身位**（卡片 200×124 实测偏移 (-100,-62)）；`Graphics` 不受影响（无 Origin/ComputedSize）。收敛为 `setViewAlignedHitArea` / `setViewAlignedHitCircle`。连带修掉 `Button.setLabel()` 把热区悄悄改回错位置、`DifficultyPanel.ModRow` **禁用态渲染得与启用态一模一样**。
- **工程（bug 族 C/D：列表点选与视图状态）**：`ShopPanel.wireSelection` 把**内容坐标当视口坐标**判越界 ⇒ 滚动量越大可点窗口越小、翻过一屏后整列全灭（同款算术被抄了三份：`ShopPanel`/`ForgePanel` 一样错、`TechPanel` 无视口校验）⇒ 新增纯函数 `src/utils/listHit.ts` 的 `listRowIndexAt`（先视口边界、再内容下标）+ `ScrollList.rowIndexAtWorldY()` 作唯一出口。族 D：`rebuild()` → `removeAll(true)` → 新 `ScrollList` 的 `scrollY` 从 0 开始 ⇒ 点击即跳回顶部 ⇒ `ScrollList.viewState` 静态表 + 缺省键 `${scene.scene.key}#${x},${y},${w},${h}`（**跨重建稳定**，15 个调用点另有显式键）。
- **工程（🔴 紧急：回合制「我方回合完全动不了」）**：根因链是 D5/D15 把 `apCost` 从全 1 分档成 1/2/3（**41 条 = 51% 是 3 AP**），而 AP 池仍是 D9 的 3 起始 / +2 每回合 / 上限 8 ⇒ `0 < AP < 最便宜技能` 成为常态，而 `BattleScene.noUsefulAllyAction()` 的 `canAllyAct()` **只认技能** ⇒ 整个我方阶段被自动让掉。`canAllyAct()` 的立论自 **D104**（防御 1 AP）起已失效。修法：新增 `CombatSystem.canAllyFallback()`（`ap >= defendApCost`）+ 改写 `noUsefulAllyAction()`（有可放技能就不让出，只有 AP=0 才让出，回到 D9 原意），依据 **D123「防御就是兵底」**。改前 `{"rounds":1,"outcome":"ongoing","ap":0}` → 改后 `{"rounds":3,"outcome":"victory","ap":1}`。证据存 `docs/evidence/smoke-out/turn-order-fix-evidence.md`。
- **产物（探针）**：`scripts/zz-modal-scrim-probe.mjs`（CDP 派发真实指针事件，`before|after|dump` + `shop-before|shop-after|shop-no-restore`；**`before` 模式在运行时把旧约定重装回去**，同一份代码只变一个变量，不改源码）；`scripts/probe-savegate.mjs` 从 7 扩到 **11**；`zz-family-check.mjs` 18/18；`zz-tactic-grant-check.mjs` 15/15。
- **验证（当日逐段）**：`verify.mjs` 46 → **84**（实施段）→ **165**（§23 遮罩/热区 7 条）→ **169**（§24 列表命中 4 条）→ **171**（§25 视图保持 2 条）→ **173**（§26 自动让出 2 条）；`tsc` 0；`smoke.mjs` **18/18**（三类错误均「无」）；`vite build` exit 0（76 modules）。
- **修掉的假绿/假红**：`smoke.mjs` 的 `AP 规则`/`AP 升耗` 两条硬编码「每技能耗 1 AP」⇒ 改为从 `dl.skill(id).apCost ?? 1` 取基准；「存档写入/读取往返」原先**只是个 dump、零断言**（且可能读到上次运行残留在 profile 里的旧档，实测报 `hasVersion: 4` 而 `SAVE_VERSION` 已是 5）⇒ 改为真写一遍、真读一遍、版本与关键字段逐项对账，失败返回 `NO_SAVE_*` 哨兵。
- **教训**：**「把 bug 写成期望 / 把 dump 当断言」是本项目最贵的一类错误**（本次 bug 就在当晚自己的冒烟输出里被读过去了）；模板字面量承载页面源码时的转义陷阱连踩三次（反引号提前结束、单个 `\n` 被外层转成真实换行），靠 **`node --check`** 当场抓住。
- **遗留（未擅自改，交用户裁决）**：`apRegen` 提到 3 vs 下调 D15 分档阈值——51% 技能要 3 AP 而收入只有 +2/回合，「每回合一个大招」不可持续，与 GDD §3.2.1 的「一回合 2 次中技或 1 次大招」**互不自洽**。

### 2026-09-19 —— 战术全员化 + 迷宫内商店 + 技能表整体重写 + 重复角色强化 + 作弊码

- **设计/工程（战术改全员生效）**：`BattleState` 新增 `tactics: ActiveBuff[]`（**D231**：队伍级承载，**不复用** `BattleUnit.buffs`）；写入走 `DamageCalculator.applySupportEffects` 的 `teamTactics` 参数，**只挂 1 份**（D273，逐单位会 ×6）；读取改**并集** `CombatSystem.buffValueOf(unit, stat)`（自身 buffs + 队伍 tactics）；老化写进**同一个** `ageTimedEffects()` 函数体（`verify §16.4` 正则扫它，另起钩子会绕过）；UI 新增 `BattleScene.redrawAllyTacticRow()`，队伍 buff 行画在我方 HP 条下方（x=1060 / y=64 / depth 25）。
- **工程（顺手修掉同族第二条：全队治疗无效）**：`ally_all` 的治疗只回给施法者那一份 HP 副本，而血条与 `teamHpRatio()` 只读 `allyUnits[0]` ⇒ 放治疗的不是首发位时**血条一点不动**。修法：`applySupportEffects` 加 `allyPool` + `healSpotsFor(group)`；按当前生命取百分比的效果（`percent_hp_damage`/`dot`/`shield`/`damage_cut`/`heal_max_ratio`）**刻意保持原落点**（逐副本会真 ×6）。
- **工程（迷宫内商店复用营地商店页面）**：`ShopPanel` 加**注入模式**（`goods` / `source: 'shop'|'merchant'` / `title` / `buyItem`）；`EventSystem` 增 `merchantShopGoods()`。绕开三个坑（都已写成断言）：注入模式**不走 `isShopListable`**（否则归途石/升阶谕令被静默下架，§18.3/§21.2 要求商人必须卖它们）、**不掷折扣**（`rollDiscounts` 种子含 `Date.now()`，而 D423 ③ 规定迷宫事件不能重掷）、必须发 `DialogOpened`/`DialogClosed`（否则 `modalDepth` 不计数，ESC 会同时关商店并弹出迷宫菜单）。
- **工程（技能表整体重写，用户 7 条需求）**：`scripts/zz-skills-rewrite.mjs` 读 GBK 编码的 `skills.csv` + `skills.json` → 覆写并出报告；改前备份到 `smoke-out/_backup-skills-20260919.json`。落地：多段阶梯 hits=2→90 / 3→105 / ≥4→120；单段 apCost=1→power 120、2→210、3→315；**3apCost 恰好两条**（`skill_sn_clearsnow_iai` 清雪居合 · `skill_mes_reap` 生死收割）；**彻底删除 `element`/`cost`/`cooldown`**；附带战术的战术**上移到 `enhanced`**。
- **🔴 修掉的三个真缺陷**：① **敌方永远无法强化**——`enemyEnhanceRate` 在 `balance.json` 与 `balance.ts` 里都是 **0**，把敌方战术移进 `enhanced` **等于删掉这条减益**（不是低概率，是永不）⇒ 用户裁决：**敌方技能的战术一律留在 `effects`**（写入 `verify §30.5`）；② **`applyEnhancedEffects` 缺队伍级落点** ⇒ 补 `caster`/`teamTactics`/`allyPool` 三参数；③ **`−`(U+2212) 无法用 GBK 表示**，用户存 CSV 时被替换成 `?` ⇒ 描述里 33 处换成全角 `－`(U+FF0D)。
- **工程（商店点选粒度 行→格）**：⚠ **用户的字面描述与代码实际行为相反**（用户说「点选 1 格变为点选一行」，实际是点一张卡整行两张一起亮）⇒ 提问澄清后才没做反。新增纯函数 **`columnIndexAtWorldX`**（与 `rowIndexAtWorldY` 同序，**落在列间隙/滚动条带/视口外一律返回 `null`**）；`selectedGood` 语义 **行号 → 格下标**，clamp 上界改 `goods.length`；**只在选中真的变化时才 rebuild**。教训：动手前必须先用代码把「现在到底怎么画、怎么点」复述给用户确认一次。
- **工程（重复角色 → 强化原角色）**：裁决为**重复 ⇒ 原角色 +1 级**（复用 `CharacterFactory.levelUp`，与「升阶谕令」同一条成长链路）。`CharacterCard` 新增 **`templateId`**（原先没有，而 uid 形态不统一：开局角色 uid 就是模板 id、招募角色是 `char_char_01_<base36>_<4位>` ⇒ 无法可靠查重）；`GameState.addOrEnhanceRecruit()` 作**招募唯一收口**；`fillDefaults` 四段回落回填 `templateId` 且**不升 `SAVE_VERSION`**（升了旧档整档被拒）；`recordRecruit` **必须留在循环内**（D362，支线 S4 依赖它）。
- **🔴 发现**：`GameEvents.CharacterChanged` **全仓零监听者** ⇒ 发事件是空转，强化不新增单位也不发 `PartyChanged`/`RosterChanged` ⇒ UI **不会自动刷新**；真正的刷新靠 `RecruitPanel.doRecruit` 末尾显式 `redraw()`。**别以为 `emit` 了就刷了。**
- **工程（控制台作弊码）**：新建 `src/systems/Cheats.ts`（`cheatUnlockAllTech()` / `cheatAddGold(n)` / `installCheats()`），挂 `window.__MAZE_CHEAT__`。用户裁决：**「解锁全部科研点」= 直接解锁整棵科技树**（不是发一笔科技点）、**作弊码发布版也保留**。实现要点：走 `TechSystem.unlock()` 真实路径（直接写 `gameState.tech[id]` 会把 `applyEffects()` 与事件**静默丢掉**）、**多轮扫描**解前置、**自垫花费 + 结束精确还原**、`cheatAddGold` 必须做 `Number.isFinite` 守卫（传 `NaN` 得 `Math.max(0, NaN) === NaN` ⇒ **把金币永久污染成 NaN 且不自愈**）。
- **验证（当日三轮）**：`verify.mjs` 173 → **193**（§28 战术 8 条 + §29 迷宫商店 7 条）→ **201**（§30 八条）→ **210**（§31 按格点选 4 + §32 重复强化 5）→ **218**（§33 作弊码 8）；`tsc` 0；`smoke` 三类错误全「（无）」；`vite build` exit 0。探针 `zz-tactic-team` / `zz-team-heal` / `zz-maze-shop` / `zz-tactic-enhanced` / `zz-recruit-dup` / `zz-cheat.mjs dev|prod` 全绿。
- **运行时对照（关键证据）**：战术 `teamWithBuffs(B).stats.spirit` 48 → **56**，反向对照（只读 `actor.buffs`）得 **48**；治疗非首发位放「茶点侍奉」后 `allyUnits[0].hp` 1270 → **1630**、6 份副本**各 +360**，反向对照 `[0,360,0,0,0,0]`；重复强化（40 个模板全塞进 `characters`）人数 **40 → 40 不变**、「缇娅」**Lv 4 → 5**。
- **教训**：**不要对同一个文件并行发多个 `Edit`**（后写覆盖先写，导致两张截图只出一张）；**扫描 Phaser `Scene` 子对象要用 `scene.children.list`**（`scene.list` 不存在 ⇒ 静默返回空数组，把「什么都没扫到」伪装成「面板没出现」）；**`verify.mjs` 只许一个写者**（此前出过并发追加把前一节标题覆盖掉的事故）；**子代理给的断言锚点必须逐条核对**（`SS32.currentVersion` 在本文件里根本不存在，真源是模块级 `CURRENT_VERSION`）。

### 2026-09-20 —— 营地布局改造 + 编成排序 + ScrollList 幽灵热区 + 任务可达性 + 体积精简

- **设计（用户两条需求）**：① 营地页面**不显示备战栏**（太挡 UI 而且很乱）；② 备战栏**可选开启排序**（先做等级与稀有度，**正序反序都要**）。澄清后裁决：常驻右栏**彻底移除**、中栏加宽填满；排序**只做在编成面板的备战列**；控件 = **两个切换按钮**（`按等级 ↔ 按稀有度`、`↑正序 ↔ ↓反序`），偏好要**记住**。
- **工程（侦察先行）**：「备战栏」在代码里是**两个不同的 UI**（营地常驻右栏 vs 编成面板右列），用户那两句话有张力（「不显示」vs「可排序」）⇒ 先问再动手。几何实锤：三栏总宽 1388 > 1280 ⇒ 中栏右缘 916 压右栏 x824 **92px**，右栏列表在 depth 20 ⇒ **盖住中栏第 2 列三个按钮**、系统区右 80px、主 CTA 右 20px，**被盖处点不动**。
- **工程（营地两栏）**：新建纯几何真源 `src/config/campLayout.ts`（`CAMP_LAYOUT` + `campGridRects/campSystemRects/campCtaRect/campPanelRects`）；左 336 + 中 **880**，网格 **3 列×274**、系统区 **3 列×277**、CTA **640×72**，`CENTER_X` 改**派生** = 816；删除 `RIGHT_X/RIGHT_W/ROSTER_ROW_H` 与 `refreshRightPanel/fillRosterRow`；「待命 N 人」并入左栏既有行。选 274 而非 420 的依据：`Button` 标签居中渲染，420 塞「背包」两字太空，实测最宽标签 219/274。
- **工程（排序）**：新建纯模块 `src/utils/rosterSort.ts` 的 `sortRosterCards(cards, by, desc)`（主键 → 另一键降序 → 名字升序 → id 升序，**兜底不随 desc 翻转**）；偏好进 `maze_rpg_settings` 的 `rosterSortBy`/`rosterSortDesc`，走**独立常量 `DEFAULT_ROSTER_SORT`**（🔴 不许并进 `DEFAULT_ACCESSIBILITY`，§22.1 对它做整体 deepEqual）；`ScrollList.resetViewState(key)` 新增；排序**只在展示层**，`gameState.roster` 顺序不变。
- **工程（顺手修掉两个既有缺陷）**：① **左栏压字**——六维块 7 行压住下面「出战成员 N 人 · 科技加成属性 M 项」且分隔线横穿文字 ⇒ 第 6 项「灵性」**看不见**，改 **2 列 × 3 行**后零相交；② **玩家可见文案里漏出类名**——`data.from` 直接拼进 toast ⇒ 截图里出现「从 **WorldMapScene**返回营地」，改为显示侧收口 `campFromLabel()` + `SceneKeys.* → 人话`映射。
- **工程（只读审计扫出的 3 个布局缺陷）**：**F1** `ScrollList` 的 `GeometryMask` ⇒ **主菜单存档浮层底部那条看起来空白的横栏，点下去静默读档**（数据丢失级；实测 `(870,630/648/660)` 三点全命中 `Button[读取]`）；**F3** 同根因受害点 `ShopPanel`（面板下方 16px 带里能点到「购买」/「−」）；**F2** 营地存档浮层声明 10 槽只装得下 4 个 ⇒ 12 个按钮在**画布之外**，**槽 5–10 实际不可用**。根因：`InputManager.inputCandidate()` **不认 mask**，只看对象自身 `input.enabled`。
- **工程（F1 修法，单文件收口）**：`ScrollList` 新增 `syncRowInteractivity()` + 递归助手 `setSubtreeInputEnabled()`；判据 `inView = rowTop + rowHeight > 0 && rowTop < viewH`（**部分可见也算可点**）；用 `input.enabled` 而**不是** `setVisible(false)`（那改渲染）也**不是** `disableInteractive()`（那会清掉 hitArea）；调用于 `setScroll()`（**偏移的单点收口**）与 `refreshLayout()`。F2 改由营地存档浮层换 `ScrollList`（`rowH 96`/一屏最多 5 行/w=780 ⇒ h=608，`restoreKey: 'camp.slots'`）。
- **工程（任务清单「显示条件被判定为不可达」）**：根因两条必须分开——① `QuestPanel` 的「显示条件」是**硬编码静态常量**（文件头「全库没有 `quests.json`」那句在写下那天是真的，后来 `quests.json` 13 条 + `QuestSystem.ts` 都落地了，面板没跟着改）⇒ 5 个 `MISSING_*` 把「条件不可达」写死，**13 条里 10 条**被渲染成红字 + 不画进度条；② **三个写入点在 `src/` 里零调用方**（`recordFloor` / `recordTreasureGold` / `recordClear`）⇒ S1/S2/S9/S10 的条件在真实游玩中**永不变化**（对照：`recordBossKill`/`recordRecruit`/`recordDefeats` 早就接上了）。
- **工程（修法）**：新增可达性唯一真源 `QuestSystem.SOURCE_WIRING`（`Record<QuestCondition['kind'], {source, wiredBy, missing?}>`，**类型穷尽性由 tsc 保证**）+ `QuestSystem.progressOf(id)`（面板唯一入口）；`QuestPanel` 改**表驱动**；接线三处（`persistRun()` 里 `recordFloor`、宝箱 `goldAmount` 由 `DungeonSystem` 回传且**与 `addGold` 同一变量**、`leaveDungeon(true)` 里 `recordClear()`）。
- **工程（顺带修掉：跨图串趟）**：`persistRun()` 无条件透传 `activeRun?.runCounters`，于是**读档 → 直接进大地图 → 对另一张图选「出发」** ⇒ 新一趟的 `maxFloor` 继承上一趟 ⇒ **S1/S2 一开局显示「已达成」**，`PurchaseLimit` 同受影响。修法：`DungeonScene.create()` 里、第一次 `persistRun()` 之前加 `if (this.params.fresh) gameState.setActiveRun(null);`（**只在 fresh 时清一次**，不破坏趟内换层保留计数）。
- **工程（体积精简）**：**项目 4887.4 MB → 501.1 MB，释放 4386.3 MB**。先保证据（`smoke-out` 里 18 个有独立价值的文件 + 根目录 2 个 → 共 **20 个 / 159 KB** 复制到 **`docs/evidence/`** 并**逐个做字节数校验**）才删 8 个 `*-out` 目录（白名单硬编码、无通配）+ 根目录 **55 个**临时文件。
- **🔴 结论**：`public/assets` **没有可以无痛删的东西**——240 个立绘（40 角色 × 3 档 × PNG/WebP）全部被引用或被断言覆盖；WebP 虽然在 `src/` 里零引用且 `build-portraits.mjs` 那句「Phaser 优先加载 webp」是**过期注释**，但 `verify.mjs` 有断言「立绘资源文件齐备（**40 组 PNG 与 WebP**）」、GDD/README 也把双格式写成有意设计 ⇒ **删它会弄红自检并移除一份「资源」，属设计变更，不是清理**。
- **验证（当日三段）**：`verify.mjs` 218 → **230**（§34 营地几何 8 + §35 排序 4）→ **233**（§36 ScrollList 命中裁剪 3）→ **244**（§37 任务可达性 11）；`tsc` 0；`smoke` 三类错误全「（无）」；`vite build` exit 0（82 modules，405.26 kB / gzip 129.09 kB）。新建 `scripts/zz-scrolllist-clip.mjs`（11 条硬断言）：视口外按钮**命中清单为空数组**（修前同区域是 `Button[读取]`）、视口内按钮**真实 CDP 点击 + 回调真的触发**（`load:0` → 滚到底 `load:5` → 滚回顶 `load:0`）。`zz-quest-wiring` 15/15、`zz-quest-panel` 14/14。
- **教训**：**`Agent` 报「完成」不等于磁盘变了**（一个 worker 空转结束，报完成但 `ScrollList.ts` 一字未改）⇒ 接手前先核 mtime + grep 关键符号；**「点了没反应」是弱断言**（负面断言要用命中测试结果里不含该对象，正面才用真实点击 + 回调触发）；**幽灵样本必须「完全在视口外」且「仍在画布内」**；**子代理给的断言里可能有恒真断言**（`assert.deepEqual(f(x), f(x))` ⇒ 改成「打乱输入后输出必须一致」）；**插入相似代码后，所有 `A < B` 型顺序断言必须全部复跑**（新加的 fresh 清表把 `verify §37.8` 与 `zz-quest-wiring` 的 `③-src` 一起打红，两处都改为在 `leaveDungeon` 函数体内比对）。
- **操作教训**：PowerShell 的 `Remove-Item` 被 safe-delete 包装层接管，有**每回合 50 个条目**的批量闸且走**回收站**（成功也不释放空间）⇒ **批量删产物目录要用 Node 的 `fs.rmSync`**；`fs.rmSync` 会在最深层 Edge 缓存子树上撞真实 `EPERM` 且**一遇错就放弃整棵树**（表现为耗时极长后报错、目录却已删掉大半）⇒ 对策：`maxRetries: 12, retryDelay: 1000` → 逐个顶层孩子删 → 换个时间点重试；**千万不要用 `( cmd &)` 跑 `cmd //c rmdir /s /q`**（MSYS 路径转换会把 `/s`、`/q` 当路径吃掉，命令静默失效）。

### 2026-09-22 —— 立绘整批换图（240 个产物）+ 编号错位隐患排查 + 源图改名

- **设计（任务）**：把 `image` 的 40 张角色立绘替换为 `image2` 的对应图，沿用原有裁切管线、保留命名与目录结构。产物 **240 个** = **40 角色 × 3 档（`portrait` 256×256 / `bust` 384×512 / `full` 512×1024）× PNG/WebP**。
- **确证（匹配规则）**：**按文件名编号一一对应**（两目录各 40 张 `1.png`…`40.png`，无缺无多，各 40 个唯一哈希、无重复图），逐张并排目检 **40/40 同角色**。⚠ 自动化剪影/颜色互相关**只有 6/40 命中**，是**方法假象**（两批画幅比不同 + 新图是不同姿态的重绘）⇒ **判定同角色只能靠目检**。`image2/` 已抠图（1088×1920、RGBA、透明底硬边），**不需要再跑抠图**。
- **🔴 重大隐患（本日最大发现）：编号错位**。线上 `public/assets/portraits/full_XX.png` **逐像素精确等于** `build_full(image/M(XX).png)`，M 是一个**错位排列**（`01→33 02→15 03→32 … 40→8`）⇒ **`image/NN.png` 不是 NN 号角色的图**（`image/1.png` 是 13 号凯瑟琳，`image/15.png` 才是 2 号云漪）。三条独立证据：① M 推出的 12 组「编号←源图」对照 `characters.json` 称号/简介 **12/12 全中**；② `scripts/head-windows.mjs` 逐张注释描述的是「该**立绘编号**的角色」且与之一致；③ 旧表里唯一填了 cx 的 40 号恰是唯一一张「启用 cx 后会变」的图。**根因**：原始文件是长文件名，构建脚本用 `sorted(listdir)`（**字符串排序**）得到 M，后来被改名成 `1.png..40.png` ⇒ **今天直接重跑会把 40 个角色的立绘整体冲乱**。
- **🔴 第二个隐患：`head-windows.mjs` 的 `cx` 字段是死代码**。`cover()` 返回的图已是 256 宽 ⇒ `por.size[0] - 256 == 0` ⇒ 横向偏移被钳到 0，头像**恒为居中裁切** ⇒ 新图 11 号只剩半张脸、21 号只剩后脑勺。修法：把横向偏移算在**缩放后窗口**坐标里（`left = clamp(round(cx*nw) - tw/2, 0, nw-tw)`）。**用户已裁决批准修复**。
- **设计（用户裁决）**：cx 修复**批准启用**；落地方式 = **源目录按编号复制**（`image/NN.png ← image2/NN.png`）+ **错位关系在管线里显式声明**（新建 `scripts/portrait-sources.mjs` 的 `PORTRAIT_SOURCE`）⇒ 改动面最小、可审计、将来换第三批图可复用；08/15/31/17/35 五张「两侧横向超宽元素被居中裁切削掉外缘」**接受现状**（画幅比变化的对称性限制）；自检加固 = **补尺寸 + alpha 覆盖率断言**（不冻结画面内容）。
- **工程**：新增 `scripts/portrait-sources.mjs`（`PORTRAIT_SOURCE` = M 表，40 条）；`build-portraits.mjs` 改查表取源图 + 新增 `--src/--heads/--out/--manifest`（默认行为不变）+ 4 项守卫 + 修 `cx`；`head-windows.mjs` 换为新批次逐张重标、**按立绘编号索引**的表（6 个编号填 cx：04/07/17/26/36/38），旧表存 `.backup/head-windows.v1.mjs`；**`analyze-portraits.mjs` 拆掉 `--build` 地雷**（原按 `sorted()` 位置定号并写 `public/assets/portraits/`，跑一次会冲乱全批；现拒绝执行并指向真源），保留色彩分析。
- **工程（护栏）**：`verify.mjs` 新增 **§38**：244 → **249 项**（尺寸/色彩类型、alpha 真实存在、`PORTRAIT_SOURCE` 自洽、静态禁 `build-portraits` 用排序下标、静态禁 `analyze-portraits` 再写图片）→ **250 项**（新增 **§38.6**：断言 `PORTRAIT_SOURCE[XX]` 必须等于「portrait 字段为 XX 的那个角色的 `name` + `.png`」）。**这把「编号错位」从「能静默发生」变成「必然变红」**——当年事故缺的正是这道护栏。
- **内容（文案同步）**：逐张走查 40 条简介与新图，修订 **3 条**：`02` 云漪「以斗笠**遮去**龙角」→「斗笠**遮不住**龙角」（新图龙角明显穿出斗笠）；`09` 罗莎琳德「白裙与**黑纱**」→「白裙与**头纱**」（新图头纱为白）；`18` 灰鸦「**银白**短发」→「**黑色**短发」。diff 证实只改了 3 个 `description` 字段、其余 37 条原样未动；已重跑 `gen-tables.mjs`。
- **工程（追加任务：源图改名为角色名）**：`image/` 与 `image2/` **两个目录都改**（40 条对照，如 `33.png→塞拉菲娜.png`、`15.png→云漪.png`）；改名前后逐文件 **sha256 比对 80/80 全部一致**（只改文件名、内容零变化）；同步更新 `portrait-sources.mjs`（值改角色名 + 重写头注释，明确「编号一律查表、禁止按排序位置推断」）、`head-windows.mjs`（40 行注释改角色名、**数值一行未动**）、`README.md`、`analyze-portraits.mjs`。
- **验证（全为实跑）**：端到端复现 **240/240 逐字节相同**（含 120 个 WebP）；血缘 **40/40**；**负对照 0/40**（证明断言有区分力）；头像独立重算 40/40；`bust` ≡ `full` 取窗 40/40；产物雷同 **0 组**；`cx` **6 张真生效**（差 76–89%）、其余 34 张逐像素等于居中变体；改名后重跑管线到临时目录，240 个产物与线上**逐字节相同**（⇒ 改名对内容零影响）；`tsc` exit 0；`verify.mjs` **250 项 / 失败 0**。独立复验（quality-lead，自写脚本 + 亲自目检 40 张）：**身份 40/40 全对，判定 PASS**（附带 2 条非阻塞 CONCERNS）。
- **产物**：`docs/portrait-swap-2026-09-22.md`（交付说明 + 五层核验指南）；溯源与判定脚本在 `_portrait-swap/`（`live-source-map.mjs` / `authority-check.mjs` / `align-check.mjs` / `scan.mjs` / `match.mjs` / `overlay-old.mjs`）；美术侧 `head-windows.v2.mjs` + `preview_v2/`；改名对照完整记录在 `_portrait-swap/rename-plan-executed.json`。
- **备份**：`.backup/image-v1/`（40 个 / 59,569,389 B）、`.backup/portraits-before-swap/`（240 个 / 46,410,502 B）、`.backup/head-windows.v1.mjs`、`.backup/build-portraits.v1.mjs`、`.backup/characters.before-desc-fix.json`。
- **遗留（已由用户裁决）**：04/36 头像脸略偏左（约 0.04×图宽）⇒ **保持现状**（脸完整）；08/15/31/17/35 外缘被削 ⇒ 用户**接受现状**；`image/_work/` 与 `assets-src/matted` 是旧批次中间产物，**已过期但未删**；临时目录 `_portrait-swap/` 从 **273.4 MB 清理到 90.9 MB**（只删 4 个「重跑产物」目录，约 182 MB，均为线上产物的逐字节副本、可一条命令重建），**保留全部报告/JSON 证据/核验脚本/目检图**。
- **新发现（未处理）**：`README.md` 有 **3 处**写着「15 个角色 / 15 张立绘」（实际 40），且角色表列的是**早期版本的过时角色名单**（夏洛特 / 克拉拉 / 艾莲 …，还带一个「职业」列）⇒ 属更早版本的文档陈欠，**需单独一轮整理**（不该顺手改，否则表与正文会各自半新半旧）。
- **教训**：**像素级血缘溯源是判定「产物出自哪张源图」的唯一硬证据**（比任何相似度指标都硬）⇒ 替换类任务开工前应**先做这一步**，否则会把「编号 = 角色」当成前提、静默冲乱全批；**管线里凡靠 `sorted()` 的位置决定业务编号的写法都是隐患**（目录改名即静默失效），**业务编号必须查显式表**。

---

## 99. 附录：D 编号索引

> 本表由 `docs/handbook-parts/10-design-rules.md` **机器生成**（脚本 `_portrait-swap/handbook-tools.mjs --dindex`）。
> 用途：从源码里的 `D` 编号**反查**手册章节。规则全文见 §10；裁决原文见 `design/gdd.md`。
>
> 覆盖：**453** 个编号（最大 D456），与 `design/gdd.md` 出现过的编号**完全对齐**。

| 编号 | 所属主题组 |
|---|---|
| **D2** | §10.1 顶层定位、通关定义与交付节奏 |
| **D4** | §10.1 顶层定位、通关定义与交付节奏 |
| **D16** | §10.1 顶层定位、通关定义与交付节奏 |
| **D29** | §10.1 顶层定位、通关定义与交付节奏 |
| **D72** | §10.1 顶层定位、通关定义与交付节奏 |
| **D74** | §10.1 顶层定位、通关定义与交付节奏 |
| **D75** | §10.1 顶层定位、通关定义与交付节奏 |
| **D138** | §10.1 顶层定位、通关定义与交付节奏 |
| **D154** | §10.1 顶层定位、通关定义与交付节奏 |
| **D194** | §10.1 顶层定位、通关定义与交付节奏 |
| **D201** | §10.1 顶层定位、通关定义与交付节奏 |
| **D230** | §10.1 顶层定位、通关定义与交付节奏 |
| **D237** | §10.1 顶层定位、通关定义与交付节奏 |
| **D240** | §10.1 顶层定位、通关定义与交付节奏 |
| **D243** | §10.1 顶层定位、通关定义与交付节奏 |
| **D265** | §10.1 顶层定位、通关定义与交付节奏 |
| **D301** | §10.1 顶层定位、通关定义与交付节奏 |
| **D303** | §10.1 顶层定位、通关定义与交付节奏 |
| **D308** | §10.1 顶层定位、通关定义与交付节奏 |
| **D311** | §10.1 顶层定位、通关定义与交付节奏 |
| **D329** | §10.1 顶层定位、通关定义与交付节奏 |
| **D347** | §10.1 顶层定位、通关定义与交付节奏 |
| **D352** | §10.1 顶层定位、通关定义与交付节奏 |
| **D357** | §10.1 顶层定位、通关定义与交付节奏 |
| **D386** | §10.1 顶层定位、通关定义与交付节奏 |
| **D389** | §10.1 顶层定位、通关定义与交付节奏 |
| **D390** | §10.1 顶层定位、通关定义与交付节奏 |
| **D391** | §10.1 顶层定位、通关定义与交付节奏 |
| **D392** | §10.1 顶层定位、通关定义与交付节奏 |
| **D395** | §10.1 顶层定位、通关定义与交付节奏 |
| **D396** | §10.1 顶层定位、通关定义与交付节奏 |
| **D418** | §10.1 顶层定位、通关定义与交付节奏 |
| **D420** | §10.1 顶层定位、通关定义与交付节奏 |
| **D444** | §10.1 顶层定位、通关定义与交付节奏 |
| **D445** | §10.1 顶层定位、通关定义与交付节奏 |
| **D452** | §10.1 顶层定位、通关定义与交付节奏 |
| **D454** | §10.1 顶层定位、通关定义与交付节奏 |
| **D456** | §10.1 顶层定位、通关定义与交付节奏 |
| **D3** | §10.2 战斗结构、回合与 AP |
| **D5** | §10.2 战斗结构、回合与 AP |
| **D9** | §10.2 战斗结构、回合与 AP |
| **D12** | §10.2 战斗结构、回合与 AP |
| **D15** | §10.2 战斗结构、回合与 AP |
| **D18** | §10.2 战斗结构、回合与 AP |
| **D56** | §10.2 战斗结构、回合与 AP |
| **D59** | §10.2 战斗结构、回合与 AP |
| **D104** | §10.2 战斗结构、回合与 AP |
| **D108** | §10.2 战斗结构、回合与 AP |
| **D123** | §10.2 战斗结构、回合与 AP |
| **D124** | §10.2 战斗结构、回合与 AP |
| **D140** | §10.2 战斗结构、回合与 AP |
| **D147** | §10.2 战斗结构、回合与 AP |
| **D227** | §10.2 战斗结构、回合与 AP |
| **D229** | §10.2 战斗结构、回合与 AP |
| **D242** | §10.2 战斗结构、回合与 AP |
| **D261** | §10.2 战斗结构、回合与 AP |
| **D262** | §10.2 战斗结构、回合与 AP |
| **D268** | §10.2 战斗结构、回合与 AP |
| **D278** | §10.2 战斗结构、回合与 AP |
| **D279** | §10.2 战斗结构、回合与 AP |
| **D304** | §10.2 战斗结构、回合与 AP |
| **D354** | §10.2 战斗结构、回合与 AP |
| **D356** | §10.2 战斗结构、回合与 AP |
| **D433** | §10.2 战斗结构、回合与 AP |
| **D10** | §10.3 技能、文案与 debuff 口径 |
| **D11** | §10.3 技能、文案与 debuff 口径 |
| **D13** | §10.3 技能、文案与 debuff 口径 |
| **D55** | §10.3 技能、文案与 debuff 口径 |
| **D146** | §10.3 技能、文案与 debuff 口径 |
| **D175** | §10.3 技能、文案与 debuff 口径 |
| **D181** | §10.3 技能、文案与 debuff 口径 |
| **D182** | §10.3 技能、文案与 debuff 口径 |
| **D199** | §10.3 技能、文案与 debuff 口径 |
| **D233** | §10.3 技能、文案与 debuff 口径 |
| **D234** | §10.3 技能、文案与 debuff 口径 |
| **D235** | §10.3 技能、文案与 debuff 口径 |
| **D236** | §10.3 技能、文案与 debuff 口径 |
| **D249** | §10.3 技能、文案与 debuff 口径 |
| **D250** | §10.3 技能、文案与 debuff 口径 |
| **D251** | §10.3 技能、文案与 debuff 口径 |
| **D252** | §10.3 技能、文案与 debuff 口径 |
| **D269** | §10.3 技能、文案与 debuff 口径 |
| **D351** | §10.3 技能、文案与 debuff 口径 |
| **D428** | §10.3 技能、文案与 debuff 口径 |
| **D447** | §10.3 技能、文案与 debuff 口径 |
| **D448** | §10.3 技能、文案与 debuff 口径 |
| **D200** | §10.4 战术系统（我方 / 敌方） |
| **D205** | §10.4 战术系统（我方 / 敌方） |
| **D211** | §10.4 战术系统（我方 / 敌方） |
| **D212** | §10.4 战术系统（我方 / 敌方） |
| **D215** | §10.4 战术系统（我方 / 敌方） |
| **D216** | §10.4 战术系统（我方 / 敌方） |
| **D217** | §10.4 战术系统（我方 / 敌方） |
| **D218** | §10.4 战术系统（我方 / 敌方） |
| **D219** | §10.4 战术系统（我方 / 敌方） |
| **D220** | §10.4 战术系统（我方 / 敌方） |
| **D221** | §10.4 战术系统（我方 / 敌方） |
| **D222** | §10.4 战术系统（我方 / 敌方） |
| **D223** | §10.4 战术系统（我方 / 敌方） |
| **D226** | §10.4 战术系统（我方 / 敌方） |
| **D228** | §10.4 战术系统（我方 / 敌方） |
| **D231** | §10.4 战术系统（我方 / 敌方） |
| **D232** | §10.4 战术系统（我方 / 敌方） |
| **D241** | §10.4 战术系统（我方 / 敌方） |
| **D259** | §10.4 战术系统（我方 / 敌方） |
| **D260** | §10.4 战术系统（我方 / 敌方） |
| **D263** | §10.4 战术系统（我方 / 敌方） |
| **D277** | §10.4 战术系统（我方 / 敌方） |
| **D317** | §10.4 战术系统（我方 / 敌方） |
| **D326** | §10.4 战术系统（我方 / 敌方） |
| **D328** | §10.4 战术系统（我方 / 敌方） |
| **D332** | §10.4 战术系统（我方 / 敌方） |
| **D337** | §10.4 战术系统（我方 / 敌方） |
| **D340** | §10.4 战术系统（我方 / 敌方） |
| **D341** | §10.4 战术系统（我方 / 敌方） |
| **D342** | §10.4 战术系统（我方 / 敌方） |
| **D344** | §10.4 战术系统（我方 / 敌方） |
| **D345** | §10.4 战术系统（我方 / 敌方） |
| **D346** | §10.4 战术系统（我方 / 敌方） |
| **D366** | §10.4 战术系统（我方 / 敌方） |
| **D383** | §10.4 战术系统（我方 / 敌方） |
| **D425** | §10.4 战术系统（我方 / 敌方） |
| **D437** | §10.4 战术系统（我方 / 敌方） |
| **D438** | §10.4 战术系统（我方 / 敌方） |
| **D439** | §10.4 战术系统（我方 / 敌方） |
| **D440** | §10.4 战术系统（我方 / 敌方） |
| **D446** | §10.4 战术系统（我方 / 敌方） |
| **D19** | §10.5 数值、成长曲线与伤害上界 |
| **D28** | §10.5 数值、成长曲线与伤害上界 |
| **D32** | §10.5 数值、成长曲线与伤害上界 |
| **D33** | §10.5 数值、成长曲线与伤害上界 |
| **D34** | §10.5 数值、成长曲线与伤害上界 |
| **D35** | §10.5 数值、成长曲线与伤害上界 |
| **D99** | §10.5 数值、成长曲线与伤害上界 |
| **D183** | §10.5 数值、成长曲线与伤害上界 |
| **D197** | §10.5 数值、成长曲线与伤害上界 |
| **D203** | §10.5 数值、成长曲线与伤害上界 |
| **D225** | §10.5 数值、成长曲线与伤害上界 |
| **D256** | §10.5 数值、成长曲线与伤害上界 |
| **D257** | §10.5 数值、成长曲线与伤害上界 |
| **D327** | §10.5 数值、成长曲线与伤害上界 |
| **D355** | §10.5 数值、成长曲线与伤害上界 |
| **D374** | §10.5 数值、成长曲线与伤害上界 |
| **D402** | §10.5 数值、成长曲线与伤害上界 |
| **D20** | §10.6 经济：金币、科技点、招募与训练场 |
| **D23** | §10.6 经济：金币、科技点、招募与训练场 |
| **D41** | §10.6 经济：金币、科技点、招募与训练场 |
| **D64** | §10.6 经济：金币、科技点、招募与训练场 |
| **D65** | §10.6 经济：金币、科技点、招募与训练场 |
| **D66** | §10.6 经济：金币、科技点、招募与训练场 |
| **D67** | §10.6 经济：金币、科技点、招募与训练场 |
| **D92** | §10.6 经济：金币、科技点、招募与训练场 |
| **D107** | §10.6 经济：金币、科技点、招募与训练场 |
| **D110** | §10.6 经济：金币、科技点、招募与训练场 |
| **D112** | §10.6 经济：金币、科技点、招募与训练场 |
| **D116** | §10.6 经济：金币、科技点、招募与训练场 |
| **D120** | §10.6 经济：金币、科技点、招募与训练场 |
| **D143** | §10.6 经济：金币、科技点、招募与训练场 |
| **D144** | §10.6 经济：金币、科技点、招募与训练场 |
| **D156** | §10.6 经济：金币、科技点、招募与训练场 |
| **D202** | §10.6 经济：金币、科技点、招募与训练场 |
| **D204** | §10.6 经济：金币、科技点、招募与训练场 |
| **D245** | §10.6 经济：金币、科技点、招募与训练场 |
| **D246** | §10.6 经济：金币、科技点、招募与训练场 |
| **D253** | §10.6 经济：金币、科技点、招募与训练场 |
| **D254** | §10.6 经济：金币、科技点、招募与训练场 |
| **D255** | §10.6 经济：金币、科技点、招募与训练场 |
| **D264** | §10.6 经济：金币、科技点、招募与训练场 |
| **D271** | §10.6 经济：金币、科技点、招募与训练场 |
| **D284** | §10.6 经济：金币、科技点、招募与训练场 |
| **D297** | §10.6 经济：金币、科技点、招募与训练场 |
| **D300** | §10.6 经济：金币、科技点、招募与训练场 |
| **D305** | §10.6 经济：金币、科技点、招募与训练场 |
| **D306** | §10.6 经济：金币、科技点、招募与训练场 |
| **D310** | §10.6 经济：金币、科技点、招募与训练场 |
| **D335** | §10.6 经济：金币、科技点、招募与训练场 |
| **D363** | §10.6 经济：金币、科技点、招募与训练场 |
| **D371** | §10.6 经济：金币、科技点、招募与训练场 |
| **D372** | §10.6 经济：金币、科技点、招募与训练场 |
| **D375** | §10.6 经济：金币、科技点、招募与训练场 |
| **D377** | §10.6 经济：金币、科技点、招募与训练场 |
| **D401** | §10.6 经济：金币、科技点、招募与训练场 |
| **D403** | §10.6 经济：金币、科技点、招募与训练场 |
| **D404** | §10.6 经济：金币、科技点、招募与训练场 |
| **D54** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D77** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D78** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D80** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D81** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D82** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D83** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D84** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D85** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D86** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D88** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D90** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D94** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D95** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D113** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D122** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D126** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D128** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D129** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D133** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D141** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D150** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D158** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D167** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D171** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D184** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D187** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D188** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D193** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D195** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D196** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D198** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D208** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D210** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D270** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D282** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D292** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D312** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D319** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D393** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D394** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D429** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D430** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D431** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D432** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D442** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D443** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D449** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D450** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D451** | §10.7 商店、装备、饰品、锻造与消耗品 |
| **D6** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D7** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D14** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D40** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D48** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D49** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D50** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D51** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D52** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D57** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D76** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D87** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D89** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D93** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D114** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D118** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D119** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D121** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D125** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D130** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D148** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D149** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D151** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D152** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D153** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D157** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D159** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D162** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D163** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D165** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D166** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D169** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D173** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D174** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D176** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D177** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D178** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D186** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D190** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D206** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D258** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D266** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D273** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D274** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D290** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D291** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D298** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D299** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D325** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D331** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D343** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D384** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D407** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D408** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D411** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D412** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D413** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D414** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D415** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D416** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D417** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D419** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D424** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D426** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D427** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D434** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D441** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D453** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D455** | §10.8 敌人、Boss、精英、虚影与族群 |
| **D1** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D8** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D17** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D21** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D22** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D53** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D58** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D60** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D61** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D62** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D63** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D73** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D79** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D109** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D111** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D115** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D127** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D131** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D132** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D136** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D142** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D145** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D160** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D161** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D164** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D168** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D170** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D172** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D179** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D185** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D189** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D191** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D224** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D247** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D289** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D321** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D322** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D323** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D349** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D350** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D373** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D376** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D378** | §10.9 迷宫、难度档、生命池与陷阱 |
| **D192** | §10.10 事件与探索辅助 |
| **D207** | §10.10 事件与探索辅助 |
| **D209** | §10.10 事件与探索辅助 |
| **D320** | §10.10 事件与探索辅助 |
| **D324** | §10.10 事件与探索辅助 |
| **D333** | §10.10 事件与探索辅助 |
| **D336** | §10.10 事件与探索辅助 |
| **D423** | §10.10 事件与探索辅助 |
| **D38** | §10.11 任务与新手引导 |
| **D43** | §10.11 任务与新手引导 |
| **D91** | §10.11 任务与新手引导 |
| **D98** | §10.11 任务与新手引导 |
| **D238** | §10.11 任务与新手引导 |
| **D239** | §10.11 任务与新手引导 |
| **D248** | §10.11 任务与新手引导 |
| **D283** | §10.11 任务与新手引导 |
| **D286** | §10.11 任务与新手引导 |
| **D294** | §10.11 任务与新手引导 |
| **D302** | §10.11 任务与新手引导 |
| **D307** | §10.11 任务与新手引导 |
| **D309** | §10.11 任务与新手引导 |
| **D318** | §10.11 任务与新手引导 |
| **D330** | §10.11 任务与新手引导 |
| **D361** | §10.11 任务与新手引导 |
| **D364** | §10.11 任务与新手引导 |
| **D367** | §10.11 任务与新手引导 |
| **D381** | §10.11 任务与新手引导 |
| **D422** | §10.11 任务与新手引导 |
| **D46** | §10.12 存档、统计与设置 |
| **D47** | §10.12 存档、统计与设置 |
| **D68** | §10.12 存档、统计与设置 |
| **D69** | §10.12 存档、统计与设置 |
| **D70** | §10.12 存档、统计与设置 |
| **D71** | §10.12 存档、统计与设置 |
| **D105** | §10.12 存档、统计与设置 |
| **D134** | §10.12 存档、统计与设置 |
| **D135** | §10.12 存档、统计与设置 |
| **D137** | §10.12 存档、统计与设置 |
| **D267** | §10.12 存档、统计与设置 |
| **D275** | §10.12 存档、统计与设置 |
| **D276** | §10.12 存档、统计与设置 |
| **D281** | §10.12 存档、统计与设置 |
| **D285** | §10.12 存档、统计与设置 |
| **D287** | §10.12 存档、统计与设置 |
| **D293** | §10.12 存档、统计与设置 |
| **D295** | §10.12 存档、统计与设置 |
| **D296** | §10.12 存档、统计与设置 |
| **D313** | §10.12 存档、统计与设置 |
| **D314** | §10.12 存档、统计与设置 |
| **D315** | §10.12 存档、统计与设置 |
| **D316** | §10.12 存档、统计与设置 |
| **D334** | §10.12 存档、统计与设置 |
| **D338** | §10.12 存档、统计与设置 |
| **D339** | §10.12 存档、统计与设置 |
| **D353** | §10.12 存档、统计与设置 |
| **D358** | §10.12 存档、统计与设置 |
| **D359** | §10.12 存档、统计与设置 |
| **D360** | §10.12 存档、统计与设置 |
| **D362** | §10.12 存档、统计与设置 |
| **D365** | §10.12 存档、统计与设置 |
| **D368** | §10.12 存档、统计与设置 |
| **D369** | §10.12 存档、统计与设置 |
| **D370** | §10.12 存档、统计与设置 |
| **D379** | §10.12 存档、统计与设置 |
| **D380** | §10.12 存档、统计与设置 |
| **D385** | §10.12 存档、统计与设置 |
| **D388** | §10.12 存档、统计与设置 |
| **D397** | §10.12 存档、统计与设置 |
| **D398** | §10.12 存档、统计与设置 |
| **D399** | §10.12 存档、统计与设置 |
| **D400** | §10.12 存档、统计与设置 |
| **D421** | §10.12 存档、统计与设置 |
| **D24** | §10.13 UI、美术与可访问性 |
| **D25** | §10.13 UI、美术与可访问性 |
| **D26** | §10.13 UI、美术与可访问性 |
| **D27** | §10.13 UI、美术与可访问性 |
| **D30** | §10.13 UI、美术与可访问性 |
| **D31** | §10.13 UI、美术与可访问性 |
| **D36** | §10.13 UI、美术与可访问性 |
| **D37** | §10.13 UI、美术与可访问性 |
| **D39** | §10.13 UI、美术与可访问性 |
| **D96** | §10.13 UI、美术与可访问性 |
| **D97** | §10.13 UI、美术与可访问性 |
| **D100** | §10.13 UI、美术与可访问性 |
| **D101** | §10.13 UI、美术与可访问性 |
| **D102** | §10.13 UI、美术与可访问性 |
| **D103** | §10.13 UI、美术与可访问性 |
| **D106** | §10.13 UI、美术与可访问性 |
| **D117** | §10.13 UI、美术与可访问性 |
| **D139** | §10.13 UI、美术与可访问性 |
| **D280** | §10.13 UI、美术与可访问性 |
| **D288** | §10.13 UI、美术与可访问性 |
| **D405** | §10.13 UI、美术与可访问性 |
| **D406** | §10.13 UI、美术与可访问性 |
| **D42** | §10.14 工程纪律、代码缺口与环境坑 |
| **D44** | §10.14 工程纪律、代码缺口与环境坑 |
| **D45** | §10.14 工程纪律、代码缺口与环境坑 |
| **D155** | §10.14 工程纪律、代码缺口与环境坑 |
| **D180** | §10.14 工程纪律、代码缺口与环境坑 |
| **D244** | §10.14 工程纪律、代码缺口与环境坑 |
| **D272** | §10.14 工程纪律、代码缺口与环境坑 |
| **D382** | §10.14 工程纪律、代码缺口与环境坑 |
| **D387** | §10.14 工程纪律、代码缺口与环境坑 |
| **D409** | §10.14 工程纪律、代码缺口与环境坑 |
| **D410** | §10.14 工程纪律、代码缺口与环境坑 |
| **D435** | §10.14 工程纪律、代码缺口与环境坑 |
| **D436** | §10.14 工程纪律、代码缺口与环境坑 |
